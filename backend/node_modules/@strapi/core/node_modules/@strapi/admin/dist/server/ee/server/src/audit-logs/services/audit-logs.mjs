const getSanitizedUser = (user)=>{
    let displayName = user.email;
    if (user.username) {
        displayName = user.username;
    } else if (user.firstname && user.lastname) {
        displayName = `${user.firstname} ${user.lastname}`;
    }
    return {
        id: user.id,
        email: user.email,
        displayName
    };
};
/**
 * @description
 * Manages audit logs interaction with the database. Accessible via strapi.get('audit-logs')
 */ const createAuditLogsService = (strapi)=>{
    return {
        async saveEvent (event) {
            const { userId, ...rest } = event;
            const auditLog = {
                ...rest,
                user: userId
            };
            // Save to database
            await strapi.db.query('admin::audit-log').create({
                data: auditLog
            });
            return this;
        },
        async findMany (query) {
            // NOTE: We get the IDs first because sorting full rows runs MySQL/MariaDB out of sort memory
            // See: https://github.com/strapi/strapi/issues/27399
            const { results: logRows, pagination } = await strapi.db.query('admin::audit-log').findPage({
                ...strapi.get('query-params').transform('admin::audit-log', query),
                select: [
                    'id'
                ]
            });
            const ids = logRows.map((log)=>log.id);
            const logs = ids.length ? await strapi.db.query('admin::audit-log').findMany({
                where: {
                    id: {
                        $in: ids
                    }
                },
                populate: [
                    'user'
                ],
                select: [
                    'action',
                    'date',
                    'payload'
                ]
            }) : [];
            const logsById = new Map(logs.map((log)=>[
                    log.id,
                    log
                ]));
            const results = ids.map((id)=>logsById.get(id)).filter(Boolean);
            const sanitizedResults = results.map((result)=>{
                const { user, ...rest } = result;
                return {
                    ...rest,
                    user: user ? getSanitizedUser(user) : null
                };
            });
            return {
                results: sanitizedResults,
                pagination
            };
        },
        async findManyUsers (query) {
            const { page = 1, pageSize = 10 } = query;
            const userAttribute = strapi.db.metadata.get('admin::audit-log').attributes.user;
            if (userAttribute?.type !== 'relation' || !('joinTable' in userAttribute) || !userAttribute.joinTable || !('inverseJoinColumn' in userAttribute.joinTable)) {
                throw new Error('The audit log user relation is expected to use a join table');
            }
            const { joinTable } = userAttribute;
            const authorIds = await strapi.db.connection(joinTable.name).distinct(joinTable.inverseJoinColumn.name).pluck(joinTable.inverseJoinColumn.name);
            const { results, pagination } = await strapi.db.query('admin::user').findPage({
                select: [
                    'id',
                    'email',
                    'username',
                    'firstname',
                    'lastname'
                ],
                where: {
                    id: {
                        $in: authorIds
                    }
                },
                orderBy: {
                    id: 'asc'
                },
                page: Number(page),
                pageSize: Number(pageSize)
            });
            return {
                results: results.map(getSanitizedUser),
                pagination
            };
        },
        async findOne (id) {
            const result = await strapi.db.query('admin::audit-log').findOne({
                where: {
                    id
                },
                populate: [
                    'user'
                ],
                select: [
                    'action',
                    'date',
                    'payload'
                ]
            });
            if (!result) {
                return null;
            }
            const { user, ...rest } = result;
            return {
                ...rest,
                user: user ? getSanitizedUser(user) : null
            };
        },
        deleteExpiredEvents (expirationDate) {
            return strapi.db.query('admin::audit-log').deleteMany({
                where: {
                    date: {
                        $lt: expirationDate.toISOString()
                    }
                }
            });
        }
    };
};

export { createAuditLogsService };
//# sourceMappingURL=audit-logs.mjs.map
