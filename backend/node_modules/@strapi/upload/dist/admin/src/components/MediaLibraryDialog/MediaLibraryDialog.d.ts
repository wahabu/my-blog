import type { File } from '../../../../shared/contracts/files';
import type { AllowedTypes } from '../AssetCard/AssetCard';
export interface MediaLibraryDialogProps {
    allowedTypes?: AllowedTypes[];
    multiple?: boolean;
    initiallySelectedAssets?: File[];
    onClose: () => void;
    onSelectAssets: (selectedAssets: File[]) => void;
}
export declare const MediaLibraryDialog: ({ onClose, onSelectAssets, allowedTypes, multiple, initiallySelectedAssets, }: MediaLibraryDialogProps) => import("react/jsx-runtime").JSX.Element;
