'use strict';

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var strapiAdmin = require('@strapi/admin/strapi-admin');
var designSystem = require('@strapi/design-system');
var reactIntl = require('react-intl');
var styledComponents = require('styled-components');
var useTracking = require('../../../hooks/useTracking.js');
var folders = require('../../../services/folders.js');
var translations = require('../../../utils/translations.js');

const StyledModalContent = styledComponents.styled(designSystem.Modal.Content)`
  max-width: 51.6rem;
`;
const FolderFormDialog = (props)=>{
    const { open, parentFolderId, onClose, mode } = props;
    const initialName = props.mode === 'rename' ? props.initialName : '';
    const { formatMessage } = reactIntl.useIntl();
    const { toggleNotification } = strapiAdmin.useNotification();
    const { trackUsage } = useTracking.useTracking();
    const [name, setName] = React.useState(initialName);
    const [fieldError, setFieldError] = React.useState();
    const inputRef = React.useRef(null);
    const [createFolder, { isLoading: isCreating }] = folders.useCreateFolderMutation();
    const [updateFolder, { isLoading: isUpdating }] = folders.useUpdateFolderMutation();
    const isLoading = mode === 'rename' ? isUpdating : isCreating;
    React.useEffect(()=>{
        if (open) {
            setName(initialName);
            setFieldError(undefined);
            // Renaming starts from the current name, so select it: typing replaces
            // the whole thing, as in Finder / VS Code / GitHub.
            if (mode === 'rename') {
                inputRef.current?.select();
            }
        }
    }, [
        open,
        initialName,
        mode
    ]);
    const handleSubmit = async (e)=>{
        e.preventDefault();
        const trimmedName = name.trim();
        if (!trimmedName) {
            setFieldError(formatMessage({
                id: translations.getTranslationKey('folder.create.form.error.name-required'),
                defaultMessage: 'Name is required'
            }));
            return;
        }
        try {
            if (props.mode === 'rename') {
                await updateFolder({
                    id: props.folderId,
                    name: trimmedName,
                    parent: parentFolderId
                }).unwrap();
                // Matches the legacy folder-edit payload. This dialog only renames
                // (parent is fixed), so the location never changes here.
                trackUsage('didEditMediaLibraryElements', {
                    location: useTracking.MEDIA_LIBRARY_LOCATION,
                    type: 'folder',
                    changeLocation: false
                });
            } else {
                await createFolder({
                    name: trimmedName,
                    parent: parentFolderId
                }).unwrap();
                trackUsage('didAddMediaLibraryFolders', {
                    location: useTracking.MEDIA_LIBRARY_LOCATION
                });
            }
            toggleNotification({
                type: 'success',
                message: mode === 'rename' ? formatMessage({
                    id: translations.getTranslationKey('folder.rename.success'),
                    defaultMessage: 'Folder has been renamed'
                }) : formatMessage({
                    id: translations.getTranslationKey('folder.create.success'),
                    defaultMessage: 'Folder has been created'
                })
            });
            onClose();
        } catch (err) {
            const apiError = err;
            if (apiError?.message) {
                setFieldError(apiError.message);
            } else {
                toggleNotification({
                    type: 'danger',
                    message: mode === 'rename' ? formatMessage({
                        id: translations.getTranslationKey('folder.rename.form.error.unknown'),
                        defaultMessage: 'An error occurred while renaming the folder'
                    }) : formatMessage({
                        id: translations.getTranslationKey('folder.create.form.error.unknown'),
                        defaultMessage: 'An error occurred while creating the folder'
                    })
                });
            }
        }
    };
    return /*#__PURE__*/ jsxRuntime.jsx(designSystem.Modal.Root, {
        open: open,
        onOpenChange: onClose,
        children: /*#__PURE__*/ jsxRuntime.jsxs(StyledModalContent, {
            children: [
                /*#__PURE__*/ jsxRuntime.jsx(designSystem.Modal.Header, {
                    children: /*#__PURE__*/ jsxRuntime.jsx(designSystem.Modal.Title, {
                        children: props.mode === 'rename' ? formatMessage({
                            id: translations.getTranslationKey('folder.rename.title'),
                            defaultMessage: 'Rename folder'
                        }) : formatMessage({
                            id: translations.getTranslationKey('folder.create.title-in'),
                            defaultMessage: 'New folder in {folderName}'
                        }, {
                            folderName: props.parentFolderName
                        })
                    })
                }),
                /*#__PURE__*/ jsxRuntime.jsxs("form", {
                    onSubmit: handleSubmit,
                    children: [
                        /*#__PURE__*/ jsxRuntime.jsx(designSystem.Modal.Body, {
                            children: /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Field.Root, {
                                error: fieldError,
                                name: "name",
                                required: true,
                                children: [
                                    /*#__PURE__*/ jsxRuntime.jsx(designSystem.Field.Label, {
                                        children: formatMessage({
                                            id: translations.getTranslationKey('folder.form.name.label'),
                                            defaultMessage: 'Folder name'
                                        })
                                    }),
                                    /*#__PURE__*/ jsxRuntime.jsx(designSystem.TextInput, {
                                        ref: inputRef,
                                        value: name,
                                        onChange: (e)=>{
                                            setName(e.target.value);
                                            setFieldError(undefined);
                                        },
                                        autoFocus: true
                                    }),
                                    /*#__PURE__*/ jsxRuntime.jsx(designSystem.Field.Error, {})
                                ]
                            })
                        }),
                        /*#__PURE__*/ jsxRuntime.jsx(designSystem.Modal.Footer, {
                            children: /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Flex, {
                                gap: 2,
                                justifyContent: "space-between",
                                width: "100%",
                                children: [
                                    /*#__PURE__*/ jsxRuntime.jsx(designSystem.Button, {
                                        variant: "tertiary",
                                        onClick: onClose,
                                        type: "button",
                                        children: formatMessage({
                                            id: 'app.components.Button.cancel',
                                            defaultMessage: 'Cancel'
                                        })
                                    }),
                                    /*#__PURE__*/ jsxRuntime.jsx(designSystem.Button, {
                                        type: "submit",
                                        loading: isLoading,
                                        disabled: mode === 'rename' && name.trim() === initialName.trim(),
                                        children: mode === 'rename' ? formatMessage({
                                            id: translations.getTranslationKey('folder.rename.submit'),
                                            defaultMessage: 'Save'
                                        }) : formatMessage({
                                            id: translations.getTranslationKey('folder.create.submit'),
                                            defaultMessage: 'Create folder'
                                        })
                                    })
                                ]
                            })
                        })
                    ]
                })
            ]
        })
    });
};

exports.FolderFormDialog = FolderFormDialog;
//# sourceMappingURL=FolderFormDialog.js.map
