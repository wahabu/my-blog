'use strict';

var enums = require('../enums.js');

const MIME_TYPE_MAP = {
    image: enums.ASSET_TYPES.Image,
    video: enums.ASSET_TYPES.Video,
    audio: enums.ASSET_TYPES.Audio,
    pdf: enums.DOC_TYPES.Pdf,
    csv: enums.DOC_TYPES.Csv,
    // For XLS files the mime is application/vnd.ms-excel so we need to check for 'excel' not 'xls'
    excel: enums.DOC_TYPES.Xls,
    zip: enums.DOC_TYPES.Zip
};
const MIME_TYPE_KEYS = Object.keys(MIME_TYPE_MAP);
const typeFromMime = (mime)=>{
    const mimeTypeKey = MIME_TYPE_KEYS.find((m)=>mime.toLowerCase().includes(m));
    if (mimeTypeKey === undefined) return enums.ASSET_TYPES.Document;
    return MIME_TYPE_MAP[mimeTypeKey] ?? enums.ASSET_TYPES.Document;
};

exports.typeFromMime = typeFromMime;
//# sourceMappingURL=typeFromMime.js.map
