import {
  contentManagerApi
} from "./chunk-CB2UCVBY.js";
import {
  createRulesEngine
} from "./chunk-SG3YXALT.js";
import {
  errorsTrads
} from "./chunk-IFOFBKTA.js";
import {
  getYupValidationErrors,
  useForm
} from "./chunk-2IWTCEYS.js";
import {
  useAPIErrorHandler
} from "./chunk-WZNNPD7U.js";
import {
  useQueryParams,
  useStrapiApp
} from "./chunk-HHY6TNZ5.js";
import {
  useNotification
} from "./chunk-52FV5OAT.js";
import {
  ValidationError,
  create2 as create,
  create3 as create2,
  create4 as create3,
  create5 as create4,
  create6 as create5,
  create7 as create6,
  create8 as create7
} from "./chunk-OG6FDUVQ.js";
import {
  generateNKeysBetween
} from "./chunk-3DRZIGZ7.js";
import {
  require_lib,
  require_pipe
} from "./chunk-NZZAX2AB.js";
import {
  require_clone,
  require_toPath
} from "./chunk-NR6XEPE4.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import {
  useParams
} from "./chunk-QBM2MGBP.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/content-manager/dist/admin/constants/collections.mjs
var SINGLE_TYPES = "single-types";
var COLLECTION_TYPES = "collection-types";

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/utils/data.mjs
var import_pipe = __toESM(require_pipe(), 1);

// node_modules/@strapi/content-manager/dist/admin/constants/attributes.mjs
var ID = "id";
var CREATED_BY_ATTRIBUTE_NAME = "createdBy";
var UPDATED_BY_ATTRIBUTE_NAME = "updatedBy";
var CREATOR_FIELDS = [
  CREATED_BY_ATTRIBUTE_NAME,
  UPDATED_BY_ATTRIBUTE_NAME
];
var PUBLISHED_BY_ATTRIBUTE_NAME = "publishedBy";
var CREATED_AT_ATTRIBUTE_NAME = "createdAt";
var UPDATED_AT_ATTRIBUTE_NAME = "updatedAt";
var PUBLISHED_AT_ATTRIBUTE_NAME = "publishedAt";
var DOCUMENT_META_FIELDS = [
  ID,
  ...CREATOR_FIELDS,
  PUBLISHED_BY_ATTRIBUTE_NAME,
  CREATED_AT_ATTRIBUTE_NAME,
  UPDATED_AT_ATTRIBUTE_NAME,
  PUBLISHED_AT_ATTRIBUTE_NAME
];
var ATTRIBUTE_TYPES_THAT_CANNOT_BE_MAIN_FIELD = [
  "dynamiczone",
  "json",
  "text",
  "relation",
  "component",
  "boolean",
  "media",
  "password",
  "richtext",
  "timestamp",
  "blocks"
];

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/utils/data.mjs
var BLOCK_LIST_ATTRIBUTE_KEYS = [
  "__component",
  "__temp_key__"
];
var getDirectParent = (data, path) => {
  if (!path) return void 0;
  const isNumericIndex = (value) => /^\d+$/.test(value);
  const segments = path.split(".");
  const parentPath = segments.slice(0, -1);
  let current = data;
  for (const segment of parentPath) {
    if (current == null) return void 0;
    if (isNumericIndex(segment)) {
      if (!Array.isArray(current)) return void 0;
      current = current[Number(segment)];
      continue;
    }
    if (typeof current !== "object") return void 0;
    current = current[segment];
  }
  return current;
};
var traverseData = (predicate, transform) => (schema, components = {}) => (data = {}) => {
  const traverse = (datum, attributes) => {
    return Object.entries(datum).reduce((acc, [key, value]) => {
      var _a;
      const attribute = attributes[key];
      if (BLOCK_LIST_ATTRIBUTE_KEYS.includes(key) || value === null || value === void 0) {
        acc[key] = value;
        return acc;
      }
      if (!attribute) {
        acc[key] = value;
        return acc;
      }
      if (attribute.type === "component") {
        if (attribute.repeatable) {
          const componentValue = predicate(attribute, value) ? transform(value, attribute) : value;
          acc[key] = componentValue.map((componentData) => {
            var _a2;
            return traverse(componentData, ((_a2 = components[attribute.component]) == null ? void 0 : _a2.attributes) ?? {});
          });
        } else {
          const componentValue = predicate(attribute, value) ? transform(value, attribute) : value;
          acc[key] = traverse(componentValue, ((_a = components[attribute.component]) == null ? void 0 : _a.attributes) ?? {});
        }
      } else if (attribute.type === "dynamiczone") {
        const dynamicZoneValue = predicate(attribute, value) ? transform(value, attribute) : value;
        acc[key] = dynamicZoneValue.map((componentData) => {
          var _a2;
          return traverse(componentData, ((_a2 = components[componentData.__component]) == null ? void 0 : _a2.attributes) ?? {});
        });
      } else if (predicate(attribute, value)) {
        acc[key] = transform(value, attribute);
      } else {
        acc[key] = value;
      }
      return acc;
    }, {});
  };
  return traverse(data, schema.attributes);
};
var removeProhibitedFields = (prohibitedFields) => traverseData((attribute) => prohibitedFields.includes(attribute.type), () => "");
var prepareRelations = traverseData((attribute) => attribute.type === "relation", () => ({
  connect: [],
  disconnect: []
}));
var prepareTempKeys = traverseData((attribute) => attribute.type === "component" && attribute.repeatable || attribute.type === "dynamiczone", (data) => {
  if (Array.isArray(data) && data.length > 0) {
    const keys = generateNKeysBetween(void 0, void 0, data.length);
    return data.map((datum, index) => ({
      ...datum,
      __temp_key__: keys[index]
    }));
  }
  return data;
});
var removeFieldsThatDontExistOnSchema = (schema) => (data) => {
  const schemaKeys = Object.keys(schema.attributes);
  const dataKeys = Object.keys(data);
  const keysToRemove = dataKeys.filter((key) => !schemaKeys.includes(key));
  const revisedData = [
    ...keysToRemove,
    ...DOCUMENT_META_FIELDS
  ].reduce((acc, key) => {
    delete acc[key];
    return acc;
  }, structuredClone(data));
  return revisedData;
};
var removeNullValues = (schema, components = {}) => traverseData((attribute, value) => value === null && attribute.type !== "boolean", () => void 0)(schema, components);
var transformDocument = (schema, components = {}) => (document) => {
  const transformations = (0, import_pipe.default)(removeFieldsThatDontExistOnSchema(schema), removeProhibitedFields([
    "password"
  ])(schema, components), removeNullValues(schema, components), prepareRelations(schema, components), prepareTempKeys(schema, components));
  return transformations(document);
};
var getItemInitialValue = (initialValue, item) => {
  if (initialValue && Array.isArray(initialValue)) {
    const matchingInitialItem = initialValue.find((initialItem) => initialItem.__temp_key__ === item.__temp_key__);
    if (matchingInitialItem) {
      return matchingInitialItem;
    }
  }
  return {};
};
var collectInvisibleAttributes = (data, schema, components, path = []) => {
  var _a;
  if (!(schema == null ? void 0 : schema.attributes)) return [];
  const rulesEngine = createRulesEngine();
  const removedPaths = [];
  const evaluatedData = {};
  for (const [attrName, attrDef] of Object.entries(schema.attributes)) {
    const fullPath = [
      ...path,
      attrName
    ].join(".");
    if ("visible" in attrDef && attrDef.visible === false) {
      continue;
    }
    const condition = (_a = attrDef == null ? void 0 : attrDef.conditions) == null ? void 0 : _a.visible;
    const isVisible = condition ? rulesEngine.evaluate(condition, {
      ...data,
      ...evaluatedData
    }) : true;
    if (!isVisible) {
      removedPaths.push(fullPath);
      continue;
    }
    if (attrName in data) {
      evaluatedData[attrName] = data[attrName];
    }
    if (attrDef.type === "component") {
      const compSchema = components[attrDef.component];
      const value = data[attrName];
      if (attrDef.repeatable && Array.isArray(value)) {
        value.forEach((item) => {
          const nestedPaths = collectInvisibleAttributes(item, compSchema, components, [
            ...path,
            `${attrName}[${item.__temp_key__}]`
          ]);
          removedPaths.push(...nestedPaths);
        });
      } else if (value && typeof value === "object") {
        const nestedPaths = collectInvisibleAttributes(value, compSchema, components, [
          ...path,
          attrName
        ]);
        removedPaths.push(...nestedPaths);
      }
    }
    if (attrDef.type === "dynamiczone" && Array.isArray(data[attrName])) {
      data[attrName].forEach((dzItem) => {
        const compUID = dzItem == null ? void 0 : dzItem.__component;
        const compSchema = components[compUID];
        const nestedPaths = collectInvisibleAttributes(dzItem, compSchema, components, [
          ...path,
          `${attrName}[${dzItem.__temp_key__}]`
        ]);
        removedPaths.push(...nestedPaths);
      });
    }
  }
  return removedPaths;
};
var filterDataByRemovedPaths = (data, initialValues, schema, components, removedPaths, currentPath = []) => {
  if (!(schema == null ? void 0 : schema.attributes)) return data;
  const result = {};
  for (const [attrName, attrDef] of Object.entries(schema.attributes)) {
    const fullPath = [
      ...currentPath,
      attrName
    ].join(".");
    if (removedPaths.includes(fullPath)) {
      continue;
    }
    if ("visible" in attrDef && attrDef.visible === false) {
      const userProvided2 = Object.hasOwn(data, attrName);
      if (userProvided2) {
        result[attrName] = data[attrName];
      } else if (attrName in initialValues) {
        result[attrName] = initialValues[attrName];
      }
      continue;
    }
    const userProvided = Object.hasOwn(data, attrName);
    const currentValue = userProvided ? data[attrName] : void 0;
    const initialValue = initialValues == null ? void 0 : initialValues[attrName];
    if (attrDef.type === "component") {
      const compSchema = components[attrDef.component];
      const value = currentValue === void 0 ? initialValue : currentValue;
      if (!value) {
        result[attrName] = attrDef.repeatable ? [] : null;
        continue;
      }
      if (attrDef.repeatable && Array.isArray(value)) {
        result[attrName] = value.map((item) => {
          const componentInitialValue = getItemInitialValue(initialValue, item);
          return filterDataByRemovedPaths(item, componentInitialValue, compSchema, components, removedPaths, [
            ...currentPath,
            `${attrName}[${item.__temp_key__}]`
          ]);
        });
      } else {
        result[attrName] = filterDataByRemovedPaths(value, initialValue ?? {}, compSchema, components, removedPaths, [
          ...currentPath,
          attrName
        ]);
      }
      continue;
    }
    if (attrDef.type === "dynamiczone") {
      if (!Array.isArray(currentValue)) {
        result[attrName] = [];
        continue;
      }
      result[attrName] = currentValue.map((dzItem) => {
        const compUID = dzItem == null ? void 0 : dzItem.__component;
        const compSchema = components[compUID];
        const componentInitialValue = getItemInitialValue(initialValue, dzItem);
        const cleaned = filterDataByRemovedPaths(dzItem, componentInitialValue, compSchema, components, removedPaths, [
          ...currentPath,
          `${attrName}[${dzItem.__temp_key__}]`
        ]);
        const processedItem = dzItem.id === void 0 || dzItem.id === null ? {
          __component: compUID,
          ...cleaned,
          id: void 0
        } : {
          __component: compUID,
          ...cleaned
        };
        return processedItem;
      });
      continue;
    }
    if (currentValue !== void 0) {
      result[attrName] = currentValue;
    } else if (initialValue !== void 0) {
      result[attrName] = initialValue;
    }
  }
  for (const [key, value] of Object.entries(data)) {
    if (!(key in result) && !(key in ((schema == null ? void 0 : schema.attributes) || {}))) {
      result[key] = value;
    }
  }
  return result;
};
var handleInvisibleAttributes = (data, { schema, initialValues = {}, components = {} }) => {
  if (!(schema == null ? void 0 : schema.attributes)) return {
    data,
    removedAttributes: []
  };
  const removedAttributes = collectInvisibleAttributes(data, schema, components);
  const filteredData = filterDataByRemovedPaths(data, initialValues, schema, components, removedAttributes);
  return {
    data: filteredData,
    removedAttributes
  };
};

// node_modules/@strapi/content-manager/dist/admin/services/documents.mjs
var import_qs = __toESM(require_lib(), 1);
var documentApi = contentManagerApi.injectEndpoints({
  overrideExisting: true,
  endpoints: (builder) => ({
    autoCloneDocument: builder.mutation({
      query: ({ model, sourceId, params }) => ({
        url: `/content-manager/collection-types/${model}/auto-clone/${sourceId}`,
        method: "POST",
        config: {
          params
        }
      }),
      invalidatesTags: (_result, error, { model }) => {
        if (error) {
          return [];
        }
        return [
          {
            type: "Document",
            id: `${model}_LIST`
          },
          "RecentDocumentList",
          "CountDocuments",
          "UpcomingReleasesList"
        ];
      }
    }),
    cloneDocument: builder.mutation({
      query: ({ model, sourceId, data, params }) => ({
        url: `/content-manager/collection-types/${model}/clone/${sourceId}`,
        method: "POST",
        data,
        config: {
          params
        }
      }),
      invalidatesTags: (_result, _error, { model }) => [
        {
          type: "Document",
          id: `${model}_LIST`
        },
        {
          type: "UidAvailability",
          id: model
        },
        "RecentDocumentList",
        "CountDocuments",
        "UpcomingReleasesList"
      ]
    }),
    /**
    * Creates a new collection-type document. This should ONLY be used for collection-types.
    * single-types should always be using `updateDocument` since they always exist.
    */
    createDocument: builder.mutation({
      query: ({ model, data, params }) => ({
        url: `/content-manager/collection-types/${model}`,
        method: "POST",
        data,
        config: {
          params
        }
      }),
      invalidatesTags: (result, _error, { model }) => [
        {
          type: "Document",
          id: `${model}_LIST`
        },
        "Relations",
        {
          type: "UidAvailability",
          id: model
        },
        "RecentDocumentList",
        "CountDocuments",
        "UpcomingReleasesList"
      ],
      transformResponse: (response, meta, arg) => {
        if (!("data" in response) && arg.model === "plugin::users-permissions.user") {
          return {
            data: response,
            meta: {
              availableStatus: [],
              availableLocales: []
            }
          };
        }
        return response;
      }
    }),
    deleteDocument: builder.mutation({
      query: ({ collectionType, model, documentId, params }) => ({
        url: `/content-manager/${collectionType}/${model}${collectionType !== SINGLE_TYPES && documentId ? `/${documentId}` : ""}`,
        method: "DELETE",
        config: {
          params
        }
      }),
      invalidatesTags: (_result, _error, { collectionType, model, documentId }) => [
        {
          type: "Document",
          id: collectionType !== SINGLE_TYPES ? `${model}_LIST` : model
        },
        {
          type: "Document",
          id: collectionType !== SINGLE_TYPES ? `${model}_${documentId}` : model
        },
        {
          type: "Document",
          id: `${model}_ALL_ITEMS`
        },
        "RecentDocumentList",
        "CountDocuments",
        "UpcomingReleasesList"
      ]
    }),
    deleteManyDocuments: builder.mutation({
      query: ({ model, params, ...body }) => ({
        url: `/content-manager/collection-types/${model}/actions/bulkDelete`,
        method: "POST",
        data: body,
        config: {
          params
        }
      }),
      invalidatesTags: (_res, _error, { model }) => [
        {
          type: "Document",
          id: `${model}_LIST`
        },
        "RecentDocumentList",
        "CountDocuments",
        "UpcomingReleasesList"
      ]
    }),
    discardDocument: builder.mutation({
      query: ({ collectionType, model, documentId, params }) => ({
        url: documentId && collectionType !== SINGLE_TYPES ? `/content-manager/${collectionType}/${model}/${documentId}/actions/discard` : `/content-manager/${collectionType}/${model}/actions/discard`,
        method: "POST",
        config: {
          params
        }
      }),
      invalidatesTags: (_result, _error, { collectionType, model, documentId }) => {
        return [
          {
            type: "Document",
            id: collectionType !== SINGLE_TYPES ? `${model}_${documentId}` : model
          },
          {
            type: "Document",
            id: `${model}_LIST`
          },
          "Relations",
          {
            type: "UidAvailability",
            id: model
          },
          "RecentDocumentList",
          "CountDocuments",
          "UpcomingReleasesList"
        ];
      }
    }),
    /**
    * Gets all documents of a collection type or single type.
    * By passing different params you can get different results e.g. only published documents or 'es' documents.
    */
    getAllDocuments: builder.query({
      query: ({ model, params }) => ({
        url: `/content-manager/collection-types/${model}`,
        method: "GET",
        config: {
          params: (0, import_qs.stringify)(params, {
            encode: true
          })
        }
      }),
      providesTags: (result, _error, arg) => {
        return [
          {
            type: "Document",
            id: `ALL_LIST`
          },
          {
            type: "Document",
            id: `${arg.model}_LIST`
          },
          ...(result == null ? void 0 : result.results.map(({ documentId }) => ({
            type: "Document",
            id: `${arg.model}_${documentId}`
          }))) ?? []
        ];
      }
    }),
    getDraftRelationCount: builder.query({
      query: ({ collectionType, model, documentId, params }) => ({
        url: documentId && collectionType !== SINGLE_TYPES ? `/content-manager/${collectionType}/${model}/${documentId}/actions/countDraftRelations` : `/content-manager/${collectionType}/${model}/actions/countDraftRelations`,
        method: "GET",
        config: {
          params
        }
      })
    }),
    /**
    * Fetches multiple documents with deep populate in a single request for bulk publish validation.
    */
    getDocumentsForValidation: builder.query({
      query: ({ model, documentIds, locale, sort }) => ({
        url: `/content-manager/collection-types/${model}/actions/bulkFindForValidation`,
        method: "POST",
        data: {
          documentIds,
          locale,
          sort
        }
      }),
      transformResponse: (response) => (response == null ? void 0 : response.results) ?? [],
      providesTags: (result, _error, { model }) => result ? [
        ...result.map((doc) => ({
          type: "Document",
          id: `${model}_${doc.documentId}`
        })),
        {
          type: "Document",
          id: `${model}_LIST`
        }
      ] : []
    }),
    getDocument: builder.query({
      // @ts-expect-error – TODO: fix ts error where data unknown doesn't work with response via an assertion?
      queryFn: async ({ collectionType, model, documentId, params }, _api, _extraOpts, baseQuery) => {
        const res = await baseQuery({
          url: `/content-manager/${collectionType}/${model}${documentId && collectionType !== SINGLE_TYPES ? `/${documentId}` : ""}`,
          method: "GET",
          config: {
            params
          }
        });
        if (res.error && res.error.name === "NotFoundError" && collectionType === SINGLE_TYPES) {
          return {
            data: {
              document: void 0
            },
            error: void 0
          };
        }
        return res;
      },
      providesTags: (result, _error, { collectionType, model, documentId }) => {
        return [
          // we prefer the result's id because we don't fetch single-types with an ID.
          {
            type: "Document",
            id: collectionType !== SINGLE_TYPES ? `${model}_${result && "documentId" in result ? result.documentId : documentId}` : model
          },
          // Make it easy to invalidate all individual documents queries for a model
          {
            type: "Document",
            id: `${model}_ALL_ITEMS`
          }
        ];
      }
    }),
    getManyDraftRelationCount: builder.query({
      query: ({ model, ...params }) => ({
        url: `/content-manager/collection-types/${model}/actions/countManyEntriesDraftRelations`,
        method: "GET",
        config: {
          params
        }
      }),
      transformResponse: (response) => response.data
    }),
    /**
    * This endpoint will either create or update documents at the same time as publishing.
    */
    publishDocument: builder.mutation({
      query: ({ collectionType, model, documentId, params, data }) => ({
        url: documentId && collectionType !== SINGLE_TYPES ? `/content-manager/${collectionType}/${model}/${documentId}/actions/publish` : `/content-manager/${collectionType}/${model}/actions/publish`,
        method: "POST",
        data,
        config: {
          params
        }
      }),
      invalidatesTags: (_result, _error, { collectionType, model, documentId }) => {
        return [
          {
            type: "Document",
            id: collectionType !== SINGLE_TYPES ? `${model}_${documentId}` : model
          },
          {
            type: "Document",
            id: `${model}_LIST`
          },
          "Relations",
          "RecentDocumentList",
          "CountDocuments",
          "UpcomingReleasesList",
          {
            type: "AILocalizationJobs",
            id: collectionType !== SINGLE_TYPES ? `${model}_${documentId}` : model
          }
        ];
      }
    }),
    publishManyDocuments: builder.mutation({
      query: ({ model, params, ...body }) => ({
        url: `/content-manager/collection-types/${model}/actions/bulkPublish`,
        method: "POST",
        data: body,
        config: {
          params
        }
      }),
      invalidatesTags: (_res, _error, { model, documentIds }) => {
        return [
          ...documentIds.map((id) => ({
            type: "Document",
            id: `${model}_${id}`
          })),
          "RecentDocumentList",
          "CountDocuments",
          "UpcomingReleasesList"
        ];
      }
    }),
    updateDocument: builder.mutation({
      query: ({ collectionType, model, documentId, data, params }) => ({
        url: `/content-manager/${collectionType}/${model}${documentId && collectionType !== SINGLE_TYPES ? `/${documentId}` : ""}`,
        method: "PUT",
        data,
        config: {
          params
        }
      }),
      invalidatesTags: (_result, _error, { collectionType, model, documentId }) => {
        return [
          {
            type: "Document",
            id: collectionType !== SINGLE_TYPES ? `${model}_${documentId}` : model
          },
          "Relations",
          {
            type: "UidAvailability",
            id: model
          },
          "RecentDocumentList",
          "CountDocuments",
          "UpcomingReleasesList",
          {
            type: "AILocalizationJobs",
            id: collectionType !== SINGLE_TYPES ? `${model}_${documentId}` : model
          }
        ];
      },
      async onQueryStarted({ data, ...patch }, { dispatch, queryFulfilled }) {
        const patchResult = dispatch(documentApi.util.updateQueryData("getDocument", patch, (draft) => {
          Object.assign(draft.data, data);
        }));
        try {
          await queryFulfilled;
        } catch {
          patchResult.undo();
        }
      },
      transformResponse: (response, meta, arg) => {
        if (!("data" in response) && arg.model === "plugin::users-permissions.user") {
          return {
            data: response,
            meta: {
              availableStatus: [],
              availableLocales: []
            }
          };
        }
        return response;
      }
    }),
    unpublishDocument: builder.mutation({
      query: ({ collectionType, model, documentId, params, data }) => ({
        url: documentId && collectionType !== SINGLE_TYPES ? `/content-manager/${collectionType}/${model}/${documentId}/actions/unpublish` : `/content-manager/${collectionType}/${model}/actions/unpublish`,
        method: "POST",
        data,
        config: {
          params
        }
      }),
      invalidatesTags: (_result, _error, { collectionType, model, documentId }) => {
        return [
          {
            type: "Document",
            id: collectionType !== SINGLE_TYPES ? `${model}_${documentId}` : model
          },
          "RecentDocumentList",
          "CountDocuments",
          "UpcomingReleasesList"
        ];
      }
    }),
    unpublishManyDocuments: builder.mutation({
      query: ({ model, params, ...body }) => ({
        url: `/content-manager/collection-types/${model}/actions/bulkUnpublish`,
        method: "POST",
        data: body,
        config: {
          params
        }
      }),
      invalidatesTags: (_res, _error, { model, documentIds }) => [
        ...documentIds.map((id) => ({
          type: "Document",
          id: `${model}_${id}`
        })),
        "RecentDocumentList",
        "CountDocuments",
        "UpcomingReleasesList"
      ]
    })
  })
});
var { useAutoCloneDocumentMutation, useCloneDocumentMutation, useCreateDocumentMutation, useDeleteDocumentMutation, useDeleteManyDocumentsMutation, useDiscardDocumentMutation, useGetAllDocumentsQuery, useGetDocumentsForValidationQuery, useLazyGetDocumentQuery, useGetDocumentQuery, useLazyGetDraftRelationCountQuery, useGetManyDraftRelationCountQuery, usePublishDocumentMutation, usePublishManyDocumentsMutation, useUpdateDocumentMutation, useUnpublishDocumentMutation, useUnpublishManyDocumentsMutation } = documentApi;

// node_modules/@strapi/content-manager/dist/admin/utils/api.mjs
var buildValidParams = (query) => {
  if (!query) return query;
  const { plugins: _, ...validQueryParams } = {
    ...query,
    ...Object.values((query == null ? void 0 : query.plugins) ?? {}).reduce((acc, current) => {
      if (typeof current === "object" && current !== null) {
        return Object.assign(acc, current);
      }
      return acc;
    }, {})
  };
  return validQueryParams;
};
var isBaseQueryError = (error) => {
  return error.name !== void 0;
};

// node_modules/@strapi/content-manager/dist/admin/utils/validation.mjs
var import_pipe2 = __toESM(require_pipe(), 1);
var arrayValidator = (attribute, options) => ({
  message: errorsTrads.required,
  test(value) {
    if (options.status === "draft") {
      return true;
    }
    if (!attribute.required) {
      return true;
    }
    if (!value) {
      return false;
    }
    if (Array.isArray(value) && value.length === 0) {
      return false;
    }
    return true;
  }
});
var escapeRegex = (str) => str.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
var createYupSchema = (attributes = {}, components = {}, options = {
  status: null
}) => {
  const createModelSchema = (attributes2, removedAttributes = []) => create5().shape(Object.entries(attributes2).reduce((acc, [name, attribute]) => {
    const getNestedPathsForAttribute = (removed, attrName) => {
      const prefix = `${attrName}.`;
      const bracketRegex = new RegExp(`^${escapeRegex(attrName)}\\[[^\\]]+\\]\\.`);
      return removed.filter((p) => p.startsWith(prefix) || bracketRegex.test(p)).map((p) => p.startsWith(prefix) ? p.slice(prefix.length) : p.replace(bracketRegex, ""));
    };
    if (DOCUMENT_META_FIELDS.includes(name)) {
      return acc;
    }
    if (removedAttributes == null ? void 0 : removedAttributes.includes(name)) {
      return acc;
    }
    const nestedRemoved = getNestedPathsForAttribute(removedAttributes, name);
    const validations = [
      addNullableValidation,
      addRequiredValidation,
      addMinLengthValidation,
      addMaxLengthValidation,
      addMinValidation,
      addMaxValidation,
      addRegexValidation
    ].map((fn) => fn(attribute, options));
    const transformSchema = (0, import_pipe2.default)(...validations);
    switch (attribute.type) {
      case "component": {
        const { attributes: attributes3 } = components[attribute.component];
        if (attribute.repeatable) {
          return {
            ...acc,
            [name]: transformSchema(create6().of(createModelSchema(attributes3, nestedRemoved).nullable(false))).test(arrayValidator(attribute, options))
          };
        } else {
          return {
            ...acc,
            [name]: transformSchema(createModelSchema(attributes3, nestedRemoved).nullable())
          };
        }
      }
      case "dynamiczone":
        return {
          ...acc,
          [name]: transformSchema(create6().of(create7((data) => {
            var _a;
            const attributes3 = (_a = components == null ? void 0 : components[data == null ? void 0 : data.__component]) == null ? void 0 : _a.attributes;
            const validation = create5().shape({
              __component: create3().required().oneOf(Object.keys(components))
            }).nullable(false);
            if (!attributes3) {
              return validation;
            }
            return validation.concat(createModelSchema(attributes3, nestedRemoved));
          }))).test(arrayValidator(attribute, options))
        };
      case "relation":
        return {
          ...acc,
          [name]: transformSchema(create7((value) => {
            if (!value) {
              return create().nullable(true);
            } else if (Array.isArray(value)) {
              return create6().of(create5().shape({
                id: create4().required()
              }));
            } else if (typeof value === "object") {
              return create5();
            } else {
              return create().test("type-error", "Relation values must be either null, an array of objects with {id} or an object.", () => false);
            }
          }))
        };
      default:
        return {
          ...acc,
          [name]: transformSchema(createAttributeSchema(attribute))
        };
    }
  }, {})).default(null);
  return createModelSchema(attributes, options.removedAttributes);
};
var createAttributeSchema = (attribute) => {
  switch (attribute.type) {
    case "biginteger":
      return create3().matches(/^-?\d*$/);
    case "boolean":
      return create2().nullable();
    case "blocks":
      return create().test("isBlocks", errorsTrads.json, (value) => {
        if (!value || Array.isArray(value)) {
          return true;
        } else {
          return false;
        }
      });
    case "decimal":
    case "float":
    case "integer":
      return create4();
    case "email":
      return create3().email(errorsTrads.email);
    case "enumeration":
      return create3().oneOf([
        ...attribute.enum,
        null
      ]);
    case "json":
      return create().test("isJSON", errorsTrads.json, (value) => {
        if (!value || typeof value === "string" && value.length === 0) {
          return true;
        }
        if (typeof value === "object") {
          try {
            JSON.stringify(value);
            return true;
          } catch {
            return false;
          }
        }
        try {
          JSON.parse(value);
          return true;
        } catch {
          return false;
        }
      });
    case "password":
      return create3().nullable();
    case "richtext":
    case "string":
    case "text":
      return create3();
    case "uid":
      return create3().matches(attribute.regex ? new RegExp(attribute.regex) : /^[A-Za-z0-9-_.~]*$/);
    default:
      return create();
  }
};
var nullableSchema = (schema) => {
  return (schema == null ? void 0 : schema.nullable) ? schema.nullable() : (
    // e.g. when the schema has been built using yup.lazy (e.g. for relations).
    // In these cases we should just return the schema as it is.
    schema
  );
};
var addNullableValidation = () => (schema) => {
  return nullableSchema(schema);
};
var addRequiredValidation = (attribute, options) => (schema) => {
  if (options.status === "draft" || !attribute.required || attribute.type === "password") {
    return schema;
  }
  if (attribute.required && "required" in schema) {
    return schema.required(errorsTrads.required);
  }
  return schema;
};
var addMinLengthValidation = (attribute, options) => (schema) => {
  if (options.status === "draft") {
    return schema;
  }
  if ("minLength" in attribute && attribute.minLength && Number.isInteger(attribute.minLength) && "min" in schema) {
    return schema.min(attribute.minLength, {
      ...errorsTrads.minLength,
      values: {
        min: attribute.minLength
      }
    });
  }
  return schema;
};
var addMaxLengthValidation = (attribute) => (schema) => {
  if ("maxLength" in attribute && attribute.maxLength && Number.isInteger(attribute.maxLength) && "max" in schema) {
    return schema.max(attribute.maxLength, {
      ...errorsTrads.maxLength,
      values: {
        max: attribute.maxLength
      }
    });
  }
  return schema;
};
var addMinValidation = (attribute, options) => (schema) => {
  if (options.status === "draft") {
    return schema;
  }
  if ("min" in attribute && "min" in schema) {
    const min = toInteger(attribute.min);
    if (min !== void 0) {
      return schema.min(min, {
        ...errorsTrads.min,
        values: {
          min
        }
      });
    }
  }
  return schema;
};
var addMaxValidation = (attribute) => (schema) => {
  if ("max" in attribute) {
    const max = toInteger(attribute.max);
    if ("max" in schema && max !== void 0) {
      return schema.max(max, {
        ...errorsTrads.max,
        values: {
          max
        }
      });
    }
  }
  return schema;
};
var toInteger = (val) => {
  if (typeof val === "number" || val === void 0) {
    return val;
  } else {
    const num = Number(val);
    return isNaN(num) ? void 0 : num;
  }
};
var addRegexValidation = (attribute) => (schema) => {
  if ("regex" in attribute && attribute.regex && "matches" in schema) {
    return schema.matches(new RegExp(attribute.regex), {
      message: {
        id: errorsTrads.regex.id,
        defaultMessage: "The value does not match the defined pattern."
      },
      excludeEmptyString: !attribute.required
    });
  }
  return schema;
};

// node_modules/@strapi/content-manager/dist/admin/services/init.mjs
var initApi = contentManagerApi.injectEndpoints({
  endpoints: (builder) => ({
    getInitialData: builder.query({
      query: () => "/content-manager/init",
      transformResponse: (response) => response.data,
      providesTags: [
        "InitialData"
      ]
    })
  })
});
var { useGetInitialDataQuery } = initApi;

// node_modules/@strapi/content-manager/dist/admin/hooks/useContentTypeSchema.mjs
var React = __toESM(require_react(), 1);
var EMPTY_COMPONENTS = {};
var schemaInfoCache = /* @__PURE__ */ new WeakMap();
var getSchemaInfo = (data, model) => {
  if (!data) {
    return {
      components: void 0,
      contentType: void 0,
      contentTypes: []
    };
  }
  let cachedByModel = schemaInfoCache.get(data);
  if (!cachedByModel) {
    cachedByModel = /* @__PURE__ */ new Map();
    schemaInfoCache.set(data, cachedByModel);
  }
  const cached = cachedByModel.get(model);
  if (cached) {
    return cached;
  }
  const contentType = data.contentTypes.find((ct) => ct.uid === model);
  const componentsByKey = data.components.reduce((acc, component) => {
    acc[component.uid] = component;
    return acc;
  }, {});
  const components = extractContentTypeComponents(contentType == null ? void 0 : contentType.attributes, componentsByKey);
  const schemaInfo = {
    components: Object.keys(components).length === 0 ? void 0 : components,
    contentType,
    contentTypes: data.contentTypes
  };
  cachedByModel.set(model, schemaInfo);
  return schemaInfo;
};
var useContentTypeSchema = (model) => {
  const { toggleNotification } = useNotification();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const { data, error, isLoading, isFetching } = useGetInitialDataQuery(void 0);
  const { components, contentType, contentTypes } = React.useMemo(() => getSchemaInfo(data, model), [
    model,
    data
  ]);
  React.useEffect(() => {
    if (error) {
      toggleNotification({
        type: "danger",
        message: formatAPIError(error)
      });
    }
  }, [
    toggleNotification,
    error,
    formatAPIError
  ]);
  return {
    components: components ?? EMPTY_COMPONENTS,
    schema: contentType,
    schemas: contentTypes,
    isLoading: isLoading || isFetching
  };
};
var extractContentTypeComponents = (attributes = {}, allComponents = {}) => {
  const getComponents = (attributes2) => {
    return attributes2.reduce((acc, attribute) => {
      var _a;
      if (attribute.type === "component") {
        const componentAttributes = Object.values(((_a = allComponents[attribute.component]) == null ? void 0 : _a.attributes) ?? {});
        acc.push(attribute.component, ...getComponents(componentAttributes));
      } else if (attribute.type === "dynamiczone") {
        acc.push(
          ...attribute.components,
          ...attribute.components.flatMap((componentUid) => {
            var _a2;
            const componentAttributes = Object.values(((_a2 = allComponents[componentUid]) == null ? void 0 : _a2.attributes) ?? {});
            return getComponents(componentAttributes);
          })
        );
      }
      return acc;
    }, []);
  };
  const componentUids = getComponents(Object.values(attributes));
  const uniqueComponentUids = [
    ...new Set(componentUids)
  ];
  const componentsByKey = uniqueComponentUids.reduce((acc, uid) => {
    const component = allComponents[uid];
    if (component) {
      acc[uid] = component;
    }
    return acc;
  }, {});
  return componentsByKey;
};

// node_modules/@strapi/content-manager/dist/admin/constants/hooks.mjs
var HOOKS = {
  /**
  * Hook that allows to mutate the displayed headers of the list view table
  * @constant
  * @type {string}
  */
  INJECT_COLUMN_IN_TABLE: "Admin/CM/pages/ListView/inject-column-in-table",
  /**
  * Hook that allows to mutate the displayed filters of the list view
  * @constant
  * @type {string}
  */
  INJECT_LIST_VIEW_FILTERS: "Admin/CM/pages/ListView/inject-in-filters",
  /**
  * Hook that allows to mutate the CM's collection types links pre-set filters
  * @constant
  * @type {string}
  */
  MUTATE_COLLECTION_TYPES_LINKS: "Admin/CM/pages/App/mutate-collection-types-links",
  /**
  * Hook that allows to mutate the CM's edit view layout
  * @constant
  * @type {string}
  */
  MUTATE_EDIT_VIEW_LAYOUT: "Admin/CM/pages/EditView/mutate-edit-view-layout",
  /**
  * Hook that allows to mutate the CM's single types links pre-set filters
  * @constant
  * @type {string}
  */
  MUTATE_SINGLE_TYPES_LINKS: "Admin/CM/pages/App/mutate-single-types-links"
};

// node_modules/@strapi/content-manager/dist/admin/utils/layouts/normalizeContentManagerLayout.mjs
var DEFAULT_RESPONSE_SETTINGS = {
  bulkable: true,
  filterable: true,
  searchable: true,
  pageSize: 10,
  mainField: "id",
  defaultSortBy: "",
  defaultSortOrder: "ASC"
};
var isRecord = (value) => {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
};
var warnOnceInDevelopment = /* @__PURE__ */ (() => {
  const warnings = /* @__PURE__ */ new Set();
  return (message) => {
    if (warnings.has(message)) {
      return;
    }
    warnings.add(message);
    console.warn(`[Content Manager] ${message}`);
  };
})();
var normalizeResponseSettings = (settings, path) => {
  if (!isRecord(settings)) {
    warnOnceInDevelopment(`Received malformed settings at "${path}". Falling back to defaults.`);
  }
  return {
    ...DEFAULT_RESPONSE_SETTINGS,
    ...isRecord(settings) ? settings : {}
  };
};
var normalizeResponseMetadatas = (metadatas, path) => {
  if (!isRecord(metadatas)) {
    warnOnceInDevelopment(`Received malformed metadatas at "${path}". Falling back to empty.`);
    return {};
  }
  return Object.entries(metadatas).reduce((acc, [name, metadata]) => {
    const metadataRecord = isRecord(metadata) ? metadata : {};
    if (!isRecord(metadata)) {
      warnOnceInDevelopment(`Received malformed metadata for "${path}.${name}". Repairing entry.`);
    }
    if (!isRecord(metadataRecord.edit)) {
      warnOnceInDevelopment(`Received malformed edit metadata for "${path}.${name}". Falling back to empty.`);
    }
    if (!isRecord(metadataRecord.list)) {
      warnOnceInDevelopment(`Received malformed list metadata for "${path}.${name}". Falling back to empty.`);
    }
    acc[name] = {
      edit: isRecord(metadataRecord.edit) ? metadataRecord.edit : {},
      list: isRecord(metadataRecord.list) ? metadataRecord.list : {}
    };
    return acc;
  }, {});
};
var normalizeResponseLayouts = (layouts, path) => {
  if (!isRecord(layouts)) {
    warnOnceInDevelopment(`Received malformed layouts at "${path}". Falling back to empty.`);
    return {
      edit: [],
      list: []
    };
  }
  if (!Array.isArray(layouts.edit)) {
    warnOnceInDevelopment(`Received malformed edit layout at "${path}". Falling back to empty.`);
  }
  if (!Array.isArray(layouts.list)) {
    warnOnceInDevelopment(`Received malformed list layout at "${path}". Falling back to empty.`);
  }
  return {
    edit: Array.isArray(layouts.edit) ? layouts.edit.map((row, rowIndex) => {
      if (!Array.isArray(row)) {
        warnOnceInDevelopment(`Dropped malformed edit layout row "${path}.edit.${rowIndex}".`);
        return [];
      }
      return row.filter((field, fieldIndex) => {
        const isValidField = isRecord(field) && typeof field.name === "string" && typeof field.size === "number";
        if (!isValidField) {
          warnOnceInDevelopment(`Dropped malformed edit layout field "${path}.edit.${rowIndex}.${fieldIndex}".`);
        }
        return isValidField;
      });
    }) : [],
    list: Array.isArray(layouts.list) ? layouts.list.filter((field, index) => {
      const isValidField = typeof field === "string";
      if (!isValidField) {
        warnOnceInDevelopment(`Dropped malformed list layout field "${path}.list.${index}".`);
      }
      return isValidField;
    }) : []
  };
};
var normalizeConfigurationResponse = (configuration, uid, path = uid ?? "configuration") => {
  if (!isRecord(configuration)) {
    warnOnceInDevelopment(`Received malformed configuration at "${path}". Repairing entry.`);
  }
  const configurationRecord = isRecord(configuration) ? configuration : {};
  return {
    ...configurationRecord,
    ...uid ? {
      uid
    } : {},
    settings: normalizeResponseSettings(configurationRecord.settings, `${path}.settings`),
    metadatas: normalizeResponseMetadatas(configurationRecord.metadatas, `${path}.metadatas`),
    layouts: normalizeResponseLayouts(configurationRecord.layouts, `${path}.layouts`)
  };
};
var normalizeComponentsRecord = (components) => {
  if (!isRecord(components)) {
    warnOnceInDevelopment("Received malformed component configuration record. Falling back to empty.");
    return {};
  }
  return Object.entries(components).reduce((acc, [uid, configuration]) => {
    acc[uid] = normalizeConfigurationResponse(configuration, uid, `components.${uid}`);
    return acc;
  }, {});
};
var normalizeContentTypeConfigurationResponse = (data, uid) => {
  if (!isRecord(data)) {
    warnOnceInDevelopment("Received malformed content-type configuration response. Repairing data.");
  }
  const dataRecord = isRecord(data) ? data : {};
  return {
    contentType: normalizeConfigurationResponse(dataRecord.contentType, uid, "contentType"),
    components: normalizeComponentsRecord(dataRecord.components)
  };
};
var normalizeContentTypeConfigurationUpdateResponse = (data) => {
  if (!isRecord(data)) {
    warnOnceInDevelopment("Received malformed content-type configuration update response. Repairing data.");
  }
  const dataRecord = isRecord(data) ? data : {};
  return {
    contentType: normalizeConfigurationResponse(dataRecord.contentType, void 0, "contentType"),
    components: normalizeComponentsRecord(dataRecord.components)
  };
};
var normalizeComponentConfigurationResponse = (data, uid) => {
  if (!isRecord(data)) {
    warnOnceInDevelopment("Received malformed component configuration response. Repairing data.");
  }
  const dataRecord = isRecord(data) ? data : {};
  return {
    component: normalizeConfigurationResponse(dataRecord.component, uid, "component"),
    components: normalizeComponentsRecord(dataRecord.components)
  };
};
var normalizeComponentConfigurationUpdateResponse = (data) => {
  return normalizeConfigurationResponse(data, void 0, "component");
};
var normalizeContentTypeSettingsResponse = (data) => {
  if (!Array.isArray(data)) {
    warnOnceInDevelopment("Received malformed content-type settings response. Falling back to empty.");
    return [];
  }
  return data.filter((entry, index) => {
    const isValidEntry = isRecord(entry);
    if (!isValidEntry) {
      warnOnceInDevelopment(`Dropped malformed content-type settings entry "${index}".`);
    }
    return isValidEntry;
  }).filter((entry) => {
    const hasUid = typeof entry.uid === "string";
    if (!hasUid) {
      warnOnceInDevelopment("Dropped content-type settings entry without a uid.");
    }
    return hasUid;
  }).map((entry) => ({
    uid: entry.uid,
    settings: normalizeResponseSettings(entry.settings, `contentTypeSettings.${entry.uid}`)
  }));
};
var hasAttribute = (attributes, name) => {
  return Boolean(name && (attributes == null ? void 0 : attributes[name]));
};
var getTargetAttributes = (attribute, { components, schemas }) => {
  var _a, _b;
  if (attribute.type === "component") {
    return (_a = components[attribute.component]) == null ? void 0 : _a.attributes;
  }
  if (attribute.type === "relation") {
    const target = "targetModel" in attribute ? attribute.targetModel : "target" in attribute ? attribute.target : void 0;
    return (_b = schemas.find((schema) => schema.uid === target)) == null ? void 0 : _b.attributes;
  }
  return void 0;
};
var hasTargetSchema = (attribute, { schemas }) => {
  if (attribute.type !== "relation") {
    return true;
  }
  const target = "targetModel" in attribute ? attribute.targetModel : "target" in attribute ? attribute.target : void 0;
  return schemas.some((schema) => schema.uid === target);
};
var normalizeMainField = (attribute, mainField, context, path) => {
  if (!mainField) {
    return mainField;
  }
  const targetAttributes = getTargetAttributes(attribute, context);
  if (!targetAttributes) {
    if (attribute.type === "relation" && !hasTargetSchema(attribute, context)) {
      return mainField;
    }
    if (attribute.type !== "component" && attribute.type !== "relation") {
      return mainField;
    }
    warnOnceInDevelopment(`Reset invalid mainField "${mainField}" at "${path}" to "id".`);
    return "id";
  }
  if (hasAttribute(targetAttributes, mainField)) {
    return mainField;
  }
  warnOnceInDevelopment(`Reset invalid mainField "${mainField}" at "${path}" to "id".`);
  return "id";
};
var normalizeSettings = (settings, attributes, path) => {
  if (!settings.mainField || hasAttribute(attributes, settings.mainField) || settings.mainField === "id") {
    return settings;
  }
  warnOnceInDevelopment(`Reset invalid mainField "${settings.mainField}" at "${path}" to "id".`);
  return {
    ...settings,
    mainField: "id"
  };
};
var hasResolvableComponent = (attribute, context) => {
  if (!attribute || !("component" in attribute) || typeof attribute.component !== "string") {
    return true;
  }
  return Boolean(context.components[attribute.component]);
};
var normalizeMetadatas = (metadatas, attributes, context, path) => {
  return Object.entries(metadatas).reduce((acc, [name, metadata]) => {
    const attribute = attributes == null ? void 0 : attributes[name];
    if (!attribute) {
      warnOnceInDevelopment(`Dropped metadata for unknown field "${path}.metadatas.${name}".`);
      return acc;
    }
    if (!hasResolvableComponent(attribute, context)) {
      warnOnceInDevelopment(`Dropped metadata for field "${path}.metadatas.${name}" because its component schema is unavailable.`);
      return acc;
    }
    acc[name] = {
      edit: {
        ...metadata.edit,
        // `mainField` is present at runtime for component/relation edit metadata.
        ..."mainField" in metadata.edit ? {
          mainField: normalizeMainField(attribute, metadata.edit.mainField, context, `${path}.metadatas.${name}.edit.mainField`)
        } : {}
      },
      list: {
        ...metadata.list,
        mainField: normalizeMainField(attribute, metadata.list.mainField, context, `${path}.metadatas.${name}.list.mainField`)
      }
    };
    return acc;
  }, {});
};
var normalizeLayouts = (layouts, attributes, metadatas, context, path) => {
  const hasRenderableField = (name) => {
    const attribute = attributes == null ? void 0 : attributes[name];
    return Boolean(attribute && metadatas[name] && hasResolvableComponent(attribute, context));
  };
  return {
    edit: (layouts.edit ?? []).map((row, rowIndex) => row.filter((field) => {
      const isRenderable = hasRenderableField(field.name);
      if (!isRenderable) {
        warnOnceInDevelopment(`Dropped non-renderable edit layout field "${path}.layouts.edit.${rowIndex}.${field.name}".`);
      }
      return isRenderable;
    })).filter((row) => row.length > 0),
    list: (layouts.list ?? []).filter((name, index) => {
      const isRenderable = hasRenderableField(name);
      if (!isRenderable) {
        warnOnceInDevelopment(`Dropped non-renderable list layout field "${path}.layouts.list.${index}.${name}".`);
      }
      return isRenderable;
    })
  };
};
var normalizeConfiguration = (configuration, attributes, context) => {
  const path = configuration.uid ?? "configuration";
  const metadatas = normalizeMetadatas(configuration.metadatas, attributes, context, path);
  const layouts = normalizeLayouts(configuration.layouts, attributes, metadatas, context, path);
  return {
    ...configuration,
    settings: normalizeSettings(configuration.settings, attributes, `${path}.settings.mainField`),
    metadatas,
    layouts
  };
};
var normalizeComponents = (componentConfigurations, context) => {
  return Object.entries(componentConfigurations).reduce((acc, [uid, configuration]) => {
    const componentSchema = context.components[uid];
    if (!componentSchema) {
      return acc;
    }
    acc[uid] = normalizeConfiguration(configuration, componentSchema.attributes, context);
    return acc;
  }, {});
};
var normalizeContentManagerLayout = (data, context) => {
  var _a;
  const normalizedComponents = normalizeComponents(data.components, context);
  return {
    contentType: normalizeConfiguration(data.contentType, (_a = context.schema) == null ? void 0 : _a.attributes, context),
    components: normalizedComponents
  };
};
var normalizeComponentConfigurationLayout = (data, context) => {
  var _a;
  const normalizedComponents = normalizeComponents(data.components, {
    components: context.components,
    schemas: context.schemas
  });
  return {
    component: normalizeConfiguration(data.component, (_a = context.schema) == null ? void 0 : _a.attributes, {
      components: context.components,
      schemas: context.schemas
    }),
    components: normalizedComponents
  };
};

// node_modules/@strapi/content-manager/dist/admin/services/contentTypes.mjs
var contentTypesApi = contentManagerApi.injectEndpoints({
  endpoints: (builder) => ({
    getContentTypeConfiguration: builder.query({
      query: (uid) => ({
        url: `/content-manager/content-types/${uid}/configuration`,
        method: "GET"
      }),
      transformResponse: (response, _meta, uid) => normalizeContentTypeConfigurationResponse(response.data, uid),
      providesTags: (_result, _error, uid) => [
        {
          type: "ContentTypesConfiguration",
          id: uid
        },
        {
          type: "ContentTypeSettings",
          id: "LIST"
        }
      ]
    }),
    getAllContentTypeSettings: builder.query({
      query: () => "/content-manager/content-types-settings",
      transformResponse: (response) => normalizeContentTypeSettingsResponse(response.data),
      providesTags: [
        {
          type: "ContentTypeSettings",
          id: "LIST"
        }
      ]
    }),
    updateContentTypeConfiguration: builder.mutation({
      query: ({ uid, ...body }) => ({
        url: `/content-manager/content-types/${uid}/configuration`,
        method: "PUT",
        data: body
      }),
      transformResponse: (response) => normalizeContentTypeConfigurationUpdateResponse(response.data),
      invalidatesTags: (_result, _error, { uid }) => [
        {
          type: "ContentTypesConfiguration",
          id: uid
        },
        {
          type: "ContentTypeSettings",
          id: "LIST"
        },
        // Is this necessary?
        {
          type: "InitialData"
        }
      ]
    })
  })
});
var { useGetContentTypeConfigurationQuery, useGetAllContentTypeSettingsQuery, useUpdateContentTypeConfigurationMutation } = contentTypesApi;

// node_modules/@strapi/content-manager/dist/admin/hooks/useDocumentLayout.mjs
var React3 = __toESM(require_react(), 1);

// node_modules/@strapi/content-manager/dist/admin/utils/attributes.mjs
var checkIfAttributeIsDisplayable = (attribute) => {
  const { type } = attribute;
  if (type === "relation") {
    return !attribute.relation.toLowerCase().includes("morph");
  }
  return ![
    "json",
    "dynamiczone",
    "richtext",
    "password",
    "blocks"
  ].includes(type) && !!type;
};
var getMainField = (attribute, mainFieldName, { schemas, components }) => {
  var _a, _b, _c, _d, _e;
  if (!mainFieldName) {
    return void 0;
  }
  if (attribute.type === "component") {
    const mainFieldType = (_c = (_b = (_a = components[attribute.component]) == null ? void 0 : _a.attributes) == null ? void 0 : _b[mainFieldName]) == null ? void 0 : _c.type;
    return {
      name: mainFieldType ? mainFieldName : "id",
      type: mainFieldType ?? "custom"
    };
  }
  if (attribute.type === "relation") {
    const target = "targetModel" in attribute ? attribute.targetModel : "target" in attribute ? attribute.target : void 0;
    const targetSchema = schemas.find((schema) => schema.uid === target);
    const mainFieldType = (_e = (_d = targetSchema == null ? void 0 : targetSchema.attributes) == null ? void 0 : _d[mainFieldName]) == null ? void 0 : _e.type;
    return {
      name: !targetSchema || mainFieldType ? mainFieldName : "id",
      type: mainFieldType ?? "custom"
    };
  }
  return {
    name: mainFieldName,
    type: "string"
  };
};

// node_modules/@strapi/content-manager/dist/admin/hooks/useDocument.mjs
var React2 = __toESM(require_react(), 1);

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/utils/forms.mjs
var createDefaultForm = (contentType, components = {}) => {
  const traverseSchema = (attributes) => {
    return Object.entries(attributes).reduce((acc, [key, attribute]) => {
      if ("default" in attribute) {
        acc[key] = attribute.default;
      } else if (attribute.type === "component" && attribute.required) {
        const defaultComponentForm = traverseSchema(components[attribute.component].attributes);
        if (attribute.repeatable) {
          acc[key] = attribute.min ? [
            ...Array(attribute.min).fill(defaultComponentForm)
          ] : [];
        } else {
          acc[key] = defaultComponentForm;
        }
      } else if (attribute.type === "dynamiczone" && attribute.required) {
        acc[key] = [];
      }
      return acc;
    }, {});
  };
  return traverseSchema(contentType.attributes);
};

// node_modules/@strapi/content-manager/dist/admin/hooks/useDocument.mjs
var isLocalizedAttribute = (attribute) => {
  var _a;
  const i18nOptions = attribute.pluginOptions;
  return ((_a = i18nOptions == null ? void 0 : i18nOptions.i18n) == null ? void 0 : _a.localized) === true;
};
var useDocument = (args, opts) => {
  const { toggleNotification } = useNotification();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const { formatMessage } = useIntl();
  const { currentData: data, isLoading: isLoadingDocument, isFetching: isFetchingDocument, error, refetch } = useGetDocumentQuery(args, {
    ...opts,
    skip: !args.documentId && args.collectionType !== SINGLE_TYPES || (opts == null ? void 0 : opts.skip)
  });
  const document = data == null ? void 0 : data.data;
  const meta = data == null ? void 0 : data.meta;
  const { components, schema, schemas, isLoading: isLoadingSchema } = useContentTypeSchema(args.model);
  const isSingleType = (schema == null ? void 0 : schema.kind) === "singleType";
  const getTitle = React2.useCallback((mainField) => {
    if (mainField !== "id" && (document == null ? void 0 : document[mainField])) {
      return document[mainField];
    }
    if ((schema == null ? void 0 : schema.kind) === "singleType" && schema.info.displayName) {
      return formatMessage({
        id: schema.info.displayName,
        defaultMessage: schema.info.displayName
      });
    }
    return formatMessage({
      id: "content-manager.containers.untitled",
      defaultMessage: "Untitled"
    });
  }, [
    document,
    formatMessage,
    schema
  ]);
  React2.useEffect(() => {
    if (error) {
      toggleNotification({
        type: "danger",
        message: formatAPIError(error)
      });
    }
  }, [
    toggleNotification,
    error,
    formatAPIError,
    args.collectionType
  ]);
  const validationSchema = React2.useMemo(() => {
    if (!schema) {
      return null;
    }
    return createYupSchema(schema.attributes, components);
  }, [
    schema,
    components
  ]);
  const validate = React2.useCallback((document2) => {
    if (!validationSchema) {
      throw new Error("There is no validation schema generated, this is likely due to the schema not being loaded yet.");
    }
    try {
      validationSchema.validateSync(document2, {
        abortEarly: false,
        strict: true
      });
      return null;
    } catch (error2) {
      if (error2 instanceof ValidationError) {
        return getYupValidationErrors(error2);
      }
      throw error2;
    }
  }, [
    validationSchema
  ]);
  const nonLocalizedScalarAndMediaFields = React2.useMemo(() => {
    if (!(schema == null ? void 0 : schema.attributes)) {
      return [];
    }
    return Object.keys(schema.attributes).filter((name) => {
      const attribute = schema.attributes[name];
      if (isLocalizedAttribute(attribute)) {
        return false;
      }
      return attribute.type !== "component" && attribute.type !== "dynamiczone" && attribute.type !== "relation";
    });
  }, [
    schema
  ]);
  const getInitialFormValues = React2.useCallback((isCreatingDocument = false) => {
    var _a;
    if (!document && !isCreatingDocument && !isSingleType || !schema) {
      return void 0;
    }
    if (document == null ? void 0 : document.id) {
      return transformDocument(schema, components)(document);
    }
    let form = createDefaultForm(schema, components);
    const sibling = (_a = meta == null ? void 0 : meta.availableLocales) == null ? void 0 : _a[0];
    if (sibling && nonLocalizedScalarAndMediaFields.length > 0) {
      const inherited = nonLocalizedScalarAndMediaFields.reduce((acc, name) => {
        if (name in sibling) {
          acc[name] = sibling[name];
        }
        return acc;
      }, {});
      form = {
        ...form,
        ...inherited
      };
    }
    return transformDocument(schema, components)(form);
  }, [
    document,
    isSingleType,
    schema,
    components,
    meta == null ? void 0 : meta.availableLocales,
    nonLocalizedScalarAndMediaFields
  ]);
  const isLoading = isLoadingDocument || isFetchingDocument || isLoadingSchema;
  const hasError = !!error;
  return React2.useMemo(() => ({
    components,
    document,
    meta,
    isLoading,
    hasError,
    schema,
    schemas,
    validate,
    getTitle,
    getInitialFormValues,
    refetch
  }), [
    components,
    document,
    meta,
    isLoading,
    hasError,
    schema,
    schemas,
    validate,
    getTitle,
    getInitialFormValues,
    refetch
  ]);
};
var useDoc = (opts) => {
  const { id, slug, collectionType, origin } = useParams();
  const [{ query }] = useQueryParams();
  const params = React2.useMemo(() => buildValidParams(query), [
    query
  ]);
  if (!collectionType) {
    throw new Error("Could not find collectionType in url params");
  }
  if (!slug) {
    throw new Error("Could not find model in url params");
  }
  const document = useDocument({
    documentId: origin || id,
    model: slug,
    collectionType,
    params
  }, {
    ...opts,
    skip: id === "create" || !origin && !id && collectionType !== SINGLE_TYPES || (opts == null ? void 0 : opts.skip)
  });
  const returnId = origin || (id === "create" ? void 0 : id);
  return {
    collectionType,
    model: slug,
    id: returnId,
    ...document
  };
};
var useContentManagerContext = () => {
  var _a;
  const { collectionType, model, id, components, isLoading: isLoadingDoc, schema, schemas } = useDoc();
  const layout = useDocumentLayout(model);
  const form = useForm("useContentManagerContext", (state) => state);
  const isSingleType = collectionType === SINGLE_TYPES;
  const slug = model;
  const isCreatingEntry = id === "create";
  useContentTypeSchema();
  const isLoading = isLoadingDoc || layout.isLoading;
  const error = layout.error;
  return {
    error,
    isLoading,
    // Base metadata
    model,
    collectionType,
    id,
    slug,
    isCreatingEntry,
    isSingleType,
    hasDraftAndPublish: ((_a = schema == null ? void 0 : schema.options) == null ? void 0 : _a.draftAndPublish) ?? false,
    // All schema infos
    components,
    contentType: schema,
    contentTypes: schemas,
    // Form state
    form,
    // layout infos
    layout
  };
};

// node_modules/@strapi/content-manager/dist/admin/hooks/useDocumentLayout.mjs
var RESOLVED_LAYOUT_CACHE_LIMIT = 25;
var resolvedLayoutCache = [];
var getCachedResolvedLayouts = ({ components, data, model, schema, schemas }) => {
  var _a;
  return (_a = resolvedLayoutCache.find((entry) => entry.components === components && entry.data === data && entry.model === model && entry.schema === schema && entry.schemas === schemas)) == null ? void 0 : _a.value;
};
var setCachedResolvedLayouts = (entry) => {
  resolvedLayoutCache.unshift(entry);
  if (resolvedLayoutCache.length > RESOLVED_LAYOUT_CACHE_LIMIT) {
    resolvedLayoutCache.pop();
  }
};
var DEFAULT_SETTINGS = {
  bulkable: false,
  filterable: false,
  searchable: false,
  pagination: false,
  defaultSortBy: "",
  defaultSortOrder: "asc",
  mainField: "id",
  pageSize: 10,
  relationOpenMode: "modal"
};
var useDocumentLayout = (model) => {
  var _a;
  const { schema, components } = useDocument({
    model,
    collectionType: ""
  }, {
    skip: true
  });
  const [{ query }] = useQueryParams();
  const runHookWaterfall = useStrapiApp("useDocumentLayout", (state) => state.runHookWaterfall);
  const { toggleNotification } = useNotification();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const { isLoading: isLoadingSchemas, schemas } = useContentTypeSchema();
  const { currentData: data, isLoading: isLoadingConfigs, error } = useGetContentTypeConfigurationQuery(model);
  const isConfigResolvedForModel = ((_a = data == null ? void 0 : data.contentType) == null ? void 0 : _a.uid) === model;
  const isLoading = isLoadingSchemas || isLoadingConfigs || !error && !isConfigResolvedForModel;
  const stableLayoutsByModelRef = React3.useRef(/* @__PURE__ */ new Map());
  React3.useEffect(() => {
    if (error) {
      toggleNotification({
        type: "danger",
        message: formatAPIError(error)
      });
    }
  }, [
    error,
    formatAPIError,
    toggleNotification
  ]);
  const resolvedLayouts = React3.useMemo(() => {
    if (!data || isLoading) {
      return null;
    }
    const cachedLayouts = getCachedResolvedLayouts({
      components,
      data,
      model,
      schema,
      schemas
    });
    if (cachedLayouts) {
      return cachedLayouts;
    }
    const normalizedData = normalizeContentManagerLayout(data, {
      schemas,
      schema,
      components
    });
    const edit2 = formatEditLayout(normalizedData, {
      schemas,
      schema,
      components
    });
    const list = formatListLayout(normalizedData, {
      schemas,
      schema,
      components
    });
    const listViewConversionContext2 = {
      componentConfigurations: normalizedData.components,
      componentSchemas: components,
      contentTypeSchemas: schemas
    };
    const layouts = {
      edit: edit2,
      list,
      listViewConversionContext: listViewConversionContext2
    };
    setCachedResolvedLayouts({
      components,
      data,
      model,
      schema,
      schemas,
      value: layouts
    });
    return layouts;
  }, [
    data,
    isLoading,
    model,
    schemas,
    schema,
    components
  ]);
  React3.useEffect(() => {
    if (resolvedLayouts) {
      stableLayoutsByModelRef.current.set(model, resolvedLayouts);
    }
  }, [
    model,
    resolvedLayouts
  ]);
  const stableLayouts = resolvedLayouts ?? stableLayoutsByModelRef.current.get(model) ?? null;
  const editLayout = React3.useMemo(() => (stableLayouts == null ? void 0 : stableLayouts.edit) ?? {
    layout: [],
    components: {},
    metadatas: {},
    options: {},
    settings: DEFAULT_SETTINGS
  }, [
    stableLayouts
  ]);
  const listLayout = React3.useMemo(() => (stableLayouts == null ? void 0 : stableLayouts.list) ?? {
    layout: [],
    metadatas: {},
    options: {},
    settings: DEFAULT_SETTINGS
  }, [
    stableLayouts
  ]);
  const { layout: edit } = React3.useMemo(() => runHookWaterfall(HOOKS.MUTATE_EDIT_VIEW_LAYOUT, {
    layout: editLayout,
    query
  }), [
    editLayout,
    query,
    runHookWaterfall
  ]);
  const listViewConversionContext = (stableLayouts == null ? void 0 : stableLayouts.listViewConversionContext) ?? null;
  return {
    error,
    isLoading,
    edit,
    list: listLayout,
    listViewConversionContext
  };
};
var useDocLayout = () => {
  const { model } = useDoc();
  return useDocumentLayout(model);
};
var formatEditLayout = (data, { schemas, schema, components }) => {
  var _a;
  let currentPanelIndex = 0;
  const panelledEditAttributes = convertEditLayoutToFieldLayouts(data.contentType.layouts.edit, schema == null ? void 0 : schema.attributes, data.contentType.metadatas, {
    configurations: data.components,
    schemas: components
  }, schemas).reduce((panels, row) => {
    if (row.some((field) => field.type === "dynamiczone")) {
      panels.push([
        row
      ]);
      currentPanelIndex += 2;
    } else {
      if (!panels[currentPanelIndex]) {
        panels.push([
          row
        ]);
      } else {
        panels[currentPanelIndex].push(row);
      }
    }
    return panels;
  }, []);
  const componentEditAttributes = Object.entries(data.components).reduce((acc, [uid, configuration]) => {
    const componentSchema = components[uid];
    if (!componentSchema) {
      return acc;
    }
    acc[uid] = {
      layout: convertEditLayoutToFieldLayouts(configuration.layouts.edit, componentSchema.attributes, configuration.metadatas, {
        configurations: data.components,
        schemas: components
      }),
      settings: {
        ...configuration.settings,
        icon: componentSchema.info.icon,
        displayName: componentSchema.info.displayName
      }
    };
    return acc;
  }, {});
  const editMetadatas = Object.entries(data.contentType.metadatas).reduce((acc, [attribute, metadata]) => {
    return {
      ...acc,
      [attribute]: metadata.edit
    };
  }, {});
  return {
    layout: panelledEditAttributes,
    components: componentEditAttributes,
    metadatas: editMetadatas,
    settings: {
      ...data.contentType.settings,
      displayName: (_a = schema == null ? void 0 : schema.info) == null ? void 0 : _a.displayName
    },
    options: {
      ...schema == null ? void 0 : schema.options,
      ...schema == null ? void 0 : schema.pluginOptions,
      ...data.contentType.options
    }
  };
};
var convertEditLayoutToFieldLayouts = (rows, attributes = {}, metadatas, components, schemas = []) => {
  return rows.map((row) => row.map((field) => {
    var _a;
    const attribute = attributes[field.name];
    if (!attribute) {
      return null;
    }
    const fieldMetadata = metadatas[field.name];
    if (!fieldMetadata) {
      return null;
    }
    const { edit: metadata } = fieldMetadata;
    const settings = attribute.type === "component" && components ? ((_a = components.configurations[attribute.component]) == null ? void 0 : _a.settings) ?? {} : {};
    return {
      attribute,
      disabled: !metadata.editable,
      hint: metadata.description,
      label: metadata.label ?? "",
      name: field.name,
      // @ts-expect-error – mainField does exist on the metadata for a relation.
      mainField: getMainField(attribute, metadata.mainField || settings.mainField, {
        schemas,
        components: (components == null ? void 0 : components.schemas) ?? {}
      }),
      placeholder: metadata.placeholder ?? "",
      required: attribute.required ?? false,
      size: field.size,
      unique: "unique" in attribute ? attribute.unique : false,
      visible: metadata.visible ?? true,
      type: attribute.type
    };
  }).filter((field) => field !== null));
};
var formatListLayout = (data, { schemas, schema, components }) => {
  var _a;
  const listMetadatas = Object.entries(data.contentType.metadatas).reduce((acc, [attribute, metadata]) => {
    return {
      ...acc,
      [attribute]: metadata.list
    };
  }, {});
  const listAttributes = convertListLayoutToFieldLayouts(data.contentType.layouts.list, schema == null ? void 0 : schema.attributes, listMetadatas, {
    configurations: data.components,
    schemas: components
  }, schemas);
  return {
    layout: listAttributes,
    settings: {
      ...data.contentType.settings,
      displayName: (_a = schema == null ? void 0 : schema.info) == null ? void 0 : _a.displayName
    },
    metadatas: listMetadatas,
    options: {
      ...schema == null ? void 0 : schema.options,
      ...schema == null ? void 0 : schema.pluginOptions,
      ...data.contentType.options
    }
  };
};
var convertListLayoutToFieldLayouts = (columns, attributes = {}, metadatas, components, schemas = []) => {
  return columns.map((name) => {
    var _a;
    const attribute = attributes[name];
    if (!attribute) {
      return null;
    }
    const metadata = metadatas[name];
    if (!metadata) {
      return null;
    }
    const settings = attribute.type === "component" && components ? ((_a = components.configurations[attribute.component]) == null ? void 0 : _a.settings) ?? {} : {};
    return {
      attribute,
      label: metadata.label ?? "",
      mainField: getMainField(attribute, metadata.mainField || settings.mainField, {
        schemas,
        components: (components == null ? void 0 : components.schemas) ?? {}
      }),
      name,
      searchable: metadata.searchable ?? true,
      sortable: metadata.sortable ?? true
    };
  }).filter((field) => field !== null);
};

// node_modules/@strapi/content-manager/dist/admin/utils/objects.mjs
var import_clone = __toESM(require_clone(), 1);
var import_toPath = __toESM(require_toPath(), 1);
var isPathContainer = (obj) => isObject(obj) || Array.isArray(obj);
var isPathTraversable = (obj) => {
  if (obj === null || obj === void 0) {
    return false;
  }
  if (typeof obj === "boolean") {
    return obj === true;
  }
  if (typeof obj === "number") {
    return obj !== 0 && Number.isNaN(obj) === false;
  }
  if (typeof obj === "bigint") {
    return obj !== 0n;
  }
  if (typeof obj === "string") {
    return obj !== "";
  }
  return true;
};
var getPathValue = (container, key) => Object(container)[key];
var setPathValue = (container, key, value) => {
  container[key] = value;
};
var deletePathValue = (container, key) => {
  delete container[key];
};
function getIn(obj, key, def, pathStartIndex = 0) {
  const path = (0, import_toPath.default)(key);
  let currentValue = obj;
  while (isPathTraversable(currentValue) && pathStartIndex < path.length) {
    currentValue = getPathValue(currentValue, path[pathStartIndex++]);
  }
  if (pathStartIndex !== path.length && isPathTraversable(currentValue) === false) {
    return def;
  }
  return currentValue === void 0 ? def : currentValue;
}
var isObject = (obj) => obj !== null && typeof obj === "object" && !Array.isArray(obj);
var isInteger = (obj) => String(Math.floor(Number(obj))) === obj;
function setIn(obj, path, value) {
  const res = (0, import_clone.default)(obj);
  let resVal = res;
  let i = 0;
  const pathArray = (0, import_toPath.default)(path);
  for (; i < pathArray.length - 1; i++) {
    const currentPath = pathArray[i];
    const currentObj = getIn(obj, pathArray.slice(0, i + 1));
    if (isObject(currentObj) || Array.isArray(currentObj)) {
      const clonedValue = (0, import_clone.default)(currentObj);
      setPathValue(resVal, currentPath, clonedValue);
      resVal = clonedValue;
    } else {
      const nextPath = pathArray[i + 1];
      const nextValue = isInteger(nextPath) && Number(nextPath) >= 0 ? [] : {};
      setPathValue(resVal, currentPath, nextValue);
      resVal = nextValue;
    }
  }
  const currentValue = isPathContainer(obj) && i === 0 ? getPathValue(obj, pathArray[i]) : getPathValue(resVal, pathArray[i]);
  if (currentValue === value) {
    return obj;
  }
  if (value === void 0) {
    deletePathValue(resVal, pathArray[i]);
  } else {
    setPathValue(resVal, pathArray[i], value);
  }
  if (i === 0 && value === void 0) {
    deletePathValue(res, pathArray[i]);
  }
  return res;
}

export {
  CREATED_BY_ATTRIBUTE_NAME,
  UPDATED_BY_ATTRIBUTE_NAME,
  CREATOR_FIELDS,
  PUBLISHED_BY_ATTRIBUTE_NAME,
  CREATED_AT_ATTRIBUTE_NAME,
  UPDATED_AT_ATTRIBUTE_NAME,
  PUBLISHED_AT_ATTRIBUTE_NAME,
  ATTRIBUTE_TYPES_THAT_CANNOT_BE_MAIN_FIELD,
  SINGLE_TYPES,
  COLLECTION_TYPES,
  getDirectParent,
  prepareTempKeys,
  removeFieldsThatDontExistOnSchema,
  transformDocument,
  handleInvisibleAttributes,
  createDefaultForm,
  useAutoCloneDocumentMutation,
  useCloneDocumentMutation,
  useCreateDocumentMutation,
  useDeleteDocumentMutation,
  useDeleteManyDocumentsMutation,
  useDiscardDocumentMutation,
  useGetAllDocumentsQuery,
  useGetDocumentsForValidationQuery,
  useLazyGetDocumentQuery,
  useLazyGetDraftRelationCountQuery,
  useGetManyDraftRelationCountQuery,
  usePublishDocumentMutation,
  usePublishManyDocumentsMutation,
  useUpdateDocumentMutation,
  useUnpublishDocumentMutation,
  useUnpublishManyDocumentsMutation,
  buildValidParams,
  isBaseQueryError,
  createYupSchema,
  useGetInitialDataQuery,
  useContentTypeSchema,
  extractContentTypeComponents,
  HOOKS,
  normalizeComponentConfigurationResponse,
  normalizeComponentConfigurationUpdateResponse,
  normalizeComponentConfigurationLayout,
  useGetContentTypeConfigurationQuery,
  useGetAllContentTypeSettingsQuery,
  useUpdateContentTypeConfigurationMutation,
  checkIfAttributeIsDisplayable,
  getMainField,
  DEFAULT_SETTINGS,
  useDocumentLayout,
  useDocLayout,
  convertEditLayoutToFieldLayouts,
  convertListLayoutToFieldLayouts,
  useDocument,
  useDoc,
  useContentManagerContext,
  getIn,
  isObject,
  setIn
};
//# sourceMappingURL=chunk-JPBDOX5J.js.map
