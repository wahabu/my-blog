const toFileDraggableId = (id)=>`file:${id}`;
const toFolderDraggableId = (id)=>`folder:${id}`;
const toFolderTargetId = (id)=>`folder-target:${id}`;
const parseFolderTargetId = (dndId)=>{
    if (typeof dndId !== 'string') {
        return null;
    }
    const match = /^folder-target:(\d+)$/.exec(dndId);
    return match ? Number(match[1]) : null;
};
const toFolderTreeTargetId = (id)=>`folder-tree-target:${id}`;
const HOME_TREE_TARGET_ID = 'folder-tree-target:home';
const parseFolderTreeTargetId = (dndId)=>{
    if (typeof dndId !== 'string') {
        return null;
    }
    if (dndId === HOME_TREE_TARGET_ID) {
        return 'root';
    }
    const match = /^folder-tree-target:(\d+)$/.exec(dndId);
    return match ? Number(match[1]) : null;
};

export { HOME_TREE_TARGET_ID, parseFolderTargetId, parseFolderTreeTargetId, toFileDraggableId, toFolderDraggableId, toFolderTargetId, toFolderTreeTargetId };
//# sourceMappingURL=dndIds.mjs.map
