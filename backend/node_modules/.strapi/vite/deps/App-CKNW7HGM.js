import {
  DndContext,
  DragOverlay,
  PointerSensor,
  pointerWithin,
  useDndContext,
  useDraggable,
  useDroppable,
  useSensor,
  useSensors
} from "./chunk-GY65XFZI.js";
import {
  Drawer,
  TruncatedText,
  formatBytes,
  getFileExtension,
  getTranslationKey,
  prefixFileUrlWithBackendUrl,
  selectCompletedUploads,
  uploadApi,
  useGenerateAiMetadataMutation,
  useTypedSelector,
  useUploadFileSilentlyMutation,
  useUploadFilesMutation,
  useUploadFromUrlsMutation,
  validateUrls
} from "./chunk-Z3ONVEQG.js";
import {
  ASSET_TYPES,
  typeFromMime
} from "./chunk-RMQCWWFZ.js";
import {
  PERMISSIONS
} from "./chunk-GEAZSL6W.js";
import {
  Item2,
  Root2
} from "./chunk-OIM6YED5.js";
import {
  useDebounce
} from "./chunk-SG3YXALT.js";
import {
  SubNav
} from "./chunk-LVMHP6GO.js";
import "./chunk-7FPOP2S5.js";
import "./chunk-Q535U6BT.js";
import "./chunk-D7FI3GAF.js";
import {
  getDisplayName
} from "./chunk-4R2QA5NM.js";
import "./chunk-TKXHHSNW.js";
import "./chunk-XTTXQ7FR.js";
import "./chunk-QHCTYJ6K.js";
import "./chunk-TKZBDVP5.js";
import {
  useClipboard
} from "./chunk-LS2USQHL.js";
import {
  useAIAvailability,
  usePersistentState
} from "./chunk-K4JPZJYK.js";
import "./chunk-PA2ESAGW.js";
import "./chunk-SLUE2XUX.js";
import "./chunk-HRURINY4.js";
import "./chunk-JES3NNDO.js";
import "./chunk-F3Y2U5J7.js";
import "./chunk-PUECFR7A.js";
import "./chunk-WVMUAD6S.js";
import "./chunk-BTNRU3I6.js";
import "./chunk-YZKIGY5W.js";
import "./chunk-XNMTWOF7.js";
import "./chunk-NCYXOGJJ.js";
import "./chunk-IFOFBKTA.js";
import "./chunk-AOU4ZZD3.js";
import "./chunk-SOVNMIKL.js";
import {
  Blocker,
  Form,
  useField,
  useForm
} from "./chunk-2IWTCEYS.js";
import {
  useRBAC
} from "./chunk-SHEU3VT4.js";
import {
  Layouts,
  useElementOnScreen
} from "./chunk-767BZMIR.js";
import {
  useIsDesktop,
  useIsMobile
} from "./chunk-A274YVTO.js";
import "./chunk-L4VZK4CF.js";
import {
  useTracking
} from "./chunk-QXVVXECA.js";
import "./chunk-6KQUGTAR.js";
import {
  Page
} from "./chunk-WZNNPD7U.js";
import {
  useQueryParams,
  withEncodedUserParams
} from "./chunk-HHY6TNZ5.js";
import {
  useNotification
} from "./chunk-52FV5OAT.js";
import "./chunk-NE2H5B4K.js";
import "./chunk-ALJOKJHY.js";
import "./chunk-ZISMY363.js";
import "./chunk-FH7EDXAD.js";
import "./chunk-TVYQEFKB.js";
import "./chunk-T5ATI4SA.js";
import "./chunk-MSCD7MZY.js";
import "./chunk-MBK4V2X7.js";
import "./chunk-BC4MOUEJ.js";
import "./chunk-JRCQYP7F.js";
import "./chunk-36WB3I5J.js";
import "./chunk-45QSVGD5.js";
import "./chunk-KS6HIU44.js";
import "./chunk-D4WYVNVM.js";
import "./chunk-MMOBCIZG.js";
import "./chunk-OG6FDUVQ.js";
import "./chunk-A6QRMII5.js";
import "./chunk-3DRZIGZ7.js";
import "./chunk-AJ6CDECK.js";
import "./chunk-YULRRBBU.js";
import "./chunk-KVWVHPCR.js";
import {
  K
} from "./chunk-Y5S667H6.js";
import "./chunk-OJ6RZTLV.js";
import "./chunk-NZZAX2AB.js";
import "./chunk-T7T43G5B.js";
import "./chunk-NR6XEPE4.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import "./chunk-HAUWLGB7.js";
import "./chunk-3MST3H25.js";
import {
  Route,
  Routes
} from "./chunk-QBM2MGBP.js";
import {
  useDispatch,
  useStore
} from "./chunk-AHJOALQ2.js";
import "./chunk-U5ASKYVL.js";
import "./chunk-QQEUXDAQ.js";
import "./chunk-LHXIEHKN.js";
import {
  $e,
  $u,
  A,
  Bt,
  C,
  Cd,
  Ct,
  Dd,
  Dn,
  Hl,
  Ld,
  Mt,
  Nr,
  Pc,
  Pd,
  Ql,
  S,
  Ud,
  Ul,
  Vl,
  Xd,
  Yl,
  _d,
  _l,
  be,
  bu,
  cr,
  dr,
  gd,
  jl,
  kt,
  md,
  pd,
  ti,
  ud,
  un,
  xd,
  yu
} from "./chunk-7ZODKSNG.js";
import "./chunk-SXAHFOGA.js";
import "./chunk-6I7FUUCQ.js";
import "./chunk-JDJCDU5Q.js";
import {
  B3,
  F5,
  I2,
  In,
  J5,
  Jn,
  K5,
  N2,
  N5,
  O2,
  Q5,
  R3,
  S5,
  Vn,
  W2,
  W3,
  X5,
  Y5,
  Z2,
  Z5,
  _5,
  d3,
  g3,
  h5,
  i5,
  j5,
  nn,
  o5,
  r3,
  t3,
  v5,
  wn,
  z5
} from "./chunk-D4B4CG5U.js";
import "./chunk-VQHBTUHH.js";
import "./chunk-PC7RLLVI.js";
import "./chunk-AHKO7ZVV.js";
import "./chunk-JCO3CRJF.js";
import "./chunk-HHU3PYNH.js";
import "./chunk-OYIDWRUX.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  ft,
  gt,
  st
} from "./chunk-4N2AQQ2K.js";
import "./chunk-4MZT7456.js";
import "./chunk-QBO3FZHQ.js";
import "./chunk-KKBG5U5K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/upload/dist/admin/future/App.mjs
var import_jsx_runtime27 = __toESM(require_jsx_runtime(), 1);
var import_react24 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/AssetsPage.mjs
var import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
var import_react23 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/components/BetaNotice.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var BetaNotice = () => {
  const { formatMessage } = useIntl();
  const [isBetaNoticeDismissed, setIsBetaNoticeDismissed] = usePersistentState("STRAPI_UPLOAD_LIBRARY_BETA_NOTICE_DISMISSED", false);
  if (isBetaNoticeDismissed) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(S, {
    paddingBottom: 4,
    children: (0, import_jsx_runtime.jsx)(ud, {
      variant: "default",
      onClose: () => setIsBetaNoticeDismissed(true),
      closeLabel: formatMessage({
        id: getTranslationKey("beta.notice.close"),
        defaultMessage: "Close"
      }),
      title: formatMessage({
        id: getTranslationKey("plugin.name"),
        defaultMessage: "Media Library"
      }),
      children: (0, import_jsx_runtime.jsxs)(C, {
        tag: "span",
        gap: 2,
        alignItems: "center",
        children: [
          (0, import_jsx_runtime.jsx)(S, {
            tag: "span",
            background: "neutral150",
            hasRadius: true,
            paddingLeft: 2,
            paddingRight: 2,
            shrink: 0,
            children: (0, import_jsx_runtime.jsx)(A, {
              variant: "sigma",
              textColor: "neutral600",
              children: formatMessage({
                id: getTranslationKey("beta.badge"),
                defaultMessage: "Beta"
              })
            })
          }),
          (0, import_jsx_runtime.jsx)(A, {
            tag: "span",
            children: formatMessage({
              id: getTranslationKey("beta.notice.content"),
              defaultMessage: "This is a beta version of the Media Library. Some features are still in progress — please report any issue you run into."
            })
          })
        ]
      })
    })
  });
};

// node_modules/@strapi/upload/dist/admin/shared/constants.mjs
var AI_METADATA_SUPPORTED_IMAGE_TYPES = [
  "image/png",
  "image/jpeg",
  "image/webp",
  "image/heic",
  "image/heif"
];
var isAIMetadataSupportedMime = (mime) => AI_METADATA_SUPPORTED_IMAGE_TYPES.includes(mime);
var AI_METADATA_CHUNK_SIZE = 20;
var AI_METADATA_MAX_FILES = AI_METADATA_CHUNK_SIZE * 2;

// node_modules/@strapi/upload/dist/admin/future/services/settings.mjs
var settingsApi = uploadApi.injectEndpoints({
  endpoints: (builder) => ({
    // Deliberately NOT named `getSettings`. Every plugin's admin API is built on
    // the same shared `adminApi` instance — one endpoint registry keyed by name,
    // under `reducerPath: 'adminApi'` — and `injectEndpoints` silently keeps the
    // FIRST registration of a duplicate name and discards later ones. The i18n
    // plugin already registers a `getSettings`, so a generic name here would,
    // depending on module load order, resolve this hook to i18n's cache entry
    // (`/i18n/settings`) instead of `/upload/settings` — non-deterministically.
    getUploadSettings: builder.query({
      query: () => ({
        url: "/upload/settings",
        method: "GET"
      })
    })
  })
});
var { useGetUploadSettingsQuery } = settingsApi;

// node_modules/@strapi/upload/dist/admin/future/hooks/useAIMetadataEnabled.mjs
var useAIMetadataEnabled = (options) => {
  var _a;
  const isAIAvailable = useAIAvailability();
  const { data: settings } = useGetUploadSettingsQuery();
  if (!isAIAvailable || !(((_a = settings == null ? void 0 : settings.data) == null ? void 0 : _a.aiMetadata) ?? false)) {
    return false;
  }
  return options === void 0 ? true : isAIMetadataSupportedMime(options.mime);
};

// node_modules/@strapi/upload/dist/admin/future/hooks/useMediaLibraryPermissions.mjs
var { main: _main, ...RBAC_PERMISSIONS } = PERMISSIONS;
var useMediaLibraryPermissions = () => {
  const { allowedActions, isLoading } = useRBAC(RBAC_PERMISSIONS);
  return {
    isLoading,
    canCreate: Boolean(allowedActions.canCreate),
    canUpdate: Boolean(allowedActions.canUpdate),
    canDownload: Boolean(allowedActions.canDownload),
    canCopyLink: Boolean(allowedActions.canCopyLink)
  };
};

// node_modules/@strapi/upload/dist/admin/future/hooks/useTracking.mjs
var import_react = __toESM(require_react(), 1);
var MEDIA_LIBRARY_VERSION = "v2";
var MEDIA_LIBRARY_LOCATION = "upload";
var useTracking2 = () => {
  const { trackUsage: trackStrapiUsage } = useTracking();
  const { data } = useGetUploadSettingsQuery();
  const isAiAvailable = useAIAvailability();
  const trackUsage = (0, import_react.useCallback)((event, properties) => {
    var _a;
    return trackStrapiUsage(event, {
      ...properties,
      ...isAiAvailable ? {
        isAiMediaLibraryConfigured: Boolean((_a = data == null ? void 0 : data.data) == null ? void 0 : _a.aiMetadata)
      } : {},
      mediaLibraryVersion: MEDIA_LIBRARY_VERSION
    });
  }, [
    trackStrapiUsage,
    isAiAvailable,
    data
  ]);
  return {
    trackUsage
  };
};

// node_modules/@strapi/upload/dist/admin/future/utils/searchQueryParam.mjs
var encodeSearchQuery = (value) => encodeURIComponent(value).replace(/\+/g, "%2B");

// node_modules/@strapi/upload/dist/admin/future/services/folders.mjs
var isDataEnvelope = (response) => typeof response === "object" && response !== null && "data" in response;
var unwrapData = (response) => isDataEnvelope(response) ? response.data : response;
var foldersApi = uploadApi.injectEndpoints({
  endpoints: (builder) => ({
    getFolders: builder.query({
      query: (params = {}) => {
        const { parentId, sort, search, filters = [] } = params;
        const queryParams = {
          // Default matches sidebar FolderTree order (server getStructure uses sortBy('name')).
          sort: sort ?? "name:ASC",
          populate: {
            parent: true
          }
        };
        if (search) {
          queryParams["_q"] = encodeSearchQuery(search);
          if (filters.length > 0) {
            queryParams["filters"] = {
              $and: [
                ...filters
              ]
            };
          }
        } else {
          const parentScope = parentId != null ? {
            parent: {
              id: parentId
            }
          } : {
            parent: {
              id: {
                $null: true
              }
            }
          };
          queryParams["filters"] = {
            $and: [
              parentScope,
              ...filters
            ]
          };
        }
        return {
          url: "/upload/folders",
          method: "GET",
          config: {
            params: queryParams
          }
        };
      },
      transformResponse: (response) => unwrapData(response),
      providesTags: (results) => {
        if (results) {
          return [
            ...results.map(({ id }) => ({
              type: "Folder",
              id
            })),
            {
              type: "Folder",
              id: "LIST"
            }
          ];
        }
        return [
          {
            type: "Folder",
            id: "LIST"
          }
        ];
      }
    }),
    createFolder: builder.mutation({
      query: (body) => ({
        url: "/upload/folders",
        method: "POST",
        data: body
      }),
      transformResponse: (response) => response.data,
      invalidatesTags: [
        {
          type: "Folder",
          id: "LIST"
        },
        {
          type: "Folder",
          id: "STRUCTURE"
        }
      ]
    }),
    updateFolder: builder.mutation({
      query: ({ id, ...body }) => ({
        url: `/upload/folders/${id}`,
        method: "PUT",
        data: body
      }),
      transformResponse: (response) => response.data,
      invalidatesTags: (_result, _error, { id }) => [
        {
          type: "Folder",
          id
        },
        {
          type: "Folder",
          id: "LIST"
        },
        {
          type: "Folder",
          id: "STRUCTURE"
        }
      ]
    }),
    /**
    * Hierarchical folder tree used by the FolderTree sidebar. Returned as a
    * single nested array — the server walks the folders table once and
    * assembles parent/child relationships.
    *
    * TODO: filter results like `admin-folder.find` for folder-scoped roles
    * (v1 returns all folders; relies on global `plugin::upload.read`).
    */
    getFolderStructure: builder.query({
      query: () => ({
        url: "/upload/folder-structure",
        method: "GET"
      }),
      // TODO: align GetFolderStructure contract with the real /upload/folder-structure
      // envelope and drop this defensive unwrap (data[] vs { data: [] }).
      transformResponse: (response) => (response == null ? void 0 : response.data) ?? response ?? [],
      providesTags: [
        {
          type: "Folder",
          id: "STRUCTURE"
        }
      ]
    }),
    /**
    * Flat list of every folder, used to populate the "Location" select in the
    * asset details drawer. No parent filter — we want the entire tree in one
    * query so the select can render any destination folder.
    */
    getAllFolders: builder.query({
      query: () => ({
        url: "/upload/folders",
        method: "GET"
      }),
      transformResponse: (response) => unwrapData(response ?? []),
      providesTags: (results) => {
        if (results) {
          return [
            ...results.map(({ id }) => ({
              type: "Folder",
              id
            })),
            {
              type: "Folder",
              id: "LIST"
            }
          ];
        }
        return [
          {
            type: "Folder",
            id: "LIST"
          }
        ];
      }
    }),
    getFolder: builder.query({
      query: ({ id }) => ({
        url: `/upload/folders/${id}`,
        method: "GET",
        config: {
          params: {
            populate: {
              parent: {
                populate: {
                  parent: "*"
                }
              },
              children: {
                count: true
              },
              files: {
                count: true
              }
            }
          }
        }
      }),
      transformResponse: (response) => response.data,
      // Also carries the LIST tag so a mutation that changes a folder's file
      // count (upload / delete into it) can refresh the header count by
      // invalidating `{ Folder, LIST }`, without needing the folder id at the
      // mutation site (it's buried in the upload FormData / not passed to the
      // delete).
      providesTags: (_result, _error, { id }) => [
        {
          type: "Folder",
          id
        },
        {
          type: "Folder",
          id: "LIST"
        }
      ]
    }),
    bulkMove: builder.mutation({
      query: ({ fileIds = [], folderIds = [], destinationFolderId }) => ({
        url: "/upload/actions/bulk-move",
        method: "POST",
        data: {
          fileIds,
          folderIds,
          destinationFolderId
        }
      }),
      transformResponse: (response) => response.data,
      invalidatesTags: [
        {
          type: "Asset",
          id: "LIST"
        },
        {
          type: "Folder",
          id: "LIST"
        },
        {
          type: "Folder",
          id: "STRUCTURE"
        }
      ]
    })
  })
});
var { useCreateFolderMutation, useUpdateFolderMutation, useGetFoldersQuery, useGetFolderQuery, useGetAllFoldersQuery, useGetFolderStructureQuery, useBulkMoveMutation } = foldersApi;

// node_modules/@strapi/upload/dist/admin/future/utils/getRelationId.mjs
var getRelationId = (relation) => {
  if (relation == null) {
    return null;
  }
  if (typeof relation === "object") {
    return relation.id ?? null;
  }
  if (typeof relation === "number") {
    return relation;
  }
  return Number(relation) || null;
};

// node_modules/@strapi/upload/dist/admin/future/utils/itemLocations.mjs
var emptyItemLocations = {
  fileFolderId: () => void 0,
  folderParentId: () => void 0
};
var buildItemLocations = (assets, folders) => {
  const fileFolderIds = /* @__PURE__ */ new Map();
  const folderParentIds = /* @__PURE__ */ new Map();
  assets.forEach((asset) => {
    fileFolderIds.set(asset.id, getRelationId(asset.folder));
  });
  folders.forEach((folder) => {
    folderParentIds.set(folder.id, getRelationId(folder.parent));
  });
  return {
    fileFolderId: (id) => fileFolderIds.get(id),
    folderParentId: (id) => folderParentIds.get(id)
  };
};
var locateItem = (locations, kind, id, fallbackFolderId) => {
  const located = kind === "file" ? locations.fileFolderId(id) : locations.folderParentId(id);
  return located === void 0 ? fallbackFolderId : located;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetDetailsDrawer.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var React5 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/hooks/useApiErrorMessage.mjs
var React = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/utils/getApiErrorMessage.mjs
var getApiErrorMessage = (error) => {
  if (!error || typeof error !== "object") {
    return void 0;
  }
  const { message } = error;
  return typeof message === "string" && message.length > 0 ? message : void 0;
};

// node_modules/@strapi/upload/dist/admin/future/hooks/useApiErrorMessage.mjs
var useApiErrorMessage = () => {
  const { formatMessage, messages } = useIntl();
  return React.useCallback((error, fallback) => {
    const message = getApiErrorMessage(error);
    if (!message) {
      return fallback;
    }
    const id = getTranslationKey(`apiError.${message}`);
    return messages[id] ? formatMessage({
      id
    }) : message;
  }, [
    formatMessage,
    messages
  ]);
};

// node_modules/@strapi/upload/dist/admin/future/services/assets.mjs
var assetsApi = uploadApi.injectEndpoints({
  endpoints: (builder) => ({
    getAssets: builder.query({
      query: (params = {}) => {
        const { folder, search, filters = [], ...rest } = params;
        const queryParams = {
          ...rest
        };
        if (search) {
          queryParams["_q"] = encodeSearchQuery(search);
          if (filters.length > 0) {
            queryParams["filters"] = {
              $and: [
                ...filters
              ]
            };
          }
        } else {
          const folderScope = folder != null ? {
            folder: {
              id: folder
            }
          } : {
            folder: {
              id: {
                $null: true
              }
            }
          };
          queryParams["filters"] = {
            $and: [
              folderScope,
              ...filters
            ]
          };
        }
        return {
          url: "/upload/files",
          method: "GET",
          config: {
            params: queryParams
          }
        };
      },
      transformResponse: (response) => response,
      providesTags: (result) => result ? [
        ...result.results.map(({ id }) => ({
          type: "Asset",
          id
        })),
        {
          type: "Asset",
          id: "LIST"
        }
      ] : [
        {
          type: "Asset",
          id: "LIST"
        }
      ]
    }),
    getAsset: builder.query({
      query: (id) => ({
        url: `/upload/files/${id}`,
        method: "GET"
      }),
      providesTags: (_result, _error, id) => [
        {
          type: "Asset",
          id
        }
      ]
    }),
    /**
    * Update the editable metadata of an existing asset, through
    * `PUT /upload/files/<id>` (`admin-upload.updateFileInfo`).
    */
    updateAsset: builder.mutation({
      query: ({ id, fileInfo }) => {
        const formData = new FormData();
        formData.append("fileInfo", JSON.stringify(fileInfo));
        return {
          url: `/upload/files/${id}`,
          method: "PUT",
          data: formData
        };
      },
      invalidatesTags: (_result, _error, { id }) => [
        {
          type: "Asset",
          id
        },
        {
          type: "Asset",
          id: "LIST"
        },
        // The Location select routes a folder move through this mutation, which
        // changes both folders' counts — refresh the folder header count.
        {
          type: "Folder",
          id: "LIST"
        }
      ]
    }),
    /**
    * Replace the binary content of an existing asset, through
    * `POST /upload/files/<id>/replace` (`admin-upload.replaceFile`) with a
    * multipart body. Uses the standard axios baseQuery (no streaming) since we
    * only ever replace one file at a time and don't need per-byte progress here.
    */
    replaceAsset: builder.mutation({
      query: ({ id, file, fileInfo }) => {
        const formData = new FormData();
        formData.append("files", file);
        if (fileInfo) {
          formData.append("fileInfo", JSON.stringify(fileInfo));
        }
        return {
          url: `/upload/files/${id}/replace`,
          method: "POST",
          data: formData
        };
      },
      invalidatesTags: (_result, _error, { id }) => [
        {
          type: "Asset",
          id
        },
        {
          type: "Asset",
          id: "LIST"
        }
      ]
    }),
    /**
    * Permanently delete an asset by id. Hits the same endpoint as the legacy
    * `useRemoveAsset` hook so server behaviour is unchanged.
    */
    deleteAsset: builder.mutation({
      query: (id) => ({
        url: `/upload/files/${id}`,
        method: "DELETE"
      }),
      invalidatesTags: (_result, _error, id) => [
        {
          type: "Asset",
          id
        },
        {
          type: "Asset",
          id: "LIST"
        },
        // Refresh the folder header count — deleting an asset changes it.
        {
          type: "Folder",
          id: "LIST"
        }
      ]
    }),
    /**
    * Permanently delete several assets and/or folders in one request. Same
    * endpoint as the legacy bulk remove (`POST /upload/actions/bulk-delete`,
    * handled by `admin-folder-file.deleteMany`) so server behaviour is
    * unchanged — folder deletion cascades to everything inside.
    */
    bulkDeleteItems: builder.mutation({
      query: ({ fileIds, folderIds }) => ({
        url: "/upload/actions/bulk-delete",
        method: "POST",
        data: {
          fileIds,
          folderIds
        }
      }),
      invalidatesTags: [
        {
          type: "Asset",
          id: "LIST"
        },
        {
          type: "Folder",
          id: "LIST"
        },
        // The sidebar FolderTree reads /upload/folder-structure, which carries
        // its own tag — without it, deleted folders linger in the tree.
        {
          type: "Folder",
          id: "STRUCTURE"
        }
      ]
    })
  })
});
var { useGetAssetsQuery, useGetAssetQuery, useUpdateAssetMutation, useReplaceAssetMutation, useDeleteAssetMutation, useBulkDeleteItemsMutation } = assetsApi;

// node_modules/@strapi/upload/dist/admin/future/utils/downloadFile.mjs
var downloadFile = async (url, fileName) => {
  const response = await fetch(url);
  const blob = await response.blob();
  const objectUrl = window.URL.createObjectURL(blob);
  const anchor = document.createElement("a");
  anchor.href = objectUrl;
  anchor.setAttribute("download", fileName);
  anchor.click();
  window.URL.revokeObjectURL(objectUrl);
};

// node_modules/@strapi/upload/dist/admin/future/utils/getAssetIcon.mjs
var DOC_ICON_MAP = {
  pdf: j5,
  csv: Q5,
  xls: K5,
  zip: X5
};
var getAssetIcon = (mime, ext) => {
  const fileExtension = getFileExtension(ext);
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Image)) {
    return r3;
  }
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Video)) {
    return wn;
  }
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Audio)) {
    return g3;
  }
  return fileExtension ? DOC_ICON_MAP[fileExtension] || Z5 : Z5;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useFolderInfo.mjs
var useFolderInfo = (currentFolderId) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const { data: currentFolder, isLoading } = useGetFolderQuery({
    id: currentFolderId
  }, {
    skip: currentFolderId === null
  });
  const { data: rootAssetsData, isLoading: isRootAssetsLoading } = useGetAssetsQuery({
    folder: null,
    pageSize: 1
  }, {
    skip: currentFolderId !== null
  });
  const homeLabel = formatMessage({
    id: getTranslationKey("plugin.home"),
    defaultMessage: "Home"
  });
  if (currentFolderId === null) {
    if (isRootAssetsLoading) {
      return {
        title: homeLabel,
        itemCount: 0
      };
    }
    return {
      title: homeLabel,
      itemCount: ((_a = rootAssetsData == null ? void 0 : rootAssetsData.pagination) == null ? void 0 : _a.total) ?? 0
    };
  }
  if (isLoading || !currentFolder) {
    return {
      title: "",
      itemCount: 0
    };
  }
  return {
    title: currentFolder.name,
    itemCount: ((_b = currentFolder.files) == null ? void 0 : _b.count) ?? 0
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useIsAssetDetailsOpen.mjs
var ASSET_DETAILS_URL_PARAM = "assetId";
var parseAssetDetailsId = (raw) => {
  const parsed = raw ? parseInt(raw, 10) : NaN;
  return Number.isNaN(parsed) ? null : parsed;
};
var useIsAssetDetailsOpen = () => {
  const [{ query }] = useQueryParams();
  return parseAssetDetailsId(query == null ? void 0 : query[ASSET_DETAILS_URL_PARAM]) !== null;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/BusyOverlay.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var BusyOverlayRoot = gt(C)`
  position: absolute;
  inset: 0;
  z-index: ${({ $zIndex }) => $zIndex};
  align-items: center;
  justify-content: center;
  background: ${({ theme }) => theme.colors.neutral0};
  opacity: 0.7;
`;
var BusyOverlay = ({ children, zIndex = 20, hideLabel = false }) => (0, import_jsx_runtime2.jsx)(BusyOverlayRoot, {
  $zIndex: zIndex,
  children: (0, import_jsx_runtime2.jsx)(kt, {
    small: hideLabel,
    children
  })
});

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetCropEditor.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var React3 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/useCropImg.mjs
var React2 = __toESM(require_react(), 1);
var QUALITY = 1;
var resolveCornerResize = ({ anchorX, anchorY, point, aspectRatio }) => {
  let width = Math.abs(point.x - anchorX);
  let height = Math.abs(point.y - anchorY);
  if (aspectRatio) {
    if (width / aspectRatio >= height) {
      height = width / aspectRatio;
    } else {
      width = height * aspectRatio;
    }
  }
  const x = point.x < anchorX ? anchorX - width : anchorX;
  const y = point.y < anchorY ? anchorY - height : anchorY;
  return {
    x,
    y,
    width,
    height
  };
};
var useCropImg = () => {
  const [naturalSize, setNaturalSize] = React2.useState({
    width: 0,
    height: 0
  });
  const [crop, setCrop] = React2.useState({
    x: 0,
    y: 0,
    width: 0,
    height: 0
  });
  const [aspectRatio, setAspectRatioState] = React2.useState(null);
  const imageRef = React2.useRef(null);
  const init = React2.useCallback((image) => {
    imageRef.current = image;
    const next = {
      width: image.naturalWidth,
      height: image.naturalHeight
    };
    setNaturalSize(next);
    setCrop({
      x: 0,
      y: 0,
      width: next.width,
      height: next.height
    });
  }, []);
  const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
  const setCropSize = React2.useCallback((next) => {
    setCrop((prev) => {
      const maxW = naturalSize.width - prev.x;
      const maxH = naturalSize.height - prev.y;
      let width = next.width !== void 0 ? clamp(next.width, 1, maxW) : prev.width;
      let height = next.height !== void 0 ? clamp(next.height, 1, maxH) : prev.height;
      if (aspectRatio) {
        if (next.width !== void 0) {
          height = clamp(width / aspectRatio, 1, maxH);
        } else if (next.height !== void 0) {
          width = clamp(height * aspectRatio, 1, maxW);
        }
      }
      return {
        ...prev,
        width,
        height
      };
    });
  }, [
    naturalSize.width,
    naturalSize.height,
    aspectRatio
  ]);
  const setCropPosition = React2.useCallback((next) => {
    setCrop((prev) => {
      const x = next.x !== void 0 ? clamp(next.x, 0, naturalSize.width - prev.width) : prev.x;
      const y = next.y !== void 0 ? clamp(next.y, 0, naturalSize.height - prev.height) : prev.y;
      return {
        ...prev,
        x,
        y
      };
    });
  }, [
    naturalSize.width,
    naturalSize.height
  ]);
  const setAspectRatio = React2.useCallback((ratio) => {
    setAspectRatioState(ratio);
    if (ratio) {
      setCrop((prev) => {
        const maxW = naturalSize.width - prev.x;
        const maxH = naturalSize.height - prev.y;
        let width = prev.width;
        let height = width / ratio;
        if (height > maxH) {
          height = maxH;
          width = height * ratio;
        }
        if (width > maxW) {
          width = maxW;
          height = width / ratio;
        }
        return {
          ...prev,
          width: Math.round(width),
          height: Math.round(height)
        };
      });
    }
  }, [
    naturalSize.width,
    naturalSize.height
  ]);
  const produceFile = React2.useCallback((name, mimeType, lastModifiedDate) => new Promise((resolve, reject) => {
    const image = imageRef.current;
    if (!image) {
      reject(new Error("Image not ready: call init() before produceFile()."));
      return;
    }
    const canvas = document.createElement("canvas");
    canvas.width = Math.max(1, Math.round(crop.width));
    canvas.height = Math.max(1, Math.round(crop.height));
    const context = canvas.getContext("2d");
    if (!context) {
      reject(new Error("Could not get a 2D canvas context to crop the image."));
      return;
    }
    context.drawImage(image, crop.x, crop.y, crop.width, crop.height, 0, 0, canvas.width, canvas.height);
    canvas.toBlob((blob) => {
      if (!blob) {
        reject(new Error("Could not export the cropped image to a blob."));
        return;
      }
      resolve(new File([
        blob
      ], name, {
        type: mimeType,
        lastModified: lastModifiedDate ? new Date(lastModifiedDate).getTime() : Date.now()
      }));
    }, mimeType, QUALITY);
  }), [
    crop.x,
    crop.y,
    crop.width,
    crop.height
  ]);
  return {
    init,
    crop,
    naturalSize,
    aspectRatio,
    setCropSize,
    setCropPosition,
    setAspectRatio,
    produceFile,
    // Round-aware getters for the px inputs.
    width: Math.round(crop.width),
    height: Math.round(crop.height)
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetCropEditor.mjs
var FOCAL_DIAMETER_REM = 5.6;
var HANDLE_PX = 12;
var Overlay = gt(C)`
  position: fixed;
  z-index: 1200;
  flex-direction: column;
  top: ${({ theme }) => theme.spaces[1]};
  left: ${({ theme }) => theme.spaces[1]};
  right: ${({ theme }) => theme.spaces[1]};
  bottom: ${({ theme }) => theme.spaces[1]};
  border-radius: ${({ theme }) => theme.borderRadius};
  border: 1px solid ${({ theme }) => theme.colors.neutral150};
  background: ${({ theme }) => theme.colors.neutral0};
  /* Focused programmatically on open (tabIndex -1) — no visible ring needed. */
  outline: none;
`;
var HeaderBar = gt(C)`
  width: 100%;
  gap: ${({ theme }) => theme.spaces[2]};
  padding: ${({ theme }) => `${theme.spaces[3]} ${theme.spaces[5]}`};
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
  background: ${({ theme }) => theme.colors.neutral0};
`;
var Body = gt(S)`
  width: 100%;
  position: relative;
  flex: 1;
  min-height: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  padding: 0 ${({ theme }) => theme.spaces[4]};
  background: repeating-conic-gradient(
      ${({ theme }) => theme.colors.neutral100} 0% 25%,
      ${({ theme }) => theme.colors.neutral0} 0% 50%
    )
    50% / 20px 20px;
`;
var CropArea = gt.div`
  position: relative;
  max-width: 100%;
  max-height: 100%;
  ${({ $aspect }) => $aspect ? `aspect-ratio: ${$aspect};` : ""}

  img {
    display: block;
    width: 100%;
    height: 100%;
    user-select: none;
    -webkit-user-drag: none;
  }
`;
var CropOverlay = gt.div`
  position: absolute;
  border: 1px dashed ${({ theme }) => theme.colors.primary600};
  box-shadow: 0 0 0 9999px rgba(33, 33, 52, 0.5);
  cursor: move;
  /* Without this, touch browsers claim the gesture for scrolling and fire
     pointercancel mid-drag — the crop drag dies while the finger is down. */
  touch-action: none;
`;
var ResizeHandle = gt.button`
  position: absolute;
  width: ${HANDLE_PX}px;
  height: ${HANDLE_PX}px;
  margin: -${HANDLE_PX / 2}px;
  padding: 0;
  border: 1px solid ${({ theme }) => theme.colors.primary600};
  border-radius: 2px;
  background: ${({ theme }) => theme.colors.neutral0};
  cursor: ${({ $cursor }) => $cursor};
  touch-action: none;
`;
var FocalPointHandle = gt.button`
  position: absolute;
  width: ${FOCAL_DIAMETER_REM}rem;
  height: ${FOCAL_DIAMETER_REM}rem;
  margin: ${-FOCAL_DIAMETER_REM / 2}rem 0 0 ${-FOCAL_DIAMETER_REM / 2}rem;
  border-radius: 50%;
  border: 1px solid ${({ theme }) => theme.colors.neutral800};
  background: transparent;
  cursor: grab;
  padding: 0;
  touch-action: none;

  &::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 16px;
    height: 16px;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.16);
    transform: translate(-50%, -50%);
  }
  &::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: ${({ theme }) => theme.colors.neutral800};
    transform: translate(-50%, -50%);
  }

  &:active {
    cursor: grabbing;
  }
`;
var InfoBox = gt(S)`
  display: none;

  ${({ theme }) => theme.breakpoints.medium} {
    display: block;
    position: absolute;
    right: ${({ theme }) => theme.spaces[1]};
    bottom: ${({ theme }) => theme.spaces[1]};
    width: 100%;
    max-width: 32rem;
    padding: ${({ theme }) => theme.spaces[3]};
    border-radius: ${({ theme }) => theme.borderRadius};
    background: ${({ theme }) => theme.colorScheme === "dark" ? theme.colors.neutral150 : theme.colors.neutral900};
    z-index: 20;
  }
`;
var FooterBar = gt(C)`
  width: 100%;
  justify-content: space-between;
  padding: ${({ theme }) => `${theme.spaces[3]} ${theme.spaces[5]}`};
  border-top: 1px solid ${({ theme }) => theme.colors.neutral150};
  background: ${({ theme }) => theme.colors.neutral0};
`;
var FieldRow = gt(Dd.Root)`
  flex-direction: row;
  align-items: center;
`;
var FieldNumberInput = gt(Ud)`
  width: 8.4rem;
`;
var LabelIcon = gt(Dd.Label)`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 1.6rem;
  height: 1.6rem;
`;
var BackgroundPreserveRatioContainer = gt(S)`
  position: absolute;
  top: 50%;
  left: 0;
  transform: translateY(-50%);

  svg {
    display: block;
  }
`;
var BackgroundPreserveRatio = () => (0, import_jsx_runtime3.jsx)(BackgroundPreserveRatioContainer, {
  children: (0, import_jsx_runtime3.jsx)("svg", {
    width: "17",
    height: "49",
    viewBox: "0 0 17 49",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    children: (0, import_jsx_runtime3.jsx)("path", {
      d: "M0.5 0.5H8.5C12.9183 0.5 16.5 4.08172 16.5 8.5M0.5 48.5H8.5C12.9183 48.5 16.5 44.9183 16.5 40.5",
      stroke: "#666687",
      strokeLinecap: "round"
    })
  })
});
var AssetCropEditor = ({ asset, isBusy = false, onClose, onApply, onSaveAsCopy, canSaveAsCopy }) => {
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const theme = st();
  const isDark = theme.colorScheme === "dark";
  const infoTextColor = isDark ? "neutral1000" : "neutral0";
  const infoMutedColor = isDark ? "neutral600" : "neutral200";
  const imgRef = React3.useRef(null);
  const cropAreaRef = React3.useRef(null);
  const overlayRef = React3.useRef(null);
  React3.useEffect(() => {
    var _a;
    (_a = overlayRef.current) == null ? void 0 : _a.focus();
  }, []);
  const { init, crop, naturalSize, aspectRatio, setCropSize, setCropPosition, setAspectRatio, produceFile, width, height } = useCropImg();
  const [aspectLocked, setAspectLocked] = React3.useState(false);
  const [focal, setFocal] = React3.useState(asset.focalPoint ?? {
    x: 50,
    y: 50
  });
  const rawImageUrl = prefixFileUrlWithBackendUrl(asset.url);
  const cacheKey = asset.updatedAt && !asset.isUrlSigned ? new Date(asset.updatedAt).getTime() : void 0;
  const imageUrl = cacheKey !== void 0 ? `${rawImageUrl}${rawImageUrl.includes("?") ? "&" : "?"}updatedAt=${cacheKey}` : rawImageUrl;
  const handleImageLoad = () => {
    if (imgRef.current) {
      init(imgRef.current);
    }
  };
  const pointerToNatural = (event) => {
    var _a;
    const rect = (_a = cropAreaRef.current) == null ? void 0 : _a.getBoundingClientRect();
    if (!rect || !naturalSize.width || !naturalSize.height) return null;
    const ratioX = naturalSize.width / rect.width;
    const ratioY = naturalSize.height / rect.height;
    return {
      x: (event.clientX - rect.left) * ratioX,
      y: (event.clientY - rect.top) * ratioY
    };
  };
  const activeDragCleanupRef = React3.useRef(null);
  React3.useEffect(() => {
    return () => {
      var _a;
      (_a = activeDragCleanupRef.current) == null ? void 0 : _a.call(activeDragCleanupRef);
    };
  }, []);
  const trackPointerDrag = (event, onMove) => {
    var _a;
    event.preventDefault();
    event.stopPropagation();
    const { pointerId } = event;
    try {
      event.currentTarget.setPointerCapture(pointerId);
    } catch {
    }
    const move = (e) => {
      if (e.pointerId !== pointerId) return;
      onMove(e);
    };
    const cleanup = () => {
      window.removeEventListener("pointermove", move);
      window.removeEventListener("pointerup", end);
      window.removeEventListener("pointercancel", end);
      activeDragCleanupRef.current = null;
    };
    const end = (e) => {
      if (e.pointerId !== pointerId) return;
      cleanup();
    };
    (_a = activeDragCleanupRef.current) == null ? void 0 : _a.call(activeDragCleanupRef);
    activeDragCleanupRef.current = cleanup;
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", end);
    window.addEventListener("pointercancel", end);
  };
  const handleMovePointerDown = (event) => {
    const start = pointerToNatural(event);
    if (!start) return;
    const startCrop = {
      ...crop
    };
    trackPointerDrag(event, (e) => {
      const next = pointerToNatural(e);
      if (!next) return;
      setCropPosition({
        x: startCrop.x + (next.x - start.x),
        y: startCrop.y + (next.y - start.y)
      });
    });
  };
  const handleResizePointerDown = (corner) => (event) => {
    const startCrop = {
      ...crop
    };
    const anchorX = corner === "tl" || corner === "bl" ? startCrop.x + startCrop.width : startCrop.x;
    const anchorY = corner === "tl" || corner === "tr" ? startCrop.y + startCrop.height : startCrop.y;
    trackPointerDrag(event, (e) => {
      const point = pointerToNatural(e);
      if (!point) return;
      const { x, y, width: w, height: h } = resolveCornerResize({
        anchorX,
        anchorY,
        point,
        aspectRatio: aspectLocked ? aspectRatio : null
      });
      setCropPosition({
        x,
        y
      });
      setCropSize({
        width: w,
        height: h
      });
    });
  };
  const toggleAspectLock = () => {
    setAspectLocked((locked) => {
      const next = !locked;
      setAspectRatio(next && height ? width / height : null);
      return next;
    });
  };
  const handleFocalPointerDown = (event) => {
    trackPointerDrag(event, (e) => {
      const point = pointerToNatural(e);
      if (!point) return;
      const px = (point.x - crop.x) / crop.width * 100;
      const py = (point.y - crop.y) / crop.height * 100;
      setFocal({
        x: Math.round(Math.min(100, Math.max(0, px))),
        y: Math.round(Math.min(100, Math.max(0, py)))
      });
    });
  };
  const focalPxX = Math.round(focal.x / 100 * width);
  const focalPxY = Math.round(focal.y / 100 * height);
  const setFocalPx = (axis, value) => {
    const dim = axis === "x" ? width : height;
    if (!dim) return;
    const pct = Math.min(100, Math.max(0, value / dim * 100));
    setFocal((prev) => ({
      ...prev,
      [axis]: Math.round(pct)
    }));
  };
  const cropPercents = naturalSize.width && naturalSize.height ? {
    left: crop.x / naturalSize.width * 100,
    top: crop.y / naturalSize.height * 100,
    width: crop.width / naturalSize.width * 100,
    height: crop.height / naturalSize.height * 100
  } : null;
  const isReady = cropPercents !== null;
  const handleAction = async (action) => {
    if (!isReady) return;
    let file;
    try {
      file = await produceFile(asset.name, asset.mime ?? "image/png", asset.updatedAt);
    } catch {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("asset-details.crop.export-error"),
          defaultMessage: "Could not process the cropped image."
        })
      });
      return;
    }
    const roundedFocal = {
      x: Math.round(focal.x),
      y: Math.round(focal.y)
    };
    if (action === "apply") {
      onApply(file, roundedFocal);
    } else {
      onSaveAsCopy(file, roundedFocal);
    }
  };
  return (0, import_jsx_runtime3.jsx)(un, {
    children: (0, import_jsx_runtime3.jsx)(yu, {
      onEscape: onClose,
      skipAutoFocus: true,
      children: (0, import_jsx_runtime3.jsxs)(Overlay, {
        ref: overlayRef,
        tabIndex: -1,
        children: [
          (0, import_jsx_runtime3.jsxs)(HeaderBar, {
            alignItems: "center",
            children: [
              (0, import_jsx_runtime3.jsx)(S5, {
                "aria-hidden": true
              }),
              (0, import_jsx_runtime3.jsx)(A, {
                variant: "omega",
                fontWeight: "bold",
                children: formatMessage({
                  id: getTranslationKey("asset-details.crop.title"),
                  defaultMessage: "Crop & Focus area"
                })
              })
            ]
          }),
          (0, import_jsx_runtime3.jsxs)(Body, {
            children: [
              (0, import_jsx_runtime3.jsxs)(CropArea, {
                ref: cropAreaRef,
                $aspect: naturalSize.width && naturalSize.height ? naturalSize.width / naturalSize.height : void 0,
                children: [
                  (0, import_jsx_runtime3.jsx)("img", {
                    ref: imgRef,
                    src: imageUrl,
                    alt: asset.name,
                    // Always anonymous, unlike AssetPreview/AssetsGrid which gate it:
                    // the editor reads pixels via canvas (useCropImg -> toBlob), which
                    // taints on a cross-origin image loaded without CORS, so every
                    // remote image must be fetched CORS-clean.
                    crossOrigin: "anonymous",
                    onLoad: handleImageLoad,
                    draggable: false
                  }),
                  cropPercents ? (0, import_jsx_runtime3.jsxs)(CropOverlay, {
                    style: {
                      left: `${cropPercents.left}%`,
                      top: `${cropPercents.top}%`,
                      width: `${cropPercents.width}%`,
                      height: `${cropPercents.height}%`
                    },
                    onPointerDown: handleMovePointerDown,
                    children: [
                      (0, import_jsx_runtime3.jsx)(ResizeHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.resize.top-left"),
                          defaultMessage: "Resize top-left"
                        }),
                        $cursor: "nwse-resize",
                        style: {
                          left: 0,
                          top: 0
                        },
                        onPointerDown: handleResizePointerDown("tl")
                      }),
                      (0, import_jsx_runtime3.jsx)(ResizeHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.resize.top-right"),
                          defaultMessage: "Resize top-right"
                        }),
                        $cursor: "nesw-resize",
                        style: {
                          right: 0,
                          top: 0
                        },
                        onPointerDown: handleResizePointerDown("tr")
                      }),
                      (0, import_jsx_runtime3.jsx)(ResizeHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.resize.bottom-left"),
                          defaultMessage: "Resize bottom-left"
                        }),
                        $cursor: "nesw-resize",
                        style: {
                          left: 0,
                          bottom: 0
                        },
                        onPointerDown: handleResizePointerDown("bl")
                      }),
                      (0, import_jsx_runtime3.jsx)(ResizeHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.resize.bottom-right"),
                          defaultMessage: "Resize bottom-right"
                        }),
                        $cursor: "nwse-resize",
                        style: {
                          right: 0,
                          bottom: 0
                        },
                        onPointerDown: handleResizePointerDown("br")
                      }),
                      (0, import_jsx_runtime3.jsx)(FocalPointHandle, {
                        type: "button",
                        "aria-label": formatMessage({
                          id: getTranslationKey("asset-details.crop.focal-point"),
                          defaultMessage: "Focal point"
                        }),
                        style: {
                          left: `${focal.x}%`,
                          top: `${focal.y}%`
                        },
                        onPointerDown: handleFocalPointerDown
                      })
                    ]
                  }) : null
                ]
              }),
              (0, import_jsx_runtime3.jsxs)(InfoBox, {
                children: [
                  (0, import_jsx_runtime3.jsxs)(C, {
                    direction: "column",
                    alignItems: "stretch",
                    gap: 1,
                    paddingBottom: 3,
                    children: [
                      (0, import_jsx_runtime3.jsx)(A, {
                        variant: "omega",
                        fontWeight: "bold",
                        textColor: infoTextColor,
                        children: formatMessage({
                          id: getTranslationKey("asset-details.crop.title"),
                          defaultMessage: "Crop & Focus area"
                        })
                      }),
                      (0, import_jsx_runtime3.jsx)(A, {
                        variant: "pi",
                        textColor: infoMutedColor,
                        children: formatMessage({
                          id: getTranslationKey("asset-details.crop.hint"),
                          defaultMessage: "Set the crop area with the rectangle. Pin the always-visible area with the circle."
                        })
                      })
                    ]
                  }),
                  (0, import_jsx_runtime3.jsxs)(C, {
                    gap: 6,
                    alignItems: "center",
                    children: [
                      (0, import_jsx_runtime3.jsxs)(C, {
                        alignItems: "center",
                        gap: 2,
                        children: [
                          (0, import_jsx_runtime3.jsxs)(C, {
                            direction: "column",
                            gap: 2,
                            children: [
                              (0, import_jsx_runtime3.jsxs)(FieldRow, {
                                name: "crop-width",
                                gap: 2,
                                children: [
                                  (0, import_jsx_runtime3.jsx)(LabelIcon, {
                                    textColor: infoTextColor,
                                    children: (0, import_jsx_runtime3.jsx)(Z2, {})
                                  }),
                                  (0, import_jsx_runtime3.jsx)(FieldNumberInput, {
                                    "aria-label": formatMessage({
                                      id: getTranslationKey("asset-details.crop.width"),
                                      defaultMessage: "Width (px)"
                                    }),
                                    value: width,
                                    min: 1,
                                    max: naturalSize.width || void 0,
                                    onValueChange: (next) => {
                                      if (next !== void 0) setCropSize({
                                        width: next
                                      });
                                    }
                                  })
                                ]
                              }),
                              (0, import_jsx_runtime3.jsxs)(FieldRow, {
                                name: "crop-height",
                                gap: 2,
                                children: [
                                  (0, import_jsx_runtime3.jsx)(LabelIcon, {
                                    textColor: infoTextColor,
                                    children: (0, import_jsx_runtime3.jsx)(N2, {})
                                  }),
                                  (0, import_jsx_runtime3.jsx)(FieldNumberInput, {
                                    "aria-label": formatMessage({
                                      id: getTranslationKey("asset-details.crop.height"),
                                      defaultMessage: "Height (px)"
                                    }),
                                    value: height,
                                    min: 1,
                                    max: naturalSize.height || void 0,
                                    onValueChange: (next) => {
                                      if (next !== void 0) setCropSize({
                                        height: next
                                      });
                                    }
                                  })
                                ]
                              })
                            ]
                          }),
                          (0, import_jsx_runtime3.jsxs)(C, {
                            position: "relative",
                            children: [
                              (0, import_jsx_runtime3.jsx)(be, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.crop.aspect-lock"),
                                  defaultMessage: "Lock aspect ratio"
                                }),
                                variant: aspectLocked ? "secondary" : "ghost",
                                onClick: toggleAspectLock,
                                children: (0, import_jsx_runtime3.jsx)(R3, {})
                              }),
                              (0, import_jsx_runtime3.jsx)(BackgroundPreserveRatio, {})
                            ]
                          })
                        ]
                      }),
                      (0, import_jsx_runtime3.jsxs)(C, {
                        direction: "column",
                        gap: 2,
                        marginLeft: "auto",
                        children: [
                          (0, import_jsx_runtime3.jsxs)(FieldRow, {
                            name: "focal-x",
                            gap: 2,
                            children: [
                              (0, import_jsx_runtime3.jsx)(Dd.Label, {
                                textColor: infoTextColor,
                                children: formatMessage({
                                  id: getTranslationKey("asset-details.crop.focal-x-axis"),
                                  defaultMessage: "X"
                                })
                              }),
                              (0, import_jsx_runtime3.jsx)(FieldNumberInput, {
                                "aria-label": formatMessage({
                                  id: getTranslationKey("asset-details.crop.focal-x"),
                                  defaultMessage: "Focal point X (px)"
                                }),
                                value: focalPxX,
                                onValueChange: (next) => {
                                  if (next !== void 0) setFocalPx("x", next);
                                }
                              })
                            ]
                          }),
                          (0, import_jsx_runtime3.jsxs)(FieldRow, {
                            name: "focal-y",
                            gap: 2,
                            children: [
                              (0, import_jsx_runtime3.jsx)(Dd.Label, {
                                textColor: infoTextColor,
                                children: formatMessage({
                                  id: getTranslationKey("asset-details.crop.focal-y-axis"),
                                  defaultMessage: "Y"
                                })
                              }),
                              (0, import_jsx_runtime3.jsx)(FieldNumberInput, {
                                "aria-label": formatMessage({
                                  id: getTranslationKey("asset-details.crop.focal-y"),
                                  defaultMessage: "Focal point Y (px)"
                                }),
                                value: focalPxY,
                                onValueChange: (next) => {
                                  if (next !== void 0) setFocalPx("y", next);
                                }
                              })
                            ]
                          })
                        ]
                      })
                    ]
                  })
                ]
              })
            ]
          }),
          (0, import_jsx_runtime3.jsxs)(FooterBar, {
            alignItems: "center",
            children: [
              (0, import_jsx_runtime3.jsx)(Ct, {
                variant: "tertiary",
                onClick: onClose,
                disabled: isBusy,
                children: formatMessage({
                  id: "app.components.Button.cancel",
                  defaultMessage: "Cancel"
                })
              }),
              (0, import_jsx_runtime3.jsxs)(C, {
                gap: 2,
                children: [
                  canSaveAsCopy && (0, import_jsx_runtime3.jsx)(Ct, {
                    variant: "secondary",
                    onClick: () => handleAction("copy"),
                    loading: isBusy,
                    disabled: !isReady,
                    children: formatMessage({
                      id: getTranslationKey("asset-details.crop.save-as-copy"),
                      defaultMessage: "Save as copy"
                    })
                  }),
                  (0, import_jsx_runtime3.jsx)(Ct, {
                    variant: "default",
                    onClick: () => handleAction("apply"),
                    loading: isBusy,
                    disabled: !isReady,
                    children: formatMessage({
                      id: getTranslationKey("asset-details.crop.apply"),
                      defaultMessage: "Apply"
                    })
                  })
                ]
              })
            ]
          })
        ]
      })
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetPreview.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var React4 = __toESM(require_react(), 1);
var PreviewContainer = gt(S)`
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
  max-height: 24rem;
  overflow: hidden;
  border-radius: ${({ theme }) => theme.borderRadius};
  padding: ${({ theme }) => theme.spaces[3]};
  background: repeating-conic-gradient(
      ${({ theme }) => theme.colors.neutral100} 0% 25%,
      transparent 0% 50%
    )
    50% / 20px 20px;
`;
var AssetContainer = gt(C)`
  justify-content: center;
  position: relative;
  z-index: 2;
  width: 100%;
  height: 100%;
`;
var StyledImage = gt.img`
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
`;
var ActionsOverlay = gt(C)`
  position: absolute;
  top: ${({ theme }) => theme.spaces[3]};
  right: ${({ theme }) => theme.spaces[3]};
  z-index: 3;
`;
var StyledVideo = gt.video`
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
`;
var StyledAudio = gt.audio`
  width: 100%;
`;
var StyledPdfIframe = gt.iframe`
  width: 100%;
  height: 100%;
  min-height: 200px;
  border: none;
`;
var IconFallback = gt(C)`
  height: 100%;
  aspect-ratio: 1;
  width: auto;
  max-width: 100%;
  margin: 0 auto;
  color: ${({ theme }) => theme.colors.neutral500};
  background: ${({ theme }) => theme.colors.neutral150};
`;
var LoaderOverlay = gt(C)`
  position: absolute;
  inset: 0;
  z-index: 1;
`;
var AssetLoader = () => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime4.jsx)(LoaderOverlay, {
    justifyContent: "center",
    alignItems: "center",
    children: (0, import_jsx_runtime4.jsx)(kt, {
      children: formatMessage({
        id: "app.loading",
        defaultMessage: "Loading..."
      })
    })
  });
};
var AssetPreview = ({ asset, actions, isLoading = false }) => {
  const { formatMessage } = useIntl();
  const { alternativeText, ext, mime, url, updatedAt, isUrlSigned, isLocal } = asset;
  const cacheKey = updatedAt && !isUrlSigned ? new Date(updatedAt).getTime() : void 0;
  const appendCacheBuster = (raw) => {
    if (!raw || cacheKey === void 0) return raw;
    return raw.includes("?") ? `${raw}&v=${cacheKey}` : `${raw}?v=${cacheKey}`;
  };
  const mediaUrl = appendCacheBuster(prefixFileUrlWithBackendUrl(url));
  const [isMediaLoaded, setIsMediaLoaded] = React4.useState(false);
  React4.useEffect(() => {
    setIsMediaLoaded(false);
  }, [
    mediaUrl
  ]);
  const imageRef = React4.useRef(null);
  React4.useEffect(() => {
    const image = imageRef.current;
    if (!image) return;
    const recompute = () => {
      const parent = image.parentElement;
      if (!parent) return;
      const c = parent.getBoundingClientRect();
      const w = image.offsetWidth;
      const h = image.offsetHeight;
      if (!w || !h || !c.width || !c.height) return;
    };
    recompute();
    const ro = new ResizeObserver(recompute);
    ro.observe(image);
    if (image.parentElement) ro.observe(image.parentElement);
    return () => ro.disconnect();
  }, [
    isMediaLoaded
  ]);
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Image)) {
    const imageUrl = appendCacheBuster(prefixFileUrlWithBackendUrl(url));
    if (imageUrl) {
      return (0, import_jsx_runtime4.jsxs)(PreviewContainer, {
        children: [
          (!isMediaLoaded || isLoading) && (0, import_jsx_runtime4.jsx)(AssetLoader, {}),
          actions ? (0, import_jsx_runtime4.jsx)(ActionsOverlay, {
            children: actions
          }) : null,
          (0, import_jsx_runtime4.jsx)(AssetContainer, {
            children: (0, import_jsx_runtime4.jsx)(StyledImage, {
              ref: imageRef,
              src: imageUrl,
              alt: alternativeText || asset.name || "",
              crossOrigin: !isLocal && isUrlSigned ? "anonymous" : void 0,
              onLoad: () => setIsMediaLoaded(true),
              onError: () => setIsMediaLoaded(true)
            })
          })
        ]
      });
    }
  }
  if ((mime == null ? void 0 : mime.includes(ASSET_TYPES.Video)) && mediaUrl) {
    return (0, import_jsx_runtime4.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime4.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime4.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime4.jsx)(StyledVideo, {
            src: mediaUrl,
            controls: true,
            title: asset.name,
            onLoadedData: () => setIsMediaLoaded(true),
            onError: () => setIsMediaLoaded(true),
            children: formatMessage({
              id: getTranslationKey("asset-details.videoNotSupported"),
              defaultMessage: "Your browser does not support the video tag."
            })
          })
        })
      ]
    });
  }
  if ((mime == null ? void 0 : mime.includes(ASSET_TYPES.Audio)) && mediaUrl) {
    return (0, import_jsx_runtime4.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime4.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime4.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime4.jsx)(C, {
            width: "100%",
            padding: 4,
            justifyContent: "center",
            alignItems: "center",
            height: "100%",
            minHeight: "12rem",
            children: (0, import_jsx_runtime4.jsx)(StyledAudio, {
              src: mediaUrl,
              controls: true,
              onLoadedData: () => setIsMediaLoaded(true),
              onError: () => setIsMediaLoaded(true)
            })
          })
        })
      ]
    });
  }
  const isPdf = (ext == null ? void 0 : ext.toLowerCase()) === "pdf" || (ext == null ? void 0 : ext.toLowerCase()) === ".pdf" || mime === "application/pdf";
  if (isPdf && mediaUrl) {
    return (0, import_jsx_runtime4.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime4.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime4.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime4.jsx)(StyledPdfIframe, {
            src: `${mediaUrl}#toolbar=0`,
            title: asset.name,
            onLoad: () => setIsMediaLoaded(true)
          })
        })
      ]
    });
  }
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime4.jsx)(PreviewContainer, {
    children: (0, import_jsx_runtime4.jsxs)(IconFallback, {
      justifyContent: "center",
      alignItems: "center",
      gap: 1,
      direction: "column",
      hasRadius: true,
      children: [
        (0, import_jsx_runtime4.jsx)(DocIcon, {
          width: 24,
          height: 24
        }),
        (0, import_jsx_runtime4.jsx)(A, {
          variant: "pi",
          children: formatMessage({
            id: getTranslationKey("asset-details.noPreview"),
            defaultMessage: "No preview available"
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetDetailsDrawer.mjs
var DrawerNotifyContext = React5.createContext(null);
var useDrawerNotify = () => {
  const ctx = React5.useContext(DrawerNotifyContext);
  if (!ctx) {
    throw new Error("useDrawerNotify must be used within AssetDetails");
  }
  return ctx;
};
var AssetOperationsContext = React5.createContext(null);
var useAssetOperation = () => {
  const ctx = React5.useContext(AssetOperationsContext);
  if (!ctx) {
    throw new Error("useAssetOperation must be used within AssetDetails");
  }
  return ctx;
};
var useAssetDetailsParam = () => {
  const [{ query }, setQuery] = useQueryParams();
  const assetId = parseAssetDetailsId(query == null ? void 0 : query[ASSET_DETAILS_URL_PARAM]);
  const hasValidId = assetId !== null;
  const [isMounted, setIsMounted] = React5.useState(hasValidId);
  const displayAssetId = React5.useRef(null);
  React5.useEffect(() => {
    if (hasValidId) {
      displayAssetId.current = assetId;
      setIsMounted(true);
    }
  }, [
    hasValidId,
    assetId
  ]);
  const onCloseAnimationEnd = React5.useCallback((event) => {
    if (event.target === event.currentTarget && !hasValidId) {
      setIsMounted(false);
    }
  }, [
    hasValidId
  ]);
  const openDetails = React5.useCallback((id) => {
    setQuery(withEncodedUserParams(query, {
      [ASSET_DETAILS_URL_PARAM]: String(id)
    }), "push", true);
  }, [
    query,
    setQuery
  ]);
  const closeDetails = React5.useCallback(() => {
    setQuery(withEncodedUserParams(query, {
      [ASSET_DETAILS_URL_PARAM]: void 0
    }), "push", true);
  }, [
    query,
    setQuery
  ]);
  return {
    assetId: hasValidId ? assetId : displayAssetId.current,
    isVisible: hasValidId,
    shouldRenderDrawer: isMounted,
    onCloseAnimationEnd,
    openDetails,
    closeDetails
  };
};
var DetailItemContainer = gt(C)`
  flex: 0 0 calc(50% - ${({ theme }) => theme.spaces[2]});
`;
var DetailItem = ({ label, value }) => (0, import_jsx_runtime5.jsxs)(DetailItemContainer, {
  direction: "column",
  justifyContent: "flex-start",
  alignItems: "flex-start",
  gap: 1,
  children: [
    (0, import_jsx_runtime5.jsx)(A, {
      variant: "sigma",
      textColor: "neutral600",
      fontWeight: "semiBold",
      textTransform: "uppercase",
      children: label
    }),
    (0, import_jsx_runtime5.jsx)(A, {
      variant: "pi",
      textColor: "neutral700",
      children: value ?? "-"
    })
  ]
});
var FormShell = gt(S)`
  display: flex;
  flex-direction: column;
  flex: 1;
  min-height: 0;

  > form {
    display: flex;
    flex-direction: column;
    flex: 1;
    min-height: 0;
    position: relative;
  }
`;
var DrawerToastSlot = gt(S)`
  position: absolute;
  top: ${({ theme }) => theme.spaces[2]};
  left: 50%;
  transform: translateX(-50%);
  z-index: 10;
  width: calc(100% - ${({ theme }) => theme.spaces[2]});
`;
var getBusyMessage = (state) => {
  if (state.isDeleting) {
    return {
      id: getTranslationKey("asset-details.delete.loading"),
      defaultMessage: "Deleting the file…"
    };
  }
  if (state.isCropCopying) {
    return {
      id: getTranslationKey("asset-details.crop.loading"),
      defaultMessage: "Saving the cropped copy…"
    };
  }
  if (state.isReplacing) {
    return {
      id: getTranslationKey("asset-details.replace.loading"),
      defaultMessage: "Replacing the file…"
    };
  }
  return null;
};
var StyledWarning = gt(Jn)`
  width: 1.6rem;
  height: 1.6rem;

  path {
    fill: ${({ theme }) => theme.colors.warning500};
  }
`;
var DetailField = ({ name, label, required, disabled }) => {
  const { formatMessage } = useIntl();
  const field = useField(name);
  const isSubmitting = useForm("DetailField", (state) => state.isSubmitting);
  const fieldValue = field.value ?? "";
  const [value, setValue] = React5.useState(fieldValue);
  React5.useEffect(() => {
    setValue(fieldValue);
  }, [
    fieldValue
  ]);
  const emptyTooltipLabel = formatMessage({
    id: getTranslationKey("asset-details.field.empty"),
    defaultMessage: "{label} is currently empty."
  }, {
    label
  });
  return (0, import_jsx_runtime5.jsxs)(Dd.Root, {
    name,
    required,
    children: [
      (0, import_jsx_runtime5.jsx)(Dd.Label, {
        children: label
      }),
      (0, import_jsx_runtime5.jsx)(Pc, {
        value,
        onChange: (event) => {
          setValue(event.target.value);
          field.onChange(name, event.target.value);
        },
        endAction: !value ? (0, import_jsx_runtime5.jsx)(Mt, {
          label: emptyTooltipLabel,
          children: (0, import_jsx_runtime5.jsx)(StyledWarning, {
            "aria-label": emptyTooltipLabel,
            role: "img"
          })
        }) : void 0,
        type: "text",
        disabled: isSubmitting || disabled
      })
    ]
  });
};
var LocationField = ({ label, rootLabel, folders, disabled }) => {
  const field = useField("folder");
  const isSubmitting = useForm("LocationField", (state) => state.isSubmitting);
  return (0, import_jsx_runtime5.jsxs)(Dd.Root, {
    name: "folder",
    required: true,
    children: [
      (0, import_jsx_runtime5.jsx)(Dd.Label, {
        children: label
      }),
      (0, import_jsx_runtime5.jsxs)(cr, {
        value: field.value == null ? "" : String(field.value),
        onChange: (value) => {
          const next = value === "" ? null : Number(value);
          field.onChange("folder", next);
        },
        disabled: isSubmitting || disabled,
        children: [
          (0, import_jsx_runtime5.jsx)(dr, {
            value: "",
            children: rootLabel
          }),
          folders.map((folder) => (0, import_jsx_runtime5.jsx)(dr, {
            value: String(folder.id),
            children: folder.name
          }, folder.id))
        ]
      })
    ]
  });
};
var DeleteAssetButton = () => {
  const { formatMessage } = useIntl();
  const { deleteAsset, isDeleting } = useAssetOperation();
  const [isOpen, setIsOpen] = React5.useState(false);
  const handleConfirm = async () => {
    await deleteAsset();
    setIsOpen(false);
  };
  const triggerLabel = formatMessage({
    id: getTranslationKey("asset-details.delete.trigger"),
    defaultMessage: "Delete this file"
  });
  return (0, import_jsx_runtime5.jsxs)(Pd.Root, {
    open: isOpen,
    onOpenChange: setIsOpen,
    children: [
      (0, import_jsx_runtime5.jsx)(Pd.Trigger, {
        children: (0, import_jsx_runtime5.jsx)(be, {
          label: triggerLabel,
          variant: "danger-light",
          children: (0, import_jsx_runtime5.jsx)(In, {})
        })
      }),
      (0, import_jsx_runtime5.jsxs)(Pd.Content, {
        children: [
          (0, import_jsx_runtime5.jsx)(Pd.Header, {
            children: formatMessage({
              id: getTranslationKey("asset-details.delete.title"),
              defaultMessage: "Delete this media file?"
            })
          }),
          (0, import_jsx_runtime5.jsx)(Pd.Body, {
            icon: (0, import_jsx_runtime5.jsx)(Jn, {
              width: "24px",
              height: "24px",
              fill: "danger600"
            }),
            textAlign: "center",
            children: formatMessage({
              id: getTranslationKey("asset-details.delete.description"),
              defaultMessage: "This file cannot be recovered once deleted. If it is currently in use, linked content will break and image containers will be empty."
            })
          }),
          (0, import_jsx_runtime5.jsxs)(Pd.Footer, {
            children: [
              (0, import_jsx_runtime5.jsx)(Pd.Cancel, {
                children: (0, import_jsx_runtime5.jsx)(Ct, {
                  variant: "tertiary",
                  disabled: isDeleting,
                  fullWidth: true,
                  children: formatMessage({
                    id: "app.components.Button.cancel",
                    defaultMessage: "Cancel"
                  })
                })
              }),
              (0, import_jsx_runtime5.jsx)(Pd.Action, {
                children: (0, import_jsx_runtime5.jsx)(Ct, {
                  variant: "danger-light",
                  loading: isDeleting,
                  onClick: handleConfirm,
                  fullWidth: true,
                  children: formatMessage({
                    id: "app.components.Button.confirm",
                    defaultMessage: "Confirm"
                  })
                })
              })
            ]
          })
        ]
      })
    ]
  });
};
var CopyLinkButton = ({ asset }) => {
  const { formatMessage } = useIntl();
  const { copy } = useClipboard();
  const notify = useDrawerNotify();
  const handleCopy = async () => {
    const url = prefixFileUrlWithBackendUrl(asset.url);
    if (!url) return;
    const didCopy = await copy(url);
    notify({
      type: didCopy ? "success" : "danger",
      message: didCopy ? formatMessage({
        id: getTranslationKey("asset-details.copy-link.success"),
        defaultMessage: "Link copied."
      }) : formatMessage({
        id: getTranslationKey("asset-details.copy-link.error"),
        defaultMessage: "Failed to copy the link."
      })
    });
  };
  return (0, import_jsx_runtime5.jsx)(be, {
    label: formatMessage({
      id: getTranslationKey("asset-details.copy-link.trigger"),
      defaultMessage: "Copy link"
    }),
    variant: "tertiary",
    onClick: handleCopy,
    children: (0, import_jsx_runtime5.jsx)(R3, {})
  });
};
var DownloadAssetButton = ({ asset }) => {
  const { formatMessage } = useIntl();
  const notify = useDrawerNotify();
  const [isDownloading, setIsDownloading] = React5.useState(false);
  const handleDownload = async () => {
    const url = prefixFileUrlWithBackendUrl(asset.url);
    if (!url) return;
    setIsDownloading(true);
    try {
      await downloadFile(url, asset.name);
    } catch {
      notify({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("asset-details.download.error"),
          defaultMessage: "Failed to download the file."
        })
      });
    } finally {
      setIsDownloading(false);
    }
  };
  return (0, import_jsx_runtime5.jsx)(be, {
    label: formatMessage({
      id: getTranslationKey("asset-details.download.trigger"),
      defaultMessage: "Download"
    }),
    variant: "tertiary",
    onClick: handleDownload,
    disabled: isDownloading,
    children: (0, import_jsx_runtime5.jsx)(z5, {})
  });
};
var ReplaceAssetButton = ({ mime }) => {
  const { formatMessage } = useIntl();
  const { replaceAsset, isReplacing } = useAssetOperation();
  const fileInputRef = React5.useRef(null);
  const [isDialogOpen, setIsDialogOpen] = React5.useState(false);
  const aiEnabled = useAIMetadataEnabled({
    mime
  });
  const handleTriggerClick = () => {
    setIsDialogOpen(true);
  };
  const handleContinue = () => {
    var _a;
    setIsDialogOpen(false);
    (_a = fileInputRef.current) == null ? void 0 : _a.click();
  };
  const handleFileChange = async (event) => {
    var _a;
    const file = (_a = event.target.files) == null ? void 0 : _a[0];
    event.target.value = "";
    if (!file) {
      return;
    }
    await replaceAsset(file);
  };
  return (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, {
    children: [
      (0, import_jsx_runtime5.jsx)($e, {
        children: (0, import_jsx_runtime5.jsx)("input", {
          ref: fileInputRef,
          type: "file",
          accept: mime ?? "",
          multiple: false,
          onChange: handleFileChange,
          "aria-hidden": true,
          tabIndex: -1
        })
      }),
      (0, import_jsx_runtime5.jsx)(be, {
        label: formatMessage({
          id: getTranslationKey("asset-details.replace.trigger"),
          defaultMessage: "Replace this file"
        }),
        variant: "tertiary",
        onClick: handleTriggerClick,
        disabled: isReplacing,
        children: (0, import_jsx_runtime5.jsx)(W2, {})
      }),
      (0, import_jsx_runtime5.jsx)(Pd.Root, {
        open: isDialogOpen,
        onOpenChange: setIsDialogOpen,
        children: (0, import_jsx_runtime5.jsxs)(Pd.Content, {
          children: [
            (0, import_jsx_runtime5.jsx)(Pd.Header, {
              children: formatMessage({
                id: getTranslationKey("asset-details.replace.title"),
                defaultMessage: "Replace this media file?"
              })
            }),
            (0, import_jsx_runtime5.jsx)(Pd.Body, {
              textAlign: "center",
              children: (0, import_jsx_runtime5.jsxs)(C, {
                direction: "column",
                textAlign: "center",
                children: [
                  (0, import_jsx_runtime5.jsx)(A, {
                    variant: "omega",
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.description"),
                      defaultMessage: "Current content will be permanently replaced."
                    })
                  }),
                  aiEnabled ? (0, import_jsx_runtime5.jsx)(A, {
                    variant: "omega",
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.description.ai"),
                      defaultMessage: "AI will generate new metadata after upload."
                    })
                  }) : null
                ]
              })
            }),
            (0, import_jsx_runtime5.jsxs)(Pd.Footer, {
              children: [
                (0, import_jsx_runtime5.jsx)(Pd.Cancel, {
                  children: (0, import_jsx_runtime5.jsx)(Ct, {
                    variant: "tertiary",
                    fullWidth: true,
                    children: formatMessage({
                      id: "app.components.Button.cancel",
                      defaultMessage: "Cancel"
                    })
                  })
                }),
                (0, import_jsx_runtime5.jsx)(Pd.Action, {
                  children: (0, import_jsx_runtime5.jsx)(Ct, {
                    variant: "secondary",
                    onClick: handleContinue,
                    fullWidth: true,
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.continue"),
                      defaultMessage: "Continue"
                    })
                  })
                })
              ]
            })
          ]
        })
      })
    ]
  });
};
var AssetImageActions = ({ onCrop }) => {
  const { formatMessage } = useIntl();
  const isSubmitting = useForm("AssetImageActions", (state) => state.isSubmitting);
  return (0, import_jsx_runtime5.jsx)(C, {
    direction: "column",
    gap: 2,
    children: (0, import_jsx_runtime5.jsx)(be, {
      label: formatMessage({
        id: getTranslationKey("asset-details.crop.trigger"),
        defaultMessage: "Crop"
      }),
      variant: "tertiary",
      onClick: onCrop,
      disabled: isSubmitting || !onCrop,
      children: (0, import_jsx_runtime5.jsx)(S5, {})
    })
  });
};
var AssetDetails = ({ asset, closeDetails }) => {
  var _a;
  const { formatMessage, formatDate } = useIntl();
  const getErrorMessage = useApiErrorMessage();
  const { canCreate, canUpdate, canDownload, canCopyLink } = useMediaLibraryPermissions();
  const { data: folders = [] } = useGetAllFoldersQuery();
  const { toggleNotification } = useNotification();
  const [updateAsset] = useUpdateAssetMutation();
  const { trackUsage } = useTracking2();
  const [replaceMutation, { isLoading: isReplacing }] = useReplaceAssetMutation();
  const [deleteMutation, { isLoading: isDeleting }] = useDeleteAssetMutation();
  const [uploadCroppedCopy, { isLoading: isCropCopying }] = useUploadFileSilentlyMutation();
  const [isCropOpen, setIsCropOpen] = React5.useState(false);
  const [drawerToast, setDrawerToast] = React5.useState(null);
  React5.useEffect(() => {
    if (!drawerToast) return;
    const timer = window.setTimeout(() => setDrawerToast(null), 5e3);
    return () => window.clearTimeout(timer);
  }, [
    drawerToast
  ]);
  const notify = React5.useCallback((toast) => setDrawerToast(toast), []);
  const isImage = (_a = asset.mime) == null ? void 0 : _a.includes(ASSET_TYPES.Image);
  const initialValues = {
    name: asset.name ?? "",
    caption: asset.caption ?? "",
    alternativeText: asset.alternativeText ?? "",
    folder: typeof asset.folder === "object" && asset.folder !== null ? asset.folder.id ?? null : asset.folder ?? null
  };
  const handleSubmit = async (values) => {
    var _a2;
    const fileInfo = {
      name: values.name,
      caption: values.caption,
      alternativeText: values.alternativeText,
      folder: values.folder
    };
    const res = await updateAsset({
      id: asset.id,
      fileInfo
    });
    if ("error" in res) {
      notify({
        type: "danger",
        message: getErrorMessage(res.error, formatMessage({
          id: getTranslationKey("asset-details.update.error"),
          defaultMessage: "Failed to update the file."
        }))
      });
      return;
    }
    trackUsage("didEditMediaLibraryElements", {
      location: MEDIA_LIBRARY_LOCATION,
      // Legacy sends the mime top-level type (image/video/audio/application…).
      type: (_a2 = asset.mime) == null ? void 0 : _a2.split("/")[0],
      changeLocation: values.folder !== initialValues.folder
    });
    notify({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.update.success"),
        defaultMessage: "File updated"
      })
    });
  };
  const { title: folderName } = useFolderInfo(typeof asset.folder === "object" && asset.folder !== null ? asset.folder.id ?? null : asset.folder ?? null);
  const handleReplace = React5.useCallback(async (file) => {
    const res = await replaceMutation({
      id: asset.id,
      file,
      fileInfo: {
        name: asset.name
      }
    });
    if ("error" in res) {
      notify({
        type: "danger",
        message: getErrorMessage(res.error, formatMessage({
          id: getTranslationKey("asset-details.replace.error"),
          defaultMessage: "Failed to replace the file."
        }))
      });
      return;
    }
    trackUsage("didReplaceMedia", {
      location: MEDIA_LIBRARY_LOCATION
    });
    notify({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.replace.success"),
        defaultMessage: "File replaced."
      })
    });
  }, [
    asset.id,
    asset.name,
    formatMessage,
    getErrorMessage,
    notify,
    replaceMutation,
    trackUsage
  ]);
  const handleDelete = React5.useCallback(async () => {
    const res = await deleteMutation(asset.id);
    if ("error" in res) {
      notify({
        type: "danger",
        message: getErrorMessage(res.error, formatMessage({
          id: getTranslationKey("asset-details.delete.error"),
          defaultMessage: "Failed to delete the asset."
        }))
      });
      return;
    }
    toggleNotification({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.delete.success"),
        defaultMessage: "1 element have been deleted from {folderName}"
      }, {
        folderName
      })
    });
    closeDetails();
  }, [
    asset.id,
    closeDetails,
    deleteMutation,
    folderName,
    formatMessage,
    getErrorMessage,
    notify,
    toggleNotification
  ]);
  const notifyCropError = (error) => {
    notify({
      type: "danger",
      message: getErrorMessage(error, formatMessage({
        id: getTranslationKey("asset-details.crop.error"),
        defaultMessage: "Failed to crop the file."
      }))
    });
  };
  const handleCropApply = async (file, focalPoint) => {
    setIsCropOpen(false);
    const res = await replaceMutation({
      id: asset.id,
      file,
      fileInfo: {
        focalPoint
      }
    });
    if ("error" in res) {
      notifyCropError(res.error);
      return;
    }
    trackUsage("didCropFile", {
      location: MEDIA_LIBRARY_LOCATION,
      duplicatedFile: false
    });
    notify({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.crop.success"),
        defaultMessage: "File cropped."
      })
    });
  };
  const handleCropSaveAsCopy = async (file, focalPoint) => {
    setIsCropOpen(false);
    const res = await uploadCroppedCopy({
      file,
      fileInfo: {
        name: asset.name,
        caption: asset.caption ?? "",
        alternativeText: asset.alternativeText ?? "",
        folder: initialValues.folder,
        focalPoint
      }
    });
    if ("error" in res) {
      notifyCropError(res.error);
      return;
    }
    trackUsage("didCropFile", {
      location: MEDIA_LIBRARY_LOCATION,
      duplicatedFile: true
    });
    notify({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.crop.copy-success"),
        defaultMessage: "Copy created."
      })
    });
  };
  const operations = React5.useMemo(() => ({
    replaceAsset: handleReplace,
    deleteAsset: handleDelete,
    isReplacing,
    isDeleting
  }), [
    handleReplace,
    handleDelete,
    isReplacing,
    isDeleting
  ]);
  return (
    // `key={asset.id}` resets the form when the drawer switches to a different
    // asset so cached values from the previous asset don't bleed in.
    (0, import_jsx_runtime5.jsx)(DrawerNotifyContext.Provider, {
      value: notify,
      children: (0, import_jsx_runtime5.jsx)(AssetOperationsContext.Provider, {
        value: operations,
        children: (0, import_jsx_runtime5.jsx)(FormShell, {
          children: (0, import_jsx_runtime5.jsx)(Form, {
            method: "POST",
            initialValues,
            onSubmit: handleSubmit,
            children: ({ modified, isSubmitting, values, resetForm }) => {
              const nameIsEmpty = (values.name ?? "").trim() === "";
              const busyMessage = getBusyMessage({
                isDeleting,
                isReplacing,
                isCropCopying
              });
              return (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, {
                children: [
                  (0, import_jsx_runtime5.jsx)(Blocker, {
                    onProceed: resetForm
                  }),
                  isCropOpen && isImage ? (0, import_jsx_runtime5.jsx)(AssetCropEditor, {
                    asset,
                    onClose: () => setIsCropOpen(false),
                    onApply: handleCropApply,
                    onSaveAsCopy: handleCropSaveAsCopy,
                    canSaveAsCopy: canCreate
                  }) : null,
                  busyMessage ? (0, import_jsx_runtime5.jsx)(BusyOverlay, {
                    children: formatMessage(busyMessage)
                  }) : null,
                  drawerToast ? (0, import_jsx_runtime5.jsx)(DrawerToastSlot, {
                    children: (0, import_jsx_runtime5.jsx)(ud, {
                      variant: drawerToast.type === "success" ? "success" : "danger",
                      closeLabel: formatMessage({
                        id: "global.close",
                        defaultMessage: "Close"
                      }),
                      onClose: () => setDrawerToast(null),
                      children: drawerToast.message
                    })
                  }) : null,
                  (0, import_jsx_runtime5.jsxs)(Drawer.ScrollableContent, {
                    children: [
                      (0, import_jsx_runtime5.jsx)(AssetPreview, {
                        asset,
                        actions: isImage && canUpdate ? (0, import_jsx_runtime5.jsx)(AssetImageActions, {
                          onCrop: () => setIsCropOpen(true)
                        }) : null
                      }),
                      (0, import_jsx_runtime5.jsxs)(C, {
                        direction: "column",
                        alignItems: "stretch",
                        gap: 4,
                        paddingTop: 4,
                        paddingBottom: 4,
                        paddingLeft: 5,
                        paddingRight: 5,
                        children: [
                          (0, import_jsx_runtime5.jsx)(A, {
                            variant: "beta",
                            fontWeight: "semiBold",
                            tag: "h3",
                            children: formatMessage({
                              id: getTranslationKey("asset-details.fileInfo"),
                              defaultMessage: "File info"
                            })
                          }),
                          (0, import_jsx_runtime5.jsxs)(C, {
                            wrap: "wrap",
                            gap: 4,
                            background: "neutral100",
                            paddingTop: 4,
                            paddingBottom: 4,
                            paddingLeft: 6,
                            paddingRight: 6,
                            alignItems: "flex-start",
                            children: [
                              (0, import_jsx_runtime5.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.creationDate"),
                                  defaultMessage: "Creation date"
                                }),
                                value: asset.createdAt ? formatDate(new Date(asset.createdAt), {
                                  dateStyle: "long",
                                  timeStyle: "short"
                                }) : null
                              }),
                              (0, import_jsx_runtime5.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.lastUpdated"),
                                  defaultMessage: "Last updated"
                                }),
                                value: asset.updatedAt ? formatDate(new Date(asset.updatedAt), {
                                  dateStyle: "long",
                                  timeStyle: "short"
                                }) : null
                              }),
                              (0, import_jsx_runtime5.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.createdBy"),
                                  defaultMessage: "Created by"
                                }),
                                value: asset.createdBy ? getDisplayName({
                                  firstname: asset.createdBy.firstname ?? void 0,
                                  lastname: asset.createdBy.lastname ?? void 0,
                                  username: asset.createdBy.username ?? void 0,
                                  email: asset.createdBy.email ?? void 0
                                }) ?? "-" : null
                              }),
                              (0, import_jsx_runtime5.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.size"),
                                  defaultMessage: "Size"
                                }),
                                value: asset.size ? formatBytes(asset.size, 1) : null
                              }),
                              isImage && (asset.width != null || asset.height != null) && (0, import_jsx_runtime5.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.dimensions"),
                                  defaultMessage: "Dimensions"
                                }),
                                value: asset.width != null && asset.height != null ? `${asset.width} × ${asset.height}` : null
                              }),
                              (0, import_jsx_runtime5.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.extension"),
                                  defaultMessage: "Extension"
                                }),
                                value: getFileExtension(asset.ext)
                              }),
                              (0, import_jsx_runtime5.jsx)(DetailItem, {
                                label: formatMessage({
                                  id: getTranslationKey("asset-details.assetId"),
                                  defaultMessage: "Asset ID"
                                }),
                                value: String(asset.id)
                              })
                            ]
                          }),
                          (0, import_jsx_runtime5.jsx)(DetailField, {
                            name: "name",
                            label: formatMessage({
                              id: getTranslationKey("asset-details.fileName"),
                              defaultMessage: "File name"
                            }),
                            required: true,
                            disabled: !canUpdate
                          }),
                          (0, import_jsx_runtime5.jsx)(LocationField, {
                            label: formatMessage({
                              id: getTranslationKey("asset-details.location"),
                              defaultMessage: "Location"
                            }),
                            rootLabel: formatMessage({
                              id: getTranslationKey("plugin.home"),
                              defaultMessage: "Home"
                            }),
                            folders,
                            disabled: !canUpdate
                          }),
                          (0, import_jsx_runtime5.jsx)(DetailField, {
                            name: "caption",
                            label: formatMessage({
                              id: getTranslationKey("asset-details.caption"),
                              defaultMessage: "Caption"
                            }),
                            disabled: !canUpdate
                          }),
                          (0, import_jsx_runtime5.jsx)(DetailField, {
                            name: "alternativeText",
                            label: formatMessage({
                              id: getTranslationKey("asset-details.alternativeText"),
                              defaultMessage: "Alternative text"
                            }),
                            disabled: !canUpdate
                          })
                        ]
                      })
                    ]
                  }),
                  (canUpdate || canCopyLink || canDownload) && (0, import_jsx_runtime5.jsxs)(C, {
                    justifyContent: "space-between",
                    alignItems: "center",
                    gap: 2,
                    padding: 3,
                    borderColor: "neutral150",
                    borderStyle: "solid",
                    borderWidth: "1px 0 0 0",
                    background: "neutral0",
                    children: [
                      (0, import_jsx_runtime5.jsxs)(C, {
                        gap: 2,
                        children: [
                          canUpdate && (0, import_jsx_runtime5.jsx)(DeleteAssetButton, {}),
                          canCopyLink && (0, import_jsx_runtime5.jsx)(CopyLinkButton, {
                            asset
                          }),
                          canDownload && (0, import_jsx_runtime5.jsx)(DownloadAssetButton, {
                            asset
                          }),
                          canUpdate && (0, import_jsx_runtime5.jsx)(ReplaceAssetButton, {
                            mime: asset.mime
                          })
                        ]
                      }),
                      canUpdate && (0, import_jsx_runtime5.jsx)(Ct, {
                        type: "submit",
                        variant: "default",
                        loading: isSubmitting,
                        // File name is required; block submit when it's empty or whitespace so the API can't 400 on a blank value.
                        disabled: !modified || isSubmitting || nameIsEmpty,
                        children: formatMessage({
                          id: getTranslationKey("asset-details.save"),
                          defaultMessage: "Save changes"
                        })
                      })
                    ]
                  })
                ]
              });
            }
          }, asset.id)
        })
      })
    })
  );
};
var HeaderIcon = gt(C)`
  flex-shrink: 0;
`;
var HeaderTitle = gt(A)`
  min-width: 0;
`;
var DrawerHeader = ({ asset, closeDetails }) => {
  const DocIcon = asset ? getAssetIcon(asset.mime, asset.ext) : N5;
  return (0, import_jsx_runtime5.jsxs)(C, {
    gap: 2,
    paddingLeft: 5,
    paddingTop: 3,
    paddingBottom: 3,
    paddingRight: 3,
    borderColor: "neutral150",
    borderStyle: "solid",
    borderWidth: "0 0 1px 0",
    children: [
      (0, import_jsx_runtime5.jsx)(HeaderIcon, {
        children: (0, import_jsx_runtime5.jsx)(DocIcon, {
          width: 20,
          height: 20
        })
      }),
      (0, import_jsx_runtime5.jsx)(Drawer.Title, {
        asChild: true,
        children: (0, import_jsx_runtime5.jsx)(HeaderTitle, {
          variant: "omega",
          fontWeight: "semiBold",
          overflow: "hidden",
          ellipsis: true,
          tag: "h2",
          children: asset.name
        })
      }),
      (0, import_jsx_runtime5.jsx)(S, {
        marginLeft: "auto",
        children: (0, import_jsx_runtime5.jsx)(Drawer.CloseButton, {
          onClose: closeDetails,
          children: (0, import_jsx_runtime5.jsx)(O2, {})
        })
      })
    ]
  });
};
var DrawerContent = ({ assetId, closeDetails }) => {
  const { formatMessage } = useIntl();
  const { data: asset, isLoading, error } = useGetAssetQuery(assetId, {
    refetchOnMountOrArgChange: false,
    refetchOnReconnect: false,
    refetchOnFocus: false
  });
  if (isLoading) {
    return (0, import_jsx_runtime5.jsx)(C, {
      justifyContent: "center",
      padding: 8,
      children: (0, import_jsx_runtime5.jsx)(kt, {
        children: formatMessage({
          id: "app.loading",
          defaultMessage: "Loading..."
        })
      })
    });
  }
  if (error || !asset) {
    return (0, import_jsx_runtime5.jsx)(C, {
      direction: "column",
      alignItems: "stretch",
      gap: 4,
      padding: 4,
      children: (0, import_jsx_runtime5.jsx)(ud, {
        variant: "danger",
        closeLabel: formatMessage({
          id: "global.close",
          defaultMessage: "Close"
        }),
        onClose: closeDetails,
        children: formatMessage({
          id: getTranslationKey("asset-details.error"),
          defaultMessage: "Failed to load file details."
        })
      })
    });
  }
  return (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, {
    children: [
      (0, import_jsx_runtime5.jsx)(DrawerHeader, {
        asset,
        closeDetails
      }),
      (0, import_jsx_runtime5.jsx)(AssetDetails, {
        asset,
        closeDetails
      })
    ]
  });
};
var AssetDetailsDrawer = () => {
  const { formatMessage } = useIntl();
  const { assetId, isVisible, shouldRenderDrawer, onCloseAnimationEnd, closeDetails } = useAssetDetailsParam();
  if (!shouldRenderDrawer || assetId === null) {
    return null;
  }
  return (0, import_jsx_runtime5.jsxs)(Drawer.Root, {
    isVisible,
    onClose: closeDetails,
    children: [
      (0, import_jsx_runtime5.jsx)("div", {
        children: (0, import_jsx_runtime5.jsxs)($e, {
          children: [
            (0, import_jsx_runtime5.jsx)(Drawer.Title, {
              children: formatMessage({
                id: getTranslationKey("asset-details.title"),
                defaultMessage: "File details"
              })
            }),
            (0, import_jsx_runtime5.jsx)(Drawer.Description, {
              children: formatMessage({
                id: getTranslationKey("asset-details.description"),
                defaultMessage: "Displays file information and metadata"
              })
            })
          ]
        })
      }),
      (0, import_jsx_runtime5.jsx)(Drawer.Body, {
        animationDirection: "left",
        width: "41.6rem",
        // dvh, not vh: the drawer is anchored to the bottom, so with 100vh on
        // mobile (where the URL bar shrinks the visual viewport below 100vh)
        // the top of the drawer — header, title, close button — is pushed
        // off-screen. dvh tracks the actual visible height.
        height: "100dvh",
        onAnimationEnd: onCloseAnimationEnd,
        children: (0, import_jsx_runtime5.jsx)(DrawerContent, {
          assetId,
          closeDetails
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsGrid.mjs
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/upload/dist/admin/future/utils/isEventFromWithin.mjs
var isEventFromWithin = (event) => event.currentTarget instanceof Node && event.target instanceof Node && event.currentTarget.contains(event.target);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useAssetSelection.mjs
var import_react2 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/utils/selection.mjs
var assetKey = (id) => `asset:${id}`;
var folderKey = (id) => `folder:${id}`;
var getIdsOfKind = (keys, kind) => {
  const ids = /* @__PURE__ */ new Set();
  keys.forEach((key) => {
    const [keyKind, id] = key.split(":");
    if (keyKind === kind) {
      ids.add(Number(id));
    }
  });
  return ids;
};
var createEmptySelection = () => ({
  selectedKeys: /* @__PURE__ */ new Set(),
  anchorKey: null
});
var toggleSelection = (state, key) => {
  const selectedKeys = new Set(state.selectedKeys);
  if (selectedKeys.has(key)) {
    selectedKeys.delete(key);
  } else {
    selectedKeys.add(key);
  }
  return {
    selectedKeys,
    anchorKey: key
  };
};
var deselect = (state, key) => {
  const selectedKeys = new Set(state.selectedKeys);
  selectedKeys.delete(key);
  return {
    selectedKeys,
    anchorKey: state.anchorKey === key ? null : state.anchorKey
  };
};
var selectRange = (state, orderedKeys, targetKey) => {
  const targetIndex = orderedKeys.indexOf(targetKey);
  if (targetIndex === -1) {
    return state;
  }
  const anchorIndex = state.anchorKey === null ? -1 : orderedKeys.indexOf(state.anchorKey);
  if (anchorIndex === -1) {
    return {
      selectedKeys: /* @__PURE__ */ new Set([
        targetKey
      ]),
      anchorKey: targetKey
    };
  }
  const start = Math.min(anchorIndex, targetIndex);
  const end = Math.max(anchorIndex, targetIndex);
  return {
    selectedKeys: new Set(orderedKeys.slice(start, end + 1)),
    anchorKey: state.anchorKey
  };
};
var selectAll = (orderedKeys) => ({
  selectedKeys: new Set(orderedKeys),
  anchorKey: orderedKeys.length > 0 ? orderedKeys[orderedKeys.length - 1] : null
});
var clearSelection = () => createEmptySelection();
var getSelectAllState = (selectedKeys, orderedKeys) => {
  if (orderedKeys.length === 0) {
    return {
      allSelected: false,
      isIndeterminate: false
    };
  }
  const selectedCount = orderedKeys.reduce((count, key) => selectedKeys.has(key) ? count + 1 : count, 0);
  const allSelected = selectedCount === orderedKeys.length;
  return {
    allSelected,
    isIndeterminate: selectedCount > 0 && !allSelected
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useAssetSelection.mjs
var AssetSelectionContext = (0, import_react2.createContext)(null);
var AssetSelectionProvider = ({ children, disabled = false }) => {
  const [state, setState] = (0, import_react2.useState)(createEmptySelection);
  const isSelected = (0, import_react2.useCallback)((key) => !disabled && state.selectedKeys.has(key), [
    disabled,
    state.selectedKeys
  ]);
  const toggle = (0, import_react2.useCallback)((key) => {
    if (disabled) return;
    setState((prev) => toggleSelection(prev, key));
  }, [
    disabled
  ]);
  const selectRange$1 = (0, import_react2.useCallback)((orderedKeys, targetKey) => {
    if (disabled) return;
    setState((prev) => selectRange(prev, orderedKeys, targetKey));
  }, [
    disabled
  ]);
  const selectAll$1 = (0, import_react2.useCallback)((orderedKeys) => {
    if (disabled) return;
    setState(selectAll(orderedKeys));
  }, [
    disabled
  ]);
  const deselect$1 = (0, import_react2.useCallback)((key) => setState((prev) => deselect(prev, key)), []);
  const clear = (0, import_react2.useCallback)(() => setState(clearSelection()), []);
  const selectedIds = (0, import_react2.useMemo)(() => getIdsOfKind(state.selectedKeys, "asset"), [
    state.selectedKeys
  ]);
  const selectedFolderIds = (0, import_react2.useMemo)(() => getIdsOfKind(state.selectedKeys, "folder"), [
    state.selectedKeys
  ]);
  const value = (0, import_react2.useMemo)(() => ({
    selectedKeys: state.selectedKeys,
    selectedIds,
    selectedFolderIds,
    anchorKey: state.anchorKey,
    isSelected,
    toggle,
    selectRange: selectRange$1,
    selectAll: selectAll$1,
    deselect: deselect$1,
    clear
  }), [
    state.selectedKeys,
    selectedIds,
    selectedFolderIds,
    state.anchorKey,
    isSelected,
    toggle,
    selectRange$1,
    selectAll$1,
    deselect$1,
    clear
  ]);
  return (0, import_react2.createElement)(AssetSelectionContext.Provider, {
    value
  }, children);
};
var useAssetSelection = () => {
  const context = (0, import_react2.useContext)(AssetSelectionContext);
  if (!context) {
    throw new Error("useAssetSelection must be used within an AssetSelectionProvider");
  }
  return context;
};
var useAssetSelectionOptional = () => (0, import_react2.useContext)(AssetSelectionContext);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useBusyAssets.mjs
var import_react3 = __toESM(require_react(), 1);
var BusyAssetsContext = (0, import_react3.createContext)(null);
var BusyAssetsProvider = ({ children }) => {
  const [busyById, setBusyById] = (0, import_react3.useState)({});
  const markBusy = (0, import_react3.useCallback)((id, message) => {
    setBusyById((prev) => ({
      ...prev,
      [id]: message
    }));
    return () => setBusyById((prev) => {
      const { [id]: _released, ...rest } = prev;
      return rest;
    });
  }, []);
  const isBusy = (0, import_react3.useCallback)((id) => busyById[id] !== void 0, [
    busyById
  ]);
  const getBusyMessage2 = (0, import_react3.useCallback)((id) => busyById[id] ?? null, [
    busyById
  ]);
  const value = (0, import_react3.useMemo)(() => ({
    isBusy,
    getBusyMessage: getBusyMessage2,
    markBusy
  }), [
    isBusy,
    getBusyMessage2,
    markBusy
  ]);
  return (0, import_react3.createElement)(BusyAssetsContext.Provider, {
    value
  }, children);
};
var useBusyAssetsOptional = () => (0, import_react3.useContext)(BusyAssetsContext);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useFolderNavigation.mjs
var import_react4 = __toESM(require_react(), 1);
var parseFolderIdFromQuery = (folder) => {
  if (!folder) {
    return null;
  }
  const id = Number(folder);
  return Number.isFinite(id) ? id : null;
};
var useFolderNavigation = () => {
  const [{ query }, setQuery] = useQueryParams();
  const currentFolderId = parseFolderIdFromQuery(query == null ? void 0 : query.folder);
  const navigateToFolder = (0, import_react4.useCallback)((folder) => {
    setQuery({
      folder: String(folder.id),
      _q: void 0
    });
  }, [
    setQuery
  ]);
  const navigateToRoot = (0, import_react4.useCallback)(() => {
    setQuery({
      folder: "",
      _q: ""
    }, "remove");
  }, [
    setQuery
  ]);
  const stripFolderParam = (0, import_react4.useCallback)(() => {
    setQuery(withEncodedUserParams(query, {
      folder: void 0
    }));
  }, [
    query,
    setQuery
  ]);
  (0, import_react4.useEffect)(() => {
    if ((query == null ? void 0 : query.folder) && currentFolderId === null) {
      stripFolderParam();
    }
  }, [
    query == null ? void 0 : query.folder,
    currentFolderId,
    stripFolderParam
  ]);
  const navigateToFolderId = (0, import_react4.useCallback)((folderId) => {
    if (folderId == null) {
      navigateToRoot();
    } else {
      setQuery({
        folder: String(folderId),
        _q: void 0
      });
    }
  }, [
    navigateToRoot,
    setQuery
  ]);
  return {
    currentFolderId,
    navigateToFolder,
    navigateToRoot,
    navigateToFolderId
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/utils/renderedKeys.mjs
var buildRenderedKeys = ({ folders, assets, mixedItems }) => mixedItems ? mixedItems.map((item) => item.kind === "folder" ? folderKey(item.folder.id) : assetKey(item.asset.id)) : [
  ...folders.map((folder) => folderKey(folder.id)),
  ...assets.map((asset) => assetKey(asset.id))
];

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetActionsMenu.mjs
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var import_react7 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/ActionsMenuContent.mjs
var ActionsMenuContent = gt(gd.Content).attrs({
  maxHeight: "min(var(--radix-popper-available-height, 100vh), 100vh)"
})`
  scrollbar-width: thin;
  -ms-overflow-style: auto;

  &::-webkit-scrollbar {
    display: block;
    width: 0.4rem;
  }

  &::-webkit-scrollbar-thumb {
    background: ${({ theme }) => theme.colors.neutral300};
    border-radius: ${({ theme }) => theme.borderRadius};
  }
`;

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/BulkMoveDialog.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var import_react5 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/utils/canDropItemOnFolder.mjs
var findNode = (nodes, id) => {
  for (const node of nodes) {
    if (node.id === id) {
      return node;
    }
    const found = findNode(node.children, id);
    if (found) {
      return found;
    }
  }
  return null;
};
var collectDescendantIds = (node) => {
  const ids = /* @__PURE__ */ new Set();
  const walk = (current) => {
    for (const child of current.children) {
      if (child.id != null) {
        ids.add(child.id);
      }
      walk(child);
    }
  };
  walk(node);
  return ids;
};
var isFolderDescendantOf = (folderStructure, ancestorId, candidateId) => {
  if (ancestorId === candidateId) {
    return true;
  }
  const ancestorNode = findNode(folderStructure, ancestorId);
  if (!ancestorNode) {
    return false;
  }
  return collectDescendantIds(ancestorNode).has(candidateId);
};
var isItemAtRoot = (item) => item.kind === "file" ? item.folderId == null : item.parentId == null;
var canDropItemOnFolder = ({ items, targetFolderId, folderStructure }) => {
  if (items.length === 0) {
    return false;
  }
  if (targetFolderId === null) {
    return items.some((item) => !isItemAtRoot(item));
  }
  const draggedFolderIds = new Set(items.filter((item) => item.kind === "folder").map((item) => item.id));
  if (draggedFolderIds.has(targetFolderId)) {
    return false;
  }
  for (const folderId of draggedFolderIds) {
    if (isFolderDescendantOf(folderStructure, folderId, targetFolderId)) {
      return false;
    }
  }
  for (const item of items) {
    if (item.kind === "file" && item.folderId === targetFolderId) {
      return false;
    }
    if (item.kind === "folder" && item.parentId === targetFolderId) {
      return false;
    }
  }
  return true;
};

// node_modules/@strapi/upload/dist/admin/future/utils/flattenFolderStructure.mjs
var flattenFolderStructure = (nodes, excludeSubtreeIds = /* @__PURE__ */ new Set(), parentLabel = "") => nodes.flatMap((node) => {
  if (node.id == null || excludeSubtreeIds.has(node.id)) {
    return [];
  }
  const label = parentLabel ? `${parentLabel} / ${node.name ?? ""}` : node.name ?? "";
  return [
    {
      id: node.id,
      label
    },
    ...flattenFolderStructure(node.children ?? [], excludeSubtreeIds, label)
  ];
});

// node_modules/@strapi/upload/dist/admin/future/utils/formatMoveSuccessMessage.mjs
var formatMoveSuccessMessage = ({ formatMessage, count, source, destination }) => {
  if (source === null) {
    return formatMessage({
      id: getTranslationKey("list.bulk-actions.move.success-multiple-sources"),
      defaultMessage: "{count, plural, =1 {# element has} other {# elements have}} been moved to {destination}"
    }, {
      count,
      destination
    });
  }
  return formatMessage({
    id: getTranslationKey("list.bulk-actions.move.success"),
    defaultMessage: "{count, plural, =1 {# element has} other {# elements have}} been moved from {source} to {destination}"
  }, {
    count,
    source,
    destination
  });
};

// node_modules/@strapi/upload/dist/admin/future/utils/itemSource.mjs
var sourceFolderIdOf = (item) => item.kind === "folder" ? item.parentId : item.folderId;
var uniformSourceFolderId = (items) => {
  if (!hasUniformSource(items)) {
    return null;
  }
  return sourceFolderIdOf(items[0]);
};
var hasUniformSource = (items) => {
  if (items.length === 0) {
    return false;
  }
  const first = sourceFolderIdOf(items[0]);
  return items.every((item) => sourceFolderIdOf(item) === first);
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/BulkMoveDialog.mjs
var StyledModalContent = gt(_d.Content)`
  max-width: 51.6rem;
`;
var BulkMoveDialog = ({ open, onClose, items, onSuccess }) => {
  var _a;
  const { formatMessage } = useIntl();
  const getErrorMessage = useApiErrorMessage();
  const { toggleNotification } = useNotification();
  const { data: folderStructure = [], isUninitialized: isStructureUninitialized, isLoading: isLoadingStructure, isError: isStructureError } = useGetFolderStructureQuery(void 0, {
    skip: !open
  });
  const [bulkMove, { isLoading: isMoving }] = useBulkMoveMutation();
  const fileIds = (0, import_react5.useMemo)(() => items.filter((item) => item.kind === "file").map((item) => item.id), [
    items
  ]);
  const folderIds = (0, import_react5.useMemo)(() => items.filter((item) => item.kind === "folder").map((item) => item.id), [
    items
  ]);
  const isSourceUniform = hasUniformSource(items);
  const sourceFolderId = uniformSourceFolderId(items);
  const { data: sourceFolder } = useGetFolderQuery({
    id: sourceFolderId
  }, {
    skip: sourceFolderId === null
  });
  const [destination, setDestination] = (0, import_react5.useState)("");
  const rootLabel = formatMessage({
    id: getTranslationKey("plugin.name"),
    defaultMessage: "Media Library"
  });
  const destinationOptions = (0, import_react5.useMemo)(() => flattenFolderStructure(folderStructure, new Set(folderIds)).filter((option) => canDropItemOnFolder({
    items,
    targetFolderId: option.id,
    folderStructure
  })), [
    folderStructure,
    folderIds,
    items
  ]);
  const canMoveToRoot = (0, import_react5.useMemo)(() => canDropItemOnFolder({
    items,
    targetFolderId: null,
    folderStructure
  }), [
    items,
    folderStructure
  ]);
  const defaultDestination = canMoveToRoot ? "" : ((_a = destinationOptions[0]) == null ? void 0 : _a.id.toString()) ?? "";
  (0, import_react5.useEffect)(() => {
    setDestination(defaultDestination);
  }, [
    open,
    defaultDestination
  ]);
  const isStructureReady = !isStructureUninitialized && !isLoadingStructure && !isStructureError;
  const hasNoDestination = isStructureReady && destinationOptions.length === 0 && !canMoveToRoot;
  const count = items.length;
  const handleMove = async () => {
    var _a2;
    if (isMoving || !isStructureReady) {
      return;
    }
    const destinationFolderId = destination === "" ? null : Number(destination);
    try {
      await bulkMove({
        fileIds,
        folderIds,
        destinationFolderId
      }).unwrap();
    } catch (error) {
      toggleNotification({
        type: "danger",
        message: getErrorMessage(error, formatMessage({
          id: getTranslationKey("list.bulk-actions.move.error"),
          defaultMessage: "An error occurred while moving the items."
        }))
      });
      return;
    }
    const sourceName = !isSourceUniform ? null : sourceFolderId === null ? rootLabel : (sourceFolder == null ? void 0 : sourceFolder.name) ?? rootLabel;
    const destinationName = destinationFolderId === null ? rootLabel : ((_a2 = destinationOptions.find((option) => option.id === destinationFolderId)) == null ? void 0 : _a2.label) ?? rootLabel;
    toggleNotification({
      type: "success",
      message: formatMoveSuccessMessage({
        formatMessage,
        count,
        source: sourceName,
        destination: destinationName
      })
    });
    onSuccess == null ? void 0 : onSuccess();
    onClose();
  };
  const renderBody = () => {
    if (isStructureError) {
      return (0, import_jsx_runtime6.jsx)(A, {
        textColor: "danger600",
        children: formatMessage({
          id: getTranslationKey("list.bulk-actions.move.load-error"),
          defaultMessage: "Couldn't load the folder list. Please try again."
        })
      });
    }
    if (hasNoDestination) {
      return (0, import_jsx_runtime6.jsx)(A, {
        textColor: "neutral600",
        children: formatMessage({
          id: getTranslationKey("list.bulk-actions.move.no-destination"),
          defaultMessage: "There is no other folder to move this to."
        })
      });
    }
    return (0, import_jsx_runtime6.jsxs)(Dd.Root, {
      name: "destination",
      children: [
        (0, import_jsx_runtime6.jsx)(Dd.Label, {
          children: formatMessage({
            id: getTranslationKey("list.bulk-actions.move.location"),
            defaultMessage: "Location"
          })
        }),
        (0, import_jsx_runtime6.jsxs)(cr, {
          value: destination,
          onChange: (value) => setDestination(String(value)),
          disabled: isMoving || !isStructureReady,
          children: [
            canMoveToRoot && (0, import_jsx_runtime6.jsx)(dr, {
              value: "",
              children: rootLabel
            }),
            destinationOptions.map((option) => (0, import_jsx_runtime6.jsx)(dr, {
              value: String(option.id),
              children: option.label
            }, option.id))
          ]
        })
      ]
    });
  };
  return (0, import_jsx_runtime6.jsx)(_d.Root, {
    open,
    onOpenChange: (nextOpen) => {
      if (!nextOpen && !isMoving) {
        onClose();
      }
    },
    children: (0, import_jsx_runtime6.jsxs)(StyledModalContent, {
      children: [
        (0, import_jsx_runtime6.jsx)(_d.Header, {
          children: (0, import_jsx_runtime6.jsx)(_d.Title, {
            children: formatMessage({
              id: getTranslationKey("list.bulk-actions.move.title"),
              defaultMessage: "Move elements to"
            })
          })
        }),
        (0, import_jsx_runtime6.jsx)(_d.Body, {
          children: renderBody()
        }),
        (0, import_jsx_runtime6.jsx)(_d.Footer, {
          children: (0, import_jsx_runtime6.jsxs)(C, {
            gap: 2,
            justifyContent: "space-between",
            width: "100%",
            children: [
              (0, import_jsx_runtime6.jsx)(Ct, {
                variant: "tertiary",
                onClick: onClose,
                disabled: isMoving,
                type: "button",
                children: formatMessage({
                  id: "app.components.Button.cancel",
                  defaultMessage: "Cancel"
                })
              }),
              (0, import_jsx_runtime6.jsx)(Ct, {
                onClick: handleMove,
                loading: isMoving,
                disabled: !isStructureReady || hasNoDestination,
                children: formatMessage({
                  id: getTranslationKey("list.bulk-actions.move.submit"),
                  defaultMessage: "Move"
                })
              })
            ]
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DeleteItemsDialog.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var import_react6 = __toESM(require_react(), 1);
var DeleteItemsDialog = ({ open, onClose, target, onSuccess, onPendingChange }) => {
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const [bulkDeleteItems, { isLoading: isDeleting }] = useBulkDeleteItemsMutation();
  const count = target.fileIds.length + target.folderIds.length;
  (0, import_react6.useEffect)(() => {
    onPendingChange == null ? void 0 : onPendingChange(isDeleting);
  }, [
    isDeleting,
    onPendingChange
  ]);
  const handleConfirmDelete = async (e) => {
    e.preventDefault();
    if (isDeleting) {
      return;
    }
    const res = await bulkDeleteItems(target);
    if ("error" in res) {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("list.bulk-actions.delete.error"),
          defaultMessage: "An error occurred while deleting the items."
        })
      });
      return;
    }
    onClose();
    toggleNotification({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("list.bulk-actions.delete.success"),
        defaultMessage: "{count, plural, =1 {# item has been deleted} other {# items have been deleted}}"
      }, {
        count
      })
    });
    onSuccess == null ? void 0 : onSuccess();
  };
  return (0, import_jsx_runtime7.jsx)(Pd.Root, {
    open,
    onOpenChange: (nextOpen) => {
      if (!nextOpen && !isDeleting) {
        onClose();
      }
    },
    children: (0, import_jsx_runtime7.jsxs)(Pd.Content, {
      children: [
        (0, import_jsx_runtime7.jsx)(Pd.Header, {
          children: formatMessage({
            id: getTranslationKey("list.bulk-actions.delete.confirm.title"),
            defaultMessage: "Delete {count, plural, =1 {# item} other {# items}}?"
          }, {
            count
          })
        }),
        (0, import_jsx_runtime7.jsx)(Pd.Body, {
          icon: (0, import_jsx_runtime7.jsx)(Jn, {
            width: "24px",
            height: "24px",
            fill: "danger600"
          }),
          textAlign: "center",
          children: (0, import_jsx_runtime7.jsx)(A, {
            children: formatMessage({
              id: getTranslationKey("list.bulk-actions.delete.confirm.description.are-you-sure"),
              defaultMessage: "These items cannot be recovered once deleted, and deleting a folder also deletes everything inside it. If they are currently in use, linked content will break and image containers will be empty."
            })
          })
        }),
        (0, import_jsx_runtime7.jsxs)(Pd.Footer, {
          children: [
            (0, import_jsx_runtime7.jsx)(Pd.Cancel, {
              children: (0, import_jsx_runtime7.jsx)(Ct, {
                variant: "tertiary",
                disabled: isDeleting,
                fullWidth: true,
                children: formatMessage({
                  id: "app.components.Button.cancel",
                  defaultMessage: "Cancel"
                })
              })
            }),
            (0, import_jsx_runtime7.jsx)(Pd.Action, {
              children: (0, import_jsx_runtime7.jsx)(Ct, {
                variant: "danger-light",
                loading: isDeleting,
                onClick: handleConfirmDelete,
                fullWidth: true,
                children: formatMessage({
                  id: "app.components.Button.confirm",
                  defaultMessage: "Confirm"
                })
              })
            })
          ]
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetActionsMenu.mjs
var AssetActionsMenu = ({ asset, dragData }) => {
  var _a;
  const { formatMessage } = useIntl();
  const getErrorMessage = useApiErrorMessage();
  const { copy } = useClipboard();
  const { toggleNotification } = useNotification();
  const { deselect: deselect2 } = useAssetSelection();
  const markBusy = ((_a = useBusyAssetsOptional()) == null ? void 0 : _a.markBusy) ?? (() => () => {
  });
  const { canUpdate, canDownload, canCopyLink, isLoading: isLoadingPermissions } = useMediaLibraryPermissions();
  const [replaceAsset, { isLoading: isReplacing }] = useReplaceAssetMutation();
  const aiEnabled = useAIMetadataEnabled({
    mime: asset.mime
  });
  const fileInputRef = (0, import_react7.useRef)(null);
  const [isReplaceOpen, setIsReplaceOpen] = (0, import_react7.useState)(false);
  const [isMoveOpen, setIsMoveOpen] = (0, import_react7.useState)(false);
  const [isDeleteOpen, setIsDeleteOpen] = (0, import_react7.useState)(false);
  const [isDownloading, setIsDownloading] = (0, import_react7.useState)(false);
  const moveItems = (0, import_react7.useMemo)(() => [
    dragData
  ], [
    dragData
  ]);
  const handleReplaceContinue = () => {
    var _a2;
    setIsReplaceOpen(false);
    (_a2 = fileInputRef.current) == null ? void 0 : _a2.click();
  };
  const handleFileChange = async (event) => {
    var _a2;
    const file = (_a2 = event.target.files) == null ? void 0 : _a2[0];
    event.target.value = "";
    if (!file) {
      return;
    }
    const releaseBusy = markBusy(asset.id, formatMessage({
      id: getTranslationKey("asset-details.replace.loading"),
      defaultMessage: "Replacing the file…"
    }));
    let res;
    try {
      res = await replaceAsset({
        id: asset.id,
        file,
        fileInfo: {
          name: asset.name
        }
      });
    } finally {
      releaseBusy();
    }
    if ("error" in res) {
      toggleNotification({
        type: "danger",
        message: getErrorMessage(res.error, formatMessage({
          id: getTranslationKey("asset-details.replace.error"),
          defaultMessage: "Failed to replace the file."
        }))
      });
      return;
    }
    toggleNotification({
      type: "success",
      message: formatMessage({
        id: getTranslationKey("asset-details.replace.success"),
        defaultMessage: "File replaced."
      })
    });
  };
  const handleCopyLink = async () => {
    const url = prefixFileUrlWithBackendUrl(asset.url);
    if (!url) {
      return;
    }
    const didCopy = await copy(url);
    toggleNotification({
      type: didCopy ? "success" : "danger",
      message: formatMessage(didCopy ? {
        id: getTranslationKey("asset-details.copy-link.success"),
        defaultMessage: "Link copied."
      } : {
        id: getTranslationKey("asset-details.copy-link.error"),
        defaultMessage: "Failed to copy the link."
      })
    });
  };
  const handleDownload = async () => {
    const url = prefixFileUrlWithBackendUrl(asset.url);
    if (!url) {
      return;
    }
    setIsDownloading(true);
    try {
      await downloadFile(url, asset.name);
    } catch {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("asset-details.download.error"),
          defaultMessage: "Failed to download the file."
        })
      });
    } finally {
      setIsDownloading(false);
    }
  };
  const hasTopGroup = canUpdate || canCopyLink || canDownload;
  const hasBottomGroup = canUpdate;
  const showSeparator = (canCopyLink || canDownload) && hasBottomGroup;
  if (!isLoadingPermissions && !hasTopGroup && !hasBottomGroup) {
    return null;
  }
  return (0, import_jsx_runtime8.jsxs)(import_jsx_runtime8.Fragment, {
    children: [
      (0, import_jsx_runtime8.jsx)($e, {
        children: (0, import_jsx_runtime8.jsx)("input", {
          ref: fileInputRef,
          type: "file",
          multiple: false,
          onChange: handleFileChange,
          "aria-hidden": true,
          tabIndex: -1
        })
      }),
      (0, import_jsx_runtime8.jsxs)(gd.Root, {
        modal: false,
        children: [
          (0, import_jsx_runtime8.jsx)(gd.Trigger, {
            tag: be,
            icon: (0, import_jsx_runtime8.jsx)(W3, {}),
            variant: "ghost",
            label: formatMessage({
              id: getTranslationKey("control-card.more-actions"),
              defaultMessage: "More actions"
            })
          }),
          (0, import_jsx_runtime8.jsxs)(ActionsMenuContent, {
            popoverPlacement: "bottom-end",
            zIndex: 2,
            minWidth: "22rem",
            children: [
              canUpdate && (0, import_jsx_runtime8.jsx)(gd.Item, {
                startIcon: (0, import_jsx_runtime8.jsx)(W2, {}),
                disabled: isReplacing,
                onSelect: () => setIsReplaceOpen(true),
                children: formatMessage({
                  id: getTranslationKey("list.assets.actions.replace"),
                  defaultMessage: "Replace media"
                })
              }),
              canCopyLink && (0, import_jsx_runtime8.jsx)(gd.Item, {
                startIcon: (0, import_jsx_runtime8.jsx)(R3, {}),
                onSelect: handleCopyLink,
                children: formatMessage({
                  id: getTranslationKey("list.assets.actions.copy-link"),
                  defaultMessage: "Copy link to media"
                })
              }),
              canDownload && (0, import_jsx_runtime8.jsx)(gd.Item, {
                startIcon: (0, import_jsx_runtime8.jsx)(z5, {}),
                disabled: isDownloading,
                onSelect: handleDownload,
                children: formatMessage({
                  id: getTranslationKey("list.assets.actions.download"),
                  defaultMessage: "Download media"
                })
              }),
              showSeparator && (0, import_jsx_runtime8.jsx)(gd.Separator, {}),
              canUpdate && (0, import_jsx_runtime8.jsxs)(import_jsx_runtime8.Fragment, {
                children: [
                  (0, import_jsx_runtime8.jsx)(gd.Item, {
                    startIcon: (0, import_jsx_runtime8.jsx)(I2, {}),
                    onSelect: () => setIsMoveOpen(true),
                    children: formatMessage({
                      id: getTranslationKey("list.assets.actions.move"),
                      defaultMessage: "Move to folder"
                    })
                  }),
                  (0, import_jsx_runtime8.jsx)(gd.Item, {
                    startIcon: (0, import_jsx_runtime8.jsx)(In, {}),
                    variant: "danger",
                    onSelect: () => setIsDeleteOpen(true),
                    children: formatMessage({
                      id: getTranslationKey("list.assets.actions.delete"),
                      defaultMessage: "Delete"
                    })
                  })
                ]
              })
            ]
          })
        ]
      }),
      (0, import_jsx_runtime8.jsx)(Pd.Root, {
        open: isReplaceOpen,
        onOpenChange: setIsReplaceOpen,
        children: (0, import_jsx_runtime8.jsxs)(Pd.Content, {
          children: [
            (0, import_jsx_runtime8.jsx)(Pd.Header, {
              children: formatMessage({
                id: getTranslationKey("asset-details.replace.title"),
                defaultMessage: "Replace this media file?"
              })
            }),
            (0, import_jsx_runtime8.jsx)(Pd.Body, {
              textAlign: "center",
              children: (0, import_jsx_runtime8.jsxs)(C, {
                direction: "column",
                textAlign: "center",
                children: [
                  (0, import_jsx_runtime8.jsx)(A, {
                    variant: "omega",
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.description"),
                      defaultMessage: "Current content will be permanently replaced."
                    })
                  }),
                  aiEnabled ? (0, import_jsx_runtime8.jsx)(A, {
                    variant: "omega",
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.description.ai"),
                      defaultMessage: "AI will generate new metadata after upload."
                    })
                  }) : null
                ]
              })
            }),
            (0, import_jsx_runtime8.jsxs)(Pd.Footer, {
              children: [
                (0, import_jsx_runtime8.jsx)(Pd.Cancel, {
                  children: (0, import_jsx_runtime8.jsx)(Ct, {
                    variant: "tertiary",
                    fullWidth: true,
                    children: formatMessage({
                      id: "app.components.Button.cancel",
                      defaultMessage: "Cancel"
                    })
                  })
                }),
                (0, import_jsx_runtime8.jsx)(Pd.Action, {
                  children: (0, import_jsx_runtime8.jsx)(Ct, {
                    variant: "secondary",
                    onClick: handleReplaceContinue,
                    fullWidth: true,
                    children: formatMessage({
                      id: getTranslationKey("asset-details.replace.continue"),
                      defaultMessage: "Continue"
                    })
                  })
                })
              ]
            })
          ]
        })
      }),
      isMoveOpen && (0, import_jsx_runtime8.jsx)(BulkMoveDialog, {
        open: true,
        onClose: () => setIsMoveOpen(false),
        items: moveItems,
        onSuccess: () => deselect2(assetKey(asset.id))
      }),
      isDeleteOpen && (0, import_jsx_runtime8.jsx)(DeleteItemsDialog, {
        open: true,
        onClose: () => setIsDeleteOpen(false),
        target: {
          fileIds: [
            asset.id
          ],
          folderIds: []
        },
        onSuccess: () => deselect2(assetKey(asset.id))
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/AssetsDndProvider.mjs
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var import_react8 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/utils/buildBulkMovePayload.mjs
var buildBulkMovePayload = (items) => {
  const fileIds = [];
  const folderIds = [];
  for (const item of items) {
    if (item.kind === "file") {
      fileIds.push(item.id);
    } else {
      folderIds.push(item.id);
    }
  }
  return {
    fileIds,
    folderIds
  };
};

// node_modules/@strapi/upload/dist/admin/future/utils/getFolderLabel.mjs
var getFolderLabel = (folderStructure, id, rootLabel) => {
  var _a;
  if (id === null) {
    return rootLabel;
  }
  const findNode2 = (nodes) => {
    for (const node of nodes) {
      if (node.id === id) {
        return node;
      }
      const found = findNode2(node.children ?? []);
      if (found) {
        return found;
      }
    }
    return null;
  };
  return ((_a = findNode2(folderStructure)) == null ? void 0 : _a.name) ?? rootLabel;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/buildDragSet.mjs
var buildDragSet = (activeData, selectedKeys, locations, fallbackFolderId) => {
  const activeKey = activeData.kind === "file" ? assetKey(activeData.id) : folderKey(activeData.id);
  const activeSourceFolderId = sourceFolderIdOf(activeData);
  if (!selectedKeys || !selectedKeys.has(activeKey)) {
    return {
      items: [
        activeData
      ],
      fromSelection: false,
      activeSourceFolderId,
      spansMultipleSources: false
    };
  }
  const items = [];
  selectedKeys.forEach((key) => {
    const separator = key.indexOf(":");
    const kind = key.slice(0, separator);
    const id = Number(key.slice(separator + 1));
    if (kind === "asset") {
      if (activeData.kind === "file" && activeData.id === id) {
        items.push(activeData);
        return;
      }
      items.push({
        kind: "file",
        id,
        name: "",
        folderId: locateItem(locations, "file", id, fallbackFolderId)
      });
      return;
    }
    if (activeData.kind === "folder" && activeData.id === id) {
      items.push(activeData);
      return;
    }
    items.push({
      kind: "folder",
      id,
      name: "",
      parentId: locateItem(locations, "folder", id, fallbackFolderId)
    });
  });
  return {
    items,
    fromSelection: true,
    activeSourceFolderId,
    spansMultipleSources: items.some((item) => sourceFolderIdOf(item) !== activeSourceFolderId)
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/computeValidDropTargets.mjs
var computeValidDropTargets = (items, folderStructure) => {
  const valid = /* @__PURE__ */ new Set();
  if (items.length === 0) {
    return valid;
  }
  if (canDropItemOnFolder({
    items,
    targetFolderId: null,
    folderStructure
  })) {
    valid.add(null);
  }
  for (const { id } of flattenFolderStructure(folderStructure)) {
    if (canDropItemOnFolder({
      items,
      targetFolderId: id,
      folderStructure
    })) {
      valid.add(id);
    }
  }
  return valid;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/dndIds.mjs
var toFileDraggableId = (id) => `file:${id}`;
var toFolderDraggableId = (id) => `folder:${id}`;
var toFolderTargetId = (id) => `folder-target:${id}`;
var parseFolderTargetId = (dndId) => {
  if (typeof dndId !== "string") {
    return null;
  }
  const match = /^folder-target:(\d+)$/.exec(dndId);
  return match ? Number(match[1]) : null;
};
var toFolderTreeTargetId = (id) => `folder-tree-target:${id}`;
var HOME_TREE_TARGET_ID = "folder-tree-target:home";
var parseFolderTreeTargetId = (dndId) => {
  if (typeof dndId !== "string") {
    return null;
  }
  if (dndId === HOME_TREE_TARGET_ID) {
    return "root";
  }
  const match = /^folder-tree-target:(\d+)$/.exec(dndId);
  return match ? Number(match[1]) : null;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/DragOverlayChip.mjs
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var FOLDER_ICON_SIZE = 20;
var FILE_ICON_SIZE = 24;
var ICON_BOX = 24;
var Chip = gt(C)`
  position: relative;
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]};
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[3]}`};
  border-radius: ${({ theme }) => theme.borderRadius};
  background: ${({ theme }) => theme.colors.primary100};
  box-shadow: ${({ theme }) => theme.shadows.tableShadow};
  cursor: grabbing;
  max-width: 24rem;
`;
var CompositeChip = gt(Chip)`
  box-shadow:
    ${({ theme }) => theme.shadows.tableShadow},
    0 4px 0 -1px ${({ theme }) => theme.colors.primary100},
    0 4px 0 0 ${({ theme }) => theme.colors.primary200},
    0 7px 0 -1px ${({ theme }) => theme.colors.primary100},
    0 7px 0 0 ${({ theme }) => theme.colors.primary200};
`;
var CountGroup = gt(C)`
  align-items: center;
  gap: ${({ theme }) => theme.spaces[1]};
`;
var IconBox = gt(C)`
  flex-shrink: 0;
  width: ${ICON_BOX}px;
  height: ${ICON_BOX}px;
  align-items: center;
  justify-content: center;
`;
var TotalBadge = gt(C)`
  position: absolute;
  top: -${({ theme }) => theme.spaces[2]};
  right: -${({ theme }) => theme.spaces[2]};
  align-items: center;
  justify-content: center;
  min-width: ${({ theme }) => theme.spaces[5]};
  height: ${({ theme }) => theme.spaces[5]};
  padding: 0 ${({ theme }) => theme.spaces[1]};
  border-radius: ${({ theme }) => theme.borderRadius};
  background: ${({ theme }) => theme.colors.primary600};
`;
var DragOverlayChip = ({ items }) => {
  const { formatMessage } = useIntl();
  if (items.length === 0) {
    return null;
  }
  if (items.length === 1) {
    const item = items[0];
    const isFolder = item.kind === "folder";
    const Icon = isFolder ? _5 : Z5;
    const size = isFolder ? FOLDER_ICON_SIZE : FILE_ICON_SIZE;
    return (0, import_jsx_runtime9.jsxs)(Chip, {
      children: [
        (0, import_jsx_runtime9.jsx)(IconBox, {
          children: (0, import_jsx_runtime9.jsx)(Icon, {
            width: size,
            height: size
          })
        }),
        (0, import_jsx_runtime9.jsx)(A, {
          textColor: "neutral800",
          fontWeight: "semiBold",
          ellipsis: true,
          children: item.name
        })
      ]
    });
  }
  const folderCount = items.filter((item) => item.kind === "folder").length;
  const fileCount = items.filter((item) => item.kind === "file").length;
  const total = folderCount + fileCount;
  return (0, import_jsx_runtime9.jsxs)(CompositeChip, {
    gap: 3,
    children: [
      folderCount > 0 ? (0, import_jsx_runtime9.jsxs)(CountGroup, {
        children: [
          (0, import_jsx_runtime9.jsx)(IconBox, {
            children: (0, import_jsx_runtime9.jsx)(_5, {
              width: FOLDER_ICON_SIZE,
              height: FOLDER_ICON_SIZE
            })
          }),
          (0, import_jsx_runtime9.jsx)(A, {
            textColor: "neutral800",
            fontWeight: "semiBold",
            children: formatMessage({
              id: getTranslationKey("dnd.overlay.folders"),
              defaultMessage: "{count, plural, one {# folder} other {# folders}}"
            }, {
              count: folderCount
            })
          })
        ]
      }) : null,
      fileCount > 0 ? (0, import_jsx_runtime9.jsxs)(CountGroup, {
        children: [
          (0, import_jsx_runtime9.jsx)(IconBox, {
            children: (0, import_jsx_runtime9.jsx)(Z5, {
              width: FILE_ICON_SIZE,
              height: FILE_ICON_SIZE
            })
          }),
          (0, import_jsx_runtime9.jsx)(A, {
            textColor: "neutral800",
            fontWeight: "semiBold",
            children: formatMessage({
              id: getTranslationKey("dnd.overlay.files"),
              defaultMessage: "{count, plural, one {# file} other {# files}}"
            }, {
              count: fileCount
            })
          })
        ]
      }) : null,
      (0, import_jsx_runtime9.jsx)(TotalBadge, {
        children: (0, import_jsx_runtime9.jsx)(A, {
          textColor: "neutral0",
          fontWeight: "bold",
          variant: "pi",
          children: total
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/AssetsDndProvider.mjs
var AssetsDndContext = (0, import_react8.createContext)(null);
var useAssetsDndOptional = () => (0, import_react8.useContext)(AssetsDndContext);
var resolveDestination = (overId) => {
  const inView = parseFolderTargetId(overId);
  if (inView != null) {
    return {
      destinationFolderId: inView
    };
  }
  const tree = parseFolderTreeTargetId(overId);
  if (tree === "root") {
    return {
      destinationFolderId: null
    };
  }
  if (typeof tree === "number") {
    return {
      destinationFolderId: tree
    };
  }
  return null;
};
var DRAG_DISABLED_DISTANCE = Number.MAX_SAFE_INTEGER;
var AssetsDndProvider = ({ children, locations = emptyItemLocations }) => {
  const { formatMessage } = useIntl();
  const getErrorMessage = useApiErrorMessage();
  const { toggleNotification } = useNotification();
  const selection = useAssetSelectionOptional();
  const { currentFolderId } = useFolderNavigation();
  const { data: folderStructure = [] } = useGetFolderStructureQuery();
  const rootLabel = formatMessage({
    id: getTranslationKey("plugin.name"),
    defaultMessage: "Media Library"
  });
  const [bulkMove, { isLoading: isMovePending }] = useBulkMoveMutation();
  const [dragItems, setDragItems] = (0, import_react8.useState)([]);
  const [liveAnnouncement, setLiveAnnouncement] = (0, import_react8.useState)("");
  const dragSessionRef = (0, import_react8.useRef)({
    items: [],
    fromSelection: false,
    activeSourceFolderId: null,
    spansMultipleSources: false
  });
  const announceToLiveRegion = (0, import_react8.useCallback)((message) => {
    setLiveAnnouncement("");
    requestAnimationFrame(() => setLiveAnnouncement(message));
  }, []);
  const { canUpdate } = useMediaLibraryPermissions();
  const sensors = useSensors(useSensor(PointerSensor, {
    activationConstraint: {
      distance: canUpdate ? 8 : DRAG_DISABLED_DISTANCE
    }
  }));
  const validDropTargets = (0, import_react8.useMemo)(() => computeValidDropTargets(dragItems, folderStructure), [
    dragItems,
    folderStructure
  ]);
  const isValidDropTarget = (0, import_react8.useCallback)((targetFolderId) => validDropTargets.has(targetFolderId), [
    validDropTargets
  ]);
  const contextValue = (0, import_react8.useMemo)(() => ({
    isInternalDragActive: dragItems.length > 0,
    isMovePending,
    isValidDropTarget
  }), [
    dragItems.length,
    isMovePending,
    isValidDropTarget
  ]);
  const clearDragState = (0, import_react8.useCallback)(() => {
    dragSessionRef.current = {
      items: [],
      fromSelection: false,
      activeSourceFolderId: null,
      spansMultipleSources: false
    };
    setDragItems([]);
  }, []);
  const handleDragStart = (0, import_react8.useCallback)((event) => {
    const data = event.active.data.current;
    if (!data) {
      clearDragState();
      return;
    }
    const dragSet = buildDragSet(data, selection == null ? void 0 : selection.selectedKeys, locations, currentFolderId);
    dragSessionRef.current = dragSet;
    setDragItems(dragSet.items);
  }, [
    clearDragState,
    currentFolderId,
    locations,
    selection == null ? void 0 : selection.selectedKeys
  ]);
  const handleDragEnd = (0, import_react8.useCallback)(async (event) => {
    const { over } = event;
    const { items, fromSelection, activeSourceFolderId, spansMultipleSources } = dragSessionRef.current;
    clearDragState();
    if (isMovePending || !over || items.length === 0) {
      return;
    }
    const destination = resolveDestination(over.id);
    if (!destination) {
      return;
    }
    const { destinationFolderId: targetFolderId } = destination;
    if (!canDropItemOnFolder({
      items,
      targetFolderId,
      folderStructure
    })) {
      return;
    }
    const payload = buildBulkMovePayload(items);
    const successMessage = formatMoveSuccessMessage({
      formatMessage,
      count: items.length,
      source: spansMultipleSources ? null : getFolderLabel(folderStructure, activeSourceFolderId, rootLabel),
      destination: getFolderLabel(folderStructure, targetFolderId, rootLabel)
    });
    const errorFallback = formatMessage({
      id: getTranslationKey("list.bulk-actions.move.error"),
      defaultMessage: "An error occurred while moving the items."
    });
    try {
      await bulkMove({
        ...payload,
        destinationFolderId: targetFolderId
      }).unwrap();
      if (fromSelection) {
        selection == null ? void 0 : selection.clear();
      }
      announceToLiveRegion(successMessage);
      toggleNotification({
        type: "success",
        message: successMessage
      });
    } catch (error) {
      const errorMessage = getErrorMessage(error, errorFallback);
      announceToLiveRegion(formatMessage({
        id: getTranslationKey("dnd.announce.move-failure"),
        defaultMessage: "Move failed. {message}"
      }, {
        message: errorMessage
      }));
      toggleNotification({
        type: "danger",
        message: errorMessage
      });
    }
  }, [
    announceToLiveRegion,
    bulkMove,
    clearDragState,
    folderStructure,
    formatMessage,
    getErrorMessage,
    isMovePending,
    rootLabel,
    selection,
    toggleNotification
  ]);
  const handleDragCancel = (0, import_react8.useCallback)(() => {
    clearDragState();
  }, [
    clearDragState
  ]);
  const announcements = (0, import_react8.useMemo)(() => ({
    onDragStart: ({ active }) => {
      const data = active.data.current;
      return data ? formatMessage({
        id: getTranslationKey("dnd.announce.drag-start"),
        defaultMessage: "Picked up {name}. Drop on a folder to move."
      }, {
        name: data.name
      }) : "";
    },
    onDragOver: () => "",
    onDragEnd: ({ active, over }) => {
      if (!over) {
        return formatMessage({
          id: getTranslationKey("dnd.announce.cancel"),
          defaultMessage: "Drag cancelled."
        });
      }
      const destination = resolveDestination(over.id);
      const activeData = active.data.current;
      if (!destination || !activeData) {
        return "";
      }
      const { items } = buildDragSet(activeData, selection == null ? void 0 : selection.selectedKeys, locations, currentFolderId);
      if (!canDropItemOnFolder({
        items,
        targetFolderId: destination.destinationFolderId,
        folderStructure
      })) {
        return formatMessage({
          id: getTranslationKey("dnd.announce.invalid-drop"),
          defaultMessage: "Cannot move item to this folder."
        });
      }
      return "";
    },
    onDragCancel: () => formatMessage({
      id: getTranslationKey("dnd.announce.cancel"),
      defaultMessage: "Drag cancelled."
    })
  }), [
    currentFolderId,
    folderStructure,
    formatMessage,
    locations,
    selection == null ? void 0 : selection.selectedKeys
  ]);
  return (0, import_jsx_runtime10.jsx)(AssetsDndContext.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime10.jsxs)(DndContext, {
      sensors,
      collisionDetection: pointerWithin,
      onDragStart: handleDragStart,
      onDragEnd: handleDragEnd,
      onDragCancel: handleDragCancel,
      accessibility: {
        announcements
      },
      children: [
        (0, import_jsx_runtime10.jsx)($e, {
          "aria-live": "polite",
          "aria-atomic": "true",
          children: liveAnnouncement
        }),
        (0, import_jsx_runtime10.jsx)(C, {
          position: "relative",
          alignItems: "stretch",
          direction: "column",
          height: "100%",
          children
        }),
        (0, import_jsx_runtime10.jsx)(DragOverlay, {
          dropAnimation: null,
          children: dragItems.length > 0 ? (0, import_jsx_runtime10.jsx)(DragOverlayChip, {
            items: dragItems
          }) : null
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/useAssetDnd.mjs
var import_react9 = __toESM(require_react(), 1);
var useFileDraggable = (asset) => {
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const data = (0, import_react9.useMemo)(() => ({
    kind: "file",
    id: asset.id,
    name: asset.name,
    folderId: getRelationId(asset.folder)
  }), [
    asset.folder,
    asset.id,
    asset.name
  ]);
  const draggable = useDraggable({
    id: toFileDraggableId(asset.id),
    data,
    disabled: isMovePending
  });
  return {
    ...draggable,
    dragData: data
  };
};
var useFolderDraggableDroppable = (folder) => {
  const { isMovePending, isValidDropTarget } = useAssetsDndOptional() ?? {
    isMovePending: false,
    isValidDropTarget: () => false
  };
  const { active } = useDndContext();
  const parentId = getRelationId(folder.parent);
  const dragData = (0, import_react9.useMemo)(() => ({
    kind: "folder",
    id: folder.id,
    name: folder.name,
    parentId
  }), [
    folder.id,
    folder.name,
    parentId
  ]);
  const dropData = (0, import_react9.useMemo)(() => ({
    kind: "folder-target",
    id: folder.id,
    name: folder.name
  }), [
    folder.id,
    folder.name
  ]);
  const draggable = useDraggable({
    id: toFolderDraggableId(folder.id),
    data: dragData,
    disabled: isMovePending
  });
  const droppable = useDroppable({
    id: toFolderTargetId(folder.id),
    data: dropData,
    disabled: isMovePending
  });
  const isValidTarget = isValidDropTarget(folder.id);
  const isOver = droppable.isOver;
  const showValidDropHighlight = isOver && isValidTarget;
  const showInvalidDropCursor = isOver && !isValidTarget && active != null;
  return {
    dragData,
    draggable,
    droppable,
    isDragging: draggable.isDragging,
    showValidDropHighlight,
    showInvalidDropCursor
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FolderActionsMenu.mjs
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
var import_react11 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FolderFormDialog.mjs
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var import_react10 = __toESM(require_react(), 1);
var StyledModalContent2 = gt(_d.Content)`
  max-width: 51.6rem;
`;
var FolderFormDialog = (props) => {
  const { open, parentFolderId, onClose, mode } = props;
  const initialName = props.mode === "rename" ? props.initialName : "";
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const { trackUsage } = useTracking2();
  const [name, setName] = (0, import_react10.useState)(initialName);
  const [fieldError, setFieldError] = (0, import_react10.useState)();
  const inputRef = (0, import_react10.useRef)(null);
  const [createFolder, { isLoading: isCreating }] = useCreateFolderMutation();
  const [updateFolder, { isLoading: isUpdating }] = useUpdateFolderMutation();
  const isLoading = mode === "rename" ? isUpdating : isCreating;
  (0, import_react10.useEffect)(() => {
    var _a;
    if (open) {
      setName(initialName);
      setFieldError(void 0);
      if (mode === "rename") {
        (_a = inputRef.current) == null ? void 0 : _a.select();
      }
    }
  }, [
    open,
    initialName,
    mode
  ]);
  const handleSubmit = async (e) => {
    e.preventDefault();
    const trimmedName = name.trim();
    if (!trimmedName) {
      setFieldError(formatMessage({
        id: getTranslationKey("folder.create.form.error.name-required"),
        defaultMessage: "Name is required"
      }));
      return;
    }
    try {
      if (props.mode === "rename") {
        await updateFolder({
          id: props.folderId,
          name: trimmedName,
          parent: parentFolderId
        }).unwrap();
        trackUsage("didEditMediaLibraryElements", {
          location: MEDIA_LIBRARY_LOCATION,
          type: "folder",
          changeLocation: false
        });
      } else {
        await createFolder({
          name: trimmedName,
          parent: parentFolderId
        }).unwrap();
        trackUsage("didAddMediaLibraryFolders", {
          location: MEDIA_LIBRARY_LOCATION
        });
      }
      toggleNotification({
        type: "success",
        message: mode === "rename" ? formatMessage({
          id: getTranslationKey("folder.rename.success"),
          defaultMessage: "Folder has been renamed"
        }) : formatMessage({
          id: getTranslationKey("folder.create.success"),
          defaultMessage: "Folder has been created"
        })
      });
      onClose();
    } catch (err) {
      const apiError = err;
      if (apiError == null ? void 0 : apiError.message) {
        setFieldError(apiError.message);
      } else {
        toggleNotification({
          type: "danger",
          message: mode === "rename" ? formatMessage({
            id: getTranslationKey("folder.rename.form.error.unknown"),
            defaultMessage: "An error occurred while renaming the folder"
          }) : formatMessage({
            id: getTranslationKey("folder.create.form.error.unknown"),
            defaultMessage: "An error occurred while creating the folder"
          })
        });
      }
    }
  };
  return (0, import_jsx_runtime11.jsx)(_d.Root, {
    open,
    onOpenChange: onClose,
    children: (0, import_jsx_runtime11.jsxs)(StyledModalContent2, {
      children: [
        (0, import_jsx_runtime11.jsx)(_d.Header, {
          children: (0, import_jsx_runtime11.jsx)(_d.Title, {
            children: props.mode === "rename" ? formatMessage({
              id: getTranslationKey("folder.rename.title"),
              defaultMessage: "Rename folder"
            }) : formatMessage({
              id: getTranslationKey("folder.create.title-in"),
              defaultMessage: "New folder in {folderName}"
            }, {
              folderName: props.parentFolderName
            })
          })
        }),
        (0, import_jsx_runtime11.jsxs)("form", {
          onSubmit: handleSubmit,
          children: [
            (0, import_jsx_runtime11.jsx)(_d.Body, {
              children: (0, import_jsx_runtime11.jsxs)(Dd.Root, {
                error: fieldError,
                name: "name",
                required: true,
                children: [
                  (0, import_jsx_runtime11.jsx)(Dd.Label, {
                    children: formatMessage({
                      id: getTranslationKey("folder.form.name.label"),
                      defaultMessage: "Folder name"
                    })
                  }),
                  (0, import_jsx_runtime11.jsx)(Pc, {
                    ref: inputRef,
                    value: name,
                    onChange: (e) => {
                      setName(e.target.value);
                      setFieldError(void 0);
                    },
                    autoFocus: true
                  }),
                  (0, import_jsx_runtime11.jsx)(Dd.Error, {})
                ]
              })
            }),
            (0, import_jsx_runtime11.jsx)(_d.Footer, {
              children: (0, import_jsx_runtime11.jsxs)(C, {
                gap: 2,
                justifyContent: "space-between",
                width: "100%",
                children: [
                  (0, import_jsx_runtime11.jsx)(Ct, {
                    variant: "tertiary",
                    onClick: onClose,
                    type: "button",
                    children: formatMessage({
                      id: "app.components.Button.cancel",
                      defaultMessage: "Cancel"
                    })
                  }),
                  (0, import_jsx_runtime11.jsx)(Ct, {
                    type: "submit",
                    loading: isLoading,
                    disabled: mode === "rename" && name.trim() === initialName.trim(),
                    children: mode === "rename" ? formatMessage({
                      id: getTranslationKey("folder.rename.submit"),
                      defaultMessage: "Save"
                    }) : formatMessage({
                      id: getTranslationKey("folder.create.submit"),
                      defaultMessage: "Create folder"
                    })
                  })
                ]
              })
            })
          ]
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FolderActionsMenu.mjs
var FolderActionsMenu = ({ folder, dragData }) => {
  const { formatMessage } = useIntl();
  const { copy } = useClipboard();
  const { toggleNotification } = useNotification();
  const { deselect: deselect2 } = useAssetSelection();
  const [isRenameOpen, setIsRenameOpen] = (0, import_react11.useState)(false);
  const [isMoveOpen, setIsMoveOpen] = (0, import_react11.useState)(false);
  const [isDeleteOpen, setIsDeleteOpen] = (0, import_react11.useState)(false);
  const moveItems = (0, import_react11.useMemo)(() => [
    dragData
  ], [
    dragData
  ]);
  const handleCopyLink = async () => {
    const url = `${window.location.origin}${window.location.pathname}?folder=${folder.id}`;
    const didCopy = await copy(url);
    toggleNotification({
      type: didCopy ? "success" : "danger",
      message: formatMessage(didCopy ? {
        id: getTranslationKey("list.folder.actions.copy-link.success"),
        defaultMessage: "Folder link copied."
      } : {
        id: getTranslationKey("list.folder.actions.copy-link.error"),
        defaultMessage: "Failed to copy the folder link."
      })
    });
  };
  return (0, import_jsx_runtime12.jsxs)(import_jsx_runtime12.Fragment, {
    children: [
      (0, import_jsx_runtime12.jsxs)(gd.Root, {
        modal: false,
        children: [
          (0, import_jsx_runtime12.jsx)(gd.Trigger, {
            tag: be,
            icon: (0, import_jsx_runtime12.jsx)(W3, {}),
            variant: "ghost",
            label: formatMessage({
              id: getTranslationKey("control-card.more-actions"),
              defaultMessage: "More actions"
            })
          }),
          (0, import_jsx_runtime12.jsxs)(ActionsMenuContent, {
            popoverPlacement: "bottom-end",
            zIndex: 2,
            minWidth: "22rem",
            children: [
              (0, import_jsx_runtime12.jsx)(gd.Item, {
                startIcon: (0, import_jsx_runtime12.jsx)(R3, {}),
                onSelect: handleCopyLink,
                children: formatMessage({
                  id: getTranslationKey("list.folder.actions.copy-link"),
                  defaultMessage: "Copy link to folder"
                })
              }),
              (0, import_jsx_runtime12.jsx)(gd.Separator, {}),
              (0, import_jsx_runtime12.jsx)(gd.Item, {
                startIcon: (0, import_jsx_runtime12.jsx)(nn, {}),
                onSelect: () => setIsRenameOpen(true),
                children: formatMessage({
                  id: getTranslationKey("list.folder.actions.rename"),
                  defaultMessage: "Rename folder"
                })
              }),
              (0, import_jsx_runtime12.jsx)(gd.Item, {
                startIcon: (0, import_jsx_runtime12.jsx)(I2, {}),
                onSelect: () => setIsMoveOpen(true),
                children: formatMessage({
                  id: getTranslationKey("list.folder.actions.move"),
                  defaultMessage: "Move to folder"
                })
              }),
              (0, import_jsx_runtime12.jsx)(gd.Item, {
                startIcon: (0, import_jsx_runtime12.jsx)(In, {}),
                variant: "danger",
                onSelect: () => setIsDeleteOpen(true),
                children: formatMessage({
                  id: getTranslationKey("list.folder.actions.delete"),
                  defaultMessage: "Delete folder"
                })
              })
            ]
          })
        ]
      }),
      isRenameOpen && (0, import_jsx_runtime12.jsx)(FolderFormDialog, {
        open: true,
        mode: "rename",
        folderId: folder.id,
        initialName: folder.name,
        parentFolderId: dragData.parentId,
        onClose: () => setIsRenameOpen(false)
      }),
      isMoveOpen && (0, import_jsx_runtime12.jsx)(BulkMoveDialog, {
        open: true,
        onClose: () => setIsMoveOpen(false),
        items: moveItems,
        onSuccess: () => deselect2(folderKey(folder.id))
      }),
      isDeleteOpen && (0, import_jsx_runtime12.jsx)(DeleteItemsDialog, {
        open: true,
        onClose: () => setIsDeleteOpen(false),
        target: {
          fileIds: [],
          folderIds: [
            folder.id
          ]
        },
        onSuccess: () => deselect2(folderKey(folder.id))
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsGrid.mjs
var stopCardEvent = (e) => {
  if (isEventFromWithin(e)) {
    e.stopPropagation();
  }
};
var CheckboxOverlay = gt(C)`
  position: absolute;
  top: ${({ theme }) => theme.spaces[3]};
  left: ${({ theme }) => theme.spaces[3]};
  z-index: 1;
  box-shadow: ${({ theme }) => theme.shadows.filterShadow};
`;
var StyledCard = gt(md)`
  border: 1px solid
    ${({ theme, $isSelected }) => $isSelected ? theme.colors.primary600 : theme.colors.neutral200};
  border-radius: 8px;
  overflow: hidden;
  isolation: isolate;
  cursor: ${({ $isMovePending, $isBusy }) => $isMovePending || $isBusy ? "wait" : "pointer"};
  opacity: ${({ $isDragging }) => $isDragging ? 0.4 : 1};
  /* No opacity change while busy — the overlay does the dimming, and stacking
     one on the other would wash the card out. */
  pointer-events: ${({ $isMovePending, $isBusy }) => $isMovePending || $isBusy ? "none" : "auto"};
  background: ${({ theme, $isSelected }) => $isSelected ? theme.colors.primary100 : void 0};
  /* Shift+click range selection must not highlight card text. */
  user-select: none;

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: 2px;
  }
`;
var FoldersRow = gt(S)`
  grid-column: 1 / -1;
`;
var StyledFolderCard = gt(C)`
  width: 100%;
  user-select: none;
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[3]}`}; // 8px 12px
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]}; // 8px
  border: 1px solid
    ${({ theme, $isSelected }) => $isSelected ? theme.colors.primary600 : theme.colors.neutral200};
  border-radius: ${({ theme }) => theme.borderRadius};
  background: ${({ theme, $isSelected }) => $isSelected ? theme.colors.primary100 : theme.colors.neutral0};
  cursor: ${({ $isMovePending, $isInvalidDropTarget }) => {
  if ($isMovePending) {
    return "wait";
  }
  return $isInvalidDropTarget ? "not-allowed" : "pointer";
}};
  opacity: ${({ $isDragging }) => $isDragging ? 0.4 : 1};
  pointer-events: ${({ $isMovePending }) => $isMovePending ? "none" : "auto"};
  transition: background 0.2s;

  ${({ $isValidDropTarget, theme }) => $isValidDropTarget && ft`
      background: ${theme.colors.primary100};
      border: 1px dashed ${theme.colors.primary600};
    `}

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: 2px;
  }
`;
var FolderIconContainer = gt(C)`
  flex-shrink: 0;
  color: ${({ theme }) => theme.colors.neutral600};
`;
var FolderName = gt(TruncatedText)`
  flex: 1;
  min-width: 0;
`;
var FolderCard = ({ folder, orderedItemKeys }) => {
  const { formatMessage } = useIntl();
  const { navigateToFolder } = useFolderNavigation();
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { isSelected, toggle, selectRange: selectRange2 } = useAssetSelection();
  const { canUpdate } = useMediaLibraryPermissions();
  const { dragData, draggable: { attributes, listeners, setNodeRef: setDragRef, isDragging }, droppable: { setNodeRef: setDropRef }, showValidDropHighlight, showInvalidDropCursor } = useFolderDraggableDroppable(folder);
  const key = folderKey(folder.id);
  const setNodeRef = (node) => {
    setDragRef(node);
    setDropRef(node);
  };
  const handleClick = (e) => {
    if (!isEventFromWithin(e)) {
      return;
    }
    if (e.shiftKey) {
      selectRange2(orderedItemKeys, key);
    } else if (e.metaKey || e.ctrlKey) {
      toggle(key);
    } else {
      navigateToFolder(folder);
    }
  };
  const handleKeyDown = (e) => {
    if (!isEventFromWithin(e)) {
      return;
    }
    if (e.key === "Enter") {
      e.preventDefault();
      navigateToFolder(folder);
    } else if (e.key === " ") {
      e.preventDefault();
      toggle(key);
    }
  };
  const handleCheckboxClick = (e) => {
    e.stopPropagation();
    if (e.shiftKey) {
      selectRange2(orderedItemKeys, key);
    } else {
      toggle(key);
    }
  };
  return (0, import_jsx_runtime13.jsxs)(StyledFolderCard, {
    ref: setNodeRef,
    ...attributes,
    ...listeners,
    $isDragging: isDragging,
    $isMovePending: isMovePending,
    $isValidDropTarget: showValidDropHighlight,
    $isInvalidDropTarget: showInvalidDropCursor,
    $isSelected: isSelected(key),
    onClick: handleClick,
    onKeyDown: handleKeyDown,
    onPointerDown: (e) => {
      var _a;
      if (isEventFromWithin(e)) {
        (_a = listeners == null ? void 0 : listeners.onPointerDown) == null ? void 0 : _a.call(listeners, e);
      }
    },
    role: "listitem",
    tabIndex: 0,
    children: [
      canUpdate && (0, import_jsx_runtime13.jsx)(C, {
        onKeyDown: (e) => e.stopPropagation(),
        children: (0, import_jsx_runtime13.jsx)(Bt, {
          checked: isSelected(key),
          onClick: handleCheckboxClick,
          "aria-label": formatMessage({
            id: getTranslationKey("list.table.row.select"),
            defaultMessage: "Select {name}"
          }, {
            name: folder.name
          })
        })
      }),
      (0, import_jsx_runtime13.jsx)(FolderIconContainer, {
        children: (0, import_jsx_runtime13.jsx)(_5, {
          width: 20,
          height: 20
        })
      }),
      (0, import_jsx_runtime13.jsx)(FolderName, {
        textColor: "neutral800",
        children: folder.name
      }),
      (0, import_jsx_runtime13.jsx)(C, {
        onClick: stopCardEvent,
        onKeyDown: stopCardEvent,
        onPointerDown: stopCardEvent,
        children: (0, import_jsx_runtime13.jsx)(FolderActionsMenu, {
          folder,
          dragData
        })
      })
    ]
  });
};
var PreviewContainer2 = gt(S)`
  position: relative;
  width: 100%;
  padding-bottom: 62.5%;
  height: 0;
  overflow: hidden;
  background: repeating-conic-gradient(
      ${({ theme }) => theme.colors.neutral100} 0% 25%,
      transparent 0% 50%
    )
    50% / 20px 20px;
`;
var StyledImage2 = gt.img`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
`;
var IconPreview = gt(C)`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  color: ${({ theme }) => theme.colors.neutral500};
  background: ${({ theme }) => theme.colors.neutral100};
`;
var AssetPreview2 = ({ asset }) => {
  var _a;
  const { alternativeText, ext, formats, mime, url, updatedAt, isLocal, isUrlSigned } = asset;
  if (mime == null ? void 0 : mime.includes(ASSET_TYPES.Image)) {
    const cacheKey = updatedAt && !isUrlSigned ? new Date(updatedAt).getTime() : void 0;
    const appendCacheBuster = (raw) => {
      if (cacheKey === void 0) {
        return raw;
      }
      return raw.includes("?") ? `${raw}&v=${cacheKey}` : `${raw}?v=${cacheKey}`;
    };
    const rawMediaURL = prefixFileUrlWithBackendUrl((_a = formats == null ? void 0 : formats.thumbnail) == null ? void 0 : _a.url) ?? prefixFileUrlWithBackendUrl(url);
    const mediaURL = rawMediaURL ? appendCacheBuster(rawMediaURL) : rawMediaURL;
    if (mediaURL) {
      return (0, import_jsx_runtime13.jsx)(PreviewContainer2, {
        children: (0, import_jsx_runtime13.jsx)(StyledImage2, {
          src: mediaURL,
          alt: alternativeText || "",
          // Only signed remote URLs need crossOrigin (cache collision with
          // the preview). Public/unsigned remote thumbnails must render
          // without a bucket CORS rule. See #26581.
          crossOrigin: !isLocal && isUrlSigned ? "anonymous" : void 0,
          draggable: false,
          onDragStart: (e) => e.preventDefault()
        })
      });
    }
  }
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime13.jsx)(PreviewContainer2, {
    children: (0, import_jsx_runtime13.jsx)(IconPreview, {
      justifyContent: "center",
      alignItems: "center",
      children: (0, import_jsx_runtime13.jsx)(DocIcon, {
        width: 48,
        height: 48
      })
    })
  });
};
var StyledCardHeader = gt(Cd)`
  position: relative;
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral200};
`;
var CardFooter = gt(C)`
  min-width: 0;
  width: 100%;
`;
var FileTypeIcon = gt(C)`
  color: ${({ theme }) => theme.colors.neutral600};
  flex-shrink: 0;
`;
var FileName = gt(TruncatedText)`
  flex: 1;
  min-width: 0;
`;
var NameButton = gt.button`
  display: inline-flex;
  flex: 1;
  min-width: 0;
  border: none;
  background: transparent;
  padding: 0;
  margin: 0;
  cursor: pointer;
  text-align: left;
  color: inherit;
  font: inherit;

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: 2px;
    border-radius: 2px;
  }
`;
var AssetCard = ({ asset, orderedItemKeys, onAssetItemClick }) => {
  var _a;
  const { formatMessage } = useIntl();
  const TypeIcon = getAssetIcon(asset.mime, asset.ext);
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { attributes, listeners, setNodeRef, isDragging, dragData } = useFileDraggable(asset);
  const { isSelected, toggle, selectRange: selectRange2 } = useAssetSelection();
  const { canUpdate } = useMediaLibraryPermissions();
  const busyMessage = ((_a = useBusyAssetsOptional()) == null ? void 0 : _a.getBusyMessage(asset.id)) ?? null;
  const key = assetKey(asset.id);
  const selected = isSelected(key);
  const handleCardClick = (e) => {
    if (!isEventFromWithin(e)) {
      return;
    }
    if (e.shiftKey) {
      selectRange2(orderedItemKeys, key);
    } else if (e.metaKey || e.ctrlKey) {
      toggle(key);
    } else {
      onAssetItemClick(asset.id);
    }
  };
  const handleKeyDown = (e) => {
    if (!isEventFromWithin(e)) {
      return;
    }
    if (e.key === "Enter") {
      e.preventDefault();
      onAssetItemClick(asset.id);
    } else if (e.key === " ") {
      e.preventDefault();
      toggle(key);
    }
  };
  const handleNameClick = (e) => {
    e.stopPropagation();
    onAssetItemClick(asset.id);
  };
  const handleCheckboxClick = (e) => {
    e.stopPropagation();
    if (e.shiftKey) {
      selectRange2(orderedItemKeys, key);
    } else {
      toggle(key);
    }
  };
  return (0, import_jsx_runtime13.jsxs)(StyledCard, {
    ref: setNodeRef,
    ...attributes,
    ...listeners,
    $isDragging: isDragging,
    $isMovePending: isMovePending,
    $isBusy: busyMessage !== null,
    $isSelected: selected,
    tabIndex: 0,
    role: "listitem",
    onDragStart: (e) => e.preventDefault(),
    onClick: handleCardClick,
    onKeyDown: handleKeyDown,
    onPointerDown: (e) => {
      var _a2;
      if (isEventFromWithin(e)) {
        (_a2 = listeners == null ? void 0 : listeners.onPointerDown) == null ? void 0 : _a2.call(listeners, e);
      }
    },
    children: [
      (0, import_jsx_runtime13.jsxs)(StyledCardHeader, {
        children: [
          canUpdate && (0, import_jsx_runtime13.jsx)(CheckboxOverlay, {
            onKeyDown: (e) => e.stopPropagation(),
            children: (0, import_jsx_runtime13.jsx)(Bt, {
              checked: selected,
              onClick: handleCheckboxClick,
              "aria-label": formatMessage({
                id: getTranslationKey("list.table.row.select"),
                defaultMessage: "Select {name}"
              }, {
                name: asset.name
              })
            })
          }),
          (0, import_jsx_runtime13.jsx)(AssetPreview2, {
            asset
          }),
          busyMessage !== null ? (0, import_jsx_runtime13.jsx)(BusyOverlay, {
            zIndex: 2,
            children: busyMessage
          }) : null
        ]
      }),
      (0, import_jsx_runtime13.jsx)(xd, {
        children: (0, import_jsx_runtime13.jsxs)(CardFooter, {
          alignItems: "center",
          gap: 2,
          children: [
            (0, import_jsx_runtime13.jsx)(FileTypeIcon, {
              children: (0, import_jsx_runtime13.jsx)(TypeIcon, {
                width: 20,
                height: 20
              })
            }),
            (0, import_jsx_runtime13.jsx)(NameButton, {
              type: "button",
              onClick: handleNameClick,
              children: (0, import_jsx_runtime13.jsx)(FileName, {
                textColor: "primary800",
                children: asset.name
              })
            }),
            (0, import_jsx_runtime13.jsx)(C, {
              onClick: stopCardEvent,
              onKeyDown: stopCardEvent,
              onPointerDown: stopCardEvent,
              children: (0, import_jsx_runtime13.jsx)(AssetActionsMenu, {
                asset,
                dragData
              })
            })
          ]
        })
      })
    ]
  });
};
var AssetsGrid = ({ assets, folders = [], renderedKeys, onAssetItemClick }) => {
  const totalItems = folders.length + assets.length;
  const orderedItemKeys = renderedKeys ?? buildRenderedKeys({
    folders,
    assets
  });
  if (totalItems === 0) {
    return null;
  }
  return (0, import_jsx_runtime13.jsxs)(Ld.Root, {
    gap: 4,
    role: "list",
    "data-testid": "assets-grid",
    children: [
      folders.length > 0 && (0, import_jsx_runtime13.jsx)(FoldersRow, {
        children: (0, import_jsx_runtime13.jsx)(Ld.Root, {
          gap: 4,
          children: folders.map((folder) => (0, import_jsx_runtime13.jsx)(Ld.Item, {
            col: 3,
            m: 4,
            s: 6,
            xs: 12,
            children: (0, import_jsx_runtime13.jsx)(FolderCard, {
              folder,
              orderedItemKeys
            })
          }, `folder-${folder.id}`))
        })
      }),
      assets.map((asset) => (0, import_jsx_runtime13.jsx)(Ld.Item, {
        col: 3,
        m: 4,
        s: 6,
        xs: 12,
        direction: "column",
        alignItems: "stretch",
        children: (0, import_jsx_runtime13.jsx)(AssetCard, {
          asset,
          orderedItemKeys,
          onAssetItemClick
        })
      }, asset.id))
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsSearchInput.mjs
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var import_react13 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useAssetSearch.mjs
var import_react12 = __toESM(require_react(), 1);
var useAssetSearch = () => {
  const [{ query }, setQuery] = useQueryParams();
  const searchQuery = (query == null ? void 0 : query._q) ?? "";
  const setSearchQuery = (0, import_react12.useCallback)((value) => {
    if (value) {
      setQuery({
        _q: encodeSearchQuery(value)
      }, "push", true);
    } else {
      setQuery({
        _q: ""
      }, "remove", true);
    }
  }, [
    setQuery
  ]);
  const clearSearch = (0, import_react12.useCallback)(() => setSearchQuery(""), [
    setSearchQuery
  ]);
  return {
    searchQuery,
    isSearching: searchQuery !== "",
    setSearchQuery,
    clearSearch
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsSearchInput.mjs
var DEBOUNCE_MS = 300;
var StyledSearchForm = gt(Ql)`
  > div {
    border: none;
  }
`;
var AssetsSearchInput = () => {
  const { formatMessage } = useIntl();
  const { searchQuery, setSearchQuery } = useAssetSearch();
  const { trackUsage } = useTracking2();
  const isMobile = useIsMobile();
  const [value, setValue] = (0, import_react13.useState)(searchQuery);
  const debouncedValue = useDebounce(value, DEBOUNCE_MS);
  const lastCommittedRef = (0, import_react13.useRef)(searchQuery);
  const [{ query }] = useQueryParams();
  const folderParam = (query == null ? void 0 : query.folder) ?? "";
  const lastFolderRef = (0, import_react13.useRef)(folderParam);
  (0, import_react13.useEffect)(() => {
    if (debouncedValue === lastCommittedRef.current) {
      return;
    }
    lastCommittedRef.current = debouncedValue;
    if (debouncedValue) {
      trackUsage("didSearchMediaLibraryElements", {
        location: MEDIA_LIBRARY_LOCATION
      });
    }
    setSearchQuery(debouncedValue);
  }, [
    debouncedValue,
    setSearchQuery,
    trackUsage
  ]);
  (0, import_react13.useEffect)(() => {
    if (searchQuery === lastCommittedRef.current) {
      return;
    }
    lastCommittedRef.current = searchQuery;
    setValue(searchQuery);
  }, [
    searchQuery
  ]);
  (0, import_react13.useEffect)(() => {
    if (folderParam === lastFolderRef.current) {
      return;
    }
    lastFolderRef.current = folderParam;
    lastCommittedRef.current = searchQuery;
    setValue(searchQuery);
  }, [
    folderParam,
    searchQuery
  ]);
  const searchForm = (0, import_jsx_runtime14.jsx)(StyledSearchForm, {
    onSubmit: (event) => event.preventDefault(),
    children: (0, import_jsx_runtime14.jsx)(Yl, {
      name: "search-assets",
      value,
      onChange: (event) => setValue(event.target.value),
      onClear: () => setValue(""),
      clearLabel: formatMessage({
        id: "clearLabel",
        defaultMessage: "Clear"
      }),
      placeholder: formatMessage({
        id: getTranslationKey("header.search.placeholder"),
        defaultMessage: "Search"
      }),
      size: "S",
      children: formatMessage({
        id: getTranslationKey("search.label"),
        defaultMessage: "Search for an asset"
      })
    })
  });
  if (isMobile) {
    return (0, import_jsx_runtime14.jsx)(S, {
      width: "100%",
      children: searchForm
    });
  }
  return searchForm;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsTable.mjs
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/constants.mjs
var localStorageKeys = {
  view: `STRAPI_UPLOAD_LIBRARY_VIEW`
};
var viewOptions = {
  GRID: 0,
  TABLE: 1
};
var TABLE_HEADERS = [
  {
    name: "name",
    label: {
      id: getTranslationKey("list.table.header.name"),
      defaultMessage: "name"
    }
  },
  {
    name: "createdAt",
    label: {
      id: getTranslationKey("list.table.header.creationDate"),
      defaultMessage: "creation date"
    }
  },
  {
    name: "updatedAt",
    label: {
      id: getTranslationKey("list.table.header.lastModified"),
      defaultMessage: "last modified"
    }
  },
  {
    name: "size",
    label: {
      id: getTranslationKey("list.table.header.size"),
      defaultMessage: "size"
    }
  },
  {
    name: "actions",
    label: {
      id: getTranslationKey("list.table.header.actions"),
      defaultMessage: "actions"
    },
    isVisuallyHidden: true
  }
];

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsTable.mjs
var StyledTable = gt(Hl)`
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  border: 1px solid ${({ theme }) => theme.colors.neutral150};
  border-radius: 4px;
  overflow: hidden;

  /* An auto layout lets every column but the name size itself to its content,
     so the dates never wrap. The name cell is what absorbs the leftover and
     ellipsizes — see NameTd. */
  table-layout: auto;

  & td:last-child,
  & th:last-child {
    width: 5.6rem;
    white-space: nowrap;
  }
`;
var StyledThead = gt(jl)`
  background: ${({ theme }) => theme.colors.neutral100};

  tr {
    border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
  }
`;
var hugColumn = ft`
  width: 1%;
  white-space: nowrap;
`;
var flexColumn = ft`
  width: 100%;
  max-width: 0;
  overflow: hidden;
`;
var StyledTh = gt(Vl)`
  height: 40px;
  padding: 0 ${({ theme }) => theme.spaces[4]};
  text-align: left;

  ${({ $flex }) => $flex ? flexColumn : hugColumn}
`;
var StyledTd = gt(Dn)`
  padding: 0 ${({ theme }) => theme.spaces[4]};
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
`;
var NameTd = gt(StyledTd)`
  ${flexColumn}
`;
var HugTd = gt(StyledTd)`
  ${hugColumn}
`;
var StyledTr = gt.tr`
  height: 48px;
  user-select: none;
  background: ${({ theme, $isSelected }) => $isSelected ? theme.colors.primary100 : theme.colors.neutral0};
  cursor: ${({ $isMovePending, $isBusy, $isInvalidDropTarget }) => {
  if ($isMovePending || $isBusy) {
    return "wait";
  }
  return $isInvalidDropTarget ? "not-allowed" : "pointer";
}};
  opacity: ${({ $isDragging, $isBusy }) => $isDragging || $isBusy ? 0.4 : 1};
  pointer-events: ${({ $isMovePending, $isBusy }) => $isMovePending || $isBusy ? "none" : "auto"};

  ${({ $isValidDropTarget, theme }) => $isValidDropTarget && ft`
      background: ${theme.colors.primary100};
      outline: 1px dashed ${theme.colors.primary600};
      outline-offset: -1px;
    `}

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }

  &:last-child {
    ${StyledTd} {
      border-bottom: 0;
    }
  }
`;
var CheckboxTd = gt(StyledTd)`
  width: 5.6rem;
  white-space: nowrap;
`;
var CheckboxTh = gt(StyledTh)`
  width: 5.6rem;
  white-space: nowrap;
`;
var MetadataWarning = gt(Jn)`
  flex-shrink: 0;
  width: 1.6rem;
  height: 1.6rem;

  path {
    fill: ${({ theme }) => theme.colors.warning500};
  }
`;
var NameButton2 = gt.button`
  display: inline-flex;
  max-width: 100%;
  border: none;
  background: transparent;
  padding: 0;
  margin: 0;
  cursor: pointer;
  text-align: left;
  color: inherit;
  font: inherit;

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: 2px;
    border-radius: 2px;
  }
`;
var stopRowEvent = (e) => {
  if (isEventFromWithin(e)) {
    e.stopPropagation();
  }
};
var AssetPreviewCell = ({ asset }) => {
  const { ext, mime } = asset;
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime15.jsx)(C, {
    justifyContent: "center",
    alignItems: "center",
    borderRadius: "4px",
    color: "neutral500",
    width: "3.2rem",
    height: "3.2rem",
    shrink: 0,
    children: (0, import_jsx_runtime15.jsx)(DocIcon, {
      width: 20,
      height: 20
    })
  });
};
var AssetRow = ({ asset, orderedItemKeys, onAssetItemClick }) => {
  var _a;
  const isDesktop = useIsDesktop();
  const { formatDate, formatMessage } = useIntl();
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { attributes, listeners, setNodeRef, isDragging, dragData } = useFileDraggable(asset);
  const { isSelected, toggle, selectRange: selectRange2 } = useAssetSelection();
  const { canUpdate } = useMediaLibraryPermissions();
  const busyMessage = ((_a = useBusyAssetsOptional()) == null ? void 0 : _a.getBusyMessage(asset.id)) ?? null;
  const key = assetKey(asset.id);
  const selected = isSelected(key);
  const isMetadataMissing = !asset.caption || !asset.alternativeText;
  const metadataMissingLabel = formatMessage({
    id: getTranslationKey("list.table.row.metadata-missing"),
    defaultMessage: "This asset is missing metadata (caption or alternative text)."
  });
  const handleRowClick = (e) => {
    if (!isEventFromWithin(e)) {
      return;
    }
    if (e.shiftKey) {
      selectRange2(orderedItemKeys, key);
    } else if (e.metaKey || e.ctrlKey) {
      toggle(key);
    } else {
      onAssetItemClick(asset.id);
    }
  };
  const handleKeyDown = (e) => {
    if (!isEventFromWithin(e)) {
      return;
    }
    if (e.key === "Enter") {
      e.preventDefault();
      onAssetItemClick(asset.id);
    } else if (e.key === " ") {
      e.preventDefault();
      toggle(key);
    }
  };
  const handleNameClick = (e) => {
    e.stopPropagation();
    onAssetItemClick(asset.id);
  };
  const handleCheckboxClick = (e) => {
    e.stopPropagation();
    if (e.shiftKey) {
      selectRange2(orderedItemKeys, key);
    } else {
      toggle(key);
    }
  };
  return (0, import_jsx_runtime15.jsxs)(StyledTr, {
    ref: setNodeRef,
    ...attributes,
    ...listeners,
    $isDragging: isDragging,
    $isMovePending: isMovePending,
    $isBusy: busyMessage !== null,
    $isSelected: selected,
    tabIndex: 0,
    role: "row",
    onDragStart: (e) => e.preventDefault(),
    onClick: handleRowClick,
    onKeyDown: handleKeyDown,
    onPointerDown: (e) => {
      var _a2;
      if (isEventFromWithin(e)) {
        (_a2 = listeners == null ? void 0 : listeners.onPointerDown) == null ? void 0 : _a2.call(listeners, e);
      }
    },
    children: [
      canUpdate && (0, import_jsx_runtime15.jsx)(CheckboxTd, {
        onClick: stopRowEvent,
        onKeyDown: stopRowEvent,
        children: (0, import_jsx_runtime15.jsx)(C, {
          children: (0, import_jsx_runtime15.jsx)(Bt, {
            checked: selected,
            onClick: handleCheckboxClick,
            "aria-label": formatMessage({
              id: getTranslationKey("list.table.row.select"),
              defaultMessage: "Select {name}"
            }, {
              name: asset.name
            })
          })
        })
      }),
      (0, import_jsx_runtime15.jsx)(NameTd, {
        children: (0, import_jsx_runtime15.jsxs)(C, {
          alignItems: "center",
          justifyContent: "space-between",
          gap: 2,
          minWidth: 0,
          children: [
            (0, import_jsx_runtime15.jsxs)(C, {
              gap: 3,
              alignItems: "center",
              minWidth: 0,
              children: [
                busyMessage !== null ? (0, import_jsx_runtime15.jsx)(C, {
                  justifyContent: "center",
                  width: "3.2rem",
                  height: "3.2rem",
                  children: (0, import_jsx_runtime15.jsx)(kt, {
                    small: true,
                    children: busyMessage
                  })
                }) : (0, import_jsx_runtime15.jsx)(AssetPreviewCell, {
                  asset
                }),
                (0, import_jsx_runtime15.jsxs)(C, {
                  direction: "column",
                  alignItems: "flex-start",
                  minWidth: 0,
                  children: [
                    (0, import_jsx_runtime15.jsx)(NameButton2, {
                      type: "button",
                      onClick: handleNameClick,
                      children: (0, import_jsx_runtime15.jsx)(TruncatedText, {
                        textColor: "neutral800",
                        fontWeight: "semiBold",
                        children: asset.name
                      })
                    }),
                    !isDesktop && (0, import_jsx_runtime15.jsx)(A, {
                      textColor: "neutral600",
                      variant: "pi",
                      children: asset.size ? formatBytes(asset.size, 1) : "-"
                    })
                  ]
                })
              ]
            }),
            isMetadataMissing && (0, import_jsx_runtime15.jsx)(Mt, {
              label: metadataMissingLabel,
              children: (0, import_jsx_runtime15.jsx)(MetadataWarning, {
                "aria-label": metadataMissingLabel,
                role: "img"
              })
            })
          ]
        })
      }),
      isDesktop && (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, {
        children: [
          (0, import_jsx_runtime15.jsx)(HugTd, {
            children: (0, import_jsx_runtime15.jsx)(A, {
              textColor: "neutral600",
              children: asset.createdAt ? formatDate(new Date(asset.createdAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime15.jsx)(HugTd, {
            children: (0, import_jsx_runtime15.jsx)(A, {
              textColor: "neutral600",
              children: asset.updatedAt ? formatDate(new Date(asset.updatedAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime15.jsx)(HugTd, {
            children: (0, import_jsx_runtime15.jsx)(A, {
              textColor: "neutral600",
              children: asset.size ? formatBytes(asset.size, 1) : "-"
            })
          })
        ]
      }),
      (0, import_jsx_runtime15.jsx)(StyledTd, {
        onClick: stopRowEvent,
        onKeyDown: stopRowEvent,
        onPointerDown: stopRowEvent,
        children: (0, import_jsx_runtime15.jsx)(C, {
          justifyContent: "flex-end",
          children: (0, import_jsx_runtime15.jsx)(AssetActionsMenu, {
            asset,
            dragData
          })
        })
      })
    ]
  });
};
var FolderTr = gt(StyledTr)`
  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }
`;
var FolderRow = ({ folder, orderedItemKeys }) => {
  const isDesktop = useIsDesktop();
  const { formatDate, formatMessage } = useIntl();
  const { navigateToFolder } = useFolderNavigation();
  const { isSelected, toggle, selectRange: selectRange2 } = useAssetSelection();
  const { canUpdate } = useMediaLibraryPermissions();
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const { dragData, draggable: { attributes, listeners, setNodeRef: setDragRef, isDragging }, droppable: { setNodeRef: setDropRef }, showValidDropHighlight, showInvalidDropCursor } = useFolderDraggableDroppable(folder);
  const key = folderKey(folder.id);
  const handleRowClick = (e) => {
    if (!isEventFromWithin(e)) {
      return;
    }
    if (e.shiftKey) {
      selectRange2(orderedItemKeys, key);
    } else if (e.metaKey || e.ctrlKey) {
      toggle(key);
    } else {
      navigateToFolder(folder);
    }
  };
  const handleKeyDown = (e) => {
    if (!isEventFromWithin(e)) {
      return;
    }
    if (e.key === "Enter") {
      e.preventDefault();
      navigateToFolder(folder);
    } else if (e.key === " ") {
      e.preventDefault();
      toggle(key);
    }
  };
  const handleCheckboxClick = (e) => {
    e.stopPropagation();
    if (e.shiftKey) {
      selectRange2(orderedItemKeys, key);
    } else {
      toggle(key);
    }
  };
  return (0, import_jsx_runtime15.jsxs)(FolderTr, {
    ref: (node) => {
      setDragRef(node);
      setDropRef(node);
    },
    ...attributes,
    ...listeners,
    $isDragging: isDragging,
    $isMovePending: isMovePending,
    $isValidDropTarget: showValidDropHighlight,
    $isInvalidDropTarget: showInvalidDropCursor,
    $isSelected: isSelected(key),
    tabIndex: 0,
    role: "row",
    onDragStart: (e) => {
      if (isEventFromWithin(e)) {
        e.preventDefault();
      }
    },
    onClick: handleRowClick,
    onKeyDown: handleKeyDown,
    onPointerDown: (e) => {
      var _a;
      if (isEventFromWithin(e)) {
        (_a = listeners == null ? void 0 : listeners.onPointerDown) == null ? void 0 : _a.call(listeners, e);
      }
    },
    children: [
      canUpdate && (0, import_jsx_runtime15.jsx)(CheckboxTd, {
        onClick: stopRowEvent,
        onKeyDown: stopRowEvent,
        children: (0, import_jsx_runtime15.jsx)(C, {
          children: (0, import_jsx_runtime15.jsx)(Bt, {
            checked: isSelected(key),
            onClick: handleCheckboxClick,
            "aria-label": formatMessage({
              id: getTranslationKey("list.table.row.select"),
              defaultMessage: "Select {name}"
            }, {
              name: folder.name
            })
          })
        })
      }),
      (0, import_jsx_runtime15.jsx)(NameTd, {
        children: (0, import_jsx_runtime15.jsxs)(C, {
          gap: 3,
          alignItems: "center",
          minWidth: 0,
          children: [
            (0, import_jsx_runtime15.jsx)(C, {
              justifyContent: "center",
              alignItems: "center",
              borderRadius: "4px",
              color: "neutral600",
              width: "3.2rem",
              height: "3.2rem",
              shrink: 0,
              children: (0, import_jsx_runtime15.jsx)(_5, {
                width: 20,
                height: 20
              })
            }),
            (0, import_jsx_runtime15.jsx)(TruncatedText, {
              textColor: "neutral800",
              fontWeight: "semiBold",
              children: folder.name
            })
          ]
        })
      }),
      isDesktop && (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, {
        children: [
          (0, import_jsx_runtime15.jsx)(HugTd, {
            children: (0, import_jsx_runtime15.jsx)(A, {
              textColor: "neutral600",
              children: folder.createdAt ? formatDate(new Date(folder.createdAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime15.jsx)(HugTd, {
            children: (0, import_jsx_runtime15.jsx)(A, {
              textColor: "neutral600",
              children: folder.updatedAt ? formatDate(new Date(folder.updatedAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime15.jsx)(HugTd, {
            children: (0, import_jsx_runtime15.jsx)(A, {
              textColor: "neutral600",
              children: "-"
            })
          })
        ]
      }),
      (0, import_jsx_runtime15.jsx)(StyledTd, {
        onClick: stopRowEvent,
        onKeyDown: stopRowEvent,
        onPointerDown: stopRowEvent,
        children: (0, import_jsx_runtime15.jsx)(C, {
          justifyContent: "flex-end",
          children: (0, import_jsx_runtime15.jsx)(FolderActionsMenu, {
            folder,
            dragData
          })
        })
      })
    ]
  });
};
var AssetsTable = ({ assets, folders = [], mixedItems = null, renderedKeys, onAssetItemClick }) => {
  const isDesktop = useIsDesktop();
  const { formatMessage } = useIntl();
  const { selectedKeys, selectAll: selectAll2, clear } = useAssetSelection();
  const { canUpdate } = useMediaLibraryPermissions();
  const { trackUsage } = useTracking2();
  const visibleHeaders = isDesktop ? TABLE_HEADERS : TABLE_HEADERS.filter((h) => h.name === "name" || h.name === "actions");
  const showCheckboxColumn = canUpdate;
  const colCount = visibleHeaders.length + (showCheckboxColumn ? 1 : 0);
  const totalRows = folders.length + assets.length;
  const orderedItemKeys = renderedKeys ?? buildRenderedKeys({
    folders,
    assets,
    mixedItems
  });
  const { allSelected, isIndeterminate } = getSelectAllState(selectedKeys, orderedItemKeys);
  const handleSelectAll = () => {
    if (allSelected) {
      clear();
    } else {
      trackUsage("didSelectAllMediaLibraryElements");
      selectAll2(orderedItemKeys);
    }
  };
  if (totalRows === 0) {
    return null;
  }
  return (0, import_jsx_runtime15.jsxs)(StyledTable, {
    colCount,
    rowCount: (mixedItems ? mixedItems.length : totalRows) + 1,
    children: [
      (0, import_jsx_runtime15.jsx)(StyledThead, {
        children: (0, import_jsx_runtime15.jsxs)(Ul, {
          children: [
            showCheckboxColumn && (0, import_jsx_runtime15.jsx)(CheckboxTh, {
              children: (0, import_jsx_runtime15.jsx)(C, {
                children: (0, import_jsx_runtime15.jsx)(Bt, {
                  checked: isIndeterminate ? "indeterminate" : allSelected,
                  disabled: orderedItemKeys.length === 0,
                  onCheckedChange: handleSelectAll,
                  "aria-label": formatMessage({
                    id: getTranslationKey("list.table.header.select-all"),
                    defaultMessage: "Select all"
                  })
                })
              })
            }),
            visibleHeaders.map((header) => {
              const tableHeaderLabel = formatMessage(header.label);
              const isVisuallyHidden = "isVisuallyHidden" in header && header.isVisuallyHidden;
              if (isVisuallyHidden) {
                return (0, import_jsx_runtime15.jsx)(StyledTh, {
                  $flex: header.name === "name",
                  children: (0, import_jsx_runtime15.jsx)($e, {
                    children: formatMessage({
                      id: getTranslationKey("table.header.actions"),
                      defaultMessage: "actions"
                    })
                  })
                }, header.name);
              }
              return (0, import_jsx_runtime15.jsx)(StyledTh, {
                $flex: header.name === "name",
                children: (0, import_jsx_runtime15.jsx)(A, {
                  textColor: "neutral600",
                  variant: "sigma",
                  children: tableHeaderLabel
                })
              }, header.name);
            })
          ]
        })
      }),
      (0, import_jsx_runtime15.jsxs)(_l, {
        children: [
          mixedItems == null ? void 0 : mixedItems.map((item) => item.kind === "folder" ? (0, import_jsx_runtime15.jsx)(FolderRow, {
            folder: item.folder,
            orderedItemKeys
          }, `folder-${item.folder.id}`) : (0, import_jsx_runtime15.jsx)(AssetRow, {
            asset: item.asset,
            orderedItemKeys,
            onAssetItemClick
          }, item.asset.id)),
          !mixedItems && folders.map((folder) => (0, import_jsx_runtime15.jsx)(FolderRow, {
            folder,
            orderedItemKeys
          }, `folder-${folder.id}`)),
          !mixedItems && assets.map((asset) => (0, import_jsx_runtime15.jsx)(AssetRow, {
            asset,
            orderedItemKeys,
            onAssetItemClick
          }, asset.id))
        ]
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/BulkActionsBar.mjs
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
var import_react14 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/utils/buildDragSetFromSelection.mjs
var buildDragSetFromSelection = (selectedIds, selectedFolderIds, locations, fallbackFolderId) => {
  const items = [];
  selectedIds.forEach((id) => {
    items.push({
      kind: "file",
      id,
      name: "",
      folderId: locateItem(locations, "file", id, fallbackFolderId)
    });
  });
  selectedFolderIds.forEach((id) => {
    items.push({
      kind: "folder",
      id,
      name: "",
      parentId: locateItem(locations, "folder", id, fallbackFolderId)
    });
  });
  return items;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/BulkActionsBar.mjs
var Bar = gt(C)`
  position: fixed;
  z-index: ${({ theme }) => theme.zIndices.popover};
  left: 0;
  right: 0;
  bottom: 0;
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]};
  padding: ${({ theme }) => `${theme.spaces[3]} ${theme.spaces[2]} ${theme.spaces[3]} ${theme.spaces[6]}`};
  background: ${({ theme }) => theme.colors.neutral0};
  border: 0;
  border-top: 1px solid ${({ theme }) => theme.colors.neutral150};
  border-radius: 0;
  box-shadow: ${({ theme }) => theme.shadows.popupShadow};

  /* Docked full-bleed at the bottom on mobile, which is exactly where the open
     drawer keeps its own actions — so it steps aside there, and only there. */
  display: ${({ $isDrawerOpen }) => $isDrawerOpen ? "none" : "flex"};

  /* Mobile with the metadata action present: the labelled button plus the icons
     no longer fit beside the count on one line, so the count takes a row of its
     own and every button drops to the next.

     Addressed by slot rather than by position: these rules used to use
     nth-child, which silently retargeted the moment a control was inserted
     into the row. */
  ${({ $isStacked }) => $isStacked && ft`
      flex-wrap: wrap;
      justify-content: space-between;

      > [data-bar-slot='count'] {
        flex-basis: 100%;
        margin-right: 0;
      }

      > [data-bar-slot='actions'] {
        margin-left: 0;
      }

      /* The divider only existed to set the clear action apart from the rest;
         with the row spread it would hang in mid-air between them. */
      > [data-bar-slot='divider'] {
        display: none;
      }
    `}

  ${({ theme }) => theme.breakpoints.medium} {
    display: flex;
    left: 50%;
    right: auto;
    bottom: ${({ theme }) => theme.spaces[4]};
    transform: translateX(-50%);
    border: 1px solid ${({ theme }) => theme.colors.neutral150};
    border-radius: ${({ theme }) => theme.borderRadius};
    /* Sized by its content, capped so the pill can never span the whole
       viewport. The nowrap is what lets the content set that width — without it
       the labels wrap and the bar reads as narrow and tall. Inherited, so it
       covers every label inside.

       Deliberately not applied on mobile: there the bar is full-bleed and
       cannot grow, so refusing to wrap would clip the last action on a narrow
       phone rather than widen anything. */
    white-space: nowrap;
    max-width: 90%;

    /* One line again from tablet up, where it fits. */
    flex-wrap: nowrap;

    > [data-bar-slot='count'] {
      flex-basis: auto;
    }

    > [data-bar-slot='actions'] {
      margin-left: auto;
    }

    > [data-bar-slot='divider'] {
      display: block;
    }
  }
`;
var ActionCluster = gt(C)`
  margin-left: auto;
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]};
`;
var VerticalDivider = gt(S)`
  width: 1px;
  align-self: stretch;
  background: ${({ theme }) => theme.colors.neutral150};
  margin-left: ${({ theme }) => theme.spaces[1]};
`;
var BulkActionsBar = ({ assets = [], locations = emptyItemLocations, renderedKeys = [] }) => {
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const isAiMetadataEnabled = useAIMetadataEnabled();
  const { canUpdate } = useMediaLibraryPermissions();
  const { selectedIds, selectedFolderIds, selectAll: selectAll2, clear } = useAssetSelection();
  const { trackUsage } = useTracking2();
  const { currentFolderId } = useFolderNavigation();
  const isDetailsDrawerOpen = useIsAssetDetailsOpen();
  const [generateAiMetadata, { isLoading: isGeneratingMetadata }] = useGenerateAiMetadataMutation();
  const [isDeleteDialogOpen, setIsDeleteDialogOpen] = (0, import_react14.useState)(false);
  const [isMoveDialogOpen, setIsMoveDialogOpen] = (0, import_react14.useState)(false);
  const [isDeleting, setIsDeleting] = (0, import_react14.useState)(false);
  const count = selectedIds.size + selectedFolderIds.size;
  const handleSelectAll = () => {
    trackUsage("didSelectAllMediaLibraryElements");
    selectAll2(renderedKeys);
  };
  const isBusy = isDeleting || isGeneratingMetadata;
  const moveItems = (0, import_react14.useMemo)(() => buildDragSetFromSelection(selectedIds, selectedFolderIds, locations, currentFolderId), [
    selectedIds,
    selectedFolderIds,
    locations,
    currentFolderId
  ]);
  const isOverMetadataLimit = selectedIds.size > AI_METADATA_MAX_FILES;
  const eligibleCount = (0, import_react14.useMemo)(() => {
    const mimeById = new Map(assets.map(({ id, mime }) => [
      id,
      mime
    ]));
    return [
      ...selectedIds
    ].filter((id) => isAIMetadataSupportedMime(mimeById.get(id))).length;
  }, [
    assets,
    selectedIds
  ]);
  const hasNoEligibleAssets = selectedIds.size > 0 && eligibleCount === 0;
  let metadataDisabledReason;
  if (isOverMetadataLimit) {
    metadataDisabledReason = formatMessage({
      id: getTranslationKey("list.bulk-actions.create-metadata.too-many"),
      defaultMessage: "Metadata can be generated for up to {max} assets at a time. Select fewer assets to continue."
    }, {
      max: AI_METADATA_MAX_FILES
    });
  } else if (hasNoEligibleAssets) {
    metadataDisabledReason = formatMessage({
      id: getTranslationKey("list.bulk-actions.create-metadata.no-eligible"),
      defaultMessage: "Metadata can only be generated for images. None of the selected assets are supported."
    });
  }
  const handleCreateMetadata = async () => {
    if (isGeneratingMetadata || isOverMetadataLimit || hasNoEligibleAssets) {
      return;
    }
    const fileIds = Array.from(selectedIds);
    const res = await generateAiMetadata({
      fileIds
    });
    if ("error" in res) {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("list.bulk-actions.create-metadata.error"),
          defaultMessage: "An error occurred while generating metadata."
        })
      });
      return;
    }
    const successCount = res.data.filter(({ status }) => status === "success").length;
    const skippedCount = res.data.filter(({ status }) => status === "skipped").length;
    const errorCount = res.data.filter(({ status }) => status === "error").length;
    const folderCount = selectedFolderIds.size;
    if (errorCount === res.data.length) {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: getTranslationKey("list.bulk-actions.create-metadata.error"),
          defaultMessage: "An error occurred while generating metadata."
        })
      });
      return;
    }
    if (skippedCount === 0 && errorCount === 0 && folderCount === 0) {
      toggleNotification({
        type: "success",
        message: formatMessage({
          id: getTranslationKey("list.bulk-actions.create-metadata.success"),
          defaultMessage: "{count, plural, =1 {Metadata generated for # asset} other {Metadata generated for # assets}}"
        }, {
          count: successCount
        })
      });
    } else {
      toggleNotification({
        type: "warning",
        message: formatMessage({
          id: getTranslationKey("list.bulk-actions.create-metadata.partial"),
          defaultMessage: "{successCount} generated, {skippedCount} skipped (unsupported file type), {errorCount} failed{folderCount, plural, =0 {} one {, # folder ignored} other {, # folders ignored}}"
        }, {
          successCount,
          skippedCount,
          errorCount,
          folderCount
        })
      });
    }
    clear();
  };
  if (count === 0 || !canUpdate) {
    return null;
  }
  return (0, import_jsx_runtime16.jsxs)(Bar, {
    $isDrawerOpen: isDetailsDrawerOpen,
    $isStacked: isAiMetadataEnabled,
    tag: "section",
    role: "region",
    "aria-label": formatMessage({
      id: getTranslationKey("list.bulk-actions.label"),
      defaultMessage: "Bulk actions"
    }),
    children: [
      (0, import_jsx_runtime16.jsx)(A, {
        "data-bar-slot": "count",
        fontWeight: "bold",
        textColor: "neutral800",
        marginRight: 4,
        children: formatMessage({
          id: getTranslationKey("list.bulk-actions.selected-count"),
          defaultMessage: "{count, plural, =1 {# item selected} other {# items selected}}"
        }, {
          count
        })
      }),
      (0, import_jsx_runtime16.jsx)(bu, {
        onClick: handleSelectAll,
        marginRight: 4,
        disabled: isBusy,
        children: formatMessage({
          id: getTranslationKey("list.bulk-actions.select-all"),
          defaultMessage: "Select all"
        })
      }),
      (0, import_jsx_runtime16.jsxs)(ActionCluster, {
        "data-bar-slot": "actions",
        children: [
          isAiMetadataEnabled && (0, import_jsx_runtime16.jsx)(Mt, {
            label: metadataDisabledReason,
            children: (0, import_jsx_runtime16.jsx)(S, {
              children: (0, import_jsx_runtime16.jsx)(Ct, {
                size: "S",
                startIcon: (0, import_jsx_runtime16.jsx)(Vn, {}),
                disabled: isBusy || selectedIds.size === 0 || isOverMetadataLimit || hasNoEligibleAssets,
                loading: isGeneratingMetadata,
                onClick: handleCreateMetadata,
                children: formatMessage({
                  id: getTranslationKey("list.bulk-actions.create-metadata"),
                  defaultMessage: "Create metadata"
                })
              })
            })
          }),
          (0, import_jsx_runtime16.jsx)(be, {
            variant: "tertiary",
            disabled: isBusy,
            label: formatMessage({
              id: getTranslationKey("list.bulk-actions.move"),
              defaultMessage: "Move"
            }),
            onClick: () => setIsMoveDialogOpen(true),
            children: (0, import_jsx_runtime16.jsx)(I2, {})
          }),
          (0, import_jsx_runtime16.jsx)(BulkMoveDialog, {
            open: isMoveDialogOpen,
            onClose: () => setIsMoveDialogOpen(false),
            items: moveItems,
            onSuccess: clear
          }),
          (0, import_jsx_runtime16.jsx)(be, {
            variant: "danger-light",
            disabled: isBusy,
            label: formatMessage({
              id: getTranslationKey("list.bulk-actions.delete"),
              defaultMessage: "Delete"
            }),
            onClick: () => setIsDeleteDialogOpen(true),
            children: (0, import_jsx_runtime16.jsx)(In, {})
          }),
          (0, import_jsx_runtime16.jsx)(DeleteItemsDialog, {
            open: isDeleteDialogOpen,
            onClose: () => setIsDeleteDialogOpen(false),
            target: {
              fileIds: Array.from(selectedIds),
              folderIds: Array.from(selectedFolderIds)
            },
            onSuccess: clear,
            onPendingChange: setIsDeleting
          })
        ]
      }),
      (0, import_jsx_runtime16.jsx)(VerticalDivider, {
        "data-bar-slot": "divider",
        "aria-hidden": true
      }),
      (0, import_jsx_runtime16.jsx)(be, {
        variant: "ghost",
        label: formatMessage({
          id: getTranslationKey("list.bulk-actions.clear"),
          defaultMessage: "Clear selection"
        }),
        onClick: clear,
        disabled: isBusy,
        children: (0, import_jsx_runtime16.jsx)(F5, {})
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZone.mjs
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
var React6 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZoneContext.mjs
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var import_react15 = __toESM(require_react(), 1);
var UploadDropZoneContext = (0, import_react15.createContext)(null);
var DropZoneWrapper = gt(S)`
  position: relative;
  display: flex;
  flex-direction: column;
  min-height: 100%;
`;
var UploadDropZoneProvider = ({ children, onDrop, disabled = false }) => {
  const [isDragging, setIsDragging] = (0, import_react15.useState)(false);
  const dragCounterRef = (0, import_react15.useRef)(0);
  const contextValue = {
    isDragging
  };
  (0, import_react15.useEffect)(() => {
    const handleDragEnd = () => {
      setIsDragging(false);
      dragCounterRef.current = 0;
    };
    const handleDocumentDragLeave = (e) => {
      if (!e.relatedTarget) {
        setIsDragging(false);
        dragCounterRef.current = 0;
      }
    };
    document.addEventListener("dragend", handleDragEnd);
    document.addEventListener("dragleave", handleDocumentDragLeave);
    return () => {
      document.removeEventListener("dragend", handleDragEnd);
      document.removeEventListener("dragleave", handleDocumentDragLeave);
    };
  }, []);
  const handleDragEnter = (0, import_react15.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    if (disabled) {
      return;
    }
    if (!e.dataTransfer.types.includes("Files")) {
      return;
    }
    dragCounterRef.current += 1;
    setIsDragging(true);
  }, [
    disabled
  ]);
  const handleDragLeave = (0, import_react15.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    dragCounterRef.current -= 1;
    if (dragCounterRef.current <= 0) {
      setIsDragging(false);
      dragCounterRef.current = 0;
    }
  }, []);
  const handleDragOver = (0, import_react15.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    e.dataTransfer.dropEffect = "copy";
  }, []);
  const handleDrop = (0, import_react15.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    setIsDragging(false);
    dragCounterRef.current = 0;
    if (disabled) {
      return;
    }
    const { files } = e.dataTransfer;
    if ((files == null ? void 0 : files.length) && onDrop) {
      onDrop(Array.from(files));
    }
  }, [
    onDrop,
    disabled
  ]);
  return (0, import_jsx_runtime17.jsx)(UploadDropZoneContext.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime17.jsx)(DropZoneWrapper, {
      "data-testid": "assets-dropzone",
      onDragEnter: handleDragEnter,
      onDragLeave: handleDragLeave,
      onDragOver: handleDragOver,
      onDrop: handleDrop,
      children
    })
  });
};
var useUploadDropZone = () => {
  const context = (0, import_react15.useContext)(UploadDropZoneContext);
  if (!context) {
    throw new Error("useUploadDropZone must be used within UploadDropZone");
  }
  return {
    isDragging: context.isDragging
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZone.mjs
var setOpacity = (hex, alpha) => `${hex}${Math.floor(alpha * 255).toString(16).padStart(2, "0")}`;
var DropZoneOverlay = gt(S)`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: ${({ theme }) => setOpacity(theme.colors.primary200, 0.3)};
  border: 1px solid ${({ theme }) => theme.colors.primary700};
  border-radius: ${({ theme }) => theme.borderRadius};
  z-index: 1;
  pointer-events: none;
`;
var DropZoneWithOverlay = ({ children }) => {
  const { isDragging: isUploadDragActive } = useUploadDropZone();
  const assetsDnd = useAssetsDndOptional();
  const isInternalDragActive = (assetsDnd == null ? void 0 : assetsDnd.isInternalDragActive) ?? false;
  const showOverlay = isUploadDragActive && !isInternalDragActive;
  return (0, import_jsx_runtime18.jsxs)(S, {
    position: "relative",
    children: [
      showOverlay && (0, import_jsx_runtime18.jsx)(DropZoneOverlay, {}),
      children
    ]
  });
};
var DropFilesMessageImpl = gt(S)`
  position: fixed;
  bottom: ${({ theme }) => theme.spaces[8]};
  left: 50%;
  transform: translateX(calc(-50% + ${({ $leftContentWidth }) => $leftContentWidth / 2}px));
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: ${({ theme }) => theme.spaces[2]};
  background: ${({ theme }) => theme.colors.primary600};
  padding: ${({ theme }) => theme.spaces[4]} ${({ theme }) => theme.spaces[6]};
  border-radius: ${({ theme }) => theme.borderRadius};
  z-index: 2;
`;
var DropFilesMessage = ({ uploadDropZoneRef, folderName }) => {
  const { formatMessage } = useIntl();
  const { isDragging: isUploadDragActive } = useUploadDropZone();
  const assetsDnd = useAssetsDndOptional();
  const isInternalDragActive = (assetsDnd == null ? void 0 : assetsDnd.isInternalDragActive) ?? false;
  const showMessage = isUploadDragActive && !isInternalDragActive;
  const [leftContentWidth, setLeftContentWidth] = React6.useState(0);
  React6.useEffect(() => {
    if (!(uploadDropZoneRef == null ? void 0 : uploadDropZoneRef.current)) return;
    const updateRect = () => {
      var _a;
      const rect = (_a = uploadDropZoneRef.current) == null ? void 0 : _a.getBoundingClientRect();
      if (rect) {
        setLeftContentWidth((prev) => prev !== rect.left ? rect.left : prev);
      }
    };
    updateRect();
    const resizeObserver = new ResizeObserver(updateRect);
    resizeObserver.observe(uploadDropZoneRef.current);
    return () => resizeObserver.disconnect();
  }, [
    uploadDropZoneRef
  ]);
  if (!showMessage) {
    return null;
  }
  return (0, import_jsx_runtime18.jsxs)(DropFilesMessageImpl, {
    $leftContentWidth: leftContentWidth,
    children: [
      (0, import_jsx_runtime18.jsx)(A, {
        textColor: "neutral0",
        children: formatMessage({
          id: getTranslationKey("dropzone.upload.message"),
          defaultMessage: "Drop here to upload to"
        })
      }),
      (0, import_jsx_runtime18.jsxs)(C, {
        gap: 2,
        alignItems: "center",
        children: [
          (0, import_jsx_runtime18.jsx)(_5, {
            width: 20,
            height: 20,
            fill: "neutral0"
          }),
          (0, import_jsx_runtime18.jsx)(A, {
            textColor: "neutral0",
            fontWeight: "semiBold",
            children: folderName
          })
        ]
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/EmptyState.mjs
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
var EmptyState = ({ onAddAssets, canAddAssets, searchQuery, onClearSearch }) => {
  const { formatMessage } = useIntl();
  const isSearchEmptyState = Boolean(searchQuery);
  return (0, import_jsx_runtime19.jsxs)(C, {
    direction: "column",
    alignItems: "center",
    gap: 6,
    padding: 11,
    children: [
      (0, import_jsx_runtime19.jsx)(K, {
        width: "16rem",
        height: "8.8rem"
      }),
      (0, import_jsx_runtime19.jsxs)(C, {
        direction: "column",
        alignItems: "center",
        gap: 2,
        textAlign: "center",
        children: [
          (0, import_jsx_runtime19.jsx)(A, {
            variant: "delta",
            tag: "p",
            fontWeight: "bold",
            textColor: "neutral800",
            children: isSearchEmptyState ? formatMessage({
              id: getTranslationKey("list.search.empty.title"),
              defaultMessage: "No results found"
            }) : formatMessage({
              id: getTranslationKey("list.empty.title"),
              defaultMessage: "No assets yet"
            })
          }),
          (0, import_jsx_runtime19.jsx)(A, {
            textColor: "neutral600",
            children: isSearchEmptyState ? formatMessage({
              id: getTranslationKey("list.search.empty.description"),
              defaultMessage: 'No assets or folders match "{query}". Try a different search.'
            }, {
              query: searchQuery
            }) : formatMessage({
              id: getTranslationKey("list.empty.description"),
              defaultMessage: "Get started by uploading assets or creating a folder."
            })
          })
        ]
      }),
      isSearchEmptyState ? (
        // Same look as the filters empty state's "Clear filters" action.
        (0, import_jsx_runtime19.jsx)(Ct, {
          variant: "secondary",
          startIcon: (0, import_jsx_runtime19.jsx)(F5, {
            "aria-hidden": true
          }),
          onClick: onClearSearch,
          children: formatMessage({
            id: getTranslationKey("list.search.empty.clear"),
            defaultMessage: "Clear search"
          })
        })
      ) : canAddAssets && (0, import_jsx_runtime19.jsx)(Ct, {
        onClick: onAddAssets,
        children: formatMessage({
          id: getTranslationKey("list.empty.add-assets"),
          defaultMessage: "Add assets"
        })
      })
    ]
  });
};
var FilteredEmptyState = ({ onClearFilters }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime19.jsxs)(C, {
    direction: "column",
    alignItems: "center",
    gap: 6,
    padding: 11,
    children: [
      (0, import_jsx_runtime19.jsx)(K, {
        width: "16rem",
        height: "8.8rem"
      }),
      (0, import_jsx_runtime19.jsx)(A, {
        textColor: "neutral600",
        children: formatMessage({
          id: getTranslationKey("list.filters.empty"),
          defaultMessage: "No items matched current filters"
        })
      }),
      (0, import_jsx_runtime19.jsx)(Ct, {
        variant: "secondary",
        startIcon: (0, import_jsx_runtime19.jsx)(F5, {
          "aria-hidden": true
        }),
        onClick: onClearFilters,
        children: formatMessage({
          id: getTranslationKey("list.filters.clear"),
          defaultMessage: "Clear filters"
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FilterBadges.mjs
var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
var import_react18 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useListFilters.mjs
var TYPE_VALUES = [
  "folder",
  "picture",
  "audio",
  "video",
  "document"
];
var DATE_PRESETS = [
  "1day",
  "3days",
  "1week",
  "1month",
  "3months",
  "6months",
  "1year"
];
var FIELD_TOKENS = {
  created: "createdAt",
  updated: "updatedAt"
};
var FIELD_TOKEN_BY_FIELD = {
  createdAt: "created",
  updatedAt: "updated"
};
var PRESET_CONDITION_TOKENS = {
  exact: "isExactly",
  within: "withinLast",
  notwithin: "notWithinLast"
};
var PRESET_TOKEN_BY_CONDITION = {
  isExactly: "exact",
  withinLast: "within",
  notWithinLast: "notwithin"
};
var RANGE_CONDITION_TOKENS = {
  rangeis: "is",
  rangenot: "isNot"
};
var RANGE_TOKEN_BY_CONDITION = {
  is: "rangeis",
  isNot: "rangenot"
};
var DATE_RE = /^\d{4}-\d{2}-\d{2}$/;
var isTypeValue = (value) => TYPE_VALUES.includes(value);
var isDatePreset = (value) => DATE_PRESETS.includes(value);
var parseBadge = (raw) => {
  const [field, condition, value] = raw.split(":");
  if (!field || !condition || !value) {
    return null;
  }
  if (field === "type") {
    if (condition !== "is" && condition !== "not") {
      return null;
    }
    const values = value.split(",").filter(isTypeValue);
    return values.length > 0 ? {
      kind: "type",
      condition: condition === "is" ? "is" : "isNot",
      values
    } : null;
  }
  const dateField = FIELD_TOKENS[field];
  if (!dateField) {
    return null;
  }
  if (condition in PRESET_CONDITION_TOKENS) {
    return isDatePreset(value) ? {
      kind: "date",
      field: dateField,
      mode: "preset",
      condition: PRESET_CONDITION_TOKENS[condition],
      preset: value
    } : null;
  }
  if (condition in RANGE_CONDITION_TOKENS) {
    const [from, to] = value.split("..");
    return DATE_RE.test(from ?? "") && DATE_RE.test(to ?? "") ? {
      kind: "date",
      field: dateField,
      mode: "range",
      condition: RANGE_CONDITION_TOKENS[condition],
      from,
      to
    } : null;
  }
  return null;
};
var parseFiltersParam = (raw) => {
  if (typeof raw !== "string" || raw === "") {
    return [];
  }
  return raw.split(";").map(parseBadge).filter((filter) => filter !== null);
};
var serializeBadge = (filter) => {
  if (filter.kind === "type") {
    return `type:${filter.condition === "is" ? "is" : "not"}:${filter.values.join(",")}`;
  }
  const field = FIELD_TOKEN_BY_FIELD[filter.field];
  if (filter.mode === "preset") {
    return `${field}:${PRESET_TOKEN_BY_CONDITION[filter.condition]}:${filter.preset}`;
  }
  return `${field}:${RANGE_TOKEN_BY_CONDITION[filter.condition]}:${filter.from}..${filter.to}`;
};
var serializeFilters = (filters) => filters.map(serializeBadge).join(";");
var useListFilters = () => {
  const [{ query }, setQuery] = useQueryParams();
  const filters = parseFiltersParam(query == null ? void 0 : query.filters);
  const writeFilters = (next) => {
    if (next.length === 0) {
      setQuery(withEncodedUserParams(query, {
        filters: void 0
      }), "push", true);
    } else {
      setQuery(withEncodedUserParams(query, {
        filters: serializeFilters(next)
      }), "push", true);
    }
  };
  return {
    filters,
    serialized: serializeFilters(filters),
    addFilter: (filter) => writeFilters([
      ...filters,
      filter
    ]),
    updateFilter: (index, filter) => writeFilters(filters.map((f, i) => i === index ? filter : f)),
    removeFilter: (index) => writeFilters(filters.filter((_, i) => i !== index)),
    clearFilters: () => writeFilters([])
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/utils/buildAssetFilters.mjs
var MIME_BY_VALUE = {
  picture: "image",
  audio: "audio",
  video: "video"
};
var ALL_MIMES = Object.values(MIME_BY_VALUE);
var PRESET_DELTAS = {
  "1day": {
    days: 1
  },
  "3days": {
    days: 3
  },
  "1week": {
    days: 7
  },
  "1month": {
    months: 1
  },
  "3months": {
    months: 3
  },
  "6months": {
    months: 6
  },
  "1year": {
    years: 1
  }
};
var subtractPreset = (now, preset) => {
  const { days = 0, months = 0, years = 0 } = PRESET_DELTAS[preset];
  const result = new Date(now.getTime());
  if (years || months) {
    const dayOfMonth = result.getDate();
    result.setDate(1);
    result.setFullYear(result.getFullYear() - years);
    result.setMonth(result.getMonth() - months);
    const daysInTargetMonth = new Date(result.getFullYear(), result.getMonth() + 1, 0).getDate();
    result.setDate(Math.min(dayOfMonth, daysInTargetMonth));
  }
  result.setDate(result.getDate() - days);
  return result;
};
var startOfDay = (date) => {
  const result = new Date(date.getTime());
  result.setHours(0, 0, 0, 0);
  return result;
};
var endOfDay = (date) => {
  const result = new Date(date.getTime());
  result.setHours(23, 59, 59, 999);
  return result;
};
var parseCalendarDate = (value) => {
  const [year, month, day] = value.split("-").map(Number);
  return new Date(year, month - 1, day);
};
var buildDateClause = (filter, now) => {
  const { field } = filter;
  if (filter.mode === "preset") {
    const pivot = subtractPreset(now, filter.preset);
    switch (filter.condition) {
      case "withinLast":
        return {
          [field]: {
            $gte: pivot.toISOString()
          }
        };
      case "notWithinLast":
        return {
          [field]: {
            $lt: pivot.toISOString()
          }
        };
      case "isExactly":
        return {
          [field]: {
            $gte: startOfDay(pivot).toISOString(),
            $lte: endOfDay(pivot).toISOString()
          }
        };
    }
  }
  const from = startOfDay(parseCalendarDate(filter.from)).toISOString();
  const to = endOfDay(parseCalendarDate(filter.to)).toISOString();
  if (filter.condition === "is") {
    return {
      [field]: {
        $gte: from,
        $lte: to
      }
    };
  }
  return {
    $or: [
      {
        [field]: {
          $lt: from
        }
      },
      {
        [field]: {
          $gt: to
        }
      }
    ]
  };
};
var buildMimeClause = (filter) => {
  const fileValues = filter.values.filter((value) => value !== "folder");
  if (fileValues.length === 0) {
    return null;
  }
  const branches = fileValues.map((value) => {
    if (value === "document") {
      return {
        $and: ALL_MIMES.map((mime) => ({
          mime: {
            $notContains: mime
          }
        }))
      };
    }
    return {
      mime: {
        $contains: MIME_BY_VALUE[value]
      }
    };
  });
  if (filter.condition === "is") {
    return branches.length === 1 ? branches[0] : {
      $or: branches
    };
  }
  const negated = fileValues.map((value) => {
    if (value === "document") {
      return {
        $or: ALL_MIMES.map((mime) => ({
          mime: {
            $contains: mime
          }
        }))
      };
    }
    return {
      mime: {
        $notContains: MIME_BY_VALUE[value]
      }
    };
  });
  return negated.length === 1 ? negated[0] : {
    $and: negated
  };
};
var buildAssetFilters = (filters, now) => {
  const fileClauses = [];
  const folderClauses = [];
  let showFolders = true;
  let showFiles = true;
  for (const filter of filters) {
    if (filter.kind === "date") {
      const clause = buildDateClause(filter, now);
      fileClauses.push(clause);
      folderClauses.push(clause);
      continue;
    }
    const includesFolder = filter.values.includes("folder");
    if (filter.condition === "is" ? !includesFolder : includesFolder) {
      showFolders = false;
    }
    const mimeClause = buildMimeClause(filter);
    if (mimeClause) {
      fileClauses.push(mimeClause);
    } else if (filter.condition === "is") {
      showFiles = false;
    }
  }
  return {
    fileClauses,
    folderClauses,
    showFolders,
    showFiles
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DateRangeCalendar.mjs
var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
var import_react16 = __toESM(require_react(), 1);
var DayCell = gt.button`
  width: 3rem;
  height: 3rem;
  border: none;
  border-radius: ${({ theme }) => theme.borderRadius};
  cursor: pointer;
  font: inherit;
  color: ${({ theme, $isEdge, $isMuted }) => {
  if ($isEdge) return theme.colors.primary600;
  if ($isMuted) return theme.colors.neutral400;
  return theme.colors.neutral800;
}};
  background: ${({ theme, $inRange, $isEdge }) => {
  if ($isEdge) return theme.colors.primary200;
  if ($inRange) return theme.colors.primary100;
  return "transparent";
}};

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }
`;
var toKey = (date) => {
  const month = `${date.getMonth() + 1}`.padStart(2, "0");
  const day = `${date.getDate()}`.padStart(2, "0");
  return `${date.getFullYear()}-${month}-${day}`;
};
var fromKey = (key) => {
  const [year, month, day] = key.split("-").map(Number);
  return new Date(year, month - 1, day);
};
var buildWeeks = (year, month) => {
  const first = new Date(year, month, 1);
  const start = new Date(first.getTime());
  start.setDate(first.getDate() - (first.getDay() + 6) % 7);
  const weeks = [];
  const cursor = new Date(start.getTime());
  do {
    const week = [];
    for (let i = 0; i < 7; i += 1) {
      week.push(new Date(cursor.getTime()));
      cursor.setDate(cursor.getDate() + 1);
    }
    weeks.push(week);
  } while (cursor.getMonth() === month && cursor.getFullYear() === year);
  return weeks;
};
var DateRangeCalendar = ({ from, to, onSelect }) => {
  const { formatMessage, formatDate } = useIntl();
  const initialMonth = from ? fromKey(from) : /* @__PURE__ */ new Date();
  const [viewYear, setViewYear] = (0, import_react16.useState)(initialMonth.getFullYear());
  const [viewMonth, setViewMonth] = (0, import_react16.useState)(initialMonth.getMonth());
  const [pendingStart, setPendingStart] = (0, import_react16.useState)(null);
  const rangeStart = pendingStart ?? from ?? null;
  const rangeEnd = pendingStart ? null : to ?? null;
  const moveMonth = (delta) => {
    const next = new Date(viewYear, viewMonth + delta, 1);
    setViewYear(next.getFullYear());
    setViewMonth(next.getMonth());
  };
  const handleDayClick = (day) => {
    const key = toKey(day);
    if (!pendingStart) {
      setPendingStart(key);
      return;
    }
    const [start, end] = key < pendingStart ? [
      key,
      pendingStart
    ] : [
      pendingStart,
      key
    ];
    setPendingStart(null);
    onSelect(start, end);
  };
  const weeks = buildWeeks(viewYear, viewMonth);
  const weekDayLabels = weeks[0].map((day) => formatDate(day, {
    weekday: "short"
  }).slice(0, 2));
  return (
    // role="group" (not "application"): these are plain buttons — hijacking
    // the screen reader's own key handling would take away navigation and
    // give nothing back.
    (0, import_jsx_runtime20.jsxs)(S, {
      padding: 2,
      width: "100%",
      role: "group",
      "aria-label": formatMessage({
        id: getTranslationKey("list.filters.calendar.label"),
        defaultMessage: "Select date range"
      }),
      "data-testid": "date-range-calendar",
      children: [
        (0, import_jsx_runtime20.jsxs)(C, {
          justifyContent: "space-between",
          alignItems: "center",
          paddingBottom: 2,
          children: [
            (0, import_jsx_runtime20.jsx)(be, {
              variant: "ghost",
              label: formatMessage({
                id: getTranslationKey("list.filters.calendar.previous-month"),
                defaultMessage: "Previous month"
              }),
              onClick: () => moveMonth(-1),
              children: (0, import_jsx_runtime20.jsx)(i5, {})
            }),
            (0, import_jsx_runtime20.jsx)(A, {
              fontWeight: "semiBold",
              textColor: "neutral800",
              children: formatDate(new Date(viewYear, viewMonth, 1), {
                month: "long",
                year: "numeric"
              })
            }),
            (0, import_jsx_runtime20.jsx)(be, {
              variant: "ghost",
              label: formatMessage({
                id: getTranslationKey("list.filters.calendar.next-month"),
                defaultMessage: "Next month"
              }),
              onClick: () => moveMonth(1),
              children: (0, import_jsx_runtime20.jsx)(v5, {})
            })
          ]
        }),
        (0, import_jsx_runtime20.jsx)(C, {
          children: weekDayLabels.map((label, index) => (0, import_jsx_runtime20.jsx)(C, {
            width: "3rem",
            height: "2.4rem",
            justifyContent: "center",
            children: (0, import_jsx_runtime20.jsx)(A, {
              variant: "pi",
              fontWeight: "semiBold",
              textColor: "neutral600",
              children: label
            })
          }, index))
        }),
        weeks.map((week, weekIndex) => (0, import_jsx_runtime20.jsx)(C, {
          children: week.map((day) => {
            const key = toKey(day);
            const isEdge = key === rangeStart || key === rangeEnd;
            const inRange = rangeStart !== null && rangeEnd !== null && key > rangeStart && key < rangeEnd;
            return (0, import_jsx_runtime20.jsxs)(DayCell, {
              type: "button",
              $isEdge: isEdge,
              $inRange: inRange,
              $isMuted: day.getMonth() !== viewMonth,
              onClick: () => handleDayClick(day),
              children: [
                (0, import_jsx_runtime20.jsx)($e, {
                  children: formatDate(day, {
                    dateStyle: "long"
                  })
                }),
                (0, import_jsx_runtime20.jsx)("span", {
                  "aria-hidden": true,
                  children: day.getDate()
                })
              ]
            }, key);
          })
        }, weekIndex))
      ]
    })
  );
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FilterMenu.mjs
var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
var import_react17 = __toESM(require_react(), 1);
var TYPE_LABELS = {
  folder: {
    id: getTranslationKey("list.filters.type.folder"),
    defaultMessage: "Folder"
  },
  picture: {
    id: getTranslationKey("list.filters.type.picture"),
    defaultMessage: "Picture"
  },
  audio: {
    id: getTranslationKey("list.filters.type.audio"),
    defaultMessage: "Audio"
  },
  video: {
    id: getTranslationKey("list.filters.type.video"),
    defaultMessage: "Video"
  },
  document: {
    id: getTranslationKey("list.filters.type.document"),
    defaultMessage: "Document"
  }
};
var PRESET_LABELS = {
  "1day": {
    id: getTranslationKey("list.filters.preset.1day"),
    defaultMessage: "1 day ago"
  },
  "3days": {
    id: getTranslationKey("list.filters.preset.3days"),
    defaultMessage: "3 days ago"
  },
  "1week": {
    id: getTranslationKey("list.filters.preset.1week"),
    defaultMessage: "1 week ago"
  },
  "1month": {
    id: getTranslationKey("list.filters.preset.1month"),
    defaultMessage: "1 month ago"
  },
  "3months": {
    id: getTranslationKey("list.filters.preset.3months"),
    defaultMessage: "3 months ago"
  },
  "6months": {
    id: getTranslationKey("list.filters.preset.6months"),
    defaultMessage: "6 months ago"
  },
  "1year": {
    id: getTranslationKey("list.filters.preset.1year"),
    defaultMessage: "1 year ago"
  }
};
var DATE_FIELD_LABELS = {
  createdAt: {
    id: getTranslationKey("list.filters.field.created"),
    defaultMessage: "Creation date"
  },
  updatedAt: {
    id: getTranslationKey("list.filters.field.updated"),
    defaultMessage: "Last modified"
  }
};
var FieldSubTrigger = gt(gd.SubTrigger)`
  width: 100%;
  justify-content: space-between;
`;
var FILTER_PANEL_WIDTH = "24.2rem";
var PANEL_MAX_HEIGHT = "70dvh";
var MOBILE_PANEL_WIDTH = `min(${FILTER_PANEL_WIDTH}, calc(100dvw - 2rem))`;
var FieldToggle = gt(gd.Item)`
  width: 100%;
`;
var OptionGroup = gt(S)`
  width: 100%;

  > * {
    width: 100%;
  }

  /* menuitem, menuitemradio and menuitemcheckbox — every option row, plus the
     "Select date range" toggle, which sits at the same level. */
  > [role^='menuitem'] {
    padding-left: ${({ theme }) => theme.spaces[6]};
  }
`;
var Chevron = gt(o5)`
  transition: transform 0.2s ease;
  transform: rotate(${({ $open }) => $open ? "180deg" : "0deg"});
`;
var SubPanel = gt(gd.SubContent)`
  margin-top: calc(-1 * (${({ theme }) => theme.spaces[1]} + 1px));
`;
var CountBadge = gt(Nr)`
  height: 1.6rem;
  min-width: auto;
  padding: 0 0.4rem;
`;
var FilterMenu = ({ listFilters }) => {
  const { formatMessage } = useIntl();
  const { trackUsage } = useTracking2();
  const [isOpen, setIsOpen] = (0, import_react17.useState)(false);
  const { filters, addFilter, updateFilter, removeFilter } = listFilters;
  const trackFilter = (filter) => trackUsage("didFilterMediaLibraryElements", {
    location: MEDIA_LIBRARY_LOCATION,
    filter
  });
  let lastTypeIndex = -1;
  for (let i = filters.length - 1; i >= 0; i -= 1) {
    if (filters[i].kind === "type") {
      lastTypeIndex = i;
      break;
    }
  }
  const lastTypeFilter = lastTypeIndex >= 0 ? filters[lastTypeIndex] : null;
  const checkedValues = lastTypeFilter && lastTypeFilter.kind === "type" ? lastTypeFilter.values : [];
  const toggleTypeValue = (value) => {
    const isAdding = !checkedValues.includes(value);
    const nextValues = isAdding ? [
      ...checkedValues,
      value
    ] : checkedValues.filter((v) => v !== value);
    if (isAdding) {
      trackFilter("type");
    }
    if (lastTypeFilter && lastTypeFilter.kind === "type") {
      if (nextValues.length === 0) {
        removeFilter(lastTypeIndex);
      } else {
        updateFilter(lastTypeIndex, {
          ...lastTypeFilter,
          values: nextValues
        });
      }
    } else if (nextValues.length > 0) {
      addFilter({
        kind: "type",
        condition: "is",
        values: nextValues
      });
    }
  };
  const addPreset = (field, preset) => {
    trackFilter(field);
    for (let i = filters.length - 1; i >= 0; i -= 1) {
      const f = filters[i];
      if (f.kind === "date" && f.mode === "preset" && f.field === field) {
        updateFilter(i, {
          ...f,
          preset
        });
        return;
      }
    }
    addFilter({
      kind: "date",
      field,
      mode: "preset",
      condition: "withinLast",
      preset
    });
  };
  const addRange = (from, to) => {
    trackFilter("createdAt");
    addFilter({
      kind: "date",
      field: "createdAt",
      mode: "range",
      condition: "is",
      from,
      to
    });
    setIsOpen(false);
  };
  const isMobile = useIsMobile();
  const [openField, setOpenField] = (0, import_react17.useState)(null);
  const [rangeOpen, setRangeOpen] = (0, import_react17.useState)(false);
  const handleOpenChange = (next) => {
    setIsOpen(next);
    if (!next) {
      setOpenField(null);
      setRangeOpen(false);
    }
  };
  const toggleField = (field) => {
    setOpenField((current) => current === field ? null : field);
    setRangeOpen(false);
  };
  const typeItems = TYPE_VALUES.map((value) => (0, import_jsx_runtime21.jsx)(gd.Item, {
    role: "menuitemcheckbox",
    "aria-checked": checkedValues.includes(value),
    onSelect: (e) => {
      e.preventDefault();
      toggleTypeValue(value);
    },
    startIcon: (0, import_jsx_runtime21.jsx)(Bt, {
      checked: checkedValues.includes(value),
      tabIndex: -1,
      "aria-hidden": true
    }),
    children: formatMessage(TYPE_LABELS[value])
  }, value));
  const selectedPresetForField = (field) => {
    for (let i = filters.length - 1; i >= 0; i -= 1) {
      const f = filters[i];
      if (f.kind === "date" && f.mode === "preset" && f.field === field) {
        return f.preset;
      }
    }
    return null;
  };
  const presetItems = (field) => {
    const selected = selectedPresetForField(field);
    return DATE_PRESETS.map((preset) => (0, import_jsx_runtime21.jsx)(gd.Item, {
      role: "menuitemradio",
      "aria-checked": selected === preset,
      onSelect: () => {
        addPreset(field, preset);
      },
      endIcon: selected === preset ? (0, import_jsx_runtime21.jsx)(h5, {
        "aria-hidden": true,
        width: "1.6rem",
        height: "1.6rem",
        fill: "primary600"
      }) : null,
      children: formatMessage(PRESET_LABELS[preset])
    }, preset));
  };
  const typeLabel = formatMessage({
    id: getTranslationKey("list.filters.field.type"),
    defaultMessage: "Type"
  });
  const selectDateRangeLabel = formatMessage({
    id: getTranslationKey("list.filters.select-date-range"),
    defaultMessage: "Select date range"
  });
  return (0, import_jsx_runtime21.jsxs)(gd.Root, {
    open: isOpen,
    onOpenChange: handleOpenChange,
    children: [
      (0, import_jsx_runtime21.jsx)(gd.Trigger, {
        variant: "tertiary",
        startIcon: (0, import_jsx_runtime21.jsx)(Y5, {
          "aria-hidden": true
        }),
        endIcon: null,
        children: (0, import_jsx_runtime21.jsxs)(C, {
          gap: 2,
          alignItems: "center",
          tag: "span",
          children: [
            formatMessage({
              id: getTranslationKey("list.filters.trigger"),
              defaultMessage: "Filter"
            }),
            filters.length > 0 && (0, import_jsx_runtime21.jsx)(CountBadge, {
              children: filters.length
            })
          ]
        })
      }),
      (0, import_jsx_runtime21.jsx)(gd.Content, {
        popoverPlacement: "bottom-start",
        zIndex: 2,
        maxHeight: PANEL_MAX_HEIGHT,
        width: isMobile ? MOBILE_PANEL_WIDTH : FILTER_PANEL_WIDTH,
        children: isMobile ? (
          // Mobile: one flat panel. Each field expands its options inline below
          // itself — Radix sub-menus can only fly out sideways, off a phone.
          (0, import_jsx_runtime21.jsxs)(import_jsx_runtime21.Fragment, {
            children: [
              (0, import_jsx_runtime21.jsx)(FieldToggle, {
                "aria-expanded": openField === "type",
                onSelect: (e) => {
                  e.preventDefault();
                  toggleField("type");
                },
                endIcon: (0, import_jsx_runtime21.jsx)(Chevron, {
                  $open: openField === "type",
                  "aria-hidden": true
                }),
                children: typeLabel
              }),
              openField === "type" && (0, import_jsx_runtime21.jsx)(OptionGroup, {
                children: typeItems
              }),
              [
                "createdAt",
                "updatedAt"
              ].map((field) => (0, import_jsx_runtime21.jsxs)(S, {
                width: "100%",
                children: [
                  (0, import_jsx_runtime21.jsx)(FieldToggle, {
                    "aria-expanded": openField === field,
                    onSelect: (e) => {
                      e.preventDefault();
                      toggleField(field);
                    },
                    endIcon: (0, import_jsx_runtime21.jsx)(Chevron, {
                      $open: openField === field,
                      "aria-hidden": true
                    }),
                    children: formatMessage(DATE_FIELD_LABELS[field])
                  }),
                  openField === field && (0, import_jsx_runtime21.jsxs)(OptionGroup, {
                    children: [
                      presetItems(field),
                      field === "createdAt" && (0, import_jsx_runtime21.jsxs)(import_jsx_runtime21.Fragment, {
                        children: [
                          (0, import_jsx_runtime21.jsx)(FieldToggle, {
                            "aria-expanded": rangeOpen,
                            onSelect: (e) => {
                              e.preventDefault();
                              setRangeOpen((open) => !open);
                            },
                            endIcon: (0, import_jsx_runtime21.jsx)(Chevron, {
                              $open: rangeOpen,
                              "aria-hidden": true
                            }),
                            children: selectDateRangeLabel
                          }),
                          rangeOpen && (0, import_jsx_runtime21.jsx)(S, {
                            paddingLeft: 2,
                            children: (0, import_jsx_runtime21.jsx)(DateRangeCalendar, {
                              onSelect: addRange
                            })
                          })
                        ]
                      })
                    ]
                  })
                ]
              }, field))
            ]
          })
        ) : (0, import_jsx_runtime21.jsxs)(import_jsx_runtime21.Fragment, {
          children: [
            (0, import_jsx_runtime21.jsxs)(gd.SubRoot, {
              children: [
                (0, import_jsx_runtime21.jsx)(FieldSubTrigger, {
                  children: typeLabel
                }),
                (0, import_jsx_runtime21.jsx)(SubPanel, {
                  zIndex: 2,
                  maxHeight: PANEL_MAX_HEIGHT,
                  width: FILTER_PANEL_WIDTH,
                  children: typeItems
                })
              ]
            }),
            [
              "createdAt",
              "updatedAt"
            ].map((field) => (0, import_jsx_runtime21.jsxs)(gd.SubRoot, {
              children: [
                (0, import_jsx_runtime21.jsx)(FieldSubTrigger, {
                  children: formatMessage(DATE_FIELD_LABELS[field])
                }),
                (0, import_jsx_runtime21.jsxs)(SubPanel, {
                  zIndex: 2,
                  maxHeight: PANEL_MAX_HEIGHT,
                  width: FILTER_PANEL_WIDTH,
                  children: [
                    presetItems(field),
                    field === "createdAt" && (0, import_jsx_runtime21.jsxs)(gd.SubRoot, {
                      children: [
                        (0, import_jsx_runtime21.jsx)(FieldSubTrigger, {
                          children: selectDateRangeLabel
                        }),
                        (0, import_jsx_runtime21.jsx)(SubPanel, {
                          zIndex: 2,
                          maxHeight: "none",
                          width: FILTER_PANEL_WIDTH,
                          children: (0, import_jsx_runtime21.jsx)(DateRangeCalendar, {
                            onSelect: addRange
                          })
                        })
                      ]
                    })
                  ]
                })
              ]
            }, field))
          ]
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FilterBadges.mjs
var BadgePill = gt(C)`
  border: 1px solid ${({ theme }) => theme.colors.neutral200};
  border-radius: ${({ theme }) => theme.borderRadius};
  background: ${({ theme }) => theme.colors.neutral0};
  overflow: hidden;
`;
var Segment = gt.button`
  border: none;
  background: transparent;
  font: inherit;
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[3]}`};
  cursor: ${({ $interactive }) => $interactive ? "pointer" : "default"};
  border-right: 1px solid ${({ theme }) => theme.colors.neutral200};

  ${({ theme }) => theme.breakpoints.medium} {
    padding: ${({ theme }) => `${theme.spaces[1]} ${theme.spaces[2]}`};
  }
  ${({ $interactive, theme }) => $interactive && `&:hover { background: ${theme.colors.primary100}; }`}

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }
`;
var FieldSegment = gt.span`
  display: inline-flex;
  align-items: center;
  padding: ${({ theme }) => `${theme.spaces[1]} ${theme.spaces[2]}`};
  border-right: 1px solid ${({ theme }) => theme.colors.neutral200};
`;
var PanelPopoverContent = gt(Xd.Content)`
  width: ${FILTER_PANEL_WIDTH};
`;
var OptionButton = gt.button`
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: ${({ theme }) => theme.spaces[4]};
  width: 100%;
  border: none;
  background: transparent;
  font-size: ${({ theme }) => theme.fontSizes[2]};
  line-height: ${({ theme }) => theme.lineHeights[4]};
  font-family: inherit;
  text-align: left;
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[4]}`};
  border-radius: ${({ theme }) => theme.borderRadius};
  cursor: pointer;
  color: ${({ theme }) => theme.colors.neutral800};

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }
`;
var RemoveButton = gt.button`
  border: none;
  background: transparent;
  display: inline-flex;
  align-items: center;
  padding: ${({ theme }) => `0 ${theme.spaces[2]}`};
  cursor: pointer;
  color: ${({ theme }) => theme.colors.neutral600};

  &:hover {
    color: ${({ theme }) => theme.colors.neutral800};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }
`;
var TYPE_CONDITION_LABELS = {
  is: {
    id: getTranslationKey("list.filters.condition.is"),
    defaultMessage: "is"
  },
  isNot: {
    id: getTranslationKey("list.filters.condition.is-not"),
    defaultMessage: "is not"
  }
};
var PRESET_CONDITION_LABELS = {
  isExactly: {
    id: getTranslationKey("list.filters.condition.is-exactly"),
    defaultMessage: "is exactly"
  },
  withinLast: {
    id: getTranslationKey("list.filters.condition.within-last"),
    defaultMessage: "within the last"
  },
  notWithinLast: {
    id: getTranslationKey("list.filters.condition.not-within-last"),
    defaultMessage: "not within the last"
  }
};
var RANGE_CONDITION_LABELS = {
  is: {
    id: getTranslationKey("list.filters.condition.is"),
    defaultMessage: "is"
  },
  isNot: {
    id: getTranslationKey("list.filters.condition.is-not"),
    defaultMessage: "is not"
  }
};
var SegmentPopover = ({ label, options, active, getOptionLabel, onPick }) => {
  const [open, setOpen] = (0, import_react18.useState)(false);
  return (0, import_jsx_runtime22.jsxs)(Xd.Root, {
    open,
    onOpenChange: setOpen,
    children: [
      (0, import_jsx_runtime22.jsx)(Xd.Trigger, {
        children: (0, import_jsx_runtime22.jsx)(Segment, {
          type: "button",
          $interactive: true,
          children: (0, import_jsx_runtime22.jsx)(A, {
            variant: "pi",
            textColor: "neutral800",
            children: label
          })
        })
      }),
      (0, import_jsx_runtime22.jsx)(PanelPopoverContent, {
        children: (0, import_jsx_runtime22.jsx)(C, {
          direction: "column",
          alignItems: "stretch",
          padding: 1,
          children: options.map((option) => (0, import_jsx_runtime22.jsxs)(OptionButton, {
            type: "button",
            onClick: () => {
              onPick(option);
              setOpen(false);
            },
            children: [
              getOptionLabel(option),
              option === active && (0, import_jsx_runtime22.jsx)(h5, {
                "aria-hidden": true,
                width: "1.6rem",
                height: "1.6rem"
              })
            ]
          }, option))
        })
      })
    ]
  });
};
var TypeValuePopover = ({ filter, onChange }) => {
  const { formatMessage } = useIntl();
  const [open, setOpen] = (0, import_react18.useState)(false);
  const label = filter.values.map((value) => formatMessage(TYPE_LABELS[value])).join(", ");
  const toggleValue = (value) => {
    const nextValues = filter.values.includes(value) ? filter.values.filter((v) => v !== value) : [
      ...filter.values,
      value
    ];
    if (nextValues.length > 0) {
      onChange({
        ...filter,
        values: nextValues
      });
    }
  };
  return (0, import_jsx_runtime22.jsxs)(Xd.Root, {
    open,
    onOpenChange: setOpen,
    children: [
      (0, import_jsx_runtime22.jsx)(Xd.Trigger, {
        children: (0, import_jsx_runtime22.jsx)(Segment, {
          type: "button",
          $interactive: true,
          children: (0, import_jsx_runtime22.jsx)(A, {
            variant: "pi",
            textColor: "neutral800",
            children: label
          })
        })
      }),
      (0, import_jsx_runtime22.jsx)(PanelPopoverContent, {
        children: (0, import_jsx_runtime22.jsx)(C, {
          direction: "column",
          alignItems: "flex-start",
          padding: 3,
          gap: 2,
          children: TYPE_VALUES.map((value) => (0, import_jsx_runtime22.jsx)(Bt, {
            checked: filter.values.includes(value),
            onCheckedChange: () => toggleValue(value),
            children: formatMessage(TYPE_LABELS[value])
          }, value))
        })
      })
    ]
  });
};
var DateValuePopover = ({ filter, onChange }) => {
  const { formatMessage, formatDate } = useIntl();
  const [open, setOpen] = (0, import_react18.useState)(false);
  const label = filter.mode === "preset" ? formatMessage(PRESET_LABELS[filter.preset]) : `${formatDate(parseCalendarDate(filter.from), {
    day: "2-digit",
    month: "short"
  })} - ${formatDate(parseCalendarDate(filter.to), {
    day: "2-digit",
    month: "short",
    year: "numeric"
  })}`;
  return (0, import_jsx_runtime22.jsxs)(Xd.Root, {
    open,
    onOpenChange: setOpen,
    children: [
      (0, import_jsx_runtime22.jsx)(Xd.Trigger, {
        children: (0, import_jsx_runtime22.jsx)(Segment, {
          type: "button",
          $interactive: true,
          children: (0, import_jsx_runtime22.jsx)(A, {
            variant: "pi",
            textColor: "neutral800",
            children: label
          })
        })
      }),
      (0, import_jsx_runtime22.jsx)(PanelPopoverContent, {
        children: filter.mode === "preset" ? (0, import_jsx_runtime22.jsx)(C, {
          direction: "column",
          alignItems: "stretch",
          padding: 1,
          children: DATE_PRESETS.map((preset) => (0, import_jsx_runtime22.jsxs)(OptionButton, {
            type: "button",
            onClick: () => {
              onChange({
                ...filter,
                preset
              });
              setOpen(false);
            },
            children: [
              formatMessage(PRESET_LABELS[preset]),
              preset === filter.preset && (0, import_jsx_runtime22.jsx)(h5, {
                "aria-hidden": true,
                width: "1.6rem",
                height: "1.6rem"
              })
            ]
          }, preset))
        }) : (0, import_jsx_runtime22.jsx)(DateRangeCalendar, {
          from: filter.from,
          to: filter.to,
          onSelect: (from, to) => {
            onChange({
              ...filter,
              from,
              to
            });
            setOpen(false);
          }
        })
      })
    ]
  });
};
var FilterBadge = ({ filter, onChange, onRemove }) => {
  const { formatMessage } = useIntl();
  const fieldLabel = filter.kind === "type" ? formatMessage({
    id: getTranslationKey("list.filters.field.type"),
    defaultMessage: "Type"
  }) : formatMessage(DATE_FIELD_LABELS[filter.field]);
  return (0, import_jsx_runtime22.jsxs)(BadgePill, {
    alignItems: "stretch",
    "data-testid": "filter-badge",
    children: [
      (0, import_jsx_runtime22.jsx)(FieldSegment, {
        children: (0, import_jsx_runtime22.jsx)(A, {
          variant: "pi",
          textColor: "neutral600",
          children: fieldLabel
        })
      }),
      filter.kind === "type" && (0, import_jsx_runtime22.jsxs)(import_jsx_runtime22.Fragment, {
        children: [
          (0, import_jsx_runtime22.jsx)(SegmentPopover, {
            label: formatMessage(TYPE_CONDITION_LABELS[filter.condition]),
            options: [
              "is",
              "isNot"
            ],
            active: filter.condition,
            getOptionLabel: (option) => formatMessage(TYPE_CONDITION_LABELS[option]),
            onPick: (condition) => onChange({
              ...filter,
              condition
            })
          }),
          (0, import_jsx_runtime22.jsx)(TypeValuePopover, {
            filter,
            onChange
          })
        ]
      }),
      filter.kind === "date" && filter.mode === "preset" && (0, import_jsx_runtime22.jsxs)(import_jsx_runtime22.Fragment, {
        children: [
          (0, import_jsx_runtime22.jsx)(SegmentPopover, {
            label: formatMessage(PRESET_CONDITION_LABELS[filter.condition]),
            options: [
              "isExactly",
              "withinLast",
              "notWithinLast"
            ],
            active: filter.condition,
            getOptionLabel: (option) => formatMessage(PRESET_CONDITION_LABELS[option]),
            onPick: (condition) => onChange({
              ...filter,
              condition
            })
          }),
          (0, import_jsx_runtime22.jsx)(DateValuePopover, {
            filter,
            onChange
          })
        ]
      }),
      filter.kind === "date" && filter.mode === "range" && (0, import_jsx_runtime22.jsxs)(import_jsx_runtime22.Fragment, {
        children: [
          (0, import_jsx_runtime22.jsx)(SegmentPopover, {
            label: formatMessage(RANGE_CONDITION_LABELS[filter.condition]),
            options: [
              "is",
              "isNot"
            ],
            active: filter.condition,
            getOptionLabel: (option) => formatMessage(RANGE_CONDITION_LABELS[option]),
            onPick: (condition) => onChange({
              ...filter,
              condition
            })
          }),
          (0, import_jsx_runtime22.jsx)(DateValuePopover, {
            filter,
            onChange
          })
        ]
      }),
      (0, import_jsx_runtime22.jsx)(RemoveButton, {
        type: "button",
        onClick: onRemove,
        "aria-label": formatMessage({
          id: getTranslationKey("list.filters.remove"),
          defaultMessage: "Remove {filter} filter"
        }, {
          filter: fieldLabel
        }),
        children: (0, import_jsx_runtime22.jsx)(F5, {
          width: "1.2rem",
          height: "1.2rem",
          "aria-hidden": true
        })
      })
    ]
  });
};
var BadgesRoot = gt(C)`
  padding-top: ${({ theme, $compact }) => $compact ? theme.spaces[1] : theme.spaces[6]};
  transition: padding-top 0.2s ease;
`;
var FilterBadges = ({ listFilters, compact = false }) => {
  const { filters, updateFilter, removeFilter } = listFilters;
  if (filters.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime22.jsx)(BadgesRoot, {
    $compact: compact,
    gap: 2,
    wrap: "wrap",
    "data-testid": "filter-badges",
    children: filters.map((filter, index) => (0, import_jsx_runtime22.jsx)(FilterBadge, {
      filter,
      onChange: (next) => updateFilter(index, next),
      onRemove: () => removeFilter(index)
    }, index))
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FolderTree/FolderTree.mjs
var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
var import_react20 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/Dnd/useFolderTreeDroppable.mjs
var useFolderTreeDroppable = (target) => {
  const { isMovePending, isValidDropTarget } = useAssetsDndOptional() ?? {
    isMovePending: false,
    isValidDropTarget: () => false
  };
  const { active } = useDndContext();
  const dropId = target.id == null ? HOME_TREE_TARGET_ID : toFolderTreeTargetId(target.id);
  const data = {
    kind: "folder-tree-target",
    id: target.id,
    name: target.name
  };
  const droppable = useDroppable({
    id: dropId,
    data,
    disabled: isMovePending
  });
  const isValidTarget = isValidDropTarget(target.id);
  const isOver = droppable.isOver;
  return {
    droppable,
    isOver,
    showValidDropHighlight: isOver && isValidTarget,
    showInvalidDropCursor: isOver && !isValidTarget && active != null
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FolderTree/useSpringLoadedExpand.mjs
var import_react19 = __toESM(require_react(), 1);
var SPRING_LOAD_DELAY_MS = 600;
var useSpringLoadedExpand = ({ isOver, canExpand, onExpand }) => {
  (0, import_react19.useEffect)(() => {
    if (!isOver || !canExpand) {
      return;
    }
    const timer = setTimeout(onExpand, SPRING_LOAD_DELAY_MS);
    return () => clearTimeout(timer);
  }, [
    isOver,
    canExpand,
    onExpand
  ]);
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/FolderTree/FolderTree.mjs
var RowButton = gt.button`
  display: flex;
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]};
  width: 100%;
  min-height: 3.2rem;
  padding: ${({ theme }) => `${theme.spaces[1]} ${theme.spaces[2]}`};
  border: 0;
  background: ${({ $isActive, $isValidDropTarget, theme }) => {
  if ($isValidDropTarget) {
    return theme.colors.primary100;
  }
  return $isActive ? theme.colors.primary100 : "transparent";
}};
  color: ${({ $isActive, theme }) => $isActive ? theme.colors.primary700 : theme.colors.neutral800};
  border-radius: ${({ theme }) => theme.borderRadius};
  cursor: ${({ $isMovePending, $isInvalidDropCursor }) => {
  if ($isMovePending) {
    return "wait";
  }
  return $isInvalidDropCursor ? "not-allowed" : "pointer";
}};
  text-align: left;
  font: inherit;
  pointer-events: ${({ $isMovePending }) => $isMovePending ? "none" : "auto"};

  ${({ $isValidDropTarget, theme }) => $isValidDropTarget && ft`
      outline: 1px dashed ${theme.colors.primary600};
      outline-offset: -1px;
    `}

  &:hover {
    background: ${({ $isActive, $isValidDropTarget, theme }) => {
  if ($isValidDropTarget) {
    return theme.colors.primary100;
  }
  return $isActive ? theme.colors.primary100 : theme.colors.neutral100;
}};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }
`;
var TreeRow = gt(C)`
  cursor: ${({ $isMovePending, $isInvalidDropCursor }) => {
  if ($isMovePending) {
    return "wait";
  }
  return $isInvalidDropCursor ? "not-allowed" : "default";
}};
  pointer-events: ${({ $isMovePending }) => $isMovePending ? "none" : "auto"};
  border-radius: ${({ theme }) => theme.borderRadius};

  ${({ $isValidDropTarget, theme }) => $isValidDropTarget && ft`
      background: ${theme.colors.primary100};
      outline: 1px dashed ${theme.colors.primary600};
      outline-offset: -1px;
    `}
`;
var findAncestorIds = (nodes, targetId, trail = []) => {
  var _a;
  for (const node of nodes) {
    if (node.id === targetId) {
      return trail;
    }
    if ((_a = node.children) == null ? void 0 : _a.length) {
      const nextTrail = node.id != null ? [
        ...trail,
        node.id
      ] : trail;
      const found = findAncestorIds(node.children, targetId, nextTrail);
      if (found !== null) {
        return found;
      }
    }
  }
  return null;
};
var useExpandedFolders = (folderStructure, currentFolderId) => {
  const [expandedIds, setExpandedIds] = (0, import_react20.useState)(() => /* @__PURE__ */ new Set());
  (0, import_react20.useEffect)(() => {
    if (currentFolderId == null) {
      return;
    }
    const ancestors = findAncestorIds(folderStructure, currentFolderId);
    if (!ancestors || ancestors.length === 0) {
      return;
    }
    setExpandedIds((prev) => {
      const next = new Set(prev);
      let changed = false;
      for (const id of ancestors) {
        if (!next.has(id)) {
          next.add(id);
          changed = true;
        }
      }
      return changed ? next : prev;
    });
  }, [
    folderStructure,
    currentFolderId
  ]);
  const toggleExpanded = (0, import_react20.useCallback)((id) => {
    setExpandedIds((prev) => {
      const next = new Set(prev);
      if (next.has(id)) {
        next.delete(id);
      } else {
        next.add(id);
      }
      return next;
    });
  }, []);
  const expandFolder = (0, import_react20.useCallback)((id) => {
    setExpandedIds((prev) => {
      if (prev.has(id)) {
        return prev;
      }
      const next = new Set(prev);
      next.add(id);
      return next;
    });
  }, []);
  const isExpanded = (0, import_react20.useCallback)((id) => expandedIds.has(id), [
    expandedIds
  ]);
  return {
    isExpanded,
    toggleExpanded,
    expandFolder
  };
};
var NavList = gt.ul`
  list-style: none;
  margin: 0;
  padding: 0;

  /* Grid rather than block, and load-bearing despite rendering a single column:
     a minmax(0, 1fr) track contributes a minimum of 0, which is what stops each
     row propagating the min-content width of its own label.

     Folder names ellipsize, and text-overflow needs white-space: nowrap — so a
     label's min-content width is the entire name, and no box lays out narrower
     than its min-content. In block flow that floor travels up to the SubNav
     ScrollArea, which widens the rail and shows a horizontal scrollbar instead of
     truncating the name. Nesting makes it worse: the indent is spent before the
     label is measured, so shorter names trigger it the deeper you go.

     Measured in Chromium — dropping either declaration brings the scrollbar
     back, and neither min-width nor overflow on the row is a substitute. */
  display: grid;
  grid-template-columns: minmax(0, 1fr);
`;
var INDENT_PER_LEVEL_REM = 1.6;
var ChevronButton = gt(be)`
  &&[aria-disabled='true'] {
    background: transparent;
    border-color: transparent;
    opacity: 0.3;
  }
`;
var RotatingChevron = gt(o5)`
  transform: rotate(${({ $expanded }) => $expanded ? "0deg" : "-90deg"});
  transition: transform 0.2s ease;
`;
var FolderTreeItemInner = ({ id, name, folderChildren, level, currentFolderId, showActiveFolder, isExpanded, onToggle, onExpand, onSelect, isMovePending }) => {
  const { formatMessage } = useIntl();
  const hasChildren = folderChildren.length > 0;
  const isFolderExpanded = isExpanded(id);
  const isActive = showActiveFolder && currentFolderId === id;
  const { droppable: { setNodeRef }, isOver, showValidDropHighlight, showInvalidDropCursor } = useFolderTreeDroppable({
    id,
    name
  });
  const handleExpand = (0, import_react20.useCallback)(() => onExpand(id), [
    id,
    onExpand
  ]);
  useSpringLoadedExpand({
    isOver,
    canExpand: hasChildren && !isFolderExpanded,
    onExpand: handleExpand
  });
  return (0, import_jsx_runtime23.jsxs)("li", {
    children: [
      (0, import_jsx_runtime23.jsxs)(TreeRow, {
        ref: setNodeRef,
        alignItems: "center",
        paddingLeft: `${level * INDENT_PER_LEVEL_REM}rem`,
        gap: 1,
        $isValidDropTarget: showValidDropHighlight,
        $isInvalidDropCursor: showInvalidDropCursor,
        $isMovePending: isMovePending,
        children: [
          (0, import_jsx_runtime23.jsx)(ChevronButton, {
            label: hasChildren ? formatMessage({
              id: getTranslationKey(isFolderExpanded ? "sidebar.tree.collapse" : "sidebar.tree.expand"),
              defaultMessage: isFolderExpanded ? "Collapse {name}" : "Expand {name}"
            }, {
              name
            }) : formatMessage({
              id: getTranslationKey("sidebar.tree.no-subfolders"),
              defaultMessage: "The folder {name} has no subfolders"
            }, {
              name
            }),
            disabled: !hasChildren,
            onClick: (event) => {
              event.stopPropagation();
              onToggle(id);
            },
            variant: "ghost",
            withTooltip: false,
            "aria-expanded": hasChildren ? isFolderExpanded : void 0,
            children: (0, import_jsx_runtime23.jsx)(RotatingChevron, {
              $expanded: isFolderExpanded,
              fill: "neutral500"
            })
          }),
          (0, import_jsx_runtime23.jsx)(S, {
            flex: "1",
            minWidth: 0,
            children: (0, import_jsx_runtime23.jsx)(RowButton, {
              type: "button",
              $isActive: isActive,
              $isValidDropTarget: showValidDropHighlight,
              $isInvalidDropCursor: showInvalidDropCursor,
              $isMovePending: isMovePending,
              "aria-current": isActive ? "page" : void 0,
              onClick: () => onSelect(id),
              "data-testid": `folder-tree-node-${id}`,
              "data-folder-id": id,
              children: (0, import_jsx_runtime23.jsx)(TruncatedText, {
                variant: "omega",
                fontWeight: isActive ? "semiBold" : "regular",
                children: name
              })
            })
          })
        ]
      }),
      hasChildren && isFolderExpanded && (0, import_jsx_runtime23.jsx)(NavList, {
        children: folderChildren.map((child) => (0, import_jsx_runtime23.jsx)(FolderTreeItem, {
          node: child,
          level: level + 1,
          currentFolderId,
          showActiveFolder,
          isExpanded,
          onToggle,
          onExpand,
          onSelect,
          isMovePending
        }, child.id ?? child.name))
      })
    ]
  });
};
var FolderTreeItem = ({ node, ...props }) => {
  if (node.id == null) {
    return null;
  }
  return (0, import_jsx_runtime23.jsx)(FolderTreeItemInner, {
    ...props,
    id: node.id,
    name: node.name ?? "",
    folderChildren: node.children ?? []
  });
};
var FolderTree = ({ currentFolderId, showActiveFolder = true, onSelectFolder }) => {
  const { formatMessage } = useIntl();
  const { data: folderStructure = [], isLoading, isError } = useGetFolderStructureQuery();
  const { isExpanded, toggleExpanded, expandFolder } = useExpandedFolders(folderStructure, currentFolderId);
  const { isMovePending } = useAssetsDndOptional() ?? {
    isMovePending: false
  };
  const isHomeActive = showActiveFolder && currentFolderId == null;
  const homeLabel = formatMessage({
    id: getTranslationKey("sidebar.home"),
    defaultMessage: "Home"
  });
  const { droppable: { setNodeRef: setHomeDropRef }, showValidDropHighlight: showHomeValidDropHighlight, showInvalidDropCursor: showHomeInvalidDropCursor } = useFolderTreeDroppable({
    id: null,
    name: homeLabel
  });
  return (0, import_jsx_runtime23.jsxs)(SubNav.Main, {
    "aria-label": formatMessage({
      id: getTranslationKey("sidebar.tree.aria-label"),
      defaultMessage: "Media library folders"
    }),
    children: [
      (0, import_jsx_runtime23.jsx)(SubNav.Header, {
        label: formatMessage({
          id: getTranslationKey("sidebar.title"),
          defaultMessage: "Media library"
        })
      }),
      (0, import_jsx_runtime23.jsx)(SubNav.Content, {
        children: (0, import_jsx_runtime23.jsxs)(C, {
          direction: "column",
          alignItems: "stretch",
          gap: 1,
          padding: 3,
          children: [
            (0, import_jsx_runtime23.jsxs)(RowButton, {
              ref: setHomeDropRef,
              type: "button",
              $isActive: isHomeActive,
              $isValidDropTarget: showHomeValidDropHighlight,
              $isInvalidDropCursor: showHomeInvalidDropCursor,
              $isMovePending: isMovePending,
              "aria-current": isHomeActive ? "page" : void 0,
              onClick: () => onSelectFolder(null),
              "data-testid": "folder-tree-home",
              children: [
                (0, import_jsx_runtime23.jsx)(d3, {
                  "aria-hidden": true,
                  width: "1.6rem",
                  height: "1.6rem"
                }),
                (0, import_jsx_runtime23.jsx)(A, {
                  variant: "omega",
                  fontWeight: isHomeActive ? "semiBold" : "regular",
                  children: homeLabel
                })
              ]
            }),
            (0, import_jsx_runtime23.jsxs)(S, {
              marginTop: 4,
              children: [
                (0, import_jsx_runtime23.jsxs)(C, {
                  alignItems: "center",
                  gap: 1,
                  paddingTop: 1,
                  paddingBottom: 1,
                  paddingLeft: 2,
                  paddingRight: 2,
                  marginBottom: 2,
                  children: [
                    (0, import_jsx_runtime23.jsx)(_5, {
                      "aria-hidden": true,
                      width: "1.6rem",
                      height: "1.6rem",
                      fill: "neutral500"
                    }),
                    (0, import_jsx_runtime23.jsx)(A, {
                      variant: "sigma",
                      textColor: "neutral600",
                      style: {
                        textTransform: "uppercase"
                      },
                      children: formatMessage({
                        id: getTranslationKey("sidebar.folders"),
                        defaultMessage: "Folders"
                      })
                    })
                  ]
                }),
                isLoading ? (
                  // TODO: revisit loading state before revamp GA
                  (0, import_jsx_runtime23.jsx)(C, {
                    justifyContent: "center",
                    padding: 1,
                    paddingTop: 2,
                    children: (0, import_jsx_runtime23.jsx)(kt, {
                      children: formatMessage({
                        id: getTranslationKey("sidebar.tree.loading"),
                        defaultMessage: "Loading folders..."
                      })
                    })
                  })
                ) : isError ? (
                  // TODO: revisit error state before revamp GA
                  (0, import_jsx_runtime23.jsx)(S, {
                    padding: 1,
                    paddingTop: 2,
                    children: (0, import_jsx_runtime23.jsx)(A, {
                      variant: "pi",
                      textColor: "danger600",
                      children: formatMessage({
                        id: getTranslationKey("sidebar.tree.error"),
                        defaultMessage: "Could not load folders."
                      })
                    })
                  })
                ) : folderStructure.length === 0 ? (
                  // TODO: revisit empty state before revamp GA
                  (0, import_jsx_runtime23.jsx)(S, {
                    padding: 1,
                    paddingTop: 2,
                    children: (0, import_jsx_runtime23.jsx)(A, {
                      variant: "pi",
                      textColor: "neutral500",
                      children: formatMessage({
                        id: getTranslationKey("sidebar.tree.empty"),
                        defaultMessage: "No folders yet"
                      })
                    })
                  })
                ) : (0, import_jsx_runtime23.jsx)(NavList, {
                  children: folderStructure.map((node) => (0, import_jsx_runtime23.jsx)(FolderTreeItem, {
                    node,
                    level: 0,
                    currentFolderId,
                    showActiveFolder,
                    isExpanded,
                    onToggle: toggleExpanded,
                    onExpand: expandFolder,
                    onSelect: onSelectFolder,
                    isMovePending
                  }, node.id ?? node.name))
                })
              ]
            })
          ]
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/ImportFromUrlDialog.mjs
var import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);
var React7 = __toESM(require_react(), 1);
var ImportFromUrlDialog = ({ open, onClose, onUpload }) => {
  const { formatMessage } = useIntl();
  const [urls, setUrls] = React7.useState("");
  const [error, setError] = React7.useState(null);
  const handleClose = () => {
    setUrls("");
    setError(null);
    onClose();
  };
  const handleSubmit = async (e) => {
    e.preventDefault();
    const { urls: validUrls, error: validationError } = validateUrls(urls);
    if (validationError) {
      setError(validationError);
      return;
    }
    setError(null);
    handleClose();
    await onUpload(validUrls);
  };
  return (0, import_jsx_runtime24.jsx)(_d.Root, {
    open,
    onOpenChange: (isOpen) => !isOpen && handleClose(),
    children: (0, import_jsx_runtime24.jsx)(_d.Content, {
      children: (0, import_jsx_runtime24.jsxs)("form", {
        onSubmit: handleSubmit,
        children: [
          (0, import_jsx_runtime24.jsx)(_d.Header, {
            children: (0, import_jsx_runtime24.jsx)(_d.Title, {
              children: formatMessage({
                id: getTranslationKey("modal.url.title"),
                defaultMessage: "Import from URL"
              })
            })
          }),
          (0, import_jsx_runtime24.jsx)(_d.Body, {
            children: (0, import_jsx_runtime24.jsxs)(Dd.Root, {
              error: error || void 0,
              hint: formatMessage({
                id: getTranslationKey("input.url.description"),
                defaultMessage: "Separate your URL links by a carriage return."
              }),
              children: [
                (0, import_jsx_runtime24.jsx)(Dd.Label, {
                  children: formatMessage({
                    id: getTranslationKey("input.url.label"),
                    defaultMessage: "URL(s)"
                  })
                }),
                (0, import_jsx_runtime24.jsx)($u, {
                  name: "urls",
                  minHeight: "unset",
                  rows: Math.min(urls.split("\n").length, 7),
                  maxHeight: "10.5rem",
                  placeholder: formatMessage({
                    id: getTranslationKey("input.url.placeholder"),
                    defaultMessage: "Empty"
                  }),
                  value: urls,
                  onChange: (e) => {
                    setUrls(e.target.value);
                    setError(null);
                  }
                }),
                (0, import_jsx_runtime24.jsx)(Dd.Hint, {}),
                (0, import_jsx_runtime24.jsx)(Dd.Error, {})
              ]
            })
          }),
          (0, import_jsx_runtime24.jsxs)(_d.Footer, {
            children: [
              (0, import_jsx_runtime24.jsx)(Ct, {
                variant: "tertiary",
                onClick: handleClose,
                children: formatMessage({
                  id: "app.components.Button.cancel",
                  defaultMessage: "Cancel"
                })
              }),
              (0, import_jsx_runtime24.jsx)(Ct, {
                type: "submit",
                children: formatMessage({
                  id: getTranslationKey("modal.url.upload"),
                  defaultMessage: "Upload"
                })
              })
            ]
          })
        ]
      })
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/SortMenu.mjs
var import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
var SORT_BY_LABELS = {
  oldestUploads: {
    id: getTranslationKey("list.sort.oldest-uploads"),
    defaultMessage: "Oldest uploads"
  },
  mostRecentUpdates: {
    id: getTranslationKey("list.sort.most-recent-updates"),
    defaultMessage: "Most recent updates"
  }
};
var SORT_DIRECTION_LABELS = {
  nameAsc: {
    id: getTranslationKey("list.sort.name-asc"),
    defaultMessage: "A to Z"
  },
  nameDesc: {
    id: getTranslationKey("list.sort.name-desc"),
    defaultMessage: "Z to A"
  },
  sizeAsc: {
    id: getTranslationKey("list.sort.size-asc"),
    defaultMessage: "File size ascending"
  },
  sizeDesc: {
    id: getTranslationKey("list.sort.size-desc"),
    defaultMessage: "File size descending"
  }
};
var FOLDERS_LABELS = {
  top: {
    id: getTranslationKey("list.sort.folders-on-top"),
    defaultMessage: "On top"
  },
  mixed: {
    id: getTranslationKey("list.sort.folders-mixed"),
    defaultMessage: "Mixed with files"
  }
};
var SortTrigger = gt(gd.Trigger)``;
var GroupLabel = gt(gd.Label)`
  width: 100%;
  display: block;
  background: ${({ theme }) => theme.colorScheme === "dark" ? theme.colors.neutral150 : theme.colors.neutral100};
  padding-inline: ${({ theme }) => theme.spaces[3]};
  border-radius: ${({ theme }) => theme.borderRadius};
`;
var SortMenu = ({ sort, showFoldersGroup = true }) => {
  const { formatMessage } = useIntl();
  const { trackUsage } = useTracking2();
  const triggerLabel = formatMessage({
    id: getTranslationKey("list.sort.trigger"),
    defaultMessage: "Sort: {active}"
  }, {
    active: sort.sortBy ? formatMessage(SORT_BY_LABELS[sort.sortBy]) : formatMessage(SORT_DIRECTION_LABELS[sort.direction])
  });
  const checkmark = (0, import_jsx_runtime25.jsx)(h5, {
    "aria-hidden": true,
    width: "1.6rem",
    height: "1.6rem",
    fill: "primary600"
  });
  return (0, import_jsx_runtime25.jsxs)(gd.Root, {
    children: [
      (0, import_jsx_runtime25.jsx)(SortTrigger, {
        variant: "ghost",
        endIcon: (0, import_jsx_runtime25.jsx)(o5, {
          "aria-hidden": true
        }),
        children: triggerLabel
      }),
      (0, import_jsx_runtime25.jsxs)(gd.Content, {
        popoverPlacement: "bottom-end",
        zIndex: 2,
        maxHeight: "70vh",
        minWidth: "25rem",
        children: [
          (0, import_jsx_runtime25.jsx)(GroupLabel, {
            children: formatMessage({
              id: getTranslationKey("list.sort.section"),
              defaultMessage: "Sort"
            })
          }),
          Object.keys(SORT_BY_LABELS).map((key) => (0, import_jsx_runtime25.jsx)(gd.Item, {
            role: "menuitemradio",
            "aria-checked": sort.sortBy === key,
            onSelect: (e) => {
              e.preventDefault();
              if (sort.sortBy !== key) {
                trackUsage("didSortMediaLibraryElements", {
                  location: MEDIA_LIBRARY_LOCATION,
                  sort: key
                });
              }
              sort.setSortBy(sort.sortBy === key ? null : key);
            },
            endIcon: sort.sortBy === key ? checkmark : null,
            children: formatMessage(SORT_BY_LABELS[key])
          }, key)),
          Object.keys(SORT_DIRECTION_LABELS).map((key) => (0, import_jsx_runtime25.jsx)(gd.Item, {
            role: "menuitemradio",
            "aria-checked": sort.direction === key,
            onSelect: (e) => {
              e.preventDefault();
              if (sort.direction !== key) {
                trackUsage("didSortMediaLibraryElements", {
                  location: MEDIA_LIBRARY_LOCATION,
                  sort: key
                });
              }
              sort.setDirection(sort.direction === key ? null : key);
            },
            endIcon: sort.direction === key ? checkmark : null,
            children: formatMessage(SORT_DIRECTION_LABELS[key])
          }, key)),
          showFoldersGroup && (0, import_jsx_runtime25.jsxs)(import_jsx_runtime25.Fragment, {
            children: [
              (0, import_jsx_runtime25.jsx)(gd.Separator, {}),
              (0, import_jsx_runtime25.jsx)(GroupLabel, {
                children: formatMessage({
                  id: getTranslationKey("list.sort.folders"),
                  defaultMessage: "Folders"
                })
              }),
              Object.keys(FOLDERS_LABELS).map((position) => (0, import_jsx_runtime25.jsx)(gd.Item, {
                role: "menuitemradio",
                "aria-checked": sort.foldersPosition === position,
                onSelect: (e) => {
                  e.preventDefault();
                  sort.setFoldersPosition(position);
                },
                endIcon: sort.foldersPosition === position ? checkmark : null,
                children: formatMessage(FOLDERS_LABELS[position])
              }, position))
            ]
          })
        ]
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useInfiniteAssets.mjs
var import_react21 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/utils/mergeUploadedAssets.mjs
var SORT_FIELDS = [
  "createdAt",
  "updatedAt",
  "name",
  "size"
];
var isSortField = (value) => SORT_FIELDS.includes(value);
var DEFAULT_RULE = "updatedAt:DESC";
var compareByField = (field, a, b) => {
  if (field === "size") {
    return (a.size ?? 0) - (b.size ?? 0);
  }
  if (field === "name") {
    return a.name.localeCompare(b.name);
  }
  return (a[field] ?? "").localeCompare(b[field] ?? "");
};
var getAssetComparator = (rule = DEFAULT_RULE) => {
  const [field, requestedDirection] = rule.split(":");
  const isKnownField = isSortField(field);
  const sortField = isKnownField ? field : "updatedAt";
  const direction = isKnownField ? requestedDirection : "DESC";
  const sign = direction === "ASC" ? 1 : -1;
  return (a, b) => {
    const byField = compareByField(sortField, a, b);
    return byField !== 0 ? sign * byField : sign * (a.id - b.id);
  };
};
var mergeUploadedAssets = ({ assets, uploaded, sort, hasNextPage }) => {
  if (uploaded.length === 0) {
    return assets;
  }
  const loadedIds = new Set(assets.map((asset) => asset.id));
  const compare = getAssetComparator(sort);
  const fresh = uploaded.filter((asset) => !loadedIds.has(asset.id)).sort(compare);
  if (fresh.length === 0) {
    return assets;
  }
  const merged = [
    ...assets
  ];
  for (const asset of fresh) {
    const at = merged.findIndex((loaded) => compare(asset, loaded) < 0);
    if (at === -1) {
      if (hasNextPage) {
        continue;
      }
      merged.push(asset);
    } else {
      merged.splice(at, 0, asset);
    }
  }
  return merged;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useInfiniteAssets.mjs
var PAGE_SIZE = 20;
var flattenPages = (pages) => {
  const byId = /* @__PURE__ */ new Map();
  for (const page of Object.keys(pages).map(Number).sort((a, b) => a - b)) {
    for (const asset of pages[page]) {
      byId.set(asset.id, asset);
    }
  }
  return [
    ...byId.values()
  ];
};
var PageSubscriber = ({ queryArgs, page, onRefreshed }) => {
  const { currentData } = useGetAssetsQuery({
    ...queryArgs,
    page,
    pageSize: PAGE_SIZE
  });
  const results = currentData == null ? void 0 : currentData.results;
  (0, import_react21.useEffect)(() => {
    if (results) {
      onRefreshed(page, results);
    }
  }, [
    results,
    page,
    onRefreshed
  ]);
  return null;
};
var useInfiniteAssets = ({ folder = null, sort, search, filters, enabled = true } = {}) => {
  const queryArgs = {
    folder,
    sort,
    search,
    filters
  };
  const queryKey = JSON.stringify(queryArgs);
  const listKey = JSON.stringify({
    folder,
    sort,
    filters
  });
  const [pageState, setPageState] = (0, import_react21.useState)({
    queryKey,
    page: 1
  });
  const [accumulated, setAccumulated] = (0, import_react21.useState)({
    queryKey,
    listKey,
    pages: {}
  });
  const page = pageState.queryKey === queryKey ? pageState.page : 1;
  if (pageState.queryKey !== queryKey) {
    setPageState({
      queryKey,
      page: 1
    });
  }
  const { currentData, isLoading, isFetching, error, startedTimeStamp, fulfilledTimeStamp } = useGetAssetsQuery({
    ...queryArgs,
    page,
    pageSize: PAGE_SIZE
  }, {
    skip: !enabled
  });
  const isSameQuery = accumulated.queryKey === queryKey;
  if (currentData && (!isSameQuery || accumulated.pages[page] !== currentData.results)) {
    setAccumulated(isSameQuery ? {
      ...accumulated,
      pages: {
        ...accumulated.pages,
        [page]: currentData.results
      },
      pagination: currentData.pagination
    } : {
      queryKey,
      listKey,
      pages: {
        [page]: currentData.results
      },
      pagination: currentData.pagination
    });
  }
  const handlePageRefreshed = (0, import_react21.useCallback)((refreshedPage, results) => {
    setAccumulated((prev) => {
      if (prev.queryKey !== queryKey || prev.pages[refreshedPage] === results) {
        return prev;
      }
      return {
        ...prev,
        pages: {
          ...prev.pages,
          [refreshedPage]: results
        }
      };
    });
  }, [
    queryKey
  ]);
  const subscribers = (0, import_react21.createElement)(import_react21.Fragment, null, Array.from({
    length: Math.max(0, page - 1)
  }, (_, index) => index + 1).map((earlierPage) => (0, import_react21.createElement)(PageSubscriber, {
    key: `${queryKey}:${earlierPage}`,
    queryArgs,
    page: earlierPage,
    onRefreshed: handlePageRefreshed
  })));
  const dispatch = useDispatch();
  const store = useStore();
  const prevFolderRef = (0, import_react21.useRef)(folder);
  (0, import_react21.useEffect)(() => {
    const prevFolder = prevFolderRef.current;
    prevFolderRef.current = folder;
    if (prevFolder === folder) {
      return;
    }
    const apiState = store.getState()[uploadApi.reducerPath];
    const removeQueryResult = uploadApi.internalActions.removeQueryResult;
    Object.keys((apiState == null ? void 0 : apiState.queries) ?? {}).forEach((cacheKey) => {
      if (!cacheKey.startsWith("getAssets(")) {
        return;
      }
      let args;
      try {
        args = JSON.parse(cacheKey.slice("getAssets(".length, -1));
      } catch {
        return;
      }
      if (args.folder === prevFolder) {
        dispatch(removeQueryResult({
          queryCacheKey: cacheKey
        }));
      }
    });
  }, [
    folder,
    dispatch,
    store
  ]);
  const isChangingList = accumulated.listKey !== listKey;
  const loadedAssets = (0, import_react21.useMemo)(() => isChangingList ? [] : flattenPages(accumulated.pages), [
    isChangingList,
    accumulated.pages
  ]);
  const hasNextPage = currentData ? page < currentData.pagination.pageCount : false;
  const completedUploads = useTypedSelector(selectCompletedUploads);
  const canPlaceUploads = !search && ((filters == null ? void 0 : filters.length) ?? 0) === 0;
  const listAnsweredAfter = fulfilledTimeStamp !== void 0 && startedTimeStamp !== void 0 && // A refetch in flight leaves `fulfilledTimeStamp` on the previous response,
  // which predates this request — so only settled data counts.
  fulfilledTimeStamp > startedTimeStamp ? startedTimeStamp : void 0;
  const assets = (0, import_react21.useMemo)(() => {
    if (!canPlaceUploads || completedUploads.length === 0) {
      return loadedAssets;
    }
    const bridging = completedUploads.filter(({ asset, completedAt }) => getRelationId(asset.folder) === folder && (listAnsweredAfter === void 0 || completedAt > listAnsweredAfter));
    return mergeUploadedAssets({
      assets: loadedAssets,
      uploaded: bridging.map(({ asset }) => asset),
      sort,
      hasNextPage
    });
  }, [
    canPlaceUploads,
    completedUploads,
    loadedAssets,
    folder,
    sort,
    hasNextPage,
    listAnsweredAfter
  ]);
  const isFetchingMore = isFetching && page > 1;
  const fetchNextPage = (0, import_react21.useCallback)(() => {
    setPageState((prev) => ({
      queryKey,
      page: (prev.queryKey === queryKey ? prev.page : 1) + 1
    }));
  }, [
    queryKey
  ]);
  if (!enabled) {
    return {
      assets: [],
      // No list to keep fresh while disabled, so nothing to subscribe.
      subscribers: null,
      pagination: void 0,
      isLoading: false,
      isFetchingMore: false,
      hasNextPage: false,
      fetchNextPage,
      error: void 0
    };
  }
  return {
    assets,
    subscribers,
    // Falls back to the accumulated total so a consumer showing the count
    // doesn't flash zero mid-transition, matching the possibly stale `assets`.
    pagination: (currentData == null ? void 0 : currentData.pagination) ?? accumulated.pagination,
    isLoading: isLoading || isChangingList,
    isFetchingMore,
    hasNextPage,
    fetchNextPage,
    error
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useInfiniteScrollSentinel.mjs
var import_react22 = __toESM(require_react(), 1);
var useInfiniteScrollSentinel = ({ hasNextPage, isFetchingMore, onLoadMore, options }) => {
  const observerRef = (0, import_react22.useRef)(null);
  const nodeRef = (0, import_react22.useRef)(null);
  const optionsRef = (0, import_react22.useRef)(options);
  optionsRef.current = options;
  const onLoadMoreRef = (0, import_react22.useRef)(onLoadMore);
  onLoadMoreRef.current = onLoadMore;
  const hasNextPageRef = (0, import_react22.useRef)(hasNextPage);
  hasNextPageRef.current = hasNextPage;
  const isFetchingMoreRef = (0, import_react22.useRef)(isFetchingMore);
  isFetchingMoreRef.current = isFetchingMore;
  const sentinelRef = (0, import_react22.useCallback)((node) => {
    var _a;
    (_a = observerRef.current) == null ? void 0 : _a.disconnect();
    nodeRef.current = node;
    if (!node) {
      return;
    }
    const observer = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting && hasNextPageRef.current && !isFetchingMoreRef.current) {
        onLoadMoreRef.current();
      }
    }, optionsRef.current);
    observer.observe(node);
    observerRef.current = observer;
  }, []);
  (0, import_react22.useEffect)(() => () => {
    var _a;
    return (_a = observerRef.current) == null ? void 0 : _a.disconnect();
  }, []);
  (0, import_react22.useEffect)(() => {
    if (isFetchingMore || !observerRef.current || !nodeRef.current) {
      return;
    }
    observerRef.current.unobserve(nodeRef.current);
    observerRef.current.observe(nodeRef.current);
  }, [
    isFetchingMore
  ]);
  return sentinelRef;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useListSort.mjs
var SORT_BY_RULES = {
  oldestUploads: "createdAt:ASC",
  mostRecentUpdates: "updatedAt:DESC"
};
var SORT_DIRECTION_RULES = {
  nameAsc: "name:ASC",
  nameDesc: "name:DESC",
  sizeAsc: "size:ASC",
  sizeDesc: "size:DESC"
};
var DEFAULT_SORT_BY = "mostRecentUpdates";
var SORT_BY_BY_RULE = Object.fromEntries(Object.entries(SORT_BY_RULES).map(([key, rule]) => [
  rule,
  key
]));
var SORT_DIRECTION_BY_RULE = Object.fromEntries(Object.entries(SORT_DIRECTION_RULES).map(([key, rule]) => [
  rule,
  key
]));
var parseSortParam = (raw) => {
  for (const rule of (raw ?? "").split(",")) {
    if (rule in SORT_BY_BY_RULE) {
      return {
        sortBy: SORT_BY_BY_RULE[rule],
        direction: null,
        isExplicit: true
      };
    }
    if (rule in SORT_DIRECTION_BY_RULE) {
      return {
        sortBy: null,
        direction: SORT_DIRECTION_BY_RULE[rule],
        isExplicit: true
      };
    }
  }
  return {
    sortBy: DEFAULT_SORT_BY,
    direction: null,
    isExplicit: false
  };
};
var serializeSort = (sortBy, direction) => {
  const rules = [
    sortBy && SORT_BY_RULES[sortBy],
    direction && SORT_DIRECTION_RULES[direction]
  ];
  return rules.filter((rule) => Boolean(rule)).join(",");
};
var useListSort = () => {
  const [{ query }, setQuery] = useQueryParams();
  const { sortBy, direction, isExplicit } = parseSortParam(query == null ? void 0 : query.sort);
  const foldersPosition = (query == null ? void 0 : query.folders) === "mixed" ? "mixed" : "top";
  const writeSort = (nextSortBy, nextDirection) => {
    if (nextSortBy === null && nextDirection === null) {
      nextSortBy = DEFAULT_SORT_BY;
    }
    const serialized = serializeSort(nextSortBy, nextDirection);
    if (nextSortBy === DEFAULT_SORT_BY && nextDirection === null) {
      setQuery(withEncodedUserParams(query, {
        sort: void 0
      }));
    } else {
      setQuery(withEncodedUserParams(query, {
        sort: serialized
      }));
    }
  };
  const setSortBy = (key) => writeSort(key, null);
  const setDirection = (key) => writeSort(null, key);
  const setFoldersPosition = (position) => {
    if (position === "mixed") {
      setQuery(withEncodedUserParams(query, {
        folders: "mixed"
      }));
    } else {
      setQuery(withEncodedUserParams(query, {
        folders: void 0
      }));
    }
  };
  const assetsSort = serializeSort(sortBy, direction);
  const folderRuleCandidates = [
    sortBy && SORT_BY_RULES[sortBy],
    direction && !direction.startsWith("size") ? SORT_DIRECTION_RULES[direction] : null
  ];
  const folderRules = folderRuleCandidates.filter((rule) => Boolean(rule));
  const foldersSort = isExplicit && folderRules.length > 0 ? folderRules.join(",") : "name:ASC";
  return {
    sortBy,
    direction,
    foldersPosition,
    assetsSort,
    foldersSort,
    setSortBy,
    setDirection,
    setFoldersPosition
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/utils/listQueryKey.mjs
var getListQueryKey = ({ folderId, search, sort, filter }) => JSON.stringify({
  folderId,
  search,
  sort,
  filter
});

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/utils/mergeMixedList.mjs
var getComparableValue = (record, field) => {
  switch (field) {
    case "createdAt":
    case "updatedAt":
      return record[field] ? new Date(record[field]).getTime() : 0;
    case "size":
      return record.size ?? 0;
    case "name":
    default:
      return (record.name ?? "").toLowerCase();
  }
};
var buildComparator = (sort) => {
  const rules = sort.split(",").map((rule) => rule.trim()).filter(Boolean).map((rule) => {
    const [field, direction] = rule.split(":");
    return {
      field,
      desc: (direction == null ? void 0 : direction.toUpperCase()) === "DESC"
    };
  });
  return (a, b) => {
    for (const { field, desc } of rules) {
      const va = getComparableValue(a, field);
      const vb = getComparableValue(b, field);
      let cmp;
      if (typeof va === "string" || typeof vb === "string") {
        cmp = String(va) < String(vb) ? -1 : String(va) > String(vb) ? 1 : 0;
      } else {
        cmp = va - vb;
      }
      if (cmp !== 0) {
        return desc ? -cmp : cmp;
      }
    }
    return 0;
  };
};
var mergeMixedList = ({ folders, assets, sort, hasNextPage }) => {
  const compare = buildComparator(sort);
  const sortedFolders = [
    ...folders
  ].sort(compare);
  const lastAsset = assets[assets.length - 1];
  const placeableFolders = !hasNextPage || !lastAsset ? hasNextPage ? [] : sortedFolders : sortedFolders.filter((folder) => compare(folder, lastAsset) <= 0);
  const items = [];
  let folderIndex = 0;
  for (const asset of assets) {
    while (folderIndex < placeableFolders.length && compare(placeableFolders[folderIndex], asset) <= 0) {
      items.push({
        kind: "folder",
        folder: placeableFolders[folderIndex]
      });
      folderIndex += 1;
    }
    items.push({
      kind: "asset",
      asset
    });
  }
  while (folderIndex < placeableFolders.length) {
    items.push({
      kind: "folder",
      folder: placeableFolders[folderIndex]
    });
    folderIndex += 1;
  }
  return items;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/AssetsPage.mjs
var INTERSECTION_OPTIONS = {
  threshold: 0,
  rootMargin: "0px 0px -1px 0px"
};
var HEADER_SENTINEL_OPTIONS = {
  threshold: 0
};
var ITEM_COUNT_MESSAGE = {
  id: getTranslationKey("header.content.item-count"),
  defaultMessage: "{count, plural, =1 {# item} other {# items}}"
};
var SEARCH_RESULTS_COUNT_MESSAGES = {
  both: {
    id: getTranslationKey("header.search-results.count"),
    defaultMessage: "{numberFolders, plural, one {1 folder} other {# folders}} - {numberAssets, plural, one {1 asset} other {# assets}}"
  },
  folders: {
    id: getTranslationKey("header.search-results.count.folders"),
    defaultMessage: "{numberFolders, plural, one {1 folder} other {# folders}}"
  },
  assets: {
    id: getTranslationKey("header.search-results.count.assets"),
    defaultMessage: "{numberAssets, plural, =0 {0 assets} one {1 asset} other {# assets}}"
  }
};
var getSearchResultsCountMessage = (numberFolders, numberAssets) => {
  if (numberFolders === 0) {
    return SEARCH_RESULTS_COUNT_MESSAGES.assets;
  }
  if (numberAssets === 0) {
    return SEARCH_RESULTS_COUNT_MESSAGES.folders;
  }
  return SEARCH_RESULTS_COUNT_MESSAGES.both;
};
var AssetsView = ({ view, folders, isLoadingFolders, assets, isLoadingAssets, isFetchingMore, hasNextPage, fetchNextPage, error, locations, searchQuery, assetsSort, foldersPosition, hasActiveFilters, onClearFilters, onAssetItemClick, onAddAssets, canAddAssets, onClearSearch }) => {
  const { formatMessage } = useIntl();
  const isGridView = view === viewOptions.GRID;
  const isLoading = isLoadingAssets || isLoadingFolders;
  const mixedItems = (0, import_react23.useMemo)(() => foldersPosition === "mixed" && !isGridView ? mergeMixedList({
    folders,
    assets,
    sort: assetsSort,
    hasNextPage
  }) : null, [
    foldersPosition,
    isGridView,
    folders,
    assets,
    assetsSort,
    hasNextPage
  ]);
  const renderedKeys = buildRenderedKeys({
    folders,
    assets,
    mixedItems
  });
  const loadMoreRef = useInfiniteScrollSentinel({
    hasNextPage,
    isFetchingMore,
    onLoadMore: fetchNextPage,
    options: INTERSECTION_OPTIONS
  });
  if (isLoading) {
    return (0, import_jsx_runtime26.jsx)(C, {
      justifyContent: "center",
      padding: 8,
      children: (0, import_jsx_runtime26.jsx)(kt, {
        children: formatMessage({
          id: "app.loading",
          defaultMessage: "Loading..."
        })
      })
    });
  }
  if (error) {
    return (0, import_jsx_runtime26.jsx)(S, {
      padding: 8,
      children: (0, import_jsx_runtime26.jsx)(A, {
        textColor: "danger600",
        children: formatMessage({
          id: getTranslationKey("list.assets.error"),
          defaultMessage: "An error occurred while fetching assets."
        })
      })
    });
  }
  if (folders.length === 0 && assets.length === 0) {
    return hasActiveFilters && !searchQuery ? (0, import_jsx_runtime26.jsx)(FilteredEmptyState, {
      onClearFilters
    }) : (0, import_jsx_runtime26.jsx)(EmptyState, {
      onAddAssets,
      canAddAssets,
      searchQuery,
      onClearSearch
    });
  }
  return (0, import_jsx_runtime26.jsxs)(import_jsx_runtime26.Fragment, {
    children: [
      isGridView ? (0, import_jsx_runtime26.jsx)(AssetsGrid, {
        folders,
        assets,
        renderedKeys,
        onAssetItemClick
      }) : (0, import_jsx_runtime26.jsx)(AssetsTable, {
        assets,
        folders,
        mixedItems,
        renderedKeys,
        onAssetItemClick
      }),
      (0, import_jsx_runtime26.jsx)("div", {
        ref: loadMoreRef,
        style: {
          height: 1
        }
      }),
      isFetchingMore && (0, import_jsx_runtime26.jsx)(C, {
        justifyContent: "center",
        padding: 4,
        children: (0, import_jsx_runtime26.jsx)(kt, {
          children: formatMessage({
            id: getTranslationKey("list.assets.loading-more"),
            defaultMessage: "Loading more assets..."
          })
        })
      }),
      (0, import_jsx_runtime26.jsx)(BulkActionsBar, {
        assets,
        renderedKeys,
        locations
      })
    ]
  });
};
var ClearSelectionOnChange = ({ listQueryKey }) => {
  const { clear } = useAssetSelection();
  (0, import_react23.useEffect)(() => {
    clear();
  }, [
    listQueryKey,
    clear
  ]);
  return null;
};
var StyledToggleGroup = gt(Root2)`
  display: flex;
  padding: ${({ theme }) => theme.spaces[1]};
  background: ${({ theme }) => theme.colors.neutral100};
  border: 1px solid ${({ theme }) => theme.colors.neutral200};
  border-radius: ${({ theme }) => theme.borderRadius};
`;
var StyledToggleItem = gt(Item2)`
  display: flex;
  flex: 1 1 50%;
  align-items: center;
  justify-content: center;
  gap: ${({ theme }) => theme.spaces[2]};
  padding: 0.6rem ${({ theme }) => theme.spaces[3]};
  border: 1px solid transparent;
  border-radius: ${({ theme }) => theme.borderRadius};
  background: transparent;
  color: ${({ theme }) => theme.colors.neutral600};
  cursor: pointer;
  font-size: ${({ theme }) => theme.fontSizes[1]};
  font-weight: ${({ theme }) => theme.fontWeights.semiBold};
  white-space: nowrap;

  &:hover {
    color: ${({ theme }) => theme.colors.neutral700};
  }

  &[data-state='on'] {
    background: ${({ theme }) => theme.colors.neutral0};
    border-color: ${({ theme }) => theme.colors.neutral200};
    color: ${({ theme }) => theme.colors.primary600};
  }

  svg {
    width: 1.6rem;
    height: 1.6rem;
  }
`;
var StickyHeader = gt(S)`
  position: sticky;
  top: 0;
  z-index: 2;
  /* Transparent at rest (the grey page shows through); an opaque background +
     shadow appear only once it sticks and content scrolls under it. */
  background: transparent;
  /* Horizontal padding matches the list's default spacing (Layouts.Content /
     RESPONSIVE_DEFAULT_SPACING: 4 / 6 / 10) so the header lines up with the rows. */
  padding: ${({ theme }) => `${theme.spaces[6]} ${theme.spaces[4]}`};
  transition:
    padding 0.2s ease,
    background 0.2s ease,
    box-shadow 0.2s ease;

  ${({ theme }) => theme.breakpoints.medium} {
    padding-left: ${({ theme }) => theme.spaces[6]};
    padding-right: ${({ theme }) => theme.spaces[6]};
  }
  ${({ theme }) => theme.breakpoints.large} {
    padding-left: ${({ theme }) => theme.spaces[10]};
    padding-right: ${({ theme }) => theme.spaces[10]};
  }

  /* Compacting is scoped to medium and up, where the header actually sticks. On
     mobile it scrolls away with the list, so shrinking it mid-scroll animated a
     header the user could no longer see — the transition read as a glitch on the
     way back up rather than as the header settling. */
  ${({ $compact, theme }) => $compact && ft`
      ${theme.breakpoints.medium} {
        padding-top: ${theme.spaces[3]};
        padding-bottom: ${theme.spaces[3]};
        padding-left: ${theme.spaces[4]};
        padding-right: ${theme.spaces[4]};
        background: ${theme.colors.neutral0};
        box-shadow: ${theme.shadows.tableShadow};
      }
      ${theme.breakpoints.large} {
        padding-left: ${theme.spaces[6]};
        padding-right: ${theme.spaces[6]};
      }
    `}
`;
var TitleRow = gt(C)`
  justify-content: space-between;
  align-items: flex-start;
  gap: ${({ theme }) => theme.spaces[4]};

  h1 {
    font-size: 1.8rem;
  }
`;
var Toolbar = gt(C)`
  margin-top: ${({ theme }) => theme.spaces[5]};
  flex-direction: column;
  align-items: stretch;
  gap: ${({ theme }) => theme.spaces[3]};
  transition: margin-top 0.2s ease;

  /* Tightening the gap to the title belongs to the compact header, so it is
     scoped to the breakpoints that compact. On mobile the header never sticks,
     and this was the last thing still shifting as the page scrolled. */
  ${({ $compact, theme }) => $compact && ft`
      ${theme.breakpoints.medium} {
        margin-top: ${theme.spaces[2]};
      }
    `}

  ${({ theme }) => theme.breakpoints.large} {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
  }
`;
var ToolbarGroup = gt(C)`
  align-items: center;
  gap: ${({ theme }) => theme.spaces[3]};
`;
var FilterSearchGroup = gt(ToolbarGroup)``;
var SortToggleGroup = gt(ToolbarGroup)`
  justify-content: space-between;

  ${({ theme }) => theme.breakpoints.large} {
    justify-content: flex-end;
    flex: 0 0 auto;
  }
`;
var SearchSlot = gt(S)`
  flex: 1;

  ${({ theme }) => theme.breakpoints.large} {
    flex: 0 1 auto;
  }
`;
var ToggleLabel = gt.span`
  display: none;

  ${({ theme }) => theme.breakpoints.large} {
    display: inline;
  }
`;
var AssetsPage = () => {
  var _a;
  const { formatMessage } = useIntl();
  const { openDetails } = useAssetDetailsParam();
  const { canCreate, canUpdate } = useMediaLibraryPermissions();
  const { currentFolderId, navigateToFolderId, navigateToRoot } = useFolderNavigation();
  const { error: currentFolderError } = useGetFolderQuery({
    id: currentFolderId
  }, {
    skip: currentFolderId === null
  });
  (0, import_react23.useEffect)(() => {
    if ((currentFolderError == null ? void 0 : currentFolderError.name) === "NotFoundError") {
      navigateToRoot();
    }
  }, [
    currentFolderError,
    navigateToRoot
  ]);
  const { title, itemCount } = useFolderInfo(currentFolderId);
  const { searchQuery, isSearching, clearSearch } = useAssetSearch();
  const listSort = useListSort();
  const listFilters = useListFilters();
  const builtFilters = (0, import_react23.useMemo)(
    () => buildAssetFilters(listFilters.filters, /* @__PURE__ */ new Date()),
    // eslint-disable-next-line react-hooks/exhaustive-deps -- serialized is the value identity of filters
    [
      listFilters.serialized
    ]
  );
  const { assets, subscribers: assetPageSubscribers, pagination, isLoading: isLoadingAssets, isFetchingMore, hasNextPage, fetchNextPage, error: assetsError } = useInfiniteAssets({
    // The real folder is passed even while searching: the service drops the folder
    // filter when `search` is set, so results are still global. List filters
    // compose with both modes.
    folder: currentFolderId,
    search: searchQuery || void 0,
    sort: listSort.assetsSort,
    filters: builtFilters.fileClauses,
    enabled: builtFilters.showFiles
  });
  const { data: fetchedFolders = [], isLoading: isLoadingFolders } = useGetFoldersQuery({
    parentId: currentFolderId,
    search: searchQuery || void 0,
    sort: listSort.foldersSort,
    filters: builtFilters.folderClauses
  }, {
    skip: !builtFilters.showFolders
  });
  const folders = (0, import_react23.useMemo)(() => builtFilters.showFolders ? fetchedFolders : [], [
    builtFilters.showFolders,
    fetchedFolders
  ]);
  const itemLocations = (0, import_react23.useMemo)(() => buildItemLocations(assets, folders), [
    assets,
    folders
  ]);
  const itemCountLabel = formatMessage(ITEM_COUNT_MESSAGE, {
    count: itemCount
  });
  const searchResultsTitle = formatMessage({
    id: getTranslationKey("header.search-results"),
    defaultMessage: 'Search results for "{query}"'
  }, {
    query: searchQuery
  });
  const numberFolders = folders.length;
  const numberAssets = (pagination == null ? void 0 : pagination.total) ?? 0;
  const searchResultsCountLabel = formatMessage(getSearchResultsCountMessage(numberFolders, numberAssets), {
    numberFolders,
    numberAssets
  });
  let pageHeaderTitle;
  if (isSearching) {
    pageHeaderTitle = `${searchResultsTitle} (${searchResultsCountLabel})`;
  } else if (title) {
    pageHeaderTitle = `${title} (${itemCountLabel})`;
  } else {
    pageHeaderTitle = formatMessage({
      id: "app.loading",
      defaultMessage: "Loading..."
    });
  }
  const [isCreateFolderDialogOpen, setIsCreateFolderDialogOpen] = (0, import_react23.useState)(false);
  const [view, setView] = usePersistentState(localStorageKeys.view, viewOptions.GRID);
  const isGridView = view === viewOptions.GRID;
  const [isUrlDialogOpen, setIsUrlDialogOpen] = (0, import_react23.useState)(false);
  const fileInputRef = (0, import_react23.useRef)(null);
  const uploadDropZoneRef = (0, import_react23.useRef)(null);
  const [isHeaderCompact, setIsHeaderCompact] = (0, import_react23.useState)(false);
  const onHeaderSentinelVisibility = (0, import_react23.useCallback)((isVisible) => setIsHeaderCompact(!isVisible), []);
  const headerSentinelRef = useElementOnScreen(onHeaderSentinelVisibility, HEADER_SENTINEL_OPTIONS);
  const [uploadFiles] = useUploadFilesMutation();
  const [uploadFromUrls] = useUploadFromUrlsMutation();
  const { data: settings } = useGetUploadSettingsQuery();
  const concurrency = ((_a = settings == null ? void 0 : settings.data) == null ? void 0 : _a.concurrentUploadRequests) ?? 1;
  const isAiMetadataEnabled = useAIMetadataEnabled();
  const { trackUsage } = useTracking2();
  const uploadFilesToFolder = async (files, folderId) => {
    if (files.length === 0) return;
    const assetsCountByType = files.reduce((acc, file) => {
      const type = typeFromMime(file.type);
      acc[type] = (acc[type] ?? 0) + 1;
      return acc;
    }, {});
    trackUsage("willAddMediaLibraryAssets", {
      location: MEDIA_LIBRARY_LOCATION,
      ...assetsCountByType
    });
    const formData = new FormData();
    const fileInfoArray = [];
    files.forEach((file) => {
      formData.append("files", file);
      fileInfoArray.push({
        name: file.name,
        caption: null,
        alternativeText: null,
        folder: folderId
      });
    });
    formData.append("fileInfo", JSON.stringify(fileInfoArray));
    try {
      await uploadFiles({
        formData,
        totalFiles: files.length,
        concurrency,
        generateAiMetadata: Boolean(isAiMetadataEnabled)
      }).unwrap();
    } catch {
    }
  };
  const handleFileSelect = () => {
    var _a2;
    (_a2 = fileInputRef.current) == null ? void 0 : _a2.click();
  };
  const handleFileChange = async (e) => {
    const files = e.target.files;
    if (files && files.length > 0) {
      trackUsage("didSelectFile", {
        source: "computer",
        location: MEDIA_LIBRARY_LOCATION
      });
      await uploadFilesToFolder(Array.from(files), currentFolderId);
    }
    e.target.value = "";
  };
  const handleDrop = async (files) => {
    if (!canCreate) return;
    trackUsage("didSelectFile", {
      source: "computer",
      location: MEDIA_LIBRARY_LOCATION
    });
    await uploadFilesToFolder(files, currentFolderId);
  };
  const handleUrlUpload = async (urls) => {
    trackUsage("didSelectFile", {
      source: "url",
      location: MEDIA_LIBRARY_LOCATION
    });
    trackUsage("willAddMediaLibraryAssets", {
      location: MEDIA_LIBRARY_LOCATION
    });
    try {
      await uploadFromUrls({
        urls,
        folderId: currentFolderId,
        generateAiMetadata: Boolean(isAiMetadataEnabled)
      }).unwrap();
    } catch {
    }
  };
  const listQueryKey = getListQueryKey({
    folderId: currentFolderId,
    search: searchQuery,
    // Folder position changes the render order too — selection must reset.
    sort: `${listSort.assetsSort};folders=${listSort.foldersPosition}`,
    filter: listFilters.serialized || null
  });
  return (0, import_jsx_runtime26.jsxs)(import_jsx_runtime26.Fragment, {
    children: [
      (0, import_jsx_runtime26.jsx)(UploadDropZoneProvider, {
        onDrop: handleDrop,
        disabled: !canCreate,
        children: (0, import_jsx_runtime26.jsx)(AssetSelectionProvider, {
          disabled: !canUpdate,
          children: (0, import_jsx_runtime26.jsx)(BusyAssetsProvider, {
            children: (0, import_jsx_runtime26.jsxs)(AssetsDndProvider, {
              locations: itemLocations,
              children: [
                (0, import_jsx_runtime26.jsx)(ClearSelectionOnChange, {
                  listQueryKey
                }),
                (0, import_jsx_runtime26.jsx)(Layouts.Root, {
                  sideNav: (0, import_jsx_runtime26.jsx)(FolderTree, {
                    currentFolderId,
                    showActiveFolder: !isSearching,
                    onSelectFolder: navigateToFolderId
                  }),
                  children: (0, import_jsx_runtime26.jsx)(Page.Main, {
                    children: (0, import_jsx_runtime26.jsxs)(S, {
                      ref: uploadDropZoneRef,
                      children: [
                        (0, import_jsx_runtime26.jsx)($e, {
                          children: (0, import_jsx_runtime26.jsx)("input", {
                            type: "file",
                            ref: fileInputRef,
                            onChange: handleFileChange,
                            multiple: true
                          })
                        }),
                        (0, import_jsx_runtime26.jsx)(S, {
                          ref: headerSentinelRef,
                          height: 0,
                          "aria-hidden": true
                        }),
                        (0, import_jsx_runtime26.jsxs)(StickyHeader, {
                          $compact: isHeaderCompact,
                          children: [
                            (0, import_jsx_runtime26.jsxs)(TitleRow, {
                              children: [
                                (0, import_jsx_runtime26.jsx)(A, {
                                  variant: "alpha",
                                  tag: "h1",
                                  children: pageHeaderTitle
                                }),
                                canCreate && (0, import_jsx_runtime26.jsxs)(ti, {
                                  popoverPlacement: "bottom-end",
                                  variant: "default",
                                  endIcon: (0, import_jsx_runtime26.jsx)(o5, {}),
                                  label: formatMessage({
                                    id: getTranslationKey("new"),
                                    defaultMessage: "New"
                                  }),
                                  children: [
                                    (0, import_jsx_runtime26.jsx)(pd, {
                                      onSelect: () => setIsCreateFolderDialogOpen(true),
                                      startIcon: (0, import_jsx_runtime26.jsx)(_5, {}),
                                      children: formatMessage({
                                        id: getTranslationKey("folder.create.title"),
                                        defaultMessage: "New folder"
                                      })
                                    }),
                                    (0, import_jsx_runtime26.jsx)(pd, {
                                      onSelect: handleFileSelect,
                                      startIcon: (0, import_jsx_runtime26.jsx)(J5, {}),
                                      children: formatMessage({
                                        id: getTranslationKey("import-files"),
                                        defaultMessage: "Import files"
                                      })
                                    }),
                                    (0, import_jsx_runtime26.jsx)(pd, {
                                      onSelect: () => setIsUrlDialogOpen(true),
                                      startIcon: (0, import_jsx_runtime26.jsx)(R3, {}),
                                      children: formatMessage({
                                        id: getTranslationKey("import-from-url"),
                                        defaultMessage: "Import from URL"
                                      })
                                    })
                                  ]
                                })
                              ]
                            }),
                            (0, import_jsx_runtime26.jsxs)(Toolbar, {
                              $compact: isHeaderCompact,
                              children: [
                                (0, import_jsx_runtime26.jsxs)(FilterSearchGroup, {
                                  children: [
                                    (0, import_jsx_runtime26.jsx)(S, {
                                      children: (0, import_jsx_runtime26.jsx)(FilterMenu, {
                                        listFilters
                                      })
                                    }),
                                    (0, import_jsx_runtime26.jsx)(SearchSlot, {
                                      children: (0, import_jsx_runtime26.jsx)(AssetsSearchInput, {})
                                    })
                                  ]
                                }),
                                (0, import_jsx_runtime26.jsxs)(SortToggleGroup, {
                                  children: [
                                    (0, import_jsx_runtime26.jsx)(S, {
                                      children: (0, import_jsx_runtime26.jsx)(SortMenu, {
                                        sort: listSort,
                                        showFoldersGroup: !isGridView
                                      })
                                    }),
                                    (0, import_jsx_runtime26.jsxs)(StyledToggleGroup, {
                                      type: "single",
                                      value: isGridView ? "grid" : "table",
                                      onValueChange: (value) => value && setView(value === "grid" ? viewOptions.GRID : viewOptions.TABLE),
                                      "aria-label": formatMessage({
                                        id: getTranslationKey("view.switch.label"),
                                        defaultMessage: "View options"
                                      }),
                                      children: [
                                        (0, import_jsx_runtime26.jsxs)(StyledToggleItem, {
                                          value: "table",
                                          "aria-label": formatMessage({
                                            id: getTranslationKey("view.table"),
                                            defaultMessage: "Table view"
                                          }),
                                          children: [
                                            (0, import_jsx_runtime26.jsx)(B3, {}),
                                            (0, import_jsx_runtime26.jsx)(ToggleLabel, {
                                              children: formatMessage({
                                                id: getTranslationKey("view.table"),
                                                defaultMessage: "Table view"
                                              })
                                            })
                                          ]
                                        }),
                                        (0, import_jsx_runtime26.jsxs)(StyledToggleItem, {
                                          value: "grid",
                                          "aria-label": formatMessage({
                                            id: getTranslationKey("view.grid"),
                                            defaultMessage: "Grid view"
                                          }),
                                          children: [
                                            (0, import_jsx_runtime26.jsx)(t3, {}),
                                            (0, import_jsx_runtime26.jsx)(ToggleLabel, {
                                              children: formatMessage({
                                                id: getTranslationKey("view.grid"),
                                                defaultMessage: "Grid view"
                                              })
                                            })
                                          ]
                                        })
                                      ]
                                    })
                                  ]
                                })
                              ]
                            }),
                            (0, import_jsx_runtime26.jsx)(FilterBadges, {
                              listFilters,
                              compact: isHeaderCompact
                            })
                          ]
                        }),
                        (0, import_jsx_runtime26.jsxs)(Layouts.Content, {
                          children: [
                            (0, import_jsx_runtime26.jsx)(BetaNotice, {}),
                            assetPageSubscribers,
                            (0, import_jsx_runtime26.jsxs)(DropZoneWithOverlay, {
                              children: [
                                (0, import_jsx_runtime26.jsx)(DropFilesMessage, {
                                  uploadDropZoneRef,
                                  folderName: title
                                }),
                                (0, import_jsx_runtime26.jsx)(AssetsView, {
                                  view,
                                  folders,
                                  isLoadingFolders,
                                  assets,
                                  isLoadingAssets,
                                  isFetchingMore,
                                  hasNextPage,
                                  fetchNextPage,
                                  error: assetsError,
                                  locations: itemLocations,
                                  searchQuery,
                                  assetsSort: listSort.assetsSort,
                                  foldersPosition: listSort.foldersPosition,
                                  hasActiveFilters: listFilters.filters.length > 0,
                                  onClearFilters: listFilters.clearFilters,
                                  onAssetItemClick: openDetails,
                                  onAddAssets: handleFileSelect,
                                  canAddAssets: canCreate,
                                  onClearSearch: clearSearch
                                })
                              ]
                            })
                          ]
                        })
                      ]
                    })
                  })
                })
              ]
            })
          })
        })
      }),
      (0, import_jsx_runtime26.jsx)(FolderFormDialog, {
        open: isCreateFolderDialogOpen,
        mode: "create",
        parentFolderName: title,
        parentFolderId: currentFolderId,
        onClose: () => setIsCreateFolderDialogOpen(false)
      }),
      (0, import_jsx_runtime26.jsx)(ImportFromUrlDialog, {
        open: isUrlDialogOpen,
        onClose: () => setIsUrlDialogOpen(false),
        onUpload: handleUrlUpload
      }),
      (0, import_jsx_runtime26.jsx)(AssetDetailsDrawer, {})
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/App.mjs
var BetaMediaLibrary = () => {
  const { formatMessage } = useIntl();
  const title = formatMessage({
    id: getTranslationKey("plugin.name"),
    defaultMessage: "Media Library"
  });
  return (0, import_jsx_runtime27.jsxs)(import_jsx_runtime27.Fragment, {
    children: [
      (0, import_jsx_runtime27.jsx)(Page.Title, {
        children: title
      }),
      (0, import_jsx_runtime27.jsx)(Routes, {
        children: (0, import_jsx_runtime27.jsx)(Route, {
          index: true,
          element: (0, import_jsx_runtime27.jsx)(AssetsPage, {})
        })
      })
    ]
  });
};
export {
  BetaMediaLibrary
};
//# sourceMappingURL=App-CKNW7HGM.js.map
