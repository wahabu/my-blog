import { resolveFileMime } from './resolveFileMime.mjs';
import { typeFromMime } from './typeFromMime.mjs';

const rawFileToAsset = async (rawFile, assetSource)=>{
    const mime = await resolveFileMime(rawFile.type, rawFile);
    return {
        size: rawFile.size / 1000,
        createdAt: new Date(rawFile.lastModified).toISOString(),
        name: rawFile.name,
        source: assetSource,
        type: typeFromMime(mime),
        url: URL.createObjectURL(rawFile),
        ext: rawFile.name.split('.').pop(),
        mime,
        rawFile,
        isLocal: true
    };
};

export { rawFileToAsset };
//# sourceMappingURL=rawFileToAsset.mjs.map
