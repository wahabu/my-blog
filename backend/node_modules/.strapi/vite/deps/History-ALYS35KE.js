import {
  useTypedSelector
} from "./chunk-U3VF4G3M.js";
import {
  ActionsDrawer,
  DocumentRBAC,
  DynamicZone,
  MemoizedBlocksInput,
  MemoizedComponentInput,
  MemoizedUIDInput,
  MemoizedWysiwyg,
  NotAllowedInput,
  PERMISSIONS,
  getConditionDependencyPaths,
  getConditionDependencySubscriptionValue,
  getDisplayName,
  getRelationLabel,
  useActionsDrawer,
  useDocumentRBAC,
  useDynamicZone,
  useFieldHint,
  useLazyComponents
} from "./chunk-O3ADBBC4.js";
import "./chunk-OIM6YED5.js";
import {
  DocumentStatus,
  RelativeTime
} from "./chunk-BCGA3ATX.js";
import "./chunk-AT2I3J2U.js";
import "./chunk-F4A3MJG3.js";
import {
  COLLECTION_TYPES,
  buildValidParams,
  prepareTempKeys,
  removeFieldsThatDontExistOnSchema,
  useDoc,
  useDocLayout,
  useDocument,
  useDocumentLayout,
  useGetContentTypeConfigurationQuery
} from "./chunk-JPBDOX5J.js";
import {
  contentManagerApi
} from "./chunk-CB2UCVBY.js";
import "./chunk-BVJQKRXI.js";
import {
  createRulesEngine
} from "./chunk-SG3YXALT.js";
import "./chunk-LVMHP6GO.js";
import "./chunk-7FPOP2S5.js";
import "./chunk-Q535U6BT.js";
import "./chunk-D7FI3GAF.js";
import "./chunk-4R2QA5NM.js";
import "./chunk-TKXHHSNW.js";
import "./chunk-XTTXQ7FR.js";
import "./chunk-QHCTYJ6K.js";
import "./chunk-TKZBDVP5.js";
import "./chunk-LS2USQHL.js";
import "./chunk-K4JPZJYK.js";
import "./chunk-PA2ESAGW.js";
import "./chunk-SLUE2XUX.js";
import "./chunk-HRURINY4.js";
import "./chunk-JES3NNDO.js";
import "./chunk-F3Y2U5J7.js";
import {
  ConfirmDialog
} from "./chunk-PUECFR7A.js";
import "./chunk-WVMUAD6S.js";
import "./chunk-BTNRU3I6.js";
import "./chunk-YZKIGY5W.js";
import "./chunk-XNMTWOF7.js";
import {
  GradientBadgeWithIcon
} from "./chunk-NCYXOGJJ.js";
import "./chunk-IFOFBKTA.js";
import "./chunk-AOU4ZZD3.js";
import {
  MemoizedInputRenderer
} from "./chunk-SOVNMIKL.js";
import {
  Form,
  useField,
  useForm
} from "./chunk-2IWTCEYS.js";
import {
  useRBAC
} from "./chunk-SHEU3VT4.js";
import {
  Layouts
} from "./chunk-767BZMIR.js";
import {
  useIsMobile
} from "./chunk-A274YVTO.js";
import "./chunk-L4VZK4CF.js";
import {
  HEIGHT_TOP_NAVIGATION,
  useTracking
} from "./chunk-QXVVXECA.js";
import "./chunk-6KQUGTAR.js";
import {
  Page
} from "./chunk-WZNNPD7U.js";
import {
  useQueryParams,
  useStrapiApp
} from "./chunk-HHY6TNZ5.js";
import {
  useNotification
} from "./chunk-52FV5OAT.js";
import "./chunk-NE2H5B4K.js";
import "./chunk-ALJOKJHY.js";
import {
  createContext
} from "./chunk-ZISMY363.js";
import "./chunk-FH7EDXAD.js";
import "./chunk-LCUEZJJN.js";
import "./chunk-2A7DKUQA.js";
import "./chunk-TVYQEFKB.js";
import "./chunk-T5ATI4SA.js";
import "./chunk-MSCD7MZY.js";
import "./chunk-MBK4V2X7.js";
import "./chunk-I4RARRLM.js";
import "./chunk-BC4MOUEJ.js";
import "./chunk-JRCQYP7F.js";
import "./chunk-36WB3I5J.js";
import "./chunk-45QSVGD5.js";
import "./chunk-KS6HIU44.js";
import "./chunk-D4WYVNVM.js";
import "./chunk-MMOBCIZG.js";
import "./chunk-OG6FDUVQ.js";
import "./chunk-A6QRMII5.js";
import "./chunk-3DRZIGZ7.js";
import "./chunk-AJ6CDECK.js";
import "./chunk-YULRRBBU.js";
import "./chunk-KVWVHPCR.js";
import "./chunk-Y5S667H6.js";
import "./chunk-OJ6RZTLV.js";
import {
  require_lib,
  require_pipe
} from "./chunk-NZZAX2AB.js";
import "./chunk-T7T43G5B.js";
import "./chunk-NR6XEPE4.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import "./chunk-HAUWLGB7.js";
import "./chunk-VQOOZHFO.js";
import "./chunk-3MST3H25.js";
import {
  Link,
  NavLink,
  Navigate,
  useNavigate,
  useParams
} from "./chunk-QBM2MGBP.js";
import "./chunk-AHJOALQ2.js";
import "./chunk-U5ASKYVL.js";
import "./chunk-QQEUXDAQ.js";
import "./chunk-LHXIEHKN.js";
import {
  A,
  C,
  Ct,
  Dd,
  Ld,
  Mt,
  Oa,
  Pd,
  S,
  Vd,
  ud,
  un,
  yn,
  yu
} from "./chunk-7ZODKSNG.js";
import "./chunk-SXAHFOGA.js";
import "./chunk-6I7FUUCQ.js";
import "./chunk-JDJCDU5Q.js";
import {
  D2,
  Jn
} from "./chunk-D4B4CG5U.js";
import "./chunk-VQHBTUHH.js";
import "./chunk-PC7RLLVI.js";
import "./chunk-AHKO7ZVV.js";
import "./chunk-JCO3CRJF.js";
import "./chunk-HHU3PYNH.js";
import "./chunk-OYIDWRUX.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  gt
} from "./chunk-4N2AQQ2K.js";
import "./chunk-4MZT7456.js";
import "./chunk-QBO3FZHQ.js";
import "./chunk-KKBG5U5K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/content-manager/dist/admin/history/pages/History.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var React4 = __toESM(require_react(), 1);
var import_qs3 = __toESM(require_lib(), 1);

// node_modules/@strapi/content-manager/dist/admin/history/components/VersionContent.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var React2 = __toESM(require_react(), 1);
var import_pipe = __toESM(require_pipe(), 1);

// node_modules/@strapi/content-manager/dist/admin/history/components/VersionInputRenderer.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var StyledAlert = gt(ud).attrs({
  closeLabel: "Close",
  onClose: () => {
  },
  shadow: "none"
})`
  button {
    display: none;
  }
`;
var LinkEllipsis = gt(Oa)`
  display: block;

  & > span {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    display: block;
  }
`;
var CustomRelationInput = (props) => {
  var _a;
  const { formatMessage } = useIntl();
  const emptyLabel = formatMessage({
    id: "content-manager.containers.empty-label",
    defaultMessage: "Untitled"
  });
  const field = useField(props.name);
  let formattedFieldValue;
  if (field) {
    formattedFieldValue = Array.isArray(field.value) ? {
      results: field.value,
      meta: {
        missingCount: 0
      }
    } : field.value;
  }
  const hasResultsShape = !!formattedFieldValue && typeof formattedFieldValue === "object" && Array.isArray(formattedFieldValue.results);
  const missingCount = hasResultsShape && typeof ((_a = formattedFieldValue.meta) == null ? void 0 : _a.missingCount) === "number" ? formattedFieldValue.meta.missingCount : 0;
  if (!hasResultsShape || formattedFieldValue.results.length === 0 && missingCount === 0) {
    return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, {
      children: [
        (0, import_jsx_runtime.jsx)(Dd.Label, {
          action: props.labelAction,
          children: props.label
        }),
        (0, import_jsx_runtime.jsx)(S, {
          marginTop: 1,
          children: (0, import_jsx_runtime.jsx)(StyledAlert, {
            variant: "default",
            children: formatMessage({
              id: "content-manager.history.content.no-relations",
              defaultMessage: "No relations."
            })
          })
        })
      ]
    });
  }
  const { results, meta } = formattedFieldValue;
  return (0, import_jsx_runtime.jsxs)(S, {
    children: [
      (0, import_jsx_runtime.jsx)(Dd.Label, {
        children: props.label
      }),
      results.length > 0 && (0, import_jsx_runtime.jsx)(C, {
        direction: "column",
        gap: 2,
        marginTop: 1,
        alignItems: "stretch",
        children: results.map((relationData) => {
          const { targetModel } = props.attribute;
          const href = `../${COLLECTION_TYPES}/${targetModel}/${relationData.documentId}`;
          const label = getRelationLabel(relationData, props.mainField, emptyLabel);
          const isAdminUserRelation = targetModel === "admin::user";
          return (0, import_jsx_runtime.jsxs)(C, {
            paddingTop: 2,
            paddingBottom: 2,
            paddingLeft: 4,
            paddingRight: 4,
            hasRadius: true,
            borderColor: "neutral200",
            background: "neutral150",
            justifyContent: "space-between",
            children: [
              (0, import_jsx_runtime.jsx)(S, {
                minWidth: 0,
                paddingTop: 1,
                paddingBottom: 1,
                paddingRight: 4,
                children: (0, import_jsx_runtime.jsx)(Mt, {
                  label,
                  children: isAdminUserRelation ? (0, import_jsx_runtime.jsx)(A, {
                    children: label
                  }) : (0, import_jsx_runtime.jsx)(LinkEllipsis, {
                    tag: NavLink,
                    to: href,
                    children: label
                  })
                })
              }),
              (0, import_jsx_runtime.jsx)(DocumentStatus, {
                status: relationData.status
              })
            ]
          }, relationData.documentId ?? relationData.id);
        })
      }),
      meta.missingCount > 0 && (0, import_jsx_runtime.jsx)(StyledAlert, {
        marginTop: 1,
        variant: "warning",
        title: formatMessage({
          id: "content-manager.history.content.missing-relations.title",
          defaultMessage: "{number, plural, =1 {Missing relation} other {{number} missing relations}}"
        }, {
          number: meta.missingCount
        }),
        children: formatMessage({
          id: "content-manager.history.content.missing-relations.message",
          defaultMessage: "{number, plural, =1 {It has} other {They have}} been deleted and can't be restored."
        }, {
          number: meta.missingCount
        })
      })
    ]
  });
};
var createInitialValuesForPath = (keyPath, value) => {
  const keys = keyPath.split(".");
  const root = {};
  let node = root;
  keys.forEach((key, index) => {
    if (key === "__proto__" || key === "constructor") return;
    if (index === keys.length - 1) {
      node[key] = value;
    } else {
      const nextNode = typeof node[key] === "object" && node[key] !== null && Array.isArray(node[key]) === false ? node[key] : {};
      node[key] = nextNode;
      node = nextNode;
    }
  });
  return root;
};
var CustomMediaInput = (props) => {
  const { value } = useField(props.name);
  const results = (value == null ? void 0 : value.results) ?? [];
  const meta = (value == null ? void 0 : value.meta) ?? {
    missingCount: 0
  };
  const { formatMessage } = useIntl();
  const fields = useStrapiApp("CustomMediaInput", (state) => state.fields);
  const MediaLibrary = fields.media;
  return (0, import_jsx_runtime.jsxs)(C, {
    direction: "column",
    gap: 2,
    alignItems: "stretch",
    children: [
      (0, import_jsx_runtime.jsx)(Form, {
        method: "PUT",
        disabled: true,
        initialValues: createInitialValuesForPath(props.name, results),
        children: (0, import_jsx_runtime.jsx)(MediaLibrary, {
          ...props,
          disabled: true,
          multiple: results.length > 1
        })
      }),
      meta.missingCount > 0 && (0, import_jsx_runtime.jsx)(StyledAlert, {
        variant: "warning",
        closeLabel: "Close",
        onClose: () => {
        },
        title: formatMessage({
          id: "content-manager.history.content.missing-assets.title",
          defaultMessage: "{number, plural, =1 {Missing asset} other {{number} missing assets}}"
        }, {
          number: meta.missingCount
        }),
        children: formatMessage({
          id: "content-manager.history.content.missing-assets.message",
          defaultMessage: "{number, plural, =1 {It has} other {They have}} been deleted in the Media Library and can't be restored."
        }, {
          number: meta.missingCount
        })
      })
    ]
  });
};
var getLabelAction = (labelAction) => {
  if (!React.isValidElement(labelAction)) {
    return labelAction;
  }
  const labelActionTitleId = labelAction.props.title.id;
  if (labelActionTitleId === "i18n.Field.localized") {
    return React.cloneElement(labelAction, {
      ...labelAction.props,
      title: {
        id: "history.content.localized",
        defaultMessage: "This value is specific to this locale. If you restore this version, the content will not be replaced for other locales."
      }
    });
  }
  if (labelActionTitleId === "i18n.Field.not-localized") {
    return React.cloneElement(labelAction, {
      ...labelAction.props,
      title: {
        id: "history.content.not-localized",
        defaultMessage: "This value is common to all locales. If you restore this version and save the changes, the content will be replaced for all locales."
      }
    });
  }
  return labelAction;
};
var resolveComponentRenderResources = (componentUID, componentsLayout, configurationComponents, components) => {
  const componentLayout = componentsLayout[componentUID];
  const componentConfiguration = configurationComponents[componentUID];
  const componentSchema = components[componentUID];
  if (!componentLayout || !componentConfiguration || !componentSchema) {
    return null;
  }
  return {
    layout: componentLayout.layout,
    metadatas: componentConfiguration.metadatas,
    schemaAttributes: componentSchema.attributes
  };
};
var VersionInputRenderer = ({ visible, hint: providedHint, shouldIgnoreRBAC = false, labelAction, ...props }) => {
  const customLabelAction = getLabelAction(labelAction);
  const { formatMessage } = useIntl();
  const version = useHistoryContext("VersionContent", (state) => state.selectedVersion);
  const configuration = useHistoryContext("VersionContent", (state) => state.configuration);
  const fieldSizes = useTypedSelector((state) => state["content-manager"].app.fieldSizes);
  const { id, components } = useDoc();
  const isFormDisabled = useForm("InputRenderer", (state) => state.disabled);
  const isInDynamicZone = useDynamicZone("isInDynamicZone", (state) => state.isInDynamicZone);
  const canCreateFields = useDocumentRBAC("InputRenderer", (rbac) => rbac.canCreateFields);
  const canReadFields = useDocumentRBAC("InputRenderer", (rbac) => rbac.canReadFields);
  const canUpdateFields = useDocumentRBAC("InputRenderer", (rbac) => rbac.canUpdateFields);
  const canUserAction = useDocumentRBAC("InputRenderer", (rbac) => rbac.canUserAction);
  const editableFields = id ? canUpdateFields : canCreateFields;
  const readableFields = id ? canReadFields : canCreateFields;
  const canUserReadField = canUserAction(props.name, readableFields, props.type);
  const canUserEditField = canUserAction(props.name, editableFields, props.type);
  const fields = useStrapiApp("InputRenderer", (app) => app.fields);
  const { lazyComponentStore } = useLazyComponents(attributeHasCustomFieldProperty(props.attribute) ? [
    props.attribute.customField
  ] : void 0);
  const hint = useFieldHint(providedHint, props.attribute);
  const { edit: { components: componentsLayout } } = useDocLayout();
  if (!visible) {
    return null;
  }
  if (!shouldIgnoreRBAC && !canUserReadField && !isInDynamicZone) {
    return (0, import_jsx_runtime.jsx)(NotAllowedInput, {
      hint,
      ...props
    });
  }
  const fieldIsDisabled = !canUserEditField && !isInDynamicZone || props.disabled || isFormDisabled;
  const addedAttributes = version.meta.unknownAttributes.added;
  if (Object.keys(addedAttributes).includes(props.name)) {
    return (0, import_jsx_runtime.jsxs)(C, {
      direction: "column",
      alignItems: "flex-start",
      gap: 1,
      children: [
        (0, import_jsx_runtime.jsx)(Dd.Label, {
          children: props.label
        }),
        (0, import_jsx_runtime.jsx)(StyledAlert, {
          width: "100%",
          closeLabel: "Close",
          onClose: () => {
          },
          variant: "warning",
          title: formatMessage({
            id: "content-manager.history.content.new-field.title",
            defaultMessage: "New field"
          }),
          children: formatMessage({
            id: "content-manager.history.content.new-field.message",
            defaultMessage: "This field didn't exist when this version was saved. If you restore this version, it will be empty."
          })
        })
      ]
    });
  }
  if (attributeHasCustomFieldProperty(props.attribute)) {
    const CustomInput = lazyComponentStore[props.attribute.customField];
    if (CustomInput) {
      return (0, import_jsx_runtime.jsx)(CustomInput, {
        ...props,
        // @ts-expect-error – TODO: fix this type error in the useLazyComponents hook.
        hint,
        labelAction: customLabelAction,
        disabled: fieldIsDisabled
      });
    }
    return (0, import_jsx_runtime.jsx)(MemoizedInputRenderer, {
      ...props,
      hint,
      labelAction: customLabelAction,
      // @ts-expect-error – this workaround lets us display that the custom field is missing.
      type: props.attribute.customField,
      disabled: fieldIsDisabled
    });
  }
  if (props.type === "media") {
    return (0, import_jsx_runtime.jsx)(CustomMediaInput, {
      ...props,
      labelAction: customLabelAction,
      disabled: fieldIsDisabled
    });
  }
  const addedInputTypes = Object.keys(fields);
  if (!attributeHasCustomFieldProperty(props.attribute) && addedInputTypes.includes(props.type)) {
    const CustomInput = fields[props.type];
    return (0, import_jsx_runtime.jsx)(CustomInput, {
      ...props,
      // @ts-expect-error – TODO: fix this type error in the useLibrary hook.
      hint,
      labelAction: customLabelAction,
      disabled: fieldIsDisabled
    });
  }
  switch (props.type) {
    case "blocks":
      return (0, import_jsx_runtime.jsx)(MemoizedBlocksInput, {
        ...props,
        hint,
        type: props.type,
        disabled: fieldIsDisabled
      });
    case "component": {
      const renderResources = resolveComponentRenderResources(props.attribute.component, componentsLayout, configuration.components, components);
      if (!renderResources) {
        return (0, import_jsx_runtime.jsx)(Dd.Label, {
          children: props.label
        });
      }
      const { layout, metadatas, schemaAttributes } = renderResources;
      const [remainingFieldsLayout] = getRemaingFieldsLayout({
        layout: [
          layout
        ],
        metadatas,
        fieldSizes,
        schemaAttributes
      });
      return (0, import_jsx_runtime.jsx)(MemoizedComponentInput, {
        ...props,
        layout: [
          ...layout,
          ...remainingFieldsLayout || []
        ],
        hint,
        labelAction: customLabelAction,
        disabled: fieldIsDisabled,
        children: (inputProps) => (0, import_jsx_runtime.jsx)(VersionInputRenderer, {
          ...inputProps,
          shouldIgnoreRBAC: true
        })
      });
    }
    case "dynamiczone":
      return (0, import_jsx_runtime.jsx)(DynamicZone, {
        ...props,
        hint,
        labelAction: customLabelAction,
        disabled: fieldIsDisabled,
        children: (inputProps) => (0, import_jsx_runtime.jsx)(VersionInputRenderer, {
          ...inputProps,
          shouldIgnoreRBAC: true
        })
      });
    case "relation":
      return (0, import_jsx_runtime.jsx)(CustomRelationInput, {
        ...props,
        hint,
        labelAction: customLabelAction,
        disabled: fieldIsDisabled
      });
    case "richtext":
      return (0, import_jsx_runtime.jsx)(MemoizedWysiwyg, {
        ...props,
        hint,
        type: props.type,
        labelAction: customLabelAction,
        disabled: fieldIsDisabled
      });
    case "uid":
      return (0, import_jsx_runtime.jsx)(MemoizedUIDInput, {
        ...props,
        hint,
        type: props.type,
        labelAction: customLabelAction,
        disabled: fieldIsDisabled
      });
    case "enumeration":
      return (0, import_jsx_runtime.jsx)(MemoizedInputRenderer, {
        ...props,
        hint,
        labelAction: customLabelAction,
        options: props.attribute.enum.map((value) => ({
          value
        })),
        // @ts-expect-error – Temp workaround so we don't forget custom-fields don't work!
        type: props.customField ? "custom-field" : props.type,
        disabled: fieldIsDisabled
      });
    default:
      const { unique: _unique, mainField: _mainField, ...restProps } = props;
      return (0, import_jsx_runtime.jsx)(MemoizedInputRenderer, {
        ...restProps,
        hint,
        labelAction: customLabelAction,
        // @ts-expect-error – Temp workaround so we don't forget custom-fields don't work!
        type: props.customField ? "custom-field" : props.type,
        disabled: fieldIsDisabled
      });
  }
};
var attributeHasCustomFieldProperty = (attribute) => "customField" in attribute && typeof attribute.customField === "string";

// node_modules/@strapi/content-manager/dist/admin/history/components/VersionContent.mjs
var createLayoutFromFields = (fields) => {
  return fields.reduce((rows, field) => {
    if (field.type === "dynamiczone") {
      rows.push([
        field
      ]);
      return rows;
    }
    if (!rows[rows.length - 1]) {
      rows.push([]);
    }
    rows[rows.length - 1].push(field);
    return rows;
  }, []).map((row) => [
    row
  ]);
};
function getRemaingFieldsLayout({ layout, metadatas, schemaAttributes, fieldSizes }) {
  const fieldsInLayout = layout.flatMap((panel) => panel.flatMap((row) => row.flatMap((field) => field.name)));
  const remainingFields = Object.entries(metadatas).reduce((currentRemainingFields, [name, field]) => {
    if (!fieldsInLayout.includes(name) && field.edit.visible === true) {
      const attribute = schemaAttributes[name];
      currentRemainingFields.push({
        attribute,
        type: attribute.type,
        visible: true,
        disabled: true,
        label: field.edit.label || name,
        name,
        size: fieldSizes[attribute.type].default ?? 12
      });
    }
    return currentRemainingFields;
  }, []);
  return createLayoutFromFields(remainingFields);
}
var rulesEngine = createRulesEngine();
var getPanelConditionDependencyPaths = (panel) => {
  var _a, _b;
  const dependencies = /* @__PURE__ */ new Set();
  for (const row of panel) {
    for (const field of row) {
      const condition = (_b = (_a = field.attribute) == null ? void 0 : _a.conditions) == null ? void 0 : _b.visible;
      if (!condition) {
        continue;
      }
      const paths = getConditionDependencyPaths(condition);
      if (paths === null) {
        return null;
      }
      for (const path of paths) {
        dependencies.add(path);
      }
    }
  }
  return [
    ...dependencies
  ].sort();
};
var FormPanel = ({ panel }) => {
  const isMobile = useIsMobile();
  const conditionDependencyPaths = React2.useMemo(() => getPanelConditionDependencyPaths(panel), [
    panel
  ]);
  const getValues = useForm("FormPanel", (state) => state.getValues);
  const conditionSubscriptionValue = useForm("FormPanel", (state) => {
    return getConditionDependencySubscriptionValue(state.values, conditionDependencyPaths);
  });
  const fieldValues = conditionDependencyPaths === null ? conditionSubscriptionValue : getValues();
  const isFieldVisible = React2.useCallback((field) => {
    var _a, _b;
    const condition = (_b = (_a = field.attribute) == null ? void 0 : _a.conditions) == null ? void 0 : _b.visible;
    if (!condition) {
      return true;
    }
    return rulesEngine.evaluate(condition, fieldValues);
  }, [
    fieldValues
  ]);
  if (panel.some((row) => row.some((field) => field.type === "dynamiczone"))) {
    const [row] = panel;
    const [field] = row;
    if (!isFieldVisible(field)) {
      return null;
    }
    return (0, import_jsx_runtime2.jsx)(Ld.Root, {
      gap: 4,
      children: (0, import_jsx_runtime2.jsx)(Ld.Item, {
        xs: 12,
        direction: "column",
        alignItems: "stretch",
        children: (0, import_jsx_runtime2.jsx)(VersionInputRenderer, {
          ...field
        })
      })
    }, field.name);
  }
  return (0, import_jsx_runtime2.jsx)(S, {
    hasRadius: !isMobile,
    background: {
      initial: "transparent",
      medium: "neutral0"
    },
    shadow: {
      initial: "none",
      medium: "tableShadow"
    },
    padding: {
      initial: 0,
      medium: 6
    },
    borderColor: {
      initial: "transparent",
      medium: "neutral150"
    },
    children: (0, import_jsx_runtime2.jsx)(C, {
      direction: "column",
      alignItems: "stretch",
      gap: 6,
      children: panel.map((row, gridRowIndex) => {
        const visibleFields = row.filter(isFieldVisible);
        if (visibleFields.length === 0) {
          return null;
        }
        return (0, import_jsx_runtime2.jsx)(Ld.Root, {
          gap: {
            initial: 6,
            medium: 4
          },
          children: visibleFields.map(({ size, ...field }) => {
            return (0, import_jsx_runtime2.jsx)(Ld.Item, {
              col: size,
              xs: 12,
              direction: "column",
              alignItems: "stretch",
              children: (0, import_jsx_runtime2.jsx)(VersionInputRenderer, {
                ...field
              })
            }, field.name);
          })
        }, gridRowIndex);
      })
    })
  });
};
var VersionContent = () => {
  const { formatMessage } = useIntl();
  const { fieldSizes } = useTypedSelector((state) => state["content-manager"].app);
  const version = useHistoryContext("VersionContent", (state) => state.selectedVersion);
  const layout = useHistoryContext("VersionContent", (state) => state.layout);
  const configuration = useHistoryContext("VersionContent", (state) => state.configuration);
  const schema = useHistoryContext("VersionContent", (state) => state.schema);
  const removedAttributes = version.meta.unknownAttributes.removed;
  const removedAttributesAsFields = Object.entries(removedAttributes).map(([attributeName, attribute]) => {
    const field = {
      attribute,
      shouldIgnoreRBAC: true,
      type: attribute.type,
      visible: true,
      disabled: true,
      label: attributeName,
      name: attributeName,
      size: fieldSizes[attribute.type].default ?? 12
    };
    return field;
  });
  const unknownFieldsLayout = createLayoutFromFields(removedAttributesAsFields);
  const remainingFieldsLayout = getRemaingFieldsLayout({
    metadatas: configuration.contentType.metadatas,
    layout,
    schemaAttributes: schema.attributes,
    fieldSizes
  });
  const { components } = useDoc();
  const transformedData = React2.useMemo(() => {
    const transform = (schemaAttributes, components2 = {}) => (document) => {
      const schema2 = {
        attributes: schemaAttributes
      };
      const transformations = (0, import_pipe.default)(removeFieldsThatDontExistOnSchema(schema2), prepareTempKeys(schema2, components2));
      return transformations(document);
    };
    return transform(version.schema, components)(version.data);
  }, [
    components,
    version.data,
    version.schema
  ]);
  return (0, import_jsx_runtime2.jsx)(Layouts.Content, {
    children: (0, import_jsx_runtime2.jsxs)(S, {
      paddingBottom: {
        initial: 0,
        large: 8
      },
      children: [
        (0, import_jsx_runtime2.jsx)(Form, {
          disabled: true,
          method: "PUT",
          initialValues: transformedData,
          children: (0, import_jsx_runtime2.jsx)(C, {
            direction: "column",
            alignItems: "stretch",
            gap: 6,
            position: "relative",
            children: [
              ...layout,
              ...remainingFieldsLayout
            ].map((panel, index) => {
              return (0, import_jsx_runtime2.jsx)(FormPanel, {
                panel
              }, index);
            })
          })
        }, version.id),
        removedAttributesAsFields.length > 0 && (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, {
          children: [
            (0, import_jsx_runtime2.jsx)(S, {
              paddingTop: {
                initial: 4,
                large: 0
              },
              children: (0, import_jsx_runtime2.jsx)(yn, {})
            }),
            (0, import_jsx_runtime2.jsxs)(S, {
              paddingTop: {
                initial: 4,
                large: 8
              },
              children: [
                (0, import_jsx_runtime2.jsxs)(C, {
                  direction: "column",
                  alignItems: "flex-start",
                  paddingBottom: {
                    initial: 4,
                    large: 6
                  },
                  gap: 1,
                  children: [
                    (0, import_jsx_runtime2.jsx)(A, {
                      variant: "delta",
                      children: formatMessage({
                        id: "content-manager.history.content.unknown-fields.title",
                        defaultMessage: "Unknown fields"
                      })
                    }),
                    (0, import_jsx_runtime2.jsx)(A, {
                      variant: "pi",
                      children: formatMessage({
                        id: "content-manager.history.content.unknown-fields.message",
                        defaultMessage: "These fields have been deleted or renamed in the Content-Type Builder. <b>These fields will not be restored.</b>"
                      }, {
                        b: (chunks) => (0, import_jsx_runtime2.jsx)(A, {
                          variant: "pi",
                          fontWeight: "bold",
                          children: chunks
                        })
                      })
                    })
                  ]
                }),
                (0, import_jsx_runtime2.jsx)(Form, {
                  disabled: true,
                  method: "PUT",
                  initialValues: version.data,
                  children: (0, import_jsx_runtime2.jsx)(C, {
                    direction: "column",
                    alignItems: "stretch",
                    gap: 6,
                    position: "relative",
                    children: unknownFieldsLayout.map((panel, index) => {
                      return (0, import_jsx_runtime2.jsx)(FormPanel, {
                        panel
                      }, index);
                    })
                  })
                }, `${version.id}-unknownFields`)
              ]
            })
          ]
        })
      ]
    })
  });
};

// node_modules/@strapi/content-manager/dist/admin/history/components/VersionHeader.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var React3 = __toESM(require_react(), 1);
var import_qs = __toESM(require_lib(), 1);

// node_modules/@strapi/content-manager/dist/admin/history/services/historyVersion.mjs
var historyVersionsApi = contentManagerApi.injectEndpoints({
  endpoints: (builder) => ({
    getHistoryVersions: builder.query({
      query(params) {
        return {
          url: `/content-manager/history-versions`,
          method: "GET",
          config: {
            params
          }
        };
      },
      providesTags: [
        "HistoryVersion"
      ]
    }),
    restoreVersion: builder.mutation({
      query({ params, body }) {
        return {
          url: `/content-manager/history-versions/${params.versionId}/restore`,
          method: "PUT",
          data: body
        };
      },
      invalidatesTags: (_res, _error, { documentId, collectionType, params }) => {
        return [
          "HistoryVersion",
          {
            type: "Document",
            id: collectionType === COLLECTION_TYPES ? `${params.contentType}_${documentId}` : params.contentType
          }
        ];
      }
    })
  })
});
var { useGetHistoryVersionsQuery, useRestoreVersionMutation } = historyVersionsApi;

// node_modules/@strapi/content-manager/dist/admin/history/components/VersionHeader.mjs
var VersionHeader = ({ headerId }) => {
  var _a;
  const [isConfirmDialogOpen, setIsConfirmDialogOpen] = React3.useState(false);
  const navigate = useNavigate();
  const { formatMessage, formatDate } = useIntl();
  const { trackUsage } = useTracking();
  const { toggleNotification } = useNotification();
  const [{ query }] = useQueryParams();
  const { collectionType, slug } = useParams();
  const [restoreVersion, { isLoading }] = useRestoreVersionMutation();
  const { allowedActions } = useRBAC(PERMISSIONS.map((action) => ({
    action,
    subject: slug
  })));
  const version = useHistoryContext("VersionHeader", (state) => state.selectedVersion);
  const mainField = useHistoryContext("VersionHeader", (state) => state.mainField);
  const schema = useHistoryContext("VersionHeader", (state) => state.schema);
  const isCurrentVersion = useHistoryContext("VersionHeader", (state) => state.page === 1 && state.versions.data[0].id === state.selectedVersion.id);
  const mainFieldValue = version.data[mainField];
  const getNextNavigation = () => {
    const pluginsQueryParams = (0, import_qs.stringify)({
      plugins: query.plugins
    }, {
      encode: false
    });
    return {
      pathname: "..",
      search: pluginsQueryParams
    };
  };
  const handleRestore = async () => {
    try {
      const response = await restoreVersion({
        documentId: version.relatedDocumentId,
        collectionType,
        params: {
          versionId: version.id,
          contentType: version.contentType
        },
        body: {
          contentType: version.contentType
        }
      });
      if ("data" in response) {
        navigate(getNextNavigation(), {
          relative: "path"
        });
        toggleNotification({
          type: "success",
          title: formatMessage({
            id: "content-manager.restore.success.title",
            defaultMessage: "Version restored."
          }),
          message: formatMessage({
            id: "content-manager.restore.success.message",
            defaultMessage: "A past version of the content was restored."
          })
        });
        trackUsage("didRestoreHistoryVersion");
      }
      if ("error" in response) {
        toggleNotification({
          type: "danger",
          message: formatMessage({
            id: "content-manager.history.restore.error.message",
            defaultMessage: "Could not restore version."
          })
        });
      }
    } catch {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: "notification.error",
          defaultMessage: "An error occurred"
        })
      });
    }
  };
  return (0, import_jsx_runtime3.jsxs)(Pd.Root, {
    open: isConfirmDialogOpen,
    onOpenChange: setIsConfirmDialogOpen,
    children: [
      (0, import_jsx_runtime3.jsx)(Layouts.BaseHeader, {
        id: headerId,
        title: formatDate(new Date(version.createdAt), {
          year: "numeric",
          month: "numeric",
          day: "numeric",
          hour: "numeric",
          minute: "numeric"
        }),
        secondaryAction: (0, import_jsx_runtime3.jsx)(GradientBadgeWithIcon, {
          label: formatMessage({
            id: "components.premiumFeature.title",
            defaultMessage: "Premium feature"
          })
        }),
        subtitle: (0, import_jsx_runtime3.jsx)(A, {
          variant: "epsilon",
          textColor: "neutral600",
          children: formatMessage({
            id: "content-manager.history.version.subtitle",
            defaultMessage: "{hasLocale, select, true {{subtitle}, in {locale}} other {{subtitle}}}"
          }, {
            hasLocale: Boolean(version.locale),
            subtitle: `${mainFieldValue || ""} (${schema.info.singularName})`.trim(),
            locale: (_a = version.locale) == null ? void 0 : _a.name
          })
        }),
        navigationAction: (0, import_jsx_runtime3.jsx)(Oa, {
          startIcon: (0, import_jsx_runtime3.jsx)(D2, {}),
          tag: NavLink,
          to: getNextNavigation(),
          relative: "path",
          isExternal: false,
          children: formatMessage({
            id: "global.back",
            defaultMessage: "Back"
          })
        }),
        sticky: false,
        primaryAction: (0, import_jsx_runtime3.jsx)(Pd.Trigger, {
          children: (0, import_jsx_runtime3.jsx)(Ct, {
            disabled: !allowedActions.canUpdate || isCurrentVersion,
            onClick: () => {
              setIsConfirmDialogOpen(true);
            },
            fullWidth: true,
            children: formatMessage({
              id: "content-manager.history.restore.confirm.button",
              defaultMessage: "Restore"
            })
          })
        })
      }),
      (0, import_jsx_runtime3.jsx)(ConfirmDialog, {
        onConfirm: handleRestore,
        endAction: (0, import_jsx_runtime3.jsx)(Ct, {
          fullWidth: true,
          variant: "secondary",
          onClick: handleRestore,
          loading: isLoading,
          children: formatMessage({
            id: "content-manager.history.restore.confirm.button",
            defaultMessage: "Restore"
          })
        }),
        children: (0, import_jsx_runtime3.jsxs)(C, {
          direction: "column",
          alignItems: "center",
          justifyContent: "center",
          gap: 2,
          textAlign: "center",
          children: [
            (0, import_jsx_runtime3.jsx)(C, {
              justifyContent: "center",
              children: (0, import_jsx_runtime3.jsx)(Jn, {
                width: "24px",
                height: "24px",
                fill: "danger600"
              })
            }),
            (0, import_jsx_runtime3.jsx)(A, {
              children: formatMessage({
                id: "content-manager.history.restore.confirm.title",
                defaultMessage: "Are you sure you want to restore this version?"
              })
            }),
            (0, import_jsx_runtime3.jsx)(A, {
              children: formatMessage({
                id: "content-manager.history.restore.confirm.message",
                defaultMessage: "{isDraft, select, true {The restored content will override your draft.} other {The restored content won't be published, it will override the draft and be saved as pending changes. You'll be able to publish the changes at anytime.}}"
              }, {
                isDraft: version.status === "draft"
              })
            })
          ]
        })
      })
    ]
  });
};

// node_modules/@strapi/content-manager/dist/admin/history/components/VersionsList.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
var import_qs2 = __toESM(require_lib(), 1);
var BlueText = (children) => (0, import_jsx_runtime4.jsx)(A, {
  textColor: "primary600",
  variant: "pi",
  children
});
var VersionAuthor = ({ version, isCurrent }) => {
  const { formatMessage } = useIntl();
  const author = version.createdBy && getDisplayName(version.createdBy);
  return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, {
    children: formatMessage({
      id: "content-manager.history.sidebar.versionDescription",
      defaultMessage: "{distanceToNow}{isAnonymous, select, true {} other { by {author}}}{isCurrent, select, true { <b>(current)</b>} other {}}"
    }, {
      distanceToNow: (0, import_jsx_runtime4.jsx)(RelativeTime, {
        timestamp: new Date(version.createdAt)
      }),
      author,
      isAnonymous: !version.createdBy,
      isCurrent,
      b: BlueText
    })
  });
};
var VersionCard = ({ version, isCurrent }) => {
  const { formatDate } = useIntl();
  const [{ query }] = useQueryParams();
  const setIsOpen = useActionsDrawer("VersionCard", (s) => s == null ? void 0 : s.setIsOpen, false);
  const isActive = query.id === version.id.toString();
  const handleClick = () => {
    setIsOpen == null ? void 0 : setIsOpen(false);
  };
  return (0, import_jsx_runtime4.jsxs)(C, {
    direction: "column",
    alignItems: "flex-start",
    gap: 3,
    hasRadius: true,
    borderWidth: "1px",
    borderStyle: "solid",
    borderColor: isActive ? "primary600" : "neutral200",
    color: "neutral800",
    padding: 5,
    tag: Link,
    to: `?${(0, import_qs2.stringify)({
      ...query,
      id: version.id
    })}`,
    style: {
      textDecoration: "none"
    },
    onClick: handleClick,
    children: [
      (0, import_jsx_runtime4.jsxs)(C, {
        direction: "column",
        gap: 1,
        alignItems: "flex-start",
        children: [
          (0, import_jsx_runtime4.jsx)(A, {
            tag: "h3",
            fontWeight: "semiBold",
            children: formatDate(version.createdAt, {
              day: "numeric",
              month: "numeric",
              year: "numeric",
              hour: "2-digit",
              minute: "2-digit"
            })
          }),
          (0, import_jsx_runtime4.jsx)(A, {
            tag: "p",
            variant: "pi",
            textColor: "neutral600",
            children: (0, import_jsx_runtime4.jsx)(VersionAuthor, {
              version,
              isCurrent
            })
          })
        ]
      }),
      version.status && (0, import_jsx_runtime4.jsx)(DocumentStatus, {
        status: version.status,
        size: "XS"
      })
    ]
  });
};
var PaginationButton = ({ page, children }) => {
  const [{ query }] = useQueryParams();
  const { id: _id, ...queryRest } = query;
  return (0, import_jsx_runtime4.jsx)(Link, {
    to: {
      search: (0, import_qs2.stringify)({
        ...queryRest,
        page
      })
    },
    style: {
      textDecoration: "none"
    },
    children: (0, import_jsx_runtime4.jsx)(A, {
      variant: "omega",
      textColor: "primary600",
      children
    })
  });
};
var VersionsListItems = () => {
  const { formatMessage } = useIntl();
  const { versions, page } = useHistoryContext("VersionsListItems", (state) => ({
    versions: state.versions,
    page: state.page
  }));
  return (0, import_jsx_runtime4.jsxs)(S, {
    flex: 1,
    overflow: "auto",
    children: [
      versions.meta.pagination.page > 1 && (0, import_jsx_runtime4.jsx)(S, {
        paddingTop: 4,
        textAlign: "center",
        children: (0, import_jsx_runtime4.jsx)(PaginationButton, {
          page: page - 1,
          children: formatMessage({
            id: "content-manager.history.sidebar.show-newer",
            defaultMessage: "Show newer versions"
          })
        })
      }),
      (0, import_jsx_runtime4.jsx)(C, {
        direction: "column",
        gap: 3,
        padding: {
          initial: 0,
          medium: 4
        },
        tag: "ul",
        alignItems: "stretch",
        children: versions.data.map((version, index) => (0, import_jsx_runtime4.jsx)("li", {
          "aria-label": formatMessage({
            id: "content-manager.history.sidebar.title.version-card.aria-label",
            defaultMessage: "Version card"
          }),
          children: (0, import_jsx_runtime4.jsx)(VersionCard, {
            version,
            isCurrent: page === 1 && index === 0
          })
        }, version.id))
      }),
      versions.meta.pagination.page < versions.meta.pagination.pageCount && (0, import_jsx_runtime4.jsx)(S, {
        paddingBottom: 4,
        textAlign: "center",
        children: (0, import_jsx_runtime4.jsx)(PaginationButton, {
          page: page + 1,
          children: formatMessage({
            id: "content-manager.history.sidebar.show-older",
            defaultMessage: "Show older versions"
          })
        })
      })
    ]
  });
};
var VersionsList = () => {
  const { formatMessage } = useIntl();
  const { versions, page } = useHistoryContext("VersionsList", (state) => ({
    versions: state.versions,
    page: state.page
  }));
  const isMobile = useIsMobile();
  const [{ query }] = useQueryParams();
  const currentVersion = versions.data.find((version) => version.id.toString() === query.id);
  return !isMobile ? (0, import_jsx_runtime4.jsxs)(C, {
    shrink: 0,
    direction: "column",
    alignItems: "stretch",
    width: {
      initial: "28rem",
      large: "32rem"
    },
    height: "100%",
    overflow: "hidden",
    background: "neutral0",
    borderColor: "neutral200",
    borderWidth: "0 0 0 1px",
    borderStyle: "solid",
    tag: "aside",
    children: [
      (0, import_jsx_runtime4.jsxs)(C, {
        direction: "row",
        justifyContent: "space-between",
        padding: 4,
        borderColor: "neutral200",
        borderWidth: "0 0 1px",
        borderStyle: "solid",
        tag: "header",
        role: "banner",
        children: [
          (0, import_jsx_runtime4.jsx)(A, {
            tag: "h2",
            variant: "omega",
            fontWeight: "semiBold",
            children: formatMessage({
              id: "content-manager.history.sidebar.title",
              defaultMessage: "Versions"
            })
          }),
          (0, import_jsx_runtime4.jsx)(S, {
            background: "neutral150",
            hasRadius: true,
            padding: 1,
            children: (0, import_jsx_runtime4.jsx)(A, {
              variant: "sigma",
              textColor: "neutral600",
              children: versions.meta.pagination.total
            })
          })
        ]
      }),
      (0, import_jsx_runtime4.jsx)(VersionsListItems, {})
    ]
  }) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, {
    children: [
      (0, import_jsx_runtime4.jsxs)(ActionsDrawer.Root, {
        hasContent: true,
        hasSideNav: true,
        children: [
          (0, import_jsx_runtime4.jsx)(ActionsDrawer.Overlay, {}),
          (0, import_jsx_runtime4.jsx)(ActionsDrawer.Header, {
            children: currentVersion && (0, import_jsx_runtime4.jsxs)(C, {
              gap: 2,
              overflow: "hidden",
              children: [
                (0, import_jsx_runtime4.jsx)(S, {
                  flex: 1,
                  overflow: "hidden",
                  children: (0, import_jsx_runtime4.jsx)(A, {
                    display: "block",
                    variant: "omega",
                    textColor: "neutral600",
                    overflow: "hidden",
                    style: {
                      textOverflow: "ellipsis",
                      whiteSpace: "nowrap"
                    },
                    children: (0, import_jsx_runtime4.jsx)(VersionAuthor, {
                      version: currentVersion,
                      isCurrent: page === 1 && versions.data.indexOf(currentVersion) === 0
                    })
                  })
                }),
                currentVersion.status && (0, import_jsx_runtime4.jsx)(DocumentStatus, {
                  status: currentVersion.status,
                  size: "XS"
                })
              ]
            })
          }),
          (0, import_jsx_runtime4.jsx)(ActionsDrawer.Content, {
            children: (0, import_jsx_runtime4.jsx)(VersionsListItems, {})
          })
        ]
      }),
      (0, import_jsx_runtime4.jsx)(S, {
        width: "100%",
        height: "5.7rem"
      })
    ]
  });
};

// node_modules/@strapi/content-manager/dist/admin/history/pages/History.mjs
var [HistoryProvider, useHistoryContext] = createContext("HistoryPage");
var HistoryPage = () => {
  var _a, _b, _c, _d, _e, _f;
  const headerId = React4.useId();
  const { formatMessage } = useIntl();
  const { slug, id: documentId, collectionType } = useParams();
  const { isLoading: isLoadingDocument, schema } = useDocument({
    collectionType,
    model: slug
  });
  const { isLoading: isLoadingLayout, edit: { layout, settings: { displayName, mainField } } } = useDocumentLayout(slug);
  const { data: configuration, isLoading: isLoadingConfiguration } = useGetContentTypeConfigurationQuery(slug);
  const [{ query }] = useQueryParams();
  const { id: selectedVersionId, ...queryWithoutId } = query;
  const validQueryParamsWithoutId = buildValidParams(queryWithoutId);
  const page = validQueryParamsWithoutId.page ? Number(validQueryParamsWithoutId.page) : 1;
  const versionsResponse = useGetHistoryVersionsQuery({
    contentType: slug,
    ...documentId ? {
      documentId
    } : {},
    // Omit id since it's not needed by the endpoint and caused extra refetches
    ...validQueryParamsWithoutId
  }, {
    refetchOnMountOrArgChange: true
  });
  const initialRequestId = React4.useRef(versionsResponse.requestId);
  const isStaleRequest = versionsResponse.requestId === initialRequestId.current;
  if (!slug || collectionType === COLLECTION_TYPES && !documentId) {
    return (0, import_jsx_runtime5.jsx)(Navigate, {
      to: "/content-manager"
    });
  }
  if (isLoadingDocument || isLoadingLayout || versionsResponse.isFetching || isStaleRequest || isLoadingConfiguration) {
    return (0, import_jsx_runtime5.jsx)(Page.Loading, {});
  }
  if (!versionsResponse.isError && !((_b = (_a = versionsResponse.data) == null ? void 0 : _a.data) == null ? void 0 : _b.length)) {
    return (0, import_jsx_runtime5.jsx)(import_jsx_runtime5.Fragment, {
      children: (0, import_jsx_runtime5.jsx)(Page.NoData, {
        action: (0, import_jsx_runtime5.jsx)(Oa, {
          tag: NavLink,
          to: `/content-manager/${collectionType}/${slug}${documentId ? `/${documentId}` : ""}`,
          children: formatMessage({
            id: "global.back",
            defaultMessage: "Back"
          })
        })
      })
    });
  }
  if (((_d = (_c = versionsResponse.data) == null ? void 0 : _c.data) == null ? void 0 : _d.length) && !selectedVersionId) {
    return (0, import_jsx_runtime5.jsx)(Navigate, {
      to: {
        search: (0, import_qs3.stringify)({
          ...query,
          id: versionsResponse.data.data[0].id
        })
      },
      replace: true
    });
  }
  const selectedVersion = (_f = (_e = versionsResponse.data) == null ? void 0 : _e.data) == null ? void 0 : _f.find((version) => version.id.toString() === selectedVersionId);
  if (versionsResponse.isError || !layout || !schema || !selectedVersion || !configuration || // This should not happen as it's covered by versionsResponse.isError, but we need it for TS
  versionsResponse.data.error) {
    return (0, import_jsx_runtime5.jsx)(Page.Error, {});
  }
  return (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, {
    children: [
      (0, import_jsx_runtime5.jsx)(Page.Title, {
        children: formatMessage({
          id: "content-manager.history.page-title",
          defaultMessage: "{contentType} history"
        }, {
          contentType: displayName
        })
      }),
      (0, import_jsx_runtime5.jsx)(HistoryProvider, {
        contentType: slug,
        id: documentId,
        schema,
        layout,
        configuration,
        selectedVersion,
        versions: versionsResponse.data,
        page,
        mainField,
        children: (0, import_jsx_runtime5.jsxs)(C, {
          direction: {
            initial: "column",
            medium: "row"
          },
          alignItems: "flex-start",
          height: "100%",
          overflow: "hidden",
          children: [
            (0, import_jsx_runtime5.jsxs)(Vd, {
              grow: 1,
              width: "100%",
              height: "100%",
              background: "neutral100",
              paddingBottom: 6,
              overflow: "hidden auto",
              labelledBy: headerId,
              children: [
                (0, import_jsx_runtime5.jsx)(VersionHeader, {
                  headerId
                }),
                (0, import_jsx_runtime5.jsx)(VersionContent, {})
              ]
            }),
            (0, import_jsx_runtime5.jsx)(VersionsList, {})
          ]
        })
      })
    ]
  });
};
var ProtectedHistoryPageImpl = () => {
  const { slug } = useParams();
  const { permissions = [], isLoading, error } = useRBAC(PERMISSIONS.map((action) => ({
    action,
    subject: slug
  })));
  if (isLoading) {
    return (0, import_jsx_runtime5.jsx)(Page.Loading, {});
  }
  if (error || !slug) {
    return (0, import_jsx_runtime5.jsx)(S, {
      width: "100dvw",
      position: "fixed",
      top: {
        initial: HEIGHT_TOP_NAVIGATION,
        large: 0
      },
      bottom: 0,
      left: 0,
      zIndex: {
        initial: 2,
        large: 4
      },
      background: "neutral0",
      children: (0, import_jsx_runtime5.jsx)(Page.Error, {})
    });
  }
  return (0, import_jsx_runtime5.jsx)(S, {
    width: "100dvw",
    position: "fixed",
    top: {
      initial: HEIGHT_TOP_NAVIGATION,
      large: 0
    },
    bottom: 0,
    left: 0,
    zIndex: {
      initial: 2,
      large: 4
    },
    background: "neutral0",
    children: (0, import_jsx_runtime5.jsx)(Page.Protect, {
      permissions,
      children: ({ permissions: permissions2 }) => (0, import_jsx_runtime5.jsx)(DocumentRBAC, {
        permissions: permissions2,
        children: (0, import_jsx_runtime5.jsx)(HistoryPage, {})
      })
    })
  });
};
var ProtectedHistoryPage = () => {
  return (0, import_jsx_runtime5.jsx)(un, {
    children: (0, import_jsx_runtime5.jsx)(yu, {
      children: (0, import_jsx_runtime5.jsx)(ProtectedHistoryPageImpl, {})
    })
  });
};
export {
  HistoryProvider,
  ProtectedHistoryPage,
  useHistoryContext
};
//# sourceMappingURL=History-ALYS35KE.js.map
