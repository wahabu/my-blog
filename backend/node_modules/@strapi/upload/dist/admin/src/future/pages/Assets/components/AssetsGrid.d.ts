import { type ItemKey } from '../utils/selection';
import type { File } from '../../../../../../shared/contracts/files';
import type { Folder } from '../../../../../../shared/contracts/folders';
interface AssetsGridProps {
    assets: File[];
    folders?: Folder[];
    /** Keys of the rendered items, in render order. Owned by the view. */
    renderedKeys?: ItemKey[];
    onAssetItemClick: (assetId: number) => void;
}
export declare const AssetsGrid: ({ assets, folders, renderedKeys, onAssetItemClick, }: AssetsGridProps) => import("react/jsx-runtime").JSX.Element | null;
export {};
