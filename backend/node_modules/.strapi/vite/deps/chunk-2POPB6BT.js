import {
  generateNKeysBetween
} from "./chunk-3DRZIGZ7.js";
import {
  require_isEqual
} from "./chunk-KVWVHPCR.js";
import {
  getIn,
  setIn
} from "./chunk-RIFP47FT.js";
import {
  createContext
} from "./chunk-TXAF42S5.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import {
  useBlocker
} from "./chunk-QBM2MGBP.js";
import {
  fn
} from "./chunk-LHXIEHKN.js";
import {
  Ct,
  Io,
  Pd,
  S,
  te
} from "./chunk-7ZODKSNG.js";
import {
  Jn
} from "./chunk-D4B4CG5U.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/Form.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var import_isEqual = __toESM(require_isEqual(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/hooks/useWarnIfUnsavedChanges.mjs
var import_react = __toESM(require_react(), 1);
function useWarnIfUnsavedChanges(enabled) {
  (0, import_react.useEffect)(() => {
    if (!enabled) return;
    const handleBeforeUnload = (e) => {
      e.preventDefault();
      e.returnValue = "";
      return "";
    };
    window.addEventListener("beforeunload", handleBeforeUnload);
    return () => window.removeEventListener("beforeunload", handleBeforeUnload);
  }, [
    enabled
  ]);
}

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/Form.mjs
var isArrayLikeOptions = (options) => {
  return typeof options === "object" && options !== null && "length" in options;
};
var isMultipleSelectTarget = (target) => {
  return "multiple" in target && target.multiple === true && "options" in target && isArrayLikeOptions(target.options);
};
var ERR_MSG = "The Form Component has not been initialised, ensure you are using this hook within a Form component";
var [FormProvider, useForm] = createContext("Form", {
  disabled: false,
  errors: {},
  getValues: () => {
    throw new Error(ERR_MSG);
  },
  initialValues: {},
  isSubmitting: false,
  modified: false,
  addFieldRow: () => {
    throw new Error(ERR_MSG);
  },
  moveFieldRow: () => {
    throw new Error(ERR_MSG);
  },
  onChange: () => {
    throw new Error(ERR_MSG);
  },
  removeFieldRow: () => {
    throw new Error(ERR_MSG);
  },
  resetForm: () => {
    throw new Error(ERR_MSG);
  },
  setErrors: () => {
    throw new Error(ERR_MSG);
  },
  setValues: () => {
    throw new Error(ERR_MSG);
  },
  setSubmitting: () => {
    throw new Error(ERR_MSG);
  },
  validate: async () => {
    throw new Error(ERR_MSG);
  },
  values: {}
});
var Form = React.forwardRef(({ disabled = false, method, onSubmit, initialErrors, ...props }, ref) => {
  const formRef = React.useRef(null);
  const initialValues = React.useRef(props.initialValues ?? {});
  const [state, dispatch] = React.useReducer(reducer, {
    errors: initialErrors ?? {},
    isSubmitting: false,
    values: props.initialValues ?? {}
  });
  const valuesRef = React.useRef(state.values);
  valuesRef.current = state.values;
  const getValues = React.useCallback(() => valuesRef.current, []);
  React.useEffect(() => {
    if (!(0, import_isEqual.default)(initialValues.current, props.initialValues)) {
      initialValues.current = props.initialValues ?? {};
      dispatch({
        type: "SET_INITIAL_VALUES",
        payload: props.initialValues ?? {}
      });
    }
  }, [
    props.initialValues
  ]);
  const setErrors = React.useCallback((errors) => {
    dispatch({
      type: "SET_ERRORS",
      payload: errors
    });
  }, []);
  const setValues = React.useCallback((values) => {
    dispatch({
      type: "SET_VALUES",
      payload: values
    });
  }, []);
  React.useEffect(() => {
    if (Object.keys(state.errors).length === 0) return;
    const ref2 = setTimeout(() => {
      const [firstError] = formRef.current.querySelectorAll("[data-strapi-field-error]");
      if (firstError) {
        const errorId = firstError.getAttribute("id");
        const formElementInError = formRef.current.querySelector(`[aria-describedby="${errorId}"]`);
        if (formElementInError && formElementInError instanceof HTMLElement) {
          formElementInError.focus();
        }
      }
    });
    return () => clearTimeout(ref2);
  }, [
    state.errors
  ]);
  const validate = React.useCallback(async (shouldSetErrors = true, options = {}) => {
    setErrors({});
    const valuesToValidate = valuesRef.current;
    if (!props.validationSchema && !props.validate) {
      return {
        data: valuesToValidate
      };
    }
    try {
      let data;
      if (props.validationSchema) {
        data = await props.validationSchema.validate(valuesToValidate, {
          abortEarly: false
        });
      } else if (props.validate) {
        data = await props.validate(valuesToValidate, options);
      } else {
        throw new Error("No validation schema or validate function provided");
      }
      return {
        data
      };
    } catch (err) {
      if (isErrorYupValidationError(err)) {
        const errors = getYupValidationErrors(err);
        if (shouldSetErrors) {
          setErrors(errors);
        }
        return {
          errors
        };
      } else {
        if (true) {
          console.warn(`Warning: An unhandled error was caught during validation in <Form validationSchema />`, err);
        }
        throw err;
      }
    }
  }, [
    props,
    setErrors
  ]);
  const handleSubmit = async (e) => {
    e.stopPropagation();
    e.preventDefault();
    if (!onSubmit) {
      return;
    }
    dispatch({
      type: "SUBMIT_ATTEMPT"
    });
    try {
      const { data, errors } = await validate();
      if (errors) {
        setErrors(errors);
        throw new Error("Submission failed");
      }
      await onSubmit(data, {
        setErrors,
        setValues,
        resetForm
      });
      dispatch({
        type: "SUBMIT_SUCCESS"
      });
    } catch (err) {
      dispatch({
        type: "SUBMIT_FAILURE"
      });
      if (err instanceof Error && err.message === "Submission failed") {
        return;
      }
    }
  };
  const modified = React.useMemo(
    () => !(0, import_isEqual.default)(initialValues.current, state.values),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      state.values,
      initialValues.current
    ]
  );
  const handleChange = Io((eventOrPath, v) => {
    if (typeof eventOrPath === "string") {
      dispatch({
        type: "SET_FIELD_VALUE",
        payload: {
          field: eventOrPath,
          value: v
        }
      });
      return;
    }
    const { target: eventTarget, currentTarget } = eventOrPath;
    const target = eventTarget ?? currentTarget;
    if (target === void 0) {
      return;
    }
    const { type, name, id, value } = target;
    let field = name;
    if (field === void 0 || field === "") {
      field = id;
    }
    if ((field === void 0 || field === "") && true) {
      console.warn(`\`onChange\` was called with an event, but you forgot to pass a \`name\` or \`id'\` attribute to your input. The field to update cannot be determined`);
    }
    if (field === void 0 || field === "") {
      return;
    }
    let val;
    if (/number|range/.test(type)) {
      const parsed = parseFloat(value);
      val = isNaN(parsed) ? "" : parsed;
    } else if (/checkbox/.test(type)) {
      val = !getIn(state.values, field);
    } else if (isMultipleSelectTarget(target)) {
      val = Array.from(target.options).filter((el) => el.selected).map((el) => el.value);
    } else {
      if (value === "") {
        val = null;
      } else {
        val = value;
      }
    }
    dispatch({
      type: "SET_FIELD_VALUE",
      payload: {
        field,
        value: val
      }
    });
  });
  const addFieldRow = React.useCallback((field, value, addAtIndex) => {
    dispatch({
      type: "ADD_FIELD_ROW",
      payload: {
        field,
        value,
        addAtIndex
      }
    });
  }, []);
  const removeFieldRow = React.useCallback((field, removeAtIndex) => {
    dispatch({
      type: "REMOVE_FIELD_ROW",
      payload: {
        field,
        removeAtIndex
      }
    });
  }, []);
  const moveFieldRow = React.useCallback((field, fromIndex, toIndex) => {
    dispatch({
      type: "MOVE_FIELD_ROW",
      payload: {
        field,
        fromIndex,
        toIndex
      }
    });
  }, []);
  const resetForm = React.useCallback((newInitialValues) => {
    if (newInitialValues) {
      initialValues.current = newInitialValues;
    }
    dispatch({
      type: "RESET_FORM",
      payload: {
        errors: {},
        isSubmitting: false,
        values: initialValues.current
      }
    });
  }, []);
  const setSubmitting = React.useCallback((isSubmitting) => {
    dispatch({
      type: "SET_ISSUBMITTING",
      payload: isSubmitting
    });
  }, []);
  const composedRefs = te(formRef, ref);
  return (0, import_jsx_runtime.jsx)(S, {
    tag: "form",
    ref: composedRefs,
    method,
    noValidate: true,
    onSubmit: handleSubmit,
    width: props.width,
    height: props.height,
    children: (0, import_jsx_runtime.jsx)(FormProvider, {
      disabled,
      getValues,
      onChange: handleChange,
      initialValues: initialValues.current,
      modified,
      addFieldRow,
      moveFieldRow,
      removeFieldRow,
      resetForm,
      setErrors,
      setValues,
      setSubmitting,
      validate,
      ...state,
      children: typeof props.children === "function" ? props.children({
        modified,
        disabled,
        onChange: handleChange,
        ...state,
        setErrors,
        resetForm
      }) : props.children
    })
  });
});
var isErrorYupValidationError = (err) => typeof err === "object" && err !== null && "name" in err && typeof err.name === "string" && err.name === "ValidationError";
var getYupValidationErrors = (err) => {
  let errors = {};
  if (err.inner) {
    if (err.inner.length === 0) {
      return setIn(errors, err.path, err.message);
    }
    for (const error of err.inner) {
      if (!getIn(errors, error.path)) {
        errors = setIn(errors, error.path, error.message);
      }
    }
  }
  return errors;
};
var setDraftValues = (draft, values) => {
  draft.values = values;
};
var reducer = (state, action) => fn(state, (draft) => {
  switch (action.type) {
    case "SET_INITIAL_VALUES":
      setDraftValues(draft, action.payload);
      break;
    case "SET_VALUES":
      setDraftValues(draft, action.payload);
      break;
    case "SUBMIT_ATTEMPT":
      draft.isSubmitting = true;
      break;
    case "SUBMIT_FAILURE":
      draft.isSubmitting = false;
      break;
    case "SUBMIT_SUCCESS":
      draft.isSubmitting = false;
      break;
    case "SET_FIELD_VALUE":
      setDraftValues(draft, setIn(state.values, action.payload.field, action.payload.value));
      break;
    case "ADD_FIELD_ROW": {
      const currentField = getIn(state.values, action.payload.field, []);
      let position = action.payload.addAtIndex;
      if (position === void 0) {
        position = currentField.length;
      } else if (position < 0) {
        position = 0;
      }
      const existingKeys = new Set(currentField.map((item) => item.__temp_key__).filter((key2) => key2 !== void 0 && key2 !== ""));
      let key;
      let lowerBound = existingKeys.size > 0 ? Array.from(existingKeys).sort().pop() : null;
      do {
        [key] = generateNKeysBetween(lowerBound, null, 1);
        if (existingKeys.has(key)) {
          lowerBound = key;
        }
      } while (existingKeys.has(key));
      setDraftValues(draft, setIn(state.values, action.payload.field, currentField.toSpliced(position, 0, {
        ...action.payload.value,
        __temp_key__: key
      })));
      break;
    }
    case "MOVE_FIELD_ROW": {
      const { field, fromIndex, toIndex } = action.payload;
      const currentField = [
        ...getIn(state.values, field, [])
      ];
      const currentRow = currentField[fromIndex];
      currentField.splice(fromIndex, 1);
      currentField.splice(toIndex, 0, currentRow);
      setDraftValues(draft, setIn(state.values, field, currentField));
      break;
    }
    case "REMOVE_FIELD_ROW": {
      const currentField = getIn(state.values, action.payload.field, []);
      let position = action.payload.removeAtIndex;
      if (position === void 0) {
        position = currentField.length - 1;
      } else if (position < 0) {
        position = 0;
      }
      const newValue = setIn(currentField, position.toString(), void 0).filter((val) => val);
      setDraftValues(draft, setIn(state.values, action.payload.field, newValue.length > 0 ? newValue : []));
      break;
    }
    case "SET_ERRORS":
      if (!(0, import_isEqual.default)(state.errors, action.payload)) {
        draft.errors = action.payload;
      }
      break;
    case "SET_ISSUBMITTING":
      draft.isSubmitting = action.payload;
      break;
    case "RESET_FORM":
      setDraftValues(draft, action.payload.values);
      draft.errors = action.payload.errors;
      draft.isSubmitting = action.payload.isSubmitting;
      break;
  }
});
function useField(path) {
  const { formatMessage } = useIntl();
  const initialValue = useForm("useField", (state) => getIn(state.initialValues, path));
  const value = useForm("useField", (state) => getIn(state.values, path));
  const handleChange = useForm("useField", (state) => state.onChange);
  const rawError = useForm("useField", (state) => getIn(state.errors, path));
  const error = useForm("useField", (state) => {
    const error2 = getIn(state.errors, path);
    if (isErrorMessageDescriptor(error2)) {
      const { values, ...message } = error2;
      return formatMessage(message, values);
    }
    return error2;
  });
  return {
    initialValue,
    /**
    * Errors can be a string, or a MessageDescriptor, so we need to handle both cases.
    * If it's anything else, we don't return it.
    */
    rawError,
    error: isErrorMessageDescriptor(error) ? formatMessage({
      id: error.id,
      defaultMessage: error.defaultMessage
    }, error.values) : typeof error === "string" ? error : void 0,
    onChange: handleChange,
    value
  };
}
var isErrorMessageDescriptor = (object) => {
  return typeof object === "object" && object !== null && !Array.isArray(object) && "id" in object && "defaultMessage" in object;
};
var Blocker = ({ onProceed = () => {
}, onCancel = () => {
} }) => {
  const { formatMessage } = useIntl();
  const modified = useForm("Blocker", (state) => state.modified);
  const isSubmitting = useForm("Blocker", (state) => state.isSubmitting);
  useWarnIfUnsavedChanges(modified && !isSubmitting);
  const blocker = useBlocker(({ currentLocation, nextLocation }) => {
    return !isSubmitting && modified && (currentLocation.pathname !== nextLocation.pathname || currentLocation.search !== nextLocation.search);
  });
  if (blocker.state === "blocked") {
    const handleCancel = (isOpen) => {
      if (!isOpen) {
        onCancel();
        blocker.reset();
      }
    };
    return (0, import_jsx_runtime.jsx)(Pd.Root, {
      open: true,
      onOpenChange: handleCancel,
      children: (0, import_jsx_runtime.jsxs)(Pd.Content, {
        children: [
          (0, import_jsx_runtime.jsx)(Pd.Header, {
            children: formatMessage({
              id: "app.components.ConfirmDialog.title",
              defaultMessage: "Confirmation"
            })
          }),
          (0, import_jsx_runtime.jsx)(Pd.Body, {
            icon: (0, import_jsx_runtime.jsx)(Jn, {
              width: "24px",
              height: "24px",
              fill: "danger600"
            }),
            textAlign: "center",
            children: formatMessage({
              id: "global.prompt.unsaved",
              defaultMessage: "You have unsaved changes, are you sure you want to leave?"
            })
          }),
          (0, import_jsx_runtime.jsxs)(Pd.Footer, {
            children: [
              (0, import_jsx_runtime.jsx)(Pd.Cancel, {
                children: (0, import_jsx_runtime.jsx)(Ct, {
                  variant: "tertiary",
                  fullWidth: true,
                  children: formatMessage({
                    id: "app.components.Button.cancel",
                    defaultMessage: "Cancel"
                  })
                })
              }),
              (0, import_jsx_runtime.jsx)(Ct, {
                onClick: () => {
                  onProceed();
                  blocker.proceed();
                },
                variant: "danger",
                fullWidth: true,
                children: formatMessage({
                  id: "app.components.Button.confirm",
                  defaultMessage: "Confirm"
                })
              })
            ]
          })
        ]
      })
    });
  }
  return null;
};

export {
  useForm,
  Form,
  getYupValidationErrors,
  useField,
  Blocker
};
//# sourceMappingURL=chunk-2POPB6BT.js.map
