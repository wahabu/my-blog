import type { ContentType, Component } from '../../../../../types';
import type { Schema } from '../../types/schema';
type SchemaWithSources = (ContentType | Component) & {
    sources?: unknown;
};
export declare const transformCTBToChat: (schema: SchemaWithSources) => Schema;
export {};
