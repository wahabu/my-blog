'use strict';

const resolvePreviewImageUrl = (preview)=>{
    if (!preview) {
        return undefined;
    }
    if (typeof preview === 'string') {
        return preview;
    }
    return preview.url;
};

exports.resolvePreviewImageUrl = resolvePreviewImageUrl;
//# sourceMappingURL=previewImage.js.map
