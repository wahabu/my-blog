import { DefaultTheme } from 'styled-components';
import { ResponsiveThemeProperty } from '../helpers/handleResponsiveValues';
declare const TEXT_VARIANTS: readonly ["alpha", "beta", "delta", "epsilon", "omega", "pi", "sigma"];
declare const ellipsis: import('styled-components').RuleSet<object>;
interface VariantProps {
    $variant?: (typeof TEXT_VARIANTS)[number];
    theme: DefaultTheme;
    $fontSize?: ResponsiveThemeProperty<'fontSizes', 'fontSize'>;
    $lineHeight?: ResponsiveThemeProperty<'lineHeights', 'lineHeight'>;
}
declare const variant: ({ $variant, theme, $fontSize, $lineHeight }: VariantProps) => import('styled-components').RuleSet<object>;
export { TEXT_VARIANTS, ellipsis, variant };
export type { VariantProps };
//# sourceMappingURL=type.d.ts.map