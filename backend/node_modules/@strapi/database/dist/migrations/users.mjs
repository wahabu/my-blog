import path from 'path';
import fse from 'fs-extra';
import { discoverMigrationFiles } from './discover.mjs';
import { createMigrationRunner } from './runner.mjs';
import { resolveMigrationFiles } from './resolver.mjs';
import { createStorage } from './storage.mjs';
import { transformLogMessage } from './logger.mjs';

const createUserMigrationProvider = (db)=>{
    const dir = db.config.settings.migrations.dir;
    fse.ensureDirSync(dir);
    const context = {
        db
    };
    const runner = createMigrationRunner({
        storage: createStorage({
            db,
            tableName: 'strapi_migrations'
        }),
        logger: {
            info (message) {
                db.logger.info(transformLogMessage('info', message));
            }
        },
        async getMigrations () {
            const filepaths = discoverMigrationFiles(path.resolve(dir));
            return resolveMigrationFiles(filepaths, context);
        }
    });
    return {
        async shouldRun () {
            const pendingMigrations = await runner.pending();
            return pendingMigrations.length > 0 && db.config?.settings?.runMigrations === true;
        },
        async up () {
            await runner.up();
        },
        async down () {
            await runner.down();
        }
    };
};

export { createUserMigrationProvider };
//# sourceMappingURL=users.mjs.map
