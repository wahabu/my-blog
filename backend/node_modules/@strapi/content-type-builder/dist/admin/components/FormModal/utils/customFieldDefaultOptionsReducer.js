'use strict';

const customFieldDefaultOptionsReducer = (acc, option)=>{
    if (option.items !== undefined) {
        return option.items.reduce((currentAcc, item)=>customFieldDefaultOptionsReducer(currentAcc, item), acc);
    }
    if ('defaultValue' in option && option.name !== undefined) {
        const { name, defaultValue } = option;
        acc.push({
            name,
            defaultValue
        });
    }
    return acc;
};

exports.customFieldDefaultOptionsReducer = customFieldDefaultOptionsReducer;
//# sourceMappingURL=customFieldDefaultOptionsReducer.js.map
