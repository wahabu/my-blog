'use strict';

var fp = require('lodash/fp');
var utils = require('@strapi/utils');

var provider = (({ strapi })=>({
        async checkFileSize (file) {
            const { sizeLimit } = strapi.config.get('plugin::upload');
            await strapi.plugin('upload').provider.checkFileSize(file, {
                sizeLimit
            });
        },
        async upload (file) {
            if (fp.isFunction(strapi.plugin('upload').provider.uploadStream)) {
                file.stream = file.getStream();
                await strapi.plugin('upload').provider.uploadStream(file);
                delete file.stream;
                if ('filepath' in file) {
                    delete file.filepath;
                }
            } else {
                file.buffer = await utils.file.streamToBuffer(file.getStream());
                await strapi.plugin('upload').provider.upload(file);
                delete file.buffer;
                if ('filepath' in file) {
                    delete file.filepath;
                }
            }
        },
        async replace (newFile, oldFile) {
            const provider = strapi.plugin('upload').provider;
            if (fp.isFunction(provider.replaceStream)) {
                newFile.stream = newFile.getStream();
                await provider.replaceStream(newFile, oldFile);
                delete newFile.stream;
                if ('filepath' in newFile) {
                    delete newFile.filepath;
                }
                return;
            }
            if (fp.isFunction(provider.replace)) {
                newFile.buffer = await utils.file.streamToBuffer(newFile.getStream());
                await provider.replace(newFile, oldFile);
                delete newFile.buffer;
                if ('filepath' in newFile) {
                    delete newFile.filepath;
                }
                return;
            }
            // Fallback: delete old then upload new — preserves current behavior for the file.
            await provider.delete(oldFile);
            await this.upload(newFile);
        }
    }));

module.exports = provider;
//# sourceMappingURL=provider.js.map
