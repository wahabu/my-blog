import type { FolderNode } from '../../../../shared/contracts/folders';
import type { DragItemData } from '../types/dnd';
export interface CanDropItemOnFolderParams {
    items: DragItemData[];
    targetFolderId: number | null;
    folderStructure: FolderNode[];
}
export declare const isFolderDescendantOf: (folderStructure: FolderNode[], ancestorId: number, candidateId: number) => boolean;
export declare const canDropItemOnFolder: ({ items, targetFolderId, folderStructure, }: CanDropItemOnFolderParams) => boolean;
