import { jsx, jsxs } from 'react/jsx-runtime';
import { useEffect, Suspense } from 'react';
import { Outlet } from 'react-router-dom';
import { GlobalNotifications } from '../../ee/admin/src/components/GlobalNotifications.mjs';
import { Page } from './components/PageHelpers.mjs';
import { Providers } from './components/Providers.mjs';
import { LANGUAGE_LOCAL_STORAGE_KEY } from './reducer.mjs';

const App = ({ strapi, store })=>{
    useEffect(()=>{
        const language = localStorage.getItem(LANGUAGE_LOCAL_STORAGE_KEY) || 'en';
        {
            document.documentElement.lang = language;
        }
    }, []);
    /**
   * @internal
   * @experimental
   *
   * The `future-global::` namespace is intended for internal use.
   * It is experimental and could change or be removed in the future.
   */ const globalComponents = Object.entries(strapi.library.components).filter(([name])=>name.startsWith('future-global::')).map(([name, Component])=>({
            name,
            Component
        }));
    return /*#__PURE__*/ jsx(Providers, {
        strapi: strapi,
        store: store,
        children: /*#__PURE__*/ jsxs(Suspense, {
            fallback: /*#__PURE__*/ jsx(Page.Loading, {}),
            children: [
                /*#__PURE__*/ jsx(GlobalNotifications, {}),
                window.strapi.future.isEnabled('betaMediaLibrary') && globalComponents.map(({ name, Component })=>/*#__PURE__*/ jsx(Component, {}, name)),
                /*#__PURE__*/ jsx(Outlet, {})
            ]
        })
    });
};

export { App };
//# sourceMappingURL=App.mjs.map
