const EMPTY_DRAFT_RELATION_COUNTS = {
    unpublishedRelations: 0,
    draftM2mLinks: 0
};
/**
 * Bidirectional manyToMany links share a join-table row and are kept on publish; they become
 * visible on the live site once the related entry is published. xToOne-style links are stripped.
 */ const isBidirectionalManyToMany = (attribute)=>attribute.relation === 'manyToMany' && Boolean(attribute.inversedBy || attribute.mappedBy);

export { EMPTY_DRAFT_RELATION_COUNTS, isBidirectionalManyToMany };
//# sourceMappingURL=draft-relations.mjs.map
