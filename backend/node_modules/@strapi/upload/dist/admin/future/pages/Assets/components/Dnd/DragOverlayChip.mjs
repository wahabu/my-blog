import { jsxs, jsx } from 'react/jsx-runtime';
import { Typography, Flex } from '@strapi/design-system';
import { Folder, File } from '@strapi/icons';
import { useIntl } from 'react-intl';
import { styled } from 'styled-components';
import { getTranslationKey } from '../../../../utils/translations.mjs';

// Folder & File (@strapi/icons) share viewBox 0 0 32 32 but File's glyph is
// narrower/lighter. Rendering File a few px larger inside a fixed square box makes
// the two previews read at the same visual size without shifting the chip layout.
const FOLDER_ICON_SIZE = 20;
const FILE_ICON_SIZE = 24;
const ICON_BOX = 24;
const Chip = styled(Flex)`
  position: relative;
  align-items: center;
  gap: ${({ theme })=>theme.spaces[2]};
  padding: ${({ theme })=>`${theme.spaces[2]} ${theme.spaces[3]}`};
  border-radius: ${({ theme })=>theme.borderRadius};
  background: ${({ theme })=>theme.colors.primary100};
  box-shadow: ${({ theme })=>theme.shadows.tableShadow};
  cursor: grabbing;
  max-width: 24rem;
`;
const CompositeChip = styled(Chip)`
  box-shadow:
    ${({ theme })=>theme.shadows.tableShadow},
    0 4px 0 -1px ${({ theme })=>theme.colors.primary100},
    0 4px 0 0 ${({ theme })=>theme.colors.primary200},
    0 7px 0 -1px ${({ theme })=>theme.colors.primary100},
    0 7px 0 0 ${({ theme })=>theme.colors.primary200};
`;
const CountGroup = styled(Flex)`
  align-items: center;
  gap: ${({ theme })=>theme.spaces[1]};
`;
const IconBox = styled(Flex)`
  flex-shrink: 0;
  width: ${ICON_BOX}px;
  height: ${ICON_BOX}px;
  align-items: center;
  justify-content: center;
`;
const TotalBadge = styled(Flex)`
  position: absolute;
  top: -${({ theme })=>theme.spaces[2]};
  right: -${({ theme })=>theme.spaces[2]};
  align-items: center;
  justify-content: center;
  min-width: ${({ theme })=>theme.spaces[5]};
  height: ${({ theme })=>theme.spaces[5]};
  padding: 0 ${({ theme })=>theme.spaces[1]};
  border-radius: ${({ theme })=>theme.borderRadius};
  background: ${({ theme })=>theme.colors.primary600};
`;
const DragOverlayChip = ({ items })=>{
    const { formatMessage } = useIntl();
    if (items.length === 0) {
        return null;
    }
    if (items.length === 1) {
        const item = items[0];
        const isFolder = item.kind === 'folder';
        const Icon = isFolder ? Folder : File;
        const size = isFolder ? FOLDER_ICON_SIZE : FILE_ICON_SIZE;
        return /*#__PURE__*/ jsxs(Chip, {
            children: [
                /*#__PURE__*/ jsx(IconBox, {
                    children: /*#__PURE__*/ jsx(Icon, {
                        width: size,
                        height: size
                    })
                }),
                /*#__PURE__*/ jsx(Typography, {
                    textColor: "neutral800",
                    fontWeight: "semiBold",
                    ellipsis: true,
                    children: item.name
                })
            ]
        });
    }
    const folderCount = items.filter((item)=>item.kind === 'folder').length;
    const fileCount = items.filter((item)=>item.kind === 'file').length;
    const total = folderCount + fileCount;
    return /*#__PURE__*/ jsxs(CompositeChip, {
        gap: 3,
        children: [
            folderCount > 0 ? /*#__PURE__*/ jsxs(CountGroup, {
                children: [
                    /*#__PURE__*/ jsx(IconBox, {
                        children: /*#__PURE__*/ jsx(Folder, {
                            width: FOLDER_ICON_SIZE,
                            height: FOLDER_ICON_SIZE
                        })
                    }),
                    /*#__PURE__*/ jsx(Typography, {
                        textColor: "neutral800",
                        fontWeight: "semiBold",
                        children: formatMessage({
                            id: getTranslationKey('dnd.overlay.folders'),
                            defaultMessage: '{count, plural, one {# folder} other {# folders}}'
                        }, {
                            count: folderCount
                        })
                    })
                ]
            }) : null,
            fileCount > 0 ? /*#__PURE__*/ jsxs(CountGroup, {
                children: [
                    /*#__PURE__*/ jsx(IconBox, {
                        children: /*#__PURE__*/ jsx(File, {
                            width: FILE_ICON_SIZE,
                            height: FILE_ICON_SIZE
                        })
                    }),
                    /*#__PURE__*/ jsx(Typography, {
                        textColor: "neutral800",
                        fontWeight: "semiBold",
                        children: formatMessage({
                            id: getTranslationKey('dnd.overlay.files'),
                            defaultMessage: '{count, plural, one {# file} other {# files}}'
                        }, {
                            count: fileCount
                        })
                    })
                ]
            }) : null,
            /*#__PURE__*/ jsx(TotalBadge, {
                children: /*#__PURE__*/ jsx(Typography, {
                    textColor: "neutral0",
                    fontWeight: "bold",
                    variant: "pi",
                    children: total
                })
            })
        ]
    });
};

export { DragOverlayChip };
//# sourceMappingURL=DragOverlayChip.mjs.map
