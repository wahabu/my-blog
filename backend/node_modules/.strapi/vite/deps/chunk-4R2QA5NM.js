// node_modules/@strapi/admin/dist/admin/admin/src/utils/users.mjs
var getDisplayName = ({ firstname, lastname, username, email } = {}) => {
  if (username) {
    return username;
  }
  if (firstname) {
    return `${firstname} ${lastname ?? ""}`.trim();
  }
  return email ?? "";
};
var getInitials = (user = {}) => {
  return (user == null ? void 0 : user.firstname) && (user == null ? void 0 : user.lastname) ? `${user.firstname.substring(0, 1)}${user.lastname.substring(0, 1)}` : getDisplayName(user).split(" ").map((name) => name.substring(0, 1)).join("").substring(0, 1).toUpperCase();
};
var hashAdminUserEmail = async (payload) => {
  if (!payload || !payload.email) {
    return null;
  }
  try {
    return await digestMessage(payload.email);
  } catch {
    return null;
  }
};
var bufferToHex = (buffer) => {
  return [
    ...new Uint8Array(buffer)
  ].map((b) => b.toString(16).padStart(2, "0")).join("");
};
var digestMessage = async (message) => {
  const msgUint8 = new TextEncoder().encode(message);
  const hashBuffer = await crypto.subtle.digest("SHA-256", msgUint8);
  return bufferToHex(hashBuffer);
};

export {
  getDisplayName,
  getInitials,
  hashAdminUserEmail
};
//# sourceMappingURL=chunk-4R2QA5NM.js.map
