'use strict';

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var designSystem = require('@strapi/design-system');
var immer = require('immer');
var cloneDeep = require('lodash/cloneDeep');
var get = require('lodash/get');
var has = require('lodash/has');
var isEmpty = require('lodash/isEmpty');
var set = require('lodash/set');
var reactIntl = require('react-intl');
var objects = require('../../../../../utils/objects.js');
var usePermissionsDataManager = require('../hooks/usePermissionsDataManager.js');
var createPermissionChecker = require('../utils/createPermissionChecker.js');
var difference = require('../utils/difference.js');
var forms = require('../utils/forms.js');
var layouts = require('../utils/layouts.js');
var localePermissionValidation = require('../utils/localePermissionValidation.js');
var permissions = require('../utils/permissions.js');
var updateConditionsToFalse = require('../utils/updateConditionsToFalse.js');
var updateValues = require('../utils/updateValues.js');
var ContentTypes = require('./ContentTypes.js');
var PluginsAndSettings = require('./PluginsAndSettings.js');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

function _interopNamespace(e) {
  if (e && e.__esModule) return e;
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      }
    });
  }
  n.default = e;
  return Object.freeze(n);
}

var React__namespace = /*#__PURE__*/_interopNamespace(React);
var cloneDeep__default = /*#__PURE__*/_interopDefault(cloneDeep);
var get__default = /*#__PURE__*/_interopDefault(get);
var has__default = /*#__PURE__*/_interopDefault(has);
var isEmpty__default = /*#__PURE__*/_interopDefault(isEmpty);
var set__default = /*#__PURE__*/_interopDefault(set);

const TAB_LABELS = [
    {
        labelId: 'app.components.LeftMenuLinkContainer.collectionTypes',
        defaultMessage: 'Collection Types',
        id: 'collectionTypes'
    },
    {
        labelId: 'app.components.LeftMenuLinkContainer.singleTypes',
        id: 'singleTypes',
        defaultMessage: 'Single Types'
    },
    {
        labelId: 'app.components.LeftMenuLinkContainer.plugins',
        defaultMessage: 'Plugins',
        id: 'plugins'
    },
    {
        labelId: 'app.components.LeftMenuLinkContainer.settings',
        defaultMessage: 'Settings',
        id: 'settings'
    }
];
const Permissions = /*#__PURE__*/ React__namespace.forwardRef(({ layout, isFormDisabled, onLocaleValidationChange, permissions: permissions$1 = [], userPermissions }, api)=>{
    const [{ initialData, layouts, modifiedData }, dispatch] = React__namespace.useReducer(reducer, initialState, ()=>init(layout, permissions$1));
    const { formatMessage } = reactIntl.useIntl();
    React__namespace.useEffect(()=>{
        onLocaleValidationChange?.(localePermissionValidation.hasLocaleValidationErrors(modifiedData));
    }, [
        modifiedData,
        onLocaleValidationChange
    ]);
    React__namespace.useImperativeHandle(api, ()=>{
        return {
            getPermissions () {
                const collectionTypesDiff = difference.difference(initialData.collectionTypes, modifiedData.collectionTypes);
                const singleTypesDiff = difference.difference(initialData.singleTypes, modifiedData.singleTypes);
                const contentTypesDiff = {
                    ...collectionTypesDiff,
                    ...singleTypesDiff
                };
                let didUpdateConditions;
                if (isEmpty__default.default(contentTypesDiff)) {
                    didUpdateConditions = false;
                } else {
                    didUpdateConditions = Object.values(contentTypesDiff).some((permission = {})=>{
                        return Object.values(permission).some((permissionValue)=>has__default.default(permissionValue, 'conditions'));
                    });
                }
                const permissionsToSend = permissions.formatPermissionsForAPI(modifiedData).map((perm)=>restoreNullLocalesIfUnchanged(perm, permissions$1, initialData, modifiedData));
                return {
                    permissionsToSend,
                    didUpdateConditions
                };
            },
            hasLocaleValidationErrors () {
                return localePermissionValidation.hasLocaleValidationErrors(modifiedData);
            },
            resetForm () {
                dispatch({
                    type: 'RESET_FORM'
                });
            },
            setFormAfterSubmit () {
                dispatch({
                    type: 'SET_FORM_AFTER_SUBMIT'
                });
            }
        };
    });
    const handleChangeCollectionTypeLeftActionRowCheckbox = React__namespace.useCallback((pathToCollectionType, propertyName, rowName, value)=>{
        dispatch({
            type: 'ON_CHANGE_COLLECTION_TYPE_ROW_LEFT_CHECKBOX',
            pathToCollectionType,
            propertyName,
            rowName,
            value,
            userPermissions
        });
    }, [
        userPermissions
    ]);
    const handleChangeCollectionTypeGlobalActionCheckbox = React__namespace.useCallback((collectionTypeKind, actionId, value)=>{
        dispatch({
            type: 'ON_CHANGE_COLLECTION_TYPE_GLOBAL_ACTION_CHECKBOX',
            collectionTypeKind,
            actionId,
            value,
            userPermissions
        });
    }, [
        userPermissions
    ]);
    const handleChangeConditions = React__namespace.useCallback((conditions)=>{
        dispatch({
            type: 'ON_CHANGE_CONDITIONS',
            conditions,
            userPermissions
        });
    }, [
        userPermissions
    ]);
    const handleChangeSimpleCheckbox = React__namespace.useCallback(({ target: { name, value } })=>{
        dispatch({
            type: 'ON_CHANGE_SIMPLE_CHECKBOX',
            keys: name,
            value,
            userPermissions
        });
    }, [
        userPermissions
    ]);
    const handleChangeParentCheckbox = React__namespace.useCallback(({ target: { name, value } })=>{
        dispatch({
            type: 'ON_CHANGE_TOGGLE_PARENT_CHECKBOX',
            keys: name,
            value,
            userPermissions
        });
    }, [
        userPermissions
    ]);
    return /*#__PURE__*/ jsxRuntime.jsx(usePermissionsDataManager.PermissionsDataManagerProvider, {
        availableConditions: layout.conditions,
        modifiedData: modifiedData,
        onChangeConditions: handleChangeConditions,
        onChangeSimpleCheckbox: handleChangeSimpleCheckbox,
        onChangeParentCheckbox: handleChangeParentCheckbox,
        onChangeCollectionTypeLeftActionRowCheckbox: handleChangeCollectionTypeLeftActionRowCheckbox,
        onChangeCollectionTypeGlobalActionCheckbox: handleChangeCollectionTypeGlobalActionCheckbox,
        userPermissions: userPermissions,
        children: /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Tabs.Root, {
            defaultValue: TAB_LABELS[0].id,
            children: [
                /*#__PURE__*/ jsxRuntime.jsx(designSystem.Tabs.List, {
                    "aria-label": formatMessage({
                        id: 'Settings.permissions.users.tabs.label',
                        defaultMessage: 'Tabs Permissions'
                    }),
                    children: TAB_LABELS.map((tabLabel)=>/*#__PURE__*/ jsxRuntime.jsx(designSystem.Tabs.Trigger, {
                            value: tabLabel.id,
                            children: formatMessage({
                                id: tabLabel.labelId,
                                defaultMessage: tabLabel.defaultMessage
                            })
                        }, tabLabel.id))
                }),
                /*#__PURE__*/ jsxRuntime.jsx(designSystem.Tabs.Content, {
                    value: TAB_LABELS[0].id,
                    children: /*#__PURE__*/ jsxRuntime.jsx(ContentTypes.ContentTypes, {
                        layout: layouts.collectionTypes,
                        kind: "collectionTypes",
                        isFormDisabled: isFormDisabled
                    })
                }),
                /*#__PURE__*/ jsxRuntime.jsx(designSystem.Tabs.Content, {
                    value: TAB_LABELS[1].id,
                    children: /*#__PURE__*/ jsxRuntime.jsx(ContentTypes.ContentTypes, {
                        layout: layouts.singleTypes,
                        kind: "singleTypes",
                        isFormDisabled: isFormDisabled
                    })
                }),
                /*#__PURE__*/ jsxRuntime.jsx(designSystem.Tabs.Content, {
                    value: TAB_LABELS[2].id,
                    children: /*#__PURE__*/ jsxRuntime.jsx(PluginsAndSettings.PluginsAndSettingsPermissions, {
                        layout: layouts.plugins,
                        kind: "plugins",
                        isFormDisabled: isFormDisabled
                    })
                }),
                /*#__PURE__*/ jsxRuntime.jsx(designSystem.Tabs.Content, {
                    value: TAB_LABELS[3].id,
                    children: /*#__PURE__*/ jsxRuntime.jsx(PluginsAndSettings.PluginsAndSettingsPermissions, {
                        layout: layouts.settings,
                        kind: "settings",
                        isFormDisabled: isFormDisabled
                    })
                })
            ]
        })
    });
});
const initialState = {
    initialData: {},
    modifiedData: {},
    layouts: {}
};
const buildInheritedConditionsFromExisting = (existing, enabledConditions = [])=>{
    if (!objects.isObject(existing)) {
        return undefined;
    }
    const enabled = new Set(enabledConditions);
    return Object.keys(existing).reduce((acc, key)=>{
        acc[key] = enabled.has(key);
        return acc;
    }, {});
};
const inheritConditionsAtPath = (data, pathToActionObject, actionId, subject, userPermissions)=>{
    if (userPermissions === undefined) {
        return;
    }
    const matchingPermission = userPermissions.find((perm)=>perm.action === actionId && perm.subject === subject);
    if (matchingPermission === undefined) {
        return;
    }
    const obj = data;
    const existingConditions = get__default.default(obj, [
        ...pathToActionObject,
        'conditions'
    ], undefined);
    const nextConditions = buildInheritedConditionsFromExisting(existingConditions, matchingPermission.conditions ?? []);
    if (nextConditions) {
        set__default.default(obj, [
            ...pathToActionObject,
            'conditions'
        ], nextConditions);
    }
};
/* eslint-disable consistent-return */ const reducer = (state, action)=>immer.produce(state, (draftState)=>{
        switch(action.type){
            case 'ON_CHANGE_COLLECTION_TYPE_GLOBAL_ACTION_CHECKBOX':
                {
                    const { collectionTypeKind, actionId, value, userPermissions } = action;
                    const pathToData = [
                        'modifiedData',
                        collectionTypeKind
                    ];
                    Object.keys(get__default.default(state, pathToData)).forEach((collectionType)=>{
                        const collectionTypeActionData = get__default.default(state, [
                            ...pathToData,
                            collectionType,
                            actionId
                        ], undefined);
                        if (collectionTypeActionData) {
                            const subjectPermissionChecker = createPermissionChecker.createFieldPermissionChecker(actionId, collectionType, userPermissions);
                            let updatedValues = updateValues.updateValuesWithPermissions(collectionTypeActionData, value, subjectPermissionChecker);
                            if (value === true) {
                                inheritConditionsAtPath(updatedValues, [], actionId, collectionType, userPermissions);
                            }
                            if (value === false && updatedValues.conditions !== undefined) {
                                // @ts-expect-error – TODO: type better
                                const updatedConditions = updateValues.updateValues(updatedValues.conditions, false);
                                updatedValues = {
                                    ...updatedValues,
                                    conditions: updatedConditions
                                };
                            }
                            set__default.default(draftState, [
                                ...pathToData,
                                collectionType,
                                actionId
                            ], updatedValues);
                        }
                    });
                    break;
                }
            case 'ON_CHANGE_COLLECTION_TYPE_ROW_LEFT_CHECKBOX':
                {
                    const { pathToCollectionType, propertyName, rowName, value, userPermissions } = action;
                    let nextModifiedDataState = cloneDeep__default.default(state.modifiedData);
                    const pathToModifiedDataCollectionType = pathToCollectionType.split('..');
                    const objToUpdate = get__default.default(nextModifiedDataState, pathToModifiedDataCollectionType, {});
                    const subject = pathToModifiedDataCollectionType[pathToModifiedDataCollectionType.length - 1];
                    Object.keys(objToUpdate).forEach((actionId)=>{
                        if (has__default.default(objToUpdate[actionId], `properties.${propertyName}`)) {
                            const objValue = get__default.default(objToUpdate, [
                                actionId,
                                'properties',
                                propertyName,
                                rowName
                            ]);
                            const pathToDataToSet = [
                                ...pathToModifiedDataCollectionType,
                                actionId,
                                'properties',
                                propertyName,
                                rowName
                            ];
                            if (!objects.isObject(objValue)) {
                                if (userPermissions !== undefined && propertyName === 'fields') {
                                    const hasPermission = userPermissions.some((perm)=>{
                                        if (perm.action !== actionId || perm.subject !== subject) return false;
                                        const fields = perm.properties?.fields;
                                        if (fields === null || fields === undefined) return true;
                                        if (Array.isArray(fields) && fields.length === 0) return false;
                                        return Array.isArray(fields) && fields.some((f)=>rowName === f || rowName.startsWith(`${f}.`));
                                    });
                                    if (hasPermission === true) {
                                        set__default.default(nextModifiedDataState, pathToDataToSet, value);
                                        if (value === true) {
                                            inheritConditionsAtPath(nextModifiedDataState, [
                                                ...pathToModifiedDataCollectionType,
                                                actionId
                                            ], actionId, subject, userPermissions);
                                        }
                                    }
                                } else {
                                    set__default.default(nextModifiedDataState, pathToDataToSet, value);
                                    if (value === true && userPermissions !== undefined) {
                                        inheritConditionsAtPath(nextModifiedDataState, [
                                            ...pathToModifiedDataCollectionType,
                                            actionId
                                        ], actionId, subject, userPermissions);
                                    }
                                }
                            } else {
                                const permissionChecker = userPermissions !== undefined && propertyName === 'fields' ? (path)=>{
                                    const fullFieldPath = [
                                        rowName,
                                        ...path
                                    ].join('.');
                                    const checker = createPermissionChecker.createFieldPermissionChecker(actionId, subject, userPermissions);
                                    return checker === undefined ? true : checker([
                                        'properties',
                                        'fields',
                                        fullFieldPath
                                    ]);
                                } : undefined;
                                const updatedValue = updateValues.updateValuesWithPermissions(objValue, value, permissionChecker);
                                set__default.default(nextModifiedDataState, pathToDataToSet, updatedValue);
                                if (value === true && userPermissions !== undefined) {
                                    inheritConditionsAtPath(nextModifiedDataState, [
                                        ...pathToModifiedDataCollectionType,
                                        actionId
                                    ], actionId, subject, userPermissions);
                                }
                            }
                        }
                    });
                    if (value === false) {
                        // @ts-expect-error – TODO: type better
                        nextModifiedDataState = updateConditionsToFalse.updateConditionsToFalse(nextModifiedDataState);
                    }
                    set__default.default(draftState, 'modifiedData', nextModifiedDataState);
                    break;
                }
            case 'ON_CHANGE_CONDITIONS':
                {
                    // In App Token context, conditions are inherited from the user's permissions and must be read-only.
                    if (action.userPermissions !== undefined) {
                        break;
                    }
                    Object.entries(action.conditions).forEach((array)=>{
                        const [stringPathToData, conditionsToUpdate] = array;
                        set__default.default(draftState, [
                            'modifiedData',
                            ...stringPathToData.split('..'),
                            'conditions'
                        ], conditionsToUpdate);
                    });
                    break;
                }
            case 'ON_CHANGE_SIMPLE_CHECKBOX':
                {
                    let nextModifiedDataState = cloneDeep__default.default(state.modifiedData);
                    const keysArray = action.keys.split('..');
                    set__default.default(nextModifiedDataState, [
                        ...keysArray
                    ], action.value);
                    // In App Token context, when enabling a permission, inherit the user's conditions.
                    if (action.value === true && action.userPermissions !== undefined) {
                        const propertiesIndex = keysArray.indexOf('properties');
                        if (propertiesIndex > 0) {
                            const actionId = keysArray[propertiesIndex - 1];
                            const root = keysArray[0];
                            if ((root === 'collectionTypes' || root === 'singleTypes') && keysArray.length >= 3) {
                                const subject = keysArray[1];
                                inheritConditionsAtPath(nextModifiedDataState, [
                                    root,
                                    subject,
                                    actionId
                                ], actionId, subject, action.userPermissions);
                            } else {
                                const pathToActionObject = keysArray.slice(0, propertiesIndex);
                                inheritConditionsAtPath(nextModifiedDataState, pathToActionObject, actionId, null, action.userPermissions);
                            }
                        }
                    }
                    if (action.value === false) {
                        // @ts-expect-error – TODO: type better
                        nextModifiedDataState = updateConditionsToFalse.updateConditionsToFalse(nextModifiedDataState);
                    }
                    set__default.default(draftState, 'modifiedData', nextModifiedDataState);
                    break;
                }
            /*
       * Here the idea is to retrieve a specific value of the modifiedObject
       * then update all the boolean values of the retrieved one
       * and update the drafState.
       *
       * For instance in order to enable create action for all the fields and locales
       * of the restaurant content type we need to :
       * 1. Retrieve the modifiedData.collectionTypes.restaurant.create object
       * 2. Toggle all the end boolean values to the desired one
       * 3. Update the draftState
       *
       * Since the case works well in order to update what we called "parent" checkbox. We can
       * reuse the action when we need to toggle change all the values that depends on this one.
       * A parent checkbox is a checkbox which value is not a boolean but depends on its children ones, therefore,
       * a parent checkbox does not have a represented value in the draftState, they are just helpers.
       *
       * Given the following data:
       *
       * const data = {
       *  restaurant: {
       *   create: {
       *     fields: { name: true },
       *     locales: { en: false }
       *   }
       *  }
       * }
       *
       * The value of the create checkbox for the restaurant will be ƒalse since not all its children have
       * truthy values and in order to set its value to true when need to have all the values of its children set to true.
       *
       * Similarly, we can reuse the logic for the components attributes
       *
       */ case 'ON_CHANGE_TOGGLE_PARENT_CHECKBOX':
                {
                    const { keys, value, userPermissions } = action;
                    const pathToValue = keys.split('..');
                    let nextModifiedDataState = cloneDeep__default.default(state.modifiedData);
                    const oldValues = get__default.default(nextModifiedDataState, pathToValue, {});
                    const root = pathToValue[0];
                    // Plugin & setting permissions are stored with `subject: null` and their real
                    // action id is the full `plugin::`/`admin::` leaf segment (not the UI subcategory).
                    // For those, let the checker derive the action from each leaf path (subject stays null).
                    const isPluginOrSetting = root === 'plugins' || root === 'settings';
                    let actionId;
                    let subject;
                    if (isPluginOrSetting) {
                        subject = null;
                    } else {
                        if (pathToValue.length >= 2) {
                            subject = pathToValue[1];
                        }
                        if (pathToValue.length >= 3) {
                            actionId = pathToValue[2];
                        }
                    }
                    const permissionChecker = createPermissionChecker.createDynamicActionPermissionChecker(subject, actionId, userPermissions);
                    const updatedValues = updateValues.updateValuesWithPermissions(oldValues, value, permissionChecker);
                    // In App Token context, when enabling, inherit conditions from the user's permissions.
                    if (value === true && userPermissions !== undefined) {
                        const subjectForLookup = root === 'collectionTypes' || root === 'singleTypes' ? subject ?? null : null;
                        if (actionId !== undefined && subjectForLookup !== null) {
                            inheritConditionsAtPath(updatedValues, [], actionId, subjectForLookup, userPermissions);
                        } else if (objects.isObject(updatedValues)) {
                            const updatedValuesObj = updatedValues;
                            Object.keys(updatedValuesObj).forEach((key)=>{
                                const maybeActionObj = updatedValuesObj[key];
                                if (objects.isObject(maybeActionObj)) {
                                    inheritConditionsAtPath(maybeActionObj, [], key, subjectForLookup, userPermissions);
                                }
                            });
                        }
                    }
                    set__default.default(nextModifiedDataState, pathToValue, updatedValues);
                    if (value === false) {
                        // @ts-expect-error – TODO: type better
                        nextModifiedDataState = updateConditionsToFalse.updateConditionsToFalse(nextModifiedDataState);
                    }
                    set__default.default(draftState, [
                        'modifiedData'
                    ], nextModifiedDataState);
                    break;
                }
            case 'RESET_FORM':
                {
                    draftState.modifiedData = state.initialData;
                    break;
                }
            case 'SET_FORM_AFTER_SUBMIT':
                {
                    draftState.initialData = state.modifiedData;
                    break;
                }
            default:
                return draftState;
        }
    });
const restoreNullLocalesIfUnchanged = (perm, originalPermissions, initialData, modifiedData)=>{
    if (!perm.subject) return perm;
    const original = originalPermissions.find((p)=>p.action === perm.action && p.subject === perm.subject);
    // null means "all locales" in legacy DBs — preserve it if the user hasn't changed anything
    if (original?.properties?.locales !== null) return perm;
    const kind = perm.subject in modifiedData.collectionTypes ? 'collectionTypes' : perm.subject in modifiedData.singleTypes ? 'singleTypes' : null;
    if (!kind) return perm;
    const initialActionForm = initialData[kind]?.[perm.subject]?.[perm.action];
    const modifiedActionForm = modifiedData[kind]?.[perm.subject]?.[perm.action];
    if (!initialActionForm || !modifiedActionForm) return perm;
    const initialLocaleEntry = initialActionForm.properties['locales'];
    const modifiedLocaleEntry = modifiedActionForm.properties['locales'];
    if (!initialLocaleEntry || typeof initialLocaleEntry === 'boolean' || !modifiedLocaleEntry || typeof modifiedLocaleEntry === 'boolean') {
        return perm;
    }
    const localesUnchanged = Object.keys(initialLocaleEntry).length === Object.keys(modifiedLocaleEntry).length && Object.entries(initialLocaleEntry).every(([locale, val])=>modifiedLocaleEntry[locale] === val);
    if (!localesUnchanged) return perm;
    return {
        ...perm,
        properties: {
            ...perm.properties,
            locales: null
        }
    };
};
/* -------------------------------------------------------------------------------------------------
 * init (reducer)
 * -----------------------------------------------------------------------------------------------*/ const init = (layout, permissions)=>{
    const { conditions, sections: { collectionTypes, singleTypes, plugins, settings } } = layout;
    const layouts$1 = {
        collectionTypes,
        singleTypes,
        plugins: layouts.formatLayout(plugins, 'plugin'),
        settings: layouts.formatLayout(settings, 'category')
    };
    const defaultForm = {
        collectionTypes: forms.createDefaultCTForm(collectionTypes, conditions, permissions),
        singleTypes: forms.createDefaultCTForm(singleTypes, conditions, permissions),
        plugins: forms.createDefaultForm(layouts$1.plugins, conditions, permissions),
        settings: forms.createDefaultForm(layouts$1.settings, conditions, permissions)
    };
    return {
        initialData: defaultForm,
        modifiedData: defaultForm,
        layouts: layouts$1
    };
};

exports.Permissions = Permissions;
//# sourceMappingURL=Permissions.js.map
