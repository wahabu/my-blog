'use strict';

var strapiAdmin = require('@strapi/admin/strapi-admin');
var yup = require('yup');
var getTrad = require('./getTrad.js');

function _interopNamespace(e) {
  if (e && e.__esModule) return e;
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      }
    });
  }
  n.default = e;
  return Object.freeze(n);
}

var yup__namespace = /*#__PURE__*/_interopNamespace(yup);

const MAX_URLS = 20;
const urlSchema = yup__namespace.object().shape({
    urls: yup__namespace.string().test({
        name: 'isUrlValid',
        // eslint-disable-next-line no-template-curly-in-string
        message: '${path}',
        test (values = '') {
            const urls = values.split(/\r?\n/);
            /**
       * Messages are message descriptors rather than bare ids because these translations
       * interpolate values (`{min}`, `{max}`, `{number}`). Without `values`, react-intl
       * fails to format them and renders the raw placeholder. See strapi/strapi#19030.
       */ if (urls.length === 0) {
                return this.createError({
                    path: this.path,
                    message: {
                        ...strapiAdmin.translatedErrors.min,
                        values: {
                            min: 1
                        }
                    }
                });
            }
            if (urls.length > MAX_URLS) {
                return this.createError({
                    path: this.path,
                    message: {
                        ...strapiAdmin.translatedErrors.max,
                        values: {
                            max: MAX_URLS
                        }
                    }
                });
            }
            const filtered = urls.filter((val)=>{
                try {
                    // eslint-disable-next-line no-new
                    new URL(val);
                    return false;
                } catch  {
                    // invalid url
                    return true;
                }
            });
            const filteredLength = filtered.length;
            if (filteredLength === 0) {
                return true;
            }
            const errorMessage = filteredLength > 1 ? {
                id: 'form.upload-url.error.url.invalids',
                defaultMessage: '{number} URLs are invalids'
            } : {
                id: 'form.upload-url.error.url.invalid',
                defaultMessage: 'One URL is invalid'
            };
            return this.createError({
                path: this.path,
                message: {
                    ...errorMessage,
                    id: getTrad.getTrad(errorMessage.id),
                    values: {
                        number: filteredLength
                    }
                }
            });
        }
    })
});

exports.urlSchema = urlSchema;
//# sourceMappingURL=urlYupSchema.js.map
