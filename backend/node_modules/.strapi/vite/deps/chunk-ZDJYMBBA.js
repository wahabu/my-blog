import {
  DocumentActionButton,
  DocumentActionsMenu,
  useDocumentActions,
  useDocumentRBAC
} from "./chunk-O3ADBBC4.js";
import {
  DocumentStatus
} from "./chunk-BCGA3ATX.js";
import {
  buildValidParams,
  createYupSchema,
  isBaseQueryError,
  transformDocument,
  useContentTypeSchema,
  useDoc,
  useDocLayout,
  useDocumentLayout,
  useGetDocumentsForValidationQuery,
  useGetManyDraftRelationCountQuery,
  usePublishManyDocumentsMutation
} from "./chunk-JPBDOX5J.js";
import {
  contentManagerApi
} from "./chunk-CB2UCVBY.js";
import {
  getTranslation
} from "./chunk-BVJQKRXI.js";
import {
  DescriptionComponentRenderer
} from "./chunk-SG3YXALT.js";
import {
  Table,
  useTable
} from "./chunk-XNMTWOF7.js";
import {
  getYupValidationErrors
} from "./chunk-2IWTCEYS.js";
import {
  useAPIErrorHandler
} from "./chunk-WZNNPD7U.js";
import {
  useQueryParams,
  useStrapiApp
} from "./chunk-HHY6TNZ5.js";
import {
  useNotification
} from "./chunk-52FV5OAT.js";
import {
  ValidationError
} from "./chunk-OG6FDUVQ.js";
import {
  require_lib
} from "./chunk-NZZAX2AB.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import {
  Link,
  NavLink,
  useHref,
  useLocation,
  useNavigate
} from "./chunk-QBM2MGBP.js";
import {
  A,
  C,
  Ct,
  Hd,
  Hl,
  Mt,
  Pd,
  S,
  _d,
  be,
  fu,
  kt,
  pu,
  uu
} from "./chunk-7ZODKSNG.js";
import {
  E5,
  Jn,
  R5,
  W2,
  h5,
  nn,
  v5,
  w5,
  y5
} from "./chunk-D4B4CG5U.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  gt
} from "./chunk-4N2AQQ2K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/BulkActions/Actions.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var React3 = __toESM(require_react(), 1);

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/BulkActions/PublishAction.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var React2 = __toESM(require_react(), 1);

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/BulkActions/ConfirmBulkActionDialog.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var ConfirmBulkActionDialog = ({ onToggleDialog, isOpen = false, dialogBody, endAction }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime.jsx)(Pd.Root, {
    open: isOpen,
    children: (0, import_jsx_runtime.jsxs)(Pd.Content, {
      children: [
        (0, import_jsx_runtime.jsx)(Pd.Header, {
          children: formatMessage({
            id: "app.components.ConfirmDialog.title",
            defaultMessage: "Confirmation"
          })
        }),
        (0, import_jsx_runtime.jsx)(Pd.Body, {
          children: (0, import_jsx_runtime.jsxs)(C, {
            direction: "column",
            alignItems: "stretch",
            gap: 2,
            children: [
              (0, import_jsx_runtime.jsx)(C, {
                justifyContent: "center",
                children: (0, import_jsx_runtime.jsx)(Jn, {
                  width: "24px",
                  height: "24px",
                  fill: "danger600"
                })
              }),
              dialogBody
            ]
          })
        }),
        (0, import_jsx_runtime.jsxs)(Pd.Footer, {
          children: [
            (0, import_jsx_runtime.jsx)(Pd.Cancel, {
              children: (0, import_jsx_runtime.jsx)(Ct, {
                width: "50%",
                onClick: onToggleDialog,
                variant: "tertiary",
                children: formatMessage({
                  id: "app.components.Button.cancel",
                  defaultMessage: "Cancel"
                })
              })
            }),
            endAction
          ]
        })
      ]
    })
  });
};
var BoldChunk = (chunks) => (0, import_jsx_runtime.jsx)(A, {
  fontWeight: "bold",
  children: chunks
});
var ConfirmDialogPublishAll = ({ isOpen, onToggleDialog, isConfirmButtonLoading = false, onConfirm }) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const selectedEntries = useTable("ConfirmDialogPublishAll", (state) => state.selectedRows);
  const { toggleNotification } = useNotification();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler(getTranslation);
  const { model } = useDoc();
  const [{ query }] = useQueryParams();
  const { data: countDraftRelations = 0, isLoading, error } = useGetManyDraftRelationCountQuery({
    model,
    documentIds: selectedEntries.map((entry) => entry.documentId),
    locale: (_b = (_a = query == null ? void 0 : query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale
  }, {
    skip: selectedEntries.length === 0
  });
  React.useEffect(() => {
    if (error) {
      toggleNotification({
        type: "danger",
        message: formatAPIError(error)
      });
    }
  }, [
    error,
    formatAPIError,
    toggleNotification
  ]);
  if (error) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(ConfirmBulkActionDialog, {
    isOpen: isOpen && !isLoading,
    onToggleDialog,
    dialogBody: (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {
      children: (0, import_jsx_runtime.jsxs)(A, {
        id: "confirm-description",
        textAlign: "center",
        children: [
          countDraftRelations > 0 && formatMessage({
            id: getTranslation(`popUpwarning.warning.bulk-has-draft-relations.message`),
            defaultMessage: "<b>{count} {count, plural, one { relation } other { relations } } out of {entities} { entities, plural, one { entry } other { entries } } {count, plural, one { is } other { are } }</b> not published yet and might lead to unexpected behavior. "
          }, {
            b: BoldChunk,
            count: countDraftRelations,
            entities: selectedEntries.length
          }),
          formatMessage({
            id: getTranslation("popUpWarning.bodyMessage.contentType.publish.all"),
            defaultMessage: "Are you sure you want to publish these entries?"
          })
        ]
      })
    }),
    endAction: (0, import_jsx_runtime.jsx)(Ct, {
      width: "50%",
      onClick: onConfirm,
      variant: "secondary",
      startIcon: (0, import_jsx_runtime.jsx)(h5, {}),
      loading: isConfirmButtonLoading,
      children: formatMessage({
        id: "app.utils.publish",
        defaultMessage: "Publish"
      })
    })
  });
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/BulkActions/PublishAction.mjs
var TypographyMaxWidth = gt(A)`
  max-width: 300px;
`;
var TableComponent = gt(Hl)`
  width: 100%;
  table-layout: fixed;
  td:first-child {
    border-right: 1px solid ${({ theme }) => theme.colors.neutral150};
  }
  td:first-of-type {
    padding: ${({ theme }) => theme.spaces[4]};
  }
`;
var formatErrorMessages = (errors, parentKey, formatMessage) => {
  if (!errors) return [];
  const messages = [];
  Object.entries(errors).forEach(([key, value]) => {
    const currentKey = parentKey ? `${parentKey}.${key}` : key;
    if (!value) return;
    const isErrorMessageDescriptor = typeof value === "object" && "id" in value && "defaultMessage" in value;
    if (isErrorMessageDescriptor || typeof value === "string") {
      const id = isErrorMessageDescriptor ? value.id : value;
      const defaultMessage = isErrorMessageDescriptor ? value.defaultMessage : value;
      const values = isErrorMessageDescriptor ? value.values : void 0;
      messages.push(formatMessage({
        id: `${id}.withField`,
        defaultMessage
      }, {
        ...values,
        field: currentKey
      }));
    } else {
      messages.push(...formatErrorMessages(value, currentKey, formatMessage));
    }
  });
  return messages;
};
var EntryValidationText = ({ validationErrors, status }) => {
  const { formatMessage } = useIntl();
  if (validationErrors) {
    const validationErrorsMessages = formatErrorMessages(validationErrors, "", formatMessage).join(" ");
    return (0, import_jsx_runtime2.jsxs)(C, {
      gap: 2,
      children: [
        (0, import_jsx_runtime2.jsx)(R5, {
          fill: "danger600"
        }),
        (0, import_jsx_runtime2.jsx)(Mt, {
          label: validationErrorsMessages,
          children: (0, import_jsx_runtime2.jsx)(TypographyMaxWidth, {
            textColor: "danger600",
            variant: "omega",
            fontWeight: "bold",
            ellipsis: true,
            children: validationErrorsMessages
          })
        })
      ]
    });
  }
  if (status === "published") {
    return (0, import_jsx_runtime2.jsxs)(C, {
      gap: 2,
      children: [
        (0, import_jsx_runtime2.jsx)(w5, {
          fill: "success600"
        }),
        (0, import_jsx_runtime2.jsx)(A, {
          textColor: "success600",
          fontWeight: "bold",
          children: formatMessage({
            id: "content-manager.bulk-publish.already-published",
            defaultMessage: "Already Published"
          })
        })
      ]
    });
  }
  if (status === "modified") {
    return (0, import_jsx_runtime2.jsxs)(C, {
      gap: 2,
      children: [
        (0, import_jsx_runtime2.jsx)(W2, {
          fill: "alternative600"
        }),
        (0, import_jsx_runtime2.jsx)(A, {
          textColor: "alternative600",
          fontWeight: "bold",
          children: formatMessage({
            id: "content-manager.bulk-publish.modified",
            defaultMessage: "Ready to publish changes"
          })
        })
      ]
    });
  }
  return (0, import_jsx_runtime2.jsxs)(C, {
    gap: 2,
    children: [
      (0, import_jsx_runtime2.jsx)(w5, {
        fill: "success600"
      }),
      (0, import_jsx_runtime2.jsx)(A, {
        textColor: "success600",
        fontWeight: "bold",
        children: formatMessage({
          id: "app.utils.ready-to-publish",
          defaultMessage: "Ready to publish"
        })
      })
    ]
  });
};
var TABLE_HEADERS = [
  {
    name: "documentId",
    label: "documentId"
  },
  {
    name: "name",
    label: "name"
  },
  {
    name: "status",
    label: "status"
  },
  {
    name: "publicationStatus",
    label: "Publication status"
  }
];
var SelectedEntriesTableContent = ({ isPublishing, rowsToDisplay = [], entriesToPublish = [], validationErrors = {} }) => {
  const { pathname } = useLocation();
  const { formatMessage } = useIntl();
  const { list: { settings: { mainField } } } = useDocLayout();
  const shouldDisplayMainField = mainField != null && mainField !== "id" && mainField !== "documentId";
  return (0, import_jsx_runtime2.jsxs)(Table.Content, {
    children: [
      (0, import_jsx_runtime2.jsxs)(Table.Head, {
        children: [
          (0, import_jsx_runtime2.jsx)(Table.HeaderCheckboxCell, {}),
          TABLE_HEADERS.filter((head) => head.name !== "name" || shouldDisplayMainField).map((head) => (0, import_jsx_runtime2.jsx)(Table.HeaderCell, {
            ...head
          }, head.name))
        ]
      }),
      (0, import_jsx_runtime2.jsx)(Table.Loading, {}),
      (0, import_jsx_runtime2.jsx)(Table.Body, {
        children: rowsToDisplay.map((row) => (0, import_jsx_runtime2.jsxs)(Table.Row, {
          children: [
            (0, import_jsx_runtime2.jsx)(Table.CheckboxCell, {
              id: row.id
            }),
            (0, import_jsx_runtime2.jsx)(Table.Cell, {
              children: (0, import_jsx_runtime2.jsx)(A, {
                children: row.documentId
              })
            }),
            shouldDisplayMainField && (0, import_jsx_runtime2.jsx)(Table.Cell, {
              children: (0, import_jsx_runtime2.jsx)(A, {
                children: row[mainField]
              })
            }),
            (0, import_jsx_runtime2.jsx)(Table.Cell, {
              children: (0, import_jsx_runtime2.jsx)(DocumentStatus, {
                status: row.status,
                maxWidth: "min-content"
              })
            }),
            (0, import_jsx_runtime2.jsx)(Table.Cell, {
              children: isPublishing && entriesToPublish.includes(row.documentId) ? (0, import_jsx_runtime2.jsxs)(C, {
                gap: 2,
                children: [
                  (0, import_jsx_runtime2.jsx)(A, {
                    children: formatMessage({
                      id: "content-manager.success.record.publishing",
                      defaultMessage: "Publishing..."
                    })
                  }),
                  (0, import_jsx_runtime2.jsx)(kt, {
                    small: true
                  })
                ]
              }) : (0, import_jsx_runtime2.jsx)(EntryValidationText, {
                validationErrors: validationErrors[row.documentId],
                status: row.status
              })
            }),
            (0, import_jsx_runtime2.jsx)(Table.Cell, {
              children: (0, import_jsx_runtime2.jsx)(C, {
                children: (0, import_jsx_runtime2.jsx)(be, {
                  tag: Link,
                  to: {
                    pathname: `${pathname}/${row.documentId}`,
                    search: row.locale && `?plugins[i18n][locale]=${row.locale}`
                  },
                  state: {
                    from: pathname
                  },
                  label: formatMessage({
                    id: "content-manager.bulk-publish.edit",
                    defaultMessage: "Edit"
                  }),
                  target: "_blank",
                  marginLeft: "auto",
                  variant: "ghost",
                  children: (0, import_jsx_runtime2.jsx)(nn, {
                    width: "1.6rem",
                    height: "1.6rem"
                  })
                })
              })
            })
          ]
        }, row.id))
      })
    ]
  });
};
var PublicationStatusSummary = ({ count, icon, message, textColor }) => {
  return (0, import_jsx_runtime2.jsxs)(C, {
    justifyContent: "space-between",
    flex: 1,
    gap: 3,
    children: [
      (0, import_jsx_runtime2.jsxs)(C, {
        gap: 2,
        children: [
          icon,
          (0, import_jsx_runtime2.jsx)(A, {
            textColor,
            fontWeight: "bold",
            children: message
          })
        ]
      }),
      (0, import_jsx_runtime2.jsx)(A, {
        fontWeight: "bold",
        children: count
      })
    ]
  });
};
var PublicationStatusGrid = ({ entriesReadyToPublishCount, entriesPublishedCount, entriesModifiedCount, entriesWithErrorsCount }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime2.jsx)(S, {
    hasRadius: true,
    borderColor: "neutral150",
    children: (0, import_jsx_runtime2.jsx)(TableComponent, {
      colCount: 2,
      rowCount: 2,
      children: (0, import_jsx_runtime2.jsxs)(uu, {
        children: [
          (0, import_jsx_runtime2.jsxs)(fu, {
            children: [
              (0, import_jsx_runtime2.jsx)(pu, {
                children: (0, import_jsx_runtime2.jsx)(PublicationStatusSummary, {
                  textColor: "success600",
                  count: entriesReadyToPublishCount,
                  icon: (0, import_jsx_runtime2.jsx)(w5, {
                    fill: "success600"
                  }),
                  message: formatMessage({
                    id: "app.utils.ready-to-publish",
                    defaultMessage: "Ready to publish"
                  })
                })
              }),
              (0, import_jsx_runtime2.jsx)(pu, {
                children: (0, import_jsx_runtime2.jsx)(PublicationStatusSummary, {
                  textColor: "success600",
                  count: entriesPublishedCount,
                  icon: (0, import_jsx_runtime2.jsx)(w5, {
                    fill: "success600"
                  }),
                  message: formatMessage({
                    id: "app.utils.already-published",
                    defaultMessage: "Already published"
                  })
                })
              })
            ]
          }),
          (0, import_jsx_runtime2.jsxs)(fu, {
            children: [
              (0, import_jsx_runtime2.jsx)(pu, {
                children: (0, import_jsx_runtime2.jsx)(PublicationStatusSummary, {
                  textColor: "alternative600",
                  count: entriesModifiedCount,
                  icon: (0, import_jsx_runtime2.jsx)(W2, {
                    fill: "alternative600"
                  }),
                  message: formatMessage({
                    id: "content-manager.bulk-publish.modified",
                    defaultMessage: "Ready to publish changes"
                  })
                })
              }),
              (0, import_jsx_runtime2.jsx)(pu, {
                children: (0, import_jsx_runtime2.jsx)(PublicationStatusSummary, {
                  textColor: "danger600",
                  count: entriesWithErrorsCount,
                  icon: (0, import_jsx_runtime2.jsx)(R5, {
                    fill: "danger600"
                  }),
                  message: formatMessage({
                    id: "content-manager.bulk-publish.waiting-for-action",
                    defaultMessage: "Waiting for action"
                  })
                })
              })
            ]
          })
        ]
      })
    })
  });
};
var SelectedEntriesModalContent = ({ listViewSelectedEntries, toggleModal, setListViewSelectedDocuments, model }) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const { schema, components } = useContentTypeSchema(model);
  const documentIds = listViewSelectedEntries.map(({ documentId }) => documentId);
  const [{ query }] = useQueryParams();
  const params = React2.useMemo(() => buildValidParams(query), [
    query
  ]);
  const { data: documents = [], isLoading, isFetching, refetch } = useGetDocumentsForValidationQuery({
    model,
    documentIds,
    locale: (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale,
    sort: query.sort
  }, {
    skip: documentIds.length === 0
  });
  const { rows, validationErrors } = React2.useMemo(() => {
    if (documents.length > 0 && schema) {
      const validate = createYupSchema(schema.attributes, components, {
        status: "published"
      });
      const transform = transformDocument(schema, components);
      const validationErrors2 = {};
      const documentsMap = new Map(documents.map((doc) => [
        doc.documentId,
        doc
      ]));
      const listViewStatusMap = new Map(listViewSelectedEntries.map(({ documentId, status }) => [
        documentId,
        status
      ]));
      const orderedDocuments = documentIds.map((id) => {
        const doc = documentsMap.get(id);
        if (!doc) return void 0;
        return {
          ...doc,
          status: listViewStatusMap.get(id) ?? doc.status
        };
      }).filter((doc) => doc !== void 0);
      const rows2 = orderedDocuments.map((entry) => {
        const transformed = transform(entry);
        try {
          validate.validateSync(transformed, {
            abortEarly: false
          });
          return entry;
        } catch (e) {
          if (e instanceof ValidationError) {
            validationErrors2[entry.documentId] = getYupValidationErrors(e);
          }
          return entry;
        }
      });
      return {
        rows: rows2,
        validationErrors: validationErrors2
      };
    }
    return {
      rows: [],
      validationErrors: {}
    };
  }, [
    components,
    documents,
    documentIds,
    listViewSelectedEntries,
    schema
  ]);
  const [isDialogOpen, setIsDialogOpen] = React2.useState(false);
  const { publishMany: bulkPublishAction, isLoading: isPublishing } = useDocumentActions();
  const [, { isLoading: isSubmittingForm }] = usePublishManyDocumentsMutation();
  const selectedRows = useTable("publishAction", (state) => state.selectedRows);
  const selectedEntries = rows.filter((entry) => selectedRows.some((selectedEntry) => selectedEntry.documentId === entry.documentId));
  const entriesToPublish = selectedEntries.filter((entry) => !validationErrors[entry.documentId]).map((entry) => entry.documentId);
  const selectedEntriesWithErrorsCount = selectedEntries.filter(({ documentId }) => validationErrors[documentId]).length;
  const selectedEntriesPublishedCount = selectedEntries.filter(({ status }) => status === "published").length;
  const selectedEntriesModifiedCount = selectedEntries.filter(({ status, documentId }) => status === "modified" && !validationErrors[documentId]).length;
  const selectedEntriesWithNoErrorsCount = selectedEntries.length - selectedEntriesWithErrorsCount - selectedEntriesPublishedCount;
  const toggleDialog = () => setIsDialogOpen((prev) => !prev);
  const handleConfirmBulkPublish = async () => {
    toggleDialog();
    const res = await bulkPublishAction({
      model,
      documentIds: entriesToPublish,
      params
    });
    if (!("error" in res)) {
      const unpublishedEntries = rows.filter((row) => {
        return !entriesToPublish.includes(row.documentId);
      });
      setListViewSelectedDocuments(unpublishedEntries);
    }
  };
  return (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, {
    children: [
      (0, import_jsx_runtime2.jsxs)(_d.Body, {
        children: [
          (0, import_jsx_runtime2.jsx)(PublicationStatusGrid, {
            entriesReadyToPublishCount: selectedEntriesWithNoErrorsCount - selectedEntriesModifiedCount,
            entriesPublishedCount: selectedEntriesPublishedCount,
            entriesModifiedCount: selectedEntriesModifiedCount,
            entriesWithErrorsCount: selectedEntriesWithErrorsCount
          }),
          (0, import_jsx_runtime2.jsx)(S, {
            marginTop: 7,
            children: (0, import_jsx_runtime2.jsx)(SelectedEntriesTableContent, {
              isPublishing: isSubmittingForm,
              rowsToDisplay: rows,
              entriesToPublish,
              validationErrors
            })
          })
        ]
      }),
      (0, import_jsx_runtime2.jsxs)(_d.Footer, {
        children: [
          (0, import_jsx_runtime2.jsx)(Ct, {
            onClick: toggleModal,
            variant: "tertiary",
            children: formatMessage({
              id: "app.components.Button.cancel",
              defaultMessage: "Cancel"
            })
          }),
          (0, import_jsx_runtime2.jsxs)(C, {
            gap: 2,
            children: [
              (0, import_jsx_runtime2.jsx)(Ct, {
                onClick: refetch,
                variant: "tertiary",
                loading: isFetching,
                children: formatMessage({
                  id: "app.utils.refresh",
                  defaultMessage: "Refresh"
                })
              }),
              (0, import_jsx_runtime2.jsx)(Ct, {
                onClick: toggleDialog,
                disabled: selectedEntries.length === 0 || selectedEntries.length === selectedEntriesWithErrorsCount || selectedEntriesPublishedCount === selectedEntries.length || isLoading,
                loading: isPublishing || isSubmittingForm,
                children: formatMessage({
                  id: "app.utils.publish",
                  defaultMessage: "Publish"
                })
              })
            ]
          })
        ]
      }),
      (0, import_jsx_runtime2.jsx)(ConfirmDialogPublishAll, {
        isOpen: isDialogOpen,
        onToggleDialog: toggleDialog,
        isConfirmButtonLoading: isPublishing || isSubmittingForm,
        onConfirm: handleConfirmBulkPublish
      })
    ]
  });
};
var PublishAction = ({ documents, model }) => {
  const { formatMessage } = useIntl();
  const hasPublishPermission = useDocumentRBAC("unpublishAction", (state) => state.canPublish);
  const showPublishButton = hasPublishPermission && documents.some(({ status }) => status !== "published");
  const setListViewSelectedDocuments = useTable("publishAction", (state) => state.selectRow);
  const refetchList = () => {
    contentManagerApi.util.invalidateTags([
      {
        type: "Document",
        id: `${model}_LIST`
      }
    ]);
  };
  if (!showPublishButton) return null;
  return {
    actionType: "publish",
    variant: "tertiary",
    label: formatMessage({
      id: "app.utils.publish",
      defaultMessage: "Publish"
    }),
    dialog: {
      type: "modal",
      title: formatMessage({
        id: getTranslation("containers.ListPage.selectedEntriesModal.title"),
        defaultMessage: "Publish entries"
      }),
      content: ({ onClose }) => {
        return (0, import_jsx_runtime2.jsx)(Table.Root, {
          rows: documents,
          defaultSelectedRows: documents,
          headers: TABLE_HEADERS,
          children: (0, import_jsx_runtime2.jsx)(SelectedEntriesModalContent, {
            listViewSelectedEntries: documents,
            toggleModal: () => {
              onClose();
              refetchList();
            },
            setListViewSelectedDocuments,
            model
          })
        });
      },
      onClose: () => {
        refetchList();
      }
    }
  };
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/BulkActions/Actions.mjs
var BulkActionsRenderer = () => {
  const plugins = useStrapiApp("BulkActionsRenderer", (state) => state.plugins);
  const { model, collectionType } = useDoc();
  const { list } = useDocumentLayout(model);
  const { selectedRows } = useTable("BulkActionsRenderer", (state) => state);
  const selectedDocuments = selectedRows;
  return (0, import_jsx_runtime3.jsx)(C, {
    gap: 2,
    children: (0, import_jsx_runtime3.jsx)(DescriptionComponentRenderer, {
      props: {
        model,
        collectionType,
        documents: selectedDocuments
      },
      descriptions: plugins["content-manager"].apis.getBulkActions(),
      children: (actions) => actions.map(({ type: _bulkType, ...action }) => {
        return list.settings.bulkable && (0, import_jsx_runtime3.jsx)(DocumentActionButton, {
          ...action
        }, action.id);
      })
    })
  });
};
var DeleteAction = ({ documents, model }) => {
  var _a;
  const { formatMessage } = useIntl();
  const { schema: contentType } = useDoc();
  const selectRow = useTable("DeleteAction", (state) => state.selectRow);
  const hasI18nEnabled = Boolean((_a = contentType == null ? void 0 : contentType.pluginOptions) == null ? void 0 : _a.i18n);
  const [{ query }] = useQueryParams();
  const params = React3.useMemo(() => buildValidParams(query), [
    query
  ]);
  const hasDeletePermission = useDocumentRBAC("deleteAction", (state) => state.canDelete);
  const { deleteMany: bulkDeleteAction, isLoading } = useDocumentActions();
  const documentIds = documents.map(({ documentId }) => documentId);
  const handleConfirmBulkDelete = async () => {
    const res = await bulkDeleteAction({
      documentIds,
      model,
      params
    });
    if (!("error" in res)) {
      selectRow([]);
    }
  };
  if (!hasDeletePermission) return null;
  return {
    variant: "danger-light",
    label: formatMessage({
      id: "global.delete",
      defaultMessage: "Delete"
    }),
    dialog: {
      type: "dialog",
      title: formatMessage({
        id: "app.components.ConfirmDialog.title",
        defaultMessage: "Confirmation"
      }),
      loading: isLoading,
      content: (0, import_jsx_runtime3.jsxs)(C, {
        direction: "column",
        alignItems: "stretch",
        gap: 2,
        children: [
          (0, import_jsx_runtime3.jsx)(C, {
            justifyContent: "center",
            children: (0, import_jsx_runtime3.jsx)(Jn, {
              width: "24px",
              height: "24px",
              fill: "danger600"
            })
          }),
          (0, import_jsx_runtime3.jsx)(A, {
            id: "confirm-description",
            textAlign: "center",
            children: formatMessage({
              id: "popUpWarning.bodyMessage.contentType.delete.all",
              defaultMessage: "Are you sure you want to delete these entries?"
            })
          }),
          hasI18nEnabled && (0, import_jsx_runtime3.jsx)(S, {
            textAlign: "center",
            padding: 3,
            children: (0, import_jsx_runtime3.jsx)(A, {
              textColor: "danger500",
              children: formatMessage({
                id: getTranslation("Settings.list.actions.deleteAdditionalInfos"),
                defaultMessage: "This will delete the active locale versions <em>(from Internationalization)</em>"
              }, {
                em: Emphasis
              })
            })
          })
        ]
      }),
      onConfirm: handleConfirmBulkDelete
    }
  };
};
DeleteAction.type = "delete";
var UnpublishAction = ({ documents, model }) => {
  var _a;
  const { formatMessage } = useIntl();
  const { schema } = useDoc();
  const selectRow = useTable("UnpublishAction", (state) => state.selectRow);
  const hasPublishPermission = useDocumentRBAC("unpublishAction", (state) => state.canPublish);
  const hasDraftAndPublishEnabled = Boolean((_a = schema == null ? void 0 : schema.options) == null ? void 0 : _a.draftAndPublish);
  const { unpublishMany: bulkUnpublishAction, isLoading } = useDocumentActions();
  const documentIds = documents.map(({ documentId }) => documentId);
  const [{ query }] = useQueryParams();
  const params = React3.useMemo(() => buildValidParams(query), [
    query
  ]);
  const handleConfirmBulkUnpublish = async () => {
    const data = await bulkUnpublishAction({
      documentIds,
      model,
      params
    });
    if (!("error" in data)) {
      selectRow([]);
    }
  };
  const showUnpublishButton = hasDraftAndPublishEnabled && hasPublishPermission && documents.some((entry) => entry.status === "published" || entry.status === "modified");
  if (!showUnpublishButton) return null;
  return {
    variant: "tertiary",
    label: formatMessage({
      id: "app.utils.unpublish",
      defaultMessage: "Unpublish"
    }),
    dialog: {
      type: "dialog",
      title: formatMessage({
        id: "app.components.ConfirmDialog.title",
        defaultMessage: "Confirmation"
      }),
      loading: isLoading,
      content: (0, import_jsx_runtime3.jsxs)(C, {
        direction: "column",
        alignItems: "stretch",
        gap: 2,
        children: [
          (0, import_jsx_runtime3.jsx)(C, {
            justifyContent: "center",
            children: (0, import_jsx_runtime3.jsx)(Jn, {
              width: "24px",
              height: "24px",
              fill: "danger600"
            })
          }),
          (0, import_jsx_runtime3.jsx)(A, {
            id: "confirm-description",
            textAlign: "center",
            children: formatMessage({
              id: "popUpWarning.bodyMessage.contentType.unpublish.all",
              defaultMessage: "Are you sure you want to unpublish these entries?"
            })
          })
        ]
      }),
      confirmButton: formatMessage({
        id: "app.utils.unpublish",
        defaultMessage: "Unpublish"
      }),
      onConfirm: handleConfirmBulkUnpublish
    }
  };
};
UnpublishAction.type = "unpublish";
var Emphasis = (chunks) => (0, import_jsx_runtime3.jsx)(A, {
  fontWeight: "semiBold",
  textColor: "danger500",
  children: chunks
});
var DEFAULT_BULK_ACTIONS = [
  PublishAction,
  UnpublishAction,
  DeleteAction
];

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/TableActions.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var React4 = __toESM(require_react(), 1);
var import_qs = __toESM(require_lib(), 1);

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/AutoCloneFailureModal.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var AutoCloneFailureModalBody = ({ prohibitedFields }) => {
  const { formatMessage } = useIntl();
  const getDefaultErrorMessage = (reason) => {
    switch (reason) {
      case "relation":
        return "Duplicating the relation could remove it from the original entry.";
      case "unique":
        return "Identical values in a unique field are not allowed";
      default:
        return reason;
    }
  };
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, {
    children: [
      (0, import_jsx_runtime4.jsx)(A, {
        variant: "beta",
        children: formatMessage({
          id: getTranslation("containers.list.autoCloneModal.title"),
          defaultMessage: "This entry can't be duplicated directly."
        })
      }),
      (0, import_jsx_runtime4.jsx)(S, {
        marginTop: 2,
        children: (0, import_jsx_runtime4.jsx)(A, {
          textColor: "neutral600",
          children: formatMessage({
            id: getTranslation("containers.list.autoCloneModal.description"),
            defaultMessage: "A new entry will be created with the same content, but you'll have to change the following fields to save it."
          })
        })
      }),
      (0, import_jsx_runtime4.jsx)(C, {
        marginTop: 6,
        gap: 2,
        direction: "column",
        alignItems: "stretch",
        children: prohibitedFields.map(([fieldPath, reason]) => (0, import_jsx_runtime4.jsxs)(C, {
          direction: "column",
          gap: 2,
          alignItems: "flex-start",
          borderColor: "neutral200",
          hasRadius: true,
          padding: 6,
          children: [
            (0, import_jsx_runtime4.jsx)(C, {
              direction: "row",
              tag: "ol",
              children: fieldPath.map((pathSegment, index) => (0, import_jsx_runtime4.jsxs)(A, {
                fontWeight: "semiBold",
                tag: "li",
                children: [
                  pathSegment,
                  index !== fieldPath.length - 1 && (0, import_jsx_runtime4.jsx)(v5, {
                    fill: "neutral500",
                    height: "0.8rem",
                    width: "0.8rem",
                    style: {
                      margin: "0 0.8rem"
                    }
                  })
                ]
              }, index))
            }),
            (0, import_jsx_runtime4.jsx)(A, {
              tag: "p",
              textColor: "neutral600",
              children: formatMessage({
                id: getTranslation(`containers.list.autoCloneModal.error.${reason}`),
                defaultMessage: getDefaultErrorMessage(reason)
              })
            })
          ]
        }, fieldPath.join()))
      })
    ]
  });
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/TableActions.mjs
var TableActions = ({ document }) => {
  const { formatMessage } = useIntl();
  const { model, collectionType } = useDoc();
  const plugins = useStrapiApp("TableActions", (state) => state.plugins);
  const props = {
    activeTab: null,
    model,
    documentId: document.documentId,
    collectionType,
    document
  };
  return (0, import_jsx_runtime5.jsx)(DescriptionComponentRenderer, {
    props,
    descriptions: plugins["content-manager"].apis.getDocumentActions("table-row").filter((action) => action.name !== "PublishAction"),
    children: (actions) => {
      const tableRowActions = actions.filter((action) => {
        const positions = Array.isArray(action.position) ? action.position : [
          action.position
        ];
        return positions.includes("table-row");
      });
      return (0, import_jsx_runtime5.jsx)(DocumentActionsMenu, {
        actions: tableRowActions,
        label: formatMessage({
          id: "content-manager.containers.list.table.row-actions",
          defaultMessage: "Row actions"
        }),
        variant: "ghost"
      });
    }
  });
};
var EditAction = ({ documentId }) => {
  const navigate = useNavigate();
  const { formatMessage } = useIntl();
  const { canRead } = useDocumentRBAC("EditAction", ({ canRead: canRead2 }) => ({
    canRead: canRead2
  }));
  const { toggleNotification } = useNotification();
  const [{ query }] = useQueryParams();
  return {
    disabled: !canRead,
    icon: (0, import_jsx_runtime5.jsx)(StyledPencil, {}),
    label: formatMessage({
      id: "content-manager.actions.edit.label",
      defaultMessage: "Edit"
    }),
    position: "table-row",
    onClick: async () => {
      if (!documentId) {
        console.error("You're trying to edit a document without an id, this is likely a bug with Strapi. Please open an issue.");
        toggleNotification({
          message: formatMessage({
            id: "content-manager.actions.edit.error",
            defaultMessage: "An error occurred while trying to edit the document."
          }),
          type: "danger"
        });
        return;
      }
      navigate({
        pathname: documentId,
        search: (0, import_qs.stringify)({
          plugins: query.plugins
        })
      });
    }
  };
};
EditAction.type = "edit";
EditAction.position = "table-row";
var StyledPencil = gt(nn)`
  path {
    fill: currentColor;
  }
`;
var OpenInNewTabAction = ({ documentId }) => {
  const { formatMessage } = useIntl();
  const { canRead } = useDocumentRBAC("OpenInNewTabAction", ({ canRead: canRead2 }) => ({
    canRead: canRead2
  }));
  const [{ query }] = useQueryParams();
  const href = useHref({
    pathname: documentId ?? "",
    search: (0, import_qs.stringify)({
      plugins: query.plugins
    })
  });
  return {
    disabled: !canRead || !documentId,
    icon: (0, import_jsx_runtime5.jsx)(StyledExternalLink, {}),
    label: formatMessage({
      id: "content-manager.actions.open-in-new-tab.label",
      defaultMessage: "Open in new tab"
    }),
    position: "table-row",
    onClick: () => {
      window.open(href, "_blank", "noopener,noreferrer");
    }
  };
};
OpenInNewTabAction.position = "table-row";
var StyledExternalLink = gt(E5)`
  path {
    fill: currentColor;
  }
`;
var CloneAction = ({ model, documentId }) => {
  const navigate = useNavigate();
  const { formatMessage } = useIntl();
  const { canCreate } = useDocumentRBAC("CloneAction", ({ canCreate: canCreate2 }) => ({
    canCreate: canCreate2
  }));
  const { toggleNotification } = useNotification();
  const { autoClone } = useDocumentActions();
  const [prohibitedFields, setProhibitedFields] = React4.useState([]);
  const [{ query }] = useQueryParams();
  return {
    disabled: !canCreate,
    icon: (0, import_jsx_runtime5.jsx)(StyledDuplicate, {}),
    label: formatMessage({
      id: "content-manager.actions.clone.label",
      defaultMessage: "Duplicate"
    }),
    position: "table-row",
    onClick: async () => {
      var _a, _b;
      if (!documentId) {
        console.error("You're trying to clone a document in the table without an id, this is likely a bug with Strapi. Please open an issue.");
        toggleNotification({
          message: formatMessage({
            id: "content-manager.actions.clone.error",
            defaultMessage: "An error occurred while trying to clone the document."
          }),
          type: "danger"
        });
        return;
      }
      const res = await autoClone({
        model,
        sourceId: documentId,
        locale: (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale
      });
      if ("data" in res) {
        navigate({
          pathname: res.data.documentId,
          search: (0, import_qs.stringify)({
            plugins: query.plugins
          })
        });
        return true;
      }
      if (isBaseQueryError(res.error) && res.error.details && typeof res.error.details === "object" && "prohibitedFields" in res.error.details && Array.isArray(res.error.details.prohibitedFields)) {
        const prohibitedFields2 = res.error.details.prohibitedFields;
        setProhibitedFields(prohibitedFields2);
      }
    },
    dialog: {
      type: "modal",
      title: formatMessage({
        id: "content-manager.containers.list.autoCloneModal.header",
        defaultMessage: "Duplicate"
      }),
      content: (0, import_jsx_runtime5.jsx)(AutoCloneFailureModalBody, {
        prohibitedFields
      }),
      footer: ({ onClose }) => {
        return (0, import_jsx_runtime5.jsxs)(_d.Footer, {
          children: [
            (0, import_jsx_runtime5.jsx)(Ct, {
              onClick: onClose,
              variant: "tertiary",
              children: formatMessage({
                id: "cancel",
                defaultMessage: "Cancel"
              })
            }),
            (0, import_jsx_runtime5.jsx)(Hd, {
              tag: NavLink,
              to: {
                pathname: `clone/${documentId}`,
                search: (0, import_qs.stringify)({
                  plugins: query.plugins
                })
              },
              children: formatMessage({
                id: "content-manager.containers.list.autoCloneModal.create",
                defaultMessage: "Create"
              })
            })
          ]
        });
      }
    }
  };
};
CloneAction.type = "clone";
CloneAction.position = "table-row";
var StyledDuplicate = gt(y5)`
  path {
    fill: currentColor;
  }
`;
var DEFAULT_TABLE_ROW_ACTIONS = [
  EditAction,
  OpenInNewTabAction,
  CloneAction
];

export {
  BulkActionsRenderer,
  DEFAULT_BULK_ACTIONS,
  TableActions,
  DEFAULT_TABLE_ROW_ACTIONS
};
//# sourceMappingURL=chunk-ZDJYMBBA.js.map
