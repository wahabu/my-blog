'use strict';

const buildBulkMovePayload = (items)=>{
    const fileIds = [];
    const folderIds = [];
    for (const item of items){
        if (item.kind === 'file') {
            fileIds.push(item.id);
        } else {
            folderIds.push(item.id);
        }
    }
    return {
        fileIds,
        folderIds
    };
};

exports.buildBulkMovePayload = buildBulkMovePayload;
//# sourceMappingURL=buildBulkMovePayload.js.map
