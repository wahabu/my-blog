'use strict';

var isEqual = require('lodash/isEqual');
var isObject = require('lodash/isObject');
var transform = require('lodash/transform');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var isEqual__default = /*#__PURE__*/_interopDefault(isEqual);
var isObject__default = /*#__PURE__*/_interopDefault(isObject);
var transform__default = /*#__PURE__*/_interopDefault(transform);

function difference(object, base) {
    function changes(object, base) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        return transform__default.default(object, (result, value, key)=>{
            if (!isEqual__default.default(value, base[key])) {
                result[key] = isObject__default.default(value) && isObject__default.default(base[key]) ? changes(value, base[key]) : value;
            }
            return result;
        });
    }
    return changes(object, base);
}

exports.difference = difference;
//# sourceMappingURL=difference.js.map
