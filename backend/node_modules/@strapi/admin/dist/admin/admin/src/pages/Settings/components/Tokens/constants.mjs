import * as yup from 'yup';
import { translatedErrors as errorsTrads } from '../../../../utils/translatedErrors.mjs';

const API_TOKEN_TYPE = 'api-token';
const TRANSFER_TOKEN_TYPE = 'transfer-token';
const apiTokenInformationSchema = yup.object().shape({
    name: yup.string().max(100).required(errorsTrads.required.id),
    type: yup.string().oneOf([
        'read-only',
        'full-access',
        'custom'
    ]).optional(),
    description: yup.string().nullable(),
    lifespan: yup.number().integer().min(0).nullable().defined(errorsTrads.required.id)
});

export { API_TOKEN_TYPE, TRANSFER_TOKEN_TYPE, apiTokenInformationSchema };
//# sourceMappingURL=constants.mjs.map
