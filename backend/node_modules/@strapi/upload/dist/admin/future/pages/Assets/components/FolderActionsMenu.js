'use strict';

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var strapiAdmin = require('@strapi/admin/strapi-admin');
var designSystem = require('@strapi/design-system');
var icons = require('@strapi/icons');
var reactIntl = require('react-intl');
var translations = require('../../../utils/translations.js');
var useAssetSelection = require('../hooks/useAssetSelection.js');
var selection = require('../utils/selection.js');
var ActionsMenuContent = require('./ActionsMenuContent.js');
var BulkMoveDialog = require('./BulkMoveDialog.js');
var DeleteItemsDialog = require('./DeleteItemsDialog.js');
var FolderFormDialog = require('./FolderFormDialog.js');

/**
 * The "..." menu on a folder row/card. Always acts on that one folder, whatever
 * the current multi-selection is (the bulk actions bar is the selection-scoped
 * affordance). It reuses the shared move and delete dialogs, which mount only
 * while open.
 *
 * A successful move or delete deselects this one folder and leaves the rest of
 * the selection intact: they only invalidate RTK tags, so without it a
 * `folder:<id>` key for a folder that no longer lives where the selection thinks
 * it does would linger. A rename leaves the id valid and the folder in place, so
 * it touches the selection not at all.
 */ const FolderActionsMenu = ({ folder, dragData })=>{
    const { formatMessage } = reactIntl.useIntl();
    const { copy } = strapiAdmin.useClipboard();
    const { toggleNotification } = strapiAdmin.useNotification();
    const { deselect } = useAssetSelection.useAssetSelection();
    const [isRenameOpen, setIsRenameOpen] = React.useState(false);
    const [isMoveOpen, setIsMoveOpen] = React.useState(false);
    const [isDeleteOpen, setIsDeleteOpen] = React.useState(false);
    // Stable identity: the move dialog memoizes its destination walk on it.
    const moveItems = React.useMemo(()=>[
            dragData
        ], [
        dragData
    ]);
    // Built from window.location so the link inherits whatever path the library is
    // mounted at. Every other query param (search, sort, open drawer) is dropped —
    // this is a clean deep-link to the folder.
    const handleCopyLink = async ()=>{
        const url = `${window.location.origin}${window.location.pathname}?folder=${folder.id}`;
        const didCopy = await copy(url);
        toggleNotification({
            type: didCopy ? 'success' : 'danger',
            message: formatMessage(didCopy ? {
                id: translations.getTranslationKey('list.folder.actions.copy-link.success'),
                defaultMessage: 'Folder link copied.'
            } : {
                id: translations.getTranslationKey('list.folder.actions.copy-link.error'),
                defaultMessage: 'Failed to copy the folder link.'
            })
        });
    };
    return /*#__PURE__*/ jsxRuntime.jsxs(jsxRuntime.Fragment, {
        children: [
            /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Menu.Root, {
                modal: false,
                children: [
                    /*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Trigger, {
                        tag: designSystem.IconButton,
                        icon: /*#__PURE__*/ jsxRuntime.jsx(icons.More, {}),
                        variant: "ghost",
                        label: formatMessage({
                            id: translations.getTranslationKey('control-card.more-actions'),
                            defaultMessage: 'More actions'
                        })
                    }),
                    /*#__PURE__*/ jsxRuntime.jsxs(ActionsMenuContent.ActionsMenuContent, {
                        popoverPlacement: "bottom-end",
                        zIndex: 2,
                        minWidth: "22rem",
                        children: [
                            /*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Item, {
                                startIcon: /*#__PURE__*/ jsxRuntime.jsx(icons.Link, {}),
                                onSelect: handleCopyLink,
                                children: formatMessage({
                                    id: translations.getTranslationKey('list.folder.actions.copy-link'),
                                    defaultMessage: 'Copy link to folder'
                                })
                            }),
                            /*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Separator, {}),
                            /*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Item, {
                                startIcon: /*#__PURE__*/ jsxRuntime.jsx(icons.Pencil, {}),
                                onSelect: ()=>setIsRenameOpen(true),
                                children: formatMessage({
                                    id: translations.getTranslationKey('list.folder.actions.rename'),
                                    defaultMessage: 'Rename folder'
                                })
                            }),
                            /*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Item, {
                                startIcon: /*#__PURE__*/ jsxRuntime.jsx(icons.ArrowRight, {}),
                                onSelect: ()=>setIsMoveOpen(true),
                                children: formatMessage({
                                    id: translations.getTranslationKey('list.folder.actions.move'),
                                    defaultMessage: 'Move to folder'
                                })
                            }),
                            /*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Item, {
                                startIcon: /*#__PURE__*/ jsxRuntime.jsx(icons.Trash, {}),
                                variant: "danger",
                                onSelect: ()=>setIsDeleteOpen(true),
                                children: formatMessage({
                                    id: translations.getTranslationKey('list.folder.actions.delete'),
                                    defaultMessage: 'Delete folder'
                                })
                            })
                        ]
                    })
                ]
            }),
            isRenameOpen && /*#__PURE__*/ jsxRuntime.jsx(FolderFormDialog.FolderFormDialog, {
                open: true,
                mode: "rename",
                folderId: folder.id,
                initialName: folder.name,
                parentFolderId: dragData.parentId,
                onClose: ()=>setIsRenameOpen(false)
            }),
            isMoveOpen && /*#__PURE__*/ jsxRuntime.jsx(BulkMoveDialog.BulkMoveDialog, {
                open: true,
                onClose: ()=>setIsMoveOpen(false),
                items: moveItems,
                onSuccess: ()=>deselect(selection.folderKey(folder.id))
            }),
            isDeleteOpen && /*#__PURE__*/ jsxRuntime.jsx(DeleteItemsDialog.DeleteItemsDialog, {
                open: true,
                onClose: ()=>setIsDeleteOpen(false),
                target: {
                    fileIds: [],
                    folderIds: [
                        folder.id
                    ]
                },
                onSuccess: ()=>deselect(selection.folderKey(folder.id))
            })
        ]
    });
};

exports.FolderActionsMenu = FolderActionsMenu;
//# sourceMappingURL=FolderActionsMenu.js.map
