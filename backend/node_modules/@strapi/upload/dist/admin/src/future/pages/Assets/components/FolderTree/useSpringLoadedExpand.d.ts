export declare const SPRING_LOAD_DELAY_MS = 600;
export declare const useSpringLoadedExpand: ({ isOver, canExpand, onExpand, }: {
    isOver: boolean;
    canExpand: boolean;
    onExpand: () => void;
}) => void;
