import {
  require_transform
} from "./chunk-RXFHXA76.js";
import {
  require_groupBy
} from "./chunk-OJIMQOYL.js";
import {
  require_isEmpty
} from "./chunk-SFCV45I7.js";
import {
  require_set
} from "./chunk-I4RARRLM.js";
import {
  require_merge,
  require_omit
} from "./chunk-BC4MOUEJ.js";
import {
  require_cloneDeep
} from "./chunk-RDVCP4FY.js";
import {
  require_has,
  require_upperFirst
} from "./chunk-OG6FDUVQ.js";
import {
  capitalise
} from "./chunk-D5MNZJMA.js";
import {
  require_isEqual
} from "./chunk-KVWVHPCR.js";
import {
  require_get
} from "./chunk-NZZAX2AB.js";
import {
  isObject
} from "./chunk-RIFP47FT.js";
import {
  require_isObject
} from "./chunk-NR6XEPE4.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import {
  fn
} from "./chunk-LHXIEHKN.js";
import {
  A,
  Bt,
  C,
  Ca,
  Ct,
  Ld,
  S,
  _d,
  cu,
  dd,
  eu,
  va
} from "./chunk-7ZODKSNG.js";
import {
  C5,
  g5,
  o5,
  p2
} from "./chunk-D4B4CG5U.js";
import {
  $c512c27ab02ef895$export$fd42f52fd3ae1109
} from "./chunk-VQHBTUHH.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  ft,
  gt
} from "./chunk-4N2AQQ2K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/Permissions.mjs
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var React8 = __toESM(require_react(), 1);
var import_cloneDeep = __toESM(require_cloneDeep(), 1);
var import_get6 = __toESM(require_get(), 1);
var import_has2 = __toESM(require_has(), 1);
var import_isEmpty3 = __toESM(require_isEmpty(), 1);
var import_set = __toESM(require_set(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/hooks/usePermissionsDataManager.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var [PermissionsDataManagerProviderRaw, usePermissionsDataManagerContext] = $c512c27ab02ef895$export$fd42f52fd3ae1109("PermissionsDataManager");
var usePermissionsDataManager = () => usePermissionsDataManagerContext("usePermissionsDataManager");
var PermissionsDataManagerProvider = ({ children, userPermissions, availableConditions, modifiedData, onChangeConditions, onChangeSimpleCheckbox, onChangeParentCheckbox, onChangeCollectionTypeLeftActionRowCheckbox, onChangeCollectionTypeGlobalActionCheckbox }) => {
  const checkUserHasPermission = React.useCallback((action, subject, field) => {
    var _a;
    if (userPermissions === void 0) {
      return true;
    }
    const matchingPermission = userPermissions.find((perm) => perm.action === action && perm.subject === subject);
    if (matchingPermission === void 0) {
      return false;
    }
    if (field === void 0) {
      return true;
    }
    const fields = (_a = matchingPermission.properties) == null ? void 0 : _a.fields;
    if (fields === null || fields === void 0) {
      return true;
    }
    if (Array.isArray(fields) === false || fields.length === 0) {
      return false;
    }
    return fields.some((allowedField) => allowedField === field || field.startsWith(`${allowedField}.`));
  }, [
    userPermissions
  ]);
  return (0, import_jsx_runtime.jsx)(PermissionsDataManagerProviderRaw, {
    availableConditions,
    modifiedData,
    onChangeConditions,
    onChangeSimpleCheckbox,
    onChangeParentCheckbox,
    onChangeCollectionTypeLeftActionRowCheckbox,
    onChangeCollectionTypeGlobalActionCheckbox,
    userPermissions,
    checkUserHasPermission,
    children
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/createPermissionChecker.mjs
var createFieldPermissionChecker = (actionId, subject, userPermissions) => {
  if (userPermissions === void 0) {
    return void 0;
  }
  return (path) => {
    var _a;
    const matchingPerm = userPermissions.find((perm) => perm.action === actionId && perm.subject === subject);
    if (matchingPerm === void 0) {
      return false;
    }
    const fieldsIndex = path.indexOf("fields");
    if (fieldsIndex === -1 || fieldsIndex >= path.length - 1) {
      return true;
    }
    const fieldPath = path.slice(fieldsIndex + 1).join(".");
    const fields = (_a = matchingPerm.properties) == null ? void 0 : _a.fields;
    if (fields === null || fields === void 0) {
      return true;
    }
    if (Array.isArray(fields) && fields.length === 0) {
      return false;
    }
    if (Array.isArray(fields)) {
      return fields.some((allowedField) => fieldPath === allowedField || fieldPath.startsWith(`${allowedField}.`));
    }
    return false;
  };
};
var createDynamicActionPermissionChecker = (subject, actionIdFromContext, userPermissions) => {
  if (userPermissions === void 0 || subject === void 0) {
    return void 0;
  }
  return (path) => {
    var _a;
    let currentActionId = actionIdFromContext;
    let adjustedPath = path;
    if (currentActionId === void 0 && path.length > 0) {
      if (path[0].includes("plugin::") || path[0].includes("admin::")) {
        currentActionId = path[0];
        adjustedPath = path.slice(1);
      }
    }
    if (currentActionId === void 0) {
      return false;
    }
    const matchingPerm = userPermissions.find((perm) => perm.action === currentActionId && perm.subject === subject);
    if (matchingPerm === void 0) {
      return false;
    }
    const fieldsIndex = adjustedPath.indexOf("fields");
    if (fieldsIndex !== -1 && fieldsIndex < adjustedPath.length - 1) {
      const fieldPath = adjustedPath.slice(fieldsIndex + 1).join(".");
      const fields = (_a = matchingPerm.properties) == null ? void 0 : _a.fields;
      if (fields === null || fields === void 0) {
        return true;
      }
      if (Array.isArray(fields) && fields.length === 0) {
        return false;
      }
      return Array.isArray(fields) && fields.some((f) => fieldPath === f || fieldPath.startsWith(`${f}.`));
    }
    return true;
  };
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/difference.mjs
var import_isEqual = __toESM(require_isEqual(), 1);
var import_isObject = __toESM(require_isObject(), 1);
var import_transform = __toESM(require_transform(), 1);
function difference(object, base) {
  function changes(object2, base2) {
    return (0, import_transform.default)(object2, (result, value, key) => {
      if (!(0, import_isEqual.default)(value, base2[key])) {
        result[key] = (0, import_isObject.default)(value) && (0, import_isObject.default)(base2[key]) ? changes(value, base2[key]) : value;
      }
      return result;
    });
  }
  return changes(object, base);
}

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/forms.mjs
var import_isEmpty = __toESM(require_isEmpty(), 1);
var import_merge = __toESM(require_merge(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/utils/arrays.mjs
var flattenDeep = (array) => {
  if (Array.isArray(array)) {
    return array.reduce((acc, value) => {
      if (Array.isArray(value)) {
        acc.push(...flattenDeep(value));
      } else {
        acc.push(value);
      }
      return acc;
    }, []);
  } else {
    return [];
  }
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/createArrayOfValues.mjs
var createArrayOfValues = (obj) => {
  if (!isObject(obj)) {
    return [];
  }
  return flattenDeep(Object.values(obj).map((value) => {
    if (isObject(value)) {
      return createArrayOfValues(value);
    }
    return value;
  }));
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/permissions.mjs
var findMatchingPermission = (permissions, action, subject) => permissions.find((perm) => perm.action === action && perm.subject === subject);
var formatPermissionsForAPI = (modifiedData) => {
  const pluginsPermissions = formatSettingsPermissions(modifiedData.plugins);
  const settingsPermissions = formatSettingsPermissions(modifiedData.settings);
  const collectionTypesPermissions = formatContentTypesPermissions(modifiedData.collectionTypes);
  const singleTypesPermissions = formatContentTypesPermissions(modifiedData.singleTypes);
  return [
    ...pluginsPermissions,
    ...settingsPermissions,
    ...collectionTypesPermissions,
    ...singleTypesPermissions
  ];
};
var formatSettingsPermissions = (settingsPermissionsObject) => {
  return Object.values(settingsPermissionsObject).reduce((formAcc, form) => {
    const currentCategoryPermissions = Object.values(form).reduce((childFormAcc, childForm) => {
      const permissions = Object.entries(childForm).reduce((responsesAcc, [actionName, { conditions, properties: { enabled } }]) => {
        if (!enabled) {
          return responsesAcc;
        }
        responsesAcc.push({
          action: actionName,
          subject: null,
          conditions: createConditionsArray(conditions),
          properties: {}
        });
        return responsesAcc;
      }, []);
      return [
        ...childFormAcc,
        ...permissions
      ];
    }, []);
    return [
      ...formAcc,
      ...currentCategoryPermissions
    ];
  }, []);
};
var formatContentTypesPermissions = (contentTypesPermissions) => {
  const permissions = Object.entries(contentTypesPermissions).reduce((allPermissions, current) => {
    const [subject, currentSubjectActions] = current;
    const permissions2 = Object.entries(currentSubjectActions).reduce((acc, current2) => {
      var _a;
      const [actionName, permissions3] = current2;
      const shouldCreatePermission = createArrayOfValues(permissions3).some((val) => val);
      if (!shouldCreatePermission) {
        return acc;
      }
      if (!((_a = permissions3 == null ? void 0 : permissions3.properties) == null ? void 0 : _a.enabled)) {
        const createdPermissionsArray = Object.entries(permissions3.properties).reduce((acc2, current3) => {
          const [propertyName, propertyValue] = current3;
          acc2.properties[propertyName] = createPropertyArray(propertyValue);
          return acc2;
        }, {
          action: actionName,
          subject,
          conditions: createConditionsArray(permissions3.conditions),
          properties: {}
        });
        return [
          ...acc,
          createdPermissionsArray
        ];
      }
      if (!permissions3.properties.enabled) {
        return acc;
      }
      acc.push({
        action: actionName,
        subject,
        properties: {},
        conditions: createConditionsArray(permissions3.conditions)
      });
      return acc;
    }, []);
    return [
      ...allPermissions,
      ...permissions2
    ];
  }, []);
  return permissions;
};
var createPropertyArray = (propertyValue, prefix = "") => {
  return Object.entries(propertyValue).reduce((acc, current) => {
    const [name, value] = current;
    if (isObject(value)) {
      return [
        ...acc,
        ...createPropertyArray(value, `${prefix}${name}.`)
      ];
    }
    if (value && !isObject(value)) {
      acc.push(`${prefix}${name}`);
    }
    return acc;
  }, []);
};
var createConditionsArray = (conditions) => Object.entries(conditions).filter(([, conditionValue]) => {
  return conditionValue;
}).map(([conditionName]) => conditionName);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/forms.mjs
var createDefaultConditionsForm = (conditions, initialConditions = []) => conditions.reduce((acc, current) => {
  acc[current.id] = initialConditions.indexOf(current.id) !== -1;
  return acc;
}, {});
var createDefaultForm = (layout, conditions, initialPermissions = []) => {
  return layout.reduce((acc, { categoryId, childrenForm }) => {
    const childrenDefaultForm = childrenForm.reduce((acc2, current) => {
      acc2[current.subCategoryId] = current.actions.reduce((acc3, current2) => {
        const foundMatchingPermission = findMatchingPermission(initialPermissions, current2.action, null);
        acc3[current2.action] = {
          properties: {
            enabled: foundMatchingPermission !== void 0
          },
          conditions: createDefaultConditionsForm(conditions, (foundMatchingPermission == null ? void 0 : foundMatchingPermission.conditions) ?? [])
        };
        return acc3;
      }, {});
      return acc2;
    }, {});
    acc[categoryId] = childrenDefaultForm;
    return acc;
  }, {});
};
var resolvePermissionPropertyValues = (property, matchingPermission) => {
  var _a, _b;
  const rawValue = matchingPermission == null ? void 0 : matchingPermission.properties[property.value];
  if (rawValue === null && property.value === "locales") {
    return (property.children ?? []).map((child) => child.value);
  }
  const storedValues = Array.isArray(rawValue) ? rawValue : [];
  const shouldDefaultToLocale = property.value === "locales" && matchingPermission !== void 0 && storedValues.length === 0;
  if (!shouldDefaultToLocale) {
    return storedValues;
  }
  const defaultLocaleCode = (_b = (_a = property.children) == null ? void 0 : _a.find((child) => child.isDefault)) == null ? void 0 : _b.value;
  return defaultLocaleCode ? [
    defaultLocaleCode
  ] : (property.children ?? []).map((child) => child.value);
};
var createDefaultPropertiesForm = (properties, subject, matchingPermission) => {
  const recursivelyCreatePropertyForm = ({ children = [] }, propertyValues, prefix = "") => {
    return children.reduce((acc, current) => {
      if (current.children) {
        return {
          ...acc,
          [current.value]: recursivelyCreatePropertyForm(current, propertyValues, `${prefix}${current.value}.`)
        };
      }
      const hasProperty = propertyValues.indexOf(`${prefix}${current.value}`) !== -1;
      acc[current.value] = hasProperty;
      return acc;
    }, {});
  };
  return properties.reduce((acc, currentPropertyName) => {
    const foundProperty = subject.properties.find(({ value }) => value === currentPropertyName);
    if (foundProperty) {
      const matchingPermissionPropertyValues = resolvePermissionPropertyValues(foundProperty, matchingPermission);
      const propertyForm = recursivelyCreatePropertyForm(foundProperty, matchingPermissionPropertyValues);
      acc.properties[currentPropertyName] = propertyForm;
    }
    return acc;
  }, {
    properties: {}
  });
};
var createDefaultCTForm = ({ subjects, actions = [] }, conditions, initialPermissions = []) => {
  return actions.reduce((defaultForm, action) => {
    const subjectLayouts = action.subjects.reduce((acc, current) => {
      const foundLayout = subjects.find(({ uid }) => uid === current) || null;
      if (foundLayout) {
        acc[current] = foundLayout;
      }
      return acc;
    }, {});
    if ((0, import_isEmpty.default)(subjectLayouts)) {
      return defaultForm;
    }
    const contentTypesActions = Object.keys(subjectLayouts).reduce((acc, currentCTUID) => {
      const { actionId, applyToProperties } = action;
      const currentSubjectLayout = subjectLayouts[currentCTUID];
      const properties = currentSubjectLayout.properties.map(({ value }) => value);
      const doesNothaveProperty = properties.every((property) => (applyToProperties || []).indexOf(property) === -1);
      const matchingPermission = findMatchingPermission(initialPermissions, actionId, currentCTUID);
      const conditionsForm = createDefaultConditionsForm(conditions, (matchingPermission == null ? void 0 : matchingPermission.conditions) ?? []);
      if (!acc[currentCTUID]) {
        acc[currentCTUID] = {};
      }
      if ((0, import_isEmpty.default)(applyToProperties) || doesNothaveProperty) {
        acc[currentCTUID][actionId] = {
          properties: {
            enabled: matchingPermission !== void 0
          },
          conditions: conditionsForm
        };
        return acc;
      }
      const propertiesForm = createDefaultPropertiesForm(applyToProperties, subjectLayouts[currentCTUID], matchingPermission);
      acc[currentCTUID][actionId] = {
        ...propertiesForm,
        conditions: conditionsForm
      };
      return acc;
    }, {});
    return (0, import_merge.default)(defaultForm, contentTypesActions);
  }, {});
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/layouts.mjs
var import_groupBy = __toESM(require_groupBy(), 1);
var formatLayout = (layout, groupByKey) => {
  return Object.entries((0, import_groupBy.default)(layout, groupByKey)).map(([itemName, item]) => ({
    category: itemName,
    categoryId: itemName.split(" ").join("-"),
    childrenForm: Object.entries((0, import_groupBy.default)(item, "subCategory")).map(([subCategoryName, actions]) => ({
      subCategoryName,
      subCategoryId: subCategoryName.split(" ").join("-"),
      actions
    }))
  }));
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/localePermissionValidation.mjs
var import_omit = __toESM(require_omit(), 1);
var getActionLocales = (actionData) => {
  if (!("properties" in actionData)) {
    return void 0;
  }
  const { properties } = actionData;
  if (!isObject(properties) || !("locales" in properties)) {
    return void 0;
  }
  return properties.locales;
};
var isActionEnabled = (actionData) => {
  return createArrayOfValues((0, import_omit.default)(actionData, "conditions")).some(Boolean);
};
var hasSelectedLocales = (locales) => {
  return createArrayOfValues(locales).some(Boolean);
};
var subjectHasLocaleValidationError = (actions = {}) => {
  return Object.values(actions).some((actionData) => {
    const locales = getActionLocales(actionData);
    if (locales === void 0) {
      return false;
    }
    return isActionEnabled(actionData) && !hasSelectedLocales(locales);
  });
};
var hasLocaleValidationErrors = (modifiedData) => {
  return [
    "collectionTypes",
    "singleTypes"
  ].some((kind) => Object.values(modifiedData[kind] ?? {}).some(subjectHasLocaleValidationError));
};
var getLocaleValidationErrorActionKeys = (modifiedData, kind, subject) => {
  var _a;
  const actions = (_a = modifiedData[kind]) == null ? void 0 : _a[subject];
  if (!actions) {
    return [];
  }
  return Object.keys(actions).filter((actionKey) => {
    const actionData = actions[actionKey];
    const locales = getActionLocales(actionData);
    if (locales === void 0) {
      return false;
    }
    return isActionEnabled(actionData) && !hasSelectedLocales(locales);
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/updateConditionsToFalse.mjs
var import_has = __toESM(require_has(), 1);
var import_omit2 = __toESM(require_omit(), 1);
var updateConditionsToFalse = (obj) => {
  return Object.keys(obj).reduce((acc, current) => {
    const currentValue = obj[current];
    if (isObject(currentValue) && !(0, import_has.default)(currentValue, "conditions")) {
      return {
        ...acc,
        [current]: updateConditionsToFalse(currentValue)
      };
    }
    if (isObject(currentValue) && (0, import_has.default)(currentValue, "conditions")) {
      const isActionEnabled2 = createArrayOfValues((0, import_omit2.default)(currentValue, "conditions")).some((val) => val);
      if (!isActionEnabled2) {
        const updatedConditions = Object.keys(currentValue.conditions).reduce((acc1, current2) => {
          acc1[current2] = false;
          return acc1;
        }, {});
        return {
          ...acc,
          [current]: {
            ...currentValue,
            conditions: updatedConditions
          }
        };
      }
    }
    acc[current] = currentValue;
    return acc;
  }, {});
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/updateValues.mjs
var updateValues = (obj, valueToSet, isFieldUpdate = false) => {
  return Object.entries(obj).reduce((acc, [key, value]) => {
    if (key === "conditions" && !isFieldUpdate) {
      acc[key] = value;
      return acc;
    }
    if (isObject(value)) {
      return {
        ...acc,
        [key]: updateValues(value, valueToSet, key === "fields")
      };
    }
    acc[key] = valueToSet;
    return acc;
  }, {});
};
var updateValuesWithPermissions = (obj, valueToSet, permissionChecker, currentPath = [], isFieldUpdate = false) => {
  if (permissionChecker === void 0) {
    return updateValues(obj, valueToSet, isFieldUpdate);
  }
  return Object.entries(obj).reduce((acc, [key, value]) => {
    const newPath = [
      ...currentPath,
      key
    ];
    if (key === "conditions" && !isFieldUpdate) {
      acc[key] = value;
      return acc;
    }
    if (isObject(value)) {
      return {
        ...acc,
        [key]: updateValuesWithPermissions(value, valueToSet, permissionChecker, newPath, key === "fields")
      };
    }
    const hasPermission = permissionChecker(newPath);
    acc[key] = hasPermission ? valueToSet : value;
    return acc;
  }, {});
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/ContentTypes.mjs
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/ContentTypeCollapses.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var React5 = __toESM(require_react(), 1);
var import_get3 = __toESM(require_get(), 1);
var import_isEmpty2 = __toESM(require_isEmpty(), 1);
var import_omit3 = __toESM(require_omit(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/constants.mjs
var cellWidth = `12rem`;
var firstRowWidth = `20rem`;
var rowHeight = `5.3rem`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/removeConditionKeyFromData.mjs
var removeConditionKeyFromData = (obj) => {
  if (!obj) {
    return null;
  }
  return Object.entries(obj).reduce((acc, [key, value]) => {
    if (key !== "conditions") {
      acc[key] = value;
    }
    return acc;
  }, {});
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/utils/getCheckboxState.mjs
var getCheckboxState = (dataObj) => {
  const dataWithoutCondition = removeConditionKeyFromData(dataObj);
  const arrayOfValues = createArrayOfValues(dataWithoutCondition);
  if (!arrayOfValues.length) {
    return {
      hasAllActionsSelected: false,
      hasSomeActionsSelected: false
    };
  }
  const hasAllActionsSelected = arrayOfValues.every((val) => val);
  const hasSomeActionsSelected = arrayOfValues.some((val) => val) && !hasAllActionsSelected;
  return {
    hasAllActionsSelected,
    hasSomeActionsSelected
  };
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/CollapsePropertyMatrix.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var React2 = __toESM(require_react(), 1);
var import_get = __toESM(require_get(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/CollapseLabel.mjs
var CollapseLabel = gt(C)`
  padding-right: ${({ theme }) => theme.spaces[2]};
  overflow: hidden;
  flex: 1;
  ${({ $isCollapsable }) => $isCollapsable && "cursor: pointer;"}
`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/HiddenAction.mjs
var HiddenAction = gt.div`
  width: ${cellWidth};
`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/RequiredSign.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var RequiredSign = () => (0, import_jsx_runtime2.jsx)(S, {
  color: "danger700",
  paddingLeft: 1,
  children: "*"
});

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/RowLabelWithCheckbox.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
var RowLabelWithCheckbox = ({ checkboxName = "", children, isActive = false, isCollapsable = false, isFormDisabled = false, label, onChange, onClick, someChecked = false, value }) => {
  const { formatMessage } = useIntl();
  const collapseLabelProps = {
    title: label,
    alignItems: "center",
    $isCollapsable: isCollapsable
  };
  if (isCollapsable) {
    Object.assign(collapseLabelProps, {
      onClick,
      "aria-expanded": isActive,
      onKeyDown({ key }) {
        if (key === "Enter" || key === " ") {
          onClick();
        }
      },
      tabIndex: 0,
      role: "button"
    });
  }
  return (0, import_jsx_runtime3.jsxs)(C, {
    alignItems: "center",
    paddingLeft: 6,
    width: firstRowWidth,
    shrink: 0,
    children: [
      (0, import_jsx_runtime3.jsx)(S, {
        paddingRight: 2,
        children: (0, import_jsx_runtime3.jsx)(Bt, {
          name: checkboxName,
          "aria-label": formatMessage({
            id: `Settings.permissions.select-all-by-permission`,
            defaultMessage: "Select all {label} permissions"
          }, {
            label
          }),
          disabled: isFormDisabled,
          // Keep same signature as packages/core/admin/admin/src/components/Roles/Permissions/index.js l.91
          onCheckedChange: (value2) => onChange({
            target: {
              name: checkboxName,
              value: !!value2
            }
          }),
          checked: someChecked ? "indeterminate" : value
        })
      }),
      (0, import_jsx_runtime3.jsxs)(CollapseLabel, {
        ...collapseLabelProps,
        children: [
          (0, import_jsx_runtime3.jsx)(A, {
            ellipsis: true,
            children: label
          }),
          children
        ]
      })
    ]
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/CollapsePropertyMatrix.mjs
var CollapsePropertyMatrix = ({ availableActions = [], childrenForm = [], isFormDisabled, label, pathToData, propertyName, subject }) => {
  const { formatMessage, formatList } = useIntl();
  const { modifiedData } = usePermissionsDataManager();
  const propertyActions = React2.useMemo(() => availableActions.map((action) => {
    const isActionRelatedToCurrentProperty = Array.isArray(action.applyToProperties) && action.applyToProperties.indexOf(propertyName) !== -1 && action.isDisplayed;
    return {
      label: action.label,
      actionId: action.actionId,
      isActionRelatedToCurrentProperty
    };
  }), [
    availableActions,
    propertyName
  ]);
  const contentTypeKind = pathToData.split("..")[0];
  const localeValidationErrorLabels = React2.useMemo(() => {
    if (propertyName !== "locales" || subject === void 0) return [];
    const errorKeys = getLocaleValidationErrorActionKeys(modifiedData, contentTypeKind, subject);
    return propertyActions.filter(({ actionId, isActionRelatedToCurrentProperty }) => isActionRelatedToCurrentProperty && errorKeys.includes(actionId)).map(({ label: label2 }) => label2);
  }, [
    propertyName,
    subject,
    modifiedData,
    contentTypeKind,
    propertyActions
  ]);
  return (0, import_jsx_runtime4.jsxs)(C, {
    display: "inline-flex",
    direction: "column",
    alignItems: "stretch",
    minWidth: 0,
    children: [
      (0, import_jsx_runtime4.jsx)(Header, {
        label,
        headers: propertyActions
      }),
      localeValidationErrorLabels.length > 0 && (0, import_jsx_runtime4.jsx)(S, {
        paddingLeft: 6,
        paddingTop: 2,
        paddingBottom: 2,
        children: (0, import_jsx_runtime4.jsx)(A, {
          role: "alert",
          variant: "pi",
          textColor: "danger600",
          children: formatMessage({
            id: "Settings.roles.form.permissions.locales.validation",
            defaultMessage: "{count, plural, one {The {actions} action must apply to at least one locale.} other {The {actions} actions must apply to at least one locale.}}"
          }, {
            count: localeValidationErrorLabels.length,
            actions: formatList(localeValidationErrorLabels, {
              type: "conjunction"
            })
          })
        })
      }),
      (0, import_jsx_runtime4.jsx)(S, {
        children: childrenForm.map(({ children: childrenForm2, label: label2, value, required }, i) => (0, import_jsx_runtime4.jsx)(ActionRow, {
          childrenForm: childrenForm2,
          label: label2,
          isFormDisabled,
          name: value,
          required,
          propertyActions,
          pathToData,
          propertyName,
          isOdd: i % 2 === 0,
          subject
        }, value))
      })
    ]
  });
};
var ActionRow = ({ childrenForm = [], label, isFormDisabled = false, name, required = false, pathToData, propertyActions, propertyName, isOdd = false, subject }) => {
  const { formatMessage } = useIntl();
  const [rowToOpen, setRowToOpen] = React2.useState(null);
  const { modifiedData, onChangeCollectionTypeLeftActionRowCheckbox, onChangeParentCheckbox, onChangeSimpleCheckbox, checkUserHasPermission } = usePermissionsDataManager();
  const isActive = rowToOpen === name;
  const recursiveChildren = React2.useMemo(() => {
    if (!Array.isArray(childrenForm)) {
      return [];
    }
    return childrenForm;
  }, [
    childrenForm
  ]);
  const isCollapsable = recursiveChildren.length > 0;
  const handleClick = React2.useCallback(() => {
    if (isCollapsable) {
      setRowToOpen((prev) => {
        if (prev === name) {
          return null;
        }
        return name;
      });
    }
  }, [
    isCollapsable,
    name
  ]);
  const handleChangeLeftRowCheckbox = ({ target: { value } }) => {
    onChangeCollectionTypeLeftActionRowCheckbox(pathToData, propertyName, name, value);
  };
  const { hasAllActionsSelected, hasSomeActionsSelected } = React2.useMemo(() => {
    return getRowLabelCheckboxState(propertyActions, modifiedData, pathToData, propertyName, name);
  }, [
    propertyActions,
    modifiedData,
    pathToData,
    propertyName,
    name
  ]);
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, {
    children: [
      (0, import_jsx_runtime4.jsx)(Wrapper, {
        alignItems: "center",
        $isCollapsable: isCollapsable,
        $isActive: isActive,
        background: isOdd ? "neutral100" : "neutral0",
        children: (0, import_jsx_runtime4.jsxs)(C, {
          children: [
            (0, import_jsx_runtime4.jsxs)(RowLabelWithCheckbox, {
              onChange: handleChangeLeftRowCheckbox,
              onClick: handleClick,
              isCollapsable,
              isFormDisabled,
              label,
              someChecked: hasSomeActionsSelected,
              value: hasAllActionsSelected,
              isActive,
              children: [
                required && (0, import_jsx_runtime4.jsx)(RequiredSign, {}),
                (0, import_jsx_runtime4.jsx)(CarretIcon, {
                  $isActive: isActive
                })
              ]
            }),
            (0, import_jsx_runtime4.jsx)(C, {
              children: propertyActions.map(({ label: label2, isActionRelatedToCurrentProperty, actionId }) => {
                if (!isActionRelatedToCurrentProperty) {
                  return (0, import_jsx_runtime4.jsx)(HiddenAction, {}, label2);
                }
                const checkboxName = [
                  ...pathToData.split(".."),
                  actionId,
                  "properties",
                  propertyName,
                  name
                ];
                if (!isCollapsable) {
                  const checkboxValue = (0, import_get.default)(modifiedData, checkboxName, false);
                  const fieldPath2 = propertyName === "fields" ? name : void 0;
                  const userHasPermission2 = checkUserHasPermission(actionId, subject, fieldPath2);
                  return (0, import_jsx_runtime4.jsx)(C, {
                    width: cellWidth,
                    position: "relative",
                    justifyContent: "center",
                    alignItems: "center",
                    children: (0, import_jsx_runtime4.jsx)(Bt, {
                      disabled: isFormDisabled || !userHasPermission2,
                      name: checkboxName.join(".."),
                      "aria-label": formatMessage({
                        id: `Settings.permissions.select-by-permission`,
                        defaultMessage: "Select {label} permission"
                      }, {
                        label: `${name} ${label2}`
                      }),
                      onCheckedChange: (value) => {
                        onChangeSimpleCheckbox({
                          target: {
                            name: checkboxName.join(".."),
                            value: !!value
                          }
                        });
                      },
                      checked: checkboxValue
                    })
                  }, actionId);
                }
                const data = (0, import_get.default)(modifiedData, checkboxName, {});
                const { hasAllActionsSelected: hasAllActionsSelected2, hasSomeActionsSelected: hasSomeActionsSelected2 } = getCheckboxState(data);
                const fieldPath = propertyName === "fields" ? name : void 0;
                const userHasPermission = checkUserHasPermission(actionId, subject, fieldPath);
                return (0, import_jsx_runtime4.jsx)(C, {
                  width: cellWidth,
                  position: "relative",
                  justifyContent: "center",
                  alignItems: "center",
                  children: (0, import_jsx_runtime4.jsx)(Bt, {
                    disabled: isFormDisabled || !userHasPermission,
                    name: checkboxName.join(".."),
                    onCheckedChange: (value) => {
                      onChangeParentCheckbox({
                        target: {
                          name: checkboxName.join(".."),
                          value: !!value
                        }
                      });
                    },
                    "aria-label": formatMessage({
                      id: `Settings.permissions.select-by-permission`,
                      defaultMessage: "Select {label} permission"
                    }, {
                      label: `${name} ${label2}`
                    }),
                    checked: hasSomeActionsSelected2 ? "indeterminate" : hasAllActionsSelected2
                  })
                }, label2);
              })
            })
          ]
        })
      }),
      isActive && (0, import_jsx_runtime4.jsx)(SubActionRow, {
        childrenForm: recursiveChildren,
        isFormDisabled,
        parentName: name,
        pathToDataFromActionRow: pathToData,
        propertyName,
        propertyActions,
        recursiveLevel: 0,
        subject
      })
    ]
  });
};
var getRowLabelCheckboxState = (propertyActions, modifiedData, pathToContentType, propertyToCheck, targetKey) => {
  const actionIds = propertyActions.reduce((acc, current) => {
    if (current.isActionRelatedToCurrentProperty) {
      acc.push(current.actionId);
    }
    return acc;
  }, []);
  const data = actionIds.reduce((acc, current) => {
    const mainData = (0, import_get.default)(modifiedData, [
      ...pathToContentType.split(".."),
      current,
      "properties",
      propertyToCheck,
      targetKey
    ], false);
    acc[current] = mainData;
    return acc;
  }, {});
  return getCheckboxState(data);
};
var Wrapper = gt(C)`
  height: ${rowHeight};
  flex: 1;

  &:hover {
    ${({ $isCollapsable, theme }) => $isCollapsable && activeStyle(theme)}
  }

  ${({ $isCollapsable }) => $isCollapsable && `
      ${CarretIcon} {
        display: flex;
      }
  `}
  ${({ $isActive, theme }) => $isActive && activeStyle(theme)};
`;
var CarretIcon = gt(p2)`
  display: none;

  svg {
    width: 1.4rem;
  }

  path {
    fill: ${({ theme }) => theme.colors.neutral200};
  }

  transform: rotate(${({ $isActive }) => $isActive ? "180" : "0"}deg);
  margin-left: ${({ theme }) => theme.spaces[2]};
`;
var SubActionRow = ({ childrenForm = [], isFormDisabled, recursiveLevel, pathToDataFromActionRow, propertyActions, parentName, propertyName, subject }) => {
  const { formatMessage } = useIntl();
  const { modifiedData, onChangeParentCheckbox, onChangeSimpleCheckbox, checkUserHasPermission } = usePermissionsDataManager();
  const [rowToOpen, setRowToOpen] = React2.useState(null);
  const handleClickToggleSubLevel = (name) => {
    setRowToOpen((prev) => {
      if (prev === name) {
        return null;
      }
      return name;
    });
  };
  const displayedRecursiveChildren = React2.useMemo(() => {
    if (!rowToOpen) {
      return null;
    }
    return childrenForm.find(({ value }) => value === rowToOpen);
  }, [
    rowToOpen,
    childrenForm
  ]);
  return (0, import_jsx_runtime4.jsxs)(S, {
    paddingLeft: `3.2rem`,
    children: [
      (0, import_jsx_runtime4.jsx)(TopTimeline, {}),
      childrenForm.map(({ label, value, required, children: subChildrenForm }, index) => {
        const isVisible = index + 1 < childrenForm.length;
        const isArrayType = Array.isArray(subChildrenForm);
        const isActive = rowToOpen === value;
        return (0, import_jsx_runtime4.jsxs)(LeftBorderTimeline, {
          $isVisible: isVisible,
          children: [
            (0, import_jsx_runtime4.jsxs)(C, {
              height: rowHeight,
              children: [
                (0, import_jsx_runtime4.jsx)(StyledBox, {
                  children: (0, import_jsx_runtime4.jsx)(Svg, {
                    width: "20",
                    height: "23",
                    viewBox: "0 0 20 23",
                    fill: "none",
                    xmlns: "http://www.w3.org/2000/svg",
                    $color: "primary200",
                    children: (0, import_jsx_runtime4.jsx)("path", {
                      fillRule: "evenodd",
                      clipRule: "evenodd",
                      d: "M7.02477 14.7513C8.65865 17.0594 11.6046 18.6059 17.5596 18.8856C18.6836 18.9384 19.5976 19.8435 19.5976 20.9688V20.9688C19.5976 22.0941 18.6841 23.0125 17.5599 22.9643C10.9409 22.6805 6.454 20.9387 3.75496 17.1258C0.937988 13.1464 0.486328 7.39309 0.486328 0.593262H4.50974C4.50974 7.54693 5.06394 11.9813 7.02477 14.7513Z",
                      fill: "#D9D8FF"
                    })
                  })
                }),
                (0, import_jsx_runtime4.jsxs)(C, {
                  style: {
                    flex: 1
                  },
                  children: [
                    (0, import_jsx_runtime4.jsx)(RowStyle, {
                      $level: recursiveLevel,
                      $isActive: isActive,
                      $isCollapsable: isArrayType,
                      children: (0, import_jsx_runtime4.jsxs)(CollapseLabel, {
                        alignItems: "center",
                        $isCollapsable: isArrayType,
                        ...isArrayType && {
                          onClick: () => handleClickToggleSubLevel(value),
                          "aria-expanded": isActive,
                          onKeyDown: ({ key }) => (key === "Enter" || key === " ") && handleClickToggleSubLevel(value),
                          tabIndex: 0,
                          role: "button"
                        },
                        title: label,
                        children: [
                          (0, import_jsx_runtime4.jsx)(RowLabel, {
                            ellipsis: true,
                            children: label
                          }),
                          required && (0, import_jsx_runtime4.jsx)(RequiredSign, {}),
                          (0, import_jsx_runtime4.jsx)(CarretIcon, {
                            $isActive: isActive
                          })
                        ]
                      })
                    }),
                    (0, import_jsx_runtime4.jsx)(C, {
                      style: {
                        flex: 1
                      },
                      children: propertyActions.map(({ actionId, label: propertyLabel, isActionRelatedToCurrentProperty }) => {
                        if (!isActionRelatedToCurrentProperty) {
                          return (0, import_jsx_runtime4.jsx)(HiddenAction, {}, actionId);
                        }
                        const checkboxName = [
                          ...pathToDataFromActionRow.split(".."),
                          actionId,
                          "properties",
                          propertyName,
                          ...parentName.split(".."),
                          value
                        ];
                        const checkboxValue = (0, import_get.default)(modifiedData, checkboxName, false);
                        if (!subChildrenForm) {
                          const fieldPath2 = propertyName === "fields" ? `${parentName}.${value}` : void 0;
                          const userHasPermission2 = checkUserHasPermission(actionId, subject, fieldPath2);
                          return (0, import_jsx_runtime4.jsx)(C, {
                            position: "relative",
                            width: cellWidth,
                            justifyContent: "center",
                            alignItems: "center",
                            children: (0, import_jsx_runtime4.jsx)(Bt, {
                              disabled: isFormDisabled || !userHasPermission2,
                              name: checkboxName.join(".."),
                              "aria-label": formatMessage({
                                id: `Settings.permissions.select-by-permission`,
                                defaultMessage: "Select {label} permission"
                              }, {
                                label: `${parentName} ${label} ${propertyLabel}`
                              }),
                              onCheckedChange: (value2) => {
                                onChangeSimpleCheckbox({
                                  target: {
                                    name: checkboxName.join(".."),
                                    value: !!value2
                                  }
                                });
                              },
                              checked: checkboxValue
                            })
                          }, propertyLabel);
                        }
                        const { hasAllActionsSelected, hasSomeActionsSelected } = getCheckboxState(checkboxValue);
                        const fieldPath = propertyName === "fields" ? `${parentName}.${value}` : void 0;
                        const userHasPermission = checkUserHasPermission(actionId, subject, fieldPath);
                        return (0, import_jsx_runtime4.jsx)(C, {
                          position: "relative",
                          width: cellWidth,
                          justifyContent: "center",
                          alignItems: "center",
                          children: (0, import_jsx_runtime4.jsx)(Bt, {
                            disabled: isFormDisabled || !userHasPermission,
                            name: checkboxName.join(".."),
                            "aria-label": formatMessage({
                              id: `Settings.permissions.select-by-permission`,
                              defaultMessage: "Select {label} permission"
                            }, {
                              label: `${parentName} ${label} ${propertyLabel}`
                            }),
                            // Keep same signature as packages/core/admin/admin/src/components/Roles/Permissions/index.js l.91
                            onCheckedChange: (value2) => {
                              onChangeParentCheckbox({
                                target: {
                                  name: checkboxName.join(".."),
                                  value: !!value2
                                }
                              });
                            },
                            checked: hasSomeActionsSelected ? "indeterminate" : hasAllActionsSelected
                          }, propertyLabel)
                        }, propertyLabel);
                      })
                    })
                  ]
                })
              ]
            }),
            displayedRecursiveChildren && isActive && (0, import_jsx_runtime4.jsx)(S, {
              paddingBottom: 2,
              children: (0, import_jsx_runtime4.jsx)(SubActionRow, {
                isFormDisabled,
                parentName: `${parentName}..${value}`,
                pathToDataFromActionRow,
                propertyActions,
                propertyName,
                recursiveLevel: recursiveLevel + 1,
                childrenForm: displayedRecursiveChildren.children,
                subject
              })
            })
          ]
        }, value);
      })
    ]
  });
};
var LeftBorderTimeline = gt(S)`
  border-left: ${({ $isVisible, theme }) => $isVisible ? `4px solid ${theme.colors.primary200}` : "4px solid transparent"};
`;
var RowStyle = gt(C)`
  padding-left: ${({ theme }) => theme.spaces[4]};
  width: ${({ $level }) => 145 - $level * 36}px;

  &:hover {
    ${({ $isCollapsable, theme }) => $isCollapsable && activeStyle(theme)}
  }

  ${({ $isCollapsable }) => $isCollapsable && `
      ${CarretIcon} {
        display: flex;
      }
  `}
  ${({ $isActive, theme }) => $isActive && activeStyle(theme)};
`;
var RowLabel = gt(A)``;
var TopTimeline = gt.div`
  padding-top: ${({ theme }) => theme.spaces[2]};
  margin-top: ${({ theme }) => theme.spaces[2]};
  width: 0.4rem;
  background-color: ${({ theme }) => theme.colors.primary200};
  border-top-left-radius: 2px;
  border-top-right-radius: 2px;
`;
var StyledBox = gt(S)`
  transform: translate(-4px, -12px);

  &:before {
    content: '';
    width: 0.4rem;
    height: 1.2rem;
    background: ${({ theme }) => theme.colors.primary200};
    display: block;
  }
`;
var Svg = gt.svg`
  position: relative;
  flex-shrink: 0;
  transform: translate(-0.5px, -1px);

  * {
    fill: ${({ theme, $color }) => theme.colors[$color]};
  }
`;
var Header = ({ headers = [], label }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime4.jsxs)(C, {
    children: [
      (0, import_jsx_runtime4.jsx)(C, {
        width: firstRowWidth,
        height: rowHeight,
        shrink: 0,
        alignItems: "center",
        paddingLeft: 6,
        children: (0, import_jsx_runtime4.jsx)(A, {
          variant: "sigma",
          textColor: "neutral500",
          children: formatMessage({
            id: "Settings.roles.form.permission.property-label",
            defaultMessage: "{label} permissions"
          }, {
            label
          })
        })
      }),
      headers.map((header) => {
        if (!header.isActionRelatedToCurrentProperty) {
          return (0, import_jsx_runtime4.jsx)(C, {
            width: cellWidth,
            shrink: 0
          }, header.label);
        }
        return (0, import_jsx_runtime4.jsx)(C, {
          width: cellWidth,
          shrink: 0,
          justifyContent: "center",
          children: (0, import_jsx_runtime4.jsx)(A, {
            variant: "sigma",
            textColor: "neutral500",
            children: formatMessage({
              id: `Settings.roles.form.permissions.${header.label.toLowerCase()}`,
              defaultMessage: header.label
            })
          })
        }, header.label);
      })
    ]
  });
};
var activeStyle = (theme) => ft`
  color: ${theme.colors.primary600};
  font-weight: ${theme.fontWeights.bold};

  ${CarretIcon} {
    path {
      fill: ${theme.colors.primary600};
    }
  }
`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/ConditionsButton.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var React3 = __toESM(require_react(), 1);
var ConditionsButtonImpl = React3.forwardRef(({ onClick, className, hasConditions = false, variant = "tertiary" }, ref) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime5.jsx)(ButtonContainer, {
    $hasConditions: hasConditions,
    className,
    children: (0, import_jsx_runtime5.jsx)(Ct, {
      variant,
      startIcon: (0, import_jsx_runtime5.jsx)(C5, {}),
      onClick,
      ref,
      type: "button",
      children: formatMessage({
        id: "global.settings",
        defaultMessage: "Settings"
      })
    })
  });
});
var ButtonContainer = gt(S)`
  ${({ $hasConditions, theme }) => $hasConditions && `
    &:before {
      content: '';
      position: absolute;
      top: -3px;
      left: -10px;
      width: 6px;
      height: 6px;
      border-radius: 2rem;
      background: ${theme.colors.primary600};
    }
  `}
`;
var ConditionsButton = gt(ConditionsButtonImpl)``;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/ConditionsModal.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var React4 = __toESM(require_react(), 1);
var import_get2 = __toESM(require_get(), 1);
var import_groupBy2 = __toESM(require_groupBy(), 1);
var import_upperFirst = __toESM(require_upperFirst(), 1);
var ConditionsModal = ({ actions = [], headerBreadCrumbs = [], isFormDisabled, isReadOnly = false, onClose }) => {
  const { formatMessage } = useIntl();
  const { availableConditions, modifiedData, onChangeConditions } = usePermissionsDataManager();
  const arrayOfOptionsGroupedByCategory = React4.useMemo(() => {
    return Object.entries((0, import_groupBy2.default)(availableConditions, "category"));
  }, [
    availableConditions
  ]);
  const actionsToDisplay = React4.useMemo(() => actions.filter(
    // @ts-expect-error – TODO: fix this type issue
    ({ isDisplayed, hasSomeActionsSelected, hasAllActionsSelected }) => isDisplayed && Boolean(hasSomeActionsSelected || hasAllActionsSelected)
  ), [
    actions
  ]);
  const [state, setState] = React4.useState(() => createDefaultConditionsForm2(actionsToDisplay, modifiedData, arrayOfOptionsGroupedByCategory));
  React4.useEffect(() => {
    if (isReadOnly === false) {
      return;
    }
    setState(createDefaultConditionsForm2(actionsToDisplay, modifiedData, arrayOfOptionsGroupedByCategory));
  }, [
    isReadOnly,
    actionsToDisplay,
    modifiedData,
    arrayOfOptionsGroupedByCategory
  ]);
  const handleChange = React4.useCallback((name, values) => {
    if (isReadOnly === true) {
      return;
    }
    setState(fn((draft) => {
      if (draft[name] === void 0) {
        draft[name] = {};
      }
      if (draft[name].default === void 0) {
        draft[name].default = {};
      }
      draft[name].default = values;
    }));
  }, [
    isReadOnly
  ]);
  const handleSubmit = React4.useCallback(() => {
    if (isReadOnly === true) {
      onClose == null ? void 0 : onClose();
      return;
    }
    const conditionsWithoutCategory = Object.entries(state).reduce((acc, current) => {
      const [key, value] = current;
      const merged = Object.values(value).reduce((acc1, current1) => {
        return {
          ...acc1,
          ...current1
        };
      }, {});
      acc[key] = merged;
      return acc;
    }, {});
    onChangeConditions(conditionsWithoutCategory);
    onClose == null ? void 0 : onClose();
  }, [
    isReadOnly,
    state,
    onChangeConditions,
    onClose
  ]);
  const onCloseModal = React4.useCallback(() => {
    setState(createDefaultConditionsForm2(actionsToDisplay, modifiedData, arrayOfOptionsGroupedByCategory));
    onClose == null ? void 0 : onClose();
  }, [
    actionsToDisplay,
    modifiedData,
    arrayOfOptionsGroupedByCategory,
    onClose
  ]);
  return (0, import_jsx_runtime6.jsxs)(_d.Content, {
    children: [
      (0, import_jsx_runtime6.jsx)(_d.Header, {
        children: (0, import_jsx_runtime6.jsx)(Ca, {
          id: "condition-modal-breadcrumbs",
          label: headerBreadCrumbs.join(", "),
          children: headerBreadCrumbs.map((label, index, arr) => (0, import_jsx_runtime6.jsx)(va, {
            isCurrent: index === arr.length - 1,
            children: (0, import_upperFirst.default)(formatMessage({
              id: label,
              defaultMessage: label
            }))
          }, label))
        })
      }),
      (0, import_jsx_runtime6.jsxs)(_d.Body, {
        children: [
          isReadOnly && (0, import_jsx_runtime6.jsx)(S, {
            paddingBottom: 4,
            children: (0, import_jsx_runtime6.jsx)(A, {
              textColor: "neutral600",
              children: formatMessage({
                id: "Settings.permissions.conditions.inherited-readonly",
                defaultMessage: "These conditions are inherited from your permissions and are read-only."
              })
            })
          }),
          actionsToDisplay.length === 0 && (0, import_jsx_runtime6.jsx)(A, {
            children: formatMessage({
              id: "Settings.permissions.conditions.no-actions",
              defaultMessage: "You first need to select actions (create, read, update, ...) before defining conditions on them."
            })
          }),
          (0, import_jsx_runtime6.jsx)("ul", {
            children: actionsToDisplay.map(({ actionId, label, pathToConditionsObject }, index) => {
              const name = pathToConditionsObject.join("..");
              return (0, import_jsx_runtime6.jsx)(ActionRow2, {
                arrayOfOptionsGroupedByCategory,
                label,
                isFormDisabled: isFormDisabled || isReadOnly,
                isReadOnly,
                isGrey: index % 2 === 0,
                name,
                onChange: handleChange,
                value: (0, import_get2.default)(state, name, {})
              }, actionId);
            })
          })
        ]
      }),
      (0, import_jsx_runtime6.jsx)(_d.Footer, {
        children: isReadOnly ? (0, import_jsx_runtime6.jsx)(Ct, {
          variant: "tertiary",
          onClick: () => onCloseModal(),
          children: formatMessage({
            id: "app.components.Button.close",
            defaultMessage: "Close"
          })
        }) : (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, {
          children: [
            (0, import_jsx_runtime6.jsx)(Ct, {
              variant: "tertiary",
              onClick: () => onCloseModal(),
              children: formatMessage({
                id: "app.components.Button.cancel",
                defaultMessage: "Cancel"
              })
            }),
            (0, import_jsx_runtime6.jsx)(Ct, {
              onClick: handleSubmit,
              children: formatMessage({
                id: "Settings.permissions.conditions.apply",
                defaultMessage: "Apply"
              })
            })
          ]
        })
      })
    ]
  });
};
var createDefaultConditionsForm2 = (actionsToDisplay, modifiedData, arrayOfOptionsGroupedByCategory) => {
  return actionsToDisplay.reduce((acc, current) => {
    const valueFromModifiedData = (0, import_get2.default)(modifiedData, [
      ...current.pathToConditionsObject,
      "conditions"
    ], {});
    const categoryDefaultForm = arrayOfOptionsGroupedByCategory.reduce((acc2, current2) => {
      const [categoryName, relatedConditions] = current2;
      const conditionsForm = relatedConditions.reduce((acc3, current3) => {
        acc3[current3.id] = (0, import_get2.default)(valueFromModifiedData, current3.id, false);
        return acc3;
      }, {});
      acc2[categoryName] = conditionsForm;
      return acc2;
    }, {});
    acc[current.pathToConditionsObject.join("..")] = categoryDefaultForm;
    return acc;
  }, {});
};
var ActionRow2 = ({ arrayOfOptionsGroupedByCategory, isFormDisabled = false, isGrey = false, isReadOnly = false, label, name, onChange, value }) => {
  const { formatMessage } = useIntl();
  const handleChange = (val) => {
    if (onChange) {
      onChange(name, getNewStateFromChangedValues(arrayOfOptionsGroupedByCategory, val));
    }
  };
  const selectedValues = getSelectedValues(value);
  return (0, import_jsx_runtime6.jsxs)(C, {
    tag: "li",
    background: isGrey ? "neutral100" : "neutral0",
    paddingBottom: 3,
    paddingTop: 3,
    justifyContent: "space-evenly",
    children: [
      (0, import_jsx_runtime6.jsxs)(C, {
        style: {
          width: 180
        },
        children: [
          (0, import_jsx_runtime6.jsxs)(A, {
            variant: "sigma",
            textColor: "neutral600",
            children: [
              formatMessage({
                id: "Settings.permissions.conditions.can",
                defaultMessage: "Can"
              }),
              " "
            ]
          }),
          (0, import_jsx_runtime6.jsx)(A, {
            variant: "sigma",
            title: label,
            textColor: "primary600",
            ellipsis: true,
            children: formatMessage({
              id: `Settings.roles.form.permissions.${label.toLowerCase()}`,
              defaultMessage: label
            })
          }),
          (0, import_jsx_runtime6.jsxs)(A, {
            variant: "sigma",
            textColor: "neutral600",
            children: [
              " ",
              formatMessage({
                id: "Settings.permissions.conditions.when",
                defaultMessage: "When"
              })
            ]
          })
        ]
      }),
      (0, import_jsx_runtime6.jsx)(S, {
        style: {
          maxWidth: 430,
          width: "100%"
        },
        children: isReadOnly ? (0, import_jsx_runtime6.jsx)(C, {
          direction: "column",
          alignItems: "flex-start",
          gap: 3,
          children: selectedValues.length === 0 ? (0, import_jsx_runtime6.jsx)(A, {
            textColor: "neutral600",
            children: formatMessage({
              id: "Settings.permissions.conditions.none-applied",
              defaultMessage: "No conditions applied."
            })
          }) : arrayOfOptionsGroupedByCategory.map(([categoryName, conditions]) => {
            const selectedConditions = conditions.filter((c) => selectedValues.includes(c.id));
            if (selectedConditions.length === 0) {
              return null;
            }
            return (0, import_jsx_runtime6.jsxs)(C, {
              direction: "column",
              alignItems: "flex-start",
              gap: 2,
              children: [
                (0, import_jsx_runtime6.jsx)(A, {
                  variant: "pi",
                  fontWeight: "bold",
                  textColor: "neutral600",
                  children: categoryName
                }),
                (0, import_jsx_runtime6.jsx)(S, {
                  children: (0, import_jsx_runtime6.jsx)(A, {
                    variant: "omega",
                    textColor: "neutral800",
                    children: selectedConditions.map((condition) => condition.displayName).join(", ")
                  })
                })
              ]
            }, categoryName);
          }).filter(Boolean)
        }) : (0, import_jsx_runtime6.jsx)(eu, {
          id: name,
          customizeContent: (values = []) => `${values.length} currently selected`,
          onChange: handleChange,
          value: selectedValues,
          options: getNestedOptions(arrayOfOptionsGroupedByCategory),
          disabled: isFormDisabled
        })
      })
    ]
  });
};
var getSelectedValues = (rawValue) => Object.values(rawValue).map((x) => Object.entries(x).filter(([, value]) => value).map(([key]) => key)).flat();
var getNestedOptions = (options) => options.reduce((acc, [label, children]) => {
  acc.push({
    label: capitalise(label),
    children: children.map((child) => ({
      label: child.displayName,
      value: child.id
    }))
  });
  return acc;
}, []);
var getNewStateFromChangedValues = (options, changedValues) => options.map(([, values]) => values).flat().reduce((acc, curr) => ({
  [curr.id]: changedValues.includes(curr.id),
  ...acc
}), {});

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/ContentTypeCollapses.mjs
var ContentTypeCollapses = ({ actions = [], isFormDisabled, pathToData, subjects = [] }) => {
  const [collapseToOpen, setCollapseToOpen] = React5.useState(null);
  const handleClickToggleCollapse = (collapseName) => () => {
    const nextCollapseToOpen = collapseToOpen === collapseName ? null : collapseName;
    setCollapseToOpen(nextCollapseToOpen);
  };
  const subjectsWithActions = React5.useMemo(() => subjects.map(({ uid, label, properties }) => {
    const availableActions = actions.map((action) => ({
      ...action,
      isDisplayed: Array.isArray(action.subjects) && action.subjects.indexOf(uid) !== -1
    }));
    return {
      uid,
      label,
      properties,
      availableActions
    };
  }), [
    subjects,
    actions
  ]);
  return (0, import_jsx_runtime7.jsx)(import_jsx_runtime7.Fragment, {
    children: subjectsWithActions.map(({ uid, label, properties, availableActions }, index) => {
      const isActive = collapseToOpen === uid;
      return (0, import_jsx_runtime7.jsxs)(C, {
        direction: "column",
        display: "inline-flex",
        alignItems: "stretch",
        minWidth: "100%",
        borderColor: isActive ? "primary600" : void 0,
        children: [
          (0, import_jsx_runtime7.jsx)(Collapse, {
            availableActions,
            isActive,
            isGrey: index % 2 === 0,
            isFormDisabled,
            label,
            onClickToggle: handleClickToggleCollapse(uid),
            pathToData: [
              pathToData,
              uid
            ].join(".."),
            subject: uid
          }),
          isActive && properties.map(({ label: propertyLabel, value, children: childrenForm }) => {
            return (0, import_jsx_runtime7.jsx)(CollapsePropertyMatrix, {
              availableActions,
              childrenForm,
              isFormDisabled,
              label: propertyLabel,
              pathToData: [
                pathToData,
                uid
              ].join(".."),
              propertyName: value,
              subject: uid
            }, value);
          })
        ]
      }, uid);
    })
  });
};
var Collapse = ({ availableActions = [], isActive = false, isGrey = false, isFormDisabled = false, label, onClickToggle, pathToData, subject }) => {
  const { formatMessage } = useIntl();
  const { modifiedData, onChangeParentCheckbox, onChangeSimpleCheckbox, checkUserHasPermission, userPermissions } = usePermissionsDataManager();
  const [isConditionModalOpen, setIsConditionModalOpen] = React5.useState(false);
  const mainData = (0, import_get3.default)(modifiedData, pathToData.split(".."), {});
  const dataWithoutCondition = React5.useMemo(() => {
    return Object.keys(mainData).reduce((acc, current) => {
      acc[current] = (0, import_omit3.default)(mainData[current], "conditions");
      return acc;
    }, {});
  }, [
    mainData
  ]);
  const { hasAllActionsSelected, hasSomeActionsSelected } = getCheckboxState(dataWithoutCondition);
  const checkboxesActions = React5.useMemo(() => {
    return generateCheckboxesActions(availableActions, modifiedData, pathToData);
  }, [
    availableActions,
    modifiedData,
    pathToData
  ]);
  const doesConditionButtonHasConditions = checkboxesActions.some((action) => action.hasConditions);
  return (0, import_jsx_runtime7.jsxs)(BoxWrapper, {
    $isActive: isActive,
    children: [
      (0, import_jsx_runtime7.jsxs)(Wrapper2, {
        height: rowHeight,
        flex: 1,
        alignItems: "center",
        background: isGrey ? "neutral100" : "neutral0",
        children: [
          (0, import_jsx_runtime7.jsx)(RowLabelWithCheckbox, {
            isCollapsable: true,
            isFormDisabled,
            label: capitalise(label),
            checkboxName: pathToData,
            onChange: onChangeParentCheckbox,
            onClick: onClickToggle,
            someChecked: hasSomeActionsSelected,
            value: hasAllActionsSelected,
            isActive,
            children: (0, import_jsx_runtime7.jsx)(Chevron, {
              paddingLeft: 2,
              children: isActive ? (0, import_jsx_runtime7.jsx)(g5, {}) : (0, import_jsx_runtime7.jsx)(o5, {})
            })
          }),
          (0, import_jsx_runtime7.jsx)(C, {
            style: {
              flex: 1
            },
            children: checkboxesActions.map(({ actionId, hasSomeActionsSelected: hasSomeActionsSelected2, isDisplayed, ...restAction }) => {
              if (!isDisplayed) {
                return (0, import_jsx_runtime7.jsx)(HiddenAction, {}, actionId);
              }
              const { hasConditions, hasAllActionsSelected: hasAllActionsSelected2, isParentCheckbox, checkboxName, label: permissionLabel } = restAction;
              const userHasPermission = checkUserHasPermission(actionId, subject);
              if (isParentCheckbox) {
                return (0, import_jsx_runtime7.jsx)(Cell, {
                  justifyContent: "center",
                  alignItems: "center",
                  children: (0, import_jsx_runtime7.jsxs)(S, {
                    position: "relative",
                    zIndex: 1,
                    children: [
                      hasConditions && (0, import_jsx_runtime7.jsx)(S, {
                        tag: "span",
                        position: "absolute",
                        top: "-6px",
                        left: "37px",
                        width: "6px",
                        height: "6px",
                        borderRadius: "20px",
                        background: "primary600"
                      }),
                      (0, import_jsx_runtime7.jsx)(Bt, {
                        disabled: isFormDisabled || !userHasPermission,
                        name: checkboxName,
                        "aria-label": formatMessage({
                          id: `Settings.permissions.select-by-permission`,
                          defaultMessage: "Select {label} permission"
                        }, {
                          label: `${permissionLabel} ${label}`
                        }),
                        // Keep same signature as packages/core/admin/admin/src/components/Roles/Permissions/index.js l.91
                        onCheckedChange: (value) => {
                          onChangeParentCheckbox({
                            target: {
                              name: checkboxName,
                              value: !!value
                            }
                          });
                        },
                        checked: hasSomeActionsSelected2 ? "indeterminate" : hasAllActionsSelected2
                      })
                    ]
                  })
                }, actionId);
              }
              return (0, import_jsx_runtime7.jsxs)(Cell, {
                justifyContent: "center",
                alignItems: "center",
                children: [
                  hasConditions && (0, import_jsx_runtime7.jsx)(S, {
                    tag: "span",
                    position: "absolute",
                    top: "-6px",
                    left: "37px",
                    width: "6px",
                    height: "6px",
                    borderRadius: "20px",
                    background: "primary600"
                  }),
                  (0, import_jsx_runtime7.jsx)(Bt, {
                    disabled: isFormDisabled || !userHasPermission,
                    name: checkboxName,
                    // Keep same signature as packages/core/admin/admin/src/components/Roles/Permissions/index.js l.91
                    onCheckedChange: (value) => {
                      onChangeSimpleCheckbox({
                        target: {
                          name: checkboxName,
                          value: !!value
                        }
                      });
                    },
                    checked: hasConditions ? "indeterminate" : hasAllActionsSelected2
                  })
                ]
              }, actionId);
            })
          })
        ]
      }),
      (0, import_jsx_runtime7.jsx)(S, {
        bottom: "10px",
        right: "9px",
        position: "absolute",
        children: (0, import_jsx_runtime7.jsxs)(_d.Root, {
          open: isConditionModalOpen,
          onOpenChange: () => {
            setIsConditionModalOpen((prev) => !prev);
          },
          children: [
            (0, import_jsx_runtime7.jsx)(_d.Trigger, {
              children: (0, import_jsx_runtime7.jsx)(ConditionsButton, {
                hasConditions: doesConditionButtonHasConditions
              })
            }),
            (0, import_jsx_runtime7.jsx)(ConditionsModal, {
              headerBreadCrumbs: [
                label,
                "Settings.permissions.conditions.conditions"
              ],
              actions: checkboxesActions,
              isFormDisabled,
              isReadOnly: userPermissions !== void 0,
              onClose: () => {
                setIsConditionModalOpen(false);
              }
            })
          ]
        })
      })
    ]
  });
};
var generateCheckboxesActions = (availableActions, modifiedData, pathToData) => {
  return availableActions.map(({ actionId, isDisplayed, applyToProperties, label }) => {
    if (!isDisplayed) {
      return {
        actionId,
        hasSomeActionsSelected: false,
        isDisplayed
      };
    }
    const baseCheckboxNameArray = [
      ...pathToData.split(".."),
      actionId
    ];
    const checkboxNameArray = (0, import_isEmpty2.default)(applyToProperties) ? [
      ...baseCheckboxNameArray,
      "properties",
      "enabled"
    ] : baseCheckboxNameArray;
    const conditionsValue = (0, import_get3.default)(modifiedData, [
      ...baseCheckboxNameArray,
      "conditions"
    ], null);
    const baseCheckboxAction = {
      actionId,
      checkboxName: checkboxNameArray.join(".."),
      hasConditions: createArrayOfValues(conditionsValue).some((val) => val),
      isDisplayed,
      label,
      pathToConditionsObject: baseCheckboxNameArray
    };
    if ((0, import_isEmpty2.default)(applyToProperties)) {
      const value = (0, import_get3.default)(modifiedData, checkboxNameArray, false);
      return {
        ...baseCheckboxAction,
        hasAllActionsSelected: value,
        hasSomeActionsSelected: value,
        isParentCheckbox: false
      };
    }
    const mainData = (0, import_get3.default)(modifiedData, checkboxNameArray, null);
    const { hasAllActionsSelected, hasSomeActionsSelected } = getCheckboxState(mainData);
    return {
      ...baseCheckboxAction,
      hasAllActionsSelected,
      hasSomeActionsSelected,
      isParentCheckbox: true
    };
  });
};
var activeRowStyle = (theme, isActive) => `
  ${Wrapper2} {
    background-color: ${theme.colors.primary100};
    color: ${theme.colors.primary600};
    border-radius: ${isActive ? "2px 2px 0 0" : "2px"};
    font-weight: ${theme.fontWeights.bold};
  }

  ${Chevron} {
    display: flex;
  }
  ${ConditionsButton} {
    display: block;
  }

  &:focus-within {
    ${() => activeRowStyle(theme, isActive)}
  }
`;
var Wrapper2 = gt(C)`
  border: 1px solid transparent;
`;
var BoxWrapper = gt.div`
  display: inline-flex;
  min-width: 100%;
  position: relative;

  ${ConditionsButton} {
    display: none;
  }

  ${({ $isActive, theme }) => $isActive && activeRowStyle(theme, $isActive)}

  &:hover {
    ${({ theme, $isActive }) => activeRowStyle(theme, $isActive)}
  }
`;
var Cell = gt(C)`
  width: ${cellWidth};
  position: relative;
`;
var Chevron = gt(S)`
  display: none;

  svg {
    width: 1.4rem;
  }

  path {
    fill: ${({ theme }) => theme.colors.primary600};
  }
`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/GlobalActions.mjs
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var React6 = __toESM(require_react(), 1);
var import_get4 = __toESM(require_get(), 1);
var GlobalActions = ({ actions = [], isFormDisabled, kind }) => {
  const { formatMessage } = useIntl();
  const { modifiedData, onChangeCollectionTypeGlobalActionCheckbox, checkUserHasPermission } = usePermissionsDataManager();
  const displayedActions = actions.filter(({ subjects }) => subjects && subjects.length);
  const checkboxesState = React6.useMemo(() => {
    const actionsIds = displayedActions.map(({ actionId }) => actionId);
    const data = modifiedData[kind];
    const relatedActionsData = actionsIds.reduce((acc, actionId) => {
      Object.keys(data).forEach((ctUid) => {
        const actionIdData = (0, import_get4.default)(data, [
          ctUid,
          actionId
        ]);
        const actionIdState = {
          [ctUid]: removeConditionKeyFromData(actionIdData)
        };
        if (!acc[actionId]) {
          acc[actionId] = actionIdState;
        } else {
          acc[actionId] = {
            ...acc[actionId],
            ...actionIdState
          };
        }
      });
      return acc;
    }, {});
    const checkboxesState2 = Object.keys(relatedActionsData).reduce((acc, current) => {
      acc[current] = getCheckboxState(relatedActionsData[current]);
      return acc;
    }, {});
    return checkboxesState2;
  }, [
    modifiedData,
    displayedActions,
    kind
  ]);
  return (0, import_jsx_runtime8.jsx)(S, {
    paddingBottom: 4,
    paddingTop: 6,
    style: {
      paddingLeft: firstRowWidth
    },
    children: (0, import_jsx_runtime8.jsx)(C, {
      gap: 0,
      children: displayedActions.map(({ label, actionId, subjects }) => {
        const allSubjects = subjects || [];
        const userHasPermissionForAll = allSubjects.length > 0 && allSubjects.every((subject) => checkUserHasPermission(actionId, subject));
        return (0, import_jsx_runtime8.jsxs)(C, {
          shrink: 0,
          width: cellWidth,
          direction: "column",
          alignItems: "center",
          justifyContent: "center",
          gap: 3,
          children: [
            (0, import_jsx_runtime8.jsx)(A, {
              variant: "sigma",
              textColor: "neutral500",
              children: formatMessage({
                id: `Settings.roles.form.permissions.${label.toLowerCase()}`,
                defaultMessage: label
              })
            }),
            (0, import_jsx_runtime8.jsx)(Bt, {
              disabled: isFormDisabled || !userHasPermissionForAll,
              onCheckedChange: (value) => {
                onChangeCollectionTypeGlobalActionCheckbox(kind, actionId, !!value);
              },
              name: actionId,
              "aria-label": formatMessage({
                id: `Settings.permissions.select-all-by-permission`,
                defaultMessage: "Select all {label} permissions"
              }, {
                label: formatMessage({
                  id: `Settings.roles.form.permissions.${label.toLowerCase()}`,
                  defaultMessage: label
                })
              }),
              checked: (0, import_get4.default)(checkboxesState, [
                actionId,
                "hasSomeActionsSelected"
              ], false) ? "indeterminate" : (0, import_get4.default)(checkboxesState, [
                actionId,
                "hasAllActionsSelected"
              ], false)
            })
          ]
        }, actionId);
      })
    })
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/ContentTypes.mjs
var ContentTypes = ({ isFormDisabled, kind, layout: { actions, subjects } }) => {
  const sortedSubjects = [
    ...subjects
  ].sort((a, b) => a.label.localeCompare(b.label));
  return (0, import_jsx_runtime9.jsxs)(S, {
    background: "neutral0",
    children: [
      (0, import_jsx_runtime9.jsx)(GlobalActions, {
        actions,
        kind,
        isFormDisabled
      }),
      (0, import_jsx_runtime9.jsx)(ContentTypeCollapses, {
        actions,
        isFormDisabled,
        pathToData: kind,
        subjects: sortedSubjects
      })
    ]
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/PluginsAndSettings.mjs
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var React7 = __toESM(require_react(), 1);
var import_get5 = __toESM(require_get(), 1);
var PluginsAndSettingsPermissions = ({ layout, ...restProps }) => {
  return (0, import_jsx_runtime10.jsx)(S, {
    padding: 6,
    background: "neutral0",
    children: (0, import_jsx_runtime10.jsx)(dd.Root, {
      size: "M",
      children: layout.map(({ category, categoryId, childrenForm }, index) => {
        return (0, import_jsx_runtime10.jsx)(Row, {
          childrenForm,
          variant: index % 2 === 1 ? "primary" : "secondary",
          name: category,
          pathToData: [
            restProps.kind,
            categoryId
          ],
          ...restProps
        }, category);
      })
    })
  });
};
var Row = ({ childrenForm, kind, name, isFormDisabled = false, variant, pathToData }) => {
  const { formatMessage } = useIntl();
  const categoryName = name.split("::").pop() ?? "";
  const categoryDisplayName = categoryName === "upload" ? "Media Library" : capitalise(categoryName.replace(/-/g, " "));
  return (0, import_jsx_runtime10.jsxs)(dd.Item, {
    value: name,
    children: [
      (0, import_jsx_runtime10.jsx)(dd.Header, {
        variant,
        children: (0, import_jsx_runtime10.jsx)(dd.Trigger, {
          caretPosition: "right",
          description: `${formatMessage({
            id: "Settings.permissions.category",
            defaultMessage: categoryName
          }, {
            category: categoryName
          })} ${kind === "plugins" ? "plugin" : kind}`,
          children: categoryDisplayName
        })
      }),
      (0, import_jsx_runtime10.jsx)(dd.Content, {
        children: (0, import_jsx_runtime10.jsx)(S, {
          padding: 6,
          children: childrenForm.map(({ actions, subCategoryName, subCategoryId }) => (0, import_jsx_runtime10.jsx)(SubCategory, {
            actions,
            categoryName,
            isFormDisabled,
            subCategoryName,
            pathToData: [
              ...pathToData,
              subCategoryId
            ]
          }, subCategoryName))
        })
      })
    ]
  });
};
var SubCategory = ({ actions = [], categoryName, isFormDisabled, subCategoryName, pathToData }) => {
  const { modifiedData, onChangeParentCheckbox, onChangeSimpleCheckbox, checkUserHasPermission, userPermissions } = usePermissionsDataManager();
  const [isConditionModalOpen, setIsConditionModalOpen] = React7.useState(false);
  const { formatMessage } = useIntl();
  const mainData = (0, import_get5.default)(modifiedData, pathToData, {});
  const dataWithoutCondition = React7.useMemo(() => {
    return Object.keys(mainData).reduce((acc, current) => {
      acc[current] = removeConditionKeyFromData(mainData[current]);
      return acc;
    }, {});
  }, [
    mainData
  ]);
  const { hasAllActionsSelected, hasSomeActionsSelected } = getCheckboxState(dataWithoutCondition);
  const formattedActions = React7.useMemo(() => {
    return actions.map((action) => {
      const checkboxName = [
        ...pathToData,
        action.action,
        "properties",
        "enabled"
      ];
      const checkboxValue = (0, import_get5.default)(modifiedData, checkboxName, false);
      const conditionValue = (0, import_get5.default)(modifiedData, [
        ...pathToData,
        action.action,
        "conditions"
      ], {});
      const hasConditions = createArrayOfValues(conditionValue).some((val) => val);
      return {
        ...action,
        isDisplayed: checkboxValue,
        checkboxName: checkboxName.join(".."),
        hasSomeActionsSelected: checkboxValue,
        value: checkboxValue,
        hasConditions,
        label: action.displayName,
        actionId: action.action,
        pathToConditionsObject: [
          ...pathToData,
          action.action
        ]
      };
    });
  }, [
    actions,
    modifiedData,
    pathToData
  ]);
  const datum = (0, import_get5.default)(modifiedData, [
    ...pathToData
  ], {});
  const doesButtonHasCondition = createArrayOfValues(Object.entries(datum).reduce((acc, current) => {
    const [catName, { conditions }] = current;
    acc[catName] = conditions;
    return acc;
  }, {})).some((val) => val);
  return (0, import_jsx_runtime10.jsx)(import_jsx_runtime10.Fragment, {
    children: (0, import_jsx_runtime10.jsxs)(S, {
      children: [
        (0, import_jsx_runtime10.jsxs)(C, {
          justifyContent: "space-between",
          alignItems: "center",
          children: [
            (0, import_jsx_runtime10.jsx)(S, {
              paddingRight: 4,
              children: (0, import_jsx_runtime10.jsx)(A, {
                variant: "sigma",
                textColor: "neutral600",
                children: subCategoryName
              })
            }),
            (0, import_jsx_runtime10.jsx)(Border, {
              flex: 1
            }),
            (0, import_jsx_runtime10.jsx)(S, {
              paddingLeft: 4,
              children: (0, import_jsx_runtime10.jsx)(Bt, {
                name: pathToData.join(".."),
                disabled: isFormDisabled,
                // Keep same signature as packages/core/admin/admin/src/components/Roles/Permissions/index.js l.91
                onCheckedChange: (value) => {
                  onChangeParentCheckbox({
                    target: {
                      name: pathToData.join(".."),
                      value: !!value
                    }
                  });
                },
                checked: hasSomeActionsSelected ? "indeterminate" : hasAllActionsSelected,
                children: formatMessage({
                  id: "app.utils.select-all",
                  defaultMessage: "Select all"
                })
              })
            })
          ]
        }),
        (0, import_jsx_runtime10.jsxs)(C, {
          paddingTop: 6,
          paddingBottom: 6,
          children: [
            (0, import_jsx_runtime10.jsx)(Ld.Root, {
              gap: 2,
              style: {
                flex: 1
              },
              children: formattedActions.map(({ checkboxName, value, action, displayName, hasConditions }) => {
                const userHasPermission = checkUserHasPermission(action, null);
                return (0, import_jsx_runtime10.jsx)(Ld.Item, {
                  col: 4,
                  m: 6,
                  xs: 12,
                  direction: "column",
                  alignItems: "start",
                  children: (0, import_jsx_runtime10.jsx)(CheckboxWrapper, {
                    $disabled: isFormDisabled || !userHasPermission,
                    $hasConditions: hasConditions,
                    children: (0, import_jsx_runtime10.jsx)(Bt, {
                      name: checkboxName,
                      disabled: isFormDisabled || !userHasPermission,
                      // Keep same signature as packages/core/admin/admin/src/components/Roles/Permissions/index.js l.91
                      onCheckedChange: (value2) => {
                        onChangeSimpleCheckbox({
                          target: {
                            name: checkboxName,
                            value: !!value2
                          }
                        });
                      },
                      checked: value,
                      children: displayName
                    })
                  })
                }, action);
              })
            }),
            (0, import_jsx_runtime10.jsxs)(_d.Root, {
              open: isConditionModalOpen,
              onOpenChange: () => {
                setIsConditionModalOpen((prev) => !prev);
              },
              children: [
                (0, import_jsx_runtime10.jsx)(_d.Trigger, {
                  children: (0, import_jsx_runtime10.jsx)(ConditionsButton, {
                    hasConditions: doesButtonHasCondition
                  })
                }),
                (0, import_jsx_runtime10.jsx)(ConditionsModal, {
                  headerBreadCrumbs: [
                    categoryName,
                    subCategoryName
                  ],
                  actions: formattedActions,
                  isFormDisabled,
                  isReadOnly: userPermissions !== void 0,
                  onClose: () => {
                    setIsConditionModalOpen(false);
                  }
                })
              ]
            })
          ]
        })
      ]
    })
  });
};
var Border = gt(S)`
  align-self: center;
  border-top: 1px solid ${({ theme }) => theme.colors.neutral150};
`;
var CheckboxWrapper = gt.div`
  position: relative;
  word-break: keep-all;
  ${({ $hasConditions, $disabled, theme }) => $hasConditions && `
    &:before {
      content: '';
      position: absolute;
      top: -0.4rem;
      left: -0.8rem;
      width: 0.6rem;
      height: 0.6rem;
      border-radius: 2rem;
      background: ${$disabled ? theme.colors.neutral100 : theme.colors.primary600};
    }
  `}
`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/pages/Roles/components/Permissions.mjs
var TAB_LABELS = [
  {
    labelId: "app.components.LeftMenuLinkContainer.collectionTypes",
    defaultMessage: "Collection Types",
    id: "collectionTypes"
  },
  {
    labelId: "app.components.LeftMenuLinkContainer.singleTypes",
    id: "singleTypes",
    defaultMessage: "Single Types"
  },
  {
    labelId: "app.components.LeftMenuLinkContainer.plugins",
    defaultMessage: "Plugins",
    id: "plugins"
  },
  {
    labelId: "app.components.LeftMenuLinkContainer.settings",
    defaultMessage: "Settings",
    id: "settings"
  }
];
var Permissions = React8.forwardRef(({ layout, isFormDisabled, onLocaleValidationChange, permissions = [], userPermissions }, api) => {
  const [{ initialData, layouts, modifiedData }, dispatch] = React8.useReducer(reducer, initialState, () => init(layout, permissions));
  const { formatMessage } = useIntl();
  React8.useEffect(() => {
    onLocaleValidationChange == null ? void 0 : onLocaleValidationChange(hasLocaleValidationErrors(modifiedData));
  }, [
    modifiedData,
    onLocaleValidationChange
  ]);
  React8.useImperativeHandle(api, () => {
    return {
      getPermissions() {
        const collectionTypesDiff = difference(initialData.collectionTypes, modifiedData.collectionTypes);
        const singleTypesDiff = difference(initialData.singleTypes, modifiedData.singleTypes);
        const contentTypesDiff = {
          ...collectionTypesDiff,
          ...singleTypesDiff
        };
        let didUpdateConditions;
        if ((0, import_isEmpty3.default)(contentTypesDiff)) {
          didUpdateConditions = false;
        } else {
          didUpdateConditions = Object.values(contentTypesDiff).some((permission = {}) => {
            return Object.values(permission).some((permissionValue) => (0, import_has2.default)(permissionValue, "conditions"));
          });
        }
        const permissionsToSend = formatPermissionsForAPI(modifiedData).map((perm) => restoreNullLocalesIfUnchanged(perm, permissions, initialData, modifiedData));
        return {
          permissionsToSend,
          didUpdateConditions
        };
      },
      hasLocaleValidationErrors() {
        return hasLocaleValidationErrors(modifiedData);
      },
      resetForm() {
        dispatch({
          type: "RESET_FORM"
        });
      },
      setFormAfterSubmit() {
        dispatch({
          type: "SET_FORM_AFTER_SUBMIT"
        });
      }
    };
  });
  const handleChangeCollectionTypeLeftActionRowCheckbox = React8.useCallback((pathToCollectionType, propertyName, rowName, value) => {
    dispatch({
      type: "ON_CHANGE_COLLECTION_TYPE_ROW_LEFT_CHECKBOX",
      pathToCollectionType,
      propertyName,
      rowName,
      value,
      userPermissions
    });
  }, [
    userPermissions
  ]);
  const handleChangeCollectionTypeGlobalActionCheckbox = React8.useCallback((collectionTypeKind, actionId, value) => {
    dispatch({
      type: "ON_CHANGE_COLLECTION_TYPE_GLOBAL_ACTION_CHECKBOX",
      collectionTypeKind,
      actionId,
      value,
      userPermissions
    });
  }, [
    userPermissions
  ]);
  const handleChangeConditions = React8.useCallback((conditions) => {
    dispatch({
      type: "ON_CHANGE_CONDITIONS",
      conditions,
      userPermissions
    });
  }, [
    userPermissions
  ]);
  const handleChangeSimpleCheckbox = React8.useCallback(({ target: { name, value } }) => {
    dispatch({
      type: "ON_CHANGE_SIMPLE_CHECKBOX",
      keys: name,
      value,
      userPermissions
    });
  }, [
    userPermissions
  ]);
  const handleChangeParentCheckbox = React8.useCallback(({ target: { name, value } }) => {
    dispatch({
      type: "ON_CHANGE_TOGGLE_PARENT_CHECKBOX",
      keys: name,
      value,
      userPermissions
    });
  }, [
    userPermissions
  ]);
  return (0, import_jsx_runtime11.jsx)(PermissionsDataManagerProvider, {
    availableConditions: layout.conditions,
    modifiedData,
    onChangeConditions: handleChangeConditions,
    onChangeSimpleCheckbox: handleChangeSimpleCheckbox,
    onChangeParentCheckbox: handleChangeParentCheckbox,
    onChangeCollectionTypeLeftActionRowCheckbox: handleChangeCollectionTypeLeftActionRowCheckbox,
    onChangeCollectionTypeGlobalActionCheckbox: handleChangeCollectionTypeGlobalActionCheckbox,
    userPermissions,
    children: (0, import_jsx_runtime11.jsxs)(cu.Root, {
      defaultValue: TAB_LABELS[0].id,
      children: [
        (0, import_jsx_runtime11.jsx)(cu.List, {
          "aria-label": formatMessage({
            id: "Settings.permissions.users.tabs.label",
            defaultMessage: "Tabs Permissions"
          }),
          children: TAB_LABELS.map((tabLabel) => (0, import_jsx_runtime11.jsx)(cu.Trigger, {
            value: tabLabel.id,
            children: formatMessage({
              id: tabLabel.labelId,
              defaultMessage: tabLabel.defaultMessage
            })
          }, tabLabel.id))
        }),
        (0, import_jsx_runtime11.jsx)(cu.Content, {
          value: TAB_LABELS[0].id,
          children: (0, import_jsx_runtime11.jsx)(ContentTypes, {
            layout: layouts.collectionTypes,
            kind: "collectionTypes",
            isFormDisabled
          })
        }),
        (0, import_jsx_runtime11.jsx)(cu.Content, {
          value: TAB_LABELS[1].id,
          children: (0, import_jsx_runtime11.jsx)(ContentTypes, {
            layout: layouts.singleTypes,
            kind: "singleTypes",
            isFormDisabled
          })
        }),
        (0, import_jsx_runtime11.jsx)(cu.Content, {
          value: TAB_LABELS[2].id,
          children: (0, import_jsx_runtime11.jsx)(PluginsAndSettingsPermissions, {
            layout: layouts.plugins,
            kind: "plugins",
            isFormDisabled
          })
        }),
        (0, import_jsx_runtime11.jsx)(cu.Content, {
          value: TAB_LABELS[3].id,
          children: (0, import_jsx_runtime11.jsx)(PluginsAndSettingsPermissions, {
            layout: layouts.settings,
            kind: "settings",
            isFormDisabled
          })
        })
      ]
    })
  });
});
var initialState = {
  initialData: {},
  modifiedData: {},
  layouts: {}
};
var buildInheritedConditionsFromExisting = (existing, enabledConditions = []) => {
  if (!isObject(existing)) {
    return void 0;
  }
  const enabled = new Set(enabledConditions);
  return Object.keys(existing).reduce((acc, key) => {
    acc[key] = enabled.has(key);
    return acc;
  }, {});
};
var inheritConditionsAtPath = (data, pathToActionObject, actionId, subject, userPermissions) => {
  if (userPermissions === void 0) {
    return;
  }
  const matchingPermission = userPermissions.find((perm) => perm.action === actionId && perm.subject === subject);
  if (matchingPermission === void 0) {
    return;
  }
  const obj = data;
  const existingConditions = (0, import_get6.default)(obj, [
    ...pathToActionObject,
    "conditions"
  ], void 0);
  const nextConditions = buildInheritedConditionsFromExisting(existingConditions, matchingPermission.conditions ?? []);
  if (nextConditions) {
    (0, import_set.default)(obj, [
      ...pathToActionObject,
      "conditions"
    ], nextConditions);
  }
};
var reducer = (state, action) => fn(state, (draftState) => {
  switch (action.type) {
    case "ON_CHANGE_COLLECTION_TYPE_GLOBAL_ACTION_CHECKBOX": {
      const { collectionTypeKind, actionId, value, userPermissions } = action;
      const pathToData = [
        "modifiedData",
        collectionTypeKind
      ];
      Object.keys((0, import_get6.default)(state, pathToData)).forEach((collectionType) => {
        const collectionTypeActionData = (0, import_get6.default)(state, [
          ...pathToData,
          collectionType,
          actionId
        ], void 0);
        if (collectionTypeActionData) {
          const subjectPermissionChecker = createFieldPermissionChecker(actionId, collectionType, userPermissions);
          let updatedValues = updateValuesWithPermissions(collectionTypeActionData, value, subjectPermissionChecker);
          if (value === true) {
            inheritConditionsAtPath(updatedValues, [], actionId, collectionType, userPermissions);
          }
          if (value === false && updatedValues.conditions !== void 0) {
            const updatedConditions = updateValues(updatedValues.conditions, false);
            updatedValues = {
              ...updatedValues,
              conditions: updatedConditions
            };
          }
          (0, import_set.default)(draftState, [
            ...pathToData,
            collectionType,
            actionId
          ], updatedValues);
        }
      });
      break;
    }
    case "ON_CHANGE_COLLECTION_TYPE_ROW_LEFT_CHECKBOX": {
      const { pathToCollectionType, propertyName, rowName, value, userPermissions } = action;
      let nextModifiedDataState = (0, import_cloneDeep.default)(state.modifiedData);
      const pathToModifiedDataCollectionType = pathToCollectionType.split("..");
      const objToUpdate = (0, import_get6.default)(nextModifiedDataState, pathToModifiedDataCollectionType, {});
      const subject = pathToModifiedDataCollectionType[pathToModifiedDataCollectionType.length - 1];
      Object.keys(objToUpdate).forEach((actionId) => {
        if ((0, import_has2.default)(objToUpdate[actionId], `properties.${propertyName}`)) {
          const objValue = (0, import_get6.default)(objToUpdate, [
            actionId,
            "properties",
            propertyName,
            rowName
          ]);
          const pathToDataToSet = [
            ...pathToModifiedDataCollectionType,
            actionId,
            "properties",
            propertyName,
            rowName
          ];
          if (!isObject(objValue)) {
            if (userPermissions !== void 0 && propertyName === "fields") {
              const hasPermission = userPermissions.some((perm) => {
                var _a;
                if (perm.action !== actionId || perm.subject !== subject) return false;
                const fields = (_a = perm.properties) == null ? void 0 : _a.fields;
                if (fields === null || fields === void 0) return true;
                if (Array.isArray(fields) && fields.length === 0) return false;
                return Array.isArray(fields) && fields.some((f) => rowName === f || rowName.startsWith(`${f}.`));
              });
              if (hasPermission === true) {
                (0, import_set.default)(nextModifiedDataState, pathToDataToSet, value);
                if (value === true) {
                  inheritConditionsAtPath(nextModifiedDataState, [
                    ...pathToModifiedDataCollectionType,
                    actionId
                  ], actionId, subject, userPermissions);
                }
              }
            } else {
              (0, import_set.default)(nextModifiedDataState, pathToDataToSet, value);
              if (value === true && userPermissions !== void 0) {
                inheritConditionsAtPath(nextModifiedDataState, [
                  ...pathToModifiedDataCollectionType,
                  actionId
                ], actionId, subject, userPermissions);
              }
            }
          } else {
            const permissionChecker = userPermissions !== void 0 && propertyName === "fields" ? (path) => {
              const fullFieldPath = [
                rowName,
                ...path
              ].join(".");
              const checker = createFieldPermissionChecker(actionId, subject, userPermissions);
              return checker === void 0 ? true : checker([
                "properties",
                "fields",
                fullFieldPath
              ]);
            } : void 0;
            const updatedValue = updateValuesWithPermissions(objValue, value, permissionChecker);
            (0, import_set.default)(nextModifiedDataState, pathToDataToSet, updatedValue);
            if (value === true && userPermissions !== void 0) {
              inheritConditionsAtPath(nextModifiedDataState, [
                ...pathToModifiedDataCollectionType,
                actionId
              ], actionId, subject, userPermissions);
            }
          }
        }
      });
      if (value === false) {
        nextModifiedDataState = updateConditionsToFalse(nextModifiedDataState);
      }
      (0, import_set.default)(draftState, "modifiedData", nextModifiedDataState);
      break;
    }
    case "ON_CHANGE_CONDITIONS": {
      if (action.userPermissions !== void 0) {
        break;
      }
      Object.entries(action.conditions).forEach((array) => {
        const [stringPathToData, conditionsToUpdate] = array;
        (0, import_set.default)(draftState, [
          "modifiedData",
          ...stringPathToData.split(".."),
          "conditions"
        ], conditionsToUpdate);
      });
      break;
    }
    case "ON_CHANGE_SIMPLE_CHECKBOX": {
      let nextModifiedDataState = (0, import_cloneDeep.default)(state.modifiedData);
      const keysArray = action.keys.split("..");
      (0, import_set.default)(nextModifiedDataState, [
        ...keysArray
      ], action.value);
      if (action.value === true && action.userPermissions !== void 0) {
        const propertiesIndex = keysArray.indexOf("properties");
        if (propertiesIndex > 0) {
          const actionId = keysArray[propertiesIndex - 1];
          const root = keysArray[0];
          if ((root === "collectionTypes" || root === "singleTypes") && keysArray.length >= 3) {
            const subject = keysArray[1];
            inheritConditionsAtPath(nextModifiedDataState, [
              root,
              subject,
              actionId
            ], actionId, subject, action.userPermissions);
          } else {
            const pathToActionObject = keysArray.slice(0, propertiesIndex);
            inheritConditionsAtPath(nextModifiedDataState, pathToActionObject, actionId, null, action.userPermissions);
          }
        }
      }
      if (action.value === false) {
        nextModifiedDataState = updateConditionsToFalse(nextModifiedDataState);
      }
      (0, import_set.default)(draftState, "modifiedData", nextModifiedDataState);
      break;
    }
    case "ON_CHANGE_TOGGLE_PARENT_CHECKBOX": {
      const { keys, value, userPermissions } = action;
      const pathToValue = keys.split("..");
      let nextModifiedDataState = (0, import_cloneDeep.default)(state.modifiedData);
      const oldValues = (0, import_get6.default)(nextModifiedDataState, pathToValue, {});
      const root = pathToValue[0];
      const isPluginOrSetting = root === "plugins" || root === "settings";
      let actionId;
      let subject;
      if (isPluginOrSetting) {
        subject = null;
      } else {
        if (pathToValue.length >= 2) {
          subject = pathToValue[1];
        }
        if (pathToValue.length >= 3) {
          actionId = pathToValue[2];
        }
      }
      const permissionChecker = createDynamicActionPermissionChecker(subject, actionId, userPermissions);
      const updatedValues = updateValuesWithPermissions(oldValues, value, permissionChecker);
      if (value === true && userPermissions !== void 0) {
        const subjectForLookup = root === "collectionTypes" || root === "singleTypes" ? subject ?? null : null;
        if (actionId !== void 0 && subjectForLookup !== null) {
          inheritConditionsAtPath(updatedValues, [], actionId, subjectForLookup, userPermissions);
        } else if (isObject(updatedValues)) {
          const updatedValuesObj = updatedValues;
          Object.keys(updatedValuesObj).forEach((key) => {
            const maybeActionObj = updatedValuesObj[key];
            if (isObject(maybeActionObj)) {
              inheritConditionsAtPath(maybeActionObj, [], key, subjectForLookup, userPermissions);
            }
          });
        }
      }
      (0, import_set.default)(nextModifiedDataState, pathToValue, updatedValues);
      if (value === false) {
        nextModifiedDataState = updateConditionsToFalse(nextModifiedDataState);
      }
      (0, import_set.default)(draftState, [
        "modifiedData"
      ], nextModifiedDataState);
      break;
    }
    case "RESET_FORM": {
      draftState.modifiedData = state.initialData;
      break;
    }
    case "SET_FORM_AFTER_SUBMIT": {
      draftState.initialData = state.modifiedData;
      break;
    }
    default:
      return draftState;
  }
});
var restoreNullLocalesIfUnchanged = (perm, originalPermissions, initialData, modifiedData) => {
  var _a, _b, _c, _d2, _e;
  if (!perm.subject) return perm;
  const original = originalPermissions.find((p) => p.action === perm.action && p.subject === perm.subject);
  if (((_a = original == null ? void 0 : original.properties) == null ? void 0 : _a.locales) !== null) return perm;
  const kind = perm.subject in modifiedData.collectionTypes ? "collectionTypes" : perm.subject in modifiedData.singleTypes ? "singleTypes" : null;
  if (!kind) return perm;
  const initialActionForm = (_c = (_b = initialData[kind]) == null ? void 0 : _b[perm.subject]) == null ? void 0 : _c[perm.action];
  const modifiedActionForm = (_e = (_d2 = modifiedData[kind]) == null ? void 0 : _d2[perm.subject]) == null ? void 0 : _e[perm.action];
  if (!initialActionForm || !modifiedActionForm) return perm;
  const initialLocaleEntry = initialActionForm.properties["locales"];
  const modifiedLocaleEntry = modifiedActionForm.properties["locales"];
  if (!initialLocaleEntry || typeof initialLocaleEntry === "boolean" || !modifiedLocaleEntry || typeof modifiedLocaleEntry === "boolean") {
    return perm;
  }
  const localesUnchanged = Object.keys(initialLocaleEntry).length === Object.keys(modifiedLocaleEntry).length && Object.entries(initialLocaleEntry).every(([locale, val]) => modifiedLocaleEntry[locale] === val);
  if (!localesUnchanged) return perm;
  return {
    ...perm,
    properties: {
      ...perm.properties,
      locales: null
    }
  };
};
var init = (layout, permissions) => {
  const { conditions, sections: { collectionTypes, singleTypes, plugins, settings } } = layout;
  const layouts = {
    collectionTypes,
    singleTypes,
    plugins: formatLayout(plugins, "plugin"),
    settings: formatLayout(settings, "category")
  };
  const defaultForm = {
    collectionTypes: createDefaultCTForm(collectionTypes, conditions, permissions),
    singleTypes: createDefaultCTForm(singleTypes, conditions, permissions),
    plugins: createDefaultForm(layouts.plugins, conditions, permissions),
    settings: createDefaultForm(layouts.settings, conditions, permissions)
  };
  return {
    initialData: defaultForm,
    modifiedData: defaultForm,
    layouts
  };
};

export {
  Permissions
};
//# sourceMappingURL=chunk-JGVDOJLW.js.map
