import {
  useTypedSelector
} from "./chunk-U3VF4G3M.js";
import {
  BulkActionsRenderer,
  TableActions
} from "./chunk-ZDJYMBBA.js";
import {
  DocumentRBAC,
  InjectionZone,
  PERMISSIONS,
  getDisplayName,
  getRelationLabel,
  prefixFileUrlWithBackendUrl,
  useDebounce,
  useDocumentRBAC,
  useGetRelationsQuery,
  usePersistentPartialQueryParams,
  usePrev
} from "./chunk-O3ADBBC4.js";
import "./chunk-OIM6YED5.js";
import {
  DocumentStatus
} from "./chunk-BCGA3ATX.js";
import "./chunk-AT2I3J2U.js";
import "./chunk-F4A3MJG3.js";
import {
  CREATOR_FIELDS,
  HOOKS,
  buildValidParams,
  checkIfAttributeIsDisplayable,
  convertListLayoutToFieldLayouts,
  getMainField,
  useContentTypeSchema,
  useDoc,
  useDocumentLayout,
  useGetAllDocumentsQuery
} from "./chunk-JPBDOX5J.js";
import "./chunk-CB2UCVBY.js";
import {
  getTranslation
} from "./chunk-BVJQKRXI.js";
import "./chunk-SG3YXALT.js";
import "./chunk-LVMHP6GO.js";
import "./chunk-7FPOP2S5.js";
import "./chunk-Q535U6BT.js";
import "./chunk-D7FI3GAF.js";
import "./chunk-4R2QA5NM.js";
import "./chunk-TKXHHSNW.js";
import "./chunk-XTTXQ7FR.js";
import "./chunk-QHCTYJ6K.js";
import "./chunk-TKZBDVP5.js";
import {
  useClipboard
} from "./chunk-LS2USQHL.js";
import {
  tours,
  useScopedPersistentState
} from "./chunk-K4JPZJYK.js";
import "./chunk-PA2ESAGW.js";
import "./chunk-SLUE2XUX.js";
import "./chunk-HRURINY4.js";
import "./chunk-JES3NNDO.js";
import {
  SearchInput
} from "./chunk-F3Y2U5J7.js";
import "./chunk-PUECFR7A.js";
import {
  BackButton
} from "./chunk-WVMUAD6S.js";
import "./chunk-BTNRU3I6.js";
import {
  Filters,
  Pagination
} from "./chunk-YZKIGY5W.js";
import {
  Table,
  useTable
} from "./chunk-XNMTWOF7.js";
import "./chunk-NCYXOGJJ.js";
import "./chunk-IFOFBKTA.js";
import {
  useAdminUsers
} from "./chunk-AOU4ZZD3.js";
import "./chunk-SOVNMIKL.js";
import {
  useField
} from "./chunk-2IWTCEYS.js";
import {
  useRBAC
} from "./chunk-SHEU3VT4.js";
import {
  Layouts
} from "./chunk-767BZMIR.js";
import {
  useIsMobile
} from "./chunk-A274YVTO.js";
import "./chunk-L4VZK4CF.js";
import {
  useTracking
} from "./chunk-QXVVXECA.js";
import "./chunk-6KQUGTAR.js";
import {
  Page,
  useAPIErrorHandler
} from "./chunk-WZNNPD7U.js";
import {
  useAuth,
  useQueryParams,
  useStrapiApp,
  withEncodedUserParams
} from "./chunk-HHY6TNZ5.js";
import {
  useNotification
} from "./chunk-52FV5OAT.js";
import "./chunk-NE2H5B4K.js";
import "./chunk-ALJOKJHY.js";
import "./chunk-ZISMY363.js";
import "./chunk-FH7EDXAD.js";
import "./chunk-LCUEZJJN.js";
import "./chunk-2A7DKUQA.js";
import "./chunk-TVYQEFKB.js";
import "./chunk-T5ATI4SA.js";
import "./chunk-MSCD7MZY.js";
import "./chunk-MBK4V2X7.js";
import {
  require_isEmpty
} from "./chunk-SFCV45I7.js";
import "./chunk-I4RARRLM.js";
import "./chunk-BC4MOUEJ.js";
import "./chunk-JRCQYP7F.js";
import "./chunk-36WB3I5J.js";
import "./chunk-45QSVGD5.js";
import "./chunk-KS6HIU44.js";
import {
  parseISO
} from "./chunk-D4WYVNVM.js";
import "./chunk-MMOBCIZG.js";
import "./chunk-OG6FDUVQ.js";
import "./chunk-A6QRMII5.js";
import "./chunk-3DRZIGZ7.js";
import "./chunk-AJ6CDECK.js";
import "./chunk-YULRRBBU.js";
import "./chunk-KVWVHPCR.js";
import {
  K
} from "./chunk-Y5S667H6.js";
import "./chunk-OJ6RZTLV.js";
import {
  require_lib
} from "./chunk-NZZAX2AB.js";
import "./chunk-T7T43G5B.js";
import {
  require_toString
} from "./chunk-NR6XEPE4.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import "./chunk-HAUWLGB7.js";
import "./chunk-VQOOZHFO.js";
import "./chunk-3MST3H25.js";
import {
  Link,
  NavLink,
  useNavigate,
  useParams
} from "./chunk-QBM2MGBP.js";
import "./chunk-AHJOALQ2.js";
import "./chunk-U5ASKYVL.js";
import "./chunk-QQEUXDAQ.js";
import "./chunk-LHXIEHKN.js";
import {
  A,
  Bt,
  C,
  Ct,
  Hd,
  Ki,
  Md,
  Mt,
  Nr,
  Qt,
  S,
  Vi,
  Wd,
  Xd,
  be,
  bu,
  gd,
  hd,
  kt
} from "./chunk-7ZODKSNG.js";
import "./chunk-SXAHFOGA.js";
import "./chunk-6I7FUUCQ.js";
import "./chunk-JDJCDU5Q.js";
import {
  C5,
  V3,
  en,
  y5
} from "./chunk-D4B4CG5U.js";
import "./chunk-VQHBTUHH.js";
import "./chunk-PC7RLLVI.js";
import "./chunk-AHKO7ZVV.js";
import "./chunk-JCO3CRJF.js";
import "./chunk-HHU3PYNH.js";
import "./chunk-OYIDWRUX.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  gt
} from "./chunk-4N2AQQ2K.js";
import "./chunk-4MZT7456.js";
import "./chunk-QBO3FZHQ.js";
import "./chunk-KKBG5U5K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/ListViewPage.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var React3 = __toESM(require_react(), 1);
var import_qs2 = __toESM(require_lib(), 1);

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/Filters.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var { INJECT_LIST_VIEW_FILTERS } = HOOKS;
var NOT_ALLOWED_FILTERS = [
  "json",
  "component",
  "media",
  "richtext",
  "dynamiczone",
  "password",
  "blocks"
];
var DEFAULT_ALLOWED_FILTERS = [
  "createdAt",
  "updatedAt"
];
var USER_FILTER_ATTRIBUTES = [
  ...CREATOR_FIELDS,
  "strapi_assignee"
];
var Root = ({ disabled, schema, layout, children }) => {
  var _a;
  const { attributes, uid: model, options } = schema;
  const { formatMessage, locale } = useIntl();
  const { trackUsage } = useTracking();
  const allPermissions = useAuth("FiltersImpl", (state) => state.permissions);
  const [{ query }] = useQueryParams();
  const { schemas } = useContentTypeSchema();
  const runHookWaterfall = useStrapiApp("FiltersImpl", ({ runHookWaterfall: runHookWaterfall2 }) => runHookWaterfall2);
  const canReadAdminUsers = React.useMemo(() => allPermissions.filter((permission) => permission.action === "admin::users.read" && permission.subject === null).length > 0, [
    allPermissions
  ]);
  const selectedUserIds = (((_a = query == null ? void 0 : query.filters) == null ? void 0 : _a.$and) ?? []).reduce((acc, filter) => {
    const [key, value] = Object.entries(filter)[0];
    if (typeof value.id !== "object") {
      return acc;
    }
    const id = value.id.$eq || value.id.$ne;
    const attribute = attributes[key];
    const isAdminUserRelation = (attribute == null ? void 0 : attribute.type) === "relation" && "target" in attribute && attribute.target === "admin::user";
    if (id && (isAdminUserRelation || USER_FILTER_ATTRIBUTES.includes(key)) && !acc.includes(id)) {
      acc.push(id);
    }
    return acc;
  }, []);
  const { data: userData } = useAdminUsers({
    filters: {
      id: {
        $in: selectedUserIds
      }
    }
  }, {
    // fetch the list of admin users only if the filter contains users and the
    // current user has permissions to display users
    skip: selectedUserIds.length === 0 || !canReadAdminUsers
  });
  const { users = [] } = userData ?? {};
  const formatter = Qt(locale, {
    sensitivity: "base"
  });
  const displayedFilters = React.useMemo(() => {
    const [{ properties: { fields = [] } = {
      fields: []
    } }] = allPermissions.filter((permission) => permission.action === "plugin::content-manager.explorer.read" && permission.subject === model);
    const allowedFields = fields.filter((field) => {
      const attribute = attributes[field] ?? {};
      return attribute.type && !NOT_ALLOWED_FILTERS.includes(attribute.type);
    });
    const baseFilters = [
      "id",
      "documentId",
      ...allowedFields,
      ...DEFAULT_ALLOWED_FILTERS,
      ...canReadAdminUsers ? CREATOR_FIELDS : [],
      ...(options == null ? void 0 : options.draftAndPublish) === true ? [
        "__status"
      ] : []
    ].map((name) => {
      var _a2;
      if (name === "__status") {
        return {
          name: "__status",
          type: "enumeration",
          label: formatMessage({
            id: getTranslation("containers.list.filters.status"),
            defaultMessage: "Status"
          }),
          operators: [
            {
              label: formatMessage({
                id: "components.FilterOptions.FILTER_TYPES.$eq",
                defaultMessage: "is"
              }),
              value: "$eq"
            }
          ],
          options: [
            {
              label: formatMessage({
                id: getTranslation("containers.List.statusFilter.draft"),
                defaultMessage: "Draft (never published)"
              }),
              value: "draft"
            },
            {
              label: formatMessage({
                id: getTranslation("containers.List.statusFilter.published"),
                defaultMessage: "Published (all)"
              }),
              value: "published"
            },
            {
              label: formatMessage({
                id: getTranslation("containers.List.statusFilter.publishedModified"),
                defaultMessage: "Published (modified)"
              }),
              value: "published-modified"
            },
            {
              label: formatMessage({
                id: getTranslation("containers.List.statusFilter.publishedUnmodified"),
                defaultMessage: "Published (unmodified)"
              }),
              value: "published-unmodified"
            }
          ]
        };
      }
      const attribute = attributes[name];
      if (!attribute || NOT_ALLOWED_FILTERS.includes(attribute.type)) {
        return null;
      }
      const fieldMetadata = (_a2 = layout.metadatas) == null ? void 0 : _a2[name];
      if (!fieldMetadata) {
        return null;
      }
      const { mainField: mainFieldName = "", label } = fieldMetadata;
      let filter = {
        name,
        label: label ?? "",
        mainField: getMainField(attribute, mainFieldName, {
          schemas,
          components: {}
        }),
        type: attribute.type
      };
      if (attribute.type === "relation" && "target" in attribute && attribute.target === "admin::user") {
        filter = {
          ...filter,
          input: AdminUsersFilter,
          options: users.map((user) => ({
            label: getDisplayName(user),
            value: user.id.toString()
          })),
          operators: [
            {
              label: formatMessage({
                id: "components.FilterOptions.FILTER_TYPES.$eq",
                defaultMessage: "is"
              }),
              value: "$eq"
            },
            {
              label: formatMessage({
                id: "components.FilterOptions.FILTER_TYPES.$ne",
                defaultMessage: "is not"
              }),
              value: "$ne"
            }
          ],
          mainField: {
            name: "id",
            type: "integer"
          }
        };
      }
      if (attribute.type === "enumeration") {
        filter = {
          ...filter,
          options: attribute.enum.map((value) => ({
            label: formatMessage({
              id: value,
              defaultMessage: value
            }),
            value
          }))
        };
      }
      return filter;
    }).filter(Boolean);
    const { displayedFilters: extendedFilters } = runHookWaterfall(INJECT_LIST_VIEW_FILTERS, {
      displayedFilters: baseFilters,
      layout
    });
    const resolveLabel = (label) => typeof label === "string" ? label : formatMessage(label);
    const userOptions = users.map((user) => ({
      label: getDisplayName(user),
      value: user.id.toString()
    }));
    const formatted = extendedFilters.map((filter) => {
      var _a2, _b;
      const next = {
        ...filter,
        label: resolveLabel(filter.label),
        operators: (_a2 = filter.operators) == null ? void 0 : _a2.map((op) => ({
          ...op,
          label: resolveLabel(op.label)
        }))
      };
      if (USER_FILTER_ATTRIBUTES.includes(filter.name) && !((_b = next.options) == null ? void 0 : _b.length)) {
        next.options = userOptions;
      }
      return next;
    });
    return formatted.toSorted((a, b) => formatter.compare(a.label, b.label));
  }, [
    allPermissions,
    canReadAdminUsers,
    model,
    attributes,
    schemas,
    layout,
    users,
    formatMessage,
    formatter,
    options == null ? void 0 : options.draftAndPublish,
    runHookWaterfall
  ]);
  const onOpenChange = (isOpen) => {
    if (isOpen) {
      trackUsage("willFilterEntries");
    }
  };
  const handleFilterChange = (data) => {
    const attribute = attributes[data.name];
    if (attribute) {
      trackUsage("didFilterEntries", {
        useRelation: attribute.type === "relation"
      });
    }
  };
  return (0, import_jsx_runtime.jsx)(Filters.Root, {
    disabled,
    options: displayedFilters,
    onOpenChange,
    onChange: handleFilterChange,
    children
  });
};
var AdminUsersFilter = ({ name }) => {
  const [pageSize, setPageSize] = React.useState(10);
  const [search, setSearch] = React.useState("");
  const { formatMessage } = useIntl();
  const debouncedSearch = useDebounce(search, 300);
  const { data, isLoading } = useAdminUsers({
    pageSize,
    _q: debouncedSearch
  });
  const field = useField(name);
  const handleOpenChange = (isOpen) => {
    if (!isOpen) {
      setPageSize(10);
    }
  };
  const { users = [], pagination } = data ?? {};
  const { pageCount = 1, page = 1 } = pagination ?? {};
  return (0, import_jsx_runtime.jsx)(Vi, {
    value: field.value,
    "aria-label": formatMessage({
      id: "content-manager.components.Filters.usersSelect.label",
      defaultMessage: "Search and select a user to filter"
    }),
    onOpenChange: handleOpenChange,
    onChange: (value) => field.onChange(name, value),
    loading: isLoading,
    onLoadMore: () => setPageSize(pageSize + 10),
    hasMoreItems: page < pageCount,
    onInputChange: (e) => {
      setSearch(e.currentTarget.value);
    },
    children: users.map((user) => {
      return (0, import_jsx_runtime.jsx)(Ki, {
        value: user.id.toString(),
        children: getDisplayName(user)
      }, user.id);
    })
  });
};
var listViewFilters = {
  Root,
  Trigger: Filters.Trigger,
  Popover: Filters.Popover,
  List: Filters.List
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/TableCells/CellContent.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var import_isEmpty = __toESM(require_isEmpty(), 1);

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/TableCells/CellValue.mjs
var import_toString = __toESM(require_toString(), 1);
var CellValue = ({ isIdColumn = false, type, value }) => {
  const { formatDate, formatTime, formatNumber, formatMessage } = useIntl();
  let formattedValue = value;
  if (type === "date" && typeof value === "string") {
    formattedValue = formatDate(parseISO(value), {
      dateStyle: "full"
    });
  }
  if (type === "datetime") {
    formattedValue = formatDate(value, {
      dateStyle: "full",
      timeStyle: "short"
    });
  }
  if (type === "time" && typeof value === "string") {
    const [hour, minute, second] = value.split(":");
    const date = /* @__PURE__ */ new Date();
    date.setHours(Number(hour));
    date.setMinutes(Number(minute));
    date.setSeconds(Number(second));
    formattedValue = formatTime(date, {
      timeStyle: "short"
    });
  }
  if ([
    "float",
    "decimal"
  ].includes(type)) {
    formattedValue = formatNumber(value, {
      // Should be kept in sync with the corresponding value
      // in the design-system/NumberInput: https://github.com/strapi/design-system/blob/main/packages/strapi-design-system/src/NumberInput/NumberInput.js#L53
      maximumFractionDigits: 20
    });
  }
  if (type === "boolean") {
    const booleanValue = Boolean(value);
    formattedValue = formatMessage({
      id: booleanValue ? "app.components.ToggleCheckbox.on-label" : "app.components.ToggleCheckbox.off-label",
      defaultMessage: booleanValue ? "true" : "false"
    });
  }
  if ([
    "integer",
    "biginteger"
  ].includes(type)) {
    formattedValue = formatNumber(value, {
      maximumFractionDigits: 0,
      useGrouping: !isIdColumn
    });
  }
  return (0, import_toString.default)(formattedValue);
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/TableCells/Components.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var SingleComponent = ({ content, mainField }) => {
  if (!mainField) {
    return null;
  }
  return (0, import_jsx_runtime2.jsx)(Mt, {
    label: content[mainField.name],
    children: (0, import_jsx_runtime2.jsx)(A, {
      maxWidth: "25rem",
      textColor: "neutral800",
      ellipsis: true,
      children: (0, import_jsx_runtime2.jsx)(CellValue, {
        type: mainField.type,
        value: content[mainField.name]
      })
    })
  });
};
var RepeatableComponent = ({ content, mainField }) => {
  const { formatMessage } = useIntl();
  if (!mainField) {
    return null;
  }
  return (0, import_jsx_runtime2.jsxs)(gd.Root, {
    children: [
      (0, import_jsx_runtime2.jsxs)(gd.Trigger, {
        onClick: (e) => e.stopPropagation(),
        children: [
          (0, import_jsx_runtime2.jsx)(Nr, {
            children: content.length
          }),
          formatMessage({
            id: "content-manager.containers.list.items",
            defaultMessage: "{number, plural, =0 {items} one {item} other {items}}"
          }, {
            number: content.length
          })
        ]
      }),
      (0, import_jsx_runtime2.jsx)(gd.Content, {
        children: content.map((item) => (0, import_jsx_runtime2.jsx)(gd.Item, {
          disabled: true,
          children: (0, import_jsx_runtime2.jsx)(A, {
            maxWidth: "50rem",
            ellipsis: true,
            children: (0, import_jsx_runtime2.jsx)(CellValue, {
              type: mainField.type,
              value: item[mainField.name]
            })
          })
        }, item.id))
      })
    ]
  });
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/TableCells/Media.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
var getFileExtension = (ext) => ext && ext[0] === "." ? ext.substring(1) : ext;
var MediaSingle = ({ url, mime, alternativeText, name, ext, formats }) => {
  var _a;
  const fileURL = prefixFileUrlWithBackendUrl(url);
  if (mime == null ? void 0 : mime.includes("image")) {
    const thumbnail = (_a = formats == null ? void 0 : formats.thumbnail) == null ? void 0 : _a.url;
    const mediaURL = prefixFileUrlWithBackendUrl(thumbnail) || fileURL;
    const mediaLabel = alternativeText || name || void 0;
    return (0, import_jsx_runtime3.jsx)(hd.Item, {
      src: mediaURL,
      alt: mediaLabel,
      fallback: mediaLabel,
      preview: true
    });
  }
  const fileExtension = getFileExtension(ext);
  const fileName = name && name.length > 100 ? `${name.substring(0, 100)}...` : name;
  return (0, import_jsx_runtime3.jsx)(Mt, {
    label: fileName,
    children: (0, import_jsx_runtime3.jsx)("span", {
      children: (0, import_jsx_runtime3.jsx)(FileWrapper, {
        children: fileExtension
      })
    })
  });
};
var FileWrapper = ({ children }) => {
  return (0, import_jsx_runtime3.jsx)(C, {
    tag: "span",
    position: "relative",
    borderRadius: "50%",
    width: "26px",
    height: "26px",
    borderColor: "neutral200",
    background: "neutral150",
    paddingLeft: "1px",
    justifyContent: "center",
    alignItems: "center",
    children: (0, import_jsx_runtime3.jsx)(FileTypography, {
      variant: "sigma",
      textColor: "neutral600",
      children
    })
  });
};
var FileTypography = gt(A)`
  font-size: 0.9rem;
  line-height: 0.9rem;
`;
var MediaMultiple = ({ content }) => {
  return (0, import_jsx_runtime3.jsx)(hd.Group, {
    children: content.map((file, index) => {
      const key = `${file.id}${index}`;
      if (index === 3) {
        const remainingFiles = `+${content.length - 3}`;
        return (0, import_jsx_runtime3.jsx)(FileWrapper, {
          children: remainingFiles
        }, key);
      }
      if (index > 3) {
        return null;
      }
      return (0, import_jsx_runtime3.jsx)(MediaSingle, {
        ...file
      }, key);
    })
  });
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/TableCells/Relations.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var React2 = __toESM(require_react(), 1);
var RelationSingle = ({ mainField, content }) => {
  const { formatMessage } = useIntl();
  const emptyLabel = formatMessage({
    id: "content-manager.containers.empty-label",
    defaultMessage: "Untitled"
  });
  return (0, import_jsx_runtime4.jsx)(A, {
    maxWidth: "50rem",
    textColor: "neutral800",
    ellipsis: true,
    children: getRelationLabel(content, mainField, emptyLabel)
  });
};
var RelationMultiple = ({ mainField, content, rowId, name }) => {
  var _a, _b;
  const { model } = useDoc();
  const { formatMessage } = useIntl();
  const emptyLabel = formatMessage({
    id: "content-manager.containers.empty-label",
    defaultMessage: "Untitled"
  });
  const { notifyStatus } = Md();
  const [isOpen, setIsOpen] = React2.useState(false);
  const [{ query }] = useQueryParams();
  const locale = (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale;
  const [targetField] = name.split(".");
  const { data, isLoading } = useGetRelationsQuery({
    model,
    id: rowId,
    targetField,
    params: {
      locale
    }
  }, {
    skip: !isOpen,
    refetchOnMountOrArgChange: true
  });
  const contentCount = Array.isArray(content) ? content.length : content.count;
  React2.useEffect(() => {
    if (data) {
      notifyStatus(formatMessage({
        id: getTranslation("ListViewTable.relation-loaded"),
        defaultMessage: "Relations have been loaded"
      }));
    }
  }, [
    data,
    formatMessage,
    notifyStatus
  ]);
  return (0, import_jsx_runtime4.jsxs)(gd.Root, {
    onOpenChange: (isOpen2) => setIsOpen(isOpen2),
    children: [
      (0, import_jsx_runtime4.jsx)(gd.Trigger, {
        onClick: (e) => e.stopPropagation(),
        children: (0, import_jsx_runtime4.jsx)(A, {
          style: {
            cursor: "pointer"
          },
          textColor: "neutral800",
          fontWeight: "regular",
          children: contentCount > 0 ? formatMessage({
            id: "content-manager.containers.list.items",
            defaultMessage: "{number} {number, plural, =0 {items} one {item} other {items}}"
          }, {
            number: contentCount
          }) : "-"
        })
      }),
      (0, import_jsx_runtime4.jsxs)(gd.Content, {
        children: [
          isLoading && (0, import_jsx_runtime4.jsx)(gd.Item, {
            disabled: true,
            children: (0, import_jsx_runtime4.jsx)(kt, {
              small: true,
              children: formatMessage({
                id: getTranslation("ListViewTable.relation-loading"),
                defaultMessage: "Relations are loading"
              })
            })
          }),
          (data == null ? void 0 : data.results) && (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, {
            children: [
              data.results.map((entry) => (0, import_jsx_runtime4.jsx)(gd.Item, {
                children: (0, import_jsx_runtime4.jsx)(A, {
                  maxWidth: "50rem",
                  ellipsis: true,
                  children: getRelationLabel(entry, mainField, emptyLabel)
                })
              }, entry.documentId)),
              (data == null ? void 0 : data.pagination) && (data == null ? void 0 : data.pagination.total) > 10 && (0, import_jsx_runtime4.jsx)(gd.Item, {
                "aria-disabled": true,
                "aria-label": formatMessage({
                  id: getTranslation("ListViewTable.relation-more"),
                  defaultMessage: "This relation contains more entities than displayed"
                }),
                children: (0, import_jsx_runtime4.jsx)(A, {
                  children: "…"
                })
              })
            ]
          })
        ]
      })
    ]
  });
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/TableCells/CellContent.mjs
var CellContent = ({ content, mainField, attribute, rowId, name, linkTo }) => {
  const isIdColumn = name === "id";
  if (!hasContent(content, mainField, attribute)) {
    return (0, import_jsx_runtime5.jsx)(A, {
      textColor: "neutral800",
      paddingLeft: attribute.type === "relation" || attribute.type === "component" ? "1.6rem" : 0,
      paddingRight: attribute.type === "relation" || attribute.type === "component" ? "1.6rem" : 0,
      children: "-"
    });
  }
  if (linkTo) {
    const link = (0, import_jsx_runtime5.jsx)(A, {
      tag: Link,
      to: linkTo,
      onClick: (e) => e.stopPropagation(),
      maxWidth: "30rem",
      ellipsis: true,
      textColor: "neutral800",
      children: (0, import_jsx_runtime5.jsx)(CellValue, {
        isIdColumn,
        type: attribute.type,
        value: content
      })
    });
    return attribute.type === "string" ? (0, import_jsx_runtime5.jsx)(Mt, {
      label: content,
      children: link
    }) : link;
  }
  switch (attribute.type) {
    case "media":
      if (!attribute.multiple) {
        return (0, import_jsx_runtime5.jsx)(MediaSingle, {
          ...content
        });
      }
      return (0, import_jsx_runtime5.jsx)(MediaMultiple, {
        content
      });
    case "relation": {
      if (isSingleRelation(attribute.relation)) {
        return (0, import_jsx_runtime5.jsx)(RelationSingle, {
          mainField,
          content
        });
      }
      return (0, import_jsx_runtime5.jsx)(RelationMultiple, {
        rowId,
        mainField,
        content,
        name
      });
    }
    case "component":
      if (attribute.repeatable) {
        return (0, import_jsx_runtime5.jsx)(RepeatableComponent, {
          mainField,
          content
        });
      }
      return (0, import_jsx_runtime5.jsx)(SingleComponent, {
        mainField,
        content
      });
    case "string":
      return (0, import_jsx_runtime5.jsx)(Mt, {
        label: content,
        children: (0, import_jsx_runtime5.jsx)(A, {
          maxWidth: "30rem",
          ellipsis: true,
          textColor: "neutral800",
          children: (0, import_jsx_runtime5.jsx)(CellValue, {
            isIdColumn,
            type: attribute.type,
            value: content
          })
        })
      });
    default:
      return (0, import_jsx_runtime5.jsx)(A, {
        maxWidth: "30rem",
        ellipsis: true,
        textColor: "neutral800",
        children: (0, import_jsx_runtime5.jsx)(CellValue, {
          isIdColumn,
          type: attribute.type,
          value: content
        })
      });
  }
};
var hasContent = (content, mainField, attribute) => {
  if (attribute.type === "component") {
    if (attribute.repeatable || !mainField) {
      return (content == null ? void 0 : content.length) > 0;
    }
    const value = content == null ? void 0 : content[mainField.name];
    if (mainField.name === "id" && ![
      void 0,
      null
    ].includes(value)) {
      return true;
    }
    return !(0, import_isEmpty.default)(value);
  }
  if (attribute.type === "relation") {
    if (isSingleRelation(attribute.relation)) {
      return !(0, import_isEmpty.default)(content);
    }
    if (Array.isArray(content)) {
      return content.length > 0;
    }
    return (content == null ? void 0 : content.count) > 0;
  }
  if ([
    "integer",
    "decimal",
    "float",
    "number"
  ].includes(attribute.type)) {
    return typeof content === "number";
  }
  if (attribute.type === "boolean") {
    return content !== null;
  }
  return !(0, import_isEmpty.default)(content);
};
var isSingleRelation = (type) => [
  "oneToOne",
  "manyToOne",
  "oneToOneMorph"
].includes(type);

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/components/ViewSettingsMenu.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_qs = __toESM(require_lib(), 1);
var ViewSettingsMenu = (props) => {
  const permissions = useTypedSelector((state) => {
    var _a;
    return ((_a = state.admin_app.permissions.contentManager) == null ? void 0 : _a.collectionTypesConfigurations) ?? [];
  });
  const [{ query }] = useQueryParams();
  const { formatMessage } = useIntl();
  const { allowedActions: { canConfigureView } } = useRBAC(permissions);
  return (0, import_jsx_runtime6.jsxs)(Xd.Root, {
    children: [
      (0, import_jsx_runtime6.jsx)(Xd.Trigger, {
        children: (0, import_jsx_runtime6.jsx)(be, {
          label: formatMessage({
            id: "components.ViewSettings.tooltip",
            defaultMessage: "View Settings"
          }),
          children: (0, import_jsx_runtime6.jsx)(C5, {})
        })
      }),
      (0, import_jsx_runtime6.jsx)(Xd.Content, {
        side: "bottom",
        align: "end",
        sideOffset: 4,
        children: (0, import_jsx_runtime6.jsxs)(C, {
          alignItems: "stretch",
          direction: "column",
          padding: 3,
          gap: 3,
          children: [
            canConfigureView ? (0, import_jsx_runtime6.jsx)(Hd, {
              size: "S",
              startIcon: (0, import_jsx_runtime6.jsx)(V3, {}),
              variant: "secondary",
              tag: NavLink,
              to: {
                pathname: "configurations/list",
                search: query.plugins ? (0, import_qs.stringify)({
                  plugins: query.plugins
                }, {
                  encode: false
                }) : ""
              },
              children: formatMessage({
                id: "app.links.configure-view",
                defaultMessage: "Configure the view"
              })
            }) : null,
            (0, import_jsx_runtime6.jsx)(FieldPicker, {
              ...props
            })
          ]
        })
      })
    ]
  });
};
var FieldPicker = ({ headers = [], resetHeaders, setHeaders }) => {
  const { trackUsage } = useTracking();
  const { formatMessage, locale } = useIntl();
  const { schema, model } = useDoc();
  const { list } = useDocumentLayout(model);
  const formatter = Qt(locale, {
    sensitivity: "base"
  });
  const attributes = (schema == null ? void 0 : schema.attributes) ?? {};
  const columns = Object.keys(attributes).filter((name) => checkIfAttributeIsDisplayable(attributes[name])).map((name) => {
    var _a;
    return {
      name,
      label: ((_a = list.metadatas[name]) == null ? void 0 : _a.label) ?? ""
    };
  }).sort((a, b) => formatter.compare(a.label, b.label));
  const handleChange = (name) => {
    trackUsage("didChangeDisplayedFields");
    const newHeaders = headers.includes(name) ? headers.filter((header) => header !== name) : [
      ...headers,
      name
    ];
    setHeaders(newHeaders);
  };
  const handleReset = () => {
    resetHeaders();
  };
  return (0, import_jsx_runtime6.jsxs)(C, {
    tag: "fieldset",
    direction: "column",
    alignItems: "stretch",
    gap: 1,
    borderWidth: 0,
    maxHeight: "240px",
    padding: 1,
    overflow: "auto",
    children: [
      (0, import_jsx_runtime6.jsxs)(C, {
        justifyContent: "space-between",
        gap: 2,
        children: [
          (0, import_jsx_runtime6.jsx)(A, {
            tag: "legend",
            variant: "pi",
            fontWeight: "bold",
            children: formatMessage({
              id: "containers.list.displayedFields",
              defaultMessage: "Displayed fields"
            })
          }),
          (0, import_jsx_runtime6.jsx)(bu, {
            onClick: handleReset,
            children: formatMessage({
              id: "app.components.Button.reset",
              defaultMessage: "Reset"
            })
          })
        ]
      }),
      (0, import_jsx_runtime6.jsx)(C, {
        direction: "column",
        alignItems: "stretch",
        children: columns.map((header) => {
          const isActive = headers.includes(header.name);
          return (0, import_jsx_runtime6.jsx)(C, {
            wrap: "wrap",
            gap: 2,
            background: isActive ? "primary100" : "transparent",
            hasRadius: true,
            padding: 2,
            marginBottom: 1,
            children: (0, import_jsx_runtime6.jsx)(Bt, {
              onCheckedChange: () => handleChange(header.name),
              checked: isActive,
              name: header.name,
              children: (0, import_jsx_runtime6.jsx)(A, {
                fontSize: 1,
                children: header.label
              })
            })
          }, header.name);
        })
      })
    ]
  });
};

// node_modules/@strapi/content-manager/dist/admin/pages/ListView/ListViewPage.mjs
var { INJECT_COLUMN_IN_TABLE } = HOOKS;
var LayoutsHeaderCustom = gt(Layouts.Header)`
  overflow-wrap: anywhere;
`;
var ListViewPage = () => {
  var _a, _b, _c, _d, _e;
  const { trackUsage } = useTracking();
  const navigate = useNavigate();
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const { copy } = useClipboard();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler(getTranslation);
  const isMobile = useIsMobile();
  const handleCopyDocumentId = React3.useCallback(async (e, documentId) => {
    e.stopPropagation();
    if (!documentId) return;
    const didCopy = await copy(documentId);
    if (didCopy) {
      toggleNotification({
        type: "success",
        message: formatMessage({
          id: "content-manager.actions.copy-documentId.success",
          defaultMessage: "Document ID copied to clipboard"
        })
      });
    }
  }, [
    copy,
    formatMessage,
    toggleNotification
  ]);
  const { collectionType, model, schema } = useDoc();
  const { list, listViewConversionContext, isLoading: isLoadingLayout } = useDocumentLayout(model);
  const persistentQueryConfigs = React3.useMemo(() => ({
    [`STRAPI_LIST_VIEW_SETTINGS:${model}`]: {
      paths: [
        "sort",
        "filters",
        "pageSize"
      ],
      scoped: true
    },
    STRAPI_LOCALE: {
      paths: [
        "plugins.i18n.locale"
      ],
      scoped: false
    }
  }), [
    model
  ]);
  const { isHydrated } = usePersistentPartialQueryParams(persistentQueryConfigs);
  const [displayedHeaderNames, setDisplayedHeaderNames] = useScopedPersistentState(`STRAPI_LIST_VIEW_DISPLAYED_HEADERS:${model}`, null);
  const mapDisplayedHeaders = React3.useCallback((headers) => headers.map((header) => header.name), []);
  const displayedHeaders = React3.useMemo(() => {
    if (!displayedHeaderNames || !list.metadatas || Object.keys(list.metadatas).length <= 0 || !(schema == null ? void 0 : schema.attributes) || !listViewConversionContext) return [];
    return convertListLayoutToFieldLayouts(displayedHeaderNames, schema.attributes, list.metadatas, {
      configurations: listViewConversionContext.componentConfigurations,
      schemas: listViewConversionContext.componentSchemas
    }, listViewConversionContext.contentTypeSchemas);
  }, [
    displayedHeaderNames,
    schema,
    list,
    listViewConversionContext
  ]);
  const handleSetHeaders = React3.useCallback((headers) => {
    setDisplayedHeaderNames(headers);
  }, [
    setDisplayedHeaderNames
  ]);
  const handleResetHeaders = React3.useCallback(() => {
    setDisplayedHeaderNames(mapDisplayedHeaders(list.layout));
  }, [
    list.layout,
    mapDisplayedHeaders,
    setDisplayedHeaderNames
  ]);
  React3.useEffect(() => {
    if (list.layout.length === 0) {
      return;
    }
    if (!displayedHeaderNames) {
      handleResetHeaders();
    }
  }, [
    displayedHeaderNames,
    handleResetHeaders,
    list.layout
  ]);
  React3.useEffect(() => {
    if (!(schema == null ? void 0 : schema.attributes)) return;
    if (!displayedHeaderNames || displayedHeaderNames.length === 0) return;
    if (schema.uid !== model) return;
    const allowedDisplayHeaders = displayedHeaderNames.filter((header) => Object.keys(schema == null ? void 0 : schema.attributes).includes(header));
    if (allowedDisplayHeaders.length !== displayedHeaderNames.length) {
      handleSetHeaders(allowedDisplayHeaders);
    }
  }, [
    displayedHeaderNames,
    handleSetHeaders,
    model,
    schema == null ? void 0 : schema.attributes,
    schema == null ? void 0 : schema.uid
  ]);
  const [{ query }, setQuery] = useQueryParams({
    page: "1",
    pageSize: list.settings.pageSize.toString(),
    sort: list.settings.defaultSortBy ? `${list.settings.defaultSortBy}:${list.settings.defaultSortOrder}` : ""
  });
  const params = React3.useMemo(() => buildValidParams(query), [
    query
  ]);
  const hasAppliedFilters = (((_b = (_a = query.filters) == null ? void 0 : _a.$and) == null ? void 0 : _b.length) ?? 0) > 0;
  const hasStatusFilter = Boolean((_d = (_c = query.filters) == null ? void 0 : _c.$and) == null ? void 0 : _d.some((filter) => {
    var _a2;
    return ((_a2 = filter.__status) == null ? void 0 : _a2.$eq) !== void 0 && filter.__status.$eq !== null;
  }));
  React3.useEffect(() => {
    if (hasStatusFilter && typeof query.sort === "string" && /(?:^|,)\s*status:/i.test(query.sort)) {
      const cleaned = query.sort.split(",").map((s) => s.trim()).filter((s) => !/^status:(ASC|DESC)$/i.test(s)).join(",");
      setQuery(withEncodedUserParams(query, {
        sort: cleaned || void 0
      }), "push", true);
    }
  }, [
    hasStatusFilter,
    query,
    setQuery
  ]);
  const { data, error, isLoading, isFetching } = useGetAllDocumentsQuery({
    model,
    params
  }, {
    skip: isLoadingLayout || !isHydrated
  });
  React3.useEffect(() => {
    if (error) {
      toggleNotification({
        type: "danger",
        message: formatAPIError(error)
      });
    }
  }, [
    error,
    formatAPIError,
    toggleNotification
  ]);
  const { results = [], pagination } = data ?? {};
  React3.useEffect(() => {
    if (pagination && pagination.pageCount > 0 && pagination.page > pagination.pageCount) {
      navigate({
        search: (0, import_qs2.stringify)({
          ...query,
          page: pagination.pageCount
        })
      }, {
        replace: true
      });
    }
  }, [
    pagination,
    formatMessage,
    query,
    navigate
  ]);
  const { canCreate } = useDocumentRBAC("ListViewPage", ({ canCreate: canCreate2 }) => ({
    canCreate: canCreate2
  }));
  const runHookWaterfall = useStrapiApp("ListViewPage", ({ runHookWaterfall: runHookWaterfall2 }) => runHookWaterfall2);
  const tableHeaders = React3.useMemo(() => {
    var _a2;
    const headers = runHookWaterfall(INJECT_COLUMN_IN_TABLE, {
      displayedHeaders,
      layout: list
    });
    const formattedHeaders = headers.displayedHeaders.map((header) => {
      var _a3;
      const translation = typeof header.label === "string" ? {
        id: `content-manager.content-types.${model}.${header.name}`,
        defaultMessage: header.label
      } : header.label;
      return {
        ...header,
        label: formatMessage(translation),
        name: `${header.name}${((_a3 = header.mainField) == null ? void 0 : _a3.name) ? `.${header.mainField.name}` : ""}`
      };
    });
    if ((_a2 = schema == null ? void 0 : schema.options) == null ? void 0 : _a2.draftAndPublish) {
      formattedHeaders.push({
        attribute: {
          type: "custom"
        },
        name: "status",
        label: formatMessage({
          id: getTranslation(`containers.list.table-headers.status`),
          defaultMessage: "status"
        }),
        searchable: false,
        sortable: !hasStatusFilter
      });
    }
    return formattedHeaders;
  }, [
    displayedHeaders,
    formatMessage,
    hasStatusFilter,
    list,
    runHookWaterfall,
    (_e = schema == null ? void 0 : schema.options) == null ? void 0 : _e.draftAndPublish,
    model
  ]);
  if (isLoadingLayout || !isHydrated || isLoading) {
    return (0, import_jsx_runtime7.jsx)(Page.Loading, {});
  }
  if (error) {
    return (0, import_jsx_runtime7.jsx)(Page.Error, {});
  }
  const contentTypeTitle = (schema == null ? void 0 : schema.info.displayName) ? formatMessage({
    id: schema.info.displayName,
    defaultMessage: schema.info.displayName
  }) : formatMessage({
    id: "content-manager.containers.untitled",
    defaultMessage: "Untitled"
  });
  const handleRowClick = (id) => () => {
    trackUsage("willEditEntryFromList");
    navigate({
      pathname: id.toString(),
      search: (0, import_qs2.stringify)({
        plugins: query.plugins
      })
    });
  };
  const NON_LINKABLE_TYPES = [
    "media",
    "relation",
    "component",
    "dynamiczone"
  ];
  const linkCandidates = tableHeaders.filter(({ name, attribute, cellFormatter }) => name !== "status" && typeof cellFormatter !== "function" && attribute && !NON_LINKABLE_TYPES.includes(attribute.type));
  const getRowLinkField = (row) => {
    var _a2;
    return (_a2 = linkCandidates.find((header) => {
      if (header.name === "documentId") {
        return Boolean(row.documentId);
      }
      const value = row[header.name.split(".")[0]];
      return hasContent(value, header.mainField, header.attribute);
    })) == null ? void 0 : _a2.name;
  };
  const isEmptyState = !isFetching && results.length === 0;
  const endActions = (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, {
    children: [
      isMobile && list.settings.filterable && schema && (0, import_jsx_runtime7.jsx)(listViewFilters.Trigger, {}),
      (0, import_jsx_runtime7.jsx)(InjectionZone, {
        area: "listView.actions"
      }),
      (0, import_jsx_runtime7.jsx)(ViewSettingsMenu, {
        setHeaders: handleSetHeaders,
        resetHeaders: handleResetHeaders,
        headers: displayedHeaderNames ?? []
      })
    ]
  });
  const searchInput = (0, import_jsx_runtime7.jsx)(import_jsx_runtime7.Fragment, {
    children: list.settings.searchable && (0, import_jsx_runtime7.jsx)(SearchInput, {
      label: formatMessage({
        id: "app.component.search.label",
        defaultMessage: "Search for {target}"
      }, {
        target: contentTypeTitle
      }),
      placeholder: formatMessage({
        id: "global.search",
        defaultMessage: "Search"
      }),
      trackedEvent: "didSearch"
    })
  });
  const startActions = (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, {
    children: [
      searchInput,
      !isMobile && list.settings.filterable && schema && (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, {
        children: [
          (0, import_jsx_runtime7.jsx)(listViewFilters.Trigger, {}),
          (0, import_jsx_runtime7.jsx)(listViewFilters.List, {})
        ]
      })
    ]
  });
  const actions = list.settings.filterable && schema ? (0, import_jsx_runtime7.jsxs)(listViewFilters.Root, {
    schema,
    layout: list,
    children: [
      (0, import_jsx_runtime7.jsx)(Layouts.Action, {
        endActions,
        startActions,
        bottomActions: isMobile && hasAppliedFilters ? (0, import_jsx_runtime7.jsx)(listViewFilters.List, {}) : null
      }),
      (0, import_jsx_runtime7.jsx)(listViewFilters.Popover, {})
    ]
  }) : (0, import_jsx_runtime7.jsx)(Layouts.Action, {
    endActions,
    startActions
  });
  return (0, import_jsx_runtime7.jsxs)(import_jsx_runtime7.Fragment, {
    children: [
      (0, import_jsx_runtime7.jsx)(tours.contentManager.Introduction, {
        children: (0, import_jsx_runtime7.jsx)(S, {})
      }),
      (0, import_jsx_runtime7.jsxs)(Page.Main, {
        children: [
          (0, import_jsx_runtime7.jsx)(Page.Title, {
            children: `${contentTypeTitle}`
          }),
          (0, import_jsx_runtime7.jsx)(LayoutsHeaderCustom, {
            primaryAction: canCreate ? (0, import_jsx_runtime7.jsx)(tours.contentManager.CreateNewEntry, {
              children: (0, import_jsx_runtime7.jsx)(CreateButton, {})
            }) : null,
            subtitle: formatMessage({
              id: getTranslation("pages.ListView.header-subtitle"),
              defaultMessage: "{number, plural, =0 {# entries} one {# entry} other {# entries}} found"
            }, {
              number: pagination == null ? void 0 : pagination.total
            }),
            title: contentTypeTitle,
            navigationAction: (0, import_jsx_runtime7.jsx)(BackButton, {})
          }),
          actions,
          (0, import_jsx_runtime7.jsx)(Layouts.Content, {
            children: isEmptyState ? (0, import_jsx_runtime7.jsx)(S, {
              background: "neutral0",
              shadow: "filterShadow",
              hasRadius: true,
              children: (0, import_jsx_runtime7.jsx)(Wd, {
                action: canCreate && (0, import_jsx_runtime7.jsx)(S, {
                  children: (0, import_jsx_runtime7.jsx)(CreateButton, {
                    variant: "secondary"
                  })
                }),
                content: formatMessage({
                  id: "app.components.EmptyStateLayout.content-document",
                  defaultMessage: "No content found"
                }),
                hasRadius: true,
                icon: (0, import_jsx_runtime7.jsx)(K, {
                  width: "16rem"
                })
              })
            }) : (0, import_jsx_runtime7.jsxs)(C, {
              gap: 4,
              direction: "column",
              alignItems: "stretch",
              children: [
                (0, import_jsx_runtime7.jsxs)(Table.Root, {
                  rows: results,
                  headers: tableHeaders,
                  isLoading: isFetching,
                  children: [
                    (0, import_jsx_runtime7.jsx)(TableActionsBar, {}),
                    (0, import_jsx_runtime7.jsxs)(Table.Content, {
                      children: [
                        (0, import_jsx_runtime7.jsxs)(Table.Head, {
                          children: [
                            (0, import_jsx_runtime7.jsx)(Table.HeaderCheckboxCell, {}),
                            tableHeaders.map((header) => (0, import_jsx_runtime7.jsx)(Table.HeaderCell, {
                              ...header
                            }, header.name))
                          ]
                        }),
                        (0, import_jsx_runtime7.jsx)(Table.Loading, {}),
                        (0, import_jsx_runtime7.jsx)(Table.Empty, {
                          action: canCreate ? (0, import_jsx_runtime7.jsx)(CreateButton, {
                            variant: "secondary"
                          }) : null
                        }),
                        (0, import_jsx_runtime7.jsx)(Table.Body, {
                          children: results.map((row) => {
                            const rowLinkField = getRowLinkField(row);
                            return (0, import_jsx_runtime7.jsxs)(Table.Row, {
                              cursor: "pointer",
                              onClick: handleRowClick(row.documentId),
                              children: [
                                (0, import_jsx_runtime7.jsx)(Table.CheckboxCell, {
                                  id: row.id
                                }),
                                tableHeaders.map(({ cellFormatter, ...header }) => {
                                  if (header.name === "status") {
                                    const { status } = row;
                                    return (0, import_jsx_runtime7.jsx)(Table.Cell, {
                                      children: (0, import_jsx_runtime7.jsx)(DocumentStatus, {
                                        status,
                                        maxWidth: "min-content"
                                      })
                                    }, header.name);
                                  }
                                  if ([
                                    "createdBy",
                                    "updatedBy"
                                  ].includes(header.name.split(".")[0])) {
                                    return (0, import_jsx_runtime7.jsx)(Table.Cell, {
                                      children: (0, import_jsx_runtime7.jsx)(A, {
                                        textColor: "neutral800",
                                        children: row[header.name.split(".")[0]] ? getDisplayName(row[header.name.split(".")[0]]) : "-"
                                      })
                                    }, header.name);
                                  }
                                  if (header.name === "documentId") {
                                    const isDocumentIdLink = header.name === rowLinkField && Boolean(row.documentId);
                                    return (0, import_jsx_runtime7.jsx)(Table.Cell, {
                                      children: (0, import_jsx_runtime7.jsxs)(C, {
                                        gap: 2,
                                        alignItems: "center",
                                        width: "100%",
                                        minWidth: 0,
                                        children: [
                                          isDocumentIdLink ? (0, import_jsx_runtime7.jsx)(A, {
                                            tag: Link,
                                            to: {
                                              pathname: row.documentId,
                                              search: (0, import_qs2.stringify)({
                                                plugins: query.plugins
                                              })
                                            },
                                            onClick: (e) => e.stopPropagation(),
                                            textColor: "neutral800",
                                            maxWidth: "30rem",
                                            ellipsis: true,
                                            children: row.documentId
                                          }) : (0, import_jsx_runtime7.jsx)(A, {
                                            textColor: "neutral800",
                                            maxWidth: "30rem",
                                            ellipsis: true,
                                            children: row.documentId || "-"
                                          }),
                                          row.documentId && (0, import_jsx_runtime7.jsx)(be, {
                                            variant: "ghost",
                                            size: "S",
                                            label: formatMessage({
                                              id: "content-manager.actions.copy-documentId.label",
                                              defaultMessage: "Copy"
                                            }),
                                            onClick: (e) => handleCopyDocumentId(e, row.documentId),
                                            children: (0, import_jsx_runtime7.jsx)(y5, {})
                                          })
                                        ]
                                      })
                                    }, header.name);
                                  }
                                  if (typeof cellFormatter === "function") {
                                    return (0, import_jsx_runtime7.jsx)(Table.Cell, {
                                      children: cellFormatter(row, header, {
                                        collectionType,
                                        model
                                      })
                                    }, header.name);
                                  }
                                  return (0, import_jsx_runtime7.jsx)(Table.Cell, {
                                    children: (0, import_jsx_runtime7.jsx)(CellContent, {
                                      content: row[header.name.split(".")[0]],
                                      rowId: row.documentId,
                                      ...header,
                                      linkTo: header.name === rowLinkField ? {
                                        pathname: row.documentId,
                                        search: (0, import_qs2.stringify)({
                                          plugins: query.plugins
                                        })
                                      } : void 0
                                    })
                                  }, header.name);
                                }),
                                (0, import_jsx_runtime7.jsx)(ActionsCell, {
                                  onClick: (e) => e.stopPropagation(),
                                  children: (0, import_jsx_runtime7.jsx)(TableActions, {
                                    document: row
                                  })
                                })
                              ]
                            }, row.id);
                          })
                        })
                      ]
                    })
                  ]
                }),
                (0, import_jsx_runtime7.jsxs)(Pagination.Root, {
                  ...pagination,
                  onPageSizeChange: () => trackUsage("willChangeNumberOfEntriesPerPage"),
                  children: [
                    (0, import_jsx_runtime7.jsx)(Pagination.PageSize, {}),
                    (0, import_jsx_runtime7.jsx)(Pagination.Links, {})
                  ]
                })
              ]
            })
          })
        ]
      })
    ]
  });
};
var ActionsCell = gt(Table.Cell)`
  display: flex;
  justify-content: flex-end;
`;
var TableActionsBar = () => {
  var _a, _b;
  const selectRow = useTable("TableActionsBar", (state) => state.selectRow);
  const [{ query }] = useQueryParams();
  const locale = (_b = (_a = query == null ? void 0 : query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale;
  const prevLocale = usePrev(locale);
  React3.useEffect(() => {
    if (prevLocale !== locale) {
      selectRow([]);
    }
  }, [
    selectRow,
    prevLocale,
    locale
  ]);
  return (0, import_jsx_runtime7.jsx)(Table.ActionBar, {
    children: (0, import_jsx_runtime7.jsx)(BulkActionsRenderer, {})
  });
};
var CreateButton = ({ variant }) => {
  const { formatMessage } = useIntl();
  const { trackUsage } = useTracking();
  const [{ query }] = useQueryParams();
  return (0, import_jsx_runtime7.jsx)(Ct, {
    variant,
    tag: Link,
    onClick: () => {
      trackUsage("willCreateEntry", {
        status: "draft"
      });
    },
    startIcon: (0, import_jsx_runtime7.jsx)(en, {}),
    style: {
      textDecoration: "none"
    },
    to: {
      pathname: "create",
      search: (0, import_qs2.stringify)({
        plugins: query.plugins
      })
    },
    minWidth: "max-content",
    fullWidth: true,
    children: formatMessage({
      id: getTranslation("HeaderLayout.button.label-add-entry"),
      defaultMessage: "Create new entry"
    })
  });
};
var ProtectedListViewPage = () => {
  const { slug = "" } = useParams();
  const { permissions = [], isLoading, error } = useRBAC(PERMISSIONS.map((action) => ({
    action,
    subject: slug
  })));
  if (isLoading) {
    return (0, import_jsx_runtime7.jsx)(Page.Loading, {});
  }
  if (error || !slug) {
    return (0, import_jsx_runtime7.jsx)(Page.Error, {});
  }
  return (0, import_jsx_runtime7.jsx)(Page.Protect, {
    permissions,
    children: ({ permissions: permissions2 }) => (0, import_jsx_runtime7.jsx)(DocumentRBAC, {
      permissions: permissions2,
      children: (0, import_jsx_runtime7.jsx)(ListViewPage, {}, slug)
    })
  });
};
export {
  ListViewPage,
  ProtectedListViewPage
};
//# sourceMappingURL=ListViewPage-GEDZOD6E.js.map
