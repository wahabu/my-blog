'use strict';

var icons = require('@strapi/icons');
var enums = require('../../enums.js');
var files = require('./files.js');

const DOC_ICON_MAP = {
    pdf: icons.FilePdf,
    csv: icons.FileCsv,
    xls: icons.FileXls,
    zip: icons.FileZip
};
const getAssetIcon = (mime, ext)=>{
    const fileExtension = files.getFileExtension(ext);
    if (mime?.includes(enums.ASSET_TYPES.Image)) {
        return icons.Image;
    }
    if (mime?.includes(enums.ASSET_TYPES.Video)) {
        return icons.Play;
    }
    if (mime?.includes(enums.ASSET_TYPES.Audio)) {
        return icons.Headphones;
    }
    return fileExtension ? DOC_ICON_MAP[fileExtension] || icons.File : icons.File;
};

exports.getAssetIcon = getAssetIcon;
//# sourceMappingURL=getAssetIcon.js.map
