import { AssetSource } from '../constants';
import type { RawFile } from '../../../shared/contracts/files';
export declare const rawFileToAsset: (rawFile: RawFile, assetSource: AssetSource) => Promise<{
    size: number;
    createdAt: string;
    name: string;
    source: AssetSource;
    type: import("../enums").DocType | import("../enums").AssetType;
    url: string;
    ext: string | undefined;
    mime: string;
    rawFile: RawFile;
    isLocal: boolean;
}>;
