import {
  reducer
} from "./chunk-UZRPIKEE.js";
import {
  DEFAULT_BULK_ACTIONS,
  DEFAULT_TABLE_ROW_ACTIONS
} from "./chunk-ZDJYMBBA.js";
import {
  ActionsPanel,
  DEFAULT_ACTIONS,
  DEFAULT_HEADER_ACTIONS,
  Editor,
  INJECTION_ZONES,
  Node,
  PLUGIN_ID,
  Path,
  ReactEditor,
  Transforms,
  baseHandleConvert,
  codeBlocks,
  linkBlocks,
  prefixFileUrlWithBackendUrl,
  pressEnterTwiceToExit,
  routes,
  useBlocksEditorContext,
  useFocused,
  useGetPreviewUrlQuery,
  useSelected
} from "./chunk-O3ADBBC4.js";
import {
  prefixPluginTranslations
} from "./chunk-BVJQKRXI.js";
import {
  useForm
} from "./chunk-2IWTCEYS.js";
import {
  useTracking
} from "./chunk-QXVVXECA.js";
import {
  useQueryParams,
  useStrapiApp
} from "./chunk-HHY6TNZ5.js";
import {
  require_lib
} from "./chunk-NZZAX2AB.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import {
  require_prism
} from "./chunk-VQOOZHFO.js";
import {
  Link,
  useLocation,
  useNavigate
} from "./chunk-QBM2MGBP.js";
import {
  combineReducers
} from "./chunk-QQEUXDAQ.js";
import {
  A,
  C,
  Ct,
  Mt,
  S
} from "./chunk-7ZODKSNG.js";
import {
  N3,
  P3,
  W5,
  Y2,
  d5,
  e3,
  h3,
  i3,
  ln,
  nn,
  o3,
  r3,
  rn,
  v3,
  w3,
  w5
} from "./chunk-D4B4CG5U.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  ft,
  gt
} from "./chunk-4N2AQQ2K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/content-manager/dist/admin/index.mjs
var import_prismjs = __toESM(require_prism(), 1);

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/components/FormInputs/BlocksInput/Blocks/Heading.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
var H1 = gt(A).attrs({
  tag: "h1"
})`
  font-size: 2.8rem;
  ${({ theme }) => theme.breakpoints.medium} {
    font-size: 3.6rem;
  }
  font-weight: 600;
  line-height: ${({ theme }) => theme.lineHeights[1]};
`;
var H2 = gt(A).attrs({
  tag: "h2"
})`
  font-size: 2.2rem;
  ${({ theme }) => theme.breakpoints.medium} {
    font-size: 3rem;
  }
  font-weight: 600;
  line-height: ${({ theme }) => theme.lineHeights[1]};
`;
var H3 = gt(A).attrs({
  tag: "h3"
})`
  font-size: 2rem;
  ${({ theme }) => theme.breakpoints.medium} {
    font-size: 2.4rem;
  }
  font-weight: 600;
  line-height: ${({ theme }) => theme.lineHeights[1]};
`;
var H4 = gt(A).attrs({
  tag: "h4"
})`
  font-size: 1.8rem;
  ${({ theme }) => theme.breakpoints.medium} {
    font-size: 2.4rem;
  }
  font-weight: 600;
  line-height: ${({ theme }) => theme.lineHeights[1]};
`;
var H5 = gt(A).attrs({
  tag: "h5"
})`
  font-size: 1.6rem;
  ${({ theme }) => theme.breakpoints.medium} {
    font-size: 2rem;
  }
  font-weight: 600;
  line-height: ${({ theme }) => theme.lineHeights[1]};
`;
var H6 = gt(A).attrs({
  tag: "h6"
})`
  font-size: 1.4rem;
  ${({ theme }) => theme.breakpoints.medium} {
    font-size: 1.6rem;
  }
  font-weight: 600;
  line-height: ${({ theme }) => theme.lineHeights[1]};
`;
var handleConvertToHeading = (editor, level) => {
  baseHandleConvert(editor, {
    type: "heading",
    level
  });
};
var headingBlocks = {
  "heading-one": {
    renderElement: (props) => (0, import_jsx_runtime.jsx)(H1, {
      ...props.attributes,
      children: props.children
    }),
    icon: e3,
    label: {
      id: "components.Blocks.blocks.heading1",
      defaultMessage: "Heading 1"
    },
    handleConvert: (editor) => handleConvertToHeading(editor, 1),
    matchNode: (node) => node.type === "heading" && node.level === 1,
    isInBlocksSelector: true,
    snippets: [
      "#"
    ],
    dragHandleTopMargin: "14px"
  },
  "heading-two": {
    renderElement: (props) => (0, import_jsx_runtime.jsx)(H2, {
      ...props.attributes,
      children: props.children
    }),
    icon: v3,
    label: {
      id: "components.Blocks.blocks.heading2",
      defaultMessage: "Heading 2"
    },
    handleConvert: (editor) => handleConvertToHeading(editor, 2),
    matchNode: (node) => node.type === "heading" && node.level === 2,
    isInBlocksSelector: true,
    snippets: [
      "##"
    ],
    dragHandleTopMargin: "10px"
  },
  "heading-three": {
    renderElement: (props) => (0, import_jsx_runtime.jsx)(H3, {
      ...props.attributes,
      children: props.children
    }),
    icon: i3,
    label: {
      id: "components.Blocks.blocks.heading3",
      defaultMessage: "Heading 3"
    },
    handleConvert: (editor) => handleConvertToHeading(editor, 3),
    matchNode: (node) => node.type === "heading" && node.level === 3,
    isInBlocksSelector: true,
    snippets: [
      "###"
    ],
    dragHandleTopMargin: "7px"
  },
  "heading-four": {
    renderElement: (props) => (0, import_jsx_runtime.jsx)(H4, {
      ...props.attributes,
      children: props.children
    }),
    icon: w3,
    label: {
      id: "components.Blocks.blocks.heading4",
      defaultMessage: "Heading 4"
    },
    handleConvert: (editor) => handleConvertToHeading(editor, 4),
    matchNode: (node) => node.type === "heading" && node.level === 4,
    isInBlocksSelector: true,
    snippets: [
      "####"
    ],
    dragHandleTopMargin: "4px"
  },
  "heading-five": {
    renderElement: (props) => (0, import_jsx_runtime.jsx)(H5, {
      ...props.attributes,
      children: props.children
    }),
    icon: h3,
    label: {
      id: "components.Blocks.blocks.heading5",
      defaultMessage: "Heading 5"
    },
    handleConvert: (editor) => handleConvertToHeading(editor, 5),
    matchNode: (node) => node.type === "heading" && node.level === 5,
    isInBlocksSelector: true,
    snippets: [
      "#####"
    ]
  },
  "heading-six": {
    renderElement: (props) => (0, import_jsx_runtime.jsx)(H6, {
      ...props.attributes,
      children: props.children
    }),
    icon: o3,
    label: {
      id: "components.Blocks.blocks.heading6",
      defaultMessage: "Heading 6"
    },
    handleConvert: (editor) => handleConvertToHeading(editor, 6),
    matchNode: (node) => node.type === "heading" && node.level === 6,
    isInBlocksSelector: true,
    snippets: [
      "######"
    ],
    dragHandleTopMargin: "-2px"
  }
};

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/components/FormInputs/BlocksInput/Blocks/Image.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var ImageWrapper = gt(C)`
  transition-property: box-shadow;
  transition-duration: 0.2s;
  ${(props) => props.$isFocused && ft`
      box-shadow: ${props.theme.colors.primary600} 0px 0px 0px 3px;
    `}

  & > img {
    height: auto;
    // The max-height is decided with the design team, the 56px is the height of the toolbar
    max-height: calc(512px - 56px);
    max-width: 100%;
    object-fit: contain;
  }
`;
var IMAGE_SCHEMA_FIELDS = [
  "name",
  "alternativeText",
  "url",
  "caption",
  "width",
  "height",
  "formats",
  "hash",
  "ext",
  "mime",
  "size",
  "previewUrl",
  "provider",
  "provider_metadata",
  "createdAt",
  "updatedAt"
];
var pick = (object, keys) => {
  const entries = keys.map((key) => [
    key,
    object[key]
  ]);
  return Object.fromEntries(entries);
};
var isImage = (element) => {
  return element.type === "image";
};
var Image = ({ attributes, children, element }) => {
  const editorIsFocused = useFocused();
  const imageIsSelected = useSelected();
  if (!isImage(element)) {
    return null;
  }
  const { url, alternativeText, width, height } = element.image;
  return (0, import_jsx_runtime2.jsxs)(S, {
    ...attributes,
    children: [
      children,
      (0, import_jsx_runtime2.jsx)(ImageWrapper, {
        background: "neutral100",
        contentEditable: false,
        justifyContent: "center",
        $isFocused: editorIsFocused && imageIsSelected,
        hasRadius: true,
        children: (0, import_jsx_runtime2.jsx)("img", {
          src: url,
          alt: alternativeText,
          width,
          height
        })
      })
    ]
  });
};
var ImageDialog = () => {
  const [isOpen, setIsOpen] = React.useState(true);
  const { editor } = useBlocksEditorContext("ImageDialog");
  const components = useStrapiApp("ImageDialog", (state) => state.components);
  if (!components || !isOpen) return null;
  const MediaLibraryDialog = components["media-library"];
  const insertImages = (images) => {
    Transforms.unwrapNodes(editor, {
      match: (node) => !Editor.isEditor(node) && node.type === "list",
      split: true
    });
    const nodeEntryBeingReplaced = Editor.above(editor, {
      match(node) {
        if (Editor.isEditor(node)) return false;
        const isInlineNode = [
          "text",
          "link"
        ].includes(node.type);
        return !isInlineNode;
      }
    });
    if (!nodeEntryBeingReplaced) return;
    const [, pathToInsert] = nodeEntryBeingReplaced;
    Transforms.removeNodes(editor);
    const nodesToInsert = images.map((image) => {
      const imageNode = {
        type: "image",
        image,
        children: [
          {
            type: "text",
            text: ""
          }
        ]
      };
      return imageNode;
    });
    Transforms.insertNodes(editor, nodesToInsert, {
      at: pathToInsert
    });
    Transforms.select(editor, pathToInsert);
  };
  const handleSelectAssets = (images) => {
    const formattedImages = images.map((image) => {
      const expectedImage = pick(image, IMAGE_SCHEMA_FIELDS);
      const nodeImage = {
        ...expectedImage,
        alternativeText: expectedImage.alternativeText || expectedImage.name,
        url: prefixFileUrlWithBackendUrl(image.url)
      };
      return nodeImage;
    });
    insertImages(formattedImages);
    setIsOpen(false);
  };
  return (0, import_jsx_runtime2.jsx)(MediaLibraryDialog, {
    allowedTypes: [
      "images"
    ],
    onClose: () => setIsOpen(false),
    onSelectAssets: handleSelectAssets
  });
};
var withImages = (editor) => {
  const { isVoid } = editor;
  editor.isVoid = (element) => {
    return element.type === "image" ? true : isVoid(element);
  };
  return editor;
};
var imageBlocks = {
  image: {
    renderElement: (props) => (0, import_jsx_runtime2.jsx)(Image, {
      ...props
    }),
    icon: r3,
    label: {
      id: "components.Blocks.blocks.image",
      defaultMessage: "Image"
    },
    matchNode: (node) => node.type === "image",
    isInBlocksSelector: true,
    handleBackspaceKey(editor) {
      if (editor.children.length === 1) {
        Transforms.setNodes(editor, {
          type: "paragraph",
          // @ts-expect-error we're only setting image as null so that Slate deletes it
          image: null,
          children: [
            {
              type: "text",
              text: ""
            }
          ]
        });
      } else {
        Transforms.removeNodes(editor);
      }
    },
    handleEnterKey(editor) {
      Transforms.insertNodes(editor, {
        type: "paragraph",
        children: [
          {
            type: "text",
            text: ""
          }
        ]
      });
    },
    handleConvert: () => {
      return () => (0, import_jsx_runtime2.jsx)(ImageDialog, {});
    },
    snippets: [
      "!["
    ],
    plugin: withImages
  }
};

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/components/FormInputs/BlocksInput/Blocks/List.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var listStyle = ft`
  display: flex;
  flex-direction: column;
  gap: ${({ theme }) => theme.spaces[2]};
  margin-inline-start: ${({ theme }) => theme.spaces[0]};
  margin-inline-end: ${({ theme }) => theme.spaces[0]};
  padding-inline-start: ${({ theme }) => theme.spaces[2]};

  ol,
  ul {
    margin-block-start: ${({ theme }) => theme.spaces[0]};
    margin-block-end: ${({ theme }) => theme.spaces[0]};
  }

  li {
    margin-inline-start: ${({ theme }) => theme.spaces[3]};
  }
`;
var Orderedlist = gt.ol`
  list-style-type: ${(props) => props.$listStyleType};
  ${listStyle}
`;
var Unorderedlist = gt.ul`
  list-style-type: ${(props) => props.$listStyleType};
  ${listStyle}
`;
var orderedStyles = [
  "decimal",
  "lower-alpha",
  "upper-roman"
];
var unorderedStyles = [
  "disc",
  "circle",
  "square"
];
var isListNode = (element) => {
  return element.type === "list";
};
var List = ({ attributes, children, element }) => {
  if (!isListNode(element)) {
    return null;
  }
  const listStyles = element.format === "ordered" ? orderedStyles : unorderedStyles;
  const nextIndex = (element.indentLevel || 0) % listStyles.length;
  const listStyleType = listStyles[nextIndex];
  if (element.format === "ordered") {
    return (0, import_jsx_runtime3.jsx)(Orderedlist, {
      $listStyleType: listStyleType,
      ...attributes,
      children
    });
  }
  return (0, import_jsx_runtime3.jsx)(Unorderedlist, {
    $listStyleType: listStyleType,
    ...attributes,
    children
  });
};
var replaceListWithEmptyBlock = (editor, currentListPath) => {
  Transforms.removeNodes(editor, {
    at: currentListPath
  });
  if (currentListPath[0] === 0) {
    Transforms.insertNodes(editor, {
      type: "paragraph",
      children: [
        {
          type: "text",
          text: ""
        }
      ]
    }, {
      at: currentListPath
    });
    Transforms.select(editor, currentListPath);
  }
};
var isText = (node) => {
  return Node.isNode(node) && !Editor.isEditor(node) && node.type === "text";
};
var handleBackspaceKeyOnList = (editor, event) => {
  if (!editor.selection) return;
  const [currentListItem, currentListItemPath] = Editor.parent(editor, editor.selection.anchor);
  const [currentList, currentListPath] = Editor.parent(editor, currentListItemPath);
  const isListEmpty = currentList.children.length === 1 && isText(currentListItem.children[0]) && currentListItem.children[0].text === "";
  const isListItemEmpty = currentListItem.children.length === 1 && isText(currentListItem.children[0]) && currentListItem.children[0].text === "";
  const isFocusAtTheBeginningOfAChild = editor.selection.focus.offset === 0 && editor.selection.focus.path.at(-2) === 0;
  if (isListEmpty) {
    const parentListEntry = Editor.above(editor, {
      at: currentListPath,
      match: (node) => !Editor.isEditor(node) && node.type === "list"
    });
    if (!parentListEntry) {
      event.preventDefault();
      replaceListWithEmptyBlock(editor, currentListPath);
    }
  } else if (isFocusAtTheBeginningOfAChild) {
    Transforms.liftNodes(editor, {
      match: (node) => !Editor.isEditor(node) && node.type === "list-item"
    });
    Transforms.setNodes(editor, {
      type: "paragraph"
    });
  } else if (isListItemEmpty) {
    const previousEntry = Editor.previous(editor, {
      at: currentListItemPath
    });
    const nextEntry = Editor.next(editor, {
      at: currentListItemPath
    });
    if (previousEntry && nextEntry) {
      event.preventDefault();
      Transforms.removeNodes(editor, {
        at: currentListItemPath
      });
      const [previousList] = previousEntry;
      const [nextList] = nextEntry;
      if (!Editor.isEditor(previousList) && !isText(previousList) && isListNode(previousList) && !Editor.isEditor(nextList) && !isText(nextList) && isListNode(nextList)) {
        if (previousList.type === "list" && nextList.type === "list" && previousList.format === nextList.format && previousList.indentLevel === nextList.indentLevel) {
          Transforms.mergeNodes(editor, {
            at: currentListItemPath
          });
        }
      }
    }
  }
};
var handleEnterKeyOnList = (editor) => {
  const currentListItemEntry = Editor.above(editor, {
    match: (node) => !Editor.isEditor(node) && node.type === "list-item"
  });
  if (!currentListItemEntry || !editor.selection) {
    return;
  }
  const [currentListItem, currentListItemPath] = currentListItemEntry;
  const [currentList, currentListPath] = Editor.parent(editor, currentListItemPath);
  const isListEmpty = currentList.children.length === 1 && isText(currentListItem.children[0]) && currentListItem.children[0].text === "";
  const isListItemEmpty = currentListItem.children.length === 1 && isText(currentListItem.children[0]) && currentListItem.children[0].text === "";
  const isFocusAtTheBeginningOfAChild = editor.selection.focus.offset === 0 && editor.selection.focus.path.at(-1) === 0;
  if (isListEmpty) {
    replaceListWithEmptyBlock(editor, currentListPath);
  } else if (isFocusAtTheBeginningOfAChild && !isListItemEmpty) {
    const currentNode = Editor.above(editor, {
      at: editor.selection.anchor
    });
    Transforms.insertNodes(editor, {
      type: "list-item",
      children: [
        {
          type: "text",
          text: ""
        }
      ]
    });
    if (currentNode) {
      const path = currentNode[1];
      const updatedPath = [
        ...path.slice(0, -1),
        path[path.length - 1] + 1
      ];
      Transforms.select(editor, {
        anchor: {
          path: updatedPath.concat(0),
          offset: 0
        },
        focus: {
          path: updatedPath.concat(0),
          offset: 0
        }
      });
    }
  } else if (isListItemEmpty) {
    if (!Editor.isEditor(currentList) && isListNode(currentList) && (currentList == null ? void 0 : currentList.indentLevel) && currentList.indentLevel > 0) {
      const previousIndentLevel = currentList.indentLevel - 1;
      const parentListNodeEntry = Editor.above(editor, {
        match: (node) => !Editor.isEditor(node) && node.type === "list" && (node.indentLevel || 0) === previousIndentLevel
      });
      if (parentListNodeEntry) {
        const modifiedPath = currentListItemPath.slice(0, -1);
        if (modifiedPath.length > 0) {
          modifiedPath[modifiedPath.length - 1] += 1;
        }
        Transforms.moveNodes(editor, {
          at: currentListItemPath,
          to: modifiedPath
        });
        return;
      }
    }
    Transforms.removeNodes(editor, {
      at: currentListItemPath
    });
    const createdParagraphPath = Path.next(currentListPath);
    Transforms.insertNodes(editor, {
      type: "paragraph",
      children: [
        {
          type: "text",
          text: ""
        }
      ]
    }, {
      at: createdParagraphPath
    });
    Transforms.select(editor, createdParagraphPath);
  } else {
    const isNodeEnd = Editor.isEnd(editor, editor.selection.anchor, currentListItemPath);
    if (isNodeEnd) {
      Transforms.insertNodes(editor, {
        type: "list-item",
        children: [
          {
            type: "text",
            text: ""
          }
        ]
      });
    } else {
      Transforms.splitNodes(editor);
    }
  }
};
var handleConvertToList = (editor, format) => {
  const convertedPath = baseHandleConvert(editor, {
    type: "list-item"
  });
  if (!convertedPath) return;
  Transforms.wrapNodes(editor, {
    type: "list",
    format,
    children: []
  }, {
    at: convertedPath
  });
};
var handleTabOnList = (editor) => {
  const currentListItemEntry = Editor.above(editor, {
    match: (node) => !Editor.isEditor(node) && node.type === "list-item"
  });
  if (!currentListItemEntry || !editor.selection) {
    return;
  }
  const [currentListItem, currentListItemPath] = currentListItemEntry;
  const [currentList] = Editor.parent(editor, currentListItemPath);
  if (currentListItem === currentList.children[0]) return;
  const currentListItemIndex = currentList.children.findIndex((item) => item === currentListItem);
  const previousNode = currentList.children[currentListItemIndex - 1];
  if (previousNode.type === "list") {
    const nodePath = ReactEditor.findPath(editor, previousNode);
    const insertAtPath = previousNode.children.length;
    Transforms.moveNodes(editor, {
      at: currentListItemPath,
      to: nodePath.concat(insertAtPath)
    });
    return;
  }
  if (!Editor.isEditor(currentList) && isListNode(currentList)) {
    Transforms.wrapNodes(editor, {
      type: "list",
      format: currentList.format,
      indentLevel: (currentList.indentLevel || 0) + 1,
      children: []
    });
  }
};
var hasChildren = (node) => {
  return typeof node === "object" && node !== null && "children" in node;
};
var isNestedListNode = (node) => {
  return hasChildren(node) && "type" in node && node.type === "list";
};
var handleShiftTabOnList = (editor) => {
  const currentListItemEntry = Editor.above(editor, {
    match: (node) => !Editor.isEditor(node) && "type" in node && node.type === "list-item"
  });
  if (!currentListItemEntry || !editor.selection) {
    return;
  }
  const [, currentListItemPath] = currentListItemEntry;
  const [currentList] = Editor.parent(editor, currentListItemPath);
  if (!Editor.isEditor(currentList) && isNestedListNode(currentList)) {
    const parentListPath = Path.parent(currentListItemPath);
    const [parentList] = Editor.parent(editor, parentListPath);
    if (Editor.isEditor(parentList)) {
      Transforms.liftNodes(editor, {
        at: currentListItemPath,
        match: (node) => !Editor.isEditor(node) && "type" in node && node.type === "list-item"
      });
      Transforms.setNodes(editor, {
        type: "paragraph"
      });
      return;
    }
    if (isNestedListNode(parentList)) {
      Transforms.liftNodes(editor, {
        at: currentListItemPath,
        match: (node) => !Editor.isEditor(node) && "type" in node && node.type === "list-item"
      });
      try {
        const [remainingList, remainingListPath] = Editor.node(editor, parentListPath);
        if (isNestedListNode(remainingList) && remainingList.children.length === 0) {
          Transforms.removeNodes(editor, {
            at: remainingListPath
          });
        }
      } catch {
      }
    }
  }
};
var baseListBlock = {
  renderElement: (props) => (0, import_jsx_runtime3.jsx)(List, {
    ...props
  }),
  isInBlocksSelector: true,
  handleEnterKey: handleEnterKeyOnList,
  handleBackspaceKey: handleBackspaceKeyOnList,
  handleTab: handleTabOnList,
  isDraggable: (element) => {
    if (!isListNode(element)) {
      throw Error();
    }
    const indentLevel = element.indentLevel ?? 0;
    return indentLevel === 0;
  }
};
var listBlocks = {
  "list-ordered": {
    ...baseListBlock,
    label: {
      id: "components.Blocks.blocks.orderedList",
      defaultMessage: "Numbered list"
    },
    icon: N3,
    matchNode: (node) => node.type === "list" && node.format === "ordered",
    handleConvert: (editor) => handleConvertToList(editor, "ordered"),
    handleShiftTab: handleShiftTabOnList,
    snippets: [
      "1."
    ]
  },
  "list-unordered": {
    ...baseListBlock,
    label: {
      id: "components.Blocks.blocks.unorderedList",
      defaultMessage: "Bulleted list"
    },
    icon: Y2,
    matchNode: (node) => node.type === "list" && node.format === "unordered",
    handleConvert: (editor) => handleConvertToList(editor, "unordered"),
    handleShiftTab: handleShiftTabOnList,
    snippets: [
      "-",
      "*",
      "+"
    ]
  },
  "list-item": {
    renderElement: (props) => (0, import_jsx_runtime3.jsx)(A, {
      tag: "li",
      ...props.attributes,
      children: props.children
    }),
    // No handleConvert, list items are created when converting to the parent list
    matchNode: (node) => node.type === "list-item",
    isInBlocksSelector: false,
    dragHandleTopMargin: "-2px",
    isDraggable: () => false
  }
};

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/components/FormInputs/BlocksInput/Blocks/Paragraph.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_react3 = __toESM(require_react(), 1);
var paragraphBlocks = {
  paragraph: {
    renderElement: (props) => (0, import_jsx_runtime4.jsx)(A, {
      tag: "p",
      variant: "omega",
      ...props.attributes,
      children: props.children
    }),
    icon: P3,
    label: {
      id: "components.Blocks.blocks.text",
      defaultMessage: "Text"
    },
    matchNode: (node) => node.type === "paragraph",
    isInBlocksSelector: true,
    dragHandleTopMargin: "-2px",
    handleConvert(editor) {
      baseHandleConvert(editor, {
        type: "paragraph"
      });
    },
    handleEnterKey(editor) {
      if (!editor.selection) {
        return;
      }
      const anchorPathInitialPosition = editor.selection.anchor.path;
      Transforms.splitNodes(editor, {
        // Makes sure we always create a new node,
        // even if there's nothing to the right of the cursor in the node.
        always: true
      });
      const parentBlockEntry = Editor.above(editor, {
        match: (node) => !Editor.isEditor(node) && node.type !== "text"
      });
      if (!parentBlockEntry) {
        return;
      }
      const [, parentBlockPath] = parentBlockEntry;
      const isNodeEnd = Editor.isEnd(editor, editor.selection.anchor, parentBlockPath);
      const [fragmentedNode] = Editor.parent(editor, editor.selection.anchor.path);
      Transforms.removeNodes(editor);
      const hasNextNode = editor.children.length - anchorPathInitialPosition[0] > 1;
      Transforms.insertNodes(editor, {
        type: "paragraph",
        // Don't carry over the modifiers from the previous node if there was no text after the cursor
        children: isNodeEnd ? [
          {
            type: "text",
            text: ""
          }
        ] : fragmentedNode.children
      }, {
        at: hasNextNode ? [
          anchorPathInitialPosition[0] + 1
        ] : [
          editor.children.length
        ]
      });
      Transforms.select(editor, editor.start([
        anchorPathInitialPosition[0] + 1
      ]));
    }
  }
};

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/components/FormInputs/BlocksInput/Blocks/Quote.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var Blockquote = gt.blockquote.attrs({
  role: "blockquote"
})`
  font-weight: ${({ theme }) => theme.fontWeights.regular};
  border-left: ${({ theme }) => `${theme.spaces[1]} solid ${theme.colors.neutral200}`};
  padding: ${({ theme }) => theme.spaces[2]} ${({ theme }) => theme.spaces[4]};
  color: ${({ theme }) => theme.colors.neutral600};
`;
var quoteBlocks = {
  quote: {
    renderElement: (props) => (
      // The div is needed to make sure the padding bottom from BlocksContent is applied properly
      // when the quote is the last block in the editor
      (0, import_jsx_runtime5.jsx)("div", {
        children: (0, import_jsx_runtime5.jsx)(Blockquote, {
          ...props.attributes,
          children: props.children
        })
      })
    ),
    icon: rn,
    label: {
      id: "components.Blocks.blocks.quote",
      defaultMessage: "Quote"
    },
    matchNode: (node) => node.type === "quote",
    isInBlocksSelector: true,
    handleConvert(editor) {
      baseHandleConvert(editor, {
        type: "quote"
      });
    },
    handleEnterKey(editor) {
      pressEnterTwiceToExit(editor);
    },
    snippets: [
      ">"
    ]
  }
};

// node_modules/@strapi/content-manager/dist/admin/pages/EditView/components/FormInputs/BlocksInput/DefaultBlocksStore.mjs
var defaultBlocksStore = {
  ...paragraphBlocks,
  ...headingBlocks,
  ...listBlocks,
  ...linkBlocks,
  ...imageBlocks,
  ...quoteBlocks,
  ...codeBlocks
};

// node_modules/@strapi/content-manager/dist/admin/content-manager.mjs
var ContentManagerPlugin = class {
  addRichTextBlocks(blocks) {
    if (typeof blocks === "function") {
      const result = blocks(this.richTextBlocksStore);
      if (typeof result !== "object" || result === null) {
        throw new Error(`Expected the \`blocks\` passed to \`addRichTextBlocks\` to be an object or a function, but received ${getPrintableType(result)}`);
      }
      this.richTextBlocksStore = result;
    } else if (typeof blocks === "object") {
      this.richTextBlocksStore = {
        ...this.richTextBlocksStore,
        ...blocks
      };
    } else {
      throw new Error(`Expected the \`blocks\` passed to \`addRichTextBlocks\` to be an object or a function, but received ${getPrintableType(blocks)}`);
    }
  }
  addEditViewSidePanel(panels) {
    if (Array.isArray(panels)) {
      validateDescriptionItems(panels, "addEditViewSidePanel", "panels");
      this.editViewSidePanels = [
        ...this.editViewSidePanels,
        ...panels
      ];
    } else if (typeof panels === "function") {
      const result = panels(this.editViewSidePanels);
      validateDescriptionItems(result, "addEditViewSidePanel", "panels");
      this.editViewSidePanels = result;
    } else {
      throw new Error(`Expected the \`panels\` passed to \`addEditViewSidePanel\` to be an array or a function, but received ${getPrintableType(panels)}`);
    }
  }
  addDocumentAction(actions) {
    if (Array.isArray(actions)) {
      validateDescriptionItems(actions, "addDocumentAction", "actions");
      this.documentActions = [
        ...this.documentActions,
        ...actions
      ];
    } else if (typeof actions === "function") {
      const result = actions(this.documentActions);
      validateDescriptionItems(result, "addDocumentAction", "actions");
      this.documentActions = result;
    } else {
      throw new Error(`Expected the \`actions\` passed to \`addDocumentAction\` to be an array or a function, but received ${getPrintableType(actions)}`);
    }
  }
  addDocumentHeaderAction(actions) {
    if (Array.isArray(actions)) {
      validateDescriptionItems(actions, "addDocumentHeaderAction", "actions");
      this.headerActions = [
        ...this.headerActions,
        ...actions
      ];
    } else if (typeof actions === "function") {
      const result = actions(this.headerActions);
      validateDescriptionItems(result, "addDocumentHeaderAction", "actions");
      this.headerActions = result;
    } else {
      throw new Error(`Expected the \`actions\` passed to \`addDocumentHeaderAction\` to be an array or a function, but received ${getPrintableType(actions)}`);
    }
  }
  addBulkAction(actions) {
    if (Array.isArray(actions)) {
      validateDescriptionItems(actions, "addBulkAction", "actions");
      this.bulkActions = [
        ...this.bulkActions,
        ...actions
      ];
    } else if (typeof actions === "function") {
      const result = actions(this.bulkActions);
      validateDescriptionItems(result, "addBulkAction", "actions");
      this.bulkActions = result;
    } else {
      throw new Error(`Expected the \`actions\` passed to \`addBulkAction\` to be an array or a function, but received ${getPrintableType(actions)}`);
    }
  }
  get config() {
    return {
      id: PLUGIN_ID,
      name: "Content Manager",
      injectionZones: INJECTION_ZONES,
      apis: {
        addBulkAction: this.addBulkAction.bind(this),
        addDocumentAction: this.addDocumentAction.bind(this),
        addDocumentHeaderAction: this.addDocumentHeaderAction.bind(this),
        addEditViewSidePanel: this.addEditViewSidePanel.bind(this),
        addRichTextBlocks: this.addRichTextBlocks.bind(this),
        getBulkActions: () => this.bulkActions,
        getDocumentActions: (position) => {
          if (position) {
            return this.documentActions.filter((action) => {
              return action.position == void 0 || [
                action.position
              ].flat().includes(position);
            });
          }
          return this.documentActions;
        },
        getEditViewSidePanels: () => this.editViewSidePanels,
        getHeaderActions: () => this.headerActions,
        getRichTextBlocks: () => ({
          ...this.richTextBlocksStore
        })
      }
    };
  }
  constructor() {
    this.richTextBlocksStore = {
      ...defaultBlocksStore
    };
    this.bulkActions = [
      ...DEFAULT_BULK_ACTIONS
    ];
    this.documentActions = [
      ...DEFAULT_ACTIONS,
      ...DEFAULT_TABLE_ROW_ACTIONS,
      ...DEFAULT_HEADER_ACTIONS
    ];
    this.editViewSidePanels = [
      ActionsPanel
    ];
    this.headerActions = [];
  }
};
var getPrintableType = (value) => {
  const nativeType = typeof value;
  if (nativeType === "object") {
    if (value === null) return "null";
    if (Array.isArray(value)) return "array";
    if (value instanceof Object && value.constructor.name !== "Object") {
      return value.constructor.name;
    }
  }
  return nativeType;
};
var validateDescriptionItems = (items, apiName, argName) => {
  items.forEach((item, index2) => {
    if (typeof item !== "function") {
      throw new Error(`Expected every item in the \`${argName}\` array passed to \`${apiName}\` to be a function that returns a description object, but received ${getPrintableType(item)} at index ${index2}. Did you forget to wrap it in a function, e.g. \`() => ({ ...yourAction })\`?`);
    }
  });
};

// node_modules/@strapi/content-manager/dist/admin/history/components/HistoryAction.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var import_qs = __toESM(require_lib(), 1);
var HistoryAction = ({ model, document }) => {
  const { formatMessage } = useIntl();
  const [{ query }] = useQueryParams();
  const navigate = useNavigate();
  const { trackUsage } = useTracking();
  const { pathname } = useLocation();
  const pluginsQueryParams = (0, import_qs.stringify)({
    plugins: query.plugins
  }, {
    encode: false
  });
  if (!window.strapi.features.isEnabled("cms-content-history")) {
    return null;
  }
  const handleOnClick = () => {
    const destination = {
      pathname: "history",
      search: pluginsQueryParams
    };
    trackUsage("willNavigate", {
      from: pathname,
      to: `${pathname}/${destination.pathname}`
    });
    navigate(destination);
  };
  return {
    icon: (0, import_jsx_runtime6.jsx)(d5, {}),
    label: formatMessage({
      id: "content-manager.history.document-action",
      defaultMessage: "Content History"
    }),
    onClick: handleOnClick,
    disabled: (
      /**
      * The user is creating a new document.
      * It hasn't been saved yet, so there's no history to go to
      */
      !document || /**
      * The document has been created but the current dimension has never been saved.
      * For example, the user is creating a new locale in an existing document,
      * so there's no history for the document in that locale
      */
      !document.id || /**
      * History is only available for content types created by the user.
      * These have the `api::` prefix, as opposed to the ones created by Strapi or plugins,
      * which start with `admin::` or `plugin::`
      */
      !model.startsWith("api::")
    ),
    position: "header"
  };
};
HistoryAction.type = "history";
HistoryAction.position = "header";

// node_modules/@strapi/content-manager/dist/admin/history/index.mjs
var historyAdmin = {
  bootstrap(app) {
    const { addDocumentAction } = app.getPlugin("content-manager").apis;
    addDocumentAction((actions) => {
      const indexOfDeleteAction = actions.findIndex((action) => action.type === "delete");
      actions.splice(indexOfDeleteAction, 0, HistoryAction);
      return actions;
    });
  }
};

// node_modules/@strapi/content-manager/dist/admin/modules/reducers.mjs
var reducer2 = combineReducers({
  app: reducer
});

// node_modules/@strapi/content-manager/dist/admin/preview/components/PreviewSidePanel.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var React2 = __toESM(require_react(), 1);
var import_qs2 = __toESM(require_lib(), 1);
var ConditionalTooltip = ({ isShown, label, children }) => {
  return (0, import_jsx_runtime7.jsx)(Mt, {
    label,
    open: isShown ? void 0 : false,
    children
  });
};
var PreviewSidePanel = ({ model, documentId, document }) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const { trackUsage } = useTracking();
  const { pathname } = useLocation();
  const [{ query }] = useQueryParams();
  const isModified = useForm("PreviewSidePanel", (state) => state.modified);
  const isUnsaved = Boolean(!document || !document.id);
  const title = formatMessage({
    id: "content-manager.preview.panel.title",
    defaultMessage: "Preview"
  });
  const { data, error } = useGetPreviewUrlQuery(
    {
      params: {
        contentType: model
      },
      query: {
        documentId,
        locale: document == null ? void 0 : document.locale,
        status: document == null ? void 0 : document.status
      }
    },
    // Don't bother making the request since we won't show any UI
    {
      skip: isUnsaved
    }
  );
  const [isPreviewEnabled, setIsPreviewEnabled] = React2.useState(false);
  const previewUrl = (_a = data == null ? void 0 : data.data) == null ? void 0 : _a.url;
  React2.useEffect(() => {
    if (previewUrl) {
      setIsPreviewEnabled(true);
    }
  }, [
    previewUrl
  ]);
  if (isUnsaved) {
    return null;
  }
  if (error && error.name === "NotFoundError") {
    return {
      title,
      content: (0, import_jsx_runtime7.jsx)(Ct, {
        variant: "tertiary",
        tag: Link,
        to: "https://docs.strapi.io/cms/features/preview",
        target: "_blank",
        rel: "noopener noreferrer",
        width: "100%",
        children: formatMessage({
          id: "content-manager.preview.panel.button-configuration",
          defaultMessage: "Set up preview"
        })
      })
    };
  }
  if (!isPreviewEnabled && (!((_b = data == null ? void 0 : data.data) == null ? void 0 : _b.url) || error)) {
    return null;
  }
  const trackNavigation = () => {
    const destinationPathname = pathname.replace(/\/$/, "") + "/preview";
    trackUsage("willNavigate", {
      from: pathname,
      to: destinationPathname
    });
  };
  return {
    title,
    content: (0, import_jsx_runtime7.jsx)(ConditionalTooltip, {
      label: formatMessage({
        id: "content-manager.preview.panel.button-disabled-tooltip",
        defaultMessage: "Please save to open the preview"
      }),
      isShown: isModified,
      children: (0, import_jsx_runtime7.jsx)(S, {
        cursor: "not-allowed",
        width: "100%",
        children: (0, import_jsx_runtime7.jsx)(Ct, {
          variant: "tertiary",
          tag: Link,
          to: {
            pathname: "preview",
            search: (0, import_qs2.stringify)(query, {
              encode: false
            })
          },
          onClick: trackNavigation,
          width: "100%",
          disabled: isModified,
          pointerEvents: isModified ? "none" : void 0,
          tabIndex: isModified ? -1 : void 0,
          children: formatMessage({
            id: "content-manager.preview.panel.button",
            defaultMessage: "Open preview"
          })
        })
      })
    })
  };
};

// node_modules/@strapi/content-manager/dist/admin/preview/index.mjs
var previewAdmin = {
  bootstrap(app) {
    const contentManagerPluginApis = app.getPlugin("content-manager").apis;
    contentManagerPluginApis.addEditViewSidePanel((panels) => {
      const actionsPanelIndex = panels.findIndex((panel) => panel.type === "actions");
      return [
        ...panels.slice(0, actionsPanelIndex + 1),
        PreviewSidePanel,
        ...panels.slice(actionsPanelIndex + 1)
      ];
    });
  }
};

// node_modules/@strapi/content-manager/dist/admin/index.mjs
function __variableDynamicImportRuntime4__(path) {
  switch (path) {
    case "./translations/ar.json":
      return import("./ar.json-5ZC5ZMHZ.js");
    case "./translations/ca.json":
      return import("./ca.json-6JTYG67X.js");
    case "./translations/cs.json":
      return import("./cs.json-MGDR2N7I.js");
    case "./translations/de.json":
      return import("./de.json-RSVD5BW2.js");
    case "./translations/en.json":
      return import("./en.json-AM6SFNPA.js");
    case "./translations/es.json":
      return import("./es.json-CVOGFDSA.js");
    case "./translations/eu.json":
      return import("./eu.json-BFHW4MVH.js");
    case "./translations/fr.json":
      return import("./fr.json-QTQFXOXT.js");
    case "./translations/gu.json":
      return import("./gu.json-IQE7NCKF.js");
    case "./translations/hi.json":
      return import("./hi.json-YAMJHISU.js");
    case "./translations/hu.json":
      return import("./hu.json-VCJF7QFM.js");
    case "./translations/id.json":
      return import("./id.json-3TAMS3S4.js");
    case "./translations/it.json":
      return import("./it.json-4G6PFN4V.js");
    case "./translations/ja.json":
      return import("./ja.json-BGI3PCGA.js");
    case "./translations/ko.json":
      return import("./ko.json-D4TSQGGO.js");
    case "./translations/ml.json":
      return import("./ml.json-P5O4DP77.js");
    case "./translations/ms.json":
      return import("./ms.json-QWUSUHDN.js");
    case "./translations/nl.json":
      return import("./nl.json-AQ3FQ5ZJ.js");
    case "./translations/pl.json":
      return import("./pl.json-MSV3KBZP.js");
    case "./translations/pt-BR.json":
      return import("./pt-BR.json-SM5UUBWK.js");
    case "./translations/pt.json":
      return import("./pt.json-6ZPJPFZU.js");
    case "./translations/ru.json":
      return import("./ru.json-CSCLHHL3.js");
    case "./translations/sa.json":
      return import("./sa.json-SOWCFMTG.js");
    case "./translations/sk.json":
      return import("./sk.json-YMS42W6L.js");
    case "./translations/sv.json":
      return import("./sv.json-3LTIBY7L.js");
    case "./translations/th.json":
      return import("./th.json-IVHDPMBC.js");
    case "./translations/tr.json":
      return import("./tr.json-TXKNB7AP.js");
    case "./translations/uk.json":
      return import("./uk.json-IMNDGDVW.js");
    case "./translations/vi.json":
      return import("./vi.json-6G2X5VWV.js");
    case "./translations/zh-Hans.json":
      return import("./zh-Hans.json-OUFRZFHO.js");
    case "./translations/zh.json":
      return import("./zh.json-CBMD6EOX.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
var index = {
  register(app) {
    const cm = new ContentManagerPlugin();
    app.addReducers({
      [PLUGIN_ID]: reducer2
    });
    app.addMenuLink({
      to: PLUGIN_ID,
      icon: W5,
      intlLabel: {
        id: `content-manager.plugin.name`,
        defaultMessage: "Content Manager"
      },
      permissions: [],
      position: 1
    });
    app.router.addRoute({
      path: "content-manager/*",
      lazy: async () => {
        const { Layout } = await import("./layout-PRNS5VYQ.js");
        return {
          Component: Layout
        };
      },
      children: routes
    });
    app.registerPlugin(cm.config);
    app.widgets.register((widgets) => {
      const lastEditedWidget = {
        icon: nn,
        title: {
          id: `${PLUGIN_ID}.widget.last-edited.title`,
          defaultMessage: "Last edited entries"
        },
        component: async () => {
          const { LastEditedWidget } = await import("./Widgets-LA6L3D5B.js");
          return LastEditedWidget;
        },
        pluginId: PLUGIN_ID,
        id: "last-edited-entries",
        permissions: [
          {
            action: "plugin::content-manager.explorer.read"
          }
        ]
      };
      const lastPublishedWidget = {
        icon: w5,
        title: {
          id: `${PLUGIN_ID}.widget.last-published.title`,
          defaultMessage: "Last published entries"
        },
        component: async () => {
          const { LastPublishedWidget } = await import("./Widgets-LA6L3D5B.js");
          return LastPublishedWidget;
        },
        pluginId: PLUGIN_ID,
        id: "last-published-entries",
        permissions: [
          {
            action: "plugin::content-manager.explorer.read"
          }
        ]
      };
      const entriesWidget = {
        icon: ln,
        title: {
          id: `${PLUGIN_ID}.widget.chart-entries.title`,
          defaultMessage: "Entries"
        },
        component: async () => {
          const { ChartEntriesWidget } = await import("./Widgets-LA6L3D5B.js");
          return ChartEntriesWidget;
        },
        pluginId: PLUGIN_ID,
        id: "chart-entries",
        permissions: [
          {
            action: "plugin::content-manager.explorer.read"
          }
        ]
      };
      const profileInfoIndex = widgets.findIndex((widget) => widget.id === "profile-info" && widget.pluginId === "admin");
      if (profileInfoIndex !== -1) {
        const newWidgets = [
          ...widgets
        ];
        newWidgets.splice(profileInfoIndex + 1, 0, entriesWidget);
        return [
          lastEditedWidget,
          lastPublishedWidget,
          ...newWidgets
        ];
      }
      return [
        lastEditedWidget,
        lastPublishedWidget,
        ...widgets,
        entriesWidget
      ];
    });
  },
  bootstrap(app) {
    if (typeof historyAdmin.bootstrap === "function") {
      historyAdmin.bootstrap(app);
    }
    if (typeof previewAdmin.bootstrap === "function") {
      previewAdmin.bootstrap(app);
    }
  },
  async registerTrads({ locales }) {
    const importedTrads = await Promise.all(locales.map((locale) => {
      return __variableDynamicImportRuntime4__(`./translations/${locale}.json`).then(({ default: data }) => {
        return {
          data: prefixPluginTranslations(data, PLUGIN_ID),
          locale
        };
      }).catch(() => {
        return {
          data: {},
          locale
        };
      });
    }));
    return Promise.resolve(importedTrads);
  }
};

export {
  index
};
//# sourceMappingURL=chunk-U62LYJ26.js.map
