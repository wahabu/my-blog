import {
  DRAG_DROP_TYPES,
  DragLayerRendered,
  Header,
  Root
} from "./chunk-3APCW6VK.js";
import {
  isBaseQueryError
} from "./chunk-PAY54OVR.js";
import {
  AVAILABLE_COLORS,
  getStageColorByHex
} from "./chunk-FOHWK26D.js";
import {
  LimitsModal,
  useReviewWorkflows
} from "./chunk-GNGPGEY7.js";
import {
  CHARGEBEE_STAGES_PER_WORKFLOW_ENTITLEMENT_NAME,
  CHARGEBEE_WORKFLOW_ENTITLEMENT_NAME,
  reviewWorkflowsApi,
  useGetContentTypesQuery
} from "./chunk-GF7A6RRV.js";
import {
  useTypedSelector
} from "./chunk-TGL3FZCW.js";
import "./chunk-SG3YXALT.js";
import "./chunk-LVMHP6GO.js";
import "./chunk-7FPOP2S5.js";
import "./chunk-Q535U6BT.js";
import "./chunk-D7FI3GAF.js";
import "./chunk-4R2QA5NM.js";
import "./chunk-TKXHHSNW.js";
import "./chunk-XTTXQ7FR.js";
import "./chunk-QHCTYJ6K.js";
import "./chunk-TKZBDVP5.js";
import "./chunk-LS2USQHL.js";
import "./chunk-K4JPZJYK.js";
import "./chunk-PA2ESAGW.js";
import {
  useLicenseLimits
} from "./chunk-SLUE2XUX.js";
import "./chunk-HRURINY4.js";
import "./chunk-JES3NNDO.js";
import "./chunk-F3Y2U5J7.js";
import {
  ConfirmDialog
} from "./chunk-PUECFR7A.js";
import {
  BackButton
} from "./chunk-WVMUAD6S.js";
import "./chunk-BTNRU3I6.js";
import "./chunk-YZKIGY5W.js";
import "./chunk-XNMTWOF7.js";
import "./chunk-NCYXOGJJ.js";
import "./chunk-IFOFBKTA.js";
import "./chunk-AOU4ZZD3.js";
import {
  MemoizedInputRenderer
} from "./chunk-SOVNMIKL.js";
import {
  Form,
  useField,
  useForm
} from "./chunk-2IWTCEYS.js";
import {
  useRBAC
} from "./chunk-SHEU3VT4.js";
import "./chunk-767BZMIR.js";
import "./chunk-A274YVTO.js";
import "./chunk-L4VZK4CF.js";
import {
  useTracking
} from "./chunk-QXVVXECA.js";
import "./chunk-6KQUGTAR.js";
import {
  Page,
  useAPIErrorHandler
} from "./chunk-WZNNPD7U.js";
import "./chunk-HHY6TNZ5.js";
import {
  useNotification
} from "./chunk-52FV5OAT.js";
import "./chunk-NE2H5B4K.js";
import "./chunk-ALJOKJHY.js";
import "./chunk-ZISMY363.js";
import "./chunk-FH7EDXAD.js";
import {
  getEmptyImage
} from "./chunk-LCUEZJJN.js";
import {
  useDrag,
  useDrop
} from "./chunk-2A7DKUQA.js";
import "./chunk-TVYQEFKB.js";
import "./chunk-MBK4V2X7.js";
import "./chunk-BC4MOUEJ.js";
import "./chunk-JRCQYP7F.js";
import "./chunk-36WB3I5J.js";
import "./chunk-45QSVGD5.js";
import "./chunk-KS6HIU44.js";
import {
  create4 as create,
  create5 as create2,
  create6 as create3,
  create7 as create4
} from "./chunk-OG6FDUVQ.js";
import "./chunk-A6QRMII5.js";
import {
  generateNKeysBetween
} from "./chunk-3DRZIGZ7.js";
import "./chunk-AJ6CDECK.js";
import "./chunk-YULRRBBU.js";
import "./chunk-KVWVHPCR.js";
import "./chunk-Y5S667H6.js";
import "./chunk-OJ6RZTLV.js";
import "./chunk-NZZAX2AB.js";
import "./chunk-T7T43G5B.js";
import "./chunk-NR6XEPE4.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import "./chunk-HAUWLGB7.js";
import "./chunk-3MST3H25.js";
import {
  useNavigate,
  useParams
} from "./chunk-QBM2MGBP.js";
import "./chunk-AHJOALQ2.js";
import "./chunk-U5ASKYVL.js";
import "./chunk-QQEUXDAQ.js";
import "./chunk-LHXIEHKN.js";
import {
  $e,
  A,
  An,
  C,
  Ct,
  Dd,
  Ld,
  Pc,
  Pd,
  Qt,
  S,
  be,
  cr,
  dd,
  dr,
  ec,
  gd,
  tc,
  te
} from "./chunk-7ZODKSNG.js";
import "./chunk-SXAHFOGA.js";
import "./chunk-6I7FUUCQ.js";
import "./chunk-JDJCDU5Q.js";
import {
  I5,
  W3,
  h5,
  on,
  q5,
  y5
} from "./chunk-D4B4CG5U.js";
import "./chunk-VQHBTUHH.js";
import "./chunk-PC7RLLVI.js";
import "./chunk-AHKO7ZVV.js";
import "./chunk-JCO3CRJF.js";
import "./chunk-HHU3PYNH.js";
import "./chunk-OYIDWRUX.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  gt
} from "./chunk-4N2AQQ2K.js";
import "./chunk-4MZT7456.js";
import "./chunk-QBO3FZHQ.js";
import "./chunk-KKBG5U5K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/id.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var React4 = __toESM(require_react(), 1);

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/components/Stages.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var React3 = __toESM(require_react(), 1);

// node_modules/@strapi/review-workflows/dist/admin/services/admin.mjs
var adminApi = reviewWorkflowsApi.injectEndpoints({
  endpoints(builder) {
    return {
      getAdminRoles: builder.query({
        query: () => ({
          url: `/admin/roles`,
          method: "GET"
        }),
        transformResponse: (res) => {
          return res.data;
        }
      })
    };
  }
});
var { useGetAdminRolesQuery } = adminApi;

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/hooks/useDragAndDrop.mjs
var React2 = __toESM(require_react(), 1);

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/hooks/useKeyboardDragAndDrop.mjs
var React = __toESM(require_react(), 1);
var useKeyboardDragAndDrop = (active, index, { onCancel, onDropItem, onGrabItem, onMoveItem }) => {
  const [isSelected, setIsSelected] = React.useState(false);
  const handleMove = (movement) => {
    if (!isSelected) {
      return;
    }
    if (typeof index === "number" && onMoveItem) {
      if (movement === "UP") {
        onMoveItem(index - 1, index);
      } else if (movement === "DOWN") {
        onMoveItem(index + 1, index);
      }
    }
  };
  const handleDragClick = () => {
    if (isSelected) {
      if (onDropItem) {
        onDropItem(index);
      }
      setIsSelected(false);
    } else {
      if (onGrabItem) {
        onGrabItem(index);
      }
      setIsSelected(true);
    }
  };
  const handleCancel = () => {
    if (isSelected) {
      setIsSelected(false);
      if (onCancel) {
        onCancel(index);
      }
    }
  };
  const handleKeyDown = (e) => {
    if (!active) {
      return;
    }
    if (e.key === "Tab" && !isSelected) {
      return;
    }
    e.preventDefault();
    switch (e.key) {
      case " ":
      case "Enter":
        handleDragClick();
        break;
      case "Escape":
        handleCancel();
        break;
      case "ArrowDown":
      case "ArrowRight":
        handleMove("DOWN");
        break;
      case "ArrowUp":
      case "ArrowLeft":
        handleMove("UP");
        break;
    }
  };
  return handleKeyDown;
};

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/hooks/useDragAndDrop.mjs
var DIRECTIONS = {
  UPWARD: "upward",
  DOWNWARD: "downward"
};
var DROP_SENSITIVITY = {
  REGULAR: "regular"
};
var useDragAndDrop = (active, { type = "STRAPI_DND", index, item, onStart, onEnd, onGrabItem, onDropItem, onCancel, onMoveItem, dropSensitivity = DROP_SENSITIVITY.REGULAR }) => {
  const objectRef = React2.useRef(null);
  const [{ handlerId, isOver }, dropRef] = useDrop({
    accept: type,
    collect(monitor) {
      return {
        handlerId: monitor.getHandlerId(),
        isOver: monitor.isOver({
          shallow: true
        })
      };
    },
    drop(item2) {
      const draggedIndex = item2.index;
      const newIndex = index;
      if (isOver && onDropItem) {
        onDropItem(draggedIndex, newIndex);
      }
    },
    hover(item2, monitor) {
      var _a;
      if (!objectRef.current || !onMoveItem) {
        return;
      }
      const dragIndex = item2.index;
      const newIndex = index;
      const hoverBoundingRect = (_a = objectRef.current) == null ? void 0 : _a.getBoundingClientRect();
      const hoverMiddleY = (hoverBoundingRect.bottom - hoverBoundingRect.top) / 2;
      const clientOffset = monitor.getClientOffset();
      if (!clientOffset) return;
      const hoverClientY = clientOffset && clientOffset.y - hoverBoundingRect.top;
      if (typeof dragIndex === "number" && typeof newIndex === "number") {
        if (dragIndex === newIndex) {
          return;
        }
        if (dropSensitivity === DROP_SENSITIVITY.REGULAR) {
          if (dragIndex < newIndex && hoverClientY < hoverMiddleY) {
            return;
          }
          if (dragIndex > newIndex && hoverClientY > hoverMiddleY) {
            return;
          }
        }
        onMoveItem(newIndex, dragIndex);
        item2.index = newIndex;
      } else {
        if (Array.isArray(dragIndex) && Array.isArray(newIndex)) {
          const minLength = Math.min(dragIndex.length, newIndex.length);
          let areEqual = true;
          let isLessThan = false;
          let isGreaterThan = false;
          for (let i = 0; i < minLength; i++) {
            if (dragIndex[i] < newIndex[i]) {
              isLessThan = true;
              areEqual = false;
              break;
            } else if (dragIndex[i] > newIndex[i]) {
              isGreaterThan = true;
              areEqual = false;
              break;
            }
          }
          if (areEqual && dragIndex.length === newIndex.length) {
            return;
          }
          if (dropSensitivity === DROP_SENSITIVITY.REGULAR) {
            if (isLessThan && !isGreaterThan && hoverClientY < hoverMiddleY) {
              return;
            }
            if (isGreaterThan && !isLessThan && hoverClientY > hoverMiddleY) {
              return;
            }
          }
        }
        onMoveItem(newIndex, dragIndex);
        item2.index = newIndex;
      }
    }
  });
  const getDragDirection = (monitor) => {
    if (monitor && monitor.isDragging() && !monitor.didDrop() && monitor.getInitialClientOffset() && monitor.getClientOffset()) {
      const deltaY = monitor.getInitialClientOffset().y - monitor.getClientOffset().y;
      if (deltaY > 0) return DIRECTIONS.UPWARD;
      if (deltaY < 0) return DIRECTIONS.DOWNWARD;
      return null;
    }
    return null;
  };
  const [{ isDragging, direction }, dragRef, dragPreviewRef] = useDrag({
    type,
    item() {
      var _a;
      if (onStart) {
        onStart();
      }
      const { width } = ((_a = objectRef.current) == null ? void 0 : _a.getBoundingClientRect()) ?? {};
      return {
        index,
        width,
        ...item
      };
    },
    end() {
      if (onEnd) {
        onEnd();
      }
    },
    canDrag: active,
    /**
    * This is useful when the item is in a virtualized list.
    * However, if we don't have an ID then we want the libraries
    * defaults to take care of this.
    */
    isDragging: (item == null ? void 0 : item.id) ? (monitor) => {
      return item.id === monitor.getItem().id;
    } : void 0,
    collect: (monitor) => ({
      isDragging: monitor.isDragging(),
      initialOffset: monitor.getInitialClientOffset(),
      currentOffset: monitor.getClientOffset(),
      direction: getDragDirection(monitor)
    })
  });
  const handleKeyDown = useKeyboardDragAndDrop(active, index, {
    onGrabItem,
    onDropItem,
    onCancel,
    onMoveItem
  });
  return [
    {
      handlerId,
      isDragging,
      handleKeyDown,
      isOverDropTarget: isOver,
      direction
    },
    objectRef,
    dropRef,
    dragRef,
    dragPreviewRef
  ];
};

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/components/AddStage.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var AddStage = ({ children, ...props }) => {
  return (0, import_jsx_runtime.jsx)(StyledButton, {
    tag: "button",
    background: "neutral0",
    borderColor: "neutral150",
    paddingBottom: 3,
    paddingLeft: 4,
    paddingRight: 4,
    paddingTop: 3,
    shadow: "filterShadow",
    ...props,
    children: (0, import_jsx_runtime.jsx)(A, {
      variant: "pi",
      fontWeight: "bold",
      children: (0, import_jsx_runtime.jsxs)(C, {
        tag: "span",
        gap: 2,
        children: [
          (0, import_jsx_runtime.jsx)(on, {
            width: "2.4rem",
            height: "2.4rem",
            "aria-hidden": true
          }),
          children
        ]
      })
    })
  });
};
var StyledButton = gt(S)`
  border-radius: 26px;
  color: ${({ theme }) => theme.colors.neutral500};

  &:hover {
    color: ${({ theme }) => theme.colors.primary600};
  }

  &:active {
    color: ${({ theme }) => theme.colors.primary600};
  }
`;

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/components/Stages.mjs
var Stages = ({ canDelete = true, canUpdate = true }) => {
  const { formatMessage } = useIntl();
  const { trackUsage } = useTracking();
  const addFieldRow = useForm("Stages", (state) => state.addFieldRow);
  const { value: stages = [] } = useField("stages");
  return (0, import_jsx_runtime2.jsxs)(C, {
    direction: "column",
    gap: 6,
    width: "100%",
    children: [
      (0, import_jsx_runtime2.jsxs)(S, {
        position: "relative",
        width: "100%",
        children: [
          (0, import_jsx_runtime2.jsx)(Background, {
            background: "neutral200",
            height: "100%",
            left: "50%",
            position: "absolute",
            top: "0",
            width: 2
          }),
          (0, import_jsx_runtime2.jsx)(C, {
            direction: "column",
            alignItems: "stretch",
            gap: 6,
            position: "relative",
            tag: "ol",
            children: stages.map((stage, index) => {
              return (0, import_jsx_runtime2.jsx)(S, {
                tag: "li",
                children: (0, import_jsx_runtime2.jsx)(Stage, {
                  index,
                  canDelete: stages.length > 1 && canDelete,
                  canReorder: stages.length > 1,
                  canUpdate,
                  stagesCount: stages.length,
                  defaultOpen: !stage.id,
                  ...stage
                })
              }, stage.__temp_key__);
            })
          })
        ]
      }),
      canUpdate && (0, import_jsx_runtime2.jsx)(AddStage, {
        type: "button",
        onClick: () => {
          addFieldRow("stages", {
            name: ""
          });
          trackUsage("willCreateStage");
        },
        children: formatMessage({
          id: "Settings.review-workflows.stage.add",
          defaultMessage: "Add new stage"
        })
      })
    ]
  });
};
var Background = gt(S)`
  transform: translateX(-50%);
`;
var Stage = ({ index, canDelete = false, canReorder = false, canUpdate = false, stagesCount, name, fromPermissions, toPermissions, color, defaultOpen }) => {
  const [liveText, setLiveText] = React3.useState();
  const { formatMessage } = useIntl();
  const { trackUsage } = useTracking();
  const stageErrors = useForm("Stages", (state) => state.errors.stages);
  const stageError = Array.isArray(stageErrors) ? stageErrors[index] : void 0;
  const error = typeof stageError === "object" && stageError !== null ? stageError : void 0;
  const addFieldRow = useForm("Stage", (state) => state.addFieldRow);
  const moveFieldRow = useForm("Stage", (state) => state.moveFieldRow);
  const removeFieldRow = useForm("Stage", (state) => state.removeFieldRow);
  const getItemPos = (index2) => `${index2 + 1} of ${stagesCount}`;
  const handleGrabStage = (index2) => {
    setLiveText(formatMessage({
      id: "dnd.grab-item",
      defaultMessage: `{item}, grabbed. Current position in list: {position}. Press up and down arrow to change position, Spacebar to drop, Escape to cancel.`
    }, {
      item: name,
      position: getItemPos(index2)
    }));
  };
  const handleDropStage = (index2) => {
    setLiveText(formatMessage({
      id: "dnd.drop-item",
      defaultMessage: `{item}, dropped. Final position in list: {position}.`
    }, {
      item: name,
      position: getItemPos(index2)
    }));
  };
  const handleCancelDragStage = () => {
    setLiveText(formatMessage({
      id: "dnd.cancel-item",
      defaultMessage: "{item}, dropped. Re-order cancelled."
    }, {
      item: name
    }));
  };
  const handleMoveStage = (newIndex, oldIndex) => {
    setLiveText(formatMessage({
      id: "dnd.reorder",
      defaultMessage: "{item}, moved. New position in list: {position}."
    }, {
      item: name,
      position: getItemPos(newIndex)
    }));
    moveFieldRow("stages", oldIndex, newIndex);
  };
  const [{ handlerId, isDragging, handleKeyDown }, stageRef, dropRef, dragRef, dragPreviewRef] = useDragAndDrop(canReorder, {
    index,
    item: {
      index,
      name
    },
    onGrabItem: handleGrabStage,
    onDropItem: handleDropStage,
    onMoveItem: handleMoveStage,
    onCancel: handleCancelDragStage,
    type: DRAG_DROP_TYPES.STAGE
  });
  const composedRef = te(stageRef, dropRef);
  React3.useEffect(() => {
    dragPreviewRef(getEmptyImage(), {
      captureDraggingState: false
    });
  }, [
    dragPreviewRef,
    index
  ]);
  const handleCloneClick = () => {
    addFieldRow("stages", {
      name,
      color,
      fromPermissions,
      toPermissions
    });
  };
  const id = React3.useId();
  return (0, import_jsx_runtime2.jsxs)(S, {
    ref: composedRef,
    shadow: "tableShadow",
    children: [
      liveText && (0, import_jsx_runtime2.jsx)($e, {
        "aria-live": "assertive",
        children: liveText
      }),
      isDragging ? (0, import_jsx_runtime2.jsx)(S, {
        background: "primary100",
        borderStyle: "dashed",
        borderColor: "primary600",
        borderWidth: "1px",
        display: "block",
        hasRadius: true,
        padding: 6
      }) : (0, import_jsx_runtime2.jsx)(AccordionRoot, {
        onValueChange: (value) => {
          if (value) {
            trackUsage("willEditStage");
          }
        },
        defaultValue: defaultOpen ? id : void 0,
        $error: Object.values(error ?? {}).length > 0,
        children: (0, import_jsx_runtime2.jsxs)(dd.Item, {
          value: id,
          children: [
            (0, import_jsx_runtime2.jsxs)(dd.Header, {
              children: [
                (0, import_jsx_runtime2.jsx)(dd.Trigger, {
                  children: name
                }),
                (0, import_jsx_runtime2.jsx)(dd.Actions, {
                  children: canDelete || canUpdate ? (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, {
                    children: [
                      (0, import_jsx_runtime2.jsxs)(gd.Root, {
                        children: [
                          (0, import_jsx_runtime2.jsxs)(ContextMenuTrigger, {
                            size: "S",
                            endIcon: null,
                            paddingLeft: 2,
                            paddingRight: 2,
                            children: [
                              (0, import_jsx_runtime2.jsx)(W3, {
                                "aria-hidden": true,
                                focusable: false
                              }),
                              (0, import_jsx_runtime2.jsx)($e, {
                                tag: "span",
                                children: formatMessage({
                                  id: "[tbdb].components.DynamicZone.more-actions",
                                  defaultMessage: "More actions"
                                })
                              })
                            ]
                          }),
                          (0, import_jsx_runtime2.jsx)(gd.Content, {
                            popoverPlacement: "bottom-end",
                            zIndex: 2,
                            children: (0, import_jsx_runtime2.jsxs)(gd.SubRoot, {
                              children: [
                                canUpdate && (0, import_jsx_runtime2.jsx)(gd.Item, {
                                  onClick: handleCloneClick,
                                  children: formatMessage({
                                    id: "Settings.review-workflows.stage.delete",
                                    defaultMessage: "Duplicate stage"
                                  })
                                }),
                                canDelete && (0, import_jsx_runtime2.jsx)(gd.Item, {
                                  onClick: () => removeFieldRow("stages", index),
                                  variant: "danger",
                                  children: formatMessage({
                                    id: "Settings.review-workflows.stage.delete",
                                    defaultMessage: "Delete"
                                  })
                                })
                              ]
                            })
                          })
                        ]
                      }),
                      canUpdate && (0, import_jsx_runtime2.jsx)(be, {
                        background: "transparent",
                        hasRadius: true,
                        variant: "ghost",
                        "data-handler-id": handlerId,
                        ref: dragRef,
                        label: formatMessage({
                          id: "Settings.review-workflows.stage.drag",
                          defaultMessage: "Drag"
                        }),
                        onClick: (e) => e.stopPropagation(),
                        onKeyDown: handleKeyDown,
                        children: (0, import_jsx_runtime2.jsx)(q5, {})
                      })
                    ]
                  }) : null
                })
              ]
            }),
            (0, import_jsx_runtime2.jsx)(dd.Content, {
              children: (0, import_jsx_runtime2.jsx)(Ld.Root, {
                gap: 4,
                padding: 6,
                children: [
                  {
                    disabled: !canUpdate,
                    label: formatMessage({
                      id: "Settings.review-workflows.stage.name.label",
                      defaultMessage: "Stage name"
                    }),
                    name: `stages.${index}.name`,
                    required: true,
                    size: 6,
                    type: "string"
                  },
                  {
                    disabled: !canUpdate,
                    label: formatMessage({
                      id: "review-workflows.stage.color",
                      defaultMessage: "Color"
                    }),
                    name: `stages.${index}.color`,
                    required: true,
                    size: 6,
                    type: "color"
                  },
                  {
                    disabled: !canUpdate,
                    label: formatMessage({
                      id: "Settings.review-workflows.stage.permissions.label",
                      defaultMessage: "Roles that can change from this stage"
                    }),
                    name: `stages.${index}.fromPermissions`,
                    placeholder: formatMessage({
                      id: "Settings.review-workflows.stage.permissions.placeholder",
                      defaultMessage: "Select a role"
                    }),
                    required: true,
                    size: 6,
                    type: "permissions"
                  },
                  {
                    disabled: !canUpdate,
                    label: formatMessage({
                      id: "Settings.review-workflows.stage.toPermissions.label",
                      defaultMessage: "Roles that can move content to this stage"
                    }),
                    name: `stages.${index}.toPermissions`,
                    placeholder: formatMessage({
                      id: "Settings.review-workflows.stage.toPermissions.placeholder",
                      defaultMessage: "Select a role"
                    }),
                    required: false,
                    size: 6,
                    type: "permissions"
                  }
                ].map(({ size, ...field }) => (0, import_jsx_runtime2.jsx)(Ld.Item, {
                  col: size,
                  xs: 12,
                  direction: "column",
                  alignItems: "stretch",
                  children: (0, import_jsx_runtime2.jsx)(InputRenderer, {
                    ...field
                  })
                }, field.name))
              })
            })
          ]
        })
      })
    ]
  });
};
var AccordionRoot = gt(dd.Root)`
  border: 1px solid
    ${({ theme, $error }) => $error ? theme.colors.danger600 : theme.colors.neutral200};
`;
var ContextMenuTrigger = gt(gd.Trigger)`
  :hover,
  :focus {
    background-color: ${({ theme }) => theme.colors.neutral100};
  }

  > span {
    font-size: 0;
  }
`;
var InputRenderer = (props) => {
  switch (props.type) {
    case "color":
      return (0, import_jsx_runtime2.jsx)(ColorSelector, {
        ...props
      });
    case "permissions":
      return (0, import_jsx_runtime2.jsx)(PermissionsField, {
        ...props
      });
    default:
      return (0, import_jsx_runtime2.jsx)(MemoizedInputRenderer, {
        ...props
      });
  }
};
var ColorSelector = ({ disabled, label, name, required }) => {
  const { formatMessage } = useIntl();
  const { value, error, onChange } = useField(name);
  const colorOptions = AVAILABLE_COLORS.map(({ hex, name: name2 }) => ({
    value: hex,
    label: formatMessage({
      id: "Settings.review-workflows.stage.color.name",
      defaultMessage: "{name}"
    }, {
      name: name2
    }),
    color: hex
  }));
  const { themeColorName } = getStageColorByHex(value) ?? {};
  return (0, import_jsx_runtime2.jsxs)(Dd.Root, {
    error,
    name,
    required,
    children: [
      (0, import_jsx_runtime2.jsx)(Dd.Label, {
        children: label
      }),
      (0, import_jsx_runtime2.jsx)(cr, {
        disabled,
        onChange: (v) => {
          onChange(name, v.toString());
        },
        value: value == null ? void 0 : value.toUpperCase(),
        startIcon: (0, import_jsx_runtime2.jsx)(C, {
          tag: "span",
          height: 2,
          background: value,
          borderColor: themeColorName === "neutral0" ? "neutral150" : "transparent",
          hasRadius: true,
          shrink: 0,
          width: 2
        }),
        children: colorOptions.map(({ value: value2, label: label2, color }) => {
          const { themeColorName: themeColorName2 } = getStageColorByHex(color) || {};
          return (0, import_jsx_runtime2.jsx)(dr, {
            value: value2,
            startIcon: (0, import_jsx_runtime2.jsx)(C, {
              tag: "span",
              height: 2,
              background: color,
              borderColor: themeColorName2 === "neutral0" ? "neutral150" : "transparent",
              hasRadius: true,
              shrink: 0,
              width: 2
            }),
            children: label2
          }, value2);
        })
      }),
      (0, import_jsx_runtime2.jsx)(Dd.Error, {})
    ]
  });
};
var PermissionsField = ({ label, disabled, name, placeholder, required }) => {
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const [isApplyAllConfirmationOpen, setIsApplyAllConfirmationOpen] = React3.useState(false);
  const { value = [], error, onChange } = useField(name);
  const allStages = useForm("PermissionsField", (state) => state.values.stages ?? []);
  const onFormValueChange = useForm("PermissionsField", (state) => state.onChange);
  const fieldKey = name.split(".").pop();
  const rolesErrorCount = React3.useRef(0);
  const { data: roles = [], isLoading, error: getRolesError } = useGetAdminRolesQuery();
  const filteredRoles = (roles == null ? void 0 : roles.filter((role) => role.code !== "strapi-super-admin")) ?? [];
  React3.useEffect(() => {
    if (!isLoading && getRolesError && "status" in getRolesError && getRolesError.status == 403 && rolesErrorCount.current === 0) {
      rolesErrorCount.current = 1;
      toggleNotification({
        blockTransition: true,
        type: "danger",
        message: formatMessage({
          id: "review-workflows.stage.permissions.noPermissions.description",
          defaultMessage: "You don’t have the permission to see roles. Contact your administrator."
        })
      });
    }
  }, [
    formatMessage,
    isLoading,
    roles,
    toggleNotification,
    getRolesError
  ]);
  if (!isLoading && filteredRoles.length === 0) {
    return (0, import_jsx_runtime2.jsxs)(Dd.Root, {
      name,
      hint: formatMessage({
        id: "Settings.review-workflows.stage.permissions.noPermissions.description",
        defaultMessage: "You don’t have the permission to see roles"
      }),
      required,
      children: [
        (0, import_jsx_runtime2.jsx)(Dd.Label, {
          children: label
        }),
        (0, import_jsx_runtime2.jsx)(Pc, {
          disabled: true,
          placeholder: formatMessage({
            id: "components.NotAllowedInput.text",
            defaultMessage: "No permissions to see this field"
          }),
          startAction: (0, import_jsx_runtime2.jsx)(I5, {
            fill: "neutral600"
          }),
          type: "text",
          value: ""
        }),
        (0, import_jsx_runtime2.jsx)(Dd.Hint, {})
      ]
    });
  }
  return (0, import_jsx_runtime2.jsx)(import_jsx_runtime2.Fragment, {
    children: (0, import_jsx_runtime2.jsxs)(C, {
      alignItems: "flex-end",
      gap: 3,
      children: [
        (0, import_jsx_runtime2.jsx)(PermissionWrapper, {
          grow: 1,
          children: (0, import_jsx_runtime2.jsxs)(Dd.Root, {
            error,
            name,
            required: true,
            children: [
              (0, import_jsx_runtime2.jsx)(Dd.Label, {
                children: label
              }),
              (0, import_jsx_runtime2.jsx)(ec, {
                disabled,
                onChange: (values) => {
                  const permissions = values.map((value2) => ({
                    role: parseInt(value2, 10),
                    action: "admin::review-workflows.stage.transition"
                  }));
                  onChange(name, permissions);
                },
                placeholder,
                // The Select component expects strings for values
                value: value.map((permission) => `${permission.role}`),
                withTags: true,
                children: (0, import_jsx_runtime2.jsx)(tc, {
                  label: formatMessage({
                    id: "Settings.review-workflows.stage.permissions.allRoles.label",
                    defaultMessage: "All roles"
                  }),
                  values: filteredRoles.map((r) => `${r.id}`),
                  children: filteredRoles.map((role) => {
                    return (0, import_jsx_runtime2.jsx)(NestedOption, {
                      value: `${role.id}`,
                      children: role.name
                    }, role.id);
                  })
                })
              }),
              (0, import_jsx_runtime2.jsx)(Dd.Error, {})
            ]
          })
        }),
        (0, import_jsx_runtime2.jsxs)(Pd.Root, {
          open: isApplyAllConfirmationOpen,
          onOpenChange: setIsApplyAllConfirmationOpen,
          children: [
            (0, import_jsx_runtime2.jsx)(Pd.Trigger, {
              children: (0, import_jsx_runtime2.jsx)(be, {
                disabled,
                label: formatMessage({
                  id: "Settings.review-workflows.stage.permissions.apply.label",
                  defaultMessage: "Apply to all stages"
                }),
                size: "L",
                children: (0, import_jsx_runtime2.jsx)(y5, {})
              })
            }),
            (0, import_jsx_runtime2.jsx)(ConfirmDialog, {
              onConfirm: () => {
                onFormValueChange("stages", allStages.map((stage) => ({
                  ...stage,
                  [fieldKey]: value
                })));
                setIsApplyAllConfirmationOpen(false);
                toggleNotification({
                  type: "success",
                  message: formatMessage({
                    id: "Settings.review-workflows.page.edit.confirm.stages.permissions.copy.success",
                    defaultMessage: "Applied roles to all other stages of the workflow"
                  })
                });
              },
              variant: "default",
              children: formatMessage({
                id: "Settings.review-workflows.page.edit.confirm.stages.permissions.copy",
                defaultMessage: "Roles that can change that stage will be applied to all the other stages."
              })
            })
          ]
        })
      ]
    })
  });
};
var NestedOption = gt(An)`
  padding-left: ${({ theme }) => theme.spaces[7]};
`;
var PermissionWrapper = gt(C)`
  > * {
    flex-grow: 1;
  }
`;

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/components/WorkflowAttributes.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var WorkflowAttributes = ({ canUpdate = true }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime3.jsxs)(Ld.Root, {
    background: "neutral0",
    hasRadius: true,
    gap: 4,
    padding: 6,
    shadow: "tableShadow",
    children: [
      (0, import_jsx_runtime3.jsx)(Ld.Item, {
        col: 6,
        xs: 12,
        direction: "column",
        alignItems: "stretch",
        children: (0, import_jsx_runtime3.jsx)(MemoizedInputRenderer, {
          disabled: !canUpdate,
          label: formatMessage({
            id: "Settings.review-workflows.workflow.name.label",
            defaultMessage: "Workflow Name"
          }),
          name: "name",
          required: true,
          type: "text"
        })
      }),
      (0, import_jsx_runtime3.jsx)(Ld.Item, {
        col: 6,
        xs: 12,
        direction: "column",
        alignItems: "stretch",
        children: (0, import_jsx_runtime3.jsx)(ContentTypesSelector, {
          disabled: !canUpdate
        })
      }),
      (0, import_jsx_runtime3.jsx)(Ld.Item, {
        col: 6,
        xs: 12,
        direction: "column",
        alignItems: "stretch",
        children: (0, import_jsx_runtime3.jsx)(StageSelector, {
          disabled: !canUpdate
        })
      })
    ]
  });
};
var ContentTypesSelector = ({ disabled }) => {
  const { formatMessage, locale } = useIntl();
  const { data: contentTypes, isLoading } = useGetContentTypesQuery();
  const { workflows } = useReviewWorkflows();
  const currentWorkflow = useForm("ContentTypesSelector", (state) => state.values);
  const { error, value = [], onChange } = useField("contentTypes");
  const formatter = Qt(locale, {
    sensitivity: "base"
  });
  const isDisabled = disabled || isLoading || !contentTypes || contentTypes.collectionType.length === 0 && contentTypes.singleType.length === 0;
  const collectionTypes = ((contentTypes == null ? void 0 : contentTypes.collectionType) ?? []).toSorted((a, b) => formatter.compare(a.info.displayName, b.info.displayName)).map((contentType) => ({
    label: contentType.info.displayName,
    value: contentType.uid
  }));
  const singleTypes = ((contentTypes == null ? void 0 : contentTypes.singleType) ?? []).map((contentType) => ({
    label: contentType.info.displayName,
    value: contentType.uid
  }));
  return (0, import_jsx_runtime3.jsxs)(Dd.Root, {
    error,
    name: "contentTypes",
    children: [
      (0, import_jsx_runtime3.jsx)(Dd.Label, {
        children: formatMessage({
          id: "Settings.review-workflows.workflow.contentTypes.label",
          defaultMessage: "Associated to"
        })
      }),
      (0, import_jsx_runtime3.jsx)(ec, {
        customizeContent: (value2) => formatMessage({
          id: "Settings.review-workflows.workflow.contentTypes.displayValue",
          defaultMessage: "{count} {count, plural, one {content type} other {content types}} selected"
        }, {
          count: value2 == null ? void 0 : value2.length
        }),
        disabled: isDisabled,
        onChange: (values) => {
          onChange("contentTypes", values);
        },
        value,
        placeholder: formatMessage({
          id: "Settings.review-workflows.workflow.contentTypes.placeholder",
          defaultMessage: "Select"
        }),
        children: [
          ...collectionTypes.length > 0 ? [
            {
              label: formatMessage({
                id: "Settings.review-workflows.workflow.contentTypes.collectionTypes.label",
                defaultMessage: "Collection Types"
              }),
              children: collectionTypes
            }
          ] : [],
          ...singleTypes.length > 0 ? [
            {
              label: formatMessage({
                id: "Settings.review-workflows.workflow.contentTypes.singleTypes.label",
                defaultMessage: "Single Types"
              }),
              children: singleTypes
            }
          ] : []
        ].map((opt) => {
          return (0, import_jsx_runtime3.jsx)(tc, {
            label: opt.label,
            values: opt.children.map((child) => child.value.toString()),
            children: opt.children.map((child) => {
              const { name: assignedWorkflowName } = (workflows == null ? void 0 : workflows.find((workflow) => (currentWorkflow && workflow.id !== currentWorkflow.id || !currentWorkflow) && workflow.contentTypes.includes(child.value))) ?? {};
              return (0, import_jsx_runtime3.jsx)(NestedOption2, {
                value: child.value,
                children: (0, import_jsx_runtime3.jsx)(A, {
                  children: (
                    // @ts-expect-error - formatMessage options doesn't expect to be a React component but that's what we need actually for the <i> and <em> components
                    formatMessage({
                      id: "Settings.review-workflows.workflow.contentTypes.assigned.notice",
                      defaultMessage: "{label} {name, select, undefined {} other {<i>(assigned to <em>{name}</em> workflow)</i>}}"
                    }, {
                      label: child.label,
                      name: assignedWorkflowName,
                      em: (...children) => (0, import_jsx_runtime3.jsx)(A, {
                        tag: "em",
                        fontWeight: "bold",
                        children
                      }),
                      i: (...children) => (0, import_jsx_runtime3.jsx)(ContentTypeTakeNotice, {
                        children
                      })
                    })
                  )
                })
              }, child.value);
            })
          }, opt.label);
        })
      })
    ]
  });
};
var NestedOption2 = gt(An)`
  padding-left: ${({ theme }) => theme.spaces[7]};
`;
var ContentTypeTakeNotice = gt(A)`
  font-style: italic;
`;
var StageSelector = ({ disabled }) => {
  const { value: stages = [] } = useField("stages");
  const { formatMessage } = useIntl();
  const { error, value, onChange } = useField("stageRequiredToPublish");
  const validStages = stages.filter((stage) => stage.name);
  return (0, import_jsx_runtime3.jsxs)(Dd.Root, {
    error,
    name: "stageRequiredToPublish",
    hint: formatMessage({
      id: "settings.review-workflows.workflow.stageRequiredToPublish.hint",
      defaultMessage: "Prevents entries from being published if they are not at the required stage."
    }),
    children: [
      (0, import_jsx_runtime3.jsx)(Dd.Label, {
        children: formatMessage({
          id: "settings.review-workflows.workflow.stageRequiredToPublish.label",
          defaultMessage: "Required stage for publishing"
        })
      }),
      (0, import_jsx_runtime3.jsxs)(cr, {
        disabled,
        onChange: (value2) => {
          onChange("stageRequiredToPublish", value2);
        },
        value,
        children: [
          (0, import_jsx_runtime3.jsx)(dr, {
            value: "",
            children: formatMessage({
              id: "settings.review-workflows.workflow.stageRequiredToPublish.any",
              defaultMessage: "Any stage"
            })
          }),
          validStages.map((stage) => {
            var _a;
            return (0, import_jsx_runtime3.jsx)(dr, {
              value: ((_a = stage.id) == null ? void 0 : _a.toString()) || stage.__temp_key__,
              children: stage.name
            }, `requiredToPublishStage-${stage.id || stage.__temp_key__}`);
          })
        ]
      }),
      (0, import_jsx_runtime3.jsx)(Dd.Hint, {})
    ]
  });
};

// node_modules/@strapi/review-workflows/dist/admin/routes/settings/id.mjs
var havePermissionsChanged = (before, after) => {
  if ((before == null ? void 0 : before.length) !== (after == null ? void 0 : after.length)) {
    return true;
  }
  return !(before == null ? void 0 : before.every((beforePermission) => after == null ? void 0 : after.some((afterPermission) => afterPermission.role === beforePermission.role)));
};
var WORKFLOW_SCHEMA = create3({
  contentTypes: create4().of(create()),
  name: create().max(255, {
    id: "review-workflows.validation.name.max-length",
    defaultMessage: "Name can not be longer than 255 characters"
  }).required().nullable(),
  stages: create4().of(create3().shape({
    name: create().nullable().required({
      id: "review-workflows.validation.stage.name",
      defaultMessage: "Name is required"
    }).max(255, {
      id: "review-workflows.validation.stage.max-length",
      defaultMessage: "Name can not be longer than 255 characters"
    }).test("unique-name", {
      id: "review-workflows.validation.stage.duplicate",
      defaultMessage: "Stage name must be unique"
    }, (stageName, context) => {
      const { stages } = context.from[1].value;
      return stages.filter((stage) => stage.name === stageName).length === 1;
    }),
    color: create().nullable().required({
      id: "review-workflows.validation.stage.color",
      defaultMessage: "Color is required"
    }).matches(/^#(?:[0-9a-fA-F]{3}){1,2}$/i),
    fromPermissions: create4(create3({
      role: create2().strict().typeError({
        id: "review-workflows.validation.stage.permissions.role.number",
        defaultMessage: "Role must be of type number"
      }).required(),
      action: create().required({
        id: "review-workflows.validation.stage.permissions.action.required",
        defaultMessage: "Action is a required argument"
      })
    })).strict(),
    toPermissions: create4(create3({
      role: create2().strict().typeError({
        id: "review-workflows.validation.stage.permissions.role.number",
        defaultMessage: "Role must be of type number"
      }).required(),
      action: create().required({
        id: "review-workflows.validation.stage.permissions.action.required",
        defaultMessage: "Action is a required argument"
      })
    })).strict()
  })).min(1),
  stageRequiredToPublish: create().nullable()
});
var EditPage = () => {
  const { id = "" } = useParams();
  const isCreatingWorkflow = id === "create";
  const { formatMessage } = useIntl();
  const { _unstableFormatValidationErrors: formatValidationErrors } = useAPIErrorHandler();
  const navigate = useNavigate();
  const { toggleNotification } = useNotification();
  const { isLoading: isLoadingWorkflow, meta, workflows, error, update, create: create5 } = useReviewWorkflows();
  const permissions = useTypedSelector((state) => {
    var _a;
    return (_a = state.admin_app.permissions["settings"]) == null ? void 0 : _a["review-workflows"];
  });
  const { allowedActions: { canDelete, canUpdate, canCreate } } = useRBAC(permissions);
  const [savePrompts, setSavePrompts] = React4.useState({});
  const { getFeature, isLoading: isLicenseLoading } = useLicenseLimits();
  const [showLimitModal, setShowLimitModal] = React4.useState(null);
  const currentWorkflow = workflows == null ? void 0 : workflows.find((workflow) => workflow.id === parseInt(id, 10));
  const contentTypesFromOtherWorkflows = workflows == null ? void 0 : workflows.filter((workflow) => workflow.id !== parseInt(id, 10)).flatMap((workflow) => workflow.contentTypes);
  const limits = getFeature("review-workflows");
  const numberOfWorkflows = limits == null ? void 0 : limits[CHARGEBEE_WORKFLOW_ENTITLEMENT_NAME];
  const stagesPerWorkflow = limits == null ? void 0 : limits[CHARGEBEE_STAGES_PER_WORKFLOW_ENTITLEMENT_NAME];
  const submitForm = async (data, helpers) => {
    var _a;
    try {
      const { stageRequiredToPublish, ...rest } = data;
      const stageRequiredToPublishName = stageRequiredToPublish === "" ? null : (_a = rest.stages.find((stage) => stage.id === Number(stageRequiredToPublish) || stage.__temp_key__ === stageRequiredToPublish)) == null ? void 0 : _a.name;
      if (!isCreatingWorkflow) {
        const res = await update(id, {
          ...rest,
          // compare permissions of stages and only submit them if at least one has
          // changed; this enables partial updates e.g. for users who don't have
          // permissions to see roles
          stages: rest.stages.map((stage) => {
            var _a2;
            const serverStage = (_a2 = currentWorkflow == null ? void 0 : currentWorkflow.stages) == null ? void 0 : _a2.find((serverStage2) => serverStage2.id === (stage == null ? void 0 : stage.id));
            const hasUpdatedPermissions = !serverStage || havePermissionsChanged(serverStage.fromPermissions, stage.fromPermissions);
            const hasUpdatedToPermissions = !serverStage || havePermissionsChanged(serverStage.toPermissions, stage.toPermissions);
            return {
              ...stage,
              fromPermissions: hasUpdatedPermissions ? stage.fromPermissions : void 0,
              toPermissions: hasUpdatedToPermissions ? stage.toPermissions : void 0
            };
          }),
          stageRequiredToPublishName
        });
        if ("error" in res && isBaseQueryError(res.error) && res.error.name === "ValidationError") {
          helpers.setErrors(formatValidationErrors(res.error));
        }
      } else {
        const res = await create5({
          ...rest,
          stageRequiredToPublishName
        });
        if ("error" in res && isBaseQueryError(res.error) && res.error.name === "ValidationError") {
          helpers.setErrors(formatValidationErrors(res.error));
        } else if ("data" in res) {
          navigate(`../${res.data.id}`, {
            replace: true
          });
        }
      }
    } catch {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: "notification.error",
          defaultMessage: "An error occurred"
        })
      });
    }
    setSavePrompts({});
  };
  const handleConfirmDeleteDialog = (data, helpers) => async () => {
    await submitForm(data, helpers);
  };
  const handleConfirmClose = () => {
    setSavePrompts({});
  };
  const handleSubmit = async (data, helpers) => {
    const isContentTypeReassignment = data.contentTypes.some((contentType) => contentTypesFromOtherWorkflows == null ? void 0 : contentTypesFromOtherWorkflows.includes(contentType));
    const hasDeletedServerStages = !isCreatingWorkflow && !(currentWorkflow == null ? void 0 : currentWorkflow.stages.every((stage) => data.stages.some((newStage) => newStage.id === stage.id)));
    if (meta && numberOfWorkflows && (meta == null ? void 0 : meta.workflowCount) > parseInt(numberOfWorkflows, 10)) {
      setShowLimitModal("workflow");
    } else if (data.stages && stagesPerWorkflow && data.stages.length > parseInt(stagesPerWorkflow, 10)) {
      setShowLimitModal("stage");
    } else if (hasDeletedServerStages || isContentTypeReassignment) {
      if (hasDeletedServerStages) {
        setSavePrompts((prev) => ({
          ...prev,
          hasDeletedServerStages: true
        }));
      }
      if (isContentTypeReassignment) {
        setSavePrompts((prev) => ({
          ...prev,
          hasReassignedContentTypes: true
        }));
      }
    } else {
      await submitForm(data, helpers);
    }
  };
  React4.useEffect(() => {
    if (!isLoadingWorkflow && !isLicenseLoading) {
      if (meta && numberOfWorkflows && (meta == null ? void 0 : meta.workflowCount) > parseInt(numberOfWorkflows, 10)) {
        setShowLimitModal("workflow");
      } else if (currentWorkflow && currentWorkflow.stages && stagesPerWorkflow && currentWorkflow.stages.length > parseInt(stagesPerWorkflow, 10)) {
        setShowLimitModal("stage");
      }
    }
  }, [
    currentWorkflow,
    isLicenseLoading,
    isLoadingWorkflow,
    limits,
    meta,
    numberOfWorkflows,
    stagesPerWorkflow
  ]);
  const initialValues = React4.useMemo(() => {
    var _a;
    if (isCreatingWorkflow || !currentWorkflow) {
      return {
        name: "",
        stages: [],
        contentTypes: [],
        stageRequiredToPublish: ""
      };
    } else {
      return {
        name: currentWorkflow.name,
        stages: addTmpKeysToStages(currentWorkflow.stages),
        contentTypes: currentWorkflow.contentTypes,
        stageRequiredToPublish: ((_a = currentWorkflow.stageRequiredToPublish) == null ? void 0 : _a.id.toString()) ?? ""
      };
    }
  }, [
    currentWorkflow,
    isCreatingWorkflow
  ]);
  if (isLoadingWorkflow) {
    return (0, import_jsx_runtime4.jsx)(Page.Loading, {});
  }
  if (error) {
    return (0, import_jsx_runtime4.jsx)(Page.Error, {});
  }
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, {
    children: [
      (0, import_jsx_runtime4.jsx)(DragLayerRendered, {}),
      (0, import_jsx_runtime4.jsx)(Form, {
        method: isCreatingWorkflow ? "POST" : "PUT",
        initialValues,
        validationSchema: WORKFLOW_SCHEMA,
        onSubmit: handleSubmit,
        children: ({ modified, isSubmitting, values, setErrors }) => {
          var _a;
          return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, {
            children: [
              (0, import_jsx_runtime4.jsx)(Header, {
                navigationAction: (
                  // The back link for mobile works differently; it is placed higher up in the DOM.
                  (0, import_jsx_runtime4.jsx)(S, {
                    display: {
                      initial: "none",
                      medium: "block"
                    },
                    children: (0, import_jsx_runtime4.jsx)(BackButton, {
                      fallback: ".."
                    })
                  })
                ),
                primaryAction: canUpdate || canCreate ? (0, import_jsx_runtime4.jsx)(Ct, {
                  startIcon: (0, import_jsx_runtime4.jsx)(h5, {}),
                  type: "submit",
                  disabled: !modified || isSubmitting || values.stages.length === 0,
                  // if the confirm dialog is open the loading state is on
                  // the confirm button already
                  loading: Object.keys(savePrompts).length === 0 && isSubmitting,
                  fullWidth: true,
                  children: formatMessage({
                    id: "global.save",
                    defaultMessage: "Save"
                  })
                }) : null,
                subtitle: formatMessage({
                  id: "review-workflows.page.subtitle",
                  defaultMessage: "{count, plural, one {# stage} other {# stages}}"
                }, {
                  count: ((_a = currentWorkflow == null ? void 0 : currentWorkflow.stages) == null ? void 0 : _a.length) ?? 0
                }),
                title: (currentWorkflow == null ? void 0 : currentWorkflow.name) || formatMessage({
                  id: "Settings.review-workflows.create.page.title",
                  defaultMessage: "Create Review Workflow"
                })
              }),
              (0, import_jsx_runtime4.jsx)(Root, {
                children: (0, import_jsx_runtime4.jsxs)(C, {
                  alignItems: "stretch",
                  direction: "column",
                  gap: 7,
                  children: [
                    (0, import_jsx_runtime4.jsx)(WorkflowAttributes, {
                      canUpdate: canUpdate || canCreate
                    }),
                    (0, import_jsx_runtime4.jsx)(Stages, {
                      canDelete,
                      canUpdate: canUpdate || canCreate,
                      isCreating: isCreatingWorkflow
                    })
                  ]
                })
              }),
              (0, import_jsx_runtime4.jsx)(Pd.Root, {
                open: Object.keys(savePrompts).length > 0,
                onOpenChange: handleConfirmClose,
                children: (0, import_jsx_runtime4.jsx)(ConfirmDialog, {
                  onConfirm: handleConfirmDeleteDialog(values, {
                    setErrors
                  }),
                  children: (0, import_jsx_runtime4.jsxs)(C, {
                    direction: "column",
                    gap: 5,
                    children: [
                      savePrompts.hasDeletedServerStages && (0, import_jsx_runtime4.jsx)(A, {
                        textAlign: "center",
                        variant: "omega",
                        children: formatMessage({
                          id: "review-workflows.page.delete.confirm.stages.body",
                          defaultMessage: "All entries assigned to deleted stages will be moved to the previous stage."
                        })
                      }),
                      savePrompts.hasReassignedContentTypes && (0, import_jsx_runtime4.jsx)(A, {
                        textAlign: "center",
                        variant: "omega",
                        children: formatMessage({
                          id: "review-workflows.page.delete.confirm.contentType.body",
                          defaultMessage: "{count} {count, plural, one {content-type} other {content-types}} {count, plural, one {is} other {are}} already mapped to {count, plural, one {another workflow} other {other workflows}}. If you save changes, {count, plural, one {this} other {these}} {count, plural, one {content-type} other {{count} content-types}} will no more be mapped to the {count, plural, one {another workflow} other {other workflows}} and all corresponding information will be removed."
                        }, {
                          count: (contentTypesFromOtherWorkflows == null ? void 0 : contentTypesFromOtherWorkflows.filter((contentType) => values.contentTypes.includes(contentType)).length) ?? 0
                        })
                      }),
                      (0, import_jsx_runtime4.jsx)(A, {
                        textAlign: "center",
                        variant: "omega",
                        children: formatMessage({
                          id: "review-workflows.page.delete.confirm.confirm",
                          defaultMessage: "Are you sure you want to save?"
                        })
                      })
                    ]
                  })
                })
              })
            ]
          });
        }
      }),
      (0, import_jsx_runtime4.jsxs)(LimitsModal.Root, {
        open: showLimitModal === "workflow",
        onOpenChange: () => setShowLimitModal(null),
        children: [
          (0, import_jsx_runtime4.jsx)(LimitsModal.Title, {
            children: formatMessage({
              id: "review-workflows.workflows.limit.title",
              defaultMessage: "You’ve reached the limit of workflows in your plan"
            })
          }),
          (0, import_jsx_runtime4.jsx)(LimitsModal.Body, {
            children: formatMessage({
              id: "review-workflows.workflows.limit.body",
              defaultMessage: "Delete a workflow or contact Sales to enable more workflows."
            })
          })
        ]
      }),
      (0, import_jsx_runtime4.jsxs)(LimitsModal.Root, {
        open: showLimitModal === "stage",
        onOpenChange: () => setShowLimitModal(null),
        children: [
          (0, import_jsx_runtime4.jsx)(LimitsModal.Title, {
            children: formatMessage({
              id: "review-workflows.stages.limit.title",
              defaultMessage: "You have reached the limit of stages for this workflow in your plan"
            })
          }),
          (0, import_jsx_runtime4.jsx)(LimitsModal.Body, {
            children: formatMessage({
              id: "review-workflows.stages.limit.body",
              defaultMessage: "Try deleting some stages or contact Sales to enable more stages."
            })
          })
        ]
      })
    ]
  });
};
var addTmpKeysToStages = (data) => {
  const keys = generateNKeysBetween(void 0, void 0, data.length);
  return data.map((datum, index) => ({
    ...datum,
    __temp_key__: keys[index]
  }));
};
var ProtectedEditPage = () => {
  const permissions = useTypedSelector((state) => {
    var _a;
    const { create: create5 = [], update = [], read = [] } = ((_a = state.admin_app.permissions.settings) == null ? void 0 : _a["review-workflows"]) ?? {};
    return [
      ...create5,
      ...update,
      ...read
    ];
  });
  return (0, import_jsx_runtime4.jsx)(Page.Protect, {
    permissions,
    children: (0, import_jsx_runtime4.jsx)(EditPage, {})
  });
};
export {
  ProtectedEditPage
};
//# sourceMappingURL=id-MWQ6C6PV.js.map
