import { engine as engine$1 } from '@strapi/permissions';
import { providerFactory } from '@strapi/utils';

const DEFAULT_TRANSFER_ACTIONS = [
    'push',
    'pull'
];
const providers = {
    action: providerFactory(),
    condition: providerFactory()
};
DEFAULT_TRANSFER_ACTIONS.forEach((action)=>{
    providers.action.register(action, {
        action
    });
});
const engine = engine$1.new({
    providers
});

export { engine, providers };
//# sourceMappingURL=permission.mjs.map
