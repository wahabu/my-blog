'use strict';

var z = require('zod/v4');

function _interopNamespace(e) {
  if (e && e.__esModule) return e;
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      }
    });
  }
  n.default = e;
  return Object.freeze(n);
}

var z__namespace = /*#__PURE__*/_interopNamespace(z);

// widths must be one of 4, 6, 8, 12
const WidthSchema = z__namespace.union([
    z__namespace.literal(4),
    z__namespace.literal(6),
    z__namespace.literal(8),
    z__namespace.literal(12)
]);
const WidgetEntrySchema = z__namespace.object({
    uid: z__namespace.string().nonempty(),
    width: WidthSchema
}).strict();
const HomepageLayoutSchema = z__namespace.object({
    version: z__namespace.number().int().min(1),
    widgets: z__namespace.array(WidgetEntrySchema).max(100),
    updatedAt: z__namespace.string().datetime()
}).strict();
const HomepageLayoutWriteSchema = z__namespace.object({
    version: z__namespace.number().int().min(1).optional(),
    widgets: z__namespace.array(WidgetEntrySchema).max(100),
    updatedAt: z__namespace.string().datetime().optional()
}).strict();

exports.HomepageLayoutSchema = HomepageLayoutSchema;
exports.HomepageLayoutWriteSchema = HomepageLayoutWriteSchema;
exports.WidthSchema = WidthSchema;
//# sourceMappingURL=schema.js.map
