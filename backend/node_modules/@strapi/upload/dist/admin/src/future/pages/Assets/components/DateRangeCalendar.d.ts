interface DateRangeCalendarProps {
    /** `YYYY-MM-DD`, both set when editing an existing range badge. */
    from?: string;
    to?: string;
    onSelect: (from: string, to: string) => void;
}
export declare const DateRangeCalendar: ({ from, to, onSelect }: DateRangeCalendarProps) => import("react/jsx-runtime").JSX.Element;
export {};
