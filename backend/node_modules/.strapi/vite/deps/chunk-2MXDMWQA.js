import {
  QueryClient,
  QueryClientProvider,
  _enum,
  boolean,
  number,
  object,
  require_defaultsDeep,
  require_logic,
  require_throttle,
  require_uniq,
  string,
  union
} from "./chunk-FH7EDXAD.js";
import {
  ADMIN_PERMISSIONS_CE,
  HOOKS
} from "./chunk-GWSYPQNJ.js";
import {
  useNpsSurveySettings
} from "./chunk-TYJZFMZM.js";
import {
  require_pick
} from "./chunk-TVYQEFKB.js";
import {
  getBasename
} from "./chunk-JWAF5VI7.js";
import {
  Login,
  getRedirectTo
} from "./chunk-JDLC4XQ5.js";
import {
  Column,
  LayoutContent,
  Logo,
  UnauthenticatedLayout
} from "./chunk-WG7HPCU2.js";
import {
  ConfigurationProvider
} from "./chunk-DQ7D5XGA.js";
import {
  useClipboard
} from "./chunk-KBYESCMF.js";
import {
  GuidedTourContext,
  useAIAvailability
} from "./chunk-7TPDZIMA.js";
import {
  useGetAiUsageQuery
} from "./chunk-SKF3XHIS.js";
import {
  useEnterprise
} from "./chunk-LCICF5EM.js";
import {
  require_merge,
  require_omit
} from "./chunk-BC4MOUEJ.js";
import {
  HistoryProvider
} from "./chunk-CZYDCJAN.js";
import {
  ADMIN_PERMISSIONS_EE,
  AUDIT_LOGS_DEFAULT_PAGE_SIZE,
  getEERoutes
} from "./chunk-35QOLVEV.js";
import {
  errorsTrads
} from "./chunk-ZRUQAY55.js";
import {
  ValidationError,
  create,
  create4 as create2,
  create6 as create3
} from "./chunk-OG6FDUVQ.js";
import {
  MemoizedInputRenderer
} from "./chunk-DCSZDLCR.js";
import {
  Form
} from "./chunk-2POPB6BT.js";
import {
  Layouts
} from "./chunk-YWXAEVTA.js";
import {
  getByteSize
} from "./chunk-D5MNZJMA.js";
import {
  RESPONSIVE_DEFAULT_SPACING,
  TrackingProvider,
  useTracking
} from "./chunk-Y55YQYLT.js";
import {
  createAbsoluteUrl,
  useInitQuery
} from "./chunk-X3A7ESLP.js";
import {
  require_isEqual
} from "./chunk-KVWVHPCR.js";
import {
  Page,
  useAPIErrorHandler
} from "./chunk-UFJCBFTE.js";
import {
  l1
} from "./chunk-Y5S667H6.js";
import {
  AuthProvider,
  StrapiAppProvider,
  getOrCreateDeviceId,
  normalizeAdminLocale,
  useAuth,
  useForgotPasswordMutation,
  useGetRegistrationInfoQuery,
  useRegisterAdminMutation,
  useRegisterUserMutation,
  useResetPasswordMutation,
  useTypedDispatch,
  useTypedSelector,
  useTypedStore
} from "./chunk-UKH6H4FJ.js";
import {
  NotificationsProvider,
  useNotification
} from "./chunk-PQFMPWTQ.js";
import {
  LANGUAGE_LOCAL_STORAGE_KEY,
  THEME_LOCAL_STORAGE_KEY,
  adminApi,
  getFetchClient,
  getStoredToken,
  isBaseQueryError,
  login,
  logout,
  reducer,
  setAvailableThemes
} from "./chunk-NKVVIWNK.js";
import {
  require_isFunction
} from "./chunk-NR6XEPE4.js";
import {
  provider_default,
  useIntl
} from "./chunk-PQFM6YJZ.js";
import {
  require_browser
} from "./chunk-HAUWLGB7.js";
import {
  require_client
} from "./chunk-3MST3H25.js";
import {
  Link,
  NavLink,
  Navigate,
  Outlet,
  RouterProvider,
  createBrowserRouter,
  createMemoryRouter,
  useLocation,
  useMatch,
  useNavigate,
  useRouteError
} from "./chunk-QBM2MGBP.js";
import {
  Provider_default,
  useDispatch
} from "./chunk-AHJOALQ2.js";
import {
  combineReducers,
  configureStore,
  isRejected
} from "./chunk-QQEUXDAQ.js";
import {
  L
} from "./chunk-LHXIEHKN.js";
import {
  A,
  Bd,
  C,
  Ct,
  Hd,
  Ld,
  Oa,
  Rd,
  S,
  Vd,
  Wd,
  nn,
  ud
} from "./chunk-7ZODKSNG.js";
import {
  Gn,
  I2,
  Jn,
  Nn,
  l5,
  r5,
  y5
} from "./chunk-D4B4CG5U.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  St,
  gt
} from "./chunk-4N2AQQ2K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/render.mjs
var import_client = __toESM(require_client(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/StrapiApp.mjs
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
var import_react6 = __toESM(require_react(), 1);
var import_invariant4 = __toESM(require_browser(), 1);
var import_isFunction = __toESM(require_isFunction(), 1);
var import_merge = __toESM(require_merge(), 1);
var import_pick = __toESM(require_pick(), 1);
var import_uniq = __toESM(require_uniq(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/assets/images/logo-strapi-2022.svg.mjs
var img = "data:image/svg+xml,%3csvg width='800' height='800' viewBox='0 0 800 800' fill='none' xmlns='http://www.w3.org/2000/svg'%3e %3cpath d='M39 282c0-118 0-176.9 36.6-213.5C112.2 32 171.1 32 288.9 32h221.2c117.8 0 176.7 0 213.3 36.6C760 105.2 760 164.1 760 281.9v221.2c0 117.8 0 176.7-36.6 213.3C686.8 753 627.9 753 510.1 753H288.9c-117.8 0-176.7 0-213.3-36.6C39 679.8 39 620.9 39 503.1V281.9Z' fill='%234945FF'/%3e %3cpath fill-rule='evenodd' clip-rule='evenodd' d='M536.4 250.7H293.7v123.8h123.8v123.7h123.8V255.5c0-2.6-2.2-4.8-4.9-4.8Z' fill='white'/%3e %3cpath fill='white' d='M412.7 374.5h4.8v4.8h-4.8z'/%3e %3cpath d='M293.8 374.5h119c2.6 0 4.8 2.1 4.8 4.8v119h-119a4.8 4.8 0 0 1-4.8-4.9v-119Z' fill='%239593FF'/%3e %3cpath d='M417.5 498.2h123.8L421.6 618a2.4 2.4 0 0 1-4-1.7v-118ZM293.8 374.5h-118a2.4 2.4 0 0 1-1.7-4.1l119.7-119.7v123.8Z' fill='%239593FF'/%3e%3c/svg%3e";

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/core/apis/CustomFields.mjs
var import_invariant = __toESM(require_browser(), 1);
var ALLOWED_TYPES = [
  "biginteger",
  "boolean",
  "date",
  "datetime",
  "decimal",
  "email",
  "enumeration",
  "float",
  "integer",
  "json",
  "password",
  "richtext",
  "string",
  "text",
  "time",
  "uid"
];
var ALLOWED_ROOT_LEVEL_OPTIONS = [
  "min",
  "minLength",
  "max",
  "maxLength",
  "required",
  "regex",
  "enum",
  "unique",
  "private",
  "default"
];
var CustomFields = class {
  constructor() {
    this.register = (customFields) => {
      if (Array.isArray(customFields)) {
        customFields.forEach((customField) => {
          this.register(customField);
        });
      } else {
        const { name, pluginId, type, intlLabel, intlDescription, components, options } = customFields;
        (0, import_invariant.default)(name, "A name must be provided");
        (0, import_invariant.default)(type, "A type must be provided");
        (0, import_invariant.default)(intlLabel, "An intlLabel must be provided");
        (0, import_invariant.default)(intlDescription, "An intlDescription must be provided");
        (0, import_invariant.default)(components, "A components object must be provided");
        (0, import_invariant.default)(components.Input, "An Input component must be provided");
        (0, import_invariant.default)(ALLOWED_TYPES.includes(type), `Custom field type: '${type}' is not a valid Strapi type or it can't be used with a Custom Field`);
        const isValidObjectKey = /^(?![0-9])[a-zA-Z0-9$_-]+$/g;
        (0, import_invariant.default)(isValidObjectKey.test(name), `Custom field name: '${name}' is not a valid object key`);
        const allFormOptions = [
          ...(options == null ? void 0 : options.base) || [],
          ...(options == null ? void 0 : options.advanced) || []
        ];
        if (allFormOptions.length) {
          const optionPathValidations = allFormOptions.reduce(optionsValidationReducer, []);
          optionPathValidations.forEach(({ isValidOptionPath, errorMessage }) => {
            (0, import_invariant.default)(isValidOptionPath, errorMessage);
          });
        }
        const uid = pluginId ? `plugin::${pluginId}.${name}` : `global::${name}`;
        const uidAlreadyUsed = Object.prototype.hasOwnProperty.call(this.customFields, uid);
        (0, import_invariant.default)(!uidAlreadyUsed, `Custom field: '${uid}' has already been registered`);
        this.customFields[uid] = customFields;
      }
    };
    this.getAll = () => {
      return this.customFields;
    };
    this.get = (uid) => {
      return this.customFields[uid];
    };
    this.customFields = {};
  }
};
var optionsValidationReducer = (acc, option) => {
  if ("items" in option) {
    return option.items.reduce(optionsValidationReducer, acc);
  }
  if (!option.name) {
    acc.push({
      isValidOptionPath: false,
      errorMessage: "The 'name' property is required on an options object"
    });
  } else {
    acc.push({
      isValidOptionPath: option.name.startsWith("options") || ALLOWED_ROOT_LEVEL_OPTIONS.includes(option.name),
      errorMessage: `'${option.name}' must be prefixed with 'options.'`
    });
  }
  return acc;
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/core/apis/Plugin.mjs
var Plugin = class {
  getInjectedComponents(containerName, blockName) {
    try {
      return this.injectionZones[containerName][blockName] || [];
    } catch (err) {
      console.error("Cannot get injected component", err);
      return [];
    }
  }
  injectComponent(containerName, blockName, component) {
    try {
      this.injectionZones[containerName][blockName].push(component);
    } catch (err) {
      console.error("Cannot inject component", err);
    }
  }
  constructor(pluginConf) {
    this[L] = true;
    this.apis = pluginConf.apis || {};
    this.initializer = pluginConf.initializer || null;
    this.injectionZones = pluginConf.injectionZones || {};
    this.isReady = pluginConf.isReady !== void 0 ? pluginConf.isReady : true;
    this.name = pluginConf.name;
    this.pluginId = pluginConf.id;
  }
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/core/apis/rbac.mjs
var RBAC = class {
  use(middleware) {
    if (Array.isArray(middleware)) {
      this.middlewares.push(...middleware);
    } else {
      this.middlewares.push(middleware);
    }
  }
  constructor() {
    this.middlewares = [];
    this.run = async (ctx, permissions) => {
      let index = 0;
      const middlewaresToRun = this.middlewares.map((middleware) => middleware(ctx));
      const next = async (permissions2) => {
        if (index < this.middlewares.length) {
          return middlewaresToRun[index++](next)(permissions2);
        }
        return permissions2;
      };
      return next(permissions);
    };
  }
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/core/apis/router.mjs
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
var import_react5 = __toESM(require_react(), 1);
var import_invariant2 = __toESM(require_browser(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/App.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var import_react4 = __toESM(require_react(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/ee/admin/src/hooks/useAIUsageWarning.mjs
var import_react = __toESM(require_react(), 1);
function useAIUsageWarning(threshold = 0.8) {
  const location = useLocation();
  const isAuthPage = location.pathname.startsWith("/auth");
  const { toggleNotification } = useNotification();
  const isAIEnabled = useAIAvailability();
  const { data, isLoading, error } = useGetAiUsageQuery(void 0, {
    refetchOnMountOrArgChange: true,
    skip: !isAIEnabled || isAuthPage
  });
  const warningLevelsRef = (0, import_react.useRef)(/* @__PURE__ */ new Set());
  (0, import_react.useEffect)(() => {
    var _a;
    if (isAuthPage || isLoading || error || !((_a = data == null ? void 0 : data.subscription) == null ? void 0 : _a.cmsAiEnabled) || !isAIEnabled) return;
    const totalCredits = data.subscription.cmsAiCreditsBase;
    const usedCredits = data.cmsAiCreditsUsed;
    const maxCredits = data.subscription.cmsAiCreditsMaxUsage;
    if (!totalCredits || totalCredits <= 0) return;
    const percentUsed = usedCredits / totalCredits;
    const percentDisplay = Math.round(percentUsed * 100);
    const remaining = Math.max(totalCredits - usedCredits, 0);
    if (percentUsed >= 1 && !warningLevelsRef.current.has(100)) {
      const hasOverageAllowance = maxCredits && maxCredits > totalCredits;
      if (hasOverageAllowance) {
        toggleNotification({
          type: "danger",
          message: `You've used 100% of your AI credits. Overages are being applied.`,
          timeout: 5e3
        });
      } else {
        toggleNotification({
          type: "danger",
          message: `You've exhausted your AI credits. No additional credits available.`,
          timeout: 5e3
        });
      }
      warningLevelsRef.current.add(100);
    } else if (percentUsed >= 0.9 && percentUsed < 1 && !warningLevelsRef.current.has(90)) {
      toggleNotification({
        type: "warning",
        message: `You've used ${percentDisplay}% of your AI credits. ${remaining} remain.`,
        timeout: 5e3
      });
      warningLevelsRef.current.add(90);
    } else if (percentUsed >= threshold && percentUsed < 0.9 && !warningLevelsRef.current.has(Math.round(threshold * 100))) {
      toggleNotification({
        type: "warning",
        message: `You've used ${percentDisplay}% of your AI credits. ${remaining} remain.`,
        timeout: 5e3
      });
      warningLevelsRef.current.add(Math.round(threshold * 100));
    }
    if (percentUsed < 0.7) {
      warningLevelsRef.current.clear();
    }
  }, [
    data,
    isLoading,
    error,
    threshold,
    toggleNotification,
    isAIEnabled,
    isAuthPage
  ]);
}

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/ee/admin/src/components/GlobalNotifications.mjs
var GlobalNotifications = () => {
  useAIUsageWarning();
  return null;
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/Providers.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_react3 = __toESM(require_react(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/GuidedTour/GuidedTourProvider.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var GuidedTourProvider = ({ children }) => {
  const isGuidedTourEnabled = true;
  return (0, import_jsx_runtime.jsx)(GuidedTourContext, {
    enabled: isGuidedTourEnabled,
    children
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/LanguageProvider.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_defaultsDeep = __toESM(require_defaultsDeep(), 1);
var LanguageProvider = ({ children, messages }) => {
  const locale = useTypedSelector((state) => state.admin_app.language.locale);
  const appMessages = (0, import_defaultsDeep.default)(messages[locale], messages.en);
  return (0, import_jsx_runtime2.jsx)(provider_default, {
    locale,
    defaultLocale: "en",
    messages: appMessages,
    textComponent: "span",
    children
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/Theme.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);
var Theme = ({ children, themes }) => {
  const { currentTheme } = useTypedSelector((state) => state.admin_app.theme);
  const [systemTheme, setSystemTheme] = React.useState();
  const { locale } = useIntl();
  const dispatch = useDispatch();
  const isIos = [
    "iPad Simulator",
    "iPhone Simulator",
    "iPod Simulator",
    "iPad",
    "iPhone",
    "iPod"
  ].includes(navigator.platform) || navigator.userAgent.includes("Mac") && "ontouchend" in document;
  React.useEffect(() => {
    const themeWatcher = window.matchMedia("(prefers-color-scheme: dark)");
    setSystemTheme(themeWatcher.matches ? "dark" : "light");
    const listener = (event) => {
      setSystemTheme(event.matches ? "dark" : "light");
    };
    themeWatcher.addEventListener("change", listener);
    return () => {
      themeWatcher.removeEventListener("change", listener);
    };
  }, []);
  React.useEffect(() => {
    dispatch(setAvailableThemes(Object.keys(themes)));
  }, [
    dispatch,
    themes
  ]);
  const computedThemeName = currentTheme === "system" ? systemTheme : currentTheme;
  return (0, import_jsx_runtime3.jsxs)(Bd, {
    locale,
    /**
    * TODO: could we make this neater i.e. by setting up the context to throw
    * if it can't find it, that way the type is always fully defined and we're
    * not checking it all the time...
    */
    theme: themes == null ? void 0 : themes[computedThemeName || "light"],
    children: [
      children,
      (0, import_jsx_runtime3.jsx)(GlobalStyle, {
        $shouldOverrideInputFontSize: isIos
      })
    ]
  });
};
var GlobalStyle = St`
  body {
    background: ${({ theme }) => theme.colors.neutral100};
  }

  // Temporary fix override to fix iOS zoom due to the 14px input font size
  ${({ $shouldOverrideInputFontSize }) => $shouldOverrideInputFontSize ? `
    input[type="color"],
    input[type="date"],
    input[type="datetime"],
    input[type="datetime-local"],
    input[type="email"],
    input[type="month"],
    input[type="number"],
    input[type="password"],
    input[type="search"],
    input[type="tel"],
    input[type="text"],
    input[type="time"],
    input[type="url"],
    input[type="week"],
    select:focus,
    textarea {
      font-size: 16px !important;
      line-height: 2.4rem !important;
    }
  ` : void 0}
`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/Providers.mjs
var queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      refetchOnWindowFocus: false
    }
  }
});
var Providers = ({ children, strapi, store }) => {
  return (0, import_jsx_runtime4.jsx)(StrapiAppProvider, {
    components: strapi.library.components,
    customFields: strapi.customFields,
    widgets: strapi.widgets,
    fields: strapi.library.fields,
    menu: strapi.router.menu,
    getAdminInjectedComponents: strapi.getAdminInjectedComponents,
    getPlugin: strapi.getPlugin,
    plugins: strapi.plugins,
    rbac: strapi.rbac,
    runHookParallel: strapi.runHookParallel,
    runHookWaterfall: (name, initialValue) => strapi.runHookWaterfall(name, initialValue, store),
    runHookSeries: strapi.runHookSeries,
    settings: strapi.router.settings,
    children: (0, import_jsx_runtime4.jsx)(Provider_default, {
      store,
      children: (0, import_jsx_runtime4.jsx)(QueryClientProvider, {
        client: queryClient,
        children: (0, import_jsx_runtime4.jsx)(AuthProvider, {
          children: (0, import_jsx_runtime4.jsx)(HistoryProvider, {
            children: (0, import_jsx_runtime4.jsx)(LanguageProvider, {
              messages: strapi.configurations.translations,
              children: (0, import_jsx_runtime4.jsx)(Theme, {
                themes: strapi.configurations.themes,
                children: (0, import_jsx_runtime4.jsx)(NotificationsProvider, {
                  children: (0, import_jsx_runtime4.jsx)(TrackingProvider, {
                    children: (0, import_jsx_runtime4.jsx)(GuidedTourProvider, {
                      children: (0, import_jsx_runtime4.jsx)(ConfigurationProvider, {
                        defaultAuthLogo: strapi.configurations.authLogo,
                        defaultMenuLogo: strapi.configurations.menuLogo,
                        showReleaseNotification: strapi.configurations.notifications.releases,
                        children
                      })
                    })
                  })
                })
              })
            })
          })
        })
      })
    })
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/App.mjs
var App = ({ strapi, store }) => {
  (0, import_react4.useEffect)(() => {
    const language = localStorage.getItem(LANGUAGE_LOCAL_STORAGE_KEY) || "en";
    {
      document.documentElement.lang = language;
    }
  }, []);
  const globalComponents = Object.entries(strapi.library.components).filter(([name]) => name.startsWith("future-global::")).map(([name, Component]) => ({
    name,
    Component
  }));
  return (0, import_jsx_runtime5.jsx)(Providers, {
    strapi,
    store,
    children: (0, import_jsx_runtime5.jsxs)(import_react4.Suspense, {
      fallback: (0, import_jsx_runtime5.jsx)(Page.Loading, {}),
      children: [
        (0, import_jsx_runtime5.jsx)(GlobalNotifications, {}),
        window.strapi.future.isEnabled("betaMediaLibrary") && globalComponents.map(({ name, Component }) => (0, import_jsx_runtime5.jsx)(Component, {}, name)),
        (0, import_jsx_runtime5.jsx)(Outlet, {})
      ]
    })
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/ErrorElement.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/utils/retryDynamicImport.mjs
var CHUNK_LOAD_ERROR_RE = /Failed to fetch dynamically imported module|Importing a module script failed|error loading dynamically imported module|Loading chunk \d+ failed|ChunkLoadError/i;
function isChunkLoadError(error) {
  if (error == null) {
    return false;
  }
  if (typeof error === "string") {
    return CHUNK_LOAD_ERROR_RE.test(error);
  }
  if (error instanceof Error) {
    if (error.name === "ChunkLoadError") {
      return true;
    }
    return CHUNK_LOAD_ERROR_RE.test(error.message);
  }
  if (typeof error === "object" && "message" in error && typeof error.message === "string") {
    return CHUNK_LOAD_ERROR_RE.test(error.message);
  }
  return false;
}
function sleep(ms) {
  return new Promise((resolve) => {
    setTimeout(resolve, ms);
  });
}
var DEFAULT_OPTIONS = {
  maxAttempts: 6,
  initialDelayMs: 400,
  maxDelayMs: 4500
};
async function retryDynamicImport(importer, options = {}) {
  const { maxAttempts, initialDelayMs, maxDelayMs } = {
    ...DEFAULT_OPTIONS,
    ...options
  };
  let lastError;
  for (let attempt = 0; attempt < maxAttempts; attempt++) {
    try {
      return await importer();
    } catch (error) {
      lastError = error;
      const isLastAttempt = attempt === maxAttempts - 1;
      if (isLastAttempt || !isChunkLoadError(error)) {
        throw error;
      }
      const exponential = Math.min(maxDelayMs, initialDelayMs * 2 ** attempt);
      const jitter = Math.random() * 200;
      await sleep(exponential + jitter);
    }
  }
  throw lastError;
}
function wrapRouteObjectLazyWithRetry(route) {
  const next = {
    ...route
  };
  if (route.lazy) {
    const originalLazy = route.lazy;
    next.lazy = () => retryDynamicImport(() => originalLazy());
  }
  if (route.children) {
    next.children = route.children.map(wrapRouteObjectLazyWithRetry);
  }
  return next;
}
function wrapRouteObjectsLazyWithRetry(routes) {
  return routes.map(wrapRouteObjectLazyWithRetry);
}

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/ErrorElement.mjs
var ErrorElement = () => {
  const error = useRouteError();
  const { formatMessage } = useIntl();
  const { copy } = useClipboard();
  if (error instanceof Error) {
    console.error(error);
    const handleCopy = async () => {
      await copy(`
\`\`\`
${error.stack}
\`\`\`
      `);
    };
    if (isChunkLoadError(error)) {
      return (0, import_jsx_runtime6.jsx)(Vd, {
        height: "100%",
        children: (0, import_jsx_runtime6.jsx)(C, {
          alignItems: "center",
          height: "100%",
          justifyContent: "center",
          paddingLeft: RESPONSIVE_DEFAULT_SPACING,
          paddingRight: RESPONSIVE_DEFAULT_SPACING,
          children: (0, import_jsx_runtime6.jsxs)(C, {
            gap: 7,
            padding: {
              initial: 6,
              small: 7,
              medium: 8
            },
            direction: "column",
            width: "100%",
            maxWidth: "512px",
            shadow: "tableShadow",
            borderColor: "neutral150",
            background: "neutral0",
            hasRadius: true,
            children: [
              (0, import_jsx_runtime6.jsxs)(C, {
                direction: "column",
                gap: 2,
                children: [
                  (0, import_jsx_runtime6.jsx)(l5, {
                    width: "32px",
                    height: "32px",
                    fill: "primary600"
                  }),
                  (0, import_jsx_runtime6.jsx)(A, {
                    fontSize: 4,
                    fontWeight: "bold",
                    textAlign: "center",
                    children: formatMessage({
                      id: "app.error.chunk.title",
                      defaultMessage: "This screen couldn't be loaded"
                    })
                  }),
                  (0, import_jsx_runtime6.jsx)(A, {
                    variant: "omega",
                    textAlign: "center",
                    children: formatMessage({
                      id: "app.error.chunk.message",
                      defaultMessage: "The admin could not load a screen. This often happens when the server is starting or the connection was interrupted. Reload the page to try again. If you recently deployed, you may need a full refresh to load the latest version."
                    })
                  })
                ]
              }),
              (0, import_jsx_runtime6.jsxs)(C, {
                gap: 4,
                direction: "column",
                width: "100%",
                children: [
                  (0, import_jsx_runtime6.jsx)(Ct, {
                    onClick: () => window.location.reload(),
                    fullWidth: true,
                    children: formatMessage({
                      id: "app.error.chunk.reload",
                      defaultMessage: "Reload the page"
                    })
                  }),
                  (0, import_jsx_runtime6.jsx)(StyledAlert, {
                    onClose: () => {
                    },
                    width: "100%",
                    closeLabel: "",
                    variant: "warning",
                    children: (0, import_jsx_runtime6.jsx)(ChunkErrorType, {
                      children: error.message
                    })
                  }),
                  (0, import_jsx_runtime6.jsx)(Ct, {
                    onClick: handleCopy,
                    variant: "tertiary",
                    startIcon: (0, import_jsx_runtime6.jsx)(y5, {}),
                    children: formatMessage({
                      id: "app.error.copy",
                      defaultMessage: "Copy to clipboard"
                    })
                  })
                ]
              })
            ]
          })
        })
      });
    }
    return (0, import_jsx_runtime6.jsx)(Vd, {
      height: "100%",
      children: (0, import_jsx_runtime6.jsx)(C, {
        alignItems: "center",
        height: "100%",
        justifyContent: "center",
        paddingLeft: RESPONSIVE_DEFAULT_SPACING,
        paddingRight: RESPONSIVE_DEFAULT_SPACING,
        children: (0, import_jsx_runtime6.jsxs)(C, {
          gap: 7,
          padding: {
            initial: 6,
            small: 7,
            medium: 8
          },
          direction: "column",
          width: "100%",
          maxWidth: "512px",
          shadow: "tableShadow",
          borderColor: "neutral150",
          background: "neutral0",
          hasRadius: true,
          children: [
            (0, import_jsx_runtime6.jsxs)(C, {
              direction: "column",
              gap: 2,
              children: [
                (0, import_jsx_runtime6.jsx)(Jn, {
                  width: "32px",
                  height: "32px",
                  fill: "danger600"
                }),
                (0, import_jsx_runtime6.jsx)(A, {
                  fontSize: 4,
                  fontWeight: "bold",
                  textAlign: "center",
                  children: formatMessage({
                    id: "app.error",
                    defaultMessage: "Something went wrong"
                  })
                }),
                (0, import_jsx_runtime6.jsx)(A, {
                  variant: "omega",
                  textAlign: "center",
                  children: formatMessage({
                    id: "app.error.message",
                    defaultMessage: `It seems like there is a bug in your instance, but we've got you covered. Please notify your technical team so they can investigate the source of the problem and report the issue to us by opening a bug report on {link}.`
                  }, {
                    link: (0, import_jsx_runtime6.jsx)(Oa, {
                      isExternal: true,
                      // hack to get rid of the current endIcon, which should be removable by using `null`.
                      endIcon: true,
                      href: "https://github.com/strapi/strapi/issues/new?assignees=&labels=&projects=&template=BUG_REPORT.md",
                      children: `Strapi's GitHub`
                    })
                  })
                })
              ]
            }),
            (0, import_jsx_runtime6.jsxs)(C, {
              gap: 4,
              direction: "column",
              width: "100%",
              children: [
                (0, import_jsx_runtime6.jsx)(StyledAlert, {
                  onClose: () => {
                  },
                  width: "100%",
                  closeLabel: "",
                  variant: "danger",
                  children: (0, import_jsx_runtime6.jsx)(ErrorType, {
                    children: error.message
                  })
                }),
                (0, import_jsx_runtime6.jsx)(Ct, {
                  onClick: handleCopy,
                  variant: "tertiary",
                  startIcon: (0, import_jsx_runtime6.jsx)(y5, {}),
                  children: formatMessage({
                    id: "app.error.copy",
                    defaultMessage: "Copy to clipboard"
                  })
                })
              ]
            })
          ]
        })
      })
    });
  }
  throw error;
};
var StyledAlert = gt(ud)`
  & > div:first-child {
    display: none;
  }

  & > button {
    display: none;
  }
`;
var ErrorType = gt(A)`
  word-break: break-all;
  color: ${({ theme }) => theme.colors.danger600};
`;
var ChunkErrorType = gt(A)`
  word-break: break-all;
  color: ${({ theme }) => theme.colors.warning600};
`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/NotFoundPage.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var NotFoundPage = () => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime7.jsxs)(Page.Main, {
    labelledBy: "title",
    children: [
      (0, import_jsx_runtime7.jsx)(Layouts.Header, {
        id: "title",
        title: formatMessage({
          id: "content-manager.pageNotFound",
          defaultMessage: "Page not found"
        })
      }),
      (0, import_jsx_runtime7.jsx)(Layouts.Content, {
        children: (0, import_jsx_runtime7.jsx)(Wd, {
          action: (0, import_jsx_runtime7.jsx)(Hd, {
            tag: Link,
            variant: "secondary",
            endIcon: (0, import_jsx_runtime7.jsx)(I2, {}),
            to: "/",
            children: formatMessage({
              id: "app.components.NotFoundPage.back",
              defaultMessage: "Back to homepage"
            })
          }),
          content: formatMessage({
            id: "app.page.not.found",
            defaultMessage: "Oops! We can't seem to find the page you're looging for..."
          }),
          hasRadius: true,
          icon: (0, import_jsx_runtime7.jsx)(l1, {
            width: "16rem"
          }),
          shadow: "tableShadow"
        })
      })
    ]
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/router.mjs
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/ee/admin/src/pages/SettingsPage/constants.mjs
var getEERoutes2 = () => [
  ...window.strapi.features.isEnabled(window.strapi.features.AUDIT_LOGS) ? [
    {
      path: "audit-logs",
      lazy: async () => {
        const { ProtectedListPage } = await import("./ListPage-7E76NV6U.js");
        return {
          Component: ProtectedListPage
        };
      }
    }
  ] : [],
  ...window.strapi.features.isEnabled(window.strapi.features.SSO) ? [
    {
      path: "single-sign-on",
      lazy: async () => {
        const { ProtectedSSO } = await import("./SingleSignOnPage-UTJCXI6F.js");
        return {
          Component: ProtectedSSO
        };
      }
    }
  ] : []
];

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Auth/AuthPage.mjs
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Auth/components/ForgotPassword.mjs
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var ForgotPassword = () => {
  const navigate = useNavigate();
  const { formatMessage } = useIntl();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const [forgotPassword, { error }] = useForgotPasswordMutation();
  return (0, import_jsx_runtime8.jsx)(UnauthenticatedLayout, {
    children: (0, import_jsx_runtime8.jsxs)(Vd, {
      children: [
        (0, import_jsx_runtime8.jsxs)(LayoutContent, {
          children: [
            (0, import_jsx_runtime8.jsxs)(Column, {
              children: [
                (0, import_jsx_runtime8.jsx)(Logo, {}),
                (0, import_jsx_runtime8.jsx)(S, {
                  paddingTop: 6,
                  paddingBottom: 7,
                  children: (0, import_jsx_runtime8.jsx)(A, {
                    tag: "h1",
                    variant: "alpha",
                    children: formatMessage({
                      id: "Auth.form.button.password-recovery",
                      defaultMessage: "Password Recovery"
                    })
                  })
                }),
                error ? (0, import_jsx_runtime8.jsx)(A, {
                  id: "global-form-error",
                  role: "alert",
                  tabIndex: -1,
                  textColor: "danger600",
                  children: isBaseQueryError(error) ? formatAPIError(error) : formatMessage({
                    id: "notification.error",
                    defaultMessage: "An error occurred"
                  })
                }) : null
              ]
            }),
            (0, import_jsx_runtime8.jsx)(Form, {
              method: "POST",
              initialValues: {
                email: ""
              },
              onSubmit: async (body) => {
                const res = await forgotPassword(body);
                if (!("error" in res)) {
                  navigate("/auth/forgot-password-success");
                }
              },
              validationSchema: create3().shape({
                email: create2().email(errorsTrads.email).required({
                  id: errorsTrads.required.id,
                  defaultMessage: "This field is required."
                }).nullable()
              }),
              children: (0, import_jsx_runtime8.jsxs)(C, {
                direction: "column",
                alignItems: "stretch",
                gap: 6,
                children: [
                  [
                    {
                      label: formatMessage({
                        id: "Auth.form.email.label",
                        defaultMessage: "Email"
                      }),
                      name: "email",
                      placeholder: formatMessage({
                        id: "Auth.form.email.placeholder",
                        defaultMessage: "kai@doe.com"
                      }),
                      required: true,
                      type: "string"
                    }
                  ].map((field) => (0, import_jsx_runtime8.jsx)(MemoizedInputRenderer, {
                    ...field
                  }, field.name)),
                  (0, import_jsx_runtime8.jsx)(Ct, {
                    type: "submit",
                    fullWidth: true,
                    children: formatMessage({
                      id: "Auth.form.button.forgot-password",
                      defaultMessage: "Send Email"
                    })
                  })
                ]
              })
            })
          ]
        }),
        (0, import_jsx_runtime8.jsx)(C, {
          justifyContent: "center",
          children: (0, import_jsx_runtime8.jsx)(S, {
            paddingTop: 4,
            children: (0, import_jsx_runtime8.jsx)(Oa, {
              tag: NavLink,
              to: "/auth/login",
              children: formatMessage({
                id: "Auth.link.ready",
                defaultMessage: "Ready to sign in?"
              })
            })
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Auth/components/ForgotPasswordSuccess.mjs
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var ForgotPasswordSuccess = () => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime9.jsx)(UnauthenticatedLayout, {
    children: (0, import_jsx_runtime9.jsxs)(Vd, {
      children: [
        (0, import_jsx_runtime9.jsx)(LayoutContent, {
          children: (0, import_jsx_runtime9.jsxs)(Column, {
            children: [
              (0, import_jsx_runtime9.jsx)(Logo, {}),
              (0, import_jsx_runtime9.jsx)(S, {
                paddingTop: 6,
                paddingBottom: 7,
                children: (0, import_jsx_runtime9.jsx)(A, {
                  tag: "h1",
                  variant: "alpha",
                  children: formatMessage({
                    id: "app.containers.AuthPage.ForgotPasswordSuccess.title",
                    defaultMessage: "Email sent"
                  })
                })
              }),
              (0, import_jsx_runtime9.jsx)(A, {
                children: formatMessage({
                  id: "app.containers.AuthPage.ForgotPasswordSuccess.text.email",
                  defaultMessage: "It can take a few minutes to receive your password recovery link."
                })
              }),
              (0, import_jsx_runtime9.jsx)(S, {
                paddingTop: 4,
                children: (0, import_jsx_runtime9.jsx)(A, {
                  children: formatMessage({
                    id: "app.containers.AuthPage.ForgotPasswordSuccess.text.contact-admin",
                    defaultMessage: "If you do not receive this link, please contact your administrator."
                  })
                })
              })
            ]
          })
        }),
        (0, import_jsx_runtime9.jsx)(C, {
          justifyContent: "center",
          children: (0, import_jsx_runtime9.jsx)(S, {
            paddingTop: 4,
            children: (0, import_jsx_runtime9.jsx)(Oa, {
              tag: NavLink,
              to: "/auth/login",
              children: formatMessage({
                id: "Auth.link.signin",
                defaultMessage: "Sign in"
              })
            })
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Auth/components/Oops.mjs
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var React2 = __toESM(require_react(), 1);
var Oops = () => {
  const { formatMessage } = useIntl();
  const { search: searchString } = useLocation();
  const query = React2.useMemo(() => new URLSearchParams(searchString), [
    searchString
  ]);
  const message = query.get("info") || formatMessage({
    id: "Auth.components.Oops.text",
    defaultMessage: "Your account has been suspended."
  });
  return (0, import_jsx_runtime10.jsx)(UnauthenticatedLayout, {
    children: (0, import_jsx_runtime10.jsxs)(Vd, {
      children: [
        (0, import_jsx_runtime10.jsx)(LayoutContent, {
          children: (0, import_jsx_runtime10.jsxs)(Column, {
            children: [
              (0, import_jsx_runtime10.jsx)(Logo, {}),
              (0, import_jsx_runtime10.jsx)(S, {
                paddingTop: 6,
                paddingBottom: 7,
                children: (0, import_jsx_runtime10.jsx)(A, {
                  tag: "h1",
                  variant: "alpha",
                  children: formatMessage({
                    id: "Auth.components.Oops.title",
                    defaultMessage: "Oops..."
                  })
                })
              }),
              (0, import_jsx_runtime10.jsx)(A, {
                children: message
              }),
              (0, import_jsx_runtime10.jsx)(S, {
                paddingTop: 4,
                children: (0, import_jsx_runtime10.jsx)(A, {
                  children: formatMessage({
                    id: "Auth.components.Oops.text.admin",
                    defaultMessage: "If this is a mistake, please contact your administrator."
                  })
                })
              })
            ]
          })
        }),
        (0, import_jsx_runtime10.jsx)(C, {
          justifyContent: "center",
          children: (0, import_jsx_runtime10.jsx)(S, {
            paddingTop: 4,
            children: (0, import_jsx_runtime10.jsx)(Oa, {
              tag: NavLink,
              to: "/auth/login",
              children: formatMessage({
                id: "Auth.link.signin",
                defaultMessage: "Sign in"
              })
            })
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Auth/components/Register.mjs
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var React3 = __toESM(require_react(), 1);
var import_omit = __toESM(require_omit(), 1);
var REGISTER_USER_SCHEMA = create3().shape({
  firstname: create2().trim().required(errorsTrads.required).nullable(),
  lastname: create2().nullable(),
  password: create2().min(8, {
    id: errorsTrads.minLength.id,
    defaultMessage: "Password must be at least 8 characters",
    values: {
      min: 8
    }
  }).test("max-bytes", {
    id: "components.Input.error.contain.maxBytes",
    defaultMessage: "Password must be less than 73 bytes"
  }, function(value) {
    if (!value || typeof value !== "string") return true;
    const byteSize = getByteSize(value);
    return byteSize <= 72;
  }).test("lowercase", {
    message: {
      id: "components.Input.error.contain.lowercase",
      defaultMessage: "Password must contain at least 1 lowercase letter"
    }
  }, (value) => {
    if (!value) return true;
    return /[a-z]/.test(value);
  }).test("uppercase", {
    message: {
      id: "components.Input.error.contain.uppercase",
      defaultMessage: "Password must contain at least 1 uppercase letter"
    }
  }, (value) => {
    if (!value) return true;
    return /[A-Z]/.test(value);
  }).test("number", {
    message: {
      id: "components.Input.error.contain.number",
      defaultMessage: "Password must contain at least 1 number"
    }
  }, (value) => {
    if (!value) return true;
    return /\d/.test(value);
  }).required({
    id: errorsTrads.required.id,
    defaultMessage: "Password is required"
  }).nullable(),
  confirmPassword: create2().required({
    id: errorsTrads.required.id,
    defaultMessage: "Confirm password is required"
  }).oneOf([
    create("password"),
    null
  ], {
    id: "components.Input.error.password.noMatch",
    defaultMessage: "Passwords must match"
  }).nullable(),
  registrationToken: create2().required({
    id: errorsTrads.required.id,
    defaultMessage: "Registration token is required"
  })
});
var REGISTER_ADMIN_SCHEMA = create3().shape({
  firstname: create2().trim().required({
    id: errorsTrads.required.id,
    defaultMessage: "Firstname is required"
  }).nullable(),
  lastname: create2().nullable(),
  password: create2().min(8, {
    id: errorsTrads.minLength.id,
    defaultMessage: "Password must be at least 8 characters",
    values: {
      min: 8
    }
  }).test("max-bytes", {
    id: "components.Input.error.contain.maxBytes",
    defaultMessage: "Password must be less than 73 bytes"
  }, function(value) {
    if (!value) return true;
    return new TextEncoder().encode(value).length <= 72;
  }).test("lowercase", {
    message: {
      id: "components.Input.error.contain.lowercase",
      defaultMessage: "Password must contain at least 1 lowercase letter"
    }
  }, (value) => {
    if (!value) return true;
    return /[a-z]/.test(value);
  }).test("uppercase", {
    message: {
      id: "components.Input.error.contain.uppercase",
      defaultMessage: "Password must contain at least 1 uppercase letter"
    }
  }, (value) => {
    if (!value) return true;
    return /[A-Z]/.test(value);
  }).test("number", {
    message: {
      id: "components.Input.error.contain.number",
      defaultMessage: "Password must contain at least 1 number"
    }
  }, (value) => {
    if (!value) return true;
    return /\d/.test(value);
  }).required({
    id: errorsTrads.required.id,
    defaultMessage: "Password is required"
  }).nullable(),
  confirmPassword: create2().required({
    id: errorsTrads.required.id,
    defaultMessage: "Confirm password is required"
  }).nullable().oneOf([
    create("password"),
    null
  ], {
    id: "components.Input.error.password.noMatch",
    defaultMessage: "Passwords must match"
  }),
  email: create2().email({
    id: errorsTrads.email.id,
    defaultMessage: "Not a valid email"
  }).strict().lowercase({
    id: errorsTrads.lowercase.id,
    defaultMessage: "Email must be lowercase"
  }).required({
    id: errorsTrads.required.id,
    defaultMessage: "Email is required"
  }).nullable()
});
var isRecord = (value) => {
  return typeof value === "object" && !Array.isArray(value) && value !== null;
};
var isMessageDescriptor = (value) => {
  return isRecord(value) && typeof value.id === "string" && typeof value.defaultMessage === "string";
};
var formatValidationMessage = (msg, formatMessage) => {
  const format = ({ values, ...message }) => formatMessage(message, values);
  try {
    if (msg === void 0 || msg === null) return "";
    if (typeof msg === "string") return msg;
    if (isMessageDescriptor(msg)) return format(msg);
    if (isRecord(msg) && isMessageDescriptor(msg.message)) {
      return format(msg.message);
    }
    if (isRecord(msg) && Array.isArray(msg.errors) && msg.errors.length > 0) {
      const first = msg.errors[0];
      if (typeof first === "string") return first;
      if (isMessageDescriptor(first)) return format(first);
    }
    if (isRecord(msg) && typeof msg.defaultMessage === "string") {
      return msg.defaultMessage;
    }
    return String(msg);
  } catch {
    return String(msg);
  }
};
var Register = ({ hasAdmin }) => {
  const { toggleNotification } = useNotification();
  const navigate = useNavigate();
  const [submitCount, setSubmitCount] = React3.useState(0);
  const [apiError, setApiError] = React3.useState();
  const { trackUsage } = useTracking();
  const { formatMessage } = useIntl();
  const { search: searchString } = useLocation();
  const query = React3.useMemo(() => new URLSearchParams(searchString), [
    searchString
  ]);
  const match = useMatch("/auth/:authType");
  const { _unstableFormatAPIError: formatAPIError, _unstableFormatValidationErrors: formatValidationErrors } = useAPIErrorHandler();
  const { setNpsSurveySettings } = useNpsSurveySettings();
  const registrationToken = query.get("registrationToken");
  const { data: userInfo, error } = useGetRegistrationInfoQuery(registrationToken, {
    skip: !registrationToken
  });
  React3.useEffect(() => {
    if (error) {
      const message = isBaseQueryError(error) ? formatAPIError(error) : error.message ?? "";
      toggleNotification({
        type: "danger",
        message
      });
      navigate(`/auth/oops?info=${encodeURIComponent(message)}`);
    }
  }, [
    error,
    formatAPIError,
    navigate,
    toggleNotification
  ]);
  const [registerAdmin] = useRegisterAdminMutation();
  const [registerUser] = useRegisterUserMutation();
  const dispatch = useTypedDispatch();
  const handleRegisterAdmin = async ({ news, ...body }, setFormErrors) => {
    const res = await registerAdmin({
      ...body,
      deviceId: getOrCreateDeviceId()
    });
    if ("data" in res) {
      dispatch(login({
        token: res.data.token
      }));
      if (news) {
        setNpsSurveySettings((s) => ({
          ...s,
          enabled: true
        }));
        navigate({
          pathname: "/usecase",
          search: `?hasAdmin=${true}`
        });
      } else {
        navigate("/");
      }
    } else {
      if (isBaseQueryError(res.error)) {
        trackUsage("didNotCreateFirstAdmin");
        if (res.error.name === "ValidationError") {
          setFormErrors(formatValidationErrors(res.error));
          return;
        }
        setApiError(formatAPIError(res.error));
      }
    }
  };
  const handleRegisterUser = async ({ news, ...body }, setFormErrors) => {
    const res = await registerUser({
      ...body,
      deviceId: getOrCreateDeviceId()
    });
    if ("data" in res) {
      dispatch(login({
        token: res.data.token
      }));
      if (news) {
        setNpsSurveySettings((s) => ({
          ...s,
          enabled: true
        }));
        navigate({
          pathname: "/usecase",
          search: `?hasAdmin=${hasAdmin}`
        });
      } else {
        navigate("/");
      }
    } else {
      if (isBaseQueryError(res.error)) {
        trackUsage("didNotCreateFirstAdmin");
        if (res.error.name === "ValidationError") {
          setFormErrors(formatValidationErrors(res.error));
          return;
        }
        setApiError(formatAPIError(res.error));
      }
    }
  };
  if (!match || match.params.authType !== "register" && match.params.authType !== "register-admin") {
    return (0, import_jsx_runtime11.jsx)(Navigate, {
      to: "/"
    });
  }
  const isAdminRegistration = match.params.authType === "register-admin";
  const schema = isAdminRegistration ? REGISTER_ADMIN_SCHEMA : REGISTER_USER_SCHEMA;
  return (0, import_jsx_runtime11.jsx)(UnauthenticatedLayout, {
    children: (0, import_jsx_runtime11.jsxs)(LayoutContent, {
      children: [
        (0, import_jsx_runtime11.jsxs)(C, {
          direction: "column",
          alignItems: "center",
          gap: 3,
          children: [
            (0, import_jsx_runtime11.jsx)(Logo, {}),
            (0, import_jsx_runtime11.jsx)(A, {
              tag: "h1",
              variant: "alpha",
              textAlign: "center",
              children: formatMessage({
                id: "Auth.form.welcome.title",
                defaultMessage: "Welcome to Strapi!"
              })
            }),
            (0, import_jsx_runtime11.jsx)(A, {
              variant: "epsilon",
              textColor: "neutral600",
              textAlign: "center",
              children: formatMessage({
                id: "Auth.form.register.subtitle",
                defaultMessage: "Credentials are only used to authenticate in Strapi. All saved data will be stored in your database."
              })
            }),
            apiError ? (0, import_jsx_runtime11.jsx)(A, {
              id: "global-form-error",
              role: "alert",
              tabIndex: -1,
              textColor: "danger600",
              children: apiError
            }) : null
          ]
        }),
        (0, import_jsx_runtime11.jsx)(Form, {
          method: "POST",
          initialValues: {
            firstname: (userInfo == null ? void 0 : userInfo.firstname) || "",
            lastname: (userInfo == null ? void 0 : userInfo.lastname) || "",
            email: (userInfo == null ? void 0 : userInfo.email) || "",
            password: "",
            confirmPassword: "",
            registrationToken: registrationToken || void 0,
            news: false
          },
          onSubmit: async (data, helpers) => {
            const normalizedData = normalizeData(data);
            try {
              await schema.validate(normalizedData, {
                abortEarly: false
              });
              if (submitCount > 0 && isAdminRegistration) {
                trackUsage("didSubmitWithErrorsFirstAdmin", {
                  count: submitCount.toString()
                });
              }
              if (normalizedData.registrationToken) {
                handleRegisterUser({
                  userInfo: (0, import_omit.default)(normalizedData, [
                    "registrationToken",
                    "confirmPassword",
                    "email",
                    "news"
                  ]),
                  registrationToken: normalizedData.registrationToken,
                  news: normalizedData.news
                }, helpers.setErrors);
              } else {
                await handleRegisterAdmin((0, import_omit.default)(normalizedData, [
                  "registrationToken",
                  "confirmPassword"
                ]), helpers.setErrors);
              }
            } catch (err) {
              if (err instanceof ValidationError) {
                const computed = err.inner.reduce((acc, { message, path }) => {
                  if (!path) return acc;
                  acc[path] = formatValidationMessage(message, formatMessage);
                  return acc;
                }, {});
                helpers.setErrors(computed);
              }
              setSubmitCount(submitCount + 1);
            }
          },
          children: (0, import_jsx_runtime11.jsxs)(C, {
            direction: "column",
            alignItems: "stretch",
            gap: 6,
            marginTop: 7,
            children: [
              (0, import_jsx_runtime11.jsx)(Ld.Root, {
                gap: 4,
                children: [
                  {
                    label: formatMessage({
                      id: "Auth.form.firstname.label",
                      defaultMessage: "Firstname"
                    }),
                    name: "firstname",
                    required: true,
                    size: 6,
                    type: "string"
                  },
                  {
                    label: formatMessage({
                      id: "Auth.form.lastname.label",
                      defaultMessage: "Lastname"
                    }),
                    name: "lastname",
                    size: 6,
                    type: "string"
                  },
                  {
                    disabled: !isAdminRegistration,
                    label: formatMessage({
                      id: "Auth.form.email.label",
                      defaultMessage: "Email"
                    }),
                    name: "email",
                    required: true,
                    size: 12,
                    type: "email"
                  },
                  {
                    hint: formatMessage({
                      id: "Auth.form.password.hint",
                      defaultMessage: "Must be at least 8 characters, 1 uppercase, 1 lowercase & 1 number"
                    }),
                    label: formatMessage({
                      id: "global.password",
                      defaultMessage: "Password"
                    }),
                    name: "password",
                    required: true,
                    size: 12,
                    type: "password"
                  },
                  {
                    label: formatMessage({
                      id: "Auth.form.confirmPassword.label",
                      defaultMessage: "Confirm Password"
                    }),
                    name: "confirmPassword",
                    required: true,
                    size: 12,
                    type: "password"
                  },
                  {
                    label: formatMessage({
                      id: "Auth.form.register.news.label",
                      defaultMessage: "Keep me updated about new features & upcoming improvements (by doing this you accept the {terms} and the {policy})."
                    }, {
                      terms: (0, import_jsx_runtime11.jsx)(A2, {
                        target: "_blank",
                        href: "https://strapi.io/terms",
                        rel: "noreferrer",
                        children: formatMessage({
                          id: "Auth.privacy-policy-agreement.terms",
                          defaultMessage: "terms"
                        })
                      }),
                      policy: (0, import_jsx_runtime11.jsx)(A2, {
                        target: "_blank",
                        href: "https://strapi.io/privacy",
                        rel: "noreferrer",
                        children: formatMessage({
                          id: "Auth.privacy-policy-agreement.policy",
                          defaultMessage: "policy"
                        })
                      })
                    }),
                    name: "news",
                    size: 12,
                    type: "checkbox"
                  }
                ].map(({ size, ...field }) => (0, import_jsx_runtime11.jsx)(Ld.Item, {
                  xs: 12,
                  m: size,
                  direction: "column",
                  alignItems: "stretch",
                  children: (0, import_jsx_runtime11.jsx)(MemoizedInputRenderer, {
                    ...field
                  })
                }, field.name))
              }),
              (0, import_jsx_runtime11.jsx)(Ct, {
                fullWidth: true,
                size: "L",
                type: "submit",
                children: formatMessage({
                  id: "Auth.form.button.register",
                  defaultMessage: "Let's start"
                })
              })
            ]
          })
        }),
        (match == null ? void 0 : match.params.authType) === "register" && (0, import_jsx_runtime11.jsx)(S, {
          paddingTop: 4,
          children: (0, import_jsx_runtime11.jsx)(C, {
            justifyContent: "center",
            children: (0, import_jsx_runtime11.jsx)(Oa, {
              tag: NavLink,
              to: "/auth/login",
              children: formatMessage({
                id: "Auth.link.signin.account",
                defaultMessage: "Already have an account?"
              })
            })
          })
        })
      ]
    })
  });
};
function normalizeData(data) {
  return Object.entries(data).reduce((acc, [key, value]) => {
    if (![
      "password",
      "confirmPassword"
    ].includes(key) && typeof value === "string") {
      acc[key] = value.trim();
      if (key === "lastname") {
        acc[key] = value || void 0;
      }
    } else {
      acc[key] = value;
    }
    return acc;
  }, {});
}
var A2 = gt.a`
  color: ${({ theme }) => theme.colors.primary600};
`;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Auth/components/ResetPassword.mjs
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
var React4 = __toESM(require_react(), 1);
var RESET_PASSWORD_SCHEMA = create3().shape({
  password: create2().min(8, {
    id: errorsTrads.minLength.id,
    defaultMessage: "Password must be at least 8 characters",
    values: {
      min: 8
    }
  }).test("required-byte-size", {
    id: "components.Input.error.contain.maxBytes",
    defaultMessage: "Password must be less than 73 bytes"
  }, function(value) {
    if (!value || typeof value !== "string") return true;
    const byteSize = getByteSize(value);
    return byteSize <= 72;
  }).test("lowercase", {
    message: {
      id: "components.Input.error.contain.lowercase",
      defaultMessage: "Password must contain at least 1 lowercase letter"
    }
  }, (value) => {
    if (!value) return true;
    return /[a-z]/.test(value);
  }).test("uppercase", {
    message: {
      id: "components.Input.error.contain.uppercase",
      defaultMessage: "Password must contain at least 1 uppercase letter"
    }
  }, (value) => {
    if (!value) return true;
    return /[A-Z]/.test(value);
  }).test("number", {
    message: {
      id: "components.Input.error.contain.number",
      defaultMessage: "Password must contain at least 1 number"
    }
  }, (value) => {
    if (!value) return true;
    return /\d/.test(value);
  }).required({
    id: errorsTrads.required.id,
    defaultMessage: "Password is required"
  }).nullable(),
  confirmPassword: create2().required({
    id: errorsTrads.required.id,
    defaultMessage: "Confirm password is required"
  }).oneOf([
    create("password"),
    null
  ], {
    id: "components.Input.error.password.noMatch",
    defaultMessage: "Passwords must match"
  }).nullable()
});
var ResetPassword = () => {
  const { formatMessage } = useIntl();
  const dispatch = useTypedDispatch();
  const navigate = useNavigate();
  const { search: searchString } = useLocation();
  const query = React4.useMemo(() => new URLSearchParams(searchString), [
    searchString
  ]);
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const [resetPassword, { error }] = useResetPasswordMutation();
  const handleSubmit = async (body) => {
    const res = await resetPassword(body);
    if ("data" in res) {
      dispatch(login({
        token: res.data.token
      }));
      navigate("/");
    }
  };
  if (!query.get("code")) {
    return (0, import_jsx_runtime12.jsx)(Navigate, {
      to: "/auth/login"
    });
  }
  return (0, import_jsx_runtime12.jsx)(UnauthenticatedLayout, {
    children: (0, import_jsx_runtime12.jsxs)(Vd, {
      children: [
        (0, import_jsx_runtime12.jsxs)(LayoutContent, {
          children: [
            (0, import_jsx_runtime12.jsxs)(Column, {
              children: [
                (0, import_jsx_runtime12.jsx)(Logo, {}),
                (0, import_jsx_runtime12.jsx)(S, {
                  paddingTop: 6,
                  paddingBottom: 7,
                  children: (0, import_jsx_runtime12.jsx)(A, {
                    tag: "h1",
                    variant: "alpha",
                    children: formatMessage({
                      id: "global.reset-password",
                      defaultMessage: "Reset password"
                    })
                  })
                }),
                error ? (0, import_jsx_runtime12.jsx)(A, {
                  id: "global-form-error",
                  role: "alert",
                  tabIndex: -1,
                  textColor: "danger600",
                  children: isBaseQueryError(error) ? formatAPIError(error) : formatMessage({
                    id: "notification.error",
                    defaultMessage: "An error occurred"
                  })
                }) : null
              ]
            }),
            (0, import_jsx_runtime12.jsx)(Form, {
              method: "POST",
              initialValues: {
                password: "",
                confirmPassword: ""
              },
              onSubmit: (values) => {
                handleSubmit({
                  password: values.password,
                  resetPasswordToken: query.get("code")
                });
              },
              validationSchema: RESET_PASSWORD_SCHEMA,
              children: (0, import_jsx_runtime12.jsxs)(C, {
                direction: "column",
                alignItems: "stretch",
                gap: 6,
                children: [
                  [
                    {
                      hint: formatMessage({
                        id: "Auth.form.password.hint",
                        defaultMessage: "Password must contain at least 8 characters, 1 uppercase, 1 lowercase and 1 number"
                      }),
                      label: formatMessage({
                        id: "global.password",
                        defaultMessage: "Password"
                      }),
                      name: "password",
                      required: true,
                      type: "password"
                    },
                    {
                      label: formatMessage({
                        id: "Auth.form.confirmPassword.label",
                        defaultMessage: "Confirm Password"
                      }),
                      name: "confirmPassword",
                      required: true,
                      type: "password"
                    }
                  ].map((field) => (0, import_jsx_runtime12.jsx)(MemoizedInputRenderer, {
                    ...field
                  }, field.name)),
                  (0, import_jsx_runtime12.jsx)(Ct, {
                    fullWidth: true,
                    type: "submit",
                    children: formatMessage({
                      id: "global.change-password",
                      defaultMessage: "Change password"
                    })
                  })
                ]
              })
            })
          ]
        }),
        (0, import_jsx_runtime12.jsx)(C, {
          justifyContent: "center",
          children: (0, import_jsx_runtime12.jsx)(S, {
            paddingTop: 4,
            children: (0, import_jsx_runtime12.jsx)(Oa, {
              tag: NavLink,
              to: "/auth/login",
              children: formatMessage({
                id: "Auth.link.ready",
                defaultMessage: "Ready to sign in?"
              })
            })
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Auth/constants.mjs
var AUTH_TYPES = {
  LOGIN: "login",
  REGISTER: "register",
  REGISTER_ADMIN: "register-admin",
  FORGOT_PASSWORD: "forgot-password",
  RESET_PASSWORD: "reset-password",
  FORGOT_PASSWORD_SUCCESS: "forgot-password-success",
  OOPS: "oops",
  PROVIDERS: "providers"
};
var AUTH_TYPE_VALUES = Object.values(AUTH_TYPES);
var isAuthType = (value) => value !== void 0 && AUTH_TYPE_VALUES.includes(value);
var FORMS = {
  [AUTH_TYPES.FORGOT_PASSWORD]: ForgotPassword,
  [AUTH_TYPES.FORGOT_PASSWORD_SUCCESS]: ForgotPasswordSuccess,
  // the `Component` attribute is set after all forms and CE/EE components are loaded, but since we
  // are here outside of a React component we can not use the hook directly
  [AUTH_TYPES.LOGIN]: () => null,
  [AUTH_TYPES.OOPS]: Oops,
  [AUTH_TYPES.REGISTER]: Register,
  [AUTH_TYPES.REGISTER_ADMIN]: Register,
  [AUTH_TYPES.RESET_PASSWORD]: ResetPassword,
  [AUTH_TYPES.PROVIDERS]: () => null
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Auth/AuthPage.mjs
var AuthPage = () => {
  const { search } = useLocation();
  const match = useMatch("/auth/:authType");
  const authType = match == null ? void 0 : match.params.authType;
  const { data } = useInitQuery();
  const { hasAdmin } = data ?? {};
  const Login$1 = useEnterprise(Login, async () => (await retryDynamicImport(() => import("./Login-WNEMN2R2.js"))).LoginEE);
  const forms = useEnterprise(FORMS, async () => (await retryDynamicImport(() => import("./constants-BNLKCBQN.js"))).FORMS, {
    combine(ceForms, eeForms) {
      return {
        ...ceForms,
        ...eeForms
      };
    },
    defaultValue: FORMS
  });
  const { token } = useAuth("AuthPage", (auth) => auth);
  if (!isAuthType(authType) || !forms) {
    return (0, import_jsx_runtime13.jsx)(Navigate, {
      to: "/"
    });
  }
  const Component = forms[authType];
  if (!Component) {
    return (0, import_jsx_runtime13.jsx)(Navigate, {
      to: "/"
    });
  }
  if (authType !== AUTH_TYPES.REGISTER_ADMIN && authType !== AUTH_TYPES.REGISTER && token) {
    return (0, import_jsx_runtime13.jsx)(Navigate, {
      to: getRedirectTo(search)
    });
  }
  if (hasAdmin && authType === AUTH_TYPES.REGISTER_ADMIN && token) {
    return (0, import_jsx_runtime13.jsx)(Navigate, {
      to: getRedirectTo(search)
    });
  }
  if (!hasAdmin && authType !== AUTH_TYPES.REGISTER_ADMIN) {
    return (0, import_jsx_runtime13.jsx)(Navigate, {
      to: {
        pathname: `/auth/${AUTH_TYPES.REGISTER_ADMIN}`,
        // Forward the `?redirectTo` from /auth/login
        // /abc => /auth/login?redirectTo=%2Fabc => /auth/register-admin?redirectTo=%2Fabc
        search
      }
    });
  }
  if (Login$1 && authType === AUTH_TYPES.LOGIN) {
    return (0, import_jsx_runtime13.jsx)(Login$1, {});
  } else if (authType === AUTH_TYPES.LOGIN && !Login$1) {
    return null;
  }
  return (0, import_jsx_runtime13.jsx)(Component, {
    hasAdmin
  });
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/pages/Settings/constants.mjs
var ADMIN_TOKEN_ROUTES = [
  {
    lazy: async () => {
      const { ProtectedListView } = await import("./ListView-HMHYTQJ7.js");
      return {
        Component: ProtectedListView
      };
    },
    path: "admin-tokens"
  },
  {
    lazy: async () => {
      const { ProtectedCreateView } = await import("./CreateView-QESXXUMC.js");
      return {
        Component: ProtectedCreateView
      };
    },
    path: "admin-tokens/create"
  },
  {
    lazy: async () => {
      const { ProtectedEditView } = await import("./EditViewPage-NNPXODKH.js");
      return {
        Component: ProtectedEditView
      };
    },
    path: "admin-tokens/:id"
  }
];
var ROUTES_CE = () => [
  {
    lazy: async () => {
      const { ProtectedListPage } = await import("./ListPage-G4LNPBQT.js");
      return {
        Component: ProtectedListPage
      };
    },
    path: "roles"
  },
  {
    lazy: async () => {
      const { ProtectedCreatePage } = await import("./CreatePage-IBX5L2A6.js");
      return {
        Component: ProtectedCreatePage
      };
    },
    path: "roles/duplicate/:id"
  },
  {
    lazy: async () => {
      const { ProtectedCreatePage } = await import("./CreatePage-IBX5L2A6.js");
      return {
        Component: ProtectedCreatePage
      };
    },
    path: "roles/new"
  },
  {
    lazy: async () => {
      const { ProtectedEditPage } = await import("./EditPage-E54PKN76.js");
      return {
        Component: ProtectedEditPage
      };
    },
    path: "roles/:id"
  },
  {
    lazy: async () => {
      const { ProtectedListPage } = await import("./ListPage-ZFEZIY7Y.js");
      return {
        Component: ProtectedListPage
      };
    },
    path: "users"
  },
  {
    lazy: async () => {
      const { ProtectedEditPage } = await import("./EditPage-5U7OFJB5.js");
      return {
        Component: ProtectedEditPage
      };
    },
    path: "users/:id"
  },
  {
    lazy: async () => {
      const { ProtectedCreatePage } = await import("./CreatePage-IDLFHSBJ.js");
      return {
        Component: ProtectedCreatePage
      };
    },
    path: "webhooks/create"
  },
  {
    lazy: async () => {
      const editWebhook = await import("./EditPage-YCWYHNMQ.js");
      return {
        Component: editWebhook.ProtectedEditPage
      };
    },
    path: "webhooks/:id"
  },
  {
    lazy: async () => {
      const { ProtectedListPage } = await import("./ListPage-PT2DIBMZ.js");
      return {
        Component: ProtectedListPage
      };
    },
    path: "webhooks"
  },
  {
    lazy: async () => {
      const { ProtectedListView } = await import("./ListView-KYFXQCOH.js");
      return {
        Component: ProtectedListView
      };
    },
    path: "api-tokens"
  },
  {
    lazy: async () => {
      const { ProtectedCreateView } = await import("./CreateView-B263VO5Q.js");
      return {
        Component: ProtectedCreateView
      };
    },
    path: "api-tokens/create"
  },
  {
    lazy: async () => {
      const { ProtectedEditView } = await import("./EditViewPage-GNLNLGYQ.js");
      return {
        Component: ProtectedEditView
      };
    },
    path: "api-tokens/:id"
  },
  ...ADMIN_TOKEN_ROUTES,
  {
    lazy: async () => {
      const { ProtectedCreateView } = await import("./CreateView-7Z5LZVWA.js");
      return {
        Component: ProtectedCreateView
      };
    },
    path: "transfer-tokens/create"
  },
  {
    lazy: async () => {
      const { ProtectedListView } = await import("./ListView-JJLWKNUK.js");
      return {
        Component: ProtectedListView
      };
    },
    path: "transfer-tokens"
  },
  {
    lazy: async () => {
      const { ProtectedEditView } = await import("./EditView-37VCQVLK.js");
      return {
        Component: ProtectedEditView
      };
    },
    path: "transfer-tokens/:id"
  },
  {
    lazy: async () => {
      const { ProtectedInstalledPlugins } = await import("./InstalledPlugins-KBJRVTSJ.js");
      return {
        Component: ProtectedInstalledPlugins
      };
    },
    path: "list-plugins"
  },
  {
    lazy: async () => {
      const { PurchaseAuditLogs } = await import("./PurchaseAuditLogs-P5HD7MSD.js");
      return {
        Component: PurchaseAuditLogs
      };
    },
    path: "purchase-audit-logs"
  },
  {
    lazy: async () => {
      const { PurchaseSingleSignOn } = await import("./PurchaseSingleSignOn-YIYXOXD3.js");
      return {
        Component: PurchaseSingleSignOn
      };
    },
    path: "purchase-single-sign-on"
  },
  {
    lazy: async () => {
      const { PurchaseContentHistory } = await import("./PurchaseContentHistory-IOMXF6AH.js");
      return {
        Component: PurchaseContentHistory
      };
    },
    path: "purchase-content-history"
  }
];

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/router.mjs
var getImmutableRoutes = () => [
  {
    path: "usecase",
    lazy: async () => {
      const { PrivateUseCasePage } = await import("./UseCasePage-ZJ2NA2WV.js");
      return {
        Component: PrivateUseCasePage
      };
    }
  },
  // this needs to go before auth/:authType because otherwise it won't match the route
  ...getEERoutes(),
  {
    path: "auth/:authType",
    element: (0, import_jsx_runtime14.jsx)(AuthPage, {})
  }
];
var getInitialRoutes = () => [
  {
    index: true,
    lazy: async () => {
      const { HomePage } = await import("./HomePage-HNUZI3RY.js");
      return {
        Component: HomePage
      };
    }
  },
  {
    path: "me",
    lazy: async () => {
      const { ProfilePage } = await import("./ProfilePage-R6N6YBXB.js");
      return {
        Component: ProfilePage
      };
    }
  },
  {
    path: "me/sessions",
    lazy: async () => {
      const { SessionsPage } = await import("./SessionsPage-ZKVBJJFV.js");
      return {
        Component: SessionsPage
      };
    }
  },
  {
    path: "settings/*",
    lazy: async () => {
      const { Layout } = await import("./Layout-H2CIREGX.js");
      return {
        Component: Layout
      };
    },
    children: [
      {
        path: "application-infos",
        lazy: async () => {
          const { ApplicationInfoPage } = await import("./ApplicationInfoPage-LXPRFS2H.js");
          return {
            Component: ApplicationInfoPage
          };
        }
      },
      // ...Object.values(this.settings).flatMap(({ links }) =>
      //   links.map(({ to, Component }) => ({
      //     path: `${to}/*`,
      //     element: (
      //       <React.Suspense fallback={<Page.Loading />}>
      //         <Component />
      //       </React.Suspense>
      //     ),
      //   }))
      // ),
      ...[
        ...getEERoutes2(),
        ...ROUTES_CE()
      ].filter((route, index, refArray) => refArray.findIndex((obj) => obj.path === route.path) === index)
    ]
  }
];

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/core/apis/router.mjs
var Router = class {
  get routes() {
    return this._routes;
  }
  get menu() {
    return this._menu;
  }
  get settings() {
    return this._settings;
  }
  /**
  * @internal This method is used internally by Strapi to create the router.
  * It should not be used by plugins, doing so will likely break the application.
  */
  createRouter(strapi, { memory, ...opts } = {}) {
    const authenticatedLayoutRoute = {
      path: "/*",
      lazy: async () => {
        const { PrivateAdminLayout } = await import("./AuthenticatedLayout-TN4J2GKO.js");
        return {
          Component: PrivateAdminLayout
        };
      },
      children: [
        ...this.routes,
        {
          path: "*",
          element: (0, import_jsx_runtime15.jsx)(NotFoundPage, {})
        }
      ]
    };
    const routes = [
      {
        path: "/*",
        errorElement: (0, import_jsx_runtime15.jsx)(Provider_default, {
          store: strapi.store,
          children: (0, import_jsx_runtime15.jsx)(LanguageProvider, {
            messages: strapi.configurations.translations,
            children: (0, import_jsx_runtime15.jsx)(Theme, {
              themes: strapi.configurations.themes,
              children: (0, import_jsx_runtime15.jsx)(ErrorElement, {})
            })
          })
        }),
        element: (0, import_jsx_runtime15.jsx)(App, {
          strapi,
          store: strapi.store
        }),
        children: [
          ...wrapRouteObjectsLazyWithRetry(getImmutableRoutes()),
          wrapRouteObjectLazyWithRetry(authenticatedLayoutRoute)
        ]
      }
    ];
    if (memory) {
      this.router = createMemoryRouter(routes, opts);
    } else {
      this.router = createBrowserRouter(routes, opts);
    }
    return this.router;
  }
  addSettingsLink(section, link) {
    var _a, _b, _c, _d;
    if (typeof section === "object" && "links" in section) {
      (0, import_invariant2.default)(section.id, "section.id should be defined");
      (0, import_invariant2.default)(((_a = section.intlLabel) == null ? void 0 : _a.id) && ((_b = section.intlLabel) == null ? void 0 : _b.defaultMessage), "section.intlLabel should be defined");
      (0, import_invariant2.default)(this.settings[section.id] === void 0, "A similar section already exists");
      (0, import_invariant2.default)(Array.isArray(section.links), "TypeError expected links to be an array");
      this.settings[section.id] = {
        ...section,
        links: []
      };
      section.links.forEach((link2) => {
        this.createSettingsLink(section.id, link2);
      });
    } else if (typeof section === "object" && link) {
      (0, import_invariant2.default)(section.id, "section.id should be defined");
      (0, import_invariant2.default)(((_c = section.intlLabel) == null ? void 0 : _c.id) && ((_d = section.intlLabel) == null ? void 0 : _d.defaultMessage), "section.intlLabel should be defined");
      (0, import_invariant2.default)(this.settings[section.id] === void 0, "A similar section already exists");
      this.settings[section.id] = {
        ...section,
        links: []
      };
      if (Array.isArray(link)) {
        link.forEach((l) => this.createSettingsLink(section.id, l));
      } else {
        this.createSettingsLink(section.id, link);
      }
    } else if (typeof section === "string" && link) {
      if (Array.isArray(link)) {
        link.forEach((l) => this.createSettingsLink(section, l));
      } else {
        this.createSettingsLink(section, link);
      }
    } else {
      throw new Error("Invalid arguments provided to addSettingsLink, at minimum a sectionId and link are required.");
    }
  }
  /**
  * @alpha
  * @description Adds a route or an array of routes to the router.
  * Otherwise, pass a function that receives the current routes and
  * returns the new routes in a reducer like fashion.
  */
  addRoute(route) {
    if (Array.isArray(route)) {
      this._routes = [
        ...this._routes,
        ...route
      ];
    } else if (typeof route === "object" && route !== null) {
      this._routes.push(route);
    } else if (typeof route === "function") {
      this._routes = route(this._routes);
    } else {
      throw new Error(`Expected the \`route\` passed to \`addRoute\` to be an array or a function, but received ${getPrintableType(route)}`);
    }
  }
  constructor(initialRoutes) {
    this._routes = [];
    this.router = null;
    this._menu = [];
    this._settings = {
      global: {
        id: "global",
        intlLabel: {
          id: "Settings.global",
          defaultMessage: "Global Settings"
        },
        links: []
      }
    };
    this.addMenuLink = (link) => {
      var _a, _b;
      (0, import_invariant2.default)(link.to, `[${link.intlLabel.defaultMessage}]: link.to should be defined`);
      (0, import_invariant2.default)(typeof link.to === "string", `[${link.intlLabel.defaultMessage}]: Expected link.to to be a string instead received ${typeof link.to}`);
      (0, import_invariant2.default)(((_a = link.intlLabel) == null ? void 0 : _a.id) && ((_b = link.intlLabel) == null ? void 0 : _b.defaultMessage), `[${link.intlLabel.defaultMessage}]: link.intlLabel.id & link.intlLabel.defaultMessage should be defined`);
      (0, import_invariant2.default)(!link.Component || link.Component && typeof link.Component === "function", `[${link.intlLabel.defaultMessage}]: link.Component must be a function returning a Promise that returns a default component. Please use: \`Component: () => import(path)\` instead.`);
      if (link.Component && typeof link.Component === "function" && // @ts-expect-error – shh
      link.Component[Symbol.toStringTag] === "AsyncFunction") {
        console.warn(`
      [${link.intlLabel.defaultMessage}]: [deprecated] addMenuLink() was called with an async Component from the plugin "${link.intlLabel.defaultMessage}". Component loaders should return a dynamic import with a default export shape, e.g. \`Component: () => import(path).then((mod) => ({ default: mod.Component }))\`. Async wrapper functions will stop being supported in a future version.
      `.trim());
      }
      if (link.to.startsWith("/")) {
        console.warn(`[${link.intlLabel.defaultMessage}]: the \`to\` property of your menu link is an absolute path, it should be relative to the root of the application. This has been corrected for you but will be removed in a future version of Strapi.`);
        link.to = link.to.slice(1);
      }
      const { Component, ...restLink } = link;
      if (Component) {
        this._routes.push({
          path: `${link.to}/*`,
          lazy: async () => {
            const mod = await Component();
            if ("default" in mod) {
              return {
                Component: mod.default
              };
            } else {
              return {
                Component: mod
              };
            }
          }
        });
      }
      this.menu.push(restLink);
    };
    this.createSettingsLink = (sectionId, link) => {
      var _a, _b;
      (0, import_invariant2.default)(this._settings[sectionId], "The section does not exist");
      (0, import_invariant2.default)(link.id, `[${link.intlLabel.defaultMessage}]: link.id should be defined`);
      (0, import_invariant2.default)(((_a = link.intlLabel) == null ? void 0 : _a.id) && ((_b = link.intlLabel) == null ? void 0 : _b.defaultMessage), `[${link.intlLabel.defaultMessage}]: link.intlLabel.id & link.intlLabel.defaultMessage`);
      (0, import_invariant2.default)(link.to, `[${link.intlLabel.defaultMessage}]: link.to should be defined`);
      (0, import_invariant2.default)(!link.Component || link.Component && typeof link.Component === "function", `[${link.intlLabel.defaultMessage}]: link.Component must be a function returning a Promise. Please use: \`Component: () => import(path)\` instead.`);
      if (link.Component && typeof link.Component === "function" && // @ts-expect-error – shh
      link.Component[Symbol.toStringTag] === "AsyncFunction") {
        console.warn(`
      [${link.intlLabel.defaultMessage}]: [deprecated] addSettingsLink() was called with an async Component from the plugin "${link.intlLabel.defaultMessage}". Component loaders should return a dynamic import with a default export shape, e.g. \`Component: () => import(path).then((mod) => ({ default: mod.Component }))\`. Async wrapper functions will stop being supported in a future version.
      `.trim());
      }
      if (link.to.startsWith("/")) {
        console.warn(`[${link.intlLabel.defaultMessage}]: the \`to\` property of your settings link is an absolute path. It should be relative to \`/settings\`. This has been corrected for you but will be removed in a future version of Strapi.`);
        link.to = link.to.slice(1);
      }
      if (link.to.split("/")[0] === "settings") {
        console.warn(`[${link.intlLabel.defaultMessage}]: the \`to\` property of your settings link has \`settings\` as the first part of it's path. It should be relative to \`settings\` and therefore, not include it. This has been corrected for you but will be removed in a future version of Strapi.`);
        link.to = link.to.split("/").slice(1).join("/");
      }
      const { Component, ...restLink } = link;
      const settingsIndex = this._routes.findIndex((route) => route.path === "settings/*");
      if (!settingsIndex) {
        console.warn("A third party plugin has removed the settings section, the settings link cannot be added.");
        return;
      } else if (!this._routes[settingsIndex].children) {
        this._routes[settingsIndex].children = [];
      }
      if (Component) {
        this._routes[settingsIndex].children.push({
          path: `${link.to}/*`,
          lazy: async () => {
            const mod = await Component();
            if ("default" in mod) {
              return {
                Component: mod.default
              };
            } else {
              return {
                Component: mod
              };
            }
          }
        });
      }
      this._settings[sectionId].links.push(restLink);
    };
    this._routes = initialRoutes;
  }
};
var getPrintableType = (value) => {
  const nativeType = typeof value;
  if (nativeType === "object") {
    if (value === null) return "null";
    if (Array.isArray(value)) return "array";
    if (value instanceof Object && value.constructor.name !== "Object") {
      return value.constructor.name;
    }
  }
  return nativeType;
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/core/apis/Widgets.mjs
var import_invariant3 = __toESM(require_browser(), 1);
var Widgets = class {
  register(widgets) {
    if (Array.isArray(widgets)) {
      this.checkWidgets(widgets);
      this.widgets = [
        ...this.widgets,
        ...widgets
      ];
    } else if (typeof widgets === "function") {
      const newWidgets = widgets(this.widgets);
      this.checkWidgets(newWidgets);
      this.widgets = newWidgets;
    } else if (typeof widgets === "object") {
      this.checkWidgets([
        widgets
      ]);
      this.widgets.push(widgets);
    } else {
      throw new Error("Expected widgets to be an array or a reducer function");
    }
  }
  constructor() {
    this.generateUid = (widget) => {
      return widget.pluginId ? `plugin::${widget.pluginId}.${widget.id}` : `global::${widget.id}`;
    };
    this.checkWidgets = (widgets) => {
      widgets.forEach((widget) => {
        (0, import_invariant3.default)(widget.id, "An id must be provided");
        (0, import_invariant3.default)(widget.component, "A component must be provided");
        (0, import_invariant3.default)(widget.title, "A title must be provided");
        (0, import_invariant3.default)(widget.icon, "An icon must be provided");
      });
    };
    this.getAll = () => {
      return this.widgets.map((widget) => {
        const { id: _id, pluginId: _pluginId, ...widgetBase } = widget;
        return {
          ...widgetBase,
          uid: this.generateUid(widget)
        };
      });
    };
    this.widgets = [];
  }
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/core/store/configure.mjs
var staticReducers = {
  [adminApi.reducerPath]: adminApi.reducer,
  admin_app: reducer
};
var injectReducerStoreEnhancer = (appReducers) => (next) => (...args) => {
  const store = next(...args);
  const asyncReducers = {};
  return {
    ...store,
    asyncReducers,
    injectReducer: (key, asyncReducer) => {
      asyncReducers[key] = asyncReducer;
      store.replaceReducer(
        // @ts-expect-error we dynamically add reducers which makes the types uncomfortable.
        combineReducers({
          ...appReducers,
          ...asyncReducers
        })
      );
    }
  };
};
var configureStoreImpl = (preloadedState = {}, appMiddlewares = [], injectedReducers = {}) => {
  const coreReducers = {
    ...staticReducers,
    ...injectedReducers
  };
  const defaultMiddlewareOptions = {};
  if (false) {
    defaultMiddlewareOptions.serializableCheck = false;
    defaultMiddlewareOptions.immutableCheck = false;
  }
  const store = configureStore({
    preloadedState: {
      admin_app: preloadedState.admin_app
    },
    reducer: coreReducers,
    devTools: true,
    middleware: (getDefaultMiddleware) => [
      ...getDefaultMiddleware(defaultMiddlewareOptions),
      rtkQueryUnauthorizedMiddleware,
      adminApi.middleware,
      ...appMiddlewares.map((m) => m())
    ],
    enhancers: [
      injectReducerStoreEnhancer(coreReducers)
    ]
  });
  return store;
};
var rtkQueryUnauthorizedMiddleware = ({ dispatch }) => (next) => (action) => {
  var _a;
  if (isRejected(action) && ((_a = action.payload) == null ? void 0 : _a.status) === 401) {
    dispatch(logout());
    const basename = getBasename();
    window.location.href = `${basename}/auth/login`;
    return;
  }
  return next(action);
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/core/utils/createHook.mjs
var createHook = () => {
  const _handlers = [];
  return {
    register(fn) {
      _handlers.push(fn);
    },
    delete(handler) {
      _handlers.splice(_handlers.indexOf(handler), 1);
    },
    runWaterfall(args, store) {
      return _handlers.reduce((acc, fn) => fn(acc, store), args);
    },
    async runWaterfallAsync(args, store) {
      let result = args;
      for (const fn of _handlers) {
        result = await fn(result, store);
      }
      return result;
    },
    runSeries(...args) {
      return _handlers.map((fn) => fn(...args));
    },
    async runSeriesAsync(...args) {
      const result = [];
      for (const fn of _handlers) {
        result.push(await fn(...args));
      }
      return result;
    },
    runParallel(...args) {
      return Promise.all(_handlers.map((fn) => {
        return fn(...args);
      }));
    }
  };
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/translations/languageNativeNames.mjs
var languageNativeNames = {
  ar: "العربية",
  ca: "Català",
  cs: "Čeština",
  de: "Deutsch",
  da: "Dansk",
  en: "English",
  "en-GB": "English (United Kingdom)",
  es: "Español",
  eu: "Euskara",
  uz: "O`zbekcha",
  "uz-Cyrl": "Ўзбекча",
  ro: "Română",
  fr: "Français",
  gu: "Gujarati",
  he: "עברית",
  hu: "Magyar",
  id: "Indonesian",
  it: "Italiano",
  ja: "日本語",
  ko: "한국어",
  ml: "Malayalam",
  ms: "Melayu",
  nl: "Nederlands",
  no: "Norwegian",
  pl: "Polski",
  "pt-BR": "Português (Brasil)",
  pt: "Português (Portugal)",
  ru: "Русский",
  sk: "Slovenčina",
  sv: "Swedish",
  th: "ไทย",
  tr: "Türkçe",
  uk: "Українська",
  vi: "Tiếng Việt",
  "zh-Hans": "中文 (简体)",
  zh: "中文 (繁體)",
  sa: "संस्कृत",
  hi: "हिन्दी",
  fi: "Suomi"
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/StrapiApp.mjs
function __variableDynamicImportRuntime5__(path) {
  switch (path) {
    case "./translations/ar.json":
      return import("./ar.json-3ENAI7RV.js");
    case "./translations/ca.json":
      return import("./ca.json-HTPQY4HK.js");
    case "./translations/cs.json":
      return import("./cs.json-INNNV74D.js");
    case "./translations/da.json":
      return import("./da.json-7DKKZ7BA.js");
    case "./translations/de.json":
      return import("./de.json-XI3YXCD6.js");
    case "./translations/en.json":
      return import("./en.json-2NYQ4AEV.js");
    case "./translations/es.json":
      return import("./es.json-HFK7V5SZ.js");
    case "./translations/eu.json":
      return import("./eu.json-MYHSJYBQ.js");
    case "./translations/fi.json":
      return import("./fi.json-PNWPSBAM.js");
    case "./translations/fr.json":
      return import("./fr.json-ZFNM5N25.js");
    case "./translations/gu.json":
      return import("./gu.json-KDKLQWY3.js");
    case "./translations/he.json":
      return import("./he.json-MK2AWQNP.js");
    case "./translations/hi.json":
      return import("./hi.json-ODYDTHVH.js");
    case "./translations/hu.json":
      return import("./hu.json-4WM34KQ2.js");
    case "./translations/id.json":
      return import("./id.json-REIGJV2E.js");
    case "./translations/it.json":
      return import("./it.json-VAQ2Z2KG.js");
    case "./translations/ja.json":
      return import("./ja.json-VVOENAEM.js");
    case "./translations/ko.json":
      return import("./ko.json-PO4IBIVT.js");
    case "./translations/ml.json":
      return import("./ml.json-3PEVYDMC.js");
    case "./translations/ms.json":
      return import("./ms.json-J2L3DI7Y.js");
    case "./translations/nl.json":
      return import("./nl.json-R5P57G4F.js");
    case "./translations/no.json":
      return import("./no.json-6JTOIGBE.js");
    case "./translations/pl.json":
      return import("./pl.json-QETSHHAG.js");
    case "./translations/pt-BR.json":
      return import("./pt-BR.json-D6XTCK2O.js");
    case "./translations/pt.json":
      return import("./pt.json-2APJRFLT.js");
    case "./translations/ru.json":
      return import("./ru.json-V6T4B4HG.js");
    case "./translations/sa.json":
      return import("./sa.json-OHD637NF.js");
    case "./translations/sk.json":
      return import("./sk.json-RCWSBZGE.js");
    case "./translations/sv.json":
      return import("./sv.json-73RJJU2N.js");
    case "./translations/th.json":
      return import("./th.json-OQEYGXB6.js");
    case "./translations/tr.json":
      return import("./tr.json-JLF27AW3.js");
    case "./translations/uk.json":
      return import("./uk.json-VYUYTPYT.js");
    case "./translations/vi.json":
      return import("./vi.json-D4UM5CRY.js");
    case "./translations/zh-Hans.json":
      return import("./zh-Hans.json-OQAQPWDV.js");
    case "./translations/zh.json":
      return import("./zh.json-5VSC7CHP.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
function __variableDynamicImportRuntime4__(path) {
  switch (path) {
    case "./translations/en-GB.js":
      return import("./en-GB-3RPAZAYS.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
var ADMIN_LOCALE_LEGACY_ALIASES = {
  da: "dk"
};
var { INJECT_COLUMN_IN_TABLE, INJECT_LIST_VIEW_FILTERS, MUTATE_COLLECTION_TYPES_LINKS, MUTATE_EDIT_VIEW_LAYOUT, MUTATE_SINGLE_TYPES_LINKS } = HOOKS;
var StrapiApp = class {
  async bootstrap(customBootstrap) {
    Object.keys(this.appPlugins).forEach((plugin) => {
      const bootstrap = this.appPlugins[plugin].bootstrap;
      if (bootstrap) {
        bootstrap({
          addSettingsLink: this.addSettingsLink,
          addSettingsLinks: this.addSettingsLinks,
          getPlugin: this.getPlugin,
          registerHook: this.registerHook
        });
      }
    });
    if ((0, import_isFunction.default)(customBootstrap)) {
      customBootstrap({
        addComponents: this.addComponents,
        addFields: this.addFields,
        addMenuLink: this.addMenuLink,
        addReducers: this.addReducers,
        addSettingsLink: this.addSettingsLink,
        addSettingsLinks: this.addSettingsLinks,
        getPlugin: this.getPlugin,
        registerHook: this.registerHook
      });
    }
  }
  async register(customRegister) {
    const widgets = [
      {
        icon: Nn,
        title: {
          id: "widget.profile.title",
          defaultMessage: "Profile"
        },
        component: async () => {
          const { ProfileWidget } = await import("./Widgets-DN65N5WO.js");
          return ProfileWidget;
        },
        pluginId: "admin",
        id: "profile-info",
        link: {
          label: {
            id: "global.profile.settings",
            defaultMessage: "Profile settings"
          },
          href: "/me"
        }
      },
      {
        icon: Gn,
        title: {
          id: "widget.key-statistics.title",
          defaultMessage: "Project statistics"
        },
        component: async () => {
          const { KeyStatisticsWidget } = await import("./Widgets-DN65N5WO.js");
          return KeyStatisticsWidget;
        },
        pluginId: "admin",
        id: "key-statistics",
        roles: [
          "strapi-super-admin"
        ]
      }
    ];
    if ("strapi-cloud" in this.appPlugins) {
      widgets.push({
        icon: r5,
        title: {
          id: "widget.deploy-now.title",
          defaultMessage: "Deploy"
        },
        component: async () => {
          const { DeployNowWidget } = await import("./Widgets-DN65N5WO.js");
          return DeployNowWidget;
        },
        pluginId: "admin",
        id: "deploy-now"
      });
    }
    this.widgets.register(widgets);
    Object.keys(this.appPlugins).forEach((plugin) => {
      this.appPlugins[plugin].register(this);
    });
    if ((0, import_isFunction.default)(customRegister)) {
      customRegister(this);
    }
    if (window.strapi.features.isEnabled(window.strapi.features.AUDIT_LOGS)) {
      this.widgets.register([
        {
          icon: l5,
          title: {
            id: "widget.last-activity.title",
            defaultMessage: "Last activity"
          },
          component: async () => {
            const { AuditLogsWidget } = await import("./Widgets-2KCBDLKE.js");
            return AuditLogsWidget;
          },
          pluginId: "admin",
          id: "audit-logs",
          link: {
            label: {
              id: "widget.last-activity.link",
              defaultMessage: "Open Audit Logs"
            },
            href: `/settings/audit-logs?pageSize=${AUDIT_LOGS_DEFAULT_PAGE_SIZE}&page=1&sort=date:DESC`
          },
          permissions: [
            {
              action: "admin::audit-logs.read"
            }
          ]
        }
      ]);
    }
  }
  mergePluginTranslationsByCanonical(pluginTradsBatch) {
    return pluginTradsBatch.reduce((acc, current) => {
      const canonicalLocale = normalizeAdminLocale(current.locale);
      acc[canonicalLocale] = {
        ...current.locale === canonicalLocale ? acc[canonicalLocale] : current.data,
        ...current.locale === canonicalLocale ? current.data : acc[canonicalLocale]
      };
      if (current.locale !== canonicalLocale && Object.keys(current.data).length > 0) {
        const deprecationKey = `${canonicalLocale}:${current.locale}`;
        if (!this.warnedLegacyLocalePairs.has(deprecationKey)) {
          this.warnedLegacyLocalePairs.add(deprecationKey);
          console.warn(`[deprecated] Admin locale "${current.locale}" is deprecated. Rename translation files to "${canonicalLocale}.json".`);
        }
      }
      return acc;
    }, {});
  }
  async loadAdminTrads() {
    const adminLocales = await Promise.all(this.configurations.locales.map(async (locale) => {
      try {
        const { default: data } = await __variableDynamicImportRuntime4__(`./translations/${locale}.js`);
        return {
          data,
          locale
        };
      } catch {
        try {
          const { default: data } = await __variableDynamicImportRuntime5__(`./translations/${locale}.json`);
          return {
            data,
            locale
          };
        } catch {
          return {
            data: null,
            locale
          };
        }
      }
    }));
    return adminLocales.reduce((acc, current) => {
      if (current.data) {
        acc[current.locale] = current.data;
      }
      return acc;
    }, {});
  }
  /**
  * Load the application's translations and merged the custom translations
  * with the default ones.
  */
  async loadTrads(customTranslations = {}) {
    const normalizedCustomTranslations = Object.keys(customTranslations).reduce((acc, key) => {
      const normalizedKey = normalizeAdminLocale(key);
      acc[normalizedKey] = {
        ...acc[normalizedKey],
        ...customTranslations[key]
      };
      return acc;
    }, {});
    const adminTranslations = await this.loadAdminTrads();
    const localesForPlugins = (0, import_uniq.default)(this.configurations.locales.flatMap((locale) => {
      const legacyLocale = ADMIN_LOCALE_LEGACY_ALIASES[locale];
      return legacyLocale ? [
        locale,
        legacyLocale
      ] : [
        locale
      ];
    }));
    const arrayOfPromises = Object.keys(this.appPlugins).map((plugin) => {
      const registerTrads = this.appPlugins[plugin].registerTrads;
      if (registerTrads) {
        return registerTrads({
          locales: localesForPlugins
        });
      }
      return null;
    }).filter((a) => a);
    const pluginsTrads = await Promise.all(arrayOfPromises);
    const mergedTrads = pluginsTrads.reduce((acc, currentPluginTrads) => {
      const pluginTrads = this.mergePluginTranslationsByCanonical(currentPluginTrads);
      Object.keys(pluginTrads).forEach((locale) => {
        acc[locale] = {
          ...acc[locale],
          ...pluginTrads[locale]
        };
      });
      return acc;
    }, {});
    const translations = this.configurations.locales.reduce((acc, current) => {
      acc[current] = {
        ...adminTranslations[current],
        ...mergedTrads[current],
        ...normalizedCustomTranslations[current]
      };
      return acc;
    }, {});
    this.configurations.translations = translations;
    return Promise.resolve();
  }
  render() {
    const localeNames = (0, import_pick.default)(languageNativeNames, this.configurations.locales || []);
    const storedLocale = localStorage.getItem(LANGUAGE_LOCAL_STORAGE_KEY) || "en";
    const locale = normalizeAdminLocale(storedLocale);
    this.store = configureStoreImpl({
      admin_app: {
        permissions: (0, import_merge.default)({}, ADMIN_PERMISSIONS_CE, ADMIN_PERMISSIONS_EE),
        theme: {
          availableThemes: [],
          currentTheme: localStorage.getItem(THEME_LOCAL_STORAGE_KEY) || "system"
        },
        language: {
          locale: localeNames[locale] ? locale : "en",
          localeNames
        },
        token: getStoredToken()
      }
    }, this.middlewares, this.reducers);
    const router = this.router.createRouter(this, {
      basename: getBasename()
    });
    return (0, import_jsx_runtime16.jsx)(RouterProvider, {
      router
    });
  }
  constructor({ config, appPlugins } = {}) {
    this.plugins = {};
    this.hooksDict = {};
    this.warnedLegacyLocalePairs = /* @__PURE__ */ new Set();
    this.admin = {
      injectionZones: {}
    };
    this.translations = {};
    this.configurations = {
      authLogo: img,
      locales: [
        "en"
      ],
      menuLogo: img,
      notifications: {
        releases: true
      },
      themes: {
        light: nn,
        dark: Rd
      },
      translations: {},
      tutorials: true
    };
    this.rbac = new RBAC();
    this.library = {
      components: {},
      fields: {}
    };
    this.middlewares = [];
    this.reducers = {};
    this.store = null;
    this.customFields = new CustomFields();
    this.widgets = new Widgets();
    this.addComponents = (components) => {
      if (Array.isArray(components)) {
        components.map((comp) => {
          (0, import_invariant4.default)(comp.Component, "A Component must be provided");
          (0, import_invariant4.default)(comp.name, "A type must be provided");
          this.library.components[comp.name] = comp.Component;
        });
      } else {
        (0, import_invariant4.default)(components.Component, "A Component must be provided");
        (0, import_invariant4.default)(components.name, "A type must be provided");
        this.library.components[components.name] = components.Component;
      }
    };
    this.addFields = (fields) => {
      if (Array.isArray(fields)) {
        fields.map((field) => {
          (0, import_invariant4.default)(field.Component, "A Component must be provided");
          (0, import_invariant4.default)(field.type, "A type must be provided");
          this.library.fields[field.type] = field.Component;
        });
      } else {
        (0, import_invariant4.default)(fields.Component, "A Component must be provided");
        (0, import_invariant4.default)(fields.type, "A type must be provided");
        this.library.fields[fields.type] = fields.Component;
      }
    };
    this.addMiddlewares = (middlewares) => {
      middlewares.forEach((middleware) => {
        this.middlewares.push(middleware);
      });
    };
    this.addRBACMiddleware = (m) => {
      if (Array.isArray(m)) {
        this.rbac.use(m);
      } else {
        this.rbac.use(m);
      }
    };
    this.addReducers = (reducers) => {
      Object.entries(reducers).forEach(([name, reducer2]) => {
        this.reducers[name] = reducer2;
      });
    };
    this.addMenuLink = (link) => this.router.addMenuLink(link);
    this.addSettingsLinks = (sectionId, links) => {
      (0, import_invariant4.default)(Array.isArray(links), "TypeError expected links to be an array");
      this.router.addSettingsLink(sectionId, links);
    };
    this.createSettingSection = (section, links) => this.router.addSettingsLink(section, links);
    this.addSettingsLink = (sectionId, link) => {
      this.router.addSettingsLink(sectionId, link);
    };
    this.createCustomConfigurations = (customConfig) => {
      var _a, _b, _c, _d;
      if (customConfig.locales) {
        const extraLocales = ((_a = customConfig.locales) == null ? void 0 : _a.filter((loc) => loc !== "en").map((loc) => normalizeAdminLocale(loc))) || [];
        this.configurations.locales = (0, import_uniq.default)([
          "en",
          ...extraLocales
        ]);
      }
      if ((_b = customConfig.auth) == null ? void 0 : _b.logo) {
        this.configurations.authLogo = customConfig.auth.logo;
      }
      if ((_c = customConfig.menu) == null ? void 0 : _c.logo) {
        this.configurations.menuLogo = customConfig.menu.logo;
      }
      if (customConfig.theme) {
        const darkTheme = customConfig.theme.dark;
        const lightTheme = customConfig.theme.light;
        if (!darkTheme && !lightTheme) {
          console.warn(`[deprecated] In future versions, Strapi will stop supporting this theme customization syntax. The theme configuration accepts a light and a dark key to customize each theme separately. See https://docs.strapi.io/developer-docs/latest/development/admin-customization.html#theme-extension.`.trim());
          (0, import_merge.default)(this.configurations.themes.light, customConfig.theme);
        }
        if (lightTheme) (0, import_merge.default)(this.configurations.themes.light, lightTheme);
        if (darkTheme) (0, import_merge.default)(this.configurations.themes.dark, darkTheme);
      }
      if (((_d = customConfig.notifications) == null ? void 0 : _d.releases) !== void 0) {
        this.configurations.notifications.releases = customConfig.notifications.releases;
      }
      if (customConfig.tutorials !== void 0) {
        this.configurations.tutorials = customConfig.tutorials;
      }
    };
    this.createHook = (name) => {
      this.hooksDict[name] = createHook();
    };
    this.getAdminInjectedComponents = (moduleName, containerName, blockName) => {
      try {
        return this.admin.injectionZones[moduleName][containerName][blockName] || [];
      } catch (err) {
        console.error("Cannot get injected component", err);
        return [];
      }
    };
    this.getPlugin = (pluginId) => this.plugins[pluginId];
    this.registerHook = (name, fn) => {
      (0, import_invariant4.default)(this.hooksDict[name], `The hook ${name} is not defined. You are trying to register a hook that does not exist in the application.`);
      this.hooksDict[name].register(fn);
    };
    this.registerPlugin = (pluginConf) => {
      const plugin = new Plugin(pluginConf);
      this.plugins[plugin.pluginId] = plugin;
    };
    this.runHookSeries = (name, asynchronous = false) => asynchronous ? this.hooksDict[name].runSeriesAsync() : this.hooksDict[name].runSeries();
    this.runHookWaterfall = (name, initialValue, store) => {
      return this.hooksDict[name].runWaterfall(initialValue, store);
    };
    this.runHookParallel = (name) => this.hooksDict[name].runParallel();
    this.appPlugins = appPlugins || {};
    this.createCustomConfigurations(config ?? {});
    this.createHook(INJECT_COLUMN_IN_TABLE);
    this.createHook(INJECT_LIST_VIEW_FILTERS);
    this.createHook(MUTATE_COLLECTION_TYPES_LINKS);
    this.createHook(MUTATE_SINGLE_TYPES_LINKS);
    this.createHook(MUTATE_EDIT_VIEW_LAYOUT);
    this.router = new Router(getInitialRoutes());
  }
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/utils/getProjectType.mjs
var getProjectType = ({ isEE, planPriceId }) => {
  if (!isEE) {
    return "Community";
  }
  if (planPriceId == null ? void 0 : planPriceId.toLowerCase().includes("growth")) {
    return "Growth";
  }
  return "Enterprise";
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/render.mjs
var renderAdmin = async (mountNode, { plugins, customisations, features }) => {
  var _a, _b;
  if (!mountNode) {
    throw new Error("[@strapi/admin]: Could not find the root element to mount the admin app");
  }
  window.strapi = {
    /**
    * This ENV variable is passed from the strapi instance, by default no url is set
    * in the config and therefore the instance returns you an empty string so URLs are relative.
    *
    * To ensure that the backendURL is always set, we use the window.location.origin as a fallback.
    */
    backendURL: createAbsoluteUrl(process.env.STRAPI_ADMIN_BACKEND_URL),
    isEE: false,
    isTrial: false,
    telemetryDisabled: process.env.STRAPI_TELEMETRY_DISABLED === "true",
    future: {
      isEnabled: (name) => {
        var _a2;
        return ((_a2 = features == null ? void 0 : features.future) == null ? void 0 : _a2[name]) === true;
      }
    },
    // eslint-disable-next-line
    // @ts-ignore – there's pollution from the global scope of Node. Cannot use @ts-expect-error because of build:code and build:types context collision. Cannot use @ts-expect-error because of build:code and build:types context collision.
    features: {
      SSO: "sso",
      AUDIT_LOGS: "audit-logs",
      REVIEW_WORKFLOWS: "review-workflows",
      /**
      * If we don't get the license then we know it's not EE
      * so no feature is enabled.
      */
      isEnabled: () => false
    },
    projectType: "Community",
    flags: {
      nps: false,
      promoteEE: true,
      docLinks: true
    },
    // eslint-disable-next-line
    // @ts-ignore – there's pollution from the global scope of Node. Cannot use @ts-expect-error because of build:code and build:types context collision.
    ai: {
      enabled: true
    }
  };
  const { get } = getFetchClient();
  try {
    const { data: { data: { isEE, isTrial, features: features2, flags, ai, planPriceId } } } = await get("/admin/project-type");
    window.strapi.isEE = isEE;
    window.strapi.isTrialLicense = isTrial;
    window.strapi.flags = flags;
    window.strapi.features = {
      ...window.strapi.features,
      isEnabled: (featureName) => features2.some((feature) => feature.name === featureName)
    };
    window.strapi.projectType = getProjectType({
      isEE,
      planPriceId
    });
    window.strapi.ai = ai;
  } catch (err) {
    console.error(err);
  }
  const app = new StrapiApp({
    config: customisations == null ? void 0 : customisations.config,
    appPlugins: plugins
  });
  await app.register(customisations == null ? void 0 : customisations.register);
  await app.bootstrap(customisations == null ? void 0 : customisations.bootstrap);
  await app.loadTrads((_a = customisations == null ? void 0 : customisations.config) == null ? void 0 : _a.translations);
  (0, import_client.createRoot)(mountNode).render(app.render());
  if (typeof module !== "undefined" && module && "hot" in module && typeof module.hot === "object" && module.hot !== null && "accept" in module.hot && typeof module.hot.accept === "function") {
    module.hot.accept();
  }
  if (typeof ((_b = import.meta.hot) == null ? void 0 : _b.accept) === "function") {
    import.meta.hot.accept();
  }
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/DescriptionComponentRenderer.mjs
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var React7 = __toESM(require_react(), 1);
var import_isEqual = __toESM(require_isEqual(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/hooks/useForceUpdate.mjs
var React6 = __toESM(require_react(), 1);

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/hooks/useIsMounted.mjs
var React5 = __toESM(require_react(), 1);
var useIsMounted = () => {
  const isMounted = React5.useRef(false);
  React5.useLayoutEffect(() => {
    isMounted.current = true;
    return () => {
      isMounted.current = false;
    };
  }, []);
  return isMounted;
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/hooks/useForceUpdate.mjs
var useForceUpdate = () => {
  const [tick, update] = React6.useState();
  const isMounted = useIsMounted();
  const forceUpdate = React6.useCallback(() => {
    if (isMounted.current) {
      update(Math.random());
    }
  }, [
    isMounted,
    update
  ]);
  return [
    tick,
    forceUpdate
  ];
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/hooks/useThrottledCallback.mjs
var import_react7 = __toESM(require_react(), 1);
var import_throttle = __toESM(require_throttle(), 1);
var useThrottledCallback = (callback, wait, options) => {
  const throttledCallback = (0, import_react7.useMemo)(() => (0, import_throttle.default)(callback, wait, options), [
    callback,
    options,
    wait
  ]);
  return throttledCallback;
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/utils/shims.mjs
var requestIdleCallbackShim = (callback) => {
  const start = Date.now();
  return setTimeout(() => {
    callback({
      didTimeout: false,
      timeRemaining() {
        return Math.max(0, Date.now() - start);
      }
    });
  }, 1);
};
var _requestIdleCallback = typeof requestIdleCallback === "undefined" ? requestIdleCallbackShim : requestIdleCallback;
var cancelIdleCallbackShim = (handle) => {
  return clearTimeout(handle);
};
var _cancelIdleCallback = typeof cancelIdleCallback === "undefined" ? cancelIdleCallbackShim : cancelIdleCallback;

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/components/DescriptionComponentRenderer.mjs
var DescriptionComponentRenderer = ({ children, props, descriptions }) => {
  const statesRef = React7.useRef({});
  const [tick, forceUpdate] = useForceUpdate();
  const requestHandle = React7.useRef(null);
  const requestUpdate = React7.useCallback(() => {
    if (requestHandle.current) {
      _cancelIdleCallback(requestHandle.current);
    }
    requestHandle.current = _requestIdleCallback(() => {
      requestHandle.current = null;
      forceUpdate();
    });
  }, [
    forceUpdate
  ]);
  const throttledRequestUpdate = useThrottledCallback(requestUpdate, 60, {
    trailing: true
  });
  const update = React7.useCallback((id, description) => {
    if (description === null) {
      delete statesRef.current[id];
    } else {
      const current = statesRef.current[id];
      statesRef.current[id] = {
        ...current,
        value: {
          ...description,
          id
        }
      };
    }
    throttledRequestUpdate();
  }, [
    throttledRequestUpdate
  ]);
  const ids2 = React7.useMemo(() => descriptions.map((description) => getCompId(description)), [
    descriptions
  ]);
  const states = React7.useMemo(
    () => ids2.map((id) => {
      var _a;
      return (_a = statesRef.current[id]) == null ? void 0 : _a.value;
    }).filter((state) => state !== null && state !== void 0),
    /**
    * we leave tick in the deps to ensure the memo is recalculated when the `update` function  is called.
    * the `ids` will most likely be stable unless we get new actions, but we can't respond to the Description
    * Component changing the ref data in any other way.
    */
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      ids2,
      tick
    ]
  );
  return (0, import_jsx_runtime17.jsxs)(import_jsx_runtime17.Fragment, {
    children: [
      descriptions.map((description) => {
        const key = getCompId(description);
        return (0, import_jsx_runtime17.jsx)(MemoizedDescription, {
          id: key,
          description,
          props,
          update
        }, key);
      }),
      children(states)
    ]
  });
};
var Description = ({ description, id, props, update }) => {
  const comp = description(props);
  useShallowCompareEffect(() => {
    update(id, comp);
    return () => {
      update(id, null);
    };
  }, comp);
  return null;
};
var MemoizedDescription = React7.memo(Description, (prev, next) => (0, import_isEqual.default)(prev.props, next.props));
var ids = /* @__PURE__ */ new WeakMap();
var counter = 0;
function getCompId(comp) {
  const cachedComponent = comp;
  const cachedId = ids.get(cachedComponent);
  if (cachedId) return cachedId;
  const id = `${comp.name || comp.displayName || "<anonymous>"}-${counter++}`;
  ids.set(cachedComponent, id);
  return id;
}
var useShallowCompareMemoize = (value) => {
  const ref = React7.useRef(void 0);
  if (!(0, import_isEqual.default)(value, ref.current)) {
    ref.current = value;
  }
  return [
    ref.current
  ];
};
var useShallowCompareEffect = (callback, dependencies) => {
  React7.useEffect(callback, useShallowCompareMemoize(dependencies));
};

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/hooks/useInjectReducer.mjs
var import_react8 = __toESM(require_react(), 1);
function useInjectReducer(namespace, reducer2) {
  const store = useTypedStore();
  (0, import_react8.useEffect)(() => {
    store.injectReducer(namespace, reducer2);
  }, [
    store,
    namespace,
    reducer2
  ]);
}

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/hooks/useDebounce.mjs
var React8 = __toESM(require_react(), 1);
function useDebounce(value, delay) {
  const [debouncedValue, setDebouncedValue] = React8.useState(value);
  React8.useEffect(() => {
    const handler = setTimeout(() => {
      setDebouncedValue(value);
    }, delay);
    return () => {
      clearTimeout(handler);
    };
  }, [
    value,
    delay
  ]);
  return debouncedValue;
}

// node_modules/@strapi/strapi/node_modules/@strapi/admin/dist/admin/admin/src/utils/rulesEngine.mjs
var import_json_logic_js = __toESM(require_logic(), 1);
var ConditionSchema = object({
  dependsOn: string().min(1),
  operator: _enum([
    "is",
    "isNot"
  ]),
  value: union([
    string(),
    number(),
    boolean()
  ])
});
function createRulesEngine() {
  const generate = (condition) => {
    const { dependsOn, operator, value } = condition;
    const operatorsMap = {
      is: "==",
      isNot: "!="
    };
    if (!operatorsMap[operator]) {
      throw new Error(`Invalid operator: ${operator}`);
    }
    return {
      [operatorsMap[operator]]: [
        {
          var: dependsOn
        },
        value
      ]
    };
  };
  const validate = (condition) => {
    ConditionSchema.parse(condition);
  };
  const evaluate = (condition, data) => {
    try {
      return import_json_logic_js.default.apply(condition, data);
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      throw new Error(`Invalid condition: ${message}`);
    }
  };
  return {
    generate,
    validate,
    evaluate
  };
}

export {
  renderAdmin,
  DescriptionComponentRenderer,
  useInjectReducer,
  useDebounce,
  createRulesEngine
};
//# sourceMappingURL=chunk-2MXDMWQA.js.map
