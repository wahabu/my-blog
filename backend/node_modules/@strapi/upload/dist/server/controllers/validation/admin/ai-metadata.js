'use strict';

var utils = require('@strapi/utils');
var constants = require('../../../shared/constants.js');

const validateGenerateAIMetadataSchema = utils.yup.object().shape({
    fileIds: utils.yup.array().of(utils.yup.strapiID().required()).min(1).max(constants.AI_METADATA_MAX_FILES, `You can generate metadata for up to ${constants.AI_METADATA_MAX_FILES} assets at a time. Select fewer assets and try again.`).required()
}).noUnknown().required();
const validateGenerateAIMetadataBody = utils.validateYupSchema(validateGenerateAIMetadataSchema);

exports.validateGenerateAIMetadataBody = validateGenerateAIMetadataBody;
//# sourceMappingURL=ai-metadata.js.map
