'use strict';

var crypto = require('crypto');
var stream = require('stream');
var providers = require('../../../../../errors/providers.js');
var cappedWarnings = require('../../../../../utils/capped-warnings.js');
var link = require('../../../../queries/link.js');
var resolveLinkRef = require('./resolve-link-ref.js');

const isErrorWithCode = (error)=>{
    return error && typeof error.code === 'string';
};
const isForeignKeyConstraintError = (e)=>{
    const MYSQL_FK_ERROR_CODES = [
        '1452',
        '1557',
        '1216',
        '1217',
        '1451'
    ];
    const POSTGRES_FK_ERROR_CODE = '23503';
    const SQLITE_FK_ERROR_CODE = 'SQLITE_CONSTRAINT_FOREIGNKEY';
    if (isErrorWithCode(e) && e.code) {
        return [
            SQLITE_FK_ERROR_CODE,
            POSTGRES_FK_ERROR_CODE,
            ...MYSQL_FK_ERROR_CODES
        ].includes(e.code);
    }
    return e.message.toLowerCase().includes('foreign key constraint');
};
const isAbortedTransactionError = (e)=>{
    return isErrorWithCode(e) && e.code === '25P02';
};
const isPostgresDialect = (strapi)=>strapi.db?.dialect?.client === 'postgres';
const withSavepoint = async (trx, fn)=>{
    const savepoint = `sp_${crypto.randomUUID().replace(/-/g, '')}`;
    await trx.raw(`SAVEPOINT ${savepoint}`);
    try {
        const result = await fn();
        await trx.raw(`RELEASE SAVEPOINT ${savepoint}`);
        return result;
    } catch (error) {
        await trx.raw(`ROLLBACK TO SAVEPOINT ${savepoint}`);
        throw error;
    }
};
const formatSkippedLinksRestoreSummary = (unmapped, foreignKey)=>{
    const total = unmapped + foreignKey;
    if (total === 0) {
        return null;
    }
    const parts = [];
    if (unmapped > 0) {
        parts.push(`${unmapped} unmapped`);
    }
    if (foreignKey > 0) {
        parts.push(`${foreignKey} foreign key`);
    }
    return `Links restore skipped ${total} relation(s) (${parts.join(', ')}). Verify relations in the admin after transfer.`;
};
const createLinksWriteStream = (mapID, strapi, transaction, onWarning)=>{
    let skippedUnmapped = 0;
    let skippedForeignKey = 0;
    const warnings = cappedWarnings.createCappedWarningReporter(onWarning);
    // Only PostgreSQL aborts the surrounding transaction on a statement error
    // (25P02). MySQL and SQLite keep the transaction usable after an FK failure,
    // so per-link savepoints are unnecessary there.
    const useSavepoints = isPostgresDialect(strapi);
    return new stream.Writable({
        objectMode: true,
        async write (link$1, _encoding, callback) {
            await transaction?.attach(async (trx)=>{
                const { left, right } = link$1;
                const query = link.createLinkQuery(strapi, trx);
                const originalLeftRef = left.ref;
                const originalRightRef = right.ref;
                const mappedLeftRef = resolveLinkRef.resolveLinkRef(strapi, link$1, 'left', mapID);
                const mappedRightRef = resolveLinkRef.resolveLinkRef(strapi, link$1, 'right', mapID);
                // A missing mapping means the referenced row was never transferred
                // during the entities stage (e.g. an orphaned component or a dangling
                // reference in the source database). Falling back to the original ID
                // would either violate a foreign key constraint — which aborts the
                // whole transaction on PostgreSQL — or silently attach the link to an
                // unrelated row, so the link is skipped instead.
                if (mappedLeftRef === undefined || mappedRightRef === undefined) {
                    const missingRefs = [
                        ...mappedLeftRef === undefined ? [
                            `${left.type}:${originalLeftRef}`
                        ] : [],
                        ...mappedRightRef === undefined ? [
                            `${right.type}:${originalRightRef}`
                        ] : []
                    ].join(' and ');
                    warnings.warn(`Skipping link ${left.type}:${originalLeftRef} -> ${right.type}:${originalRightRef} because ${missingRefs} was not transferred during the entities stage.`);
                    skippedUnmapped += 1;
                    return callback(null);
                }
                left.ref = mappedLeftRef;
                right.ref = mappedRightRef;
                try {
                    if (trx && useSavepoints) {
                        await withSavepoint(trx, ()=>query().insert(link$1));
                    } else {
                        await query().insert(link$1);
                    }
                } catch (e) {
                    if (e instanceof Error) {
                        if (isForeignKeyConstraintError(e) || isAbortedTransactionError(e)) {
                            warnings.warn(`Skipping link ${left.type}:${originalLeftRef} -> ${right.type}:${originalRightRef} due to a foreign key constraint.`);
                            skippedForeignKey += 1;
                            return callback(null);
                        }
                        return callback(e);
                    }
                    return callback(new providers.ProviderTransferError(`An error happened while trying to import a ${left.type} link.`));
                }
                callback(null);
            });
        },
        final (callback) {
            const summary = formatSkippedLinksRestoreSummary(skippedUnmapped, skippedForeignKey);
            if (summary) {
                onWarning?.(summary);
            }
            callback();
        }
    });
};

exports.createLinksWriteStream = createLinksWriteStream;
exports.formatSkippedLinksRestoreSummary = formatSkippedLinksRestoreSummary;
//# sourceMappingURL=links.js.map
