import {
  buildValidParams as buildValidParams2
} from "./chunk-PAY54OVR.js";
import {
  AssigneeColumn,
  StageColumn,
  getDisplayName
} from "./chunk-Y5J4UCED.js";
import {
  getStageColorByHex
} from "./chunk-FOHWK26D.js";
import {
  LimitsModal,
  useReviewWorkflows
} from "./chunk-GNGPGEY7.js";
import {
  CHARGEBEE_STAGES_PER_WORKFLOW_ENTITLEMENT_NAME,
  CHARGEBEE_WORKFLOW_ENTITLEMENT_NAME,
  FEATURE_ID,
  PLUGIN_ID as PLUGIN_ID2,
  useGetStagesQuery,
  useUpdateAssigneeMutation,
  useUpdateStageMutation
} from "./chunk-GF7A6RRV.js";
import {
  useTypedSelector as useTypedSelector2
} from "./chunk-TGL3FZCW.js";
import {
  ReleaseActionMenu,
  ReleaseActionOptions
} from "./chunk-6FZVUD42.js";
import {
  index
} from "./chunk-U62LYJ26.js";
import {
  PERMISSIONS as PERMISSIONS5,
  PLUGIN_ID,
  pluginId as pluginId4
} from "./chunk-EBVWQ5LF.js";
import {
  getTimezoneOffset,
  useCreateManyReleaseActionsMutation,
  useCreateReleaseActionMutation,
  useGetMappedEntriesInReleasesQuery,
  useGetReleasesForEntryQuery,
  useGetReleasesQuery
} from "./chunk-U2WIPNFF.js";
import {
  PERMISSIONS as PERMISSIONS4,
  getTranslation,
  i18nApi,
  pluginId as pluginId3,
  useGetLocalesQuery,
  useGetSettingsQuery
} from "./chunk-INMU44PI.js";
import {
  Drawer,
  TruncatedText,
  abortUpload,
  cancelUpload,
  closeUploadProgress,
  getTranslationKey,
  selectAggregateProgress,
  selectCountBasedProgress,
  selectIsGeneratingMetadata,
  selectMetadataOutcome,
  selectMetadataProgress,
  selectReportsByteProgress,
  toggleMinimize,
  uploadProgressReducer,
  useRetryCancelledFilesMutation,
  useTypedDispatch,
  useTypedSelector
} from "./chunk-Z3ONVEQG.js";
import {
  AssetGridList,
  AudioPreview,
  Breadcrumbs,
  CopyLinkButton,
  EditAssetContent,
  EditAssetDialog,
  EditFolderContent,
  EditFolderDialog,
  EmptyAssets,
  FilterList,
  FilterPopover,
  FolderCard,
  FolderCardBody,
  FolderCardBodyAction,
  FolderGridList,
  SortPicker,
  TableList,
  UploadAssetDialog,
  VideoPreview,
  containsAssetFilter,
  createAssetUrl,
  displayedFilters,
  getFileIconComponent,
  prefixFileUrlWithBackendUrl,
  rawFileToAsset,
  useAssets,
  useFolder,
  useFolders,
  useMediaLibraryPermissions,
  usePersistentState,
  useSelectionState
} from "./chunk-QEFSEVBH.js";
import {
  ASSET_TYPES
} from "./chunk-RMQCWWFZ.js";
import {
  useConfig
} from "./chunk-C2WVOW7C.js";
import {
  useTracking
} from "./chunk-OESLMHCS.js";
import {
  ASSET_SOURCES,
  PERMISSIONS as PERMISSIONS3,
  getTrad,
  localStorageKeys,
  pluginId as pluginId2,
  pluginPkg,
  viewOptions
} from "./chunk-GEAZSL6W.js";
import {
  PERMISSIONS as PERMISSIONS2
} from "./chunk-PDGPTUUZ.js";
import {
  PERMISSIONS,
  reducer,
  reducer2
} from "./chunk-A3EIWUOS.js";
import {
  pluginId
} from "./chunk-5QJFIL32.js";
import {
  useDocumentActions
} from "./chunk-O3ADBBC4.js";
import {
  buildValidParams,
  useContentManagerContext,
  useDocument,
  useDocumentLayout
} from "./chunk-JPBDOX5J.js";
import {
  useDebounce
} from "./chunk-SG3YXALT.js";
import {
  useAIAvailability
} from "./chunk-K4JPZJYK.js";
import {
  useLicenseLimits
} from "./chunk-SLUE2XUX.js";
import {
  Table,
  useTable
} from "./chunk-XNMTWOF7.js";
import {
  useAdminUsers
} from "./chunk-AOU4ZZD3.js";
import {
  useField,
  useForm
} from "./chunk-2IWTCEYS.js";
import {
  useRBAC
} from "./chunk-SHEU3VT4.js";
import {
  Page,
  useAPIErrorHandler
} from "./chunk-WZNNPD7U.js";
import {
  useAuth,
  useQueryParams,
  withEncodedUserParams
} from "./chunk-HHY6TNZ5.js";
import {
  useNotification
} from "./chunk-52FV5OAT.js";
import {
  adminApi,
  isFetchError
} from "./chunk-NE2H5B4K.js";
import {
  renderAdmin
} from "./chunk-2MXDMWQA.js";
import {
  require_baseUniq
} from "./chunk-FH7EDXAD.js";
import {
  require_isArrayLikeObject,
  require_omit
} from "./chunk-BC4MOUEJ.js";
import {
  require_cloneDeep
} from "./chunk-RDVCP4FY.js";
import {
  require_baseRest
} from "./chunk-36WB3I5J.js";
import {
  Form,
  Formik,
  useFormik
} from "./chunk-45QSVGD5.js";
import {
  create3 as create,
  create4 as create2,
  create6 as create3,
  require_has
} from "./chunk-OG6FDUVQ.js";
import {
  K
} from "./chunk-Y5S667H6.js";
import {
  require_baseFlatten,
  require_get,
  require_lib,
  skipToken
} from "./chunk-NZZAX2AB.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import {
  Link,
  NavLink,
  matchPath,
  useNavigate,
  useParams
} from "./chunk-QBM2MGBP.js";
import {
  useDispatch
} from "./chunk-AHJOALQ2.js";
import {
  $e,
  A,
  Ad,
  Bt,
  C,
  Ct,
  Dd,
  Ed,
  Hd,
  Ki,
  Ld,
  Mt,
  Nr,
  Oa,
  Od,
  Pd,
  Ql,
  Qt,
  S,
  Vi,
  Wd,
  Xd,
  Yl,
  _d,
  be,
  bu,
  cr,
  cu,
  dr,
  gd,
  kt,
  st,
  tu,
  yn
} from "./chunk-7ZODKSNG.js";
import {
  A3,
  B3,
  Cn,
  F5,
  In,
  Jn,
  L3,
  M3,
  Mn,
  O3,
  Qn,
  R5,
  T5,
  V3,
  Vn,
  W2,
  Y5,
  en,
  h5,
  i5,
  k3,
  l5,
  nn,
  o5,
  on,
  p2,
  t3,
  u3,
  v5,
  w5
} from "./chunk-D4B4CG5U.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  Et,
  ft,
  gt
} from "./chunk-4N2AQQ2K.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __commonJS,
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/lodash/union.js
var require_union = __commonJS({
  "node_modules/lodash/union.js"(exports, module) {
    var baseFlatten = require_baseFlatten();
    var baseRest = require_baseRest();
    var baseUniq = require_baseUniq();
    var isArrayLikeObject = require_isArrayLikeObject();
    var union2 = baseRest(function(arrays) {
      return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true));
    });
    module.exports = union2;
  }
});

// node_modules/@strapi/content-type-builder/dist/admin/reducers.mjs
var reducers = {
  [`content-type-builder_formModal`]: reducer2,
  [`content-type-builder_dataManagerProvider`]: reducer
};

// node_modules/@strapi/content-type-builder/dist/admin/utils/formAPI.mjs
var import_cloneDeep = __toESM(require_cloneDeep(), 1);
var import_get = __toESM(require_get(), 1);
var formsAPI = {
  components: {
    inputs: {},
    add({ id, component }) {
      if (this.inputs[id] === void 0) {
        this.inputs[id] = component;
      }
    }
  },
  types: {
    attribute: {},
    contentType: {
      validators: [],
      form: {
        advanced: [],
        base: []
      }
    },
    component: {
      validators: [],
      form: {
        advanced: [],
        base: []
      }
    }
  },
  contentTypeSchemaMutations: [],
  addContentTypeSchemaMutation(cb) {
    this.contentTypeSchemaMutations.push(cb);
  },
  extendContentType({ validator, form: { advanced, base } }) {
    const { contentType } = this.types;
    if (validator !== void 0) {
      contentType.validators.push(validator);
    }
    contentType.form.advanced.push(advanced);
    contentType.form.base.push(base);
  },
  extendFields(fields, { validator, form: { advanced, base } }) {
    const formType = this.types.attribute;
    fields.forEach((field) => {
      if (!formType[field]) {
        formType[field] = {
          validators: [],
          form: {
            advanced: [],
            base: []
          }
        };
      }
      if (validator !== void 0) {
        formType[field].validators.push(validator);
      }
      formType[field].form.advanced.push(advanced);
      formType[field].form.base.push(base);
    });
  },
  getAdvancedForm(target, props = null) {
    const advancedForms = (0, import_get.default)(this.types, [
      ...target,
      "form",
      "advanced"
    ], []);
    const sectionsToAdd = advancedForms.reduce((acc, current) => {
      const sections = current(props);
      return [
        ...acc,
        ...sections
      ];
    }, []);
    return sectionsToAdd;
  },
  makeCustomFieldValidator(attributeShape, validator, ...validatorArgs) {
    if (validator === void 0) {
      return attributeShape;
    }
    return attributeShape.shape({
      options: create3().shape(validator(validatorArgs))
    });
  },
  makeValidator(target, initShape, ...args) {
    const validators = (0, import_get.default)(this.types, [
      ...target,
      "validators"
    ], []);
    const pluginOptionsShape = validators.reduce((acc, current) => {
      const pluginOptionShape = current(args);
      return {
        ...acc,
        ...pluginOptionShape
      };
    }, {});
    return initShape.shape({
      pluginOptions: create3().shape(pluginOptionsShape)
    });
  },
  mutateContentTypeSchema(data, initialData) {
    let enhancedData = (0, import_cloneDeep.default)(data);
    const refData = (0, import_cloneDeep.default)(initialData);
    this.contentTypeSchemaMutations.forEach((cb) => {
      enhancedData = cb(enhancedData, refData);
    });
    return enhancedData;
  }
};

// node_modules/@strapi/content-type-builder/dist/admin/utils/prefixPluginTranslations.mjs
var prefixPluginTranslations = (trad, pluginId5) => {
  return Object.keys(trad).reduce((acc, current) => {
    acc[`${pluginId5}.${current}`] = trad[current];
    return acc;
  }, {});
};

// node_modules/@strapi/content-type-builder/dist/admin/index.mjs
function __variableDynamicImportRuntime1__(path) {
  switch (path) {
    case "./translations/ar.json":
      return import("./ar.json-FYZHJW3V.js");
    case "./translations/cs.json":
      return import("./cs.json-CEIKGOMX.js");
    case "./translations/da.json":
      return import("./da.json-QB2KGKUC.js");
    case "./translations/de.json":
      return import("./de.json-CFD4J53M.js");
    case "./translations/en.json":
      return import("./en.json-CLVZWS4Y.js");
    case "./translations/es.json":
      return import("./es.json-TSUAMKLN.js");
    case "./translations/fr.json":
      return import("./fr.json-AK465OLF.js");
    case "./translations/id.json":
      return import("./id.json-NSDN6MWL.js");
    case "./translations/it.json":
      return import("./it.json-7LDCMAEO.js");
    case "./translations/ja.json":
      return import("./ja.json-5S7DYD3Q.js");
    case "./translations/ko.json":
      return import("./ko.json-D37S6TCX.js");
    case "./translations/ms.json":
      return import("./ms.json-DLZRXFHN.js");
    case "./translations/nl.json":
      return import("./nl.json-WXYUVPS2.js");
    case "./translations/pl.json":
      return import("./pl.json-RZMPHANQ.js");
    case "./translations/pt-BR.json":
      return import("./pt-BR.json-ZWGHVL4E.js");
    case "./translations/pt.json":
      return import("./pt.json-EZH6TO4O.js");
    case "./translations/ru.json":
      return import("./ru.json-MLZAAZYF.js");
    case "./translations/sk.json":
      return import("./sk.json-PQ7ACHOO.js");
    case "./translations/sv.json":
      return import("./sv.json-RCTDATUR.js");
    case "./translations/th.json":
      return import("./th.json-U3VOVTFW.js");
    case "./translations/tr.json":
      return import("./tr.json-OAHEABHI.js");
    case "./translations/uk.json":
      return import("./uk.json-FUB4QKAV.js");
    case "./translations/zh-Hans.json":
      return import("./zh-Hans.json-XI5SXKQT.js");
    case "./translations/zh.json":
      return import("./zh.json-ASEVIWFD.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
var index2 = {
  register(app) {
    app.addReducers(reducers);
    app.addMenuLink({
      to: `plugins/${pluginId}`,
      icon: A3,
      intlLabel: {
        id: `${pluginId}.plugin.name`,
        defaultMessage: "Content-Type Builder"
      },
      permissions: PERMISSIONS.main,
      Component: () => import("./App-MIEGIQ3Z.js"),
      position: 5
    });
    app.registerPlugin({
      id: pluginId,
      name: pluginId,
      // Internal APIs exposed by the CTB for the other plugins to use
      apis: {
        forms: formsAPI
      }
    });
  },
  bootstrap() {
  },
  async registerTrads({ locales }) {
    const importedTrads = await Promise.all(locales.map((locale) => {
      return __variableDynamicImportRuntime1__(`./translations/${locale}.json`).then(({ default: data }) => {
        return {
          data: prefixPluginTranslations(data, pluginId),
          locale
        };
      }).catch(() => {
        return {
          data: {},
          locale
        };
      });
    }));
    return Promise.resolve(importedTrads);
  }
};

// node_modules/@strapi/email/dist/admin/utils/prefixPluginTranslations.mjs
var prefixPluginTranslations2 = (trad, pluginId5) => {
  return Object.keys(trad).reduce((acc, current) => {
    acc[`${pluginId5}.${current}`] = trad[current];
    return acc;
  }, {});
};

// node_modules/@strapi/email/dist/admin/index.mjs
function __variableDynamicImportRuntime1__2(path) {
  switch (path) {
    case "./translations/ar.json":
      return import("./ar.json-BOWHATT2.js");
    case "./translations/cs.json":
      return import("./cs.json-TFQXUKRJ.js");
    case "./translations/da.json":
      return import("./da.json-ZK7G23YZ.js");
    case "./translations/de.json":
      return import("./de.json-2JOVIX6O.js");
    case "./translations/en.json":
      return import("./en.json-7CIJO7HU.js");
    case "./translations/es.json":
      return import("./es.json-YH4GBQU6.js");
    case "./translations/fr.json":
      return import("./fr.json-I33ZYKPJ.js");
    case "./translations/id.json":
      return import("./id.json-MEE2O3QY.js");
    case "./translations/it.json":
      return import("./it.json-UAMTDJP2.js");
    case "./translations/ja.json":
      return import("./ja.json-ZZBMEC5A.js");
    case "./translations/ko.json":
      return import("./ko.json-BKFXRR5W.js");
    case "./translations/ms.json":
      return import("./ms.json-IMZTAT47.js");
    case "./translations/nl.json":
      return import("./nl.json-V7BDQGZM.js");
    case "./translations/pl.json":
      return import("./pl.json-GHLFA2UR.js");
    case "./translations/pt-BR.json":
      return import("./pt-BR.json-7T2OPS7D.js");
    case "./translations/pt.json":
      return import("./pt.json-YNX2TIPA.js");
    case "./translations/ru.json":
      return import("./ru.json-XJGAL54Z.js");
    case "./translations/sk.json":
      return import("./sk.json-2S3WPNPN.js");
    case "./translations/th.json":
      return import("./th.json-P3KJX3GL.js");
    case "./translations/tr.json":
      return import("./tr.json-O7G2DM3Y.js");
    case "./translations/uk.json":
      return import("./uk.json-P3AVEUSD.js");
    case "./translations/vi.json":
      return import("./vi.json-AD67C3K6.js");
    case "./translations/zh-Hans.json":
      return import("./zh-Hans.json-EUSPT53O.js");
    case "./translations/zh.json":
      return import("./zh.json-ZCMRIBPL.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
var admin = {
  // TODO typing app in strapi/types as every plugin needs it
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  register(app) {
    app.addSettingsLink({
      id: "email",
      intlLabel: {
        id: "email.SettingsNav.section-label",
        defaultMessage: "Email Plugin"
      }
    }, {
      intlLabel: {
        id: "email.Settings.email.plugin.title",
        defaultMessage: "Settings"
      },
      id: "settings",
      to: "email",
      Component: () => import("./Settings-ZWLGCQTS.js").then((mod) => ({
        default: mod.ProtectedSettingsPage
      })),
      permissions: PERMISSIONS2.settings
    });
    app.registerPlugin({
      id: "email",
      name: "email"
    });
  },
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  bootstrap() {
  },
  async registerTrads({ locales }) {
    const importedTrads = await Promise.all(locales.map((locale) => {
      return __variableDynamicImportRuntime1__2(`./translations/${locale}.json`).then(({ default: data }) => {
        return {
          data: prefixPluginTranslations2(data, "email"),
          locale
        };
      }).catch(() => {
        return {
          data: {},
          locale
        };
      });
    }));
    return Promise.resolve(importedTrads);
  }
};

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryDialog/MediaLibraryDialog.mjs
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var React6 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/AssetDialog.mjs
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var React5 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/hooks/useModalQueryParams.mjs
var React = __toESM(require_react(), 1);
var import_qs = __toESM(require_lib(), 1);
var useModalQueryParams = (initialState) => {
  const { trackUsage } = useTracking();
  const { config: { data: config } } = useConfig();
  const [queryObject, setQueryObject] = React.useState({
    page: 1,
    sort: "updatedAt:DESC",
    pageSize: 10,
    filters: {
      $and: []
    },
    ...initialState
  });
  React.useEffect(() => {
    if (config && "sort" in config && "pageSize" in config) {
      setQueryObject((prevQuery) => ({
        ...prevQuery,
        sort: config.sort,
        pageSize: config.pageSize
      }));
    }
  }, [
    config
  ]);
  const handleChangeFilters = (nextFilters) => {
    if (nextFilters) {
      trackUsage("didFilterMediaLibraryElements", {
        location: "content-manager",
        filter: Object.keys(nextFilters[nextFilters.length - 1])[0]
      });
      setQueryObject((prev) => ({
        ...prev,
        page: 1,
        filters: {
          $and: nextFilters
        }
      }));
    }
  };
  const handleChangePageSize = (pageSize) => {
    setQueryObject((prev) => ({
      ...prev,
      pageSize: typeof pageSize === "string" ? parseInt(pageSize, 10) : pageSize,
      page: 1
    }));
  };
  const handeChangePage = (page) => {
    setQueryObject((prev) => ({
      ...prev,
      page
    }));
  };
  const handleChangeSort = (sort) => {
    if (sort) {
      trackUsage("didSortMediaLibraryElements", {
        location: "content-manager",
        sort
      });
      setQueryObject((prev) => ({
        ...prev,
        sort
      }));
    }
  };
  const handleChangeSearch = (_q) => {
    if (_q) {
      setQueryObject((prev) => ({
        ...prev,
        _q,
        page: 1
      }));
    } else {
      const newState = {
        page: 1
      };
      Object.keys(queryObject).forEach((key) => {
        if (![
          "page",
          "_q"
        ].includes(key)) {
          newState[key] = queryObject[key];
        }
      });
      setQueryObject(newState);
    }
  };
  const handleChangeFolder = (folder, folderPath) => {
    setQueryObject((prev) => ({
      ...prev,
      folder: folder ?? null,
      folderPath
    }));
  };
  return [
    {
      queryObject,
      rawQuery: (0, import_qs.stringify)(queryObject, {
        encode: false
      })
    },
    {
      onChangeFilters: handleChangeFilters,
      onChangeFolder: handleChangeFolder,
      onChangePage: handeChangePage,
      onChangePageSize: handleChangePageSize,
      onChangeSort: handleChangeSort,
      onChangeSearch: handleChangeSearch
    }
  ];
};

// node_modules/@strapi/upload/dist/admin/utils/toSingularTypes.mjs
var toSingularTypes = (types) => {
  if (!types) {
    return [];
  }
  return types.map((type) => type.substring(0, type.length - 1));
};

// node_modules/@strapi/upload/dist/admin/utils/getAllowedFiles.mjs
var getAllowedFiles = (pluralTypes, files) => {
  if (!pluralTypes) {
    return files;
  }
  const singularTypes = toSingularTypes(pluralTypes);
  const allowedFiles = files.filter((file) => {
    var _a;
    const fileType = (_a = file == null ? void 0 : file.mime) == null ? void 0 : _a.split("/")[0];
    if (!fileType) {
      return singularTypes.includes("file");
    }
    if (singularTypes.includes("file") && ![
      "video",
      "image",
      "audio"
    ].includes(fileType)) {
      return true;
    }
    return singularTypes.includes(fileType);
  });
  return allowedFiles;
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/AssetDialog.mjs
var import_qs6 = __toESM(require_lib(), 1);

// node_modules/@strapi/upload/dist/admin/utils/moveElement.mjs
var move = (array, oldIndex, newIndex) => {
  if (newIndex >= array.length) {
    newIndex = array.length - 1;
  }
  array.splice(newIndex, 0, array.splice(oldIndex, 1)[0]);
  return array;
};
var moveElement = (array, index4, offset) => {
  const newIndex = index4 + offset;
  return move(array, index4, newIndex);
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/BrowseStep.mjs
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/upload/dist/admin/utils/getBreadcrumbDataCM.mjs
var getBreadcrumbDataCM = (folder) => {
  var _a;
  const data = [
    {
      id: null,
      label: {
        id: getTrad("plugin.name"),
        defaultMessage: "Media Library"
      }
    }
  ];
  if ((_a = folder == null ? void 0 : folder.parent) == null ? void 0 : _a.parent) {
    data.push([]);
  }
  if (folder == null ? void 0 : folder.parent) {
    data.push({
      id: folder.parent.id,
      label: folder.parent.name,
      path: folder.parent.path
    });
  }
  if (folder) {
    data.push({
      id: folder.id,
      label: folder.name,
      path: folder.path
    });
  }
  return data;
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/BrowseStep.mjs
var import_qs4 = __toESM(require_lib(), 1);

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/Filters.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var React2 = __toESM(require_react(), 1);
var import_qs2 = __toESM(require_lib(), 1);
var Filters = ({ appliedFilters, onChangeFilters }) => {
  const [open, setOpen] = React2.useState(false);
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime.jsxs)(Xd.Root, {
    open,
    onOpenChange: setOpen,
    children: [
      (0, import_jsx_runtime.jsx)(Xd.Trigger, {
        children: (0, import_jsx_runtime.jsx)(Ct, {
          variant: "tertiary",
          startIcon: (0, import_jsx_runtime.jsx)(Y5, {}),
          size: "S",
          children: formatMessage({
            id: "app.utils.filters",
            defaultMessage: "Filters"
          })
        })
      }),
      (0, import_jsx_runtime.jsx)(FilterPopover, {
        onToggle: () => setOpen((prev) => !prev),
        displayedFilters,
        filters: appliedFilters,
        onSubmit: onChangeFilters
      }),
      appliedFilters && (0, import_jsx_runtime.jsx)(FilterList, {
        appliedFilters,
        filtersSchema: displayedFilters,
        onRemoveFilter: onChangeFilters
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/PageSize.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var PageSize = ({ onChangePageSize, pageSize }) => {
  const { formatMessage } = useIntl();
  const handleChange = (value) => {
    onChangePageSize(Number(value));
  };
  return (0, import_jsx_runtime2.jsxs)(C, {
    children: [
      (0, import_jsx_runtime2.jsxs)(cr, {
        size: "S",
        "aria-label": formatMessage({
          id: "components.PageFooter.select",
          defaultMessage: "Entries per page"
        }),
        onChange: handleChange,
        value: pageSize.toString(),
        children: [
          (0, import_jsx_runtime2.jsx)(dr, {
            value: "10",
            children: "10"
          }),
          (0, import_jsx_runtime2.jsx)(dr, {
            value: "20",
            children: "20"
          }),
          (0, import_jsx_runtime2.jsx)(dr, {
            value: "50",
            children: "50"
          }),
          (0, import_jsx_runtime2.jsx)(dr, {
            value: "100",
            children: "100"
          })
        ]
      }),
      (0, import_jsx_runtime2.jsx)(S, {
        paddingLeft: 2,
        children: (0, import_jsx_runtime2.jsx)(A, {
          textColor: "neutral600",
          tag: "label",
          htmlFor: "page-size",
          children: formatMessage({
            id: "components.PageFooter.select",
            defaultMessage: "Entries per page"
          })
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/PaginationFooter/PaginationFooter.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/PaginationFooter/Pagination.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var React3 = __toESM(require_react(), 1);
var PaginationContext = React3.createContext({
  activePage: 1,
  pageCount: 1
});
var usePagination = () => React3.useContext(PaginationContext);
var Pagination = ({ children, activePage, pageCount, label = "pagination" }) => {
  const paginationValue = React3.useMemo(() => ({
    activePage,
    pageCount
  }), [
    activePage,
    pageCount
  ]);
  return (0, import_jsx_runtime3.jsx)(PaginationContext.Provider, {
    value: paginationValue,
    children: (0, import_jsx_runtime3.jsx)(S, {
      tag: "nav",
      "aria-label": label,
      children: (0, import_jsx_runtime3.jsx)(C, {
        tag: "ul",
        gap: 1,
        children
      })
    })
  });
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/PaginationFooter/PaginationFooter.mjs
var PaginationText = gt(A)`
  line-height: revert;
`;
var linkWrapperStyles = ft`
  padding: ${({ theme }) => theme.spaces[3]};
  border-radius: ${({ theme }) => theme.borderRadius};
  box-shadow: ${({ $active, theme }) => $active ? theme.shadows.filterShadow : void 0};
  text-decoration: none;
  display: flex;
  position: relative;
  outline: none;

  &:after {
    transition-property: all;
    transition-duration: 0.2s;
    border-radius: 8px;
    content: '';
    position: absolute;
    top: -4px;
    bottom: -4px;
    left: -4px;
    right: -4px;
    border: 2px solid transparent;
  }

  &:focus-visible {
    outline: none;

    &:after {
      border-radius: 8px;
      content: '';
      position: absolute;
      top: -5px;
      bottom: -5px;
      left: -5px;
      right: -5px;
      border: 2px solid ${(props) => props.theme.colors.primary600};
    }
  }
`;
var LinkWrapperButton = gt.button`
  ${linkWrapperStyles}
`;
var LinkWrapperDiv = gt.div`
  ${linkWrapperStyles}
`;
LinkWrapperButton.defaultProps = {
  type: "button"
};
var PageLinkWrapper = gt(LinkWrapperButton)`
  color: ${({ theme, $active }) => $active ? theme.colors.primary700 : theme.colors.neutral800};
  background: ${({ theme, $active }) => $active ? theme.colors.neutral0 : void 0};

  &:hover {
    box-shadow: ${({ theme }) => theme.shadows.filterShadow};
  }
`;
var ActionLinkWrapper = gt(LinkWrapperButton)`
  font-size: 1.1rem;
  svg path {
    fill: ${(p) => p["aria-disabled"] ? p.theme.colors.neutral300 : p.theme.colors.neutral600};
  }

  &:focus,
  &:hover {
    svg path {
      fill: ${(p) => p["aria-disabled"] ? p.theme.colors.neutral300 : p.theme.colors.neutral700};
    }
  }

  ${(p) => p["aria-disabled"] ? `
  pointer-events: none;
    ` : void 0}
`;
var DotsWrapper = gt(LinkWrapperDiv)`
  color: ${({ theme }) => theme.colors.neutral800};
`;
var PreviousLink = ({ children, ...props }) => {
  const { activePage } = usePagination();
  const disabled = activePage === 1;
  return (0, import_jsx_runtime4.jsx)("li", {
    children: (0, import_jsx_runtime4.jsxs)(ActionLinkWrapper, {
      "aria-disabled": disabled,
      tabIndex: disabled ? -1 : void 0,
      ...props,
      children: [
        (0, import_jsx_runtime4.jsx)($e, {
          children
        }),
        (0, import_jsx_runtime4.jsx)(i5, {
          "aria-hidden": true
        })
      ]
    })
  });
};
var NextLink = ({ children, ...props }) => {
  const { activePage, pageCount } = usePagination();
  const disabled = activePage === pageCount;
  return (0, import_jsx_runtime4.jsx)("li", {
    children: (0, import_jsx_runtime4.jsxs)(ActionLinkWrapper, {
      "aria-disabled": disabled,
      tabIndex: disabled ? -1 : void 0,
      ...props,
      children: [
        (0, import_jsx_runtime4.jsx)($e, {
          children
        }),
        (0, import_jsx_runtime4.jsx)(v5, {
          "aria-hidden": true
        })
      ]
    })
  });
};
var PageLink = ({ number, children, ...props }) => {
  const { activePage } = usePagination();
  const isActive = activePage === number;
  return (0, import_jsx_runtime4.jsx)("li", {
    children: (0, import_jsx_runtime4.jsxs)(PageLinkWrapper, {
      ...props,
      $active: isActive,
      children: [
        (0, import_jsx_runtime4.jsx)($e, {
          children
        }),
        (0, import_jsx_runtime4.jsx)(PaginationText, {
          "aria-hidden": true,
          variant: "pi",
          fontWeight: isActive ? "bold" : "",
          children: number
        })
      ]
    })
  });
};
var Dots = ({ children, ...props }) => (0, import_jsx_runtime4.jsx)("li", {
  children: (0, import_jsx_runtime4.jsxs)(DotsWrapper, {
    ...props,
    as: "div",
    children: [
      (0, import_jsx_runtime4.jsx)($e, {
        children
      }),
      (0, import_jsx_runtime4.jsx)(PaginationText, {
        "aria-hidden": true,
        small: true,
        children: "…"
      })
    ]
  })
});
var PaginationFooter = ({ activePage, onChangePage, pagination: { pageCount } }) => {
  const { formatMessage } = useIntl();
  const previousActivePage = activePage - 1;
  const nextActivePage = activePage + 1;
  const firstLinks = [
    (0, import_jsx_runtime4.jsx)(PageLink, {
      number: 1,
      onClick: () => {
        onChangePage(1);
      },
      children: formatMessage({
        id: "components.pagination.go-to",
        defaultMessage: "Go to page {page}"
      }, {
        page: 1
      })
    }, 1)
  ];
  if (pageCount <= 4) {
    const links = Array.from({
      length: pageCount
    }).map((_, i) => i + 1).map((number) => {
      return (0, import_jsx_runtime4.jsx)(PageLink, {
        number,
        onClick: () => onChangePage(number),
        children: formatMessage({
          id: "components.pagination.go-to",
          defaultMessage: "Go to page {page}"
        }, {
          page: number
        })
      }, number);
    });
    return (0, import_jsx_runtime4.jsxs)(Pagination, {
      activePage,
      pageCount,
      children: [
        (0, import_jsx_runtime4.jsx)(PreviousLink, {
          onClick: () => onChangePage(previousActivePage),
          children: formatMessage({
            id: "components.pagination.go-to-previous",
            defaultMessage: "Go to previous page"
          })
        }),
        links,
        (0, import_jsx_runtime4.jsx)(NextLink, {
          onClick: () => onChangePage(nextActivePage),
          children: formatMessage({
            id: "components.pagination.go-to-next",
            defaultMessage: "Go to next page"
          })
        })
      ]
    });
  }
  let firstLinksToCreate = [];
  const lastLinks = [];
  let lastLinksToCreate = [];
  const middleLinks = [];
  if (pageCount > 1) {
    lastLinks.push((0, import_jsx_runtime4.jsx)(PageLink, {
      number: pageCount,
      onClick: () => onChangePage(pageCount),
      children: formatMessage({
        id: "components.pagination.go-to",
        defaultMessage: "Go to page {page}"
      }, {
        page: pageCount
      })
    }, pageCount));
  }
  if (activePage === 1 && pageCount >= 3) {
    firstLinksToCreate = [
      2
    ];
  }
  if (activePage === 2 && pageCount >= 3) {
    if (pageCount === 5) {
      firstLinksToCreate = [
        2,
        3,
        4
      ];
    } else if (pageCount === 3) {
      firstLinksToCreate = [
        2
      ];
    } else {
      firstLinksToCreate = [
        2,
        3
      ];
    }
  }
  if (activePage === 4 && pageCount >= 3) {
    firstLinksToCreate = [
      2
    ];
  }
  if (activePage === pageCount && pageCount >= 3) {
    lastLinksToCreate = [
      pageCount - 1
    ];
  }
  if (activePage === pageCount - 2 && pageCount > 3) {
    lastLinksToCreate = [
      activePage + 1,
      activePage,
      activePage - 1
    ];
  }
  if (activePage === pageCount - 3 && pageCount > 3 && activePage > 5) {
    lastLinksToCreate = [
      activePage + 2,
      activePage + 1,
      activePage,
      activePage - 1
    ];
  }
  if (activePage === pageCount - 1 && pageCount > 3) {
    lastLinksToCreate = [
      activePage,
      activePage - 1
    ];
  }
  lastLinksToCreate.forEach((number) => {
    lastLinks.unshift((0, import_jsx_runtime4.jsxs)(PageLink, {
      number,
      onClick: () => onChangePage(number),
      children: [
        "Go to page ",
        number
      ]
    }, number));
  });
  firstLinksToCreate.forEach((number) => {
    firstLinks.push((0, import_jsx_runtime4.jsx)(PageLink, {
      number,
      onClick: () => onChangePage(number),
      children: formatMessage({
        id: "components.pagination.go-to",
        defaultMessage: "Go to page {page}"
      }, {
        page: number
      })
    }, number));
  });
  if (![
    1,
    2
  ].includes(activePage) && activePage <= pageCount - 3 && firstLinks.length + lastLinks.length < 6) {
    const middleLinksToCreate = [
      activePage - 1,
      activePage,
      activePage + 1
    ];
    middleLinksToCreate.forEach((number) => {
      middleLinks.push((0, import_jsx_runtime4.jsx)(PageLink, {
        number,
        onClick: () => onChangePage(number),
        children: formatMessage({
          id: "components.pagination.go-to",
          defaultMessage: "Go to page {page}"
        }, {
          page: number
        })
      }, number));
    });
  }
  const shouldShowDotsAfterFirstLink = pageCount > 5 || pageCount === 5 && (activePage === 1 || activePage === 5);
  const shouldShowMiddleDots = middleLinks.length > 2 && activePage > 4 && pageCount > 5;
  const beforeDotsLinksLength = shouldShowMiddleDots ? pageCount - activePage - 1 : pageCount - firstLinks.length - lastLinks.length;
  const afterDotsLength = shouldShowMiddleDots ? pageCount - firstLinks.length - lastLinks.length : pageCount - activePage - 1;
  return (0, import_jsx_runtime4.jsxs)(Pagination, {
    activePage,
    pageCount,
    children: [
      (0, import_jsx_runtime4.jsx)(PreviousLink, {
        onClick: () => onChangePage(previousActivePage),
        children: formatMessage({
          id: "components.pagination.go-to-previous",
          defaultMessage: "Go to previous page"
        })
      }),
      firstLinks,
      shouldShowMiddleDots && (0, import_jsx_runtime4.jsx)(Dots, {
        children: formatMessage({
          id: "components.pagination.remaining-links",
          defaultMessage: "And {number} other links"
        }, {
          number: beforeDotsLinksLength
        })
      }),
      middleLinks,
      shouldShowDotsAfterFirstLink && (0, import_jsx_runtime4.jsx)(Dots, {
        children: formatMessage({
          id: "components.pagination.remaining-links",
          defaultMessage: "And {number} other links"
        }, {
          number: afterDotsLength
        })
      }),
      lastLinks,
      (0, import_jsx_runtime4.jsx)(NextLink, {
        onClick: () => onChangePage(nextActivePage),
        children: formatMessage({
          id: "components.pagination.go-to-next",
          defaultMessage: "Go to next page"
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/SearchAsset/SearchAsset.mjs
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var React4 = __toESM(require_react(), 1);
var import_qs3 = __toESM(require_lib(), 1);
var SearchAsset = ({ onChangeSearch, queryValue = null }) => {
  const { formatMessage } = useIntl();
  const { trackUsage } = useTracking();
  const [isOpen, setIsOpen] = React4.useState(!!queryValue);
  const [value, setValue] = React4.useState(queryValue || "");
  const wrapperRef = React4.useRef(null);
  React4.useLayoutEffect(() => {
    if (isOpen) {
      setTimeout(() => {
        var _a, _b;
        (_b = (_a = wrapperRef.current) == null ? void 0 : _a.querySelector("input")) == null ? void 0 : _b.focus();
      }, 0);
    }
  }, [
    isOpen
  ]);
  const handleToggle = () => {
    setIsOpen((prev) => !prev);
  };
  const handleClear = () => {
    handleToggle();
    onChangeSearch(null);
  };
  const handleSubmit = (e) => {
    e.preventDefault();
    e.stopPropagation();
    trackUsage("didSearchMediaLibraryElements", {
      location: "content-manager"
    });
    onChangeSearch(value);
  };
  if (isOpen) {
    return (0, import_jsx_runtime5.jsx)("div", {
      ref: wrapperRef,
      children: (0, import_jsx_runtime5.jsx)(Ql, {
        onSubmit: handleSubmit,
        children: (0, import_jsx_runtime5.jsx)(Yl, {
          name: "search",
          onClear: handleClear,
          onChange: (e) => setValue(e.target.value),
          clearLabel: formatMessage({
            id: getTrad("search.clear.label"),
            defaultMessage: "Clear the search"
          }),
          "aria-label": "search",
          size: "S",
          value,
          placeholder: formatMessage({
            id: getTrad("search.placeholder"),
            defaultMessage: "e.g: the first dog on the moon"
          }),
          children: formatMessage({
            id: getTrad("search.label"),
            defaultMessage: "Search for an asset"
          })
        })
      })
    });
  }
  return (0, import_jsx_runtime5.jsx)(be, {
    label: "Search",
    onClick: handleToggle,
    children: (0, import_jsx_runtime5.jsx)(Cn, {})
  });
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/utils/isSelectable.mjs
var isSelectable = (allowedTypes, mime = "") => {
  if (!mime) return false;
  const fileType = mime.split("/")[0];
  return allowedTypes.includes(fileType) || allowedTypes.includes("file") && ![
    "video",
    "image",
    "audio"
  ].includes(fileType);
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/BrowseStep/BrowseStep.mjs
var TypographyMaxWidth = gt(A)`
  max-width: 100%;
`;
var ActionContainer = gt(S)`
  svg {
    path {
      fill: ${({ theme }) => theme.colors.neutral500};
    }
  }
`;
var BrowseStep = ({ allowedTypes = [], assets: rawAssets, canCreate, canRead, folders = [], multiple = false, onAddAsset, onChangeFilters, onChangePage, onChangePageSize, onChangeSearch, onChangeSort, onChangeFolder, onEditAsset, onEditFolder, onSelectAllAsset, onSelectAsset, pagination, queryObject, selectedAssets }) => {
  var _a, _b, _c;
  const { formatMessage } = useIntl();
  const [view, setView] = usePersistentState(localStorageKeys.modalView, viewOptions.GRID);
  const isGridView = view === viewOptions.GRID;
  const { data: currentFolder, isLoading: isCurrentFolderLoading } = useFolder(queryObject == null ? void 0 : queryObject.folder, {
    enabled: canRead && !!(queryObject == null ? void 0 : queryObject.folder)
  });
  const singularTypes = toSingularTypes(allowedTypes);
  const assets = rawAssets.map((asset) => ({
    ...asset,
    isSelectable: isSelectable(singularTypes, asset == null ? void 0 : asset.mime),
    type: "asset"
  }));
  const breadcrumbs = !isCurrentFolderLoading ? getBreadcrumbDataCM(currentFolder) : void 0;
  const allAllowedAsset = getAllowedFiles(allowedTypes, assets);
  const areAllAssetSelected = allAllowedAsset.length > 0 && selectedAssets.length > 0 && allAllowedAsset.every((asset) => selectedAssets.findIndex((currAsset) => currAsset.id === asset.id) !== -1);
  const hasSomeAssetSelected = allAllowedAsset.some((asset) => selectedAssets.findIndex((currAsset) => currAsset.id === asset.id) !== -1);
  const isSearching = !!(queryObject == null ? void 0 : queryObject._q);
  const isFiltering = !!((_b = (_a = queryObject == null ? void 0 : queryObject.filters) == null ? void 0 : _a.$and) == null ? void 0 : _b.length) && queryObject.filters.$and.length > 0;
  const isSearchingOrFiltering = isSearching || isFiltering;
  const assetCount = assets.length;
  const folderCount = folders.length;
  const handleClickFolderCard = (...args) => {
    onChangeSearch("");
    onChangeFolder(...args);
  };
  return (0, import_jsx_runtime6.jsxs)(S, {
    children: [
      onSelectAllAsset && (0, import_jsx_runtime6.jsx)(S, {
        paddingBottom: 4,
        children: (0, import_jsx_runtime6.jsxs)(C, {
          justifyContent: "space-between",
          alignItems: "flex-start",
          children: [
            (assetCount > 0 || folderCount > 0 || isFiltering) && (0, import_jsx_runtime6.jsxs)(C, {
              gap: 2,
              wrap: "wrap",
              children: [
                multiple && isGridView && (0, import_jsx_runtime6.jsx)(C, {
                  paddingLeft: 2,
                  paddingRight: 2,
                  background: "neutral0",
                  hasRadius: true,
                  borderColor: "neutral200",
                  height: "3.2rem",
                  children: (0, import_jsx_runtime6.jsx)(Bt, {
                    "aria-label": formatMessage({
                      id: getTrad("bulk.select.label"),
                      defaultMessage: "Select all assets"
                    }),
                    checked: !areAllAssetSelected && hasSomeAssetSelected ? "indeterminate" : areAllAssetSelected,
                    onCheckedChange: onSelectAllAsset
                  })
                }),
                isGridView && (0, import_jsx_runtime6.jsx)(SortPicker, {
                  onChangeSort,
                  value: queryObject == null ? void 0 : queryObject.sort
                }),
                (0, import_jsx_runtime6.jsx)(Filters, {
                  appliedFilters: (_c = queryObject == null ? void 0 : queryObject.filters) == null ? void 0 : _c.$and,
                  onChangeFilters
                })
              ]
            }),
            (assetCount > 0 || folderCount > 0 || isSearching) && (0, import_jsx_runtime6.jsxs)(C, {
              marginLeft: "auto",
              shrink: 0,
              gap: 2,
              children: [
                (0, import_jsx_runtime6.jsx)(ActionContainer, {
                  paddingTop: 1,
                  paddingBottom: 1,
                  children: (0, import_jsx_runtime6.jsx)(be, {
                    label: isGridView ? formatMessage({
                      id: "view-switch.list",
                      defaultMessage: "List View"
                    }) : formatMessage({
                      id: "view-switch.grid",
                      defaultMessage: "Grid View"
                    }),
                    onClick: () => setView(isGridView ? viewOptions.LIST : viewOptions.GRID),
                    children: isGridView ? (0, import_jsx_runtime6.jsx)(B3, {}) : (0, import_jsx_runtime6.jsx)(t3, {})
                  })
                }),
                (0, import_jsx_runtime6.jsx)(SearchAsset, {
                  onChangeSearch,
                  queryValue: queryObject._q || ""
                })
              ]
            })
          ]
        })
      }),
      canRead && (breadcrumbs == null ? void 0 : breadcrumbs.length) && breadcrumbs.length > 0 && currentFolder && (0, import_jsx_runtime6.jsx)(S, {
        paddingTop: 3,
        children: (0, import_jsx_runtime6.jsx)(Breadcrumbs, {
          onChangeFolder,
          label: formatMessage({
            id: getTrad("header.breadcrumbs.nav.label"),
            defaultMessage: "Folders navigation"
          }),
          breadcrumbs,
          currentFolderId: queryObject == null ? void 0 : queryObject.folder
        })
      }),
      assetCount === 0 && folderCount === 0 && (0, import_jsx_runtime6.jsx)(S, {
        paddingBottom: 6,
        children: (0, import_jsx_runtime6.jsx)(EmptyAssets, {
          size: "S",
          count: 6,
          action: canCreate && !isFiltering && !isSearching && (0, import_jsx_runtime6.jsx)(Ct, {
            variant: "secondary",
            startIcon: (0, import_jsx_runtime6.jsx)(en, {}),
            onClick: onAddAsset,
            children: formatMessage({
              id: getTrad("header.actions.add-assets"),
              defaultMessage: "Add new assets"
            })
          }),
          content: (
            // eslint-disable-next-line no-nested-ternary
            isSearchingOrFiltering ? formatMessage({
              id: getTrad("list.assets-empty.title-withSearch"),
              defaultMessage: "There are no assets with the applied filters"
            }) : canCreate && !isSearching ? formatMessage({
              id: getTrad("list.assets.empty"),
              defaultMessage: "Upload your first assets..."
            }) : formatMessage({
              id: getTrad("list.assets.empty.no-permissions"),
              defaultMessage: "The asset list is empty"
            })
          )
        })
      }),
      !isGridView && (folderCount > 0 || assetCount > 0) && (0, import_jsx_runtime6.jsx)(TableList, {
        allowedTypes,
        assetCount,
        folderCount,
        indeterminate: !areAllAssetSelected && hasSomeAssetSelected,
        isFolderSelectionAllowed: false,
        onChangeSort,
        onChangeFolder: handleClickFolderCard,
        onEditAsset,
        onEditFolder,
        onSelectOne: onSelectAsset,
        onSelectAll: onSelectAllAsset,
        rows: [
          ...folders.map((folder) => ({
            ...folder,
            type: "folder"
          })),
          ...assets
        ],
        selected: selectedAssets,
        shouldDisableBulkSelect: !multiple,
        sortQuery: (queryObject == null ? void 0 : queryObject.sort) ?? ""
      }),
      isGridView && (0, import_jsx_runtime6.jsxs)(import_jsx_runtime6.Fragment, {
        children: [
          folderCount > 0 && (0, import_jsx_runtime6.jsx)(FolderGridList, {
            title: (isSearchingOrFiltering && assetCount > 0 || !isSearchingOrFiltering) && formatMessage({
              id: getTrad("list.folders.title"),
              defaultMessage: "Folders ({count})"
            }, {
              count: folderCount
            }) || "",
            children: folders.map((folder) => {
              var _a2, _b2;
              return (0, import_jsx_runtime6.jsx)(Ld.Item, {
                col: 3,
                m: 4,
                s: 6,
                xs: 12,
                direction: "column",
                alignItems: "stretch",
                children: (0, import_jsx_runtime6.jsx)(FolderCard, {
                  ariaLabel: folder.name,
                  id: `folder-${folder.id}`,
                  onClick: () => handleClickFolderCard(folder.id, folder.path),
                  cardActions: onEditFolder && (0, import_jsx_runtime6.jsx)(be, {
                    withTooltip: false,
                    label: formatMessage({
                      id: getTrad("list.folder.edit"),
                      defaultMessage: "Edit folder"
                    }),
                    onClick: () => onEditFolder(folder),
                    children: (0, import_jsx_runtime6.jsx)(nn, {})
                  }),
                  children: (0, import_jsx_runtime6.jsx)(FolderCardBody, {
                    children: (0, import_jsx_runtime6.jsx)(FolderCardBodyAction, {
                      onClick: () => handleClickFolderCard(folder.id, folder.path),
                      children: (0, import_jsx_runtime6.jsxs)(C, {
                        tag: "h2",
                        direction: "column",
                        alignItems: "start",
                        maxWidth: "100%",
                        children: [
                          (0, import_jsx_runtime6.jsxs)(TypographyMaxWidth, {
                            fontWeight: "semiBold",
                            ellipsis: true,
                            textColor: "neutral800",
                            children: [
                              folder.name,
                              (0, import_jsx_runtime6.jsx)($e, {
                                children: "-"
                              })
                            ]
                          }),
                          (0, import_jsx_runtime6.jsx)(TypographyMaxWidth, {
                            tag: "span",
                            textColor: "neutral600",
                            variant: "pi",
                            ellipsis: true,
                            children: formatMessage({
                              id: getTrad("list.folder.subtitle"),
                              defaultMessage: "{folderCount, plural, one {# folder} other {# folders}}, {filesCount, plural, one {# asset} other {# assets}}"
                            }, {
                              folderCount: (_a2 = folder.children) == null ? void 0 : _a2.count,
                              filesCount: (_b2 = folder.files) == null ? void 0 : _b2.count
                            })
                          })
                        ]
                      })
                    })
                  })
                })
              }, `folder-${folder.id}`);
            })
          }),
          assetCount > 0 && folderCount > 0 && (0, import_jsx_runtime6.jsx)(S, {
            paddingTop: 6,
            children: (0, import_jsx_runtime6.jsx)(yn, {})
          }),
          assetCount > 0 && (0, import_jsx_runtime6.jsx)(S, {
            paddingTop: 6,
            children: (0, import_jsx_runtime6.jsx)(AssetGridList, {
              allowedTypes,
              size: "S",
              assets,
              onSelectAsset,
              selectedAssets,
              onEditAsset,
              title: (!isSearchingOrFiltering || isSearchingOrFiltering && folderCount > 0) && queryObject.page === 1 && formatMessage({
                id: getTrad("list.assets.title"),
                defaultMessage: "Assets ({count})"
              }, {
                count: assetCount
              }) || ""
            })
          })
        ]
      }),
      pagination.pageCount > 0 && (0, import_jsx_runtime6.jsxs)(C, {
        justifyContent: "space-between",
        paddingTop: 4,
        position: "relative",
        zIndex: 1,
        overflow: "hidden",
        children: [
          (0, import_jsx_runtime6.jsx)(PageSize, {
            pageSize: queryObject.pageSize,
            onChangePageSize
          }),
          (0, import_jsx_runtime6.jsx)(PaginationFooter, {
            activePage: queryObject.page,
            onChangePage,
            pagination
          })
        ]
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/DialogFooter.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var DialogFooter = ({ onClose, onValidate }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime7.jsxs)(_d.Footer, {
    children: [
      (0, import_jsx_runtime7.jsx)(Ct, {
        onClick: onClose,
        variant: "tertiary",
        children: formatMessage({
          id: "app.components.Button.cancel",
          defaultMessage: "Cancel"
        })
      }),
      onValidate && (0, import_jsx_runtime7.jsx)(Ct, {
        onClick: onValidate,
        children: formatMessage({
          id: "global.finish",
          defaultMessage: "Finish"
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/SelectedStep/SelectedStep.mjs
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var import_qs5 = __toESM(require_lib(), 1);
var SelectedStep = ({ selectedAssets, onSelectAsset, onReorderAsset }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime8.jsxs)(C, {
    direction: "column",
    alignItems: "stretch",
    gap: 4,
    children: [
      (0, import_jsx_runtime8.jsxs)(C, {
        gap: 0,
        direction: "column",
        alignItems: "start",
        children: [
          (0, import_jsx_runtime8.jsx)(A, {
            variant: "pi",
            fontWeight: "bold",
            textColor: "neutral800",
            children: formatMessage({
              id: getTrad("list.assets.to-upload"),
              defaultMessage: "{number, plural, =0 {No asset} one {1 asset} other {# assets}} ready to upload"
            }, {
              number: selectedAssets.length
            })
          }),
          (0, import_jsx_runtime8.jsx)(A, {
            variant: "pi",
            textColor: "neutral600",
            children: formatMessage({
              id: getTrad("modal.upload-list.sub-header-subtitle"),
              defaultMessage: "Manage the assets before adding them to the Media Library"
            })
          })
        ]
      }),
      (0, import_jsx_runtime8.jsx)(AssetGridList, {
        size: "S",
        assets: selectedAssets,
        onSelectAsset,
        selectedAssets,
        onReorderAsset
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/components/AssetDialog/AssetDialog.mjs
var LoadingBody = gt(C)`
  /* 80px are coming from the Tabs component that is not included in the ModalBody */
  min-height: ${() => `calc(60dvh + 8rem)`};
`;
var AssetContent = ({ allowedTypes = [], folderId = null, onClose, onAddAsset, onAddFolder, onChangeFolder, onValidate, multiple = false, initiallySelectedAssets = [], trackedLocation }) => {
  const [assetToEdit, setAssetToEdit] = React5.useState(void 0);
  const [folderToEdit, setFolderToEdit] = React5.useState(void 0);
  const { formatMessage } = useIntl();
  const { canRead, canCreate, isLoading: isLoadingPermissions, canUpdate, canCopyLink, canDownload } = useMediaLibraryPermissions();
  const [{ queryObject }, { onChangeFilters, onChangePage, onChangePageSize, onChangeSort, onChangeSearch, onChangeFolder: onChangeFolderParam }] = useModalQueryParams({
    folder: folderId
  });
  const { data: { pagination, results: assets } = {}, isLoading: isLoadingAssets, error: errorAssets } = useAssets({
    skipWhen: !canRead,
    query: queryObject
  });
  const { data: folders, isLoading: isLoadingFolders, error: errorFolders } = useFolders({
    enabled: canRead && !containsAssetFilter(queryObject) && (pagination == null ? void 0 : pagination.page) === 1,
    query: queryObject
  });
  const [selectedAssets, { selectOne, selectOnly, setSelections, selectMultiple, deselectMultiple }] = useSelectionState([
    "id"
  ], initiallySelectedAssets);
  const handleSelectAllAssets = () => {
    const allowedAssets = getAllowedFiles(allowedTypes, assets);
    if (!multiple) {
      return void 0;
    }
    const alreadySelected = allowedAssets.filter((asset) => selectedAssets.findIndex((selectedAsset) => selectedAsset.id === asset.id) !== -1);
    if (alreadySelected.length > 0) {
      deselectMultiple(alreadySelected);
    } else {
      selectMultiple(allowedAssets);
    }
  };
  const handleSelectAsset = (asset) => {
    return multiple ? selectOne(asset) : selectOnly(asset);
  };
  const isLoading = isLoadingPermissions || isLoadingAssets || isLoadingFolders;
  const hasError = errorAssets || errorFolders;
  const [activeTab, setActiveTab] = React5.useState(selectedAssets.length > 0 ? "selected" : "browse");
  if (isLoading) {
    return (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, {
      children: [
        (0, import_jsx_runtime9.jsx)(_d.Header, {
          children: (0, import_jsx_runtime9.jsx)(_d.Title, {
            children: formatMessage({
              id: getTrad("header.actions.add-assets"),
              defaultMessage: "Add new assets"
            })
          })
        }),
        (0, import_jsx_runtime9.jsx)(LoadingBody, {
          justifyContent: "center",
          paddingTop: 4,
          paddingBottom: 4,
          children: (0, import_jsx_runtime9.jsx)(kt, {
            children: formatMessage({
              id: getTrad("content.isLoading"),
              defaultMessage: "Content is loading."
            })
          })
        }),
        (0, import_jsx_runtime9.jsx)(DialogFooter, {
          onClose
        })
      ]
    });
  }
  if (hasError) {
    return (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, {
      children: [
        (0, import_jsx_runtime9.jsx)(_d.Header, {
          children: (0, import_jsx_runtime9.jsx)(_d.Title, {
            children: formatMessage({
              id: getTrad("header.actions.add-assets"),
              defaultMessage: "Add new assets"
            })
          })
        }),
        (0, import_jsx_runtime9.jsx)(Page.Error, {}),
        (0, import_jsx_runtime9.jsx)(DialogFooter, {
          onClose
        })
      ]
    });
  }
  if (!canRead) {
    return (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, {
      children: [
        (0, import_jsx_runtime9.jsx)(_d.Header, {
          children: (0, import_jsx_runtime9.jsx)(_d.Title, {
            children: formatMessage({
              id: getTrad("header.actions.add-assets"),
              defaultMessage: "Add new assets"
            })
          })
        }),
        (0, import_jsx_runtime9.jsx)(Page.NoPermissions, {}),
        (0, import_jsx_runtime9.jsx)(DialogFooter, {
          onClose
        })
      ]
    });
  }
  if (assetToEdit) {
    return (0, import_jsx_runtime9.jsx)(EditAssetContent, {
      onClose: () => setAssetToEdit(void 0),
      asset: assetToEdit,
      canUpdate,
      canCopyLink,
      canDownload,
      trackedLocation
    });
  }
  if (folderToEdit) {
    return (0, import_jsx_runtime9.jsx)(EditFolderContent, {
      folder: folderToEdit,
      onClose: () => setFolderToEdit(void 0),
      location: "content-manager",
      parentFolderId: queryObject == null ? void 0 : queryObject.folder
    });
  }
  const handleMoveItem = (hoverIndex, destIndex) => {
    const offset = destIndex - hoverIndex;
    const orderedAssetsClone = selectedAssets.slice();
    const nextAssets = moveElement(orderedAssetsClone, hoverIndex, offset);
    setSelections(nextAssets);
  };
  const handleFolderChange = (folderId2, folderPath) => {
    onChangeFolder(folderId2);
    if (onChangeFolderParam) {
      onChangeFolderParam(folderId2, folderPath);
    }
  };
  return (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, {
    children: [
      (0, import_jsx_runtime9.jsx)(_d.Header, {
        children: (0, import_jsx_runtime9.jsx)(_d.Title, {
          children: formatMessage({
            id: getTrad("header.actions.add-assets"),
            defaultMessage: "Add new assets"
          })
        })
      }),
      (0, import_jsx_runtime9.jsxs)(TabsRoot, {
        variant: "simple",
        value: activeTab,
        onValueChange: setActiveTab,
        children: [
          (0, import_jsx_runtime9.jsxs)(C, {
            paddingLeft: 8,
            paddingRight: 8,
            paddingTop: 6,
            justifyContent: "space-between",
            children: [
              (0, import_jsx_runtime9.jsxs)(cu.List, {
                children: [
                  (0, import_jsx_runtime9.jsx)(cu.Trigger, {
                    value: "browse",
                    children: formatMessage({
                      id: getTrad("modal.nav.browse"),
                      defaultMessage: "Browse"
                    })
                  }),
                  (0, import_jsx_runtime9.jsxs)(cu.Trigger, {
                    value: "selected",
                    children: [
                      formatMessage({
                        id: getTrad("modal.header.select-files"),
                        defaultMessage: "Selected files"
                      }),
                      (0, import_jsx_runtime9.jsx)(Nr, {
                        marginLeft: 2,
                        children: selectedAssets.length
                      })
                    ]
                  })
                ]
              }),
              (0, import_jsx_runtime9.jsxs)(C, {
                gap: 2,
                children: [
                  (0, import_jsx_runtime9.jsx)(Ct, {
                    variant: "secondary",
                    onClick: () => onAddFolder({
                      folderId: queryObject == null ? void 0 : queryObject.folder
                    }),
                    children: formatMessage({
                      id: getTrad("modal.upload-list.sub-header.add-folder"),
                      defaultMessage: "Add folder"
                    })
                  }),
                  (0, import_jsx_runtime9.jsx)(Ct, {
                    onClick: () => onAddAsset({
                      folderId: queryObject == null ? void 0 : queryObject.folder
                    }),
                    children: formatMessage({
                      id: getTrad("modal.upload-list.sub-header.button"),
                      defaultMessage: "Add more assets"
                    })
                  })
                ]
              })
            ]
          }),
          (0, import_jsx_runtime9.jsx)(yn, {}),
          (0, import_jsx_runtime9.jsx)(st, {
            children: (0, import_jsx_runtime9.jsxs)(_d.Body, {
              children: [
                (0, import_jsx_runtime9.jsx)(cu.Content, {
                  value: "browse",
                  children: (0, import_jsx_runtime9.jsx)(BrowseStep, {
                    allowedTypes,
                    assets,
                    canCreate,
                    canRead,
                    folders,
                    onSelectAsset: handleSelectAsset,
                    selectedAssets,
                    multiple,
                    onSelectAllAsset: handleSelectAllAssets,
                    onEditAsset: setAssetToEdit,
                    onEditFolder: setFolderToEdit,
                    pagination,
                    queryObject,
                    onAddAsset,
                    onChangeFilters: (filters) => onChangeFilters(filters),
                    onChangeFolder: handleFolderChange,
                    onChangePage,
                    onChangePageSize,
                    onChangeSort: (sort) => onChangeSort(sort),
                    onChangeSearch
                  })
                }),
                (0, import_jsx_runtime9.jsx)(cu.Content, {
                  value: "selected",
                  children: (0, import_jsx_runtime9.jsx)(SelectedStep, {
                    selectedAssets,
                    onSelectAsset: handleSelectAsset,
                    onReorderAsset: handleMoveItem
                  })
                })
              ]
            })
          })
        ]
      }),
      (0, import_jsx_runtime9.jsx)(DialogFooter, {
        onClose,
        onValidate: () => onValidate(selectedAssets)
      })
    ]
  });
};
var AssetDialog = ({ open = false, onClose, ...restProps }) => {
  return (0, import_jsx_runtime9.jsx)(_d.Root, {
    open,
    onOpenChange: onClose,
    children: (0, import_jsx_runtime9.jsx)(_d.Content, {
      children: (0, import_jsx_runtime9.jsx)(AssetContent, {
        onClose,
        ...restProps
      })
    })
  });
};
var TabsRoot = gt(cu.Root)`
  display: flex;
  flex-direction: column;
  overflow: hidden;
`;

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryDialog/MediaLibraryDialog.mjs
var STEPS = {
  AssetSelect: "SelectAsset",
  AssetUpload: "UploadAsset",
  FolderCreate: "FolderCreate"
};
var MediaLibraryDialog = ({ onClose, onSelectAssets, allowedTypes = [
  "files",
  "images",
  "videos",
  "audios"
], multiple = true, initiallySelectedAssets = [] }) => {
  const [step, setStep] = React6.useState(STEPS.AssetSelect);
  const [folderId, setFolderId] = React6.useState(null);
  switch (step) {
    case STEPS.AssetSelect:
      return (0, import_jsx_runtime10.jsx)(AssetDialog, {
        allowedTypes,
        folderId,
        open: true,
        onClose,
        onValidate: onSelectAssets,
        initiallySelectedAssets,
        onAddAsset: () => setStep(STEPS.AssetUpload),
        onAddFolder: () => setStep(STEPS.FolderCreate),
        onChangeFolder: (folderId2) => setFolderId(folderId2),
        multiple
      });
    case STEPS.FolderCreate:
      return (0, import_jsx_runtime10.jsx)(EditFolderDialog, {
        open: true,
        onClose: () => setStep(STEPS.AssetSelect),
        parentFolderId: folderId
      });
    default:
      return (0, import_jsx_runtime10.jsx)(UploadAssetDialog, {
        open: true,
        onClose: () => setStep(STEPS.AssetSelect),
        folderId
      });
  }
};

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryInput/MediaLibraryInput.mjs
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
var React9 = __toESM(require_react(), 1);
var import_qs10 = __toESM(require_lib(), 1);

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryInput/Carousel/CarouselAssets.mjs
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var React8 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryInput/Carousel/CarouselAsset.mjs
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var import_qs7 = __toESM(require_lib(), 1);
var DocAsset = gt(C)`
  background: linear-gradient(180deg, #ffffff 0%, #f6f6f9 121.48%);
`;
var VideoPreviewWrapper = gt(S)`
  canvas,
  video {
    max-width: 100%;
    height: 124px;
  }
`;
var AudioPreviewWrapper = gt(S)`
  canvas,
  audio {
    max-width: 100%;
  }
`;
var CarouselAsset = ({ asset }) => {
  var _a, _b, _c;
  if ((_a = asset.mime) == null ? void 0 : _a.includes(ASSET_TYPES.Video)) {
    return (0, import_jsx_runtime11.jsx)(VideoPreviewWrapper, {
      height: "100%",
      children: (0, import_jsx_runtime11.jsx)(VideoPreview, {
        url: createAssetUrl(asset, true),
        mime: asset.mime,
        alt: asset.alternativeText || asset.name
      })
    });
  }
  if ((_b = asset.mime) == null ? void 0 : _b.includes(ASSET_TYPES.Audio)) {
    return (0, import_jsx_runtime11.jsx)(AudioPreviewWrapper, {
      children: (0, import_jsx_runtime11.jsx)(AudioPreview, {
        url: createAssetUrl(asset, true),
        alt: asset.alternativeText || asset.name
      })
    });
  }
  if ((_c = asset.mime) == null ? void 0 : _c.includes(ASSET_TYPES.Image)) {
    const assetUrl = createAssetUrl(asset, true);
    if (!assetUrl) return null;
    const cacheBustedUrl = asset.isUrlSigned ? assetUrl : `${assetUrl}${assetUrl.includes("?") ? "&" : "?"}updatedAt=${asset.updatedAt}`;
    const crossOrigin = !asset.isLocal && asset.isUrlSigned ? "anonymous" : void 0;
    return (0, import_jsx_runtime11.jsx)(S, {
      tag: "img",
      crossOrigin,
      maxHeight: "100%",
      maxWidth: "100%",
      src: cacheBustedUrl,
      alt: asset.alternativeText || asset.name
    });
  }
  const IconComponent = getFileIconComponent(asset.ext);
  return (0, import_jsx_runtime11.jsx)(DocAsset, {
    width: "100%",
    height: "100%",
    justifyContent: "center",
    hasRadius: true,
    children: (0, import_jsx_runtime11.jsx)(IconComponent, {
      "aria-label": asset.alternativeText || asset.name,
      width: "24px",
      height: "32px"
    })
  });
};

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryInput/Carousel/CarouselAssetActions.mjs
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
var import_qs8 = __toESM(require_lib(), 1);
var CarouselAssetActions = ({ asset, onDeleteAsset, onAddAsset, onEditAsset }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime12.jsxs)(Ad, {
    children: [
      onAddAsset && (0, import_jsx_runtime12.jsx)(be, {
        label: formatMessage({
          id: getTrad("control-card.add"),
          defaultMessage: "Add"
        }),
        onClick: () => onAddAsset(asset),
        children: (0, import_jsx_runtime12.jsx)(en, {})
      }),
      (0, import_jsx_runtime12.jsx)(CopyLinkButton, {
        url: prefixFileUrlWithBackendUrl(asset.url)
      }),
      onDeleteAsset && (0, import_jsx_runtime12.jsx)(be, {
        label: formatMessage({
          id: "global.delete",
          defaultMessage: "Delete"
        }),
        onClick: () => onDeleteAsset(asset),
        children: (0, import_jsx_runtime12.jsx)(In, {})
      }),
      onEditAsset && (0, import_jsx_runtime12.jsx)(be, {
        label: formatMessage({
          id: getTrad("control-card.edit"),
          defaultMessage: "edit"
        }),
        onClick: onEditAsset,
        children: (0, import_jsx_runtime12.jsx)(nn, {})
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryInput/Carousel/EmptyStateAsset.mjs
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
var React7 = __toESM(require_react(), 1);
var import_qs9 = __toESM(require_lib(), 1);
var TextAlignTypography = gt(A)`
  align-items: center;
`;
var EmptyStateAsset = ({ disabled = false, onClick, onDropAsset }) => {
  const { formatMessage } = useIntl();
  const [dragOver, setDragOver] = React7.useState(false);
  const handleDragEnter = (e) => {
    e.preventDefault();
    setDragOver(true);
  };
  const handleDragLeave = (e) => {
    if (!e.currentTarget.contains(e.relatedTarget)) {
      setDragOver(false);
    }
  };
  const handleDragOver = (e) => {
    e.preventDefault();
  };
  const handleDrop = async (e) => {
    var _a;
    e.preventDefault();
    if ((_a = e == null ? void 0 : e.dataTransfer) == null ? void 0 : _a.files) {
      const files = Array.from(e.dataTransfer.files);
      const assets = await Promise.all(files.map((file) => rawFileToAsset(file, ASSET_SOURCES.Computer)));
      onDropAsset(assets);
    }
    setDragOver(false);
  };
  return (0, import_jsx_runtime13.jsxs)(C, {
    borderStyle: dragOver ? "dashed" : void 0,
    borderWidth: dragOver ? "1px" : void 0,
    borderColor: dragOver ? "primary600" : void 0,
    direction: "column",
    justifyContent: "center",
    alignItems: "center",
    height: "100%",
    width: "100%",
    tag: "button",
    type: "button",
    disabled,
    onClick,
    onDragEnter: handleDragEnter,
    onDragLeave: handleDragLeave,
    onDragOver: handleDragOver,
    onDrop: handleDrop,
    gap: 3,
    style: {
      cursor: disabled ? "not-allowed" : "pointer"
    },
    children: [
      (0, import_jsx_runtime13.jsx)(on, {
        "aria-hidden": true,
        width: "3.2rem",
        height: "3.2rem",
        fill: disabled ? "neutral400" : "primary600"
      }),
      (0, import_jsx_runtime13.jsx)(TextAlignTypography, {
        variant: "pi",
        fontWeight: "bold",
        textColor: "neutral600",
        style: {
          textAlign: "center"
        },
        tag: "span",
        children: formatMessage({
          id: getTrad("mediaLibraryInput.placeholder"),
          defaultMessage: "Click to add an asset or drag and drop one in this area"
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryInput/Carousel/CarouselAssets.mjs
var CarouselAssets = React8.forwardRef(({ assets, disabled = false, error, hint, label, labelAction, onAddAsset, onDeleteAsset, onDeleteAssetFromMediaLibrary, onDropAsset, onEditAsset, onNext, onPrevious, onDoubleClickAsset, required = false, selectedAssetIndex, trackedLocation }, forwardedRef) => {
  const { formatMessage } = useIntl();
  const [isEditingAsset, setIsEditingAsset] = React8.useState(false);
  const currentAsset = assets[selectedAssetIndex];
  const canEditMedia = !disabled && onEditAsset;
  return (0, import_jsx_runtime14.jsxs)(import_jsx_runtime14.Fragment, {
    children: [
      (0, import_jsx_runtime14.jsx)(Ed, {
        ref: forwardedRef,
        label,
        labelAction,
        secondaryLabel: currentAsset == null ? void 0 : currentAsset.name,
        selectedSlide: selectedAssetIndex,
        previousLabel: formatMessage({
          id: getTrad("mediaLibraryInput.actions.previousSlide"),
          defaultMessage: "Previous slide"
        }),
        nextLabel: formatMessage({
          id: getTrad("mediaLibraryInput.actions.nextSlide"),
          defaultMessage: "Next slide"
        }),
        onNext,
        onPrevious,
        hint,
        error,
        required,
        actions: currentAsset ? (0, import_jsx_runtime14.jsx)(CarouselAssetActions, {
          asset: currentAsset,
          onDeleteAsset: disabled ? void 0 : onDeleteAsset,
          onAddAsset: disabled ? void 0 : onAddAsset,
          onEditAsset: canEditMedia ? () => setIsEditingAsset(true) : void 0
        }) : void 0,
        children: assets.length === 0 ? (0, import_jsx_runtime14.jsx)(Od, {
          label: formatMessage({
            id: getTrad("mediaLibraryInput.slideCount"),
            defaultMessage: "{n} of {m} slides"
          }, {
            n: 1,
            m: 1
          }),
          children: (0, import_jsx_runtime14.jsx)(EmptyStateAsset, {
            disabled,
            onClick: onAddAsset,
            onDropAsset
          })
        }) : assets.map((asset, index4) => (0, import_jsx_runtime14.jsx)(Od, {
          label: formatMessage({
            id: getTrad("mediaLibraryInput.slideCount"),
            defaultMessage: "{n} of {m} slides"
          }, {
            n: index4 + 1,
            m: assets.length
          }),
          onDoubleClick: (event) => {
            if (disabled) return;
            setIsEditingAsset(true);
            onDoubleClickAsset(asset, event);
          },
          "aria-selected": currentAsset.id === asset.id,
          children: (0, import_jsx_runtime14.jsx)(CarouselAsset, {
            asset
          })
        }, asset.id))
      }),
      (0, import_jsx_runtime14.jsx)(EditAssetDialog, {
        open: isEditingAsset,
        onClose: (editedAsset) => {
          setIsEditingAsset(false);
          if (editedAsset === null) {
            onDeleteAssetFromMediaLibrary();
          }
          if (editedAsset && typeof editedAsset !== "boolean") {
            onEditAsset == null ? void 0 : onEditAsset(editedAsset);
          }
        },
        asset: currentAsset,
        canUpdate: !disabled,
        canCopyLink: true,
        canDownload: true,
        trackedLocation
      })
    ]
  });
});

// node_modules/@strapi/upload/dist/admin/components/MediaLibraryInput/MediaLibraryInput.mjs
var STEPS2 = {
  AssetSelect: "SelectAsset",
  AssetUpload: "UploadAsset",
  FolderCreate: "FolderCreate"
};
var MediaLibraryInput = React9.forwardRef(({ attribute: { allowedTypes = null, multiple = false } = {}, label, hint, disabled = false, labelAction = void 0, name: name2, required = false }, forwardedRef) => {
  const { formatMessage } = useIntl();
  const { onChange, value, error } = useField(name2);
  const [uploadedFiles, setUploadedFiles] = React9.useState([]);
  const [step, setStep] = React9.useState(void 0);
  const [selectedIndex, setSelectedIndex] = React9.useState(0);
  const [droppedAssets, setDroppedAssets] = React9.useState();
  const [folderId, setFolderId] = React9.useState(null);
  const { toggleNotification } = useNotification();
  React9.useEffect(() => {
    if (step === void 0) {
      setUploadedFiles([]);
    }
  }, [
    step
  ]);
  let selectedAssets = [];
  if (Array.isArray(value)) {
    selectedAssets = value;
  } else if (value !== null && value !== void 0) {
    selectedAssets = [
      value
    ];
  }
  const handleValidation = (nextSelectedAssets) => {
    const value2 = multiple ? nextSelectedAssets : nextSelectedAssets[0];
    onChange(name2, value2);
    setStep(void 0);
  };
  const handleDeleteAssetFromMediaLibrary = () => {
    let nextValue;
    if (multiple) {
      const nextSelectedAssets = selectedAssets.filter((_, assetIndex) => assetIndex !== selectedIndex);
      nextValue = nextSelectedAssets.length > 0 ? nextSelectedAssets : null;
    } else {
      nextValue = null;
    }
    const value2 = nextValue;
    onChange(name2, value2);
    setSelectedIndex(0);
  };
  const handleDeleteAsset = (asset) => {
    let nextValue;
    if (multiple) {
      const nextSelectedAssets = selectedAssets.filter((prevAsset) => prevAsset.id !== asset.id);
      nextValue = nextSelectedAssets.length > 0 ? nextSelectedAssets : null;
    } else {
      nextValue = null;
    }
    onChange(name2, nextValue);
    setSelectedIndex(0);
  };
  const handleAssetEdit = (asset) => {
    const nextSelectedAssets = selectedAssets.map((prevAsset) => prevAsset.id === asset.id ? asset : prevAsset);
    onChange(name2, multiple ? nextSelectedAssets : nextSelectedAssets[0]);
  };
  const validateAssetsTypes = (assets, callback) => {
    const allowedAssets = getAllowedFiles(allowedTypes, assets);
    if (allowedAssets.length > 0) {
      callback(allowedAssets);
    } else {
      toggleNotification({
        type: "danger",
        timeout: 4e3,
        message: formatMessage({
          id: getTrad("input.notification.not-supported"),
          defaultMessage: `You can't upload this type of file.`
        }, {
          fileTypes: (allowedTypes ?? []).join(",")
        })
      });
    }
  };
  const handleAssetDrop = (assets) => {
    validateAssetsTypes(assets, (allowedAssets) => {
      setDroppedAssets(allowedAssets);
      setStep(STEPS2.AssetUpload);
    });
  };
  if (multiple && selectedAssets.length > 0) {
    label = `${label} (${selectedIndex + 1} / ${selectedAssets.length})`;
  }
  const handleNext = () => {
    setSelectedIndex((current) => current < selectedAssets.length - 1 ? current + 1 : 0);
  };
  const handlePrevious = () => {
    setSelectedIndex((current) => current > 0 ? current - 1 : selectedAssets.length - 1);
  };
  const handleFilesUploadSucceeded = (uploadedFiles2) => {
    setUploadedFiles((prev) => [
      ...prev,
      ...uploadedFiles2
    ]);
  };
  let initiallySelectedAssets = selectedAssets;
  if (uploadedFiles.length > 0) {
    const allowedUploadedFiles = getAllowedFiles(allowedTypes, uploadedFiles);
    initiallySelectedAssets = multiple ? [
      ...allowedUploadedFiles,
      ...selectedAssets
    ] : [
      allowedUploadedFiles[0]
    ];
  }
  const handleDoubleClickAsset = (asset) => {
    if (disabled) return;
    handleAssetEdit(asset);
  };
  return (0, import_jsx_runtime15.jsxs)(import_jsx_runtime15.Fragment, {
    children: [
      (0, import_jsx_runtime15.jsx)(CarouselAssets, {
        ref: forwardedRef,
        assets: selectedAssets,
        disabled,
        label,
        labelAction,
        onDeleteAsset: handleDeleteAsset,
        onDeleteAssetFromMediaLibrary: handleDeleteAssetFromMediaLibrary,
        onAddAsset: () => setStep(STEPS2.AssetSelect),
        onDropAsset: handleAssetDrop,
        onEditAsset: handleAssetEdit,
        onNext: handleNext,
        onPrevious: handlePrevious,
        onDoubleClickAsset: handleDoubleClickAsset,
        error,
        hint,
        required,
        selectedAssetIndex: selectedIndex,
        trackedLocation: "content-manager"
      }),
      step === STEPS2.AssetSelect && (0, import_jsx_runtime15.jsx)(AssetDialog, {
        allowedTypes,
        initiallySelectedAssets,
        folderId,
        onClose: () => {
          setStep(void 0);
          setFolderId(null);
        },
        open: step === STEPS2.AssetSelect,
        onValidate: handleValidation,
        multiple,
        onAddAsset: () => setStep(STEPS2.AssetUpload),
        onAddFolder: () => setStep(STEPS2.FolderCreate),
        onChangeFolder: (folder) => setFolderId(folder),
        trackedLocation: "content-manager"
      }),
      step === STEPS2.AssetUpload && (0, import_jsx_runtime15.jsx)(UploadAssetDialog, {
        open: step === STEPS2.AssetUpload,
        onClose: () => setStep(STEPS2.AssetSelect),
        initialAssetsToAdd: droppedAssets,
        addUploadedFiles: handleFilesUploadSucceeded,
        trackedLocation: "content-manager",
        folderId,
        validateAssetsTypes
      }),
      step === STEPS2.FolderCreate && (0, import_jsx_runtime15.jsx)(EditFolderDialog, {
        open: step === STEPS2.FolderCreate,
        onClose: () => setStep(STEPS2.AssetSelect),
        parentFolderId: folderId
      })
    ]
  });
});

// node_modules/@strapi/upload/dist/admin/future/components/UploadProgressDialog.mjs
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
var HeaderStatusMessage = ({ title, subtitle, metadataSubtitle }) => {
  return (0, import_jsx_runtime16.jsxs)(C, {
    direction: "column",
    alignItems: "flex-start",
    paddingLeft: 2,
    paddingRight: 2,
    paddingTop: 1,
    paddingBottom: 1,
    children: [
      (0, import_jsx_runtime16.jsx)(Drawer.Title, {
        children: (0, import_jsx_runtime16.jsx)(A, {
          variant: "omega",
          children: title
        })
      }),
      (0, import_jsx_runtime16.jsx)(Drawer.Description, {
        children: (0, import_jsx_runtime16.jsx)(A, {
          variant: "pi",
          textColor: "neutral600",
          children: subtitle
        })
      }),
      metadataSubtitle && (0, import_jsx_runtime16.jsx)(A, {
        variant: "pi",
        textColor: "neutral600",
        children: metadataSubtitle
      })
    ]
  });
};
var HeaderStatusIcon = gt(C)`
  align-self: stretch;
  padding: ${({ theme }) => theme.spaces[3]};
  border-radius: ${({ theme }) => `${theme.borderRadius} 0 0 ${theme.borderRadius}`};

  > svg {
    height: 24px;
    width: 24px;
  }
`;
var HeaderStatusWrapper = gt(C)`
  align-items: center;
`;
var HeaderStatus = ({ status, progress, totalFiles, successfulCount, errorCount, metadataProgress, isGeneratingMetadata, metadataOutcome }) => {
  const { formatMessage } = useIntl();
  const metadataSubtitle = (() => {
    if (metadataProgress !== null && isGeneratingMetadata) {
      return formatMessage({
        id: getTranslationKey("upload.progress.generatingMetadata.withCount"),
        defaultMessage: "Generating metadata with AI ({percentage}%)"
      }, {
        percentage: metadataProgress
      });
    }
    if (metadataOutcome === null) {
      return void 0;
    }
    if (metadataOutcome.generated === 0) {
      return void 0;
    }
    if (metadataOutcome.failed > 0) {
      return formatMessage({
        id: getTranslationKey("upload.progress.metadataGenerated.withFailures"),
        defaultMessage: "{generatedCount} generated, {failedCount} failed"
      }, {
        generatedCount: metadataOutcome.generated,
        failedCount: metadataOutcome.failed
      });
    }
    return formatMessage({
      id: getTranslationKey("upload.progress.metadataGenerated.withCount"),
      defaultMessage: "{count, plural, one {Metadata successfully generated on # file} other {Metadata successfully generated on # files}}"
    }, {
      count: metadataOutcome.generated
    });
  })();
  if (status === "error") {
    return (0, import_jsx_runtime16.jsxs)(HeaderStatusWrapper, {
      children: [
        (0, import_jsx_runtime16.jsx)(HeaderStatusIcon, {
          background: "danger200",
          children: (0, import_jsx_runtime16.jsx)(F5, {
            fill: "danger700"
          })
        }),
        (0, import_jsx_runtime16.jsx)(HeaderStatusMessage, {
          title: formatMessage({
            id: getTranslationKey("upload.progress.failed"),
            defaultMessage: "Upload failed"
          }),
          subtitle: formatMessage({
            id: getTranslationKey("upload.progress.failed.subtitle"),
            defaultMessage: "Please try to upload files again"
          })
        })
      ]
    });
  }
  if (status === "success") {
    const subtitle = errorCount > 0 ? formatMessage({
      id: getTranslationKey("upload.progress.success.subtitle.withErrors"),
      defaultMessage: "{successCount} uploaded, {errorCount} failed"
    }, {
      successCount: successfulCount,
      errorCount
    }) : formatMessage({
      id: getTranslationKey("upload.progress.success.subtitle"),
      defaultMessage: "{count} files uploaded successfully"
    }, {
      count: successfulCount
    });
    return (0, import_jsx_runtime16.jsxs)(HeaderStatusWrapper, {
      children: [
        (0, import_jsx_runtime16.jsx)(HeaderStatusIcon, {
          background: "success200",
          children: (0, import_jsx_runtime16.jsx)(h5, {
            fill: "success700"
          })
        }),
        (0, import_jsx_runtime16.jsx)(HeaderStatusMessage, {
          title: formatMessage({
            id: getTranslationKey("upload.progress.success"),
            defaultMessage: "Upload successful!"
          }),
          subtitle,
          metadataSubtitle
        })
      ]
    });
  }
  if (status === "canceled") {
    return (0, import_jsx_runtime16.jsxs)(HeaderStatusWrapper, {
      children: [
        (0, import_jsx_runtime16.jsx)(HeaderStatusIcon, {
          background: "neutral200",
          children: (0, import_jsx_runtime16.jsx)(O3, {
            fill: "neutral700"
          })
        }),
        (0, import_jsx_runtime16.jsx)(HeaderStatusMessage, {
          title: formatMessage({
            id: getTranslationKey("upload.progress.canceled"),
            defaultMessage: "Upload canceled"
          }),
          subtitle: formatMessage({
            id: getTranslationKey("upload.progress.canceled.subtitle"),
            defaultMessage: "Some files were not uploaded"
          }),
          metadataSubtitle
        })
      ]
    });
  }
  if (status === "uploading") {
    const title = progress === null ? formatMessage({
      id: getTranslationKey("upload.progress.uploading.indeterminate"),
      defaultMessage: "Uploading {total, plural, one {# item} other {# items}}"
    }, {
      total: totalFiles
    }) : formatMessage({
      id: getTranslationKey("upload.progress.uploading.withCount"),
      defaultMessage: "Uploading {total, plural, one {# item} other {# items}} ({percentage}%)"
    }, {
      total: totalFiles,
      percentage: Math.round(progress)
    });
    return (0, import_jsx_runtime16.jsxs)(HeaderStatusWrapper, {
      children: [
        (0, import_jsx_runtime16.jsx)(HeaderStatusIcon, {
          background: "primary200",
          children: (0, import_jsx_runtime16.jsx)(Qn, {
            fill: "primary700"
          })
        }),
        (0, import_jsx_runtime16.jsx)(HeaderStatusMessage, {
          title,
          metadataSubtitle
        })
      ]
    });
  }
  return null;
};
var HeaderIconButton = gt(be)`
  &:hover {
    background: transparent;
  }
`;
var ChevronWrapper = gt.span`
  display: flex;
  transition: transform 0.5s ease-in-out;
  transform: ${({ $isMinimized }) => $isMinimized ? "rotate(180deg)" : "rotate(0deg)"};
`;
var HEADER_COLOR_MAP = {
  uploading: {
    background: "primary100"
  },
  canceled: {
    background: "neutral100"
  },
  success: {
    background: "success100"
  },
  error: {
    background: "danger100"
  }
};
var DialogHeader = ({ handleClose }) => {
  const { formatMessage } = useIntl();
  const { isMinimized, files, uploadId, totalFiles } = useTypedSelector((state) => state.uploadProgress);
  const aggregateProgress = useTypedSelector(selectAggregateProgress);
  const reportsByteProgress = useTypedSelector(selectReportsByteProgress);
  const countBasedProgress = useTypedSelector(selectCountBasedProgress);
  const progress = reportsByteProgress ? aggregateProgress : countBasedProgress;
  const metadataProgress = useTypedSelector(selectMetadataProgress);
  const isGeneratingMetadata = useTypedSelector(selectIsGeneratingMetadata);
  const metadataOutcome = useTypedSelector(selectMetadataOutcome);
  const dispatch = useTypedDispatch();
  const [retryCancelledFiles] = useRetryCancelledFilesMutation();
  const isComplete = files.length > 0 && files.every((f) => f.status === "complete" || f.status === "error" || f.status === "cancelled");
  const isAllUploaded = isComplete;
  const isAllErrored = isComplete && files.every((f) => f.status === "error");
  const hasCancelledFiles = files.some((f) => f.status === "cancelled");
  const successfulCount = files.filter((f) => f.status === "complete").length;
  const errorCount = files.filter((f) => f.status === "error").length;
  const isSuccess = isComplete && successfulCount > 0 && !hasCancelledFiles;
  const status = (() => {
    if (isAllErrored) return "error";
    if (isSuccess) return "success";
    if (hasCancelledFiles) return "canceled";
    return "uploading";
  })();
  const handleCancel = () => {
    abortUpload(uploadId);
    dispatch(cancelUpload());
  };
  const handleRetry = async () => {
    try {
      await retryCancelledFiles().unwrap();
    } catch {
    }
  };
  const handleToggleMinimize = () => {
    dispatch(toggleMinimize());
  };
  return (0, import_jsx_runtime16.jsxs)(C, {
    background: HEADER_COLOR_MAP[status].background,
    justifyContent: "space-between",
    margin: 1,
    hasRadius: true,
    children: [
      (0, import_jsx_runtime16.jsx)(HeaderStatus, {
        status,
        progress,
        totalFiles,
        successfulCount,
        errorCount,
        metadataProgress,
        isGeneratingMetadata,
        metadataOutcome
      }),
      (0, import_jsx_runtime16.jsxs)(C, {
        gap: 1,
        children: [
          !isAllUploaded && (0, import_jsx_runtime16.jsx)(bu, {
            onClick: handleCancel,
            fontWeight: "bold",
            children: formatMessage({
              id: getTranslationKey("upload.progress.cancel"),
              defaultMessage: "Cancel all"
            })
          }),
          hasCancelledFiles && (0, import_jsx_runtime16.jsx)(bu, {
            onClick: handleRetry,
            fontWeight: "bold",
            children: formatMessage({
              id: getTranslationKey("upload.progress.retry"),
              defaultMessage: "Retry"
            })
          }),
          (0, import_jsx_runtime16.jsx)(HeaderIconButton, {
            onClick: handleToggleMinimize,
            label: formatMessage({
              id: getTranslationKey(isMinimized ? "upload.progress.maximize" : "upload.progress.minimize"),
              defaultMessage: isMinimized ? "Maximize" : "Minimize"
            }),
            variant: "ghost",
            children: (0, import_jsx_runtime16.jsx)(ChevronWrapper, {
              $isMinimized: isMinimized,
              children: (0, import_jsx_runtime16.jsx)(o5, {})
            })
          }),
          isComplete && (0, import_jsx_runtime16.jsx)(Drawer.CloseButton, {
            onClose: handleClose,
            label: formatMessage({
              id: getTranslationKey("upload.progress.close"),
              defaultMessage: "Close"
            })
          })
        ]
      })
    ]
  });
};
var ProgressTrack = gt.div`
  width: 100%;
  height: ${({ theme }) => theme.spaces[1]};
  background-color: ${({ theme }) => theme.colors.neutral200};
  border-radius: 4px;
  overflow: hidden;
`;
var ProgressIndicator = gt.div`
  height: 100%;
  width: ${({ $percent }) => $percent}%;
  background-color: ${({ theme }) => theme.colors.primary700};
  border-radius: 4px;
  transition: width 0.15s linear;
`;
var DeterminateBar = ({ percent }) => {
  const clamped = Math.min(100, Math.max(0, Math.round(percent)));
  return (0, import_jsx_runtime16.jsx)(ProgressTrack, {
    role: "progressbar",
    "aria-valuemin": 0,
    "aria-valuemax": 100,
    "aria-valuenow": clamped,
    children: (0, import_jsx_runtime16.jsx)(ProgressIndicator, {
      $percent: clamped
    })
  });
};
var INDETERMINATE_INDICATOR_WIDTH = 40;
var indeterminateSlide = Et`
  from {
    transform: translateX(-100%);
  }
  to {
    transform: translateX(${100 / INDETERMINATE_INDICATOR_WIDTH * 100}%);
  }
`;
var IndeterminateIndicator = gt.div`
  height: 100%;
  width: ${INDETERMINATE_INDICATOR_WIDTH}%;
  background-color: ${({ theme }) => theme.colors.primary700};
  border-radius: 4px;
  /* Linear, not an eased curve: the DS easings decelerate to a stop, which on a loop
     reads as a stall at the wrap point. Constant speed is what makes it read as a sweep. */
  animation: ${indeterminateSlide} 1.2s linear infinite;

  /* A perpetually moving bar is a vestibular trigger; fall back to a static track fill. */
  @media (prefers-reduced-motion: reduce) {
    animation: none;
    width: 100%;
    opacity: 0.5;
  }
`;
var IndeterminateBar = () => {
  return (0, import_jsx_runtime16.jsx)(ProgressTrack, {
    role: "progressbar",
    "aria-valuemin": 0,
    "aria-valuemax": 100,
    children: (0, import_jsx_runtime16.jsx)(IndeterminateIndicator, {})
  });
};
var FileRowName = gt(C)`
  min-width: 0;

  > :first-child {
    flex-shrink: 0;
  }

  > :last-child {
    min-width: 0;
  }
`;
var FileRow = ({ icon, fileName, children }) => {
  return (0, import_jsx_runtime16.jsxs)(C, {
    direction: "column",
    alignItems: "stretch",
    justifyContent: "center",
    gap: 1,
    width: "100%",
    children: [
      (0, import_jsx_runtime16.jsxs)(FileRowName, {
        gap: 2,
        children: [
          icon,
          (0, import_jsx_runtime16.jsx)(TruncatedText, {
            variant: "omega",
            fontWeight: "semiBold",
            children: fileName
          })
        ]
      }),
      children
    ]
  });
};
var COMPLETED_ROW_PRESENTATION = {
  none: {
    icon: (0, import_jsx_runtime16.jsx)(w5, {
      fill: "success500"
    }),
    message: {
      id: getTranslationKey("upload.progress.file.uploaded"),
      defaultMessage: "Uploaded"
    }
  },
  generating: {
    icon: (0, import_jsx_runtime16.jsx)(Vn, {
      fill: "primary600"
    }),
    message: {
      id: getTranslationKey("upload.progress.file.generatingMetadata"),
      defaultMessage: "Uploaded • Generating metadata…"
    }
  },
  generated: {
    icon: (0, import_jsx_runtime16.jsx)(w5, {
      fill: "success500"
    }),
    message: {
      id: getTranslationKey("upload.progress.file.metadataGenerated"),
      defaultMessage: "Uploaded • Metadata generated"
    }
  },
  skipped: {
    icon: (0, import_jsx_runtime16.jsx)(M3, {
      fill: "neutral500"
    }),
    message: {
      id: getTranslationKey("upload.progress.file.metadataSkipped"),
      defaultMessage: "Upload complete • Metadata generation skipped"
    }
  },
  failed: {
    icon: (0, import_jsx_runtime16.jsx)(Jn, {
      fill: "warning500"
    }),
    message: {
      id: getTranslationKey("upload.progress.file.metadataFailed"),
      defaultMessage: "Upload complete • Metadata generation failed"
    }
  }
};
var FileRowRenderer = ({ file }) => {
  const { formatMessage } = useIntl();
  const isError = file.status === "error";
  const isCurrentFile = file.status === "uploading";
  const isCompleted = file.status === "complete";
  const isCancelled = file.status === "cancelled";
  const isQueued = file.status === "pending";
  if (isQueued) {
    return (0, import_jsx_runtime16.jsx)(FileRow, {
      icon: (0, import_jsx_runtime16.jsx)(l5, {
        fill: "neutral600"
      }),
      fileName: file.name,
      children: (0, import_jsx_runtime16.jsx)(A, {
        variant: "pi",
        textColor: "neutral600",
        children: formatMessage({
          id: getTranslationKey("upload.progress.file.queued"),
          defaultMessage: "Queued"
        })
      })
    });
  }
  if (isCurrentFile) {
    const hasReportedProgress = file.size > 0 && file.uploadedBytes > 0;
    return (0, import_jsx_runtime16.jsxs)(FileRow, {
      icon: (0, import_jsx_runtime16.jsx)(W2, {
        fill: "secondary600"
      }),
      fileName: file.name,
      children: [
        (0, import_jsx_runtime16.jsx)(A, {
          variant: "pi",
          textColor: "neutral600",
          children: formatMessage({
            id: getTranslationKey("upload.progress.file.uploading"),
            defaultMessage: "Uploading..."
          })
        }),
        hasReportedProgress ? (0, import_jsx_runtime16.jsx)(DeterminateBar, {
          percent: file.uploadedBytes / file.size * 100
        }) : (0, import_jsx_runtime16.jsx)(IndeterminateBar, {})
      ]
    });
  }
  if (isError) {
    return (0, import_jsx_runtime16.jsx)(FileRow, {
      icon: (0, import_jsx_runtime16.jsx)(R5, {
        fill: "danger500"
      }),
      fileName: file.name,
      children: (0, import_jsx_runtime16.jsx)(A, {
        variant: "pi",
        textColor: "neutral600",
        children: file.error ? (
          /**
          * The server may return a machine-readable code (e.g. `FileTooBig` from the
          * body middleware) or an already human-readable sentence (e.g. the upload
          * plugin's size-limit message). Translate the former, fall back to the latter.
          */
          formatMessage({
            id: getTranslationKey(`apiError.${file.error}`),
            defaultMessage: file.error
          })
        ) : formatMessage({
          id: getTranslationKey("upload.generic-error"),
          defaultMessage: "An error occurred while uploading the file."
        })
      })
    });
  }
  if (isCancelled) {
    return (0, import_jsx_runtime16.jsx)(FileRow, {
      icon: (0, import_jsx_runtime16.jsx)(O3, {
        fill: "neutral600"
      }),
      fileName: file.name,
      children: (0, import_jsx_runtime16.jsx)(A, {
        variant: "pi",
        textColor: "neutral600",
        children: formatMessage({
          id: getTranslationKey("upload.progress.file.canceled"),
          defaultMessage: "Canceled"
        })
      })
    });
  }
  if (isCompleted) {
    const { icon, message } = COMPLETED_ROW_PRESENTATION[file.metadataStatus ?? "none"];
    return (0, import_jsx_runtime16.jsxs)(FileRow, {
      icon,
      fileName: file.name,
      children: [
        (0, import_jsx_runtime16.jsx)(A, {
          variant: "pi",
          textColor: "neutral600",
          children: formatMessage(message)
        }),
        file.metadataStatus === "generating" && (0, import_jsx_runtime16.jsx)(IndeterminateBar, {})
      ]
    });
  }
  return null;
};
var CompletedFilesList = gt(C)`
  flex-direction: column;
  gap: ${({ theme }) => theme.spaces[2]};
  width: 100%;
`;
var UploadProgressDialog = () => {
  const dispatch = useTypedDispatch();
  const { isVisible, isMinimized, files } = useTypedSelector((state) => state.uploadProgress);
  const uploadingFiles = files.filter((f) => f.status === "uploading");
  const queuedFiles = files.filter((f) => f.status === "pending");
  const completedFiles = files.filter((f) => f.status === "complete" || f.status === "error" || f.status === "cancelled").sort((a, b) => {
    const priority = {
      error: 0,
      cancelled: 1,
      complete: 2,
      uploading: 3,
      pending: 4
    };
    return priority[a.status] - priority[b.status];
  });
  const handleClose = () => {
    dispatch(closeUploadProgress());
  };
  return (0, import_jsx_runtime16.jsx)(Drawer.Root, {
    isVisible,
    onClose: handleClose,
    children: (0, import_jsx_runtime16.jsxs)(Drawer.Body, {
      animationDirection: "up",
      width: "41.6rem",
      maxHeight: "34.2rem",
      children: [
        (0, import_jsx_runtime16.jsx)(DialogHeader, {
          handleClose
        }),
        (0, import_jsx_runtime16.jsx)(Drawer.ScrollableContent, {
          isContentExpanded: !isMinimized,
          children: (0, import_jsx_runtime16.jsxs)(C, {
            direction: "column",
            alignItems: "stretch",
            gap: 4,
            paddingTop: 4,
            paddingBottom: 4,
            paddingLeft: 4,
            paddingRight: 4,
            children: [
              uploadingFiles.map((file) => (0, import_jsx_runtime16.jsx)(FileRowRenderer, {
                file
              }, file.index)),
              queuedFiles.map((file) => (0, import_jsx_runtime16.jsx)(FileRowRenderer, {
                file
              }, file.index)),
              completedFiles.length > 0 && (0, import_jsx_runtime16.jsx)(CompletedFilesList, {
                children: completedFiles.map((file) => (0, import_jsx_runtime16.jsx)(FileRowRenderer, {
                  file
                }, file.index))
              })
            ]
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/index.mjs
var import_qs11 = __toESM(require_lib(), 1);

// node_modules/@strapi/upload/dist/admin/utils/prefixPluginTranslations.mjs
var prefixPluginTranslations3 = (trad, pluginId5) => {
  if (!pluginId5) {
    throw new TypeError("pluginId can't be empty");
  }
  return Object.keys(trad).reduce((acc, current) => {
    acc[`${pluginId5}.${current}`] = trad[current];
    return acc;
  }, {});
};

// node_modules/@strapi/upload/dist/admin/index.mjs
function __variableDynamicImportRuntime3__(path) {
  switch (path) {
    case "./translations/ca.json":
      return import("./ca.json-UPAJLBGH.js");
    case "./translations/da.json":
      return import("./da.json-Z5WNFYEH.js");
    case "./translations/de.json":
      return import("./de.json-E7FKVE3I.js");
    case "./translations/en.json":
      return import("./en.json-GICYIRTN.js");
    case "./translations/es.json":
      return import("./es.json-3CP7VYF5.js");
    case "./translations/fr.json":
      return import("./fr.json-FNSB2M5K.js");
    case "./translations/he.json":
      return import("./he.json-RGECPZJE.js");
    case "./translations/it.json":
      return import("./it.json-4OXGKKXS.js");
    case "./translations/ja.json":
      return import("./ja.json-CMGQG67C.js");
    case "./translations/ko.json":
      return import("./ko.json-Y54FBSVY.js");
    case "./translations/ms.json":
      return import("./ms.json-HJ7MRRPH.js");
    case "./translations/nl.json":
      return import("./nl.json-XIWMMK2V.js");
    case "./translations/pl.json":
      return import("./pl.json-F2KD5CTN.js");
    case "./translations/pt-BR.json":
      return import("./pt-BR.json-IYFQHYFV.js");
    case "./translations/pt.json":
      return import("./pt.json-U5VJYEWQ.js");
    case "./translations/ru.json":
      return import("./ru.json-S2W6PQFA.js");
    case "./translations/sk.json":
      return import("./sk.json-WFSF4HAM.js");
    case "./translations/th.json":
      return import("./th.json-GHLCPRG2.js");
    case "./translations/tr.json":
      return import("./tr.json-TC2HB7H4.js");
    case "./translations/uk.json":
      return import("./uk.json-7E6U5GIW.js");
    case "./translations/zh-Hans.json":
      return import("./zh-Hans.json-GSNID2G5.js");
    case "./translations/zh.json":
      return import("./zh.json-Z7BUV6CC.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
var name = pluginPkg.strapi.name;
var admin2 = {
  register(app) {
    const isBetaMediaLibrary = window.strapi.future.isEnabled("betaMediaLibrary");
    app.addMenuLink({
      to: `plugins/${pluginId2}`,
      icon: u3,
      intlLabel: {
        id: `${pluginId2}.plugin.name`,
        defaultMessage: "Media Library"
      },
      permissions: PERMISSIONS3.main,
      Component: isBetaMediaLibrary ? () => {
        return import("./App-CKNW7HGM.js").then((mod) => ({
          default: mod.BetaMediaLibrary
        }));
      } : () => {
        return import("./App-A6GYS3XR.js").then((mod) => ({
          default: mod.Upload
        }));
      },
      position: 4
    });
    if (isBetaMediaLibrary) {
      app.addReducers({
        uploadProgress: uploadProgressReducer
      });
      app.addComponents([
        {
          name: "future-global::upload-progress",
          Component: UploadProgressDialog
        }
      ]);
    }
    app.addSettingsLink("global", {
      id: "media-library-settings",
      to: "media-library",
      intlLabel: {
        id: getTrad("plugin.name"),
        defaultMessage: "Media Library"
      },
      Component() {
        return import("./SettingsPage-6CXKPWW3.js").then((mod) => ({
          default: mod.ProtectedSettingsPage
        }));
      },
      permissions: PERMISSIONS3.settings
    });
    app.addFields({
      type: "media",
      Component: MediaLibraryInput
    });
    app.addComponents([
      {
        name: "media-library",
        Component: MediaLibraryDialog
      }
    ]);
    app.registerPlugin({
      id: pluginId2,
      name
    });
  },
  async registerTrads({ locales }) {
    const importedTrads = await Promise.all(locales.map((locale) => {
      return __variableDynamicImportRuntime3__(`./translations/${locale}.json`).then(({ default: data }) => {
        return {
          data: prefixPluginTranslations3(data, pluginId2),
          locale
        };
      }).catch(() => {
        return {
          data: {},
          locale
        };
      });
    }));
    return Promise.resolve(importedTrads);
  }
};

// node_modules/@strapi/i18n/dist/admin/index.mjs
var import_get3 = __toESM(require_get(), 1);

// node_modules/@strapi/i18n/dist/admin/components/CheckboxConfirmation.mjs
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var React10 = __toESM(require_react(), 1);
var TextAlignTypography2 = gt(A)`
  text-align: center;
`;
var CheckboxConfirmation = ({ description, isCreating = false, intlLabel, name: name2, onChange, value }) => {
  const { formatMessage } = useIntl();
  const [isOpen, setIsOpen] = React10.useState(false);
  const handleChange = (value2) => {
    if (isCreating || value2) {
      return onChange({
        target: {
          name: name2,
          value: value2,
          type: "checkbox"
        }
      });
    }
    if (!value2) {
      return setIsOpen(true);
    }
    return null;
  };
  const handleConfirm = () => {
    onChange({
      target: {
        name: name2,
        value: false,
        type: "checkbox"
      }
    });
  };
  const label = intlLabel.id ? formatMessage({
    id: intlLabel.id,
    defaultMessage: intlLabel.defaultMessage
  }, {
    ...intlLabel.values
  }) : name2;
  const hint = description ? formatMessage({
    id: description.id,
    defaultMessage: description.defaultMessage
  }, {
    ...description.values
  }) : "";
  return (0, import_jsx_runtime17.jsxs)(Pd.Root, {
    open: isOpen,
    onOpenChange: setIsOpen,
    children: [
      (0, import_jsx_runtime17.jsxs)(Dd.Root, {
        hint,
        name: name2,
        children: [
          (0, import_jsx_runtime17.jsx)(Bt, {
            onCheckedChange: handleChange,
            checked: value,
            children: label
          }),
          (0, import_jsx_runtime17.jsx)(Dd.Hint, {})
        ]
      }),
      (0, import_jsx_runtime17.jsxs)(Pd.Content, {
        children: [
          (0, import_jsx_runtime17.jsx)(Pd.Header, {
            children: formatMessage({
              id: getTranslation("CheckboxConfirmation.Modal.title"),
              defaultMessage: "Disable localization"
            })
          }),
          (0, import_jsx_runtime17.jsx)(Pd.Body, {
            icon: (0, import_jsx_runtime17.jsx)(Jn, {}),
            children: (0, import_jsx_runtime17.jsxs)(C, {
              direction: "column",
              alignItems: "stretch",
              gap: 2,
              children: [
                (0, import_jsx_runtime17.jsx)(C, {
                  justifyContent: "center",
                  children: (0, import_jsx_runtime17.jsx)(TextAlignTypography2, {
                    children: formatMessage({
                      id: getTranslation("CheckboxConfirmation.Modal.content"),
                      defaultMessage: "Disabling localization will engender the deletion of all your content but the one associated to your default locale (if existing)."
                    })
                  })
                }),
                (0, import_jsx_runtime17.jsx)(C, {
                  justifyContent: "center",
                  children: (0, import_jsx_runtime17.jsx)(A, {
                    fontWeight: "semiBold",
                    children: formatMessage({
                      id: getTranslation("CheckboxConfirmation.Modal.body"),
                      defaultMessage: "Do you want to disable it?"
                    })
                  })
                })
              ]
            })
          }),
          (0, import_jsx_runtime17.jsxs)(Pd.Footer, {
            children: [
              (0, import_jsx_runtime17.jsx)(Pd.Cancel, {
                children: (0, import_jsx_runtime17.jsx)(Ct, {
                  variant: "tertiary",
                  children: formatMessage({
                    id: "components.popUpWarning.button.cancel",
                    defaultMessage: "No, cancel"
                  })
                })
              }),
              (0, import_jsx_runtime17.jsx)(Pd.Action, {
                children: (0, import_jsx_runtime17.jsx)(Ct, {
                  variant: "danger-light",
                  onClick: handleConfirm,
                  children: formatMessage({
                    id: getTranslation("CheckboxConfirmation.Modal.button-confirm"),
                    defaultMessage: "Yes, disable"
                  })
                })
              })
            ]
          })
        ]
      })
    ]
  });
};

// node_modules/@strapi/i18n/dist/admin/components/CMHeaderActions.mjs
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
var React13 = __toESM(require_react(), 1);

// node_modules/@strapi/i18n/dist/admin/hooks/useAILocalizationJobsPolling.mjs
var React11 = __toESM(require_react(), 1);

// node_modules/@strapi/i18n/dist/admin/services/aiLocalizationJobs.mjs
var SINGLE_TYPES = "single-types";
var aiLocalizationJobsApi = i18nApi.injectEndpoints({
  endpoints: (builder) => ({
    getAILocalizationJobsByDocument: builder.query({
      query: ({ documentId, collectionType, model }) => ({
        url: collectionType === SINGLE_TYPES ? `/i18n/ai-localization-jobs/single-types/${model}` : `/i18n/ai-localization-jobs/collection-types/${model}/${documentId}`,
        method: "GET"
      }),
      providesTags: (result, error, { documentId, model, collectionType }) => [
        {
          type: "AILocalizationJobs",
          id: collectionType !== SINGLE_TYPES ? `${model}_${documentId}` : model
        }
      ]
    })
  })
});
var { useGetAILocalizationJobsByDocumentQuery } = aiLocalizationJobsApi;

// node_modules/@strapi/i18n/dist/admin/hooks/useAILocalizationJobsPolling.mjs
var useAILocalizationJobsPolling = ({ documentId, model, collectionType }) => {
  var _a;
  const { toggleNotification } = useNotification();
  const { formatMessage } = useIntl();
  const dispatch = useDispatch();
  const [previousJobStatus, setPreviousJobStatus] = React11.useState(null);
  const { data: initialJobData } = useGetAILocalizationJobsByDocumentQuery({
    documentId,
    model,
    collectionType
  });
  const shouldPoll = ((_a = initialJobData == null ? void 0 : initialJobData.data) == null ? void 0 : _a.status) === "processing" || previousJobStatus === "processing";
  const { data: jobData } = useGetAILocalizationJobsByDocumentQuery({
    documentId,
    model,
    collectionType
  }, {
    skip: !shouldPoll,
    pollingInterval: 1e3
  });
  const job = (jobData == null ? void 0 : jobData.data) || (initialJobData == null ? void 0 : initialJobData.data);
  const currentJobStatus = job == null ? void 0 : job.status;
  const invalidateDocument = React11.useCallback(() => {
    dispatch(adminApi.util.invalidateTags([
      {
        // @ts-expect-error tag isn't available
        type: "Document",
        id: collectionType !== "single-types" ? `${model}_${documentId}` : model
      }
    ]));
  }, [
    dispatch,
    collectionType,
    model,
    documentId
  ]);
  React11.useEffect(() => {
    if (!currentJobStatus) return;
    if (previousJobStatus === "processing" && currentJobStatus === "completed") {
      toggleNotification({
        type: "success",
        message: formatMessage({
          id: getTranslation("CMEditViewAITranslation.job-completed"),
          defaultMessage: "AI translation completed successfully!"
        })
      });
      invalidateDocument();
    }
    if (previousJobStatus === "processing" && currentJobStatus === "failed") {
      toggleNotification({
        type: "warning",
        message: formatMessage({
          id: getTranslation("CMEditViewAITranslation.job-failed"),
          defaultMessage: "AI translation failed. Please try again."
        })
      });
      invalidateDocument();
    }
    if (previousJobStatus !== currentJobStatus) {
      setPreviousJobStatus(currentJobStatus);
    }
  }, [
    currentJobStatus,
    previousJobStatus,
    setPreviousJobStatus,
    toggleNotification,
    formatMessage,
    invalidateDocument
  ]);
  return {
    status: job == null ? void 0 : job.status
  };
};

// node_modules/@strapi/i18n/dist/admin/hooks/useI18n.mjs
var React12 = __toESM(require_react(), 1);
var import_union = __toESM(require_union(), 1);

// node_modules/@strapi/i18n/dist/admin/utils/fields.mjs
var import_has = __toESM(require_has(), 1);
var LOCALIZED_FIELDS = [
  "biginteger",
  "boolean",
  "component",
  "date",
  "datetime",
  "decimal",
  "dynamiczone",
  "email",
  "enumeration",
  "float",
  "integer",
  "json",
  "media",
  "number",
  "password",
  "richtext",
  "blocks",
  "string",
  "text",
  "time"
];
var doesPluginOptionsHaveI18nLocalized = (opts) => (0, import_has.default)(opts, [
  "i18n",
  "localized"
]);

// node_modules/@strapi/i18n/dist/admin/utils/strings.mjs
var capitalize = (str) => str.charAt(0).toUpperCase() + str.slice(1);

// node_modules/@strapi/i18n/dist/admin/hooks/useI18n.mjs
var useI18n = () => {
  const params = useParams();
  const userPermissions = useAuth("useI18n", (state) => state.permissions);
  const actions = React12.useMemo(() => {
    const permissions = userPermissions.filter((permission) => permission.subject === params.slug);
    return permissions.reduce((acc, permission) => {
      var _a;
      const [actionShorthand] = permission.action.split(".").slice(-1);
      const permissionKey = `can${capitalize(actionShorthand)}`;
      acc[permissionKey] = (0, import_union.default)(acc[permissionKey] ?? [], ((_a = permission.properties) == null ? void 0 : _a.locales) ?? []);
      return acc;
    }, {
      canCreate: [],
      canRead: [],
      canUpdate: [],
      canDelete: [],
      canPublish: []
    });
  }, [
    params.slug,
    userPermissions
  ]);
  const { schema } = useDocument({
    // We can non-null assert these because below we skip the query if they are not present
    collectionType: params.collectionType,
    model: params.slug
  }, {
    skip: true
  });
  if (doesPluginOptionsHaveI18nLocalized(schema == null ? void 0 : schema.pluginOptions)) {
    return {
      hasI18n: schema.pluginOptions.i18n.localized,
      ...actions
    };
  }
  return {
    hasI18n: false,
    ...actions
  };
};

// node_modules/@strapi/i18n/dist/admin/services/fillFromLocale.mjs
var fillFromLocaleApi = i18nApi.injectEndpoints({
  endpoints: (builder) => ({
    getFillFromLocaleData: builder.query({
      query: ({ model, ...params }) => ({
        url: `/i18n/content-manager/get-fill-from-locale/${model}`,
        config: {
          params
        }
      })
    })
  })
});
var { useGetFillFromLocaleDataQuery, useLazyGetFillFromLocaleDataQuery } = fillFromLocaleApi;

// node_modules/@strapi/i18n/dist/admin/services/relations.mjs
var relationsApi = i18nApi.injectEndpoints({
  overrideExisting: true,
  endpoints: (builder) => ({
    getManyDraftRelationCount: builder.query({
      query: ({ model, ...params }) => ({
        url: `/content-manager/collection-types/${model}/actions/countManyEntriesDraftRelations`,
        method: "GET",
        config: {
          params
        }
      }),
      transformResponse: (response) => response.data
    })
  })
});
var { useGetManyDraftRelationCountQuery } = relationsApi;

// node_modules/@strapi/i18n/dist/admin/components/BulkLocaleActionModal.mjs
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_qs12 = __toESM(require_lib(), 1);
var isErrorMessageDescriptor = (object) => {
  return typeof object === "object" && object !== null && "id" in object && "defaultMessage" in object;
};
var EntryValidationText = ({ status = "draft", validationErrors, action }) => {
  const { formatMessage } = useIntl();
  const getErrorStr = (key, value) => {
    if (typeof value === "string") {
      return `${key}: ${value}`;
    } else if (isErrorMessageDescriptor(value)) {
      return `${key}: ${formatMessage(value)}`;
    } else if (Array.isArray(value)) {
      return value.map((v) => getErrorStr(key, v)).join(" ");
    } else if (typeof value === "object" && !Array.isArray(value)) {
      return Object.entries(value).map(([k, v]) => getErrorStr(k, v)).join(" ");
    } else {
      return "";
    }
  };
  if (validationErrors !== null) {
    const validationErrorsMessages = Object.entries(validationErrors).map(([key, value]) => {
      return getErrorStr(key, value);
    }).join(" ");
    return (0, import_jsx_runtime18.jsxs)(C, {
      gap: 2,
      children: [
        (0, import_jsx_runtime18.jsx)(R5, {
          fill: "danger600"
        }),
        (0, import_jsx_runtime18.jsx)(Mt, {
          label: validationErrorsMessages,
          children: (0, import_jsx_runtime18.jsx)(A, {
            maxWidth: "30rem",
            textColor: "danger600",
            variant: "omega",
            fontWeight: "semiBold",
            ellipsis: true,
            children: validationErrorsMessages
          })
        })
      ]
    });
  }
  const getStatusMessage = () => {
    if (action === "bulk-publish") {
      if (status === "published") {
        return {
          icon: (0, import_jsx_runtime18.jsx)(w5, {
            fill: "success600"
          }),
          text: formatMessage({
            id: "content-manager.bulk-publish.already-published",
            defaultMessage: "Already Published"
          }),
          textColor: "success600",
          fontWeight: "bold"
        };
      } else if (status === "modified") {
        return {
          icon: (0, import_jsx_runtime18.jsx)(W2, {
            fill: "alternative600"
          }),
          text: formatMessage({
            id: "app.utils.ready-to-publish-changes",
            defaultMessage: "Ready to publish changes"
          })
        };
      } else {
        return {
          icon: (0, import_jsx_runtime18.jsx)(w5, {
            fill: "success600"
          }),
          text: formatMessage({
            id: "app.utils.ready-to-publish",
            defaultMessage: "Ready to publish"
          })
        };
      }
    } else {
      if (status === "draft") {
        return {
          icon: (0, import_jsx_runtime18.jsx)(w5, {
            fill: "success600"
          }),
          text: formatMessage({
            id: "content-manager.bulk-unpublish.already-unpublished",
            defaultMessage: "Already Unpublished"
          }),
          textColor: "success600",
          fontWeight: "bold"
        };
      } else {
        return {
          icon: (0, import_jsx_runtime18.jsx)(w5, {
            fill: "success600"
          }),
          text: formatMessage({
            id: "app.utils.ready-to-unpublish-changes",
            defaultMessage: "Ready to unpublish"
          }),
          textColor: "success600",
          fontWeight: "bold"
        };
      }
    }
  };
  const { icon, text, textColor = "success600", fontWeight = "normal" } = getStatusMessage();
  return (0, import_jsx_runtime18.jsxs)(C, {
    gap: 2,
    children: [
      icon,
      (0, import_jsx_runtime18.jsx)(A, {
        textColor,
        fontWeight,
        children: text
      })
    ]
  });
};
var BoldChunk = (chunks) => (0, import_jsx_runtime18.jsx)(A, {
  fontWeight: "bold",
  children: chunks
});
var BulkLocaleActionModal = ({ headers, rows, localesMetadata, validationErrors = {}, action }) => {
  const { formatMessage } = useIntl();
  const selectedRows = useTable("BulkLocaleActionModal", (state) => state.selectedRows);
  const getFormattedCountMessage = () => {
    const currentStatusByLocale = rows.reduce((acc, { locale, status }) => {
      acc[locale] = status;
      return acc;
    }, {});
    const localesWithErrors = Object.keys(validationErrors);
    const publishedCount = selectedRows.filter(({ locale }) => currentStatusByLocale[locale] === "published").length;
    const draftCount = selectedRows.filter(({ locale }) => (currentStatusByLocale[locale] === "draft" || currentStatusByLocale[locale] === "modified") && !localesWithErrors.includes(locale)).length;
    const withErrorsCount = localesWithErrors.length;
    const messageId = action === "bulk-publish" ? "content-manager.containers.list.selectedEntriesModal.selectedCount.publish" : "content-manager.containers.list.selectedEntriesModal.selectedCount.unpublish";
    const defaultMessage = action === "bulk-publish" ? "<b>{publishedCount}</b> {publishedCount, plural, =0 {entries} one {entry} other {entries}} already published. <b>{draftCount}</b> {draftCount, plural, =0 {entries} one {entry} other {entries}} ready to publish. <b>{withErrorsCount}</b> {withErrorsCount, plural, =0 {entries} one {entry} other {entries}} waiting for action." : "<b>{draftCount}</b> {draftCount, plural, =0 {entries} one {entry} other {entries}} already unpublished. <b>{publishedCount}</b> {publishedCount, plural, =0 {entries} one {entry} other {entries}} ready to unpublish.";
    return formatMessage({
      id: messageId,
      defaultMessage
    }, {
      withErrorsCount,
      draftCount,
      publishedCount,
      b: BoldChunk
    });
  };
  return (0, import_jsx_runtime18.jsxs)(_d.Body, {
    children: [
      (0, import_jsx_runtime18.jsx)(A, {
        children: getFormattedCountMessage()
      }),
      (0, import_jsx_runtime18.jsx)(S, {
        marginTop: 5,
        children: (0, import_jsx_runtime18.jsxs)(Table.Content, {
          children: [
            (0, import_jsx_runtime18.jsxs)(Table.Head, {
              children: [
                (0, import_jsx_runtime18.jsx)(Table.HeaderCheckboxCell, {}),
                headers.map((head) => (0, import_jsx_runtime18.jsx)(Table.HeaderCell, {
                  ...head
                }, head.name))
              ]
            }),
            (0, import_jsx_runtime18.jsx)(Table.Body, {
              children: rows.map(({ locale, status }, index4) => {
                var _a;
                const error = (validationErrors == null ? void 0 : validationErrors[locale]) ?? null;
                const statusVariant = status === "draft" ? "primary" : status === "published" ? "success" : "alternative";
                return status && locale ? (0, import_jsx_runtime18.jsxs)(Table.Row, {
                  children: [
                    (0, import_jsx_runtime18.jsx)(Table.CheckboxCell, {
                      id: locale,
                      "aria-label": `Select ${locale}`
                    }),
                    (0, import_jsx_runtime18.jsx)(Table.Cell, {
                      children: (0, import_jsx_runtime18.jsx)(A, {
                        variant: "sigma",
                        textColor: "neutral600",
                        children: Array.isArray(localesMetadata) ? (_a = localesMetadata.find((localeEntry) => localeEntry.code === locale)) == null ? void 0 : _a.name : locale
                      })
                    }),
                    (0, import_jsx_runtime18.jsx)(Table.Cell, {
                      children: (0, import_jsx_runtime18.jsx)(S, {
                        display: "flex",
                        children: (0, import_jsx_runtime18.jsx)(tu, {
                          display: "flex",
                          paddingLeft: "6px",
                          paddingRight: "6px",
                          paddingTop: "2px",
                          paddingBottom: "2px",
                          size: "S",
                          variant: statusVariant,
                          children: (0, import_jsx_runtime18.jsx)(A, {
                            tag: "span",
                            variant: "pi",
                            fontWeight: "bold",
                            children: capitalize(status)
                          })
                        })
                      })
                    }),
                    (0, import_jsx_runtime18.jsx)(Table.Cell, {
                      children: (0, import_jsx_runtime18.jsx)(EntryValidationText, {
                        validationErrors: error,
                        status,
                        action
                      })
                    }),
                    (0, import_jsx_runtime18.jsx)(Table.Cell, {
                      children: (0, import_jsx_runtime18.jsx)(be, {
                        tag: Link,
                        to: {
                          search: (0, import_qs12.stringify)({
                            plugins: {
                              i18n: {
                                locale
                              }
                            }
                          })
                        },
                        label: formatMessage({
                          id: getTranslation("Settings.list.actions.edit"),
                          defaultMessage: "Edit {name} locale"
                        }, {
                          name: locale
                        }),
                        variant: "ghost",
                        children: (0, import_jsx_runtime18.jsx)(nn, {})
                      })
                    })
                  ]
                }, index4) : null;
              })
            })
          ]
        })
      })
    ]
  });
};

// node_modules/@strapi/i18n/dist/admin/components/CMHeaderActions.mjs
var statusVariants = {
  draft: "secondary",
  published: "success",
  modified: "alternative"
};
var LocaleOption = ({ isDraftAndPublishEnabled, locale, status, entryExists }) => {
  const { formatMessage } = useIntl();
  if (!entryExists) {
    return formatMessage({
      id: getTranslation("CMEditViewLocalePicker.locale.create"),
      defaultMessage: "Create <bold>{locale}</bold> locale"
    }, {
      bold: (locale2) => (0, import_jsx_runtime19.jsx)("b", {
        children: locale2
      }),
      locale: locale.name
    });
  }
  return (0, import_jsx_runtime19.jsxs)(C, {
    width: "100%",
    gap: 1,
    justifyContent: "space-between",
    children: [
      (0, import_jsx_runtime19.jsx)(A, {
        children: locale.name
      }),
      isDraftAndPublishEnabled ? (0, import_jsx_runtime19.jsx)(tu, {
        display: "flex",
        paddingLeft: "6px",
        paddingRight: "6px",
        paddingTop: "2px",
        paddingBottom: "2px",
        size: "S",
        variant: statusVariants[status],
        children: (0, import_jsx_runtime19.jsx)(A, {
          tag: "span",
          variant: "pi",
          fontWeight: "bold",
          children: capitalize(status)
        })
      }) : null
    ]
  });
};
var LocaleOptionStartIcon = ({ entryWithLocaleExists, translationStatus, index: index4 }) => {
  const isAiAvailable = useAIAvailability();
  if (!entryWithLocaleExists) {
    return (0, import_jsx_runtime19.jsx)(en, {});
  }
  if (isAiAvailable && index4 !== 0 && translationStatus === "failed") {
    return (0, import_jsx_runtime19.jsx)(Jn, {
      fill: "warning600"
    });
  }
  return null;
};
var LocalePickerAction = ({ document, meta, model, collectionType, documentId }) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const [{ query }, setQuery] = useQueryParams();
  const { hasI18n, canCreate, canRead } = useI18n();
  const { data: locales = [] } = useGetLocalesQuery();
  const currentDesiredLocale = (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale;
  const { schema } = useDocument({
    model,
    collectionType,
    documentId,
    params: {
      locale: currentDesiredLocale
    }
  });
  const { data: jobData } = useGetAILocalizationJobsByDocumentQuery({
    documentId,
    model,
    collectionType
  });
  const { data: settings } = useGetSettingsQuery();
  const isAiAvailable = useAIAvailability();
  const handleSelect = React13.useCallback((value) => {
    setQuery({
      plugins: {
        ...query.plugins,
        i18n: {
          locale: value
        }
      }
    }, "push", true);
  }, [
    query.plugins,
    setQuery
  ]);
  React13.useEffect(() => {
    if (!Array.isArray(locales) || !hasI18n) {
      return;
    }
    const doesLocaleExist = locales.find((loc) => loc.code === currentDesiredLocale);
    const accessibleLocales = locales.filter((locale) => canRead.includes(locale.code));
    const targetLocale = accessibleLocales.find((locale) => locale.isDefault) ?? accessibleLocales[0];
    if (!doesLocaleExist && (targetLocale == null ? void 0 : targetLocale.code)) {
      handleSelect(targetLocale.code);
    }
  }, [
    handleSelect,
    hasI18n,
    locales,
    currentDesiredLocale,
    canRead
  ]);
  const currentLocale = Array.isArray(locales) ? locales.find((locale) => locale.code === currentDesiredLocale) : void 0;
  const availableLocales = (meta == null ? void 0 : meta.availableLocales) ?? [];
  const documentLocalizations = (document == null ? void 0 : document.localizations) ?? [];
  const allLocalizations = availableLocales.length > 0 ? availableLocales : documentLocalizations;
  const allCurrentLocales = [
    {
      status: getDocumentStatus(document, meta),
      locale: currentLocale == null ? void 0 : currentLocale.code
    },
    ...allLocalizations
  ];
  if (!hasI18n || !Array.isArray(locales) || locales.length === 0) {
    return null;
  }
  const displayedLocales = locales.filter((locale) => {
    return canRead.includes(locale.code);
  });
  const localesSortingDefaultFirst = displayedLocales.sort((a, b) => a.isDefault ? -1 : b.isDefault ? 1 : 0);
  return {
    label: formatMessage({
      id: getTranslation("Settings.locales.modal.locales.label"),
      defaultMessage: "Locales"
    }),
    options: localesSortingDefaultFirst.map((locale, index4) => {
      var _a2, _b2, _c;
      const entryWithLocaleExists = allCurrentLocales.some((doc) => doc.locale === locale.code);
      const currentLocaleDoc = allCurrentLocales.find((doc) => "locale" in doc ? doc.locale === locale.code : false);
      const permissionsToCheck = currentLocaleDoc ? canRead : canCreate;
      if (isAiAvailable && ((_a2 = settings == null ? void 0 : settings.data) == null ? void 0 : _a2.aiLocalizations)) {
        return {
          _render: () => {
            var _a3, _b3;
            return (0, import_jsx_runtime19.jsxs)(React13.Fragment, {
              children: [
                (0, import_jsx_runtime19.jsx)(dr, {
                  disabled: !permissionsToCheck.includes(locale.code),
                  startIcon: (0, import_jsx_runtime19.jsx)(LocaleOptionStartIcon, {
                    entryWithLocaleExists,
                    translationStatus: (_a3 = jobData == null ? void 0 : jobData.data) == null ? void 0 : _a3.status,
                    index: index4
                  }),
                  value: locale.code,
                  children: (0, import_jsx_runtime19.jsx)(LocaleOption, {
                    isDraftAndPublishEnabled: !!((_b3 = schema == null ? void 0 : schema.options) == null ? void 0 : _b3.draftAndPublish),
                    locale,
                    status: currentLocaleDoc == null ? void 0 : currentLocaleDoc.status,
                    entryExists: entryWithLocaleExists
                  })
                }, locale.code),
                localesSortingDefaultFirst.length > 1 && index4 === 0 && (0, import_jsx_runtime19.jsx)(S, {
                  paddingRight: 4,
                  paddingLeft: 4,
                  paddingTop: 2,
                  paddingBottom: 2,
                  children: (0, import_jsx_runtime19.jsx)(A, {
                    variant: "sigma",
                    children: formatMessage({
                      id: getTranslation("CMEditViewLocalePicker.locale.ai-translations"),
                      defaultMessage: "AI Translations"
                    })
                  })
                })
              ]
            }, index4);
          }
        };
      }
      return {
        disabled: !permissionsToCheck.includes(locale.code),
        value: locale.code,
        label: (0, import_jsx_runtime19.jsx)(LocaleOption, {
          isDraftAndPublishEnabled: !!((_b2 = schema == null ? void 0 : schema.options) == null ? void 0 : _b2.draftAndPublish),
          locale,
          status: currentLocaleDoc == null ? void 0 : currentLocaleDoc.status,
          entryExists: entryWithLocaleExists,
          translationStatus: (_c = jobData == null ? void 0 : jobData.data) == null ? void 0 : _c.status
        }),
        startIcon: (0, import_jsx_runtime19.jsx)(LocaleOptionStartIcon, {
          entryWithLocaleExists
        })
      };
    }),
    customizeContent: () => currentLocale == null ? void 0 : currentLocale.name,
    onSelect: handleSelect,
    value: currentLocale
  };
};
var getDocumentStatus = (document, meta) => {
  const docStatus = document == null ? void 0 : document.status;
  const statuses = (meta == null ? void 0 : meta.availableStatus) ?? [];
  if (!docStatus) {
    return "draft";
  }
  if (docStatus === "draft" && statuses.find((doc) => doc.publishedAt !== null)) {
    return "published";
  }
  return docStatus;
};
var AITranslationStatusIcon = gt(tu)`
  display: flex;
  gap: ${({ theme }) => theme.spaces[1]};
  justify-content: center;
  align-items: center;
  height: 3.2rem;
  width: 3.2rem;

  // Disabled state
  ${({ $isAISettingEnabled, theme }) => !$isAISettingEnabled && `
    background-color: ${theme.colors.neutral150};
  `}

  svg {
    ${({ $isAISettingEnabled, theme }) => !$isAISettingEnabled && `
        fill: ${theme.colors.neutral300};
      `}
  }
`;
var SpinningLoader = gt(L3)`
  @keyframes spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  animation: spin 2s linear infinite;
`;
var AITranslationStatusAction = ({ documentId, model, collectionType }) => {
  var _a;
  const { formatMessage } = useIntl();
  const isAIAvailable = useAIAvailability();
  const { data: settings } = useGetSettingsQuery();
  const isAISettingEnabled = (_a = settings == null ? void 0 : settings.data) == null ? void 0 : _a.aiLocalizations;
  const { hasI18n } = useI18n();
  const { status } = useAILocalizationJobsPolling({
    documentId,
    model,
    collectionType
  });
  const statusVariant = (() => {
    if (status === "failed") {
      return "warning";
    }
    if (isAISettingEnabled) {
      return "alternative";
    }
    return "neutral";
  })();
  if (!hasI18n) {
    return null;
  }
  if (!isAIAvailable) {
    return null;
  }
  return {
    _status: {
      message: (0, import_jsx_runtime19.jsx)(C, {
        height: "100%",
        alignItems: "center",
        "aria-label": formatMessage({
          id: getTranslation("CMEditViewAITranslation.status-aria-label"),
          defaultMessage: "AI Translation Status"
        }),
        children: (0, import_jsx_runtime19.jsx)(AITranslationStatusIcon, {
          $isAISettingEnabled: Boolean(isAISettingEnabled),
          variant: statusVariant,
          size: "S",
          children: status === "processing" ? (0, import_jsx_runtime19.jsx)(SpinningLoader, {}) : (0, import_jsx_runtime19.jsx)(Vn, {})
        })
      }),
      tooltip: (0, import_jsx_runtime19.jsxs)(C, {
        direction: "column",
        padding: 4,
        alignItems: "flex-start",
        width: "25rem",
        children: [
          (0, import_jsx_runtime19.jsx)(A, {
            variant: "pi",
            fontWeight: "600",
            children: formatMessage({
              id: getTranslation("CMEditViewAITranslation.status-title"),
              defaultMessage: "{enabled, select, true {AI translation enabled} false {AI translation disabled} other {AI translation disabled}}"
            }, {
              enabled: isAISettingEnabled
            })
          }),
          (0, import_jsx_runtime19.jsx)(A, {
            variant: "pi",
            paddingTop: 1,
            paddingBottom: 3,
            children: formatMessage({
              id: getTranslation("CMEditViewAITranslation.status-description"),
              defaultMessage: "Our AI translates content in all locales each time you save a modification in the default locale."
            })
          }),
          (0, import_jsx_runtime19.jsx)(Oa, {
            fontSize: "inherit",
            tag: NavLink,
            to: "/settings/internationalization",
            style: {
              alignSelf: "flex-end"
            },
            children: (0, import_jsx_runtime19.jsx)(A, {
              variant: "pi",
              textAlign: "right",
              children: formatMessage({
                id: getTranslation("CMEditViewAITranslation.settings-link"),
                defaultMessage: "{enabled, select, true {Disable it in settings} false {Enable it in settings} other {Enable it in settings}}"
              }, {
                enabled: isAISettingEnabled
              })
            })
          })
        ]
      })
    }
  };
};
var FillFromAnotherLocaleAction = ({ documentId, meta, model, collectionType }) => {
  var _a, _b, _c;
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const [{ query }] = useQueryParams();
  const { hasI18n } = useI18n();
  const currentDesiredLocale = (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale;
  const [localeSelected, setLocaleSelected] = React13.useState(null);
  const setValues = useForm("FillFromAnotherLocale", (state) => state.setValues);
  const [getFillFromLocaleData, { isLoading: isFillFromLocaleLoading }] = useLazyGetFillFromLocaleDataQuery();
  const { data: locales = [] } = useGetLocalesQuery();
  const isAIAvailable = useAIAvailability();
  const { data: settings } = useGetSettingsQuery();
  const isAISettingEnabled = (_c = settings == null ? void 0 : settings.data) == null ? void 0 : _c.aiLocalizations;
  const availableLocales = Array.isArray(locales) ? locales.filter((locale) => meta == null ? void 0 : meta.availableLocales.some((l) => l.locale === locale.code)) : [];
  const fillFromLocale = (onClose) => async () => {
    if (!localeSelected || !currentDesiredLocale) {
      return;
    }
    try {
      const { data } = await getFillFromLocaleData({
        model,
        documentId,
        sourceLocale: localeSelected,
        targetLocale: currentDesiredLocale,
        collectionType
      }).unwrap();
      if (data) {
        setValues(data);
      }
      onClose();
    } catch (err) {
      toggleNotification({
        type: "danger",
        message: formatAPIError(err)
      });
    }
  };
  if (!hasI18n) {
    return null;
  }
  if (isAIAvailable && isAISettingEnabled) {
    return null;
  }
  return {
    type: "icon",
    icon: (0, import_jsx_runtime19.jsx)(T5, {}),
    disabled: availableLocales.length === 0,
    label: formatMessage({
      id: getTranslation("CMEditViewCopyLocale.copy-text"),
      defaultMessage: "Fill in from another locale"
    }),
    dialog: {
      type: "dialog",
      title: formatMessage({
        id: getTranslation("CMEditViewCopyLocale.dialog.title"),
        defaultMessage: "Confirmation"
      }),
      content: ({ onClose }) => (0, import_jsx_runtime19.jsxs)(import_jsx_runtime19.Fragment, {
        children: [
          (0, import_jsx_runtime19.jsx)(Pd.Body, {
            children: (0, import_jsx_runtime19.jsxs)(C, {
              direction: "column",
              gap: 3,
              children: [
                (0, import_jsx_runtime19.jsx)(Jn, {
                  width: "24px",
                  height: "24px",
                  fill: "danger600"
                }),
                (0, import_jsx_runtime19.jsx)(A, {
                  textAlign: "center",
                  children: formatMessage({
                    id: getTranslation("CMEditViewCopyLocale.dialog.body"),
                    defaultMessage: "Your current content will be erased and filled by the content of the selected locale:"
                  })
                }),
                (0, import_jsx_runtime19.jsxs)(Dd.Root, {
                  width: "100%",
                  children: [
                    (0, import_jsx_runtime19.jsx)(Dd.Label, {
                      children: formatMessage({
                        id: getTranslation("CMEditViewCopyLocale.dialog.field.label"),
                        defaultMessage: "Locale"
                      })
                    }),
                    (0, import_jsx_runtime19.jsx)(cr, {
                      value: localeSelected,
                      placeholder: formatMessage({
                        id: getTranslation("CMEditViewCopyLocale.dialog.field.placeholder"),
                        defaultMessage: "Select one locale..."
                      }),
                      // @ts-expect-error – the DS will handle numbers, but we're not allowing the API.
                      onChange: (value) => setLocaleSelected(value),
                      children: availableLocales.map((locale) => (0, import_jsx_runtime19.jsx)(dr, {
                        value: locale.code,
                        children: locale.name
                      }, locale.code))
                    })
                  ]
                })
              ]
            })
          }),
          (0, import_jsx_runtime19.jsx)(Pd.Footer, {
            children: (0, import_jsx_runtime19.jsxs)(C, {
              gap: 2,
              width: "100%",
              children: [
                (0, import_jsx_runtime19.jsx)(Ct, {
                  flex: "auto",
                  variant: "tertiary",
                  onClick: onClose,
                  disabled: isFillFromLocaleLoading,
                  children: formatMessage({
                    id: getTranslation("CMEditViewCopyLocale.cancel-text"),
                    defaultMessage: "No, cancel"
                  })
                }),
                (0, import_jsx_runtime19.jsx)(Ct, {
                  flex: "auto",
                  variant: "success",
                  onClick: fillFromLocale(onClose),
                  loading: isFillFromLocaleLoading,
                  disabled: isFillFromLocaleLoading,
                  children: formatMessage({
                    id: getTranslation("CMEditViewCopyLocale.submit-text"),
                    defaultMessage: "Yes, fill in"
                  })
                })
              ]
            })
          })
        ]
      })
    }
  };
};
var DeleteLocaleAction = ({ document, documentId, model, collectionType }) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const navigate = useNavigate();
  const { toggleNotification } = useNotification();
  const { delete: deleteAction, isLoading } = useDocumentActions();
  const { hasI18n, canDelete } = useI18n();
  const [{ query }] = useQueryParams();
  const { data: locales = [] } = useGetLocalesQuery();
  const currentDesiredLocale = (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale;
  const locale = !("error" in locales) && locales.find((loc) => loc.code === currentDesiredLocale);
  if (!hasI18n) {
    return null;
  }
  return {
    disabled: (document == null ? void 0 : document.locale) && !canDelete.includes(document.locale) || !document || !document.id,
    position: [
      "header",
      "table-row"
    ],
    label: formatMessage({
      id: getTranslation("actions.delete.label"),
      defaultMessage: "Delete entry ({locale})"
    }, {
      locale: locale && locale.name
    }),
    icon: (0, import_jsx_runtime19.jsx)(StyledTrash, {}),
    variant: "danger",
    dialog: {
      type: "dialog",
      title: formatMessage({
        id: getTranslation("actions.delete.dialog.title"),
        defaultMessage: "Confirmation"
      }),
      content: (0, import_jsx_runtime19.jsxs)(C, {
        direction: "column",
        gap: 2,
        children: [
          (0, import_jsx_runtime19.jsx)(Jn, {
            width: "24px",
            height: "24px",
            fill: "danger600"
          }),
          (0, import_jsx_runtime19.jsx)(A, {
            tag: "p",
            variant: "omega",
            textAlign: "center",
            children: formatMessage({
              id: getTranslation("actions.delete.dialog.body"),
              defaultMessage: "Are you sure?"
            })
          })
        ]
      }),
      loading: isLoading,
      onConfirm: async () => {
        const unableToDelete = (
          // We are unable to delete a collection type without a document ID
          // & unable to delete generally if there is no document locale
          collectionType !== "single-types" && !documentId || !(document == null ? void 0 : document.locale)
        );
        if (unableToDelete) {
          console.error("You're trying to delete a document without an id or locale, this is likely a bug with Strapi. Please open an issue.");
          toggleNotification({
            message: formatMessage({
              id: getTranslation("actions.delete.error"),
              defaultMessage: "An error occurred while trying to delete the document locale."
            }),
            type: "danger"
          });
          return;
        }
        const res = await deleteAction({
          documentId,
          model,
          collectionType,
          params: {
            locale: document.locale
          }
        });
        if (!("error" in res)) {
          navigate({
            pathname: `../${collectionType}/${model}`
          }, {
            replace: true
          });
        }
      }
    }
  };
};
var BulkLocaleAction = ({ document, documentId, model, collectionType, action, meta }) => {
  var _a;
  const locale = (document == null ? void 0 : document.locale) ?? null;
  const [{ query }] = useQueryParams();
  const params = React13.useMemo(() => buildValidParams(query), [
    query
  ]);
  const isOnPublishedTab = query.status === "published";
  const { formatMessage } = useIntl();
  const { hasI18n, canPublish } = useI18n();
  const { toggleNotification } = useNotification();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const [selectedRows, setSelectedRows] = React13.useState([]);
  const [isDraftRelationConfirmationOpen, setIsDraftRelationConfirmationOpen] = React13.useState(false);
  const { publishMany: publishManyAction, unpublishMany: unpublishManyAction } = useDocumentActions();
  const { schema, validate } = useDocument({
    model,
    collectionType,
    documentId,
    params: {
      locale
    }
  }, {
    // No need to fetch the document, the data is already available in the `document` prop
    skip: true
  });
  const { data: localesMetadata = [] } = useGetLocalesQuery(hasI18n ? void 0 : skipToken);
  const headers = [
    {
      label: formatMessage({
        id: "global.name",
        defaultMessage: "Name"
      }),
      name: "name"
    },
    {
      label: formatMessage({
        id: getTranslation("CMEditViewBulkLocale.status"),
        defaultMessage: "Status"
      }),
      name: "status"
    },
    {
      label: formatMessage({
        id: getTranslation("CMEditViewBulkLocale.publication-status"),
        defaultMessage: "Publication Status"
      }),
      name: "publication-status"
    }
  ];
  const [rows, validationErrors] = React13.useMemo(() => {
    if (!document) {
      return [
        [],
        {}
      ];
    }
    const metaLocalizations = ((meta == null ? void 0 : meta.availableLocales) ?? []).map((locale2) => ({
      locale: locale2.locale,
      status: locale2.status ?? "draft"
    }));
    const documentLocalizations = (document.localizations ?? []).map((doc) => ({
      locale: doc.locale ?? void 0,
      status: doc.status ?? "draft"
    }));
    const localesMap = /* @__PURE__ */ new Map();
    metaLocalizations.forEach(({ locale: locale2, status }) => {
      if (locale2) {
        localesMap.set(locale2, {
          locale: locale2,
          status
        });
      }
    });
    documentLocalizations.forEach(({ locale: locale2, status }) => {
      if (locale2) {
        localesMap.set(locale2, {
          locale: locale2,
          status
        });
      }
    });
    const locales = [];
    if (document == null ? void 0 : document.locale) {
      locales.push({
        locale: document.locale,
        status: document.status ?? "draft"
      });
    }
    locales.push(...Array.from(localesMap.entries()).filter(([locale2]) => locale2 !== (document == null ? void 0 : document.locale)).map(([, value]) => value));
    if (locales.length === 0 && (document == null ? void 0 : document.locale)) {
      locales.push({
        locale: document.locale,
        status: document.status ?? "draft"
      });
    }
    const errors = {};
    if (document.locale) {
      const validation = validate(document);
      if (validation !== null) {
        errors[document.locale] = validation;
      }
    }
    return [
      locales,
      errors
    ];
  }, [
    document,
    meta == null ? void 0 : meta.availableLocales,
    validate
  ]);
  const isBulkPublish = action === "bulk-publish";
  const localesForAction = selectedRows.reduce((acc, selectedRow) => {
    const isValidLocale = (
      // Validation errors are irrelevant if we are trying to unpublish
      !isBulkPublish || !Object.keys(validationErrors).includes(selectedRow.locale)
    );
    const shouldAddLocale = isBulkPublish ? selectedRow.status !== "published" && isValidLocale : selectedRow.status !== "draft" && isValidLocale;
    if (shouldAddLocale) {
      acc.push(selectedRow.locale);
    }
    return acc;
  }, []);
  const { data: draftRelationsCount = 0, isLoading: isDraftRelationsLoading, error: isDraftRelationsError } = useGetManyDraftRelationCountQuery({
    model,
    documentIds: [
      documentId
    ],
    locale: localesForAction
  }, {
    skip: true
  });
  React13.useEffect(() => {
    if (isDraftRelationsError) {
      toggleNotification({
        type: "danger",
        message: formatAPIError(isDraftRelationsError)
      });
    }
  }, [
    isDraftRelationsError,
    toggleNotification,
    formatAPIError
  ]);
  if (!((_a = schema == null ? void 0 : schema.options) == null ? void 0 : _a.draftAndPublish)) {
    return null;
  }
  if (!hasI18n) {
    return null;
  }
  if (!documentId) {
    return null;
  }
  const publish = async () => {
    await publishManyAction({
      model,
      documentIds: [
        documentId
      ],
      params: {
        ...params,
        locale: localesForAction
      }
    });
    setSelectedRows([]);
  };
  const unpublish = async () => {
    await unpublishManyAction({
      model,
      documentIds: [
        documentId
      ],
      params: {
        ...params,
        locale: localesForAction
      }
    });
    setSelectedRows([]);
  };
  const handleAction = async () => {
    if (draftRelationsCount > 0) {
      setIsDraftRelationConfirmationOpen(true);
    } else if (isBulkPublish) {
      await publish();
    } else {
      await unpublish();
    }
  };
  if (isDraftRelationConfirmationOpen) {
    return {
      label: formatMessage({
        id: "app.components.ConfirmDialog.title",
        defaultMessage: "Confirmation"
      }),
      variant: "danger",
      dialog: {
        onCancel: () => {
          setIsDraftRelationConfirmationOpen(false);
        },
        onConfirm: async () => {
          await publish();
          setIsDraftRelationConfirmationOpen(false);
        },
        type: "dialog",
        title: formatMessage({
          id: getTranslation("actions.publish.dialog.title"),
          defaultMessage: "Confirmation"
        }),
        content: (0, import_jsx_runtime19.jsxs)(C, {
          direction: "column",
          alignItems: "center",
          gap: 2,
          children: [
            (0, import_jsx_runtime19.jsx)(Jn, {
              width: "2.4rem",
              height: "2.4rem",
              fill: "danger600"
            }),
            (0, import_jsx_runtime19.jsx)(A, {
              textAlign: "center",
              children: formatMessage({
                id: getTranslation("CMEditViewBulkLocale.draft-relation-warning"),
                defaultMessage: "Some locales are related to draft entries. Publishing them could leave broken links in your app."
              })
            }),
            (0, import_jsx_runtime19.jsx)(A, {
              textAlign: "center",
              children: formatMessage({
                id: getTranslation("CMEditViewBulkLocale.continue-confirmation"),
                defaultMessage: "Are you sure you want to continue?"
              })
            })
          ]
        })
      }
    };
  }
  const hasPermission = selectedRows.map(({ locale: locale2 }) => locale2).every((locale2) => canPublish.includes(locale2));
  return {
    label: formatMessage({
      id: getTranslation(`CMEditViewBulkLocale.${isBulkPublish ? "publish" : "unpublish"}-title`),
      defaultMessage: `${isBulkPublish ? "Publish" : "Unpublish"} Multiple Locales`
    }),
    variant: isBulkPublish ? "secondary" : "danger",
    icon: isBulkPublish ? (0, import_jsx_runtime19.jsx)(V3, {}) : (0, import_jsx_runtime19.jsx)(F5, {}),
    disabled: isOnPublishedTab || canPublish.length === 0,
    position: [
      "panel"
    ],
    dialog: {
      type: "modal",
      title: formatMessage({
        id: getTranslation(`CMEditViewBulkLocale.${isBulkPublish ? "publish" : "unpublish"}-title`),
        defaultMessage: `${isBulkPublish ? "Publish" : "Unpublish"} Multiple Locales`
      }),
      content: () => {
        return (0, import_jsx_runtime19.jsx)(Table.Root, {
          headers,
          rows: rows.map((row) => ({
            ...row,
            id: row.locale
          })),
          selectedRows,
          onSelectedRowsChange: (tableSelectedRows) => setSelectedRows(tableSelectedRows),
          children: (0, import_jsx_runtime19.jsx)(BulkLocaleActionModal, {
            validationErrors,
            headers,
            rows,
            localesMetadata,
            action: action ?? "bulk-publish"
          })
        });
      },
      footer: () => (0, import_jsx_runtime19.jsx)(_d.Footer, {
        justifyContent: "flex-end",
        children: (0, import_jsx_runtime19.jsx)(Ct, {
          loading: isDraftRelationsLoading,
          disabled: !hasPermission || localesForAction.length === 0,
          variant: "default",
          onClick: handleAction,
          children: formatMessage({
            id: isBulkPublish ? "app.utils.publish" : "app.utils.unpublish",
            defaultMessage: isBulkPublish ? "Publish" : "Unpublish"
          })
        })
      })
    }
  };
};
var BulkLocalePublishAction = (props) => {
  return BulkLocaleAction({
    action: "bulk-publish",
    ...props
  });
};
var BulkLocaleUnpublishAction = (props) => {
  return BulkLocaleAction({
    action: "bulk-unpublish",
    ...props
  });
};
var StyledTrash = gt(In)`
  path {
    fill: currentColor;
  }
`;

// node_modules/@strapi/i18n/dist/admin/components/CMListViewModalsAdditionalInformation.mjs
var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
var import_react3 = __toESM(require_react(), 1);
var Emphasis = (chunks) => {
  return (0, import_jsx_runtime20.jsx)(A, {
    fontWeight: "semiBold",
    textColor: "danger500",
    children: chunks
  });
};
var DeleteModalAdditionalInfo = () => {
  const { hasI18n } = useI18n();
  const { formatMessage } = useIntl();
  if (!hasI18n) {
    return null;
  }
  return (0, import_jsx_runtime20.jsx)(A, {
    textColor: "danger500",
    children: formatMessage({
      id: getTranslation("Settings.list.actions.deleteAdditionalInfos"),
      defaultMessage: "This will delete the active locale versions <em>(from Internationalization)</em>"
    }, {
      em: Emphasis
    })
  });
};
var PublishModalAdditionalInfo = () => {
  const { hasI18n } = useI18n();
  const { formatMessage } = useIntl();
  if (!hasI18n) {
    return null;
  }
  return (0, import_jsx_runtime20.jsx)(A, {
    textColor: "danger500",
    children: formatMessage({
      id: getTranslation("Settings.list.actions.publishAdditionalInfos"),
      defaultMessage: "This will publish the active locale versions <em>(from Internationalization)</em>"
    }, {
      em: Emphasis
    })
  });
};
var UnpublishModalAdditionalInfo = () => {
  const { hasI18n } = useI18n();
  const { formatMessage } = useIntl();
  if (!hasI18n) {
    return null;
  }
  return (0, import_jsx_runtime20.jsx)(A, {
    textColor: "danger500",
    children: formatMessage({
      id: getTranslation("Settings.list.actions.unpublishAdditionalInfos"),
      defaultMessage: "This will unpublish the active locale versions <em>(from Internationalization)</em>"
    }, {
      em: Emphasis
    })
  });
};

// node_modules/@strapi/i18n/dist/admin/components/LocalePicker.mjs
var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
var React14 = __toESM(require_react(), 1);
var LocalePicker = () => {
  var _a, _b, _c, _d2, _e;
  const { formatMessage } = useIntl();
  const [{ query }, setQuery] = useQueryParams();
  const { hasI18n, canRead, canCreate } = useI18n();
  const { data: locales = [] } = useGetLocalesQuery(void 0, {
    skip: !hasI18n
  });
  const handleChange = React14.useCallback((code, replace = false) => {
    setQuery(withEncodedUserParams(query, {
      page: 1,
      plugins: {
        ...query.plugins,
        i18n: {
          locale: code
        }
      }
    }), "push", replace);
  }, [
    query,
    setQuery
  ]);
  React14.useEffect(() => {
    var _a2, _b2;
    if (!Array.isArray(locales) || !hasI18n) {
      return;
    }
    const currentDesiredLocale = (_b2 = (_a2 = query.plugins) == null ? void 0 : _a2.i18n) == null ? void 0 : _b2.locale;
    const doesLocaleExist = locales.find((loc) => loc.code === currentDesiredLocale);
    const readableLocales = locales.filter((locale) => canRead.includes(locale.code));
    const targetLocale = readableLocales.find((locale) => locale.isDefault) ?? readableLocales[0];
    if (!doesLocaleExist && (targetLocale == null ? void 0 : targetLocale.code)) {
      handleChange(targetLocale.code, true);
    }
  }, [
    hasI18n,
    handleChange,
    locales,
    (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale,
    canRead
  ]);
  const sortedLocaleOptions = React14.useMemo(() => {
    const displayedLocales = Array.isArray(locales) ? locales.filter((locale) => {
      return canCreate.includes(locale.code) || canRead.includes(locale.code);
    }) : [];
    return displayedLocales.sort((a, b) => a.name.localeCompare(b.name)).map((locale) => (0, import_jsx_runtime21.jsx)(dr, {
      value: locale.code,
      children: locale.name
    }, locale.id));
  }, [
    locales,
    canCreate,
    canRead
  ]);
  if (!hasI18n || !Array.isArray(locales) || locales.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime21.jsx)(cr, {
    size: "S",
    "aria-label": formatMessage({
      id: getTranslation("actions.select-locale"),
      defaultMessage: "Select locale"
    }),
    value: ((_d2 = (_c = query.plugins) == null ? void 0 : _c.i18n) == null ? void 0 : _d2.locale) || ((_e = locales.find((locale) => locale.isDefault)) == null ? void 0 : _e.code),
    // @ts-expect-error – This can be removed in V2 of the DS.
    onChange: handleChange,
    children: sortedLocaleOptions
  });
};

// node_modules/@strapi/i18n/dist/admin/contentManagerHooks/editView.mjs
var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
var hasI18nLayoutOptions = (options) => "i18n" in options && typeof options.i18n === "object" && options.i18n !== null;
var mutateEditViewHook = ({ layout }) => {
  if (!("i18n" in layout.options) || typeof layout.options.i18n === "object" && layout.options.i18n !== null && "localized" in layout.options.i18n && !layout.options.i18n.localized) {
    return {
      layout
    };
  }
  const decorateField = (field) => addLabelActionToField(field, layout);
  const components = Object.entries(layout.components).reduce((acc, [key, componentLayout]) => {
    return {
      ...acc,
      [key]: {
        ...componentLayout,
        layout: componentLayout.layout.map((row) => row.map(decorateField))
      }
    };
  }, {});
  return {
    layout: {
      ...layout,
      components,
      layout: layout.layout.map((panel) => panel.map((row) => row.map(decorateField)))
    }
  };
};
var isFieldLocalized = (attribute, layout) => {
  var _a, _b;
  const contentTypeLocalized = hasI18nLayoutOptions(layout.options) && ((_a = layout.options.i18n) == null ? void 0 : _a.localized) === true;
  if (!contentTypeLocalized) {
    return false;
  }
  const pluginOptions = attribute && typeof attribute === "object" && "pluginOptions" in attribute ? attribute.pluginOptions : void 0;
  return ((_b = pluginOptions == null ? void 0 : pluginOptions.i18n) == null ? void 0 : _b.localized) === true;
};
var addLabelActionToField = (field, layout) => {
  const localized = isFieldLocalized(field.attribute, layout);
  if (!localized) {
    return field;
  }
  const title = {
    id: getTranslation("Field.localized"),
    defaultMessage: "This value is unique for the selected locale"
  };
  return {
    ...field,
    labelAction: (0, import_jsx_runtime22.jsx)(LabelAction, {
      title
    })
  };
};
var LabelAction = ({ title }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime22.jsxs)(Span, {
    tag: "span",
    title,
    children: [
      (0, import_jsx_runtime22.jsx)($e, {
        tag: "span",
        children: formatMessage(title)
      }),
      (0, import_jsx_runtime22.jsx)(Mt, {
        label: formatMessage(title),
        children: (0, import_jsx_runtime22.jsx)(T5, {
          "aria-hidden": true,
          focusable: false
        })
      })
    ]
  });
};
var Span = gt(C)`
  svg {
    width: 12px;
    height: 12px;

    fill: ${({ theme }) => theme.colors.neutral500};

    path {
      fill: ${({ theme }) => theme.colors.neutral500};
    }
  }
`;

// node_modules/@strapi/i18n/dist/admin/contentManagerHooks/listView.mjs
var import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/i18n/dist/admin/components/LocaleListCell.mjs
var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
var import_qs13 = __toESM(require_lib(), 1);
var LocaleListCell = ({ locale: currentLocale, localizations, documentId }) => {
  const { locale: language, formatMessage } = useIntl();
  const { data: locales = [] } = useGetLocalesQuery();
  const navigate = useNavigate();
  const [{ query }] = useQueryParams();
  const formatter = Qt(language, {
    sensitivity: "base"
  });
  if (!Array.isArray(locales) || !localizations) {
    return null;
  }
  const availableLocales = localizations.map((loc) => loc.locale);
  const localesForDocument = locales.reduce((acc, locale) => {
    const createdLocale = [
      currentLocale,
      ...availableLocales
    ].find((loc) => {
      return loc === locale.code;
    });
    if (createdLocale) {
      const name2 = locale.isDefault ? `${locale.name} (default)` : locale.name;
      acc.push({
        code: locale.code,
        name: name2
      });
    }
    return acc;
  }, []).toSorted((a, b) => formatter.compare(a.name, b.name));
  const getDisplayText = () => {
    const displayedLocales = localesForDocument.slice(0, 2);
    const remainingCount = localesForDocument.length - 2;
    const baseText = displayedLocales.map(({ name: name2 }) => name2).join(", ");
    if (remainingCount <= 0) {
      return baseText;
    }
    return formatMessage({
      id: getTranslation("CMListView.popover.display-locales.more"),
      defaultMessage: "{locales} + {count} more"
    }, {
      locales: baseText,
      count: remainingCount
    });
  };
  const handleLocaleClick = (localeCode) => {
    navigate({
      pathname: documentId,
      search: (0, import_qs13.stringify)({
        plugins: {
          ...query.plugins,
          i18n: {
            locale: localeCode
          }
        }
      })
    });
  };
  return (0, import_jsx_runtime23.jsxs)(gd.Root, {
    children: [
      (0, import_jsx_runtime23.jsx)(gd.Trigger, {
        children: (0, import_jsx_runtime23.jsx)(C, {
          minWidth: "100%",
          alignItems: "center",
          justifyContent: "center",
          fontWeight: "regular",
          children: (0, import_jsx_runtime23.jsx)(A, {
            textColor: "neutral800",
            ellipsis: true,
            marginRight: 2,
            children: getDisplayText()
          })
        })
      }),
      (0, import_jsx_runtime23.jsx)(gd.Content, {
        children: localesForDocument.map(({ code, name: name2 }) => (0, import_jsx_runtime23.jsx)(gd.Item, {
          onClick: (e) => {
            e.stopPropagation();
            handleLocaleClick(code);
          },
          children: (0, import_jsx_runtime23.jsx)(A, {
            textColor: "neutral800",
            fontWeight: "regular",
            children: name2
          })
        }, code))
      })
    ]
  });
};

// node_modules/@strapi/i18n/dist/admin/contentManagerHooks/listView.mjs
var addColumnToTableHook = ({ displayedHeaders, layout }) => {
  const { options } = layout;
  const isFieldLocalized2 = doesPluginOptionsHaveI18nLocalized(options) ? options.i18n.localized : false;
  if (!isFieldLocalized2) {
    return {
      displayedHeaders,
      layout
    };
  }
  return {
    displayedHeaders: [
      ...displayedHeaders,
      {
        attribute: {
          type: "string"
        },
        label: {
          id: getTranslation("list-view.table.header.label"),
          defaultMessage: "Available in"
        },
        searchable: false,
        sortable: false,
        name: "locales",
        // @ts-expect-error – ID is seen as number | string; this will change when we move the type over.
        cellFormatter: (props, _header, meta) => (0, import_jsx_runtime24.jsx)(LocaleListCell, {
          ...props,
          ...meta,
          documentId: props.documentId
        })
      }
    ],
    layout
  };
};

// node_modules/@strapi/i18n/dist/admin/contentReleasesHooks/releaseDetailsView.mjs
var addLocaleToReleasesHook = ({ displayedHeaders = [] }) => {
  return {
    displayedHeaders: [
      ...displayedHeaders,
      {
        label: {
          id: "content-releases.page.ReleaseDetails.table.header.label.locale",
          defaultMessage: "locale"
        },
        name: "locale"
      }
    ],
    hasI18nEnabled: true
  };
};

// node_modules/@strapi/i18n/dist/admin/middlewares/extendCTBAttributeInitialData.mjs
var import_get2 = __toESM(require_get(), 1);
var extendCTBAttributeInitialDataMiddleware = () => {
  return ({ getState }) => (next) => (action) => {
    const enhanceAction = ({ uid }) => {
      var _a, _b;
      try {
        const store = getState();
        const type2 = (0, import_get2.default)(store, [
          "content-type-builder_dataManagerProvider",
          "current",
          "contentTypes",
          uid
        ]);
        if (!type2 || type2.modelType !== "contentType") {
          return next(action);
        }
        const hasi18nEnabled = (0, import_get2.default)(type2, [
          "pluginOptions",
          "i18n",
          "localized"
        ], false);
        if (hasi18nEnabled) {
          return next({
            ...action,
            payload: {
              ...action.payload,
              options: {
                pluginOptions: {
                  ...(_b = (_a = action == null ? void 0 : action.payload) == null ? void 0 : _a.options) == null ? void 0 : _b.pluginOptions,
                  i18n: {
                    localized: true
                  }
                }
              }
            }
          });
        }
        return next(action);
      } catch {
        return next(action);
      }
    };
    const { payload = {}, type } = action ?? {};
    if (type === "formModal/setAttributeDataSchema" && ![
      "relation",
      "component"
    ].includes(payload.attributeType) && !payload.isEditing) {
      return enhanceAction({
        uid: payload.uid
      });
    }
    if (type === "formModal/setCustomFieldDataSchema" && !payload.isEditing) {
      return enhanceAction({
        uid: payload.uid
      });
    }
    if (type === "formModal/resetPropsAndSetFormForAddingAnExistingCompo" || type === "formModal/resetPropsAndSaveCurrentData") {
      return enhanceAction({
        uid: payload.uid
      });
    }
    return next(action);
  };
};

// node_modules/@strapi/i18n/dist/admin/middlewares/extendCTBInitialData.mjs
var extendCTBInitialDataMiddleware = () => {
  return () => (next) => (action) => {
    var _a, _b;
    if (action.type === "ContentTypeBuilder/FormModal/SET_DATA_TO_EDIT" && action.modalType === "contentType") {
      const i18n = {
        localized: false
      };
      const pluginOptions = action.data.pluginOptions ? {
        ...action.data.pluginOptions,
        i18n
      } : {
        i18n
      };
      const data = {
        ...action.data,
        pluginOptions
      };
      if (action.actionType === "create") {
        return next({
          ...action,
          data
        });
      }
      if (!((_b = (_a = action.data.pluginOptions) == null ? void 0 : _a.i18n) == null ? void 0 : _b.localized)) {
        return next({
          ...action,
          data
        });
      }
    }
    return next(action);
  };
};

// node_modules/@strapi/i18n/dist/admin/middlewares/rbac-middleware.mjs
var qs = __toESM(require_lib(), 1);
var localeMiddleware = (ctx) => (next) => (permissions) => {
  const match = matchPath("/content-manager/:collectionType/:model?/:id", ctx.pathname);
  if (!match) {
    return next(permissions);
  }
  const search = qs.parse(ctx.search);
  if (typeof search !== "object") {
    return next(permissions);
  }
  if (!("plugins" in search && typeof search.plugins === "object")) {
    return next(permissions);
  }
  if (!("i18n" in search.plugins && typeof search.plugins.i18n === "object" && !Array.isArray(search.plugins.i18n))) {
    return next(permissions);
  }
  const { locale } = search.plugins.i18n;
  if (typeof locale !== "string") {
    return next(permissions);
  }
  const revisedPermissions = permissions.filter((permission) => {
    var _a;
    return !((_a = permission.properties) == null ? void 0 : _a.locales) || permission.properties.locales.includes(locale);
  });
  return next(revisedPermissions);
};

// node_modules/@strapi/i18n/dist/admin/utils/prefixPluginTranslations.mjs
var prefixPluginTranslations4 = (trad, pluginId5) => {
  return Object.keys(trad).reduce((acc, current) => {
    acc[`${pluginId5}.${current}`] = trad[current];
    return acc;
  }, {});
};

// node_modules/@strapi/i18n/dist/admin/utils/schemas.mjs
var import_omit = __toESM(require_omit(), 1);
var mutateCTBContentTypeSchema = (nextSchema, prevSchema) => {
  if (!prevSchema) {
    return nextSchema;
  }
  if (!doesPluginOptionsHaveI18nLocalized(nextSchema.pluginOptions)) {
    return nextSchema;
  }
  const isNextSchemaLocalized = nextSchema.pluginOptions.i18n.localized;
  const isPrevSchemaLocalized = doesPluginOptionsHaveI18nLocalized(prevSchema == null ? void 0 : prevSchema.pluginOptions) ? prevSchema == null ? void 0 : prevSchema.pluginOptions.i18n.localized : false;
  if (isNextSchemaLocalized && isPrevSchemaLocalized) {
    return nextSchema;
  }
  if (isNextSchemaLocalized) {
    const attributes = addLocalisationToFields(nextSchema.attributes);
    return {
      ...nextSchema,
      attributes
    };
  }
  if (!isNextSchemaLocalized) {
    const pluginOptions = (0, import_omit.default)(nextSchema.pluginOptions, "i18n");
    const attributes = disableAttributesLocalisation(nextSchema.attributes);
    return {
      ...nextSchema,
      pluginOptions,
      attributes
    };
  }
  return nextSchema;
};
var addLocalisationToFields = (attributes) => {
  return attributes.map((currentAttribute) => {
    if (LOCALIZED_FIELDS.includes(currentAttribute.type)) {
      const i18n = {
        localized: true
      };
      const pluginOptions = currentAttribute.pluginOptions ? {
        ...currentAttribute.pluginOptions,
        i18n
      } : {
        i18n
      };
      return {
        ...currentAttribute,
        pluginOptions
      };
    }
    return currentAttribute;
  });
};
var disableAttributesLocalisation = (attributes) => {
  return attributes.map((currentAttribute) => {
    return (0, import_omit.default)(currentAttribute, "pluginOptions.i18n");
  });
};

// node_modules/@strapi/i18n/dist/admin/index.mjs
function __variableDynamicImportRuntime1__3(path) {
  switch (path) {
    case "./translations/cs.json":
      return import("./cs.json-63PCD7AP.js");
    case "./translations/da.json":
      return import("./da.json-RFQERNUD.js");
    case "./translations/de.json":
      return import("./de.json-BCMVU5IJ.js");
    case "./translations/en.json":
      return import("./en.json-W26MAW2Z.js");
    case "./translations/es.json":
      return import("./es.json-ASNZQTVF.js");
    case "./translations/fr.json":
      return import("./fr.json-RNMMSXHA.js");
    case "./translations/ja.json":
      return import("./ja.json-5S3CT7YO.js");
    case "./translations/ko.json":
      return import("./ko.json-2SDVYBHB.js");
    case "./translations/nl.json":
      return import("./nl.json-NWR22CNP.js");
    case "./translations/pl.json":
      return import("./pl.json-L7IYTY3R.js");
    case "./translations/ru.json":
      return import("./ru.json-MFOVXBQZ.js");
    case "./translations/sk.json":
      return import("./sk.json-PCVMALZY.js");
    case "./translations/tr.json":
      return import("./tr.json-F7UQH26U.js");
    case "./translations/uk.json":
      return import("./uk.json-HPAKS75W.js");
    case "./translations/zh-Hans.json":
      return import("./zh-Hans.json-622CVQAO.js");
    case "./translations/zh.json":
      return import("./zh.json-DEPIRTVU.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
var index3 = {
  register(app) {
    app.addMiddlewares([
      extendCTBAttributeInitialDataMiddleware,
      extendCTBInitialDataMiddleware
    ]);
    app.addMiddlewares([
      () => i18nApi.middleware
    ]);
    app.addReducers({
      [i18nApi.reducerPath]: i18nApi.reducer
    });
    app.addRBACMiddleware([
      localeMiddleware
    ]);
    app.registerPlugin({
      id: pluginId3,
      name: pluginId3
    });
  },
  bootstrap(app) {
    app.registerHook("Admin/CM/pages/ListView/inject-column-in-table", addColumnToTableHook);
    app.registerHook("Admin/CM/pages/EditView/mutate-edit-view-layout", mutateEditViewHook);
    app.registerHook("ContentReleases/pages/ReleaseDetails/add-locale-in-releases", addLocaleToReleasesHook);
    app.addSettingsLink("global", {
      intlLabel: {
        id: getTranslation("plugin.name"),
        defaultMessage: "Internationalization"
      },
      id: "internationalization",
      to: "internationalization",
      Component: () => import("./SettingsPage-75KOLGHI.js").then((mod) => ({
        default: mod.ProtectedSettingsPage
      })),
      permissions: PERMISSIONS4.accessMain
    });
    const contentManager = app.getPlugin("content-manager");
    const contentManagerApis = contentManager.apis;
    contentManagerApis.addDocumentHeaderAction([
      AITranslationStatusAction,
      LocalePickerAction,
      FillFromAnotherLocaleAction
    ]);
    contentManagerApis.addDocumentAction((actions) => {
      const indexOfDeleteAction = actions.findIndex((action) => action.type === "delete");
      actions.splice(indexOfDeleteAction, 0, DeleteLocaleAction);
      return actions;
    });
    contentManagerApis.addDocumentAction((actions) => {
      actions.splice(2, 0, BulkLocalePublishAction);
      actions.splice(5, 0, BulkLocaleUnpublishAction);
      return actions;
    });
    contentManager.injectComponent("listView", "actions", {
      name: "i18n-locale-filter",
      Component: LocalePicker
    });
    contentManager.injectComponent("listView", "publishModalAdditionalInfos", {
      name: "i18n-publish-bullets-in-modal",
      Component: PublishModalAdditionalInfo
    });
    contentManager.injectComponent("listView", "unpublishModalAdditionalInfos", {
      name: "i18n-unpublish-bullets-in-modal",
      Component: UnpublishModalAdditionalInfo
    });
    contentManager.injectComponent("listView", "deleteModalAdditionalInfos", {
      name: "i18n-delete-bullets-in-modal",
      Component: DeleteModalAdditionalInfo
    });
    const ctbPlugin = app.getPlugin("content-type-builder");
    if (ctbPlugin) {
      const ctbFormsAPI = ctbPlugin.apis.forms;
      ctbFormsAPI.addContentTypeSchemaMutation(mutateCTBContentTypeSchema);
      ctbFormsAPI.components.add({
        id: "checkboxConfirmation",
        component: CheckboxConfirmation
      });
      ctbFormsAPI.extendContentType({
        validator: () => ({
          i18n: create3().shape({
            localized: create()
          })
        }),
        form: {
          advanced() {
            return [
              {
                name: "pluginOptions.i18n.localized",
                description: {
                  id: getTranslation("plugin.schema.i18n.localized.description-content-type"),
                  defaultMessage: "Allows translating an entry into different languages"
                },
                type: "checkboxConfirmation",
                intlLabel: {
                  id: getTranslation("plugin.schema.i18n.localized.label-content-type"),
                  defaultMessage: "Localization"
                }
              }
            ];
          }
        }
      });
      ctbFormsAPI.extendFields(LOCALIZED_FIELDS, {
        form: {
          advanced({ contentTypeSchema, forTarget, type, step }) {
            if (forTarget !== "contentType") {
              return [];
            }
            const hasI18nEnabled = (0, import_get3.default)(contentTypeSchema, [
              "pluginOptions",
              "i18n",
              "localized"
            ], false);
            if (!hasI18nEnabled) {
              return [];
            }
            if (type === "component" && step === "1") {
              return [];
            }
            return [
              {
                name: "pluginOptions.i18n.localized",
                description: {
                  id: getTranslation("plugin.schema.i18n.localized.description-field"),
                  defaultMessage: "The field can have different values in each locale"
                },
                type: "checkbox",
                intlLabel: {
                  id: getTranslation("plugin.schema.i18n.localized.label-field"),
                  defaultMessage: "Enable localization for this field"
                }
              }
            ];
          }
        }
      });
    }
  },
  async registerTrads({ locales }) {
    const importedTrads = await Promise.all(locales.map((locale) => {
      return __variableDynamicImportRuntime1__3(`./translations/${locale}.json`).then(({ default: data }) => {
        return {
          data: prefixPluginTranslations4(data, pluginId3),
          locale
        };
      }).catch(() => {
        return {
          data: {},
          locale
        };
      });
    }));
    return Promise.resolve(importedTrads);
  }
};

// node_modules/@strapi/content-releases/dist/admin/components/ReleaseAction.mjs
var import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
var import_react5 = __toESM(require_react(), 1);

// node_modules/@strapi/content-releases/dist/admin/components/ReleaseActionModal.mjs
var import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
var import_react4 = __toESM(require_react(), 1);
var RELEASE_ACTION_FORM_SCHEMA = create3().shape({
  type: create2().oneOf([
    "publish",
    "unpublish"
  ]).required(),
  releaseId: create2().required()
});
var INITIAL_VALUES = {
  type: "publish",
  releaseId: ""
};
var NoReleases = () => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime25.jsx)(Wd, {
    icon: (0, import_jsx_runtime25.jsx)(K, {
      width: "16rem"
    }),
    content: formatMessage({
      id: "content-releases.content-manager-edit-view.add-to-release.no-releases-message",
      defaultMessage: "No available releases. Open the list of releases and create a new one from there."
    }),
    action: (0, import_jsx_runtime25.jsx)(Hd, {
      to: {
        pathname: "/plugins/content-releases"
      },
      tag: Link,
      variant: "secondary",
      children: formatMessage({
        id: "content-releases.content-manager-edit-view.add-to-release.redirect-button",
        defaultMessage: "Open the list of releases"
      })
    }),
    shadow: "none"
  });
};
var AddActionToReleaseModal = ({ contentType, documentId, onInputChange, values }) => {
  var _a, _b, _c;
  const { formatMessage } = useIntl();
  const [{ query }] = useQueryParams();
  const locale = (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale;
  const response = useGetReleasesForEntryQuery({
    contentType,
    entryDocumentId: documentId,
    hasEntryAttached: false,
    locale
  });
  const releases = (_c = response.data) == null ? void 0 : _c.data;
  if ((releases == null ? void 0 : releases.length) === 0) {
    return (0, import_jsx_runtime25.jsx)(NoReleases, {});
  }
  return (0, import_jsx_runtime25.jsxs)(C, {
    direction: "column",
    alignItems: "stretch",
    gap: 2,
    children: [
      (0, import_jsx_runtime25.jsx)(S, {
        paddingBottom: 6,
        children: (0, import_jsx_runtime25.jsxs)(Dd.Root, {
          required: true,
          children: [
            (0, import_jsx_runtime25.jsx)(Dd.Label, {
              children: formatMessage({
                id: "content-releases.content-manager-edit-view.add-to-release.select-label",
                defaultMessage: "Select a release"
              })
            }),
            (0, import_jsx_runtime25.jsx)(cr, {
              required: true,
              placeholder: formatMessage({
                id: "content-releases.content-manager-edit-view.add-to-release.select-placeholder",
                defaultMessage: "Select"
              }),
              name: "releaseId",
              onChange: (value) => onInputChange("releaseId", value),
              value: values.releaseId,
              children: releases == null ? void 0 : releases.map((release) => (0, import_jsx_runtime25.jsx)(dr, {
                value: release.id,
                children: release.name
              }, release.id))
            })
          ]
        })
      }),
      (0, import_jsx_runtime25.jsx)(Dd.Label, {
        children: formatMessage({
          id: "content-releases.content-manager-edit-view.add-to-release.action-type-label",
          defaultMessage: "What do you want to do with this entry?"
        })
      }),
      (0, import_jsx_runtime25.jsx)(ReleaseActionOptions, {
        selected: values.type,
        handleChange: (e) => onInputChange("type", e.target.value),
        name: "type"
      })
    ]
  });
};
var ReleaseActionModalForm = ({ documentId, document, model, collectionType }) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const { allowedActions } = useRBAC(PERMISSIONS5);
  const { canCreateAction } = allowedActions;
  const [createReleaseAction, { isLoading }] = useCreateReleaseActionMutation();
  const { toggleNotification } = useNotification();
  const { formatAPIError } = useAPIErrorHandler();
  const [{ query }] = useQueryParams();
  const locale = (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale;
  const handleSubmit = async (e, onClose) => {
    try {
      await formik.handleSubmit(e);
      onClose();
    } catch (error) {
      if (isFetchError(error)) {
        toggleNotification({
          type: "danger",
          message: formatAPIError(error)
        });
      } else {
        toggleNotification({
          type: "danger",
          message: formatMessage({
            id: "notification.error",
            defaultMessage: "An error occurred"
          })
        });
      }
    }
  };
  const formik = useFormik({
    initialValues: INITIAL_VALUES,
    validationSchema: RELEASE_ACTION_FORM_SCHEMA,
    onSubmit: async (values) => {
      if (collectionType === "collection-types" && !documentId) {
        throw new Error("Document id is required");
      }
      const response = await createReleaseAction({
        body: {
          type: values.type,
          contentType: model,
          entryDocumentId: documentId,
          locale
        },
        params: {
          releaseId: values.releaseId
        }
      });
      if ("data" in response) {
        toggleNotification({
          type: "success",
          message: formatMessage({
            id: "content-releases.content-manager-edit-view.add-to-release.notification.success",
            defaultMessage: "Entry added to release"
          })
        });
        return;
      }
      if ("error" in response) {
        throw response.error;
      }
    }
  });
  const { edit: { options } } = useDocumentLayout(model);
  if (!window.strapi.isEE || !(options == null ? void 0 : options.draftAndPublish) || !canCreateAction) {
    return null;
  }
  if (collectionType === "collection-types" && (!documentId || documentId === "create")) {
    return null;
  }
  return {
    label: formatMessage({
      id: "content-releases.content-manager-edit-view.add-to-release",
      defaultMessage: "Add to release"
    }),
    icon: (0, import_jsx_runtime25.jsx)(k3, {}),
    // Entry is creating so we don't want to allow adding it to a release
    disabled: !document,
    position: [
      "panel",
      "table-row"
    ],
    dialog: {
      type: "modal",
      title: formatMessage({
        id: "content-releases.content-manager-edit-view.add-to-release",
        defaultMessage: "Add to release"
      }),
      content: (0, import_jsx_runtime25.jsx)(AddActionToReleaseModal, {
        contentType: model,
        documentId,
        onInputChange: formik.setFieldValue,
        values: formik.values
      }),
      footer: ({ onClose }) => (0, import_jsx_runtime25.jsxs)(_d.Footer, {
        children: [
          (0, import_jsx_runtime25.jsx)(Ct, {
            onClick: onClose,
            variant: "tertiary",
            name: "cancel",
            children: formatMessage({
              id: "content-releases.content-manager-edit-view.add-to-release.cancel-button",
              defaultMessage: "Cancel"
            })
          }),
          (0, import_jsx_runtime25.jsx)(Ct, {
            type: "submit",
            // @ts-expect-error - formik ReactEvent types don't match button onClick types as they expect a MouseEvent
            onClick: (e) => handleSubmit(e, onClose),
            disabled: !formik.values.releaseId,
            loading: isLoading,
            children: formatMessage({
              id: "content-releases.content-manager-edit-view.add-to-release.continue-button",
              defaultMessage: "Continue"
            })
          })
        ]
      })
    }
  };
};

// node_modules/@strapi/content-releases/dist/admin/components/ReleaseAction.mjs
var getContentPermissions = (subject) => {
  const permissions = {
    publish: [
      {
        action: "plugin::content-manager.explorer.publish",
        subject,
        id: "",
        actionParameters: {},
        properties: {},
        conditions: []
      }
    ]
  };
  return permissions;
};
var ReleaseAction = ({ documents, model }) => {
  var _a;
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const { formatAPIError } = useAPIErrorHandler();
  const [{ query }] = useQueryParams();
  const contentPermissions = getContentPermissions(model);
  const { allowedActions: { canPublish } } = useRBAC(contentPermissions);
  const { allowedActions: { canCreate } } = useRBAC(PERMISSIONS5);
  const { hasDraftAndPublish } = useContentManagerContext();
  const response = useGetReleasesQuery();
  const releases = (_a = response.data) == null ? void 0 : _a.data;
  const [createManyReleaseActions, { isLoading }] = useCreateManyReleaseActionsMutation();
  const documentIds = documents.map((doc) => doc.documentId);
  const handleSubmit = async (values) => {
    var _a2, _b;
    const locale = (_b = (_a2 = query.plugins) == null ? void 0 : _a2.i18n) == null ? void 0 : _b.locale;
    const releaseActionEntries = documentIds.map((entryDocumentId) => ({
      type: values.type,
      contentType: model,
      entryDocumentId,
      locale
    }));
    const response2 = await createManyReleaseActions({
      body: releaseActionEntries,
      params: {
        releaseId: values.releaseId
      }
    });
    if ("data" in response2) {
      const notificationMessage = formatMessage({
        id: "content-releases.content-manager-list-view.add-to-release.notification.success.message",
        defaultMessage: "{entriesAlreadyInRelease} out of {totalEntries} entries were already in the release."
      }, {
        entriesAlreadyInRelease: response2.data.meta.entriesAlreadyInRelease,
        totalEntries: response2.data.meta.totalEntries
      });
      const notification = {
        type: "success",
        title: formatMessage({
          id: "content-releases.content-manager-list-view.add-to-release.notification.success.title",
          defaultMessage: "Successfully added to release."
        }, {
          entriesAlreadyInRelease: response2.data.meta.entriesAlreadyInRelease,
          totalEntries: response2.data.meta.totalEntries
        }),
        message: response2.data.meta.entriesAlreadyInRelease ? notificationMessage : ""
      };
      toggleNotification(notification);
      return true;
    }
    if ("error" in response2) {
      if (isFetchError(response2.error)) {
        toggleNotification({
          type: "warning",
          message: formatAPIError(response2.error)
        });
      } else {
        toggleNotification({
          type: "warning",
          message: formatMessage({
            id: "notification.error",
            defaultMessage: "An error occurred"
          })
        });
      }
    }
  };
  if (!hasDraftAndPublish || !canCreate || !canPublish) return null;
  return {
    actionType: "release",
    variant: "tertiary",
    label: formatMessage({
      id: "content-manager-list-view.add-to-release",
      defaultMessage: "Add to Release"
    }),
    dialog: {
      type: "modal",
      title: formatMessage({
        id: "content-manager-list-view.add-to-release",
        defaultMessage: "Add to Release"
      }),
      content: ({ onClose }) => {
        return (0, import_jsx_runtime26.jsx)(Formik, {
          onSubmit: async (values) => {
            const data = await handleSubmit(values);
            if (data) {
              return onClose();
            }
          },
          validationSchema: RELEASE_ACTION_FORM_SCHEMA,
          initialValues: INITIAL_VALUES,
          children: ({ values, setFieldValue }) => (0, import_jsx_runtime26.jsxs)(Form, {
            children: [
              (releases == null ? void 0 : releases.length) === 0 ? (0, import_jsx_runtime26.jsx)(NoReleases, {}) : (0, import_jsx_runtime26.jsx)(_d.Body, {
                children: (0, import_jsx_runtime26.jsxs)(C, {
                  direction: "column",
                  alignItems: "stretch",
                  gap: 2,
                  children: [
                    (0, import_jsx_runtime26.jsx)(S, {
                      paddingBottom: 6,
                      children: (0, import_jsx_runtime26.jsxs)(Dd.Root, {
                        required: true,
                        children: [
                          (0, import_jsx_runtime26.jsx)(Dd.Label, {
                            children: formatMessage({
                              id: "content-releases.content-manager-list-view.add-to-release.select-label",
                              defaultMessage: "Select a release"
                            })
                          }),
                          (0, import_jsx_runtime26.jsx)(cr, {
                            placeholder: formatMessage({
                              id: "content-releases.content-manager-list-view.add-to-release.select-placeholder",
                              defaultMessage: "Select"
                            }),
                            onChange: (value) => setFieldValue("releaseId", value),
                            value: values.releaseId,
                            children: releases == null ? void 0 : releases.map((release) => (0, import_jsx_runtime26.jsx)(dr, {
                              value: release.id,
                              children: release.name
                            }, release.id))
                          })
                        ]
                      })
                    }),
                    (0, import_jsx_runtime26.jsx)(Dd.Label, {
                      children: formatMessage({
                        id: "content-releases.content-manager-list-view.add-to-release.action-type-label",
                        defaultMessage: "What do you want to do with these entries?"
                      })
                    }),
                    (0, import_jsx_runtime26.jsx)(ReleaseActionOptions, {
                      selected: values.type,
                      handleChange: (e) => setFieldValue("type", e.target.value),
                      name: "type"
                    })
                  ]
                })
              }),
              (0, import_jsx_runtime26.jsxs)(_d.Footer, {
                children: [
                  (0, import_jsx_runtime26.jsx)(Ct, {
                    onClick: onClose,
                    variant: "tertiary",
                    name: "cancel",
                    children: formatMessage({
                      id: "content-releases.content-manager-list-view.add-to-release.cancel-button",
                      defaultMessage: "Cancel"
                    })
                  }),
                  (0, import_jsx_runtime26.jsx)(Ct, {
                    type: "submit",
                    disabled: !values.releaseId,
                    loading: isLoading,
                    children: formatMessage({
                      id: "content-releases.content-manager-list-view.add-to-release.continue-button",
                      defaultMessage: "Continue"
                    })
                  })
                ]
              })
            ]
          })
        });
      }
    }
  };
};

// node_modules/@strapi/content-releases/dist/admin/components/ReleaseListCell.mjs
var import_jsx_runtime27 = __toESM(require_jsx_runtime(), 1);
var import_react6 = __toESM(require_react(), 1);
var isDocumentRow = (entry) => {
  if ("documentId" in entry) {
    return typeof entry.documentId === "string";
  }
  return false;
};
var useReleasesList = (contentTypeUid, documentId) => {
  var _a, _b;
  const listViewData = useTable("ListView", (state) => state.rows);
  const documentIds = listViewData.filter((entry) => isDocumentRow(entry)).map((entry) => entry.documentId);
  const [{ query }] = useQueryParams();
  const locale = ((_b = (_a = query == null ? void 0 : query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale) || void 0;
  const response = useGetMappedEntriesInReleasesQuery({
    contentTypeUid,
    documentIds,
    locale
  }, {
    skip: !contentTypeUid || documentIds.length === 0
  });
  const mappedEntriesInReleases = response.data || {};
  return (mappedEntriesInReleases == null ? void 0 : mappedEntriesInReleases[documentId]) || [];
};
var addColumnToTableHook2 = ({ displayedHeaders, layout }) => {
  const { options } = layout;
  if (!(options == null ? void 0 : options.draftAndPublish)) {
    return {
      displayedHeaders,
      layout
    };
  }
  return {
    displayedHeaders: [
      ...displayedHeaders,
      {
        searchable: false,
        sortable: false,
        name: "releases",
        label: {
          id: "content-releases.content-manager.list-view.releases.header",
          defaultMessage: "To be released in"
        },
        cellFormatter: (props, _, { model }) => (0, import_jsx_runtime27.jsx)(ReleaseListCell, {
          ...props,
          model
        })
      }
    ],
    layout
  };
};
var ReleaseListCell = ({ documentId, model }) => {
  const releases = useReleasesList(model, documentId);
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime27.jsxs)(Xd.Root, {
    children: [
      (0, import_jsx_runtime27.jsx)(Xd.Trigger, {
        children: (0, import_jsx_runtime27.jsx)(Ct, {
          variant: "ghost",
          onClick: (e) => e.stopPropagation(),
          // TODO: find a way in the DS to define the widht and height of the icon
          endIcon: releases.length > 0 ? (0, import_jsx_runtime27.jsx)(p2, {
            width: "1.2rem",
            height: "1.2rem"
          }) : null,
          children: (0, import_jsx_runtime27.jsx)(A, {
            style: {
              maxWidth: "252px",
              cursor: "pointer"
            },
            textColor: "neutral800",
            fontWeight: "regular",
            children: releases.length > 0 ? formatMessage({
              id: "content-releases.content-manager.list-view.releases-number",
              defaultMessage: "{number} {number, plural, one {release} other {releases}}"
            }, {
              number: releases.length
            }) : "-"
          })
        })
      }),
      (0, import_jsx_runtime27.jsx)(Xd.Content, {
        children: (0, import_jsx_runtime27.jsx)("ul", {
          children: releases.map(({ id, name: name2 }) => (0, import_jsx_runtime27.jsx)(S, {
            padding: 3,
            tag: "li",
            children: (0, import_jsx_runtime27.jsx)(Oa, {
              href: `/admin/plugins/content-releases/${id}`,
              isExternal: false,
              children: name2
            })
          }, id))
        })
      })
    ]
  });
};

// node_modules/@strapi/content-releases/dist/admin/components/ReleasesPanel.mjs
var import_jsx_runtime28 = __toESM(require_jsx_runtime(), 1);
var Panel = ({ model, document, documentId, collectionType }) => {
  var _a, _b, _c;
  const [{ query }] = useQueryParams();
  const locale = (_b = (_a = query.plugins) == null ? void 0 : _a.i18n) == null ? void 0 : _b.locale;
  const { edit: { options } } = useDocumentLayout(model);
  const { formatMessage, formatDate, formatTime } = useIntl();
  const { allowedActions } = useRBAC(PERMISSIONS5);
  const { canRead, canDeleteAction } = allowedActions;
  const response = useGetReleasesForEntryQuery({
    contentType: model,
    entryDocumentId: documentId,
    locale,
    hasEntryAttached: true
  }, {
    skip: !document
  });
  const releases = (_c = response.data) == null ? void 0 : _c.data;
  const getReleaseColorVariant = (actionType, shade) => {
    if (actionType === "unpublish") {
      return `secondary${shade}`;
    }
    return `success${shade}`;
  };
  if (!window.strapi.isEE || !(options == null ? void 0 : options.draftAndPublish) || !canRead) {
    return null;
  }
  if (collectionType === "collection-types" && (!documentId || documentId === "create")) {
    return null;
  }
  if (!releases || releases.length === 0) {
    return null;
  }
  return {
    title: formatMessage({
      id: "content-releases.plugin.name",
      defaultMessage: "Releases"
    }),
    content: (0, import_jsx_runtime28.jsx)(C, {
      direction: "column",
      alignItems: "stretch",
      gap: 3,
      width: "100%",
      children: releases == null ? void 0 : releases.map((release) => (0, import_jsx_runtime28.jsxs)(C, {
        direction: "column",
        alignItems: "start",
        borderWidth: "1px",
        borderStyle: "solid",
        borderColor: getReleaseColorVariant(release.actions[0].type, "200"),
        overflow: "hidden",
        hasRadius: true,
        children: [
          (0, import_jsx_runtime28.jsx)(S, {
            paddingTop: 3,
            paddingBottom: 3,
            paddingLeft: 4,
            paddingRight: 4,
            background: getReleaseColorVariant(release.actions[0].type, "100"),
            width: "100%",
            children: (0, import_jsx_runtime28.jsx)(A, {
              fontSize: 1,
              variant: "pi",
              textColor: getReleaseColorVariant(release.actions[0].type, "600"),
              children: formatMessage({
                id: "content-releases.content-manager-edit-view.list-releases.title",
                defaultMessage: "{isPublish, select, true {Will be published in} other {Will be unpublished in}}"
              }, {
                isPublish: release.actions[0].type === "publish"
              })
            })
          }),
          (0, import_jsx_runtime28.jsxs)(C, {
            padding: 4,
            direction: "column",
            gap: 2,
            width: "100%",
            alignItems: "flex-start",
            children: [
              (0, import_jsx_runtime28.jsx)(A, {
                fontSize: 2,
                fontWeight: "bold",
                variant: "omega",
                textColor: "neutral700",
                children: release.name
              }),
              release.scheduledAt && release.timezone && (0, import_jsx_runtime28.jsx)(A, {
                variant: "pi",
                textColor: "neutral600",
                children: formatMessage({
                  id: "content-releases.content-manager-edit-view.scheduled.date",
                  defaultMessage: "{date} at {time} ({offset})"
                }, {
                  date: formatDate(new Date(release.scheduledAt), {
                    day: "2-digit",
                    month: "2-digit",
                    year: "numeric",
                    timeZone: release.timezone
                  }),
                  time: formatTime(new Date(release.scheduledAt), {
                    hourCycle: "h23",
                    timeZone: release.timezone
                  }),
                  offset: getTimezoneOffset(release.timezone, new Date(release.scheduledAt))
                })
              }),
              canDeleteAction ? (0, import_jsx_runtime28.jsxs)(ReleaseActionMenu.Root, {
                hasTriggerBorder: true,
                children: [
                  (0, import_jsx_runtime28.jsx)(ReleaseActionMenu.EditReleaseItem, {
                    releaseId: release.id
                  }),
                  (0, import_jsx_runtime28.jsx)(ReleaseActionMenu.DeleteReleaseActionItem, {
                    releaseId: release.id,
                    actionId: release.actions[0].id
                  })
                ]
              }) : null
            ]
          })
        ]
      }, release.id))
    })
  };
};

// node_modules/@strapi/content-releases/dist/admin/utils/prefixPluginTranslations.mjs
var prefixPluginTranslations5 = (trad, pluginId5) => {
  return Object.keys(trad).reduce((acc, current) => {
    acc[`${pluginId5}.${current}`] = trad[current];
    return acc;
  }, {});
};

// node_modules/@strapi/content-releases/dist/admin/index.mjs
function __variableDynamicImportRuntime4__(path) {
  switch (path) {
    case "./translations/cs.json":
      return import("./cs.json-KHVHU5EM.js");
    case "./translations/en.json":
      return import("./en.json-PDSEQBXT.js");
    case "./translations/es.json":
      return import("./es.json-LY2OYII7.js");
    case "./translations/ja.json":
      return import("./ja.json-2LH6RAHK.js");
    case "./translations/ko.json":
      return import("./ko.json-KBKGM5BS.js");
    case "./translations/nl.json":
      return import("./nl.json-47LCD75A.js");
    case "./translations/pl.json":
      return import("./pl.json-B7MPAS3G.js");
    case "./translations/sk.json":
      return import("./sk.json-UB4I5BQ4.js");
    case "./translations/uk.json":
      return import("./uk.json-G465WNRT.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
var admin3 = {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  register(app) {
    var _a, _b;
    app.createHook("ContentReleases/pages/ReleaseDetails/add-locale-in-releases");
    if (window.strapi.features.isEnabled("cms-content-releases")) {
      app.addMenuLink({
        to: `plugins/${pluginId4}`,
        icon: k3,
        intlLabel: {
          id: `${pluginId4}.plugin.name`,
          defaultMessage: "Releases"
        },
        Component: () => import("./App-WN6VTSG6.js").then((mod) => ({
          default: mod.App
        })),
        permissions: PERMISSIONS5.main,
        position: 2
      });
      const contentManagerPluginApis = app.getPlugin("content-manager").apis;
      if ("addEditViewSidePanel" in contentManagerPluginApis && typeof contentManagerPluginApis.addEditViewSidePanel === "function") {
        contentManagerPluginApis.addEditViewSidePanel([
          Panel
        ]);
      }
      if ("addDocumentAction" in contentManagerPluginApis && typeof contentManagerPluginApis.addDocumentAction === "function") {
        contentManagerPluginApis.addDocumentAction((actions) => {
          const indexOfDeleteAction = actions.findIndex((action) => action.type === "unpublish");
          actions.splice(indexOfDeleteAction, 0, ReleaseActionModalForm);
          return actions;
        });
      }
      app.addSettingsLink("global", {
        id: pluginId4,
        to: "releases",
        intlLabel: {
          id: `${pluginId4}.plugin.name`,
          defaultMessage: "Releases"
        },
        licenseOnly: true,
        permissions: [],
        Component() {
          return import("./ReleasesSettingsPage-GIBDLIEM.js").then((mod) => ({
            default: mod.ProtectedReleasesSettingsPage
          }));
        }
      });
      if ("addBulkAction" in contentManagerPluginApis && typeof contentManagerPluginApis.addBulkAction === "function") {
        contentManagerPluginApis.addBulkAction((actions) => {
          const deleteActionIndex = actions.findIndex((action) => action.type === "delete");
          actions.splice(deleteActionIndex, 0, ReleaseAction);
          return actions;
        });
      }
      app.registerHook("Admin/CM/pages/ListView/inject-column-in-table", addColumnToTableHook2);
      app.widgets.register([
        {
          icon: k3,
          title: {
            id: `${PLUGIN_ID}.widget.upcoming-releases.title`,
            defaultMessage: "Upcoming releases"
          },
          component: async () => {
            const { UpcomingReleasesWidget } = await import("./Widgets-RXMHZ7SC.js");
            return UpcomingReleasesWidget;
          },
          pluginId: PLUGIN_ID,
          id: "upcoming-releases",
          link: {
            label: {
              id: `${PLUGIN_ID}.widget.upcoming-releases.link`,
              defaultMessage: "Open Releases"
            },
            href: "/plugins/content-releases"
          }
        }
      ]);
    } else if (!window.strapi.features.isEnabled("cms-content-releases") && ((_b = (_a = window.strapi) == null ? void 0 : _a.flags) == null ? void 0 : _b.promoteEE)) {
      app.addSettingsLink("global", {
        id: pluginId4,
        to: "purchase-content-releases",
        intlLabel: {
          id: `${pluginId4}.plugin.name`,
          defaultMessage: "Releases"
        },
        permissions: [],
        Component() {
          return import("./PurchaseContentReleases-CZIRYPCI.js").then((mod) => ({
            default: mod.PurchaseContentReleases
          }));
        },
        licenseOnly: true
      });
    }
  },
  async registerTrads({ locales }) {
    const importedTrads = await Promise.all(locales.map((locale) => {
      return __variableDynamicImportRuntime4__(`./translations/${locale}.json`).then(({ default: data }) => {
        return {
          data: prefixPluginTranslations5(data, "content-releases"),
          locale
        };
      }).catch(() => {
        return {
          data: {},
          locale
        };
      });
    }));
    return Promise.resolve(importedTrads);
  }
};

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/id/components/Header.mjs
var import_jsx_runtime31 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/id/components/AssigneeSelect.mjs
var import_jsx_runtime29 = __toESM(require_jsx_runtime(), 1);
var React15 = __toESM(require_react(), 1);

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/id/components/constants.mjs
var STAGE_ATTRIBUTE_NAME = "strapi_stage";
var ASSIGNEE_ATTRIBUTE_NAME = "strapi_assignee";

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/id/components/AssigneeSelect.mjs
var PAGE_SIZE = 10;
var contains = (value) => ({
  $containsi: value
});
var AssigneeSelect = ({ isCompact }) => {
  var _a;
  const { collectionType = "", id, slug: model = "" } = useParams();
  const permissions = useTypedSelector2((state) => state.admin_app.permissions);
  const { formatMessage } = useIntl();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const { toggleNotification } = useNotification();
  const { allowedActions: { canRead }, isLoading: isLoadingPermissions } = useRBAC((_a = permissions.settings) == null ? void 0 : _a.users);
  const [{ query }] = useQueryParams();
  const params = React15.useMemo(() => buildValidParams2(query), [
    query
  ]);
  const [pageSize, setPageSize] = React15.useState(PAGE_SIZE);
  const [search, setSearch] = React15.useState("");
  const debouncedSearch = useDebounce(search, 300);
  const searchFilters = React15.useMemo(() => {
    const value = debouncedSearch.trim();
    if (!value) {
      return void 0;
    }
    const [firstTerm, ...restTerms] = value.split(/\s+/);
    const rest = restTerms.join(" ");
    const filters = {
      $or: [
        {
          firstname: contains(value)
        },
        {
          lastname: contains(value)
        },
        {
          username: contains(value)
        },
        {
          email: contains(value)
        }
      ]
    };
    if (rest) {
      filters.$or = [
        ...filters.$or ?? [],
        {
          $and: [
            {
              firstname: contains(firstTerm)
            },
            {
              lastname: contains(rest)
            }
          ]
        },
        {
          $and: [
            {
              firstname: contains(rest)
            },
            {
              lastname: contains(firstTerm)
            }
          ]
        }
      ];
    }
    return filters;
  }, [
    debouncedSearch
  ]);
  const { data, isLoading: isLoadingUsers, isError } = useAdminUsers({
    pageSize,
    filters: searchFilters
  }, {
    skip: isLoadingPermissions || !canRead
  });
  const { document } = useDocument({
    collectionType,
    model,
    documentId: id,
    params
  }, {
    skip: !id && collectionType !== "single-types"
  });
  const users = React15.useMemo(() => (data == null ? void 0 : data.users) ?? [], [
    data == null ? void 0 : data.users
  ]);
  const { pageCount = 1, page = 1 } = (data == null ? void 0 : data.pagination) ?? {};
  const currentAssignee = document ? document[ASSIGNEE_ATTRIBUTE_NAME] : null;
  const options = React15.useMemo(() => {
    if (!currentAssignee) return users;
    return users.some((u) => u.id === currentAssignee.id) ? users : [
      currentAssignee,
      ...users
    ];
  }, [
    users,
    currentAssignee
  ]);
  const handleOpenChange = (isOpen) => {
    if (!isOpen) {
      setPageSize(PAGE_SIZE);
      setSearch("");
    }
  };
  const handleLoadMore = () => {
    setPageSize(pageSize + PAGE_SIZE);
  };
  const handleInputChange = (e) => {
    setSearch(e.currentTarget.value);
    setPageSize(PAGE_SIZE);
  };
  const [updateAssignee, { error, isLoading: isMutating }] = useUpdateAssigneeMutation();
  if (!collectionType || !model || !(document == null ? void 0 : document.documentId)) {
    return null;
  }
  const handleChange = async (assigneeId) => {
    if ((currentAssignee == null ? void 0 : currentAssignee.id) === assigneeId) {
      return;
    }
    const res = await updateAssignee({
      slug: collectionType,
      model,
      id: document.documentId,
      params,
      data: {
        id: assigneeId ? parseInt(assigneeId, 10) : null
      }
    });
    if ("data" in res) {
      toggleNotification({
        type: "success",
        message: formatMessage({
          id: "review-workflows.assignee.notification.saved",
          defaultMessage: "Assignee updated"
        })
      });
    }
    if (isCompact && "error" in res) {
      toggleNotification({
        type: "danger",
        message: formatAPIError(res.error)
      });
    }
  };
  const isDisabled = !isLoadingPermissions && !isLoadingUsers && users.length === 0 || !document.documentId;
  const isLoading = isLoadingUsers || isLoadingPermissions || isMutating;
  const hasMoreItems = page < pageCount;
  const assigneeLabel = formatMessage({
    id: "review-workflows.assignee.label",
    defaultMessage: "Assignee"
  });
  const assigneeClearLabel = formatMessage({
    id: "review-workflows.assignee.clear",
    defaultMessage: "Clear assignee"
  });
  const assigneePlaceholder = formatMessage({
    id: "review-workflows.assignee.placeholder",
    defaultMessage: "Select…"
  });
  if (isCompact) {
    return (0, import_jsx_runtime29.jsxs)(Dd.Root, {
      name: ASSIGNEE_ATTRIBUTE_NAME,
      id: ASSIGNEE_ATTRIBUTE_NAME,
      children: [
        (0, import_jsx_runtime29.jsx)($e, {
          children: (0, import_jsx_runtime29.jsx)(Dd.Label, {
            children: assigneeLabel
          })
        }),
        (0, import_jsx_runtime29.jsx)(Vi, {
          clearLabel: assigneeClearLabel,
          disabled: isDisabled,
          value: currentAssignee ? currentAssignee.id.toString() : null,
          onChange: handleChange,
          onClear: () => handleChange(null),
          onOpenChange: handleOpenChange,
          onLoadMore: handleLoadMore,
          hasMoreItems,
          onInputChange: handleInputChange,
          placeholder: assigneePlaceholder,
          loading: isLoading || isLoadingPermissions || isMutating,
          size: "S",
          children: options.map((user) => {
            return (0, import_jsx_runtime29.jsx)(Ki, {
              value: user.id.toString(),
              textValue: getDisplayName(user),
              children: getDisplayName(user)
            }, user.id);
          })
        })
      ]
    });
  }
  return (0, import_jsx_runtime29.jsxs)(Dd.Root, {
    name: ASSIGNEE_ATTRIBUTE_NAME,
    id: ASSIGNEE_ATTRIBUTE_NAME,
    error: (isError && canRead && formatMessage({
      id: "review-workflows.assignee.error",
      defaultMessage: "An error occurred while fetching users"
    }) || error && formatAPIError(error)) ?? void 0,
    children: [
      (0, import_jsx_runtime29.jsx)(Dd.Label, {
        children: assigneeLabel
      }),
      (0, import_jsx_runtime29.jsx)(Vi, {
        clearLabel: assigneeClearLabel,
        disabled: !isLoadingPermissions && !isLoading && users.length === 0 || !document.documentId,
        value: currentAssignee ? currentAssignee.id.toString() : null,
        onChange: handleChange,
        onClear: () => handleChange(null),
        onOpenChange: handleOpenChange,
        onLoadMore: handleLoadMore,
        hasMoreItems,
        onInputChange: handleInputChange,
        placeholder: assigneePlaceholder,
        loading: isLoading || isLoadingPermissions || isMutating,
        children: options.map((user) => {
          return (0, import_jsx_runtime29.jsx)(Ki, {
            value: user.id.toString(),
            textValue: getDisplayName(user),
            children: getDisplayName(user)
          }, user.id);
        })
      }),
      (0, import_jsx_runtime29.jsx)(Dd.Error, {})
    ]
  });
};

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/id/components/StageSelect.mjs
var import_jsx_runtime30 = __toESM(require_jsx_runtime(), 1);
var React16 = __toESM(require_react(), 1);
var WorkflowLimitModal = ({ open, onOpenChange }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime30.jsxs)(LimitsModal.Root, {
    open,
    onOpenChange,
    children: [
      (0, import_jsx_runtime30.jsx)(LimitsModal.Title, {
        children: formatMessage({
          id: "review-workflows.workflows.limit.title",
          defaultMessage: "You’ve reached the limit of workflows in your plan"
        })
      }),
      (0, import_jsx_runtime30.jsx)(LimitsModal.Body, {
        children: formatMessage({
          id: "review-workflows.workflows.limit.body",
          defaultMessage: "Delete a workflow or contact Sales to enable more workflows."
        })
      })
    ]
  });
};
var StageLimitModal = ({ open, onOpenChange }) => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime30.jsxs)(LimitsModal.Root, {
    open,
    onOpenChange,
    children: [
      (0, import_jsx_runtime30.jsx)(LimitsModal.Title, {
        children: formatMessage({
          id: "review-workflows.stages.limit.title",
          defaultMessage: "You have reached the limit of stages for this workflow in your plan"
        })
      }),
      (0, import_jsx_runtime30.jsx)(LimitsModal.Body, {
        children: formatMessage({
          id: "review-workflows.stages.limit.body",
          defaultMessage: "Try deleting some stages or contact Sales to enable more stages."
        })
      })
    ]
  });
};
var Select = ({ stages, activeWorkflowStage, isLoading, ...props }) => {
  const { formatMessage } = useIntl();
  const { themeColorName } = getStageColorByHex(activeWorkflowStage == null ? void 0 : activeWorkflowStage.color) ?? {};
  return (0, import_jsx_runtime30.jsx)(cr, {
    disabled: stages.length === 0,
    placeholder: formatMessage({
      id: "review-workflows.assignee.placeholder",
      defaultMessage: "Select…"
    }),
    startIcon: activeWorkflowStage && (0, import_jsx_runtime30.jsx)(C, {
      tag: "span",
      height: 2,
      background: activeWorkflowStage == null ? void 0 : activeWorkflowStage.color,
      borderColor: themeColorName === "neutral0" ? "neutral150" : void 0,
      hasRadius: true,
      shrink: 0,
      width: 2,
      marginRight: "-3px"
    }),
    // @ts-expect-error – `customizeContent` is not correctly typed in the DS.
    customizeContent: () => {
      return (0, import_jsx_runtime30.jsxs)(C, {
        tag: "span",
        justifyContent: "space-between",
        alignItems: "center",
        width: "100%",
        children: [
          (0, import_jsx_runtime30.jsx)(A, {
            textColor: "neutral800",
            ellipsis: true,
            lineHeight: "inherit",
            children: (activeWorkflowStage == null ? void 0 : activeWorkflowStage.name) ?? ""
          }),
          isLoading ? (0, import_jsx_runtime30.jsx)(kt, {
            small: true,
            style: {
              display: "flex"
            },
            "data-testid": "loader"
          }) : null
        ]
      });
    },
    ...props,
    children: stages.map(({ id, color, name: name2 }) => {
      const { themeColorName: themeColorName2 } = getStageColorByHex(color) ?? {};
      return (0, import_jsx_runtime30.jsx)(dr, {
        startIcon: (0, import_jsx_runtime30.jsx)(C, {
          height: 2,
          background: color,
          borderColor: themeColorName2 === "neutral0" ? "neutral150" : void 0,
          hasRadius: true,
          shrink: 0,
          width: 2
        }),
        value: id,
        textValue: name2,
        children: name2
      }, id);
    })
  });
};
var StageSelect = ({ isCompact }) => {
  const { collectionType = "", slug: model = "", id = "" } = useParams();
  const { formatMessage } = useIntl();
  const { _unstableFormatAPIError: formatAPIError } = useAPIErrorHandler();
  const { toggleNotification } = useNotification();
  const [{ query }] = useQueryParams();
  const params = React16.useMemo(() => buildValidParams2(query), [
    query
  ]);
  const { document, isLoading: isLoadingDocument } = useDocument({
    collectionType,
    model,
    documentId: id,
    params
  }, {
    skip: !id && collectionType !== "single-types"
  });
  const stagesDocumentId = (document == null ? void 0 : document.documentId) ?? id;
  const { data, isLoading: isLoadingStages } = useGetStagesQuery({
    slug: collectionType,
    model,
    id: stagesDocumentId,
    params
  }, {
    skip: !stagesDocumentId || stagesDocumentId === "create"
  });
  const { meta, stages = [] } = data ?? {};
  const { getFeature } = useLicenseLimits();
  const [showLimitModal, setShowLimitModal] = React16.useState(null);
  const limits = getFeature("review-workflows") ?? {};
  const activeWorkflowStage = document ? document[STAGE_ATTRIBUTE_NAME] : null;
  const [updateStage, { error }] = useUpdateStageMutation();
  const handleChange = async (stageId) => {
    try {
      if ((limits == null ? void 0 : limits[CHARGEBEE_WORKFLOW_ENTITLEMENT_NAME]) && parseInt(limits[CHARGEBEE_WORKFLOW_ENTITLEMENT_NAME], 10) < ((meta == null ? void 0 : meta.workflowCount) ?? 0)) {
        setShowLimitModal("workflow");
      } else if ((limits == null ? void 0 : limits[CHARGEBEE_STAGES_PER_WORKFLOW_ENTITLEMENT_NAME]) && parseInt(limits[CHARGEBEE_STAGES_PER_WORKFLOW_ENTITLEMENT_NAME], 10) < stages.length) {
        setShowLimitModal("stage");
      } else {
        if (document == null ? void 0 : document.documentId) {
          const res = await updateStage({
            model,
            id: document.documentId,
            slug: collectionType,
            params,
            data: {
              id: stageId
            }
          });
          if ("data" in res) {
            toggleNotification({
              type: "success",
              message: formatMessage({
                id: "review-workflows.stage.notification.saved",
                defaultMessage: "Review stage updated"
              })
            });
          }
          if (isCompact && "error" in res) {
            toggleNotification({
              type: "danger",
              message: formatAPIError(res.error)
            });
          }
        }
      }
    } catch {
      toggleNotification({
        type: "danger",
        message: formatMessage({
          id: "review-workflows.stage.notification.error",
          defaultMessage: "An error occurred while updating the review stage"
        })
      });
    }
  };
  const isLoading = isLoadingStages || isLoadingDocument;
  const reviewStageLabel = formatMessage({
    id: "review-workflows.stage.label",
    defaultMessage: "Review stage"
  });
  const hasDocumentForLocale = Boolean(document == null ? void 0 : document.documentId);
  const totalWorkflowStages = (meta == null ? void 0 : meta.stageCount) ?? 0;
  const canTransition = (meta == null ? void 0 : meta.canTransition) ?? true;
  let reviewStageHint;
  if (!isLoading) {
    if (!hasDocumentForLocale) {
      reviewStageHint = formatMessage({
        id: "review-workflows.stages.save-first",
        defaultMessage: "Save this entry to assign a workflow stage."
      });
    } else if (stages.length === 0) {
      if (canTransition && totalWorkflowStages === 1) {
        reviewStageHint = formatMessage({
          id: "review-workflows.stages.single-stage",
          defaultMessage: "This workflow only has one stage. Add more stages to be able to update it here."
        });
      } else {
        reviewStageHint = formatMessage({
          id: "review-workflows.stages.no-transition",
          defaultMessage: "You don’t have the permission to update this stage."
        });
      }
    }
  }
  if (isCompact) {
    return (0, import_jsx_runtime30.jsxs)(import_jsx_runtime30.Fragment, {
      children: [
        (0, import_jsx_runtime30.jsx)(Mt, {
          label: reviewStageHint,
          children: (0, import_jsx_runtime30.jsx)(Dd.Root, {
            name: STAGE_ATTRIBUTE_NAME,
            id: STAGE_ATTRIBUTE_NAME,
            children: (0, import_jsx_runtime30.jsxs)(import_jsx_runtime30.Fragment, {
              children: [
                (0, import_jsx_runtime30.jsx)($e, {
                  children: (0, import_jsx_runtime30.jsx)(Dd.Label, {
                    children: reviewStageLabel
                  })
                }),
                (0, import_jsx_runtime30.jsx)(Select, {
                  stages,
                  activeWorkflowStage,
                  isLoading,
                  size: "S",
                  disabled: stages.length === 0,
                  value: activeWorkflowStage == null ? void 0 : activeWorkflowStage.id,
                  onChange: handleChange,
                  placeholder: formatMessage({
                    id: "review-workflows.assignee.placeholder",
                    defaultMessage: "Select…"
                  })
                })
              ]
            })
          })
        }),
        (0, import_jsx_runtime30.jsx)(WorkflowLimitModal, {
          open: showLimitModal === "workflow",
          onOpenChange: () => setShowLimitModal(null)
        }),
        (0, import_jsx_runtime30.jsx)(StageLimitModal, {
          open: showLimitModal === "stage",
          onOpenChange: () => setShowLimitModal(null)
        })
      ]
    });
  }
  return (0, import_jsx_runtime30.jsxs)(import_jsx_runtime30.Fragment, {
    children: [
      (0, import_jsx_runtime30.jsxs)(Dd.Root, {
        hint: reviewStageHint,
        error: error && formatAPIError(error) || void 0,
        name: STAGE_ATTRIBUTE_NAME,
        id: STAGE_ATTRIBUTE_NAME,
        children: [
          (0, import_jsx_runtime30.jsx)(Dd.Label, {
            children: reviewStageLabel
          }),
          (0, import_jsx_runtime30.jsx)(Select, {
            stages,
            activeWorkflowStage,
            isLoading,
            disabled: stages.length === 0,
            value: activeWorkflowStage == null ? void 0 : activeWorkflowStage.id,
            onChange: handleChange,
            placeholder: formatMessage({
              id: "review-workflows.assignee.placeholder",
              defaultMessage: "Select…"
            })
          }),
          (0, import_jsx_runtime30.jsx)(Dd.Hint, {}),
          (0, import_jsx_runtime30.jsx)(Dd.Error, {})
        ]
      }),
      (0, import_jsx_runtime30.jsx)(WorkflowLimitModal, {
        open: showLimitModal === "workflow",
        onOpenChange: () => setShowLimitModal(null)
      }),
      (0, import_jsx_runtime30.jsx)(StageLimitModal, {
        open: showLimitModal === "stage",
        onOpenChange: () => setShowLimitModal(null)
      })
    ]
  });
};

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/id/components/Header.mjs
var Header = () => {
  const { slug = "", id, collectionType } = useParams();
  const { edit: { options } } = useDocumentLayout(slug);
  if (!window.strapi.isEE || !(options == null ? void 0 : options.reviewWorkflows) || collectionType !== "single-types" && !id || id === "create") {
    return null;
  }
  return (0, import_jsx_runtime31.jsxs)(C, {
    gap: 2,
    children: [
      (0, import_jsx_runtime31.jsx)(AssigneeSelect, {
        isCompact: true
      }),
      (0, import_jsx_runtime31.jsx)(StageSelect, {
        isCompact: true
      })
    ]
  });
};
Header.type = "preview";

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/id/components/Panel.mjs
var import_jsx_runtime32 = __toESM(require_jsx_runtime(), 1);
var Panel2 = () => {
  const { slug = "", id, collectionType } = useParams();
  const { edit: { options } } = useDocumentLayout(slug);
  const { formatMessage } = useIntl();
  if (!window.strapi.isEE || !(options == null ? void 0 : options.reviewWorkflows) || collectionType !== "single-types" && !id || id === "create") {
    return null;
  }
  return {
    title: formatMessage({
      id: "content-manager.containers.edit.panels.review-workflows.title",
      defaultMessage: "Review Workflows"
    }),
    content: (0, import_jsx_runtime32.jsxs)(C, {
      direction: "column",
      gap: 2,
      alignItems: "stretch",
      width: "100%",
      children: [
        (0, import_jsx_runtime32.jsx)(AssigneeSelect, {}),
        (0, import_jsx_runtime32.jsx)(StageSelect, {})
      ]
    })
  };
};
Panel2.type = "review-workflows";

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/constants.mjs
var import_jsx_runtime35 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/components/AssigneeFilter.mjs
var import_jsx_runtime33 = __toESM(require_jsx_runtime(), 1);
var React17 = __toESM(require_react(), 1);
var PAGE_SIZE2 = 10;
var AssigneeFilter = ({ name: name2 }) => {
  const [pageSize, setPageSize] = React17.useState(PAGE_SIZE2);
  const [hasOpened, setHasOpened] = React17.useState(false);
  const { formatMessage } = useIntl();
  const field = useField(name2);
  const shouldFetch = hasOpened || field.value !== void 0 && field.value !== "";
  const { data, isLoading } = useAdminUsers({
    pageSize
  }, {
    skip: !shouldFetch
  });
  const handleChange = (value) => {
    setPageSize(PAGE_SIZE2);
    field.onChange(name2, value);
  };
  const handleOpenChange = (isOpen) => {
    if (isOpen && !hasOpened) {
      setHasOpened(true);
    }
  };
  const { pageCount = 1, page = 1 } = (data == null ? void 0 : data.pagination) ?? {};
  const hasMoreItems = page < pageCount;
  const handleLoadMore = () => {
    if (!hasMoreItems) {
      return;
    }
    setPageSize((prev) => prev + PAGE_SIZE2);
  };
  const options = React17.useMemo(() => ((data == null ? void 0 : data.users) ?? []).map((user) => ({
    id: user.id,
    label: getDisplayName(user)
  })), [
    data == null ? void 0 : data.users
  ]);
  return (0, import_jsx_runtime33.jsx)(Vi, {
    value: field.value,
    "aria-label": formatMessage({
      id: "content-manager.components.Filters.usersSelect.label",
      defaultMessage: "Search and select an user to filter"
    }),
    onChange: handleChange,
    onOpenChange: handleOpenChange,
    loading: isLoading,
    onLoadMore: handleLoadMore,
    hasMoreItems,
    children: options.map((option) => (0, import_jsx_runtime33.jsx)(Ki, {
      value: option.id.toString(),
      children: option.label
    }, option.id))
  });
};

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/components/StageFilter.mjs
var import_jsx_runtime34 = __toESM(require_jsx_runtime(), 1);
var StageFilter = (props) => {
  const { slug: model } = useParams();
  const { formatMessage } = useIntl();
  const { workflows, isLoading } = useReviewWorkflows({
    filters: {
      contentTypes: model
    }
  });
  const field = useField(props.name);
  const [workflow] = workflows ?? [];
  return (0, import_jsx_runtime34.jsx)(cr, {
    "aria-label": formatMessage({
      id: "content-manager.components.Filters.reviewWorkflows.label",
      defaultMessage: "Search and select an workflow stage to filter"
    }),
    value: field.value,
    onChange: (value) => {
      field.onChange(props.name, value);
    },
    loading: isLoading,
    // @ts-expect-error – DS type error with SingleSelect['customizeContent']
    customizeContent: () => (0, import_jsx_runtime34.jsxs)(C, {
      tag: "span",
      justifyContent: "space-between",
      alignItems: "center",
      width: "100%",
      children: [
        (0, import_jsx_runtime34.jsx)(A, {
          textColor: "neutral800",
          ellipsis: true,
          children: field.value
        }),
        isLoading ? (0, import_jsx_runtime34.jsx)(kt, {
          small: true,
          style: {
            display: "flex"
          }
        }) : null
      ]
    }),
    children: ((workflow == null ? void 0 : workflow.stages) ?? []).map(({ id, color, name: name2 }) => {
      const { themeColorName } = getStageColorByHex(color) ?? {};
      return (0, import_jsx_runtime34.jsx)(dr, {
        startIcon: (0, import_jsx_runtime34.jsx)(C, {
          height: 2,
          background: color,
          borderColor: themeColorName === "neutral0" ? "neutral150" : void 0,
          hasRadius: true,
          shrink: 0,
          width: 2
        }),
        value: name2,
        children: name2
      }, id);
    })
  });
};

// node_modules/@strapi/review-workflows/dist/admin/routes/content-manager/model/constants.mjs
var REVIEW_WORKFLOW_COLUMNS = [
  {
    name: STAGE_ATTRIBUTE_NAME,
    attribute: {
      type: "relation",
      relation: "oneToMany",
      target: "admin::review-workflow-stage"
    },
    label: {
      id: "review-workflows.containers.list.table-headers.reviewWorkflows.stage",
      defaultMessage: "review stage"
    },
    searchable: false,
    sortable: true,
    mainField: {
      name: "name",
      type: "string"
    },
    cellFormatter: (props) => (0, import_jsx_runtime35.jsx)(StageColumn, {
      ...props
    })
  },
  {
    name: ASSIGNEE_ATTRIBUTE_NAME,
    attribute: {
      type: "relation",
      target: "admin::user",
      relation: "oneToMany"
    },
    label: {
      id: "review-workflows.containers.list.table-headers.reviewWorkflows.assignee",
      defaultMessage: "assignee"
    },
    searchable: false,
    sortable: true,
    mainField: {
      name: "firstname",
      type: "string"
    },
    cellFormatter: (props) => (0, import_jsx_runtime35.jsx)(AssigneeColumn, {
      ...props
    })
  }
];
var REVIEW_WORKFLOW_FILTERS = [
  {
    mainField: {
      name: "name",
      type: "string"
    },
    input: StageFilter,
    label: {
      id: "review-workflows.containers.list.table-headers.reviewWorkflows.stage",
      defaultMessage: "review stage"
    },
    name: "strapi_stage",
    type: "relation"
  },
  {
    type: "relation",
    mainField: {
      name: "id",
      type: "integer"
    },
    input: AssigneeFilter,
    operators: [
      {
        label: {
          id: "components.FilterOptions.FILTER_TYPES.$eq",
          defaultMessage: "is"
        },
        value: "$eq"
      },
      {
        label: {
          id: "components.FilterOptions.FILTER_TYPES.$ne",
          defaultMessage: "is not"
        },
        value: "$ne"
      },
      {
        label: {
          id: "components.FilterOptions.FILTER_TYPES.$null",
          defaultMessage: "is null"
        },
        value: "$null"
      },
      {
        label: {
          id: "components.FilterOptions.FILTER_TYPES.$notNull",
          defaultMessage: "is not null"
        },
        value: "$notNull"
      }
    ],
    label: {
      id: "review-workflows.containers.list.table-headers.reviewWorkflows.assignee",
      defaultMessage: "assignee"
    },
    name: "strapi_assignee"
  }
];

// node_modules/@strapi/review-workflows/dist/admin/utils/cm-hooks.mjs
var addColumnToTableHook3 = ({ displayedHeaders, layout }) => {
  const { options } = layout;
  if (!options.reviewWorkflows) {
    return {
      displayedHeaders,
      layout
    };
  }
  return {
    displayedHeaders: [
      ...displayedHeaders,
      ...REVIEW_WORKFLOW_COLUMNS
    ],
    layout
  };
};
var addFilterToListViewHook = ({ displayedFilters: displayedFilters2, layout }) => {
  var _a;
  if (!((_a = layout == null ? void 0 : layout.options) == null ? void 0 : _a.reviewWorkflows)) {
    return {
      displayedFilters: displayedFilters2,
      layout
    };
  }
  return {
    displayedFilters: [
      ...displayedFilters2,
      ...REVIEW_WORKFLOW_FILTERS
    ],
    layout
  };
};

// node_modules/@strapi/review-workflows/dist/admin/utils/translations.mjs
var prefixPluginTranslations6 = (trad, pluginId5) => {
  return Object.keys(trad).reduce((acc, current) => {
    acc[`${pluginId5}.${current}`] = trad[current];
    return acc;
  }, {});
};

// node_modules/@strapi/review-workflows/dist/admin/index.mjs
function __variableDynamicImportRuntime3__2(path) {
  switch (path) {
    case "./translations/cs.json":
      return import("./cs.json-XJLTMHGK.js");
    case "./translations/de.json":
      return import("./de.json-X4VNEJNY.js");
    case "./translations/en.json":
      return import("./en.json-XV2QTBUA.js");
    case "./translations/es.json":
      return import("./es.json-TX6XTTG7.js");
    case "./translations/fr.json":
      return import("./fr.json-SKFHA2DC.js");
    case "./translations/ja.json":
      return import("./ja.json-5DNRGFAJ.js");
    case "./translations/ko.json":
      return import("./ko.json-2AYMA3SH.js");
    case "./translations/nl.json":
      return import("./nl.json-VVKXBG6U.js");
    case "./translations/pl.json":
      return import("./pl.json-QY53XBZO.js");
    case "./translations/ru.json":
      return import("./ru.json-HV545727.js");
    case "./translations/sk.json":
      return import("./sk.json-BFMNYL5Q.js");
    case "./translations/uk.json":
      return import("./uk.json-Y3CFAYTC.js");
    case "./translations/zh-Hans.json":
      return import("./zh-Hans.json-G67CRYJR.js");
    default:
      return new Promise(function(resolve, reject) {
        (typeof queueMicrotask === "function" ? queueMicrotask : setTimeout)(
          reject.bind(null, new Error("Unknown variable dynamic import: " + path))
        );
      });
  }
}
var admin4 = {
  register(app) {
    var _a, _b;
    if (window.strapi.features.isEnabled(FEATURE_ID)) {
      app.registerHook("Admin/CM/pages/ListView/inject-column-in-table", addColumnToTableHook3);
      app.registerHook("Admin/CM/pages/ListView/inject-in-filters", addFilterToListViewHook);
      const contentManagerPluginApis = app.getPlugin("content-manager").apis;
      if ("addEditViewSidePanel" in contentManagerPluginApis && typeof contentManagerPluginApis.addEditViewSidePanel === "function") {
        contentManagerPluginApis.addEditViewSidePanel([
          Panel2
        ]);
      }
      app.addSettingsLink("global", {
        id: PLUGIN_ID2,
        to: `review-workflows`,
        intlLabel: {
          id: `${PLUGIN_ID2}.plugin.name`,
          defaultMessage: "Review Workflows"
        },
        licenseOnly: true,
        permissions: [],
        Component() {
          return import("./router-NOYKPHGC.js").then((mod) => ({
            default: mod.Router
          }));
        }
      });
      app.widgets.register((widgets) => [
        ...widgets,
        {
          icon: Mn,
          title: {
            id: `${PLUGIN_ID2}.widget.assigned.title`,
            defaultMessage: "Assigned to me"
          },
          component: async () => {
            const { AssignedWidget } = await import("./Widgets-RSTMYKFV.js");
            return AssignedWidget;
          },
          pluginId: PLUGIN_ID2,
          id: "assigned",
          permissions: [
            {
              action: "plugin::content-manager.explorer.read"
            }
          ]
        }
      ]);
    } else if (!window.strapi.features.isEnabled(FEATURE_ID) && ((_b = (_a = window.strapi) == null ? void 0 : _a.flags) == null ? void 0 : _b.promoteEE)) {
      app.addSettingsLink("global", {
        id: PLUGIN_ID2,
        to: `purchase-review-workflows`,
        intlLabel: {
          id: `${PLUGIN_ID2}.plugin.name`,
          defaultMessage: "Review Workflows"
        },
        licenseOnly: true,
        permissions: [],
        Component() {
          return import("./purchase-review-workflows-6MSUM5EP.js").then((mod) => ({
            default: mod.PurchaseReviewWorkflows
          }));
        }
      });
    }
  },
  bootstrap(app) {
    if (window.strapi.features.isEnabled(FEATURE_ID)) {
      app.getPlugin("content-manager").injectComponent("preview", "actions", {
        name: "review-workflows-assignee",
        Component: Header
      });
    }
  },
  async registerTrads({ locales }) {
    const importedTrads = await Promise.all(locales.map((locale) => {
      return __variableDynamicImportRuntime3__2(`./translations/${locale}.json`).then(({ default: data }) => {
        return {
          data: prefixPluginTranslations6(data, PLUGIN_ID2),
          locale
        };
      }).catch(() => {
        return {
          data: {},
          locale
        };
      });
    }));
    return Promise.resolve(importedTrads);
  }
};

// node_modules/@strapi/strapi/dist/admin.mjs
var render = (mountNode, { plugins, ...restArgs }) => {
  return renderAdmin(mountNode, {
    ...restArgs,
    plugins: {
      "content-manager": index,
      "content-type-builder": index2,
      email: admin,
      upload: admin2,
      contentReleases: admin3,
      i18n: index3,
      reviewWorkflows: admin4,
      ...plugins
    }
  });
};

export {
  render
};
//# sourceMappingURL=chunk-2UTNSC4H.js.map
