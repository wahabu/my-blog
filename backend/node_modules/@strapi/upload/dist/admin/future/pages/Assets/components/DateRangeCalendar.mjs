import { jsxs, jsx } from 'react/jsx-runtime';
import { useState } from 'react';
import { Box, Flex, IconButton, Typography, VisuallyHidden } from '@strapi/design-system';
import { ChevronLeft, ChevronRight } from '@strapi/icons';
import { useIntl } from 'react-intl';
import { styled } from 'styled-components';
import { getTranslationKey } from '../../../utils/translations.mjs';

/**
 * Month-grid range picker: first click sets the start, second click the end
 * (clicks are reordered when the second date is before the first). The DS only
 * ships a single-value DatePicker, so the range grid is built locally.
 *
 * Month/year move via chevrons rather than the mock's dropdowns — nested
 * popovers inside a dropdown menu fight Radix's outside-interaction handling.
 */ const DayCell = styled.button`
  width: 3rem;
  height: 3rem;
  border: none;
  border-radius: ${({ theme })=>theme.borderRadius};
  cursor: pointer;
  font: inherit;
  color: ${({ theme, $isEdge, $isMuted })=>{
    if ($isEdge) return theme.colors.primary600;
    if ($isMuted) return theme.colors.neutral400;
    return theme.colors.neutral800;
}};
  background: ${({ theme, $inRange, $isEdge })=>{
    if ($isEdge) return theme.colors.primary200;
    if ($inRange) return theme.colors.primary100;
    return 'transparent';
}};

  &:hover {
    background: ${({ theme })=>theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme })=>theme.colors.primary600};
    outline-offset: -2px;
  }
`;
const toKey = (date)=>{
    const month = `${date.getMonth() + 1}`.padStart(2, '0');
    const day = `${date.getDate()}`.padStart(2, '0');
    return `${date.getFullYear()}-${month}-${day}`;
};
const fromKey = (key)=>{
    const [year, month, day] = key.split('-').map(Number);
    return new Date(year, month - 1, day);
};
/** Monday-first weeks covering the given month, padded with adjacent days. */ const buildWeeks = (year, month)=>{
    const first = new Date(year, month, 1);
    const start = new Date(first.getTime());
    start.setDate(first.getDate() - (first.getDay() + 6) % 7);
    const weeks = [];
    const cursor = new Date(start.getTime());
    do {
        const week = [];
        for(let i = 0; i < 7; i += 1){
            week.push(new Date(cursor.getTime()));
            cursor.setDate(cursor.getDate() + 1);
        }
        weeks.push(week);
    }while (cursor.getMonth() === month && cursor.getFullYear() === year)
    return weeks;
};
const DateRangeCalendar = ({ from, to, onSelect })=>{
    const { formatMessage, formatDate } = useIntl();
    const initialMonth = from ? fromKey(from) : new Date();
    const [viewYear, setViewYear] = useState(initialMonth.getFullYear());
    const [viewMonth, setViewMonth] = useState(initialMonth.getMonth());
    // In-progress selection: set on the first click, cleared on commit.
    const [pendingStart, setPendingStart] = useState(null);
    const rangeStart = pendingStart ?? from ?? null;
    const rangeEnd = pendingStart ? null : to ?? null;
    const moveMonth = (delta)=>{
        const next = new Date(viewYear, viewMonth + delta, 1);
        setViewYear(next.getFullYear());
        setViewMonth(next.getMonth());
    };
    const handleDayClick = (day)=>{
        const key = toKey(day);
        if (!pendingStart) {
            setPendingStart(key);
            return;
        }
        const [start, end] = key < pendingStart ? [
            key,
            pendingStart
        ] : [
            pendingStart,
            key
        ];
        setPendingStart(null);
        onSelect(start, end);
    };
    const weeks = buildWeeks(viewYear, viewMonth);
    const weekDayLabels = weeks[0].map((day)=>formatDate(day, {
            weekday: 'short'
        }).slice(0, 2));
    return(// role="group" (not "application"): these are plain buttons — hijacking
    // the screen reader's own key handling would take away navigation and
    // give nothing back.
    /*#__PURE__*/ jsxs(Box, {
        padding: 2,
        width: "100%",
        role: "group",
        "aria-label": formatMessage({
            id: getTranslationKey('list.filters.calendar.label'),
            defaultMessage: 'Select date range'
        }),
        "data-testid": "date-range-calendar",
        children: [
            /*#__PURE__*/ jsxs(Flex, {
                justifyContent: "space-between",
                alignItems: "center",
                paddingBottom: 2,
                children: [
                    /*#__PURE__*/ jsx(IconButton, {
                        variant: "ghost",
                        label: formatMessage({
                            id: getTranslationKey('list.filters.calendar.previous-month'),
                            defaultMessage: 'Previous month'
                        }),
                        onClick: ()=>moveMonth(-1),
                        children: /*#__PURE__*/ jsx(ChevronLeft, {})
                    }),
                    /*#__PURE__*/ jsx(Typography, {
                        fontWeight: "semiBold",
                        textColor: "neutral800",
                        children: formatDate(new Date(viewYear, viewMonth, 1), {
                            month: 'long',
                            year: 'numeric'
                        })
                    }),
                    /*#__PURE__*/ jsx(IconButton, {
                        variant: "ghost",
                        label: formatMessage({
                            id: getTranslationKey('list.filters.calendar.next-month'),
                            defaultMessage: 'Next month'
                        }),
                        onClick: ()=>moveMonth(1),
                        children: /*#__PURE__*/ jsx(ChevronRight, {})
                    })
                ]
            }),
            /*#__PURE__*/ jsx(Flex, {
                children: weekDayLabels.map((label, index)=>/*#__PURE__*/ jsx(Flex, {
                        width: "3rem",
                        height: "2.4rem",
                        justifyContent: "center",
                        children: /*#__PURE__*/ jsx(Typography, {
                            variant: "pi",
                            fontWeight: "semiBold",
                            textColor: "neutral600",
                            children: label
                        })
                    }, index))
            }),
            weeks.map((week, weekIndex)=>/*#__PURE__*/ jsx(Flex, {
                    children: week.map((day)=>{
                        const key = toKey(day);
                        const isEdge = key === rangeStart || key === rangeEnd;
                        const inRange = rangeStart !== null && rangeEnd !== null && key > rangeStart && key < rangeEnd;
                        return /*#__PURE__*/ jsxs(DayCell, {
                            type: "button",
                            $isEdge: isEdge,
                            $inRange: inRange,
                            $isMuted: day.getMonth() !== viewMonth,
                            onClick: ()=>handleDayClick(day),
                            children: [
                                /*#__PURE__*/ jsx(VisuallyHidden, {
                                    children: formatDate(day, {
                                        dateStyle: 'long'
                                    })
                                }),
                                /*#__PURE__*/ jsx("span", {
                                    "aria-hidden": true,
                                    children: day.getDate()
                                })
                            ]
                        }, key);
                    })
                }, weekIndex))
        ]
    }));
};

export { DateRangeCalendar };
//# sourceMappingURL=DateRangeCalendar.mjs.map
