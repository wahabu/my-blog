import {
  useControllableState
} from "./chunk-XNMTWOF7.js";
import {
  MemoizedInputRenderer
} from "./chunk-SOVNMIKL.js";
import {
  Form
} from "./chunk-2IWTCEYS.js";
import {
  deepEncodeQueryValues,
  useQueryParams,
  withEncodedUserParams
} from "./chunk-HHY6TNZ5.js";
import {
  createContext
} from "./chunk-ZISMY363.js";
import {
  require_lib
} from "./chunk-NZZAX2AB.js";
import {
  useIntl
} from "./chunk-PQFM6YJZ.js";
import {
  Link
} from "./chunk-QBM2MGBP.js";
import {
  A,
  C,
  Ct,
  Gd,
  Kd,
  Qd,
  S,
  Xd,
  Yd,
  Zl,
  cr,
  dr,
  qd
} from "./chunk-7ZODKSNG.js";
import {
  F5,
  Y5,
  en
} from "./chunk-D4B4CG5U.js";
import {
  require_jsx_runtime
} from "./chunk-ER74Z7BL.js";
import {
  require_react
} from "./chunk-5VGG4VTU.js";
import {
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/@strapi/admin/dist/admin/admin/src/components/Filters.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var React = __toESM(require_react(), 1);

// node_modules/@strapi/admin/dist/admin/admin/src/constants/filters.mjs
var BASE_FILTERS = [
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$eq",
      defaultMessage: "is"
    },
    value: "$eq"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$ne",
      defaultMessage: "is not"
    },
    value: "$ne"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$null",
      defaultMessage: "is null"
    },
    value: "$null"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$notNull",
      defaultMessage: "is not null"
    },
    value: "$notNull"
  }
];
var NUMERIC_FILTERS = [
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$gt",
      defaultMessage: "is greater than"
    },
    value: "$gt"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$gte",
      defaultMessage: "is greater than or equal to"
    },
    value: "$gte"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$lt",
      defaultMessage: "is less than"
    },
    value: "$lt"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$lte",
      defaultMessage: "is less than or equal to"
    },
    value: "$lte"
  }
];
var IS_SENSITIVE_FILTERS = [
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$eqi",
      defaultMessage: "is (case insensitive)"
    },
    value: "$eqi"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$nei",
      defaultMessage: "is not (case insensitive)"
    },
    value: "$nei"
  }
];
var CONTAINS_FILTERS = [
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$contains",
      defaultMessage: "contains"
    },
    value: "$contains"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$containsi",
      defaultMessage: "contains (case insensitive)"
    },
    value: "$containsi"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$notContains",
      defaultMessage: "not contains"
    },
    value: "$notContains"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$notContainsi",
      defaultMessage: "not contains (case insensitive)"
    },
    value: "$notContainsi"
  }
];
var STRING_PARSE_FILTERS = [
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$startsWith",
      defaultMessage: "starts with"
    },
    value: "$startsWith"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$startsWithi",
      defaultMessage: "starts with (case insensitive)"
    },
    value: "$startsWithi"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$endsWith",
      defaultMessage: "ends with"
    },
    value: "$endsWith"
  },
  {
    label: {
      id: "components.FilterOptions.FILTER_TYPES.$endsWithi",
      defaultMessage: "ends with (case insensitive)"
    },
    value: "$endsWithi"
  }
];
var FILTERS_WITH_NO_VALUE = [
  "$null",
  "$notNull"
];

// node_modules/@strapi/admin/dist/admin/admin/src/components/Filters.mjs
var [FiltersProvider, useFilters] = createContext("Filters");
var getFilterDetails = (filterEntry, options) => {
  var _a, _b;
  const [name] = Object.keys(filterEntry);
  const option = options.find((o) => o.name === name);
  if (!option) {
    return null;
  }
  const operatorObj = option.type === "relation" ? (_b = filterEntry[name]) == null ? void 0 : _b[((_a = option.mainField) == null ? void 0 : _a.name) ?? "id"] : filterEntry[name];
  if (typeof operatorObj !== "object" || operatorObj === null) {
    return null;
  }
  const [operator] = Object.keys(operatorObj);
  if (!operator) {
    return null;
  }
  return {
    name,
    operator,
    value: operatorObj[operator]
  };
};
var Root = ({ children, disabled = false, onChange, options = [], onOpenChange, open: openProp, defaultOpen, ...restProps }) => {
  const [editingFilter, setEditingFilter] = React.useState(null);
  const handleChange = (data) => {
    if (onChange) {
      onChange(data);
    }
  };
  const [open = false, setOpen] = useControllableState({
    prop: openProp,
    defaultProp: defaultOpen,
    onChange: onOpenChange
  });
  React.useEffect(() => {
    if (!open) {
      setEditingFilter(null);
    }
  }, [
    open
  ]);
  return (0, import_jsx_runtime.jsx)(Xd.Root, {
    open,
    onOpenChange: setOpen,
    ...restProps,
    children: (0, import_jsx_runtime.jsx)(FiltersProvider, {
      setOpen,
      disabled,
      onChange: handleChange,
      options,
      editingFilter,
      setEditingFilter,
      children
    })
  });
};
var Trigger = React.forwardRef(({ label }, forwardedRef) => {
  const { formatMessage } = useIntl();
  const disabled = useFilters("Trigger", ({ disabled: disabled2 }) => disabled2);
  return (0, import_jsx_runtime.jsx)(Xd.Trigger, {
    children: (0, import_jsx_runtime.jsx)(Ct, {
      variant: "tertiary",
      ref: forwardedRef,
      startIcon: (0, import_jsx_runtime.jsx)(Y5, {}),
      size: "S",
      disabled,
      children: label || formatMessage({
        id: "app.utils.filters",
        defaultMessage: "Filters"
      })
    })
  });
});
var PopoverImpl = ({ zIndex }) => {
  const [{ query }, setQuery] = useQueryParams();
  const { formatMessage } = useIntl();
  const options = useFilters("Popover", ({ options: options2 }) => options2);
  const onChange = useFilters("Popover", ({ onChange: onChange2 }) => onChange2);
  const setOpen = useFilters("Popover", ({ setOpen: setOpen2 }) => setOpen2);
  const editingFilter = useFilters("Popover", ({ editingFilter: editingFilter2 }) => editingFilter2);
  const setEditingFilter = useFilters("Popover", ({ setEditingFilter: setEditingFilter2 }) => setEditingFilter2);
  const initialValues = React.useMemo(() => {
    var _a;
    return editingFilter ?? {
      name: (_a = options[0]) == null ? void 0 : _a.name,
      filter: BASE_FILTERS[0].value
    };
  }, [
    editingFilter,
    options
  ]);
  if (options.length === 0) {
    return null;
  }
  const handleSubmit = (data) => {
    var _a, _b;
    const value = FILTERS_WITH_NO_VALUE.includes(data.filter) ? "true" : data.value ?? "";
    if (!value) {
      return;
    }
    if (onChange) {
      onChange(data);
    }
    const fieldOptions = options.find((filter) => filter.name === data.name);
    const operatorValuePairing = {
      [data.filter]: value
    };
    const newFilterEntry = {
      [data.name]: fieldOptions.type === "relation" ? {
        [((_a = fieldOptions.mainField) == null ? void 0 : _a.name) ?? "id"]: operatorValuePairing
      } : operatorValuePairing
    };
    const existingFilters = ((_b = query.filters) == null ? void 0 : _b.$and) ?? [];
    const newFilterQuery = editingFilter ? {
      ...query.filters,
      $and: existingFilters.map((filter, i) => i === editingFilter.index ? newFilterEntry : filter)
    } : {
      ...query.filters,
      $and: [
        ...existingFilters,
        newFilterEntry
      ]
    };
    setQuery(withEncodedUserParams(query, {
      filters: deepEncodeQueryValues(newFilterQuery),
      page: 1
    }), "push", true);
    setOpen(false);
    setEditingFilter(null);
  };
  return (0, import_jsx_runtime.jsx)(Xd.Content, {
    style: {
      zIndex
    },
    children: (0, import_jsx_runtime.jsx)(S, {
      padding: 3,
      children: (0, import_jsx_runtime.jsx)(Form, {
        method: "POST",
        initialValues,
        onSubmit: handleSubmit,
        children: ({ values: formValues, modified, isSubmitting }) => {
          var _a;
          const filter = options.find((filter2) => filter2.name === formValues.name);
          const Input = (filter == null ? void 0 : filter.input) || MemoizedInputRenderer;
          return (0, import_jsx_runtime.jsxs)(C, {
            direction: "column",
            alignItems: "stretch",
            gap: 2,
            style: {
              minWidth: 184
            },
            children: [
              [
                {
                  ["aria-label"]: formatMessage({
                    id: "app.utils.select-field",
                    defaultMessage: "Select field"
                  }),
                  name: "name",
                  options: options.map((filter2) => ({
                    label: filter2.label,
                    value: filter2.name
                  })),
                  placholder: formatMessage({
                    id: "app.utils.select-field",
                    defaultMessage: "Select field"
                  }),
                  type: "enumeration"
                },
                {
                  ["aria-label"]: formatMessage({
                    id: "app.utils.select-filter",
                    defaultMessage: "Select filter"
                  }),
                  name: "filter",
                  options: (filter == null ? void 0 : filter.operators) || getFilterList(filter).map((opt) => ({
                    label: formatMessage(opt.label),
                    value: opt.value
                  })),
                  placeholder: formatMessage({
                    id: "app.utils.select-filter",
                    defaultMessage: "Select filter"
                  }),
                  type: "enumeration"
                }
              ].map((field) => (0, import_jsx_runtime.jsx)(MemoizedInputRenderer, {
                ...field
              }, field.name)),
              filter && formValues.filter && !FILTERS_WITH_NO_VALUE.includes(formValues.filter) ? (0, import_jsx_runtime.jsx)(Input, {
                ...filter,
                label: null,
                "aria-label": filter.label,
                name: "value",
                // @ts-expect-error – if type is `custom` then `Input` will be a custom component.
                type: ((_a = filter.mainField) == null ? void 0 : _a.type) ?? filter.type
              }) : null,
              (0, import_jsx_runtime.jsx)(Ct, {
                disabled: !modified || isSubmitting,
                size: "L",
                variant: "secondary",
                startIcon: (0, import_jsx_runtime.jsx)(en, {}),
                type: "submit",
                fullWidth: true,
                children: editingFilter ? formatMessage({
                  id: "app.utils.update-filter",
                  defaultMessage: "Update filter"
                }) : formatMessage({
                  id: "app.utils.add-filter",
                  defaultMessage: "Add filter"
                })
              })
            ]
          });
        }
      }, editingFilter ? `edit-${editingFilter.index}-${editingFilter.name}-${editingFilter.filter}-${editingFilter.value ?? "empty"}` : "create")
    })
  });
};
var getFilterList = (filter) => {
  var _a;
  if (!filter) {
    return [];
  }
  const type = ((_a = filter.mainField) == null ? void 0 : _a.type) ? filter.mainField.type : filter.type;
  switch (type) {
    case "email":
    case "text":
    case "string": {
      return [
        ...BASE_FILTERS,
        ...IS_SENSITIVE_FILTERS,
        ...CONTAINS_FILTERS,
        ...STRING_PARSE_FILTERS
      ];
    }
    case "float":
    case "integer":
    case "biginteger":
    case "decimal": {
      return [
        ...BASE_FILTERS,
        ...NUMERIC_FILTERS
      ];
    }
    case "time":
    case "date": {
      return [
        ...BASE_FILTERS,
        ...NUMERIC_FILTERS,
        ...CONTAINS_FILTERS
      ];
    }
    case "datetime": {
      return [
        ...BASE_FILTERS,
        ...NUMERIC_FILTERS
      ];
    }
    case "enumeration": {
      return BASE_FILTERS;
    }
    default:
      return [
        ...BASE_FILTERS,
        ...IS_SENSITIVE_FILTERS
      ];
  }
};
var List = () => {
  var _a, _b, _c, _d;
  const [{ query }, setQuery] = useQueryParams();
  const options = useFilters("List", ({ options: options2 }) => options2);
  const handleRemove = (index) => {
    var _a2;
    const nextFilters = (((_a2 = query == null ? void 0 : query.filters) == null ? void 0 : _a2.$and) ?? []).filter((_, i) => i !== index);
    setQuery(withEncodedUserParams(query, {
      filters: deepEncodeQueryValues({
        $and: nextFilters
      }),
      page: 1
    }));
  };
  if (!((_b = (_a = query == null ? void 0 : query.filters) == null ? void 0 : _a.$and) == null ? void 0 : _b.length)) {
    return null;
  }
  return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {
    children: (_d = (_c = query == null ? void 0 : query.filters) == null ? void 0 : _c.$and) == null ? void 0 : _d.map((queryFilter, index) => {
      const details = getFilterDetails(queryFilter, options);
      if (!details || typeof details.value === "object") {
        return null;
      }
      const filter = options.find(({ name }) => name === details.name);
      if (!filter) {
        return null;
      }
      return (0, import_jsx_runtime.jsx)(AttributeTag, {
        ...filter,
        index,
        onRemove: handleRemove,
        operator: details.operator,
        value: String(details.value)
      }, `${index}-${details.name}-${details.operator}-${details.value}`);
    })
  });
};
var AttributeTag = ({ index, input, label, mainField, name, onRemove, operator, options, value, ...filter }) => {
  const { formatMessage, formatDate, formatTime, formatNumber } = useIntl();
  const setOpen = useFilters("AttributeTag", ({ setOpen: setOpen2 }) => setOpen2);
  const setEditingFilter = useFilters("AttributeTag", ({ setEditingFilter: setEditingFilter2 }) => setEditingFilter2);
  const handleEdit = () => {
    setEditingFilter({
      name,
      filter: operator,
      index,
      value: FILTERS_WITH_NO_VALUE.includes(operator) ? void 0 : value
    });
    setOpen(true);
  };
  const handleRemove = () => {
    onRemove(index);
  };
  const type = (mainField == null ? void 0 : mainField.type) ? mainField.type : filter.type;
  let formattedValue = value;
  switch (type) {
    case "date":
      formattedValue = formatDate(value, {
        dateStyle: "full"
      });
      break;
    case "datetime":
      formattedValue = formatDate(value, {
        dateStyle: "full",
        timeStyle: "short"
      });
      break;
    case "time":
      const [hour, minute] = value.split(":");
      const date = /* @__PURE__ */ new Date();
      date.setHours(Number(hour));
      date.setMinutes(Number(minute));
      formattedValue = formatTime(date, {
        hour: "numeric",
        minute: "numeric"
      });
      break;
    case "float":
    case "integer":
    case "biginteger":
    case "decimal":
      formattedValue = formatNumber(Number(value));
      break;
  }
  if (input && options) {
    const selectedOption = options.find((option) => {
      return (typeof option === "string" ? option : option.value) === value;
    });
    formattedValue = selectedOption ? typeof selectedOption === "string" ? selectedOption : selectedOption.label ?? selectedOption.value : value;
  }
  const operatorLabel = formatMessage({
    id: `components.FilterOptions.FILTER_TYPES.${operator}`,
    defaultMessage: operator
  });
  const content = FILTERS_WITH_NO_VALUE.includes(operator) ? `${label} ${operatorLabel}` : `${label} ${operatorLabel} ${formattedValue}`;
  return (0, import_jsx_runtime.jsx)(Zl, {
    padding: 1,
    onClick: handleRemove,
    icon: (0, import_jsx_runtime.jsx)(F5, {}),
    label: content,
    children: (0, import_jsx_runtime.jsx)(S, {
      tag: "span",
      cursor: "pointer",
      onClick: handleEdit,
      children: content
    })
  });
};
var Filters = {
  List,
  Popover: PopoverImpl,
  Root,
  Trigger
};

// node_modules/@strapi/admin/dist/admin/admin/src/components/Pagination.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var React2 = __toESM(require_react(), 1);
var import_qs = __toESM(require_lib(), 1);
var [PaginationProvider, usePagination] = createContext("Pagination");
var Root2 = React2.forwardRef(({ children, defaultPageSize = 10, pageCount = 0, defaultPage = 1, onPageSizeChange, total = 0 }, forwardedRef) => {
  const [{ query }, setQuery] = useQueryParams({
    pageSize: defaultPageSize.toString(),
    page: defaultPage.toString()
  });
  const setPageSize = (pageSize) => {
    setQuery(withEncodedUserParams(query, {
      pageSize,
      page: "1"
    }));
    if (onPageSizeChange) {
      onPageSizeChange(pageSize);
    }
  };
  return (0, import_jsx_runtime2.jsx)(C, {
    ref: forwardedRef,
    paddingTop: 4,
    paddingBottom: 4,
    alignItems: "flex-end",
    justifyContent: "space-between",
    children: (0, import_jsx_runtime2.jsx)(PaginationProvider, {
      currentQuery: query,
      page: query.page,
      pageSize: query.pageSize,
      pageCount: pageCount.toString(),
      setPageSize,
      total,
      children
    })
  });
});
var PageSize = ({ options = [
  "10",
  "20",
  "50",
  "100"
] }) => {
  const { formatMessage } = useIntl();
  const pageSize = usePagination("PageSize", (state) => state.pageSize);
  const totalCount = usePagination("PageSize", (state) => state.total);
  const setPageSize = usePagination("PageSize", (state) => state.setPageSize);
  const handleChange = (value) => {
    setPageSize(value);
  };
  const minimumOption = parseInt(options[0], 10);
  if (minimumOption >= totalCount) {
    return null;
  }
  return (0, import_jsx_runtime2.jsxs)(C, {
    gap: 2,
    children: [
      (0, import_jsx_runtime2.jsx)(cr, {
        size: "S",
        "aria-label": formatMessage({
          id: "components.PageFooter.select",
          defaultMessage: "Entries per page"
        }),
        // @ts-expect-error from the DS V2 this won't be needed because we're only returning strings.
        onChange: handleChange,
        value: pageSize,
        children: options.map((option) => (0, import_jsx_runtime2.jsx)(dr, {
          value: option,
          children: option
        }, option))
      }),
      (0, import_jsx_runtime2.jsx)(A, {
        textColor: "neutral600",
        tag: "span",
        children: formatMessage({
          id: "components.PageFooter.select",
          defaultMessage: "Entries per page"
        })
      })
    ]
  });
};
var Links = ({ boundaryCount = 1, siblingCount = 1 }) => {
  const { formatMessage } = useIntl();
  const query = usePagination("Links", (state) => state.currentQuery);
  const currentPage = usePagination("Links", (state) => state.page);
  const totalPages = usePagination("Links", (state) => state.pageCount);
  const pageCount = parseInt(totalPages, 10);
  const activePage = parseInt(currentPage, 10);
  const range = (start, end) => {
    const length = end - start + 1;
    return Array.from({
      length
    }, (_, i) => start + i);
  };
  const startPages = range(1, Math.min(boundaryCount, pageCount));
  const endPages = range(Math.max(pageCount - boundaryCount + 1, boundaryCount + 1), pageCount);
  const siblingsStart = Math.max(
    Math.min(
      // Natural start
      activePage - siblingCount,
      // Lower boundary when page is high
      pageCount - boundaryCount - siblingCount * 2 - 1
    ),
    // Greater than startPages
    boundaryCount + 2
  );
  const siblingsEnd = Math.min(
    Math.max(
      // Natural end
      activePage + siblingCount,
      // Upper boundary when page is low
      boundaryCount + siblingCount * 2 + 2
    ),
    // Less than endPages
    endPages.length > 0 ? endPages[0] - 2 : pageCount - 1
  );
  const items = [
    ...startPages,
    // Start ellipsis
    // eslint-disable-next-line no-nested-ternary
    ...siblingsStart > boundaryCount + 2 ? [
      "start-ellipsis"
    ] : boundaryCount + 1 < pageCount - boundaryCount ? [
      boundaryCount + 1
    ] : [],
    // Sibling pages
    ...range(siblingsStart, siblingsEnd),
    // End ellipsis
    // eslint-disable-next-line no-nested-ternary
    ...siblingsEnd < pageCount - boundaryCount - 1 ? [
      "end-ellipsis"
    ] : pageCount - boundaryCount > boundaryCount ? [
      pageCount - boundaryCount
    ] : [],
    ...endPages
  ];
  if (pageCount <= 1) {
    return null;
  }
  return (0, import_jsx_runtime2.jsxs)(Gd, {
    activePage,
    pageCount,
    children: [
      (0, import_jsx_runtime2.jsx)(qd, {
        tag: Link,
        to: {
          search: (0, import_qs.stringify)({
            ...query,
            page: activePage - 1
          })
        },
        children: formatMessage({
          id: "components.pagination.go-to-previous",
          defaultMessage: "Go to previous page"
        })
      }),
      items.map((item) => {
        if (typeof item === "number") {
          return (0, import_jsx_runtime2.jsx)(Yd, {
            tag: Link,
            number: item,
            to: {
              search: (0, import_qs.stringify)({
                ...query,
                page: item
              })
            },
            children: formatMessage({
              id: "components.pagination.go-to",
              defaultMessage: "Go to page {page}"
            }, {
              page: item
            })
          }, item);
        }
        return (0, import_jsx_runtime2.jsx)(Qd, {}, item);
      }),
      (0, import_jsx_runtime2.jsx)(Kd, {
        tag: Link,
        to: {
          search: (0, import_qs.stringify)({
            ...query,
            page: activePage + 1
          })
        },
        children: formatMessage({
          id: "components.pagination.go-to-next",
          defaultMessage: "Go to next page"
        })
      })
    ]
  });
};
var Pagination = {
  Root: Root2,
  Links,
  PageSize
};

export {
  Filters,
  Pagination
};
//# sourceMappingURL=chunk-YZKIGY5W.js.map
