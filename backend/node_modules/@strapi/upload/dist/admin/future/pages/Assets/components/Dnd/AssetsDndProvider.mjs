import { jsx, jsxs } from 'react/jsx-runtime';
import { useState, useRef, useCallback, useMemo, createContext, useContext } from 'react';
import { useSensors, useSensor, PointerSensor, DndContext, pointerWithin, DragOverlay } from '@dnd-kit/core';
import { useNotification } from '@strapi/admin/strapi-admin';
import { VisuallyHidden, Flex } from '@strapi/design-system';
import { useIntl } from 'react-intl';
import { useApiErrorMessage } from '../../../../hooks/useApiErrorMessage.mjs';
import { useMediaLibraryPermissions } from '../../../../hooks/useMediaLibraryPermissions.mjs';
import { useGetFolderStructureQuery, useBulkMoveMutation } from '../../../../services/folders.mjs';
import { buildBulkMovePayload } from '../../../../utils/buildBulkMovePayload.mjs';
import { canDropItemOnFolder } from '../../../../utils/canDropItemOnFolder.mjs';
import { formatMoveSuccessMessage } from '../../../../utils/formatMoveSuccessMessage.mjs';
import { getFolderLabel } from '../../../../utils/getFolderLabel.mjs';
import { emptyItemLocations } from '../../../../utils/itemLocations.mjs';
import { getTranslationKey } from '../../../../utils/translations.mjs';
import { useAssetSelectionOptional } from '../../hooks/useAssetSelection.mjs';
import { useFolderNavigation } from '../../hooks/useFolderNavigation.mjs';
import { buildDragSet } from './buildDragSet.mjs';
import { computeValidDropTargets } from './computeValidDropTargets.mjs';
import { parseFolderTargetId, parseFolderTreeTargetId } from './dndIds.mjs';
import { DragOverlayChip } from './DragOverlayChip.mjs';

const AssetsDndContext = /*#__PURE__*/ createContext(null);
const useAssetsDndOptional = ()=>useContext(AssetsDndContext);
const resolveDestination = (overId)=>{
    const inView = parseFolderTargetId(overId);
    if (inView != null) {
        return {
            destinationFolderId: inView
        };
    }
    const tree = parseFolderTreeTargetId(overId);
    if (tree === 'root') {
        return {
            destinationFolderId: null
        };
    }
    if (typeof tree === 'number') {
        return {
            destinationFolderId: tree
        };
    }
    return null;
};
// Activation distance no real pointer gesture will ever reach — used to keep a
// constant-length sensor array while disabling drag for users without the move
// permission (see the note in the provider).
const DRAG_DISABLED_DISTANCE = Number.MAX_SAFE_INTEGER;
const AssetsDndProvider = ({ children, locations = emptyItemLocations })=>{
    const { formatMessage } = useIntl();
    const getErrorMessage = useApiErrorMessage();
    const { toggleNotification } = useNotification();
    const selection = useAssetSelectionOptional();
    const { currentFolderId } = useFolderNavigation();
    const { data: folderStructure = [] } = useGetFolderStructureQuery();
    const rootLabel = formatMessage({
        id: getTranslationKey('plugin.name'),
        defaultMessage: 'Media Library'
    });
    const [bulkMove, { isLoading: isMovePending }] = useBulkMoveMutation();
    const [dragItems, setDragItems] = useState([]);
    const [liveAnnouncement, setLiveAnnouncement] = useState('');
    // Sync session for drag-end — state alone can lag one render behind dragStart.
    const dragSessionRef = useRef({
        items: [],
        fromSelection: false,
        activeSourceFolderId: null,
        spansMultipleSources: false
    });
    const announceToLiveRegion = useCallback((message)=>{
        setLiveAnnouncement('');
        requestAnimationFrame(()=>setLiveAnnouncement(message));
    }, []);
    // Moving is a mutation (bulk-move → `assets.update`); without the permission
    // pointer drag must never activate. Keyboard moves go through BulkMoveDialog,
    // which is itself gated.
    //
    // The gate has to keep BOTH the `useSensor`/`useSensors` calls AND the sensor
    // array's length constant across renders: `canUpdate` starts false and flips
    // true once the RBAC check resolves, and dnd-kit keys internal memos/effects
    // on the sensor list, erroring if its size changes ("argument changed size
    // between renders"). So adding/removing a sensor is out — instead keep one
    // PointerSensor always and push its activation distance out of reach when the
    // user can't move, which no real drag gesture will ever satisfy.
    const { canUpdate } = useMediaLibraryPermissions();
    const sensors = useSensors(useSensor(PointerSensor, {
        activationConstraint: {
            distance: canUpdate ? 8 : DRAG_DISABLED_DISTANCE
        }
    }));
    const validDropTargets = useMemo(()=>computeValidDropTargets(dragItems, folderStructure), [
        dragItems,
        folderStructure
    ]);
    const isValidDropTarget = useCallback((targetFolderId)=>validDropTargets.has(targetFolderId), [
        validDropTargets
    ]);
    const contextValue = useMemo(()=>({
            isInternalDragActive: dragItems.length > 0,
            isMovePending,
            isValidDropTarget
        }), [
        dragItems.length,
        isMovePending,
        isValidDropTarget
    ]);
    const clearDragState = useCallback(()=>{
        dragSessionRef.current = {
            items: [],
            fromSelection: false,
            activeSourceFolderId: null,
            spansMultipleSources: false
        };
        setDragItems([]);
    }, []);
    const handleDragStart = useCallback((event)=>{
        const data = event.active.data.current;
        if (!data) {
            clearDragState();
            return;
        }
        const dragSet = buildDragSet(data, selection?.selectedKeys, locations, currentFolderId);
        dragSessionRef.current = dragSet;
        setDragItems(dragSet.items);
    }, [
        clearDragState,
        currentFolderId,
        locations,
        selection?.selectedKeys
    ]);
    const handleDragEnd = useCallback(async (event)=>{
        const { over } = event;
        const { items, fromSelection, activeSourceFolderId, spansMultipleSources } = dragSessionRef.current;
        clearDragState();
        if (isMovePending || !over || items.length === 0) {
            return;
        }
        const destination = resolveDestination(over.id);
        if (!destination) {
            return;
        }
        const { destinationFolderId: targetFolderId } = destination;
        if (!canDropItemOnFolder({
            items,
            targetFolderId,
            folderStructure
        })) {
            return;
        }
        const payload = buildBulkMovePayload(items);
        // Source is the grabbed row's own location, not the folder currently open:
        // a global search result can be dragged out of a folder the user isn't in.
        // A search-built selection can also span folders, and then no single
        // source is true for the whole set — `null` drops it from the wording
        // instead of naming one item's folder for all of them. Destination is the
        // drop target. Both use leaf names so the DnD toast reads identically to
        // the dialog's.
        const successMessage = formatMoveSuccessMessage({
            formatMessage,
            count: items.length,
            source: spansMultipleSources ? null : getFolderLabel(folderStructure, activeSourceFolderId, rootLabel),
            destination: getFolderLabel(folderStructure, targetFolderId, rootLabel)
        });
        const errorFallback = formatMessage({
            id: getTranslationKey('list.bulk-actions.move.error'),
            defaultMessage: 'An error occurred while moving the items.'
        });
        try {
            await bulkMove({
                ...payload,
                destinationFolderId: targetFolderId
            }).unwrap();
            if (fromSelection) {
                selection?.clear();
            }
            announceToLiveRegion(successMessage);
            toggleNotification({
                type: 'success',
                message: successMessage
            });
        } catch (error) {
            const errorMessage = getErrorMessage(error, errorFallback);
            announceToLiveRegion(formatMessage({
                id: getTranslationKey('dnd.announce.move-failure'),
                defaultMessage: 'Move failed. {message}'
            }, {
                message: errorMessage
            }));
            toggleNotification({
                type: 'danger',
                message: errorMessage
            });
        }
    }, [
        announceToLiveRegion,
        bulkMove,
        clearDragState,
        folderStructure,
        formatMessage,
        getErrorMessage,
        isMovePending,
        rootLabel,
        selection,
        toggleNotification
    ]);
    const handleDragCancel = useCallback(()=>{
        clearDragState();
    }, [
        clearDragState
    ]);
    const announcements = useMemo(()=>({
            onDragStart: ({ active })=>{
                const data = active.data.current;
                return data ? formatMessage({
                    id: getTranslationKey('dnd.announce.drag-start'),
                    defaultMessage: 'Picked up {name}. Drop on a folder to move.'
                }, {
                    name: data.name
                }) : '';
            },
            onDragOver: ()=>'',
            onDragEnd: ({ active, over })=>{
                if (!over) {
                    return formatMessage({
                        id: getTranslationKey('dnd.announce.cancel'),
                        defaultMessage: 'Drag cancelled.'
                    });
                }
                const destination = resolveDestination(over.id);
                const activeData = active.data.current;
                if (!destination || !activeData) {
                    return '';
                }
                const { items } = buildDragSet(activeData, selection?.selectedKeys, locations, currentFolderId);
                if (!canDropItemOnFolder({
                    items,
                    targetFolderId: destination.destinationFolderId,
                    folderStructure
                })) {
                    return formatMessage({
                        id: getTranslationKey('dnd.announce.invalid-drop'),
                        defaultMessage: 'Cannot move item to this folder.'
                    });
                }
                return '';
            },
            onDragCancel: ()=>formatMessage({
                    id: getTranslationKey('dnd.announce.cancel'),
                    defaultMessage: 'Drag cancelled.'
                })
        }), [
        currentFolderId,
        folderStructure,
        formatMessage,
        locations,
        selection?.selectedKeys
    ]);
    return /*#__PURE__*/ jsx(AssetsDndContext.Provider, {
        value: contextValue,
        children: /*#__PURE__*/ jsxs(DndContext, {
            sensors: sensors,
            collisionDetection: pointerWithin,
            onDragStart: handleDragStart,
            onDragEnd: handleDragEnd,
            onDragCancel: handleDragCancel,
            accessibility: {
                announcements
            },
            children: [
                /*#__PURE__*/ jsx(VisuallyHidden, {
                    "aria-live": "polite",
                    "aria-atomic": "true",
                    children: liveAnnouncement
                }),
                /*#__PURE__*/ jsx(Flex, {
                    position: "relative",
                    alignItems: "stretch",
                    direction: "column",
                    height: "100%",
                    children: children
                }),
                /*#__PURE__*/ jsx(DragOverlay, {
                    dropAnimation: null,
                    children: dragItems.length > 0 ? /*#__PURE__*/ jsx(DragOverlayChip, {
                        items: dragItems
                    }) : null
                })
            ]
        })
    });
}; // Keyboard-accessible moves are handled by `BulkMoveDialog` (reachable from the
 // bulk-actions bar): pointer drag is a mouse-only enhancement, the dialog is the
 // a11y path. No `KeyboardSensor` by design.

export { AssetsDndProvider, useAssetsDndOptional };
//# sourceMappingURL=AssetsDndProvider.mjs.map
